/* ── data: URI fetch shim ─────────────────────────────────────────────────
   MUST run before the library loads. <cs-icon> resolves every icon to a URL and
   then fetch()es it — including the data: URIs the library itself uses for the
   component chrome (chevrons, checkmarks, close buttons) in its `system` library.
   A page served with a Content-Security-Policy whose connect-src does not list
   data: refuses that connection, the error is swallowed, and EVERY icon renders
   as an empty box. This design system is served under such a policy.

   The shim answers data: URIs locally instead of letting them reach fetch(), so no
   connection is attempted and the policy is never involved. Every other URL is
   passed straight through to the real fetch. Nothing is bypassed or weakened: a
   data: URI is already the fully-inlined payload.

   Upstream fix would be for resolveIcon() to return the SVG directly when the
   resolver hands back a data: URI, rather than round-tripping it through fetch. */
(function () {
  if (typeof window === "undefined" || typeof window.fetch !== "function") return;
  var real = window.fetch;
  window.fetch = function (input, init) {
    var url = typeof input === "string" ? input : (input && input.url);
    if (typeof url === "string" && url.lastIndexOf("data:", 0) === 0) {
      try {
        var comma = url.indexOf(",");
        var meta = url.slice(5, comma);
        var payload = url.slice(comma + 1);
        var body = /;base64/i.test(meta) ? atob(payload) : decodeURIComponent(payload);
        var type = meta.split(";")[0] || "text/plain";
        return Promise.resolve(new Response(body, { status: 200, headers: { "Content-Type": type } }));
      } catch (e) {
        return Promise.reject(e);
      }
    }
    return real.apply(this, arguments);
  };
})();

var Cornerstone = (() => {
  var __defProp = Object.defineProperty;
  var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
  var __getOwnPropNames = Object.getOwnPropertyNames;
  var __hasOwnProp = Object.prototype.hasOwnProperty;
  var __export = (target, all) => {
    for (var name in all)
      __defProp(target, name, { get: all[name], enumerable: true });
  };
  var __copyProps = (to, from, except, desc) => {
    if (from && typeof from === "object" || typeof from === "function") {
      for (let key of __getOwnPropNames(from))
        if (!__hasOwnProp.call(to, key) && key !== except)
          __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
    }
    return to;
  };
  var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

  // entry/index.js
  var index_exports = {};
  __export(index_exports, {
    allDefined: () => allDefined,
    discover: () => discover,
    getAnimationNames: () => getAnimationNames,
    getBasePath: () => getBasePath,
    getDefaultIconFamily: () => getDefaultIconFamily,
    getEasingNames: () => getEasingNames,
    getIconFileName: () => getIconFileName,
    getIconPath: () => getIconPath,
    getIconStyle: () => getIconStyle,
    getIconWeight: () => getIconWeight,
    preventTurboFouce: () => preventTurboFouce,
    registerIconLibrary: () => registerIconLibrary,
    registerTranslation: () => registerTranslation,
    serialize: () => serialize,
    setBasePath: () => setBasePath,
    setDefaultIconFamily: () => setDefaultIconFamily,
    setIconPath: () => setIconPath,
    startLoader: () => startLoader,
    stopLoader: () => stopLoader,
    unregisterIconLibrary: () => unregisterIconLibrary
  });

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.WI4KFPQA.js
  var import_meta = {};
  var basePath = "";
  var iconPath = "";
  function setBasePath(path) {
    basePath = path;
  }
  function packageRoot() {
    return new URL("../", import_meta.url).href.replace(/\/$/, "");
  }
  function getBasePath(subpath = "") {
    if (!basePath) {
      const el = document.querySelector("[data-cornerstone]");
      const attribute = el?.getAttribute("data-cornerstone");
      if (attribute) {
        setBasePath(new URL(attribute, window.location.href).href.replace(/\/$/, ""));
      } else {
        setBasePath(packageRoot());
      }
    }
    return basePath.replace(/\/$/, "") + (subpath ? `/${subpath.replace(/^\//, "")}` : ``);
  }
  function setIconPath(path) {
    iconPath = path;
  }
  function getIconPath() {
    return iconPath.replace(/\/$/, "");
  }

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.O637MP3W.js
  var observer = new MutationObserver((mutations) => {
    for (const { addedNodes } of mutations) {
      for (const node of addedNodes) {
        if (node.nodeType === Node.ELEMENT_NODE) {
          discover(node);
        }
      }
    }
  });
  function startLoader() {
    discover(document);
    observer.observe(document.documentElement, { subtree: true, childList: true });
  }
  function stopLoader() {
    observer.disconnect();
  }
  async function discover(root) {
    const rootTagName = root instanceof Element ? root.tagName.toLowerCase() : "";
    const rootIsCornerstoneComponent = rootTagName.startsWith("cs-");
    const tags = [...root.querySelectorAll(":not(:defined)")].map((el) => el.tagName.toLowerCase()).filter((tag2) => tag2.startsWith("cs-"));
    if (rootIsCornerstoneComponent && !customElements.get(rootTagName)) {
      tags.push(rootTagName);
    }
    const preloadSelectors = root.querySelectorAll("[data-cs-preload]");
    const preloadRoots = root instanceof Element && root.hasAttribute("data-cs-preload") ? [root, ...preloadSelectors] : preloadSelectors;
    for (const el of preloadRoots) {
      tags.push(
        ...el.getAttribute("data-cs-preload").split(/\s+/).filter((tag2) => tag2.startsWith("cs-"))
      );
    }
    const tagsToRegister = [...new Set(tags)];
    const imports = await Promise.allSettled(tagsToRegister.map((tagName) => register(tagName)));
    for (const imp of imports) {
      if (imp.status === "rejected") {
        console.error(imp.reason);
      }
    }
    await new Promise(requestAnimationFrame);
    root.dispatchEvent(
      new CustomEvent("cs-discovery-complete", {
        bubbles: false,
        cancelable: false,
        composed: true
      })
    );
  }
  function register(tagName) {
    if (customElements.get(tagName)) {
      return Promise.resolve();
    }
    const tagWithoutPrefix = tagName.replace(/^cs-/i, "");
    const path = getBasePath(`components/${tagWithoutPrefix}/${tagWithoutPrefix}.js`);
    return new Promise((resolve, reject) => {
      import(path).then(() => resolve()).catch(() => reject(new Error(`Unable to autoload <${tagName}> from ${path}`)));
    });
  }
  var _timeout = 2e3;
  function preventTurboFouce(timeout = 2e3) {
    _timeout = timeout;
    document.addEventListener("turbo:before-render", handleRender);
  }
  async function handleRender(event) {
    const newBody = event.detail.newBody;
    event.preventDefault();
    try {
      await Promise.race([discover(newBody), new Promise((resolve) => setTimeout(resolve, _timeout))]);
    } finally {
      event.detail.resume();
    }
  }

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.PWDONGWF.js
  async function allDefined(options2) {
    const opts = {
      match: (tagName) => tagName.startsWith("cs-"),
      additionalElements: [],
      root: document,
      ...options2
    };
    const additionalElements = Array.isArray(opts.additionalElements) ? opts.additionalElements : [opts.additionalElements];
    const undefinedElements = [...opts.root.querySelectorAll(":not(:defined)")].map((el) => el.localName).filter((tag2, index, arr) => arr.indexOf(tag2) === index).filter((tag2) => opts.match(tag2));
    const tagsToAwait = [...undefinedElements, ...additionalElements];
    await Promise.all(tagsToAwait.map((tag2) => customElements.whenDefined(tag2)));
    await new Promise(requestAnimationFrame);
  }

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.WHPMQ7KH.js
  function serialize(form) {
    const formData = new FormData(form);
    const object = {};
    formData.forEach((value, key) => {
      if (Reflect.has(object, key)) {
        const entry = object[key];
        if (Array.isArray(entry)) {
          entry.push(value);
        } else {
          object[key] = [object[key], value];
        }
      } else {
        object[key] = value;
      }
    });
    return object;
  }

  // node_modules/@shoelace-style/animations/dist/index.js
  var dist_exports = {};
  __export(dist_exports, {
    backInDown: () => backInDown,
    backInLeft: () => backInLeft,
    backInRight: () => backInRight,
    backInUp: () => backInUp,
    backOutDown: () => backOutDown,
    backOutLeft: () => backOutLeft,
    backOutRight: () => backOutRight,
    backOutUp: () => backOutUp,
    bounce: () => bounce,
    bounceIn: () => bounceIn,
    bounceInDown: () => bounceInDown,
    bounceInLeft: () => bounceInLeft,
    bounceInRight: () => bounceInRight,
    bounceInUp: () => bounceInUp,
    bounceOut: () => bounceOut,
    bounceOutDown: () => bounceOutDown,
    bounceOutLeft: () => bounceOutLeft,
    bounceOutRight: () => bounceOutRight,
    bounceOutUp: () => bounceOutUp,
    easings: () => easings,
    fadeIn: () => fadeIn,
    fadeInBottomLeft: () => fadeInBottomLeft,
    fadeInBottomRight: () => fadeInBottomRight,
    fadeInDown: () => fadeInDown,
    fadeInDownBig: () => fadeInDownBig,
    fadeInLeft: () => fadeInLeft,
    fadeInLeftBig: () => fadeInLeftBig,
    fadeInRight: () => fadeInRight,
    fadeInRightBig: () => fadeInRightBig,
    fadeInTopLeft: () => fadeInTopLeft,
    fadeInTopRight: () => fadeInTopRight,
    fadeInUp: () => fadeInUp,
    fadeInUpBig: () => fadeInUpBig,
    fadeOut: () => fadeOut,
    fadeOutBottomLeft: () => fadeOutBottomLeft,
    fadeOutBottomRight: () => fadeOutBottomRight,
    fadeOutDown: () => fadeOutDown,
    fadeOutDownBig: () => fadeOutDownBig,
    fadeOutLeft: () => fadeOutLeft,
    fadeOutLeftBig: () => fadeOutLeftBig,
    fadeOutRight: () => fadeOutRight,
    fadeOutRightBig: () => fadeOutRightBig,
    fadeOutTopLeft: () => fadeOutTopLeft,
    fadeOutTopRight: () => fadeOutTopRight,
    fadeOutUp: () => fadeOutUp,
    fadeOutUpBig: () => fadeOutUpBig,
    flash: () => flash,
    flip: () => flip,
    flipInX: () => flipInX,
    flipInY: () => flipInY,
    flipOutX: () => flipOutX,
    flipOutY: () => flipOutY,
    headShake: () => headShake,
    heartBeat: () => heartBeat,
    hinge: () => hinge,
    jackInTheBox: () => jackInTheBox,
    jello: () => jello,
    lightSpeedInLeft: () => lightSpeedInLeft,
    lightSpeedInRight: () => lightSpeedInRight,
    lightSpeedOutLeft: () => lightSpeedOutLeft,
    lightSpeedOutRight: () => lightSpeedOutRight,
    pulse: () => pulse,
    rollIn: () => rollIn,
    rollOut: () => rollOut,
    rotateIn: () => rotateIn,
    rotateInDownLeft: () => rotateInDownLeft,
    rotateInDownRight: () => rotateInDownRight,
    rotateInUpLeft: () => rotateInUpLeft,
    rotateInUpRight: () => rotateInUpRight,
    rotateOut: () => rotateOut,
    rotateOutDownLeft: () => rotateOutDownLeft,
    rotateOutDownRight: () => rotateOutDownRight,
    rotateOutUpLeft: () => rotateOutUpLeft,
    rotateOutUpRight: () => rotateOutUpRight,
    rubberBand: () => rubberBand,
    shake: () => shake,
    shakeX: () => shakeX,
    shakeY: () => shakeY,
    slideInDown: () => slideInDown,
    slideInLeft: () => slideInLeft,
    slideInRight: () => slideInRight,
    slideInUp: () => slideInUp,
    slideOutDown: () => slideOutDown,
    slideOutLeft: () => slideOutLeft,
    slideOutRight: () => slideOutRight,
    slideOutUp: () => slideOutUp,
    swing: () => swing,
    tada: () => tada,
    wobble: () => wobble,
    zoomIn: () => zoomIn,
    zoomInDown: () => zoomInDown,
    zoomInLeft: () => zoomInLeft,
    zoomInRight: () => zoomInRight,
    zoomInUp: () => zoomInUp,
    zoomOut: () => zoomOut,
    zoomOutDown: () => zoomOutDown,
    zoomOutLeft: () => zoomOutLeft,
    zoomOutRight: () => zoomOutRight,
    zoomOutUp: () => zoomOutUp
  });

  // node_modules/@shoelace-style/animations/dist/attention_seekers/bounce.js
  var bounce = [
    { offset: 0, easing: "cubic-bezier(0.215, 0.61, 0.355, 1)", transform: "translate3d(0, 0, 0)" },
    { offset: 0.2, easing: "cubic-bezier(0.215, 0.61, 0.355, 1)", transform: "translate3d(0, 0, 0)" },
    { offset: 0.4, easing: "cubic-bezier(0.755, 0.05, 0.855, 0.06)", transform: "translate3d(0, -30px, 0) scaleY(1.1)" },
    { offset: 0.43, easing: "cubic-bezier(0.755, 0.05, 0.855, 0.06)", transform: "translate3d(0, -30px, 0) scaleY(1.1)" },
    { offset: 0.53, easing: "cubic-bezier(0.215, 0.61, 0.355, 1)", transform: "translate3d(0, 0, 0)" },
    { offset: 0.7, easing: "cubic-bezier(0.755, 0.05, 0.855, 0.06)", transform: "translate3d(0, -15px, 0) scaleY(1.05)" },
    {
      offset: 0.8,
      "transition-timing-function": "cubic-bezier(0.215, 0.61, 0.355, 1)",
      transform: "translate3d(0, 0, 0) scaleY(0.95)"
    },
    { offset: 0.9, transform: "translate3d(0, -4px, 0) scaleY(1.02)" },
    { offset: 1, easing: "cubic-bezier(0.215, 0.61, 0.355, 1)", transform: "translate3d(0, 0, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/attention_seekers/flash.js
  var flash = [
    { offset: 0, opacity: "1" },
    { offset: 0.25, opacity: "0" },
    { offset: 0.5, opacity: "1" },
    { offset: 0.75, opacity: "0" },
    { offset: 1, opacity: "1" }
  ];

  // node_modules/@shoelace-style/animations/dist/attention_seekers/headShake.js
  var headShake = [
    { offset: 0, transform: "translateX(0)" },
    { offset: 0.065, transform: "translateX(-6px) rotateY(-9deg)" },
    { offset: 0.185, transform: "translateX(5px) rotateY(7deg)" },
    { offset: 0.315, transform: "translateX(-3px) rotateY(-5deg)" },
    { offset: 0.435, transform: "translateX(2px) rotateY(3deg)" },
    { offset: 0.5, transform: "translateX(0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/attention_seekers/heartBeat.js
  var heartBeat = [
    { offset: 0, transform: "scale(1)" },
    { offset: 0.14, transform: "scale(1.3)" },
    { offset: 0.28, transform: "scale(1)" },
    { offset: 0.42, transform: "scale(1.3)" },
    { offset: 0.7, transform: "scale(1)" }
  ];

  // node_modules/@shoelace-style/animations/dist/attention_seekers/jello.js
  var jello = [
    { offset: 0, transform: "translate3d(0, 0, 0)" },
    { offset: 0.111, transform: "translate3d(0, 0, 0)" },
    { offset: 0.222, transform: "skewX(-12.5deg) skewY(-12.5deg)" },
    { offset: 0.33299999999999996, transform: "skewX(6.25deg) skewY(6.25deg)" },
    { offset: 0.444, transform: "skewX(-3.125deg) skewY(-3.125deg)" },
    { offset: 0.555, transform: "skewX(1.5625deg) skewY(1.5625deg)" },
    { offset: 0.6659999999999999, transform: "skewX(-0.78125deg) skewY(-0.78125deg)" },
    { offset: 0.777, transform: "skewX(0.390625deg) skewY(0.390625deg)" },
    { offset: 0.888, transform: "skewX(-0.1953125deg) skewY(-0.1953125deg)" },
    { offset: 1, transform: "translate3d(0, 0, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/attention_seekers/pulse.js
  var pulse = [
    { offset: 0, transform: "scale3d(1, 1, 1)" },
    { offset: 0.5, transform: "scale3d(1.05, 1.05, 1.05)" },
    { offset: 1, transform: "scale3d(1, 1, 1)" }
  ];

  // node_modules/@shoelace-style/animations/dist/attention_seekers/rubberBand.js
  var rubberBand = [
    { offset: 0, transform: "scale3d(1, 1, 1)" },
    { offset: 0.3, transform: "scale3d(1.25, 0.75, 1)" },
    { offset: 0.4, transform: "scale3d(0.75, 1.25, 1)" },
    { offset: 0.5, transform: "scale3d(1.15, 0.85, 1)" },
    { offset: 0.65, transform: "scale3d(0.95, 1.05, 1)" },
    { offset: 0.75, transform: "scale3d(1.05, 0.95, 1)" },
    { offset: 1, transform: "scale3d(1, 1, 1)" }
  ];

  // node_modules/@shoelace-style/animations/dist/attention_seekers/shake.js
  var shake = [
    { offset: 0, transform: "translate3d(0, 0, 0)" },
    { offset: 0.1, transform: "translate3d(-10px, 0, 0)" },
    { offset: 0.2, transform: "translate3d(10px, 0, 0)" },
    { offset: 0.3, transform: "translate3d(-10px, 0, 0)" },
    { offset: 0.4, transform: "translate3d(10px, 0, 0)" },
    { offset: 0.5, transform: "translate3d(-10px, 0, 0)" },
    { offset: 0.6, transform: "translate3d(10px, 0, 0)" },
    { offset: 0.7, transform: "translate3d(-10px, 0, 0)" },
    { offset: 0.8, transform: "translate3d(10px, 0, 0)" },
    { offset: 0.9, transform: "translate3d(-10px, 0, 0)" },
    { offset: 1, transform: "translate3d(0, 0, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/attention_seekers/shakeX.js
  var shakeX = [
    { offset: 0, transform: "translate3d(0, 0, 0)" },
    { offset: 0.1, transform: "translate3d(-10px, 0, 0)" },
    { offset: 0.2, transform: "translate3d(10px, 0, 0)" },
    { offset: 0.3, transform: "translate3d(-10px, 0, 0)" },
    { offset: 0.4, transform: "translate3d(10px, 0, 0)" },
    { offset: 0.5, transform: "translate3d(-10px, 0, 0)" },
    { offset: 0.6, transform: "translate3d(10px, 0, 0)" },
    { offset: 0.7, transform: "translate3d(-10px, 0, 0)" },
    { offset: 0.8, transform: "translate3d(10px, 0, 0)" },
    { offset: 0.9, transform: "translate3d(-10px, 0, 0)" },
    { offset: 1, transform: "translate3d(0, 0, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/attention_seekers/shakeY.js
  var shakeY = [
    { offset: 0, transform: "translate3d(0, 0, 0)" },
    { offset: 0.1, transform: "translate3d(0, -10px, 0)" },
    { offset: 0.2, transform: "translate3d(0, 10px, 0)" },
    { offset: 0.3, transform: "translate3d(0, -10px, 0)" },
    { offset: 0.4, transform: "translate3d(0, 10px, 0)" },
    { offset: 0.5, transform: "translate3d(0, -10px, 0)" },
    { offset: 0.6, transform: "translate3d(0, 10px, 0)" },
    { offset: 0.7, transform: "translate3d(0, -10px, 0)" },
    { offset: 0.8, transform: "translate3d(0, 10px, 0)" },
    { offset: 0.9, transform: "translate3d(0, -10px, 0)" },
    { offset: 1, transform: "translate3d(0, 0, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/attention_seekers/swing.js
  var swing = [
    { offset: 0.2, transform: "rotate3d(0, 0, 1, 15deg)" },
    { offset: 0.4, transform: "rotate3d(0, 0, 1, -10deg)" },
    { offset: 0.6, transform: "rotate3d(0, 0, 1, 5deg)" },
    { offset: 0.8, transform: "rotate3d(0, 0, 1, -5deg)" },
    { offset: 1, transform: "rotate3d(0, 0, 1, 0deg)" }
  ];

  // node_modules/@shoelace-style/animations/dist/attention_seekers/tada.js
  var tada = [
    { offset: 0, transform: "scale3d(1, 1, 1)" },
    { offset: 0.1, transform: "scale3d(0.9, 0.9, 0.9) rotate3d(0, 0, 1, -3deg)" },
    { offset: 0.2, transform: "scale3d(0.9, 0.9, 0.9) rotate3d(0, 0, 1, -3deg)" },
    { offset: 0.3, transform: "scale3d(1.1, 1.1, 1.1) rotate3d(0, 0, 1, 3deg)" },
    { offset: 0.4, transform: "scale3d(1.1, 1.1, 1.1) rotate3d(0, 0, 1, -3deg)" },
    { offset: 0.5, transform: "scale3d(1.1, 1.1, 1.1) rotate3d(0, 0, 1, 3deg)" },
    { offset: 0.6, transform: "scale3d(1.1, 1.1, 1.1) rotate3d(0, 0, 1, -3deg)" },
    { offset: 0.7, transform: "scale3d(1.1, 1.1, 1.1) rotate3d(0, 0, 1, 3deg)" },
    { offset: 0.8, transform: "scale3d(1.1, 1.1, 1.1) rotate3d(0, 0, 1, -3deg)" },
    { offset: 0.9, transform: "scale3d(1.1, 1.1, 1.1) rotate3d(0, 0, 1, 3deg)" },
    { offset: 1, transform: "scale3d(1, 1, 1)" }
  ];

  // node_modules/@shoelace-style/animations/dist/attention_seekers/wobble.js
  var wobble = [
    { offset: 0, transform: "translate3d(0, 0, 0)" },
    { offset: 0.15, transform: "translate3d(-25%, 0, 0) rotate3d(0, 0, 1, -5deg)" },
    { offset: 0.3, transform: "translate3d(20%, 0, 0) rotate3d(0, 0, 1, 3deg)" },
    { offset: 0.45, transform: "translate3d(-15%, 0, 0) rotate3d(0, 0, 1, -3deg)" },
    { offset: 0.6, transform: "translate3d(10%, 0, 0) rotate3d(0, 0, 1, 2deg)" },
    { offset: 0.75, transform: "translate3d(-5%, 0, 0) rotate3d(0, 0, 1, -1deg)" },
    { offset: 1, transform: "translate3d(0, 0, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/back_entrances/backInDown.js
  var backInDown = [
    { offset: 0, transform: "translateY(-1200px) scale(0.7)", opacity: "0.7" },
    { offset: 0.8, transform: "translateY(0px) scale(0.7)", opacity: "0.7" },
    { offset: 1, transform: "scale(1)", opacity: "1" }
  ];

  // node_modules/@shoelace-style/animations/dist/back_entrances/backInLeft.js
  var backInLeft = [
    { offset: 0, transform: "translateX(-2000px) scale(0.7)", opacity: "0.7" },
    { offset: 0.8, transform: "translateX(0px) scale(0.7)", opacity: "0.7" },
    { offset: 1, transform: "scale(1)", opacity: "1" }
  ];

  // node_modules/@shoelace-style/animations/dist/back_entrances/backInRight.js
  var backInRight = [
    { offset: 0, transform: "translateX(2000px) scale(0.7)", opacity: "0.7" },
    { offset: 0.8, transform: "translateX(0px) scale(0.7)", opacity: "0.7" },
    { offset: 1, transform: "scale(1)", opacity: "1" }
  ];

  // node_modules/@shoelace-style/animations/dist/back_entrances/backInUp.js
  var backInUp = [
    { offset: 0, transform: "translateY(1200px) scale(0.7)", opacity: "0.7" },
    { offset: 0.8, transform: "translateY(0px) scale(0.7)", opacity: "0.7" },
    { offset: 1, transform: "scale(1)", opacity: "1" }
  ];

  // node_modules/@shoelace-style/animations/dist/back_exits/backOutDown.js
  var backOutDown = [
    { offset: 0, transform: "scale(1)", opacity: "1" },
    { offset: 0.2, transform: "translateY(0px) scale(0.7)", opacity: "0.7" },
    { offset: 1, transform: "translateY(700px) scale(0.7)", opacity: "0.7" }
  ];

  // node_modules/@shoelace-style/animations/dist/back_exits/backOutLeft.js
  var backOutLeft = [
    { offset: 0, transform: "scale(1)", opacity: "1" },
    { offset: 0.2, transform: "translateX(0px) scale(0.7)", opacity: "0.7" },
    { offset: 1, transform: "translateX(-2000px) scale(0.7)", opacity: "0.7" }
  ];

  // node_modules/@shoelace-style/animations/dist/back_exits/backOutRight.js
  var backOutRight = [
    { offset: 0, transform: "scale(1)", opacity: "1" },
    { offset: 0.2, transform: "translateX(0px) scale(0.7)", opacity: "0.7" },
    { offset: 1, transform: "translateX(2000px) scale(0.7)", opacity: "0.7" }
  ];

  // node_modules/@shoelace-style/animations/dist/back_exits/backOutUp.js
  var backOutUp = [
    { offset: 0, transform: "scale(1)", opacity: "1" },
    { offset: 0.2, transform: "translateY(0px) scale(0.7)", opacity: "0.7" },
    { offset: 1, transform: "translateY(-700px) scale(0.7)", opacity: "0.7" }
  ];

  // node_modules/@shoelace-style/animations/dist/bouncing_entrances/bounceIn.js
  var bounceIn = [
    { offset: 0, opacity: "0", transform: "scale3d(0.3, 0.3, 0.3)" },
    { offset: 0, easing: "cubic-bezier(0.215, 0.61, 0.355, 1)" },
    { offset: 0.2, transform: "scale3d(1.1, 1.1, 1.1)" },
    { offset: 0.2, easing: "cubic-bezier(0.215, 0.61, 0.355, 1)" },
    { offset: 0.4, transform: "scale3d(0.9, 0.9, 0.9)" },
    { offset: 0.4, easing: "cubic-bezier(0.215, 0.61, 0.355, 1)" },
    { offset: 0.6, opacity: "1", transform: "scale3d(1.03, 1.03, 1.03)" },
    { offset: 0.6, easing: "cubic-bezier(0.215, 0.61, 0.355, 1)" },
    { offset: 0.8, transform: "scale3d(0.97, 0.97, 0.97)" },
    { offset: 0.8, easing: "cubic-bezier(0.215, 0.61, 0.355, 1)" },
    { offset: 1, opacity: "1", transform: "scale3d(1, 1, 1)" },
    { offset: 1, easing: "cubic-bezier(0.215, 0.61, 0.355, 1)" }
  ];

  // node_modules/@shoelace-style/animations/dist/bouncing_entrances/bounceInDown.js
  var bounceInDown = [
    { offset: 0, opacity: "0", transform: "translate3d(0, -3000px, 0) scaleY(3)" },
    { offset: 0, easing: "cubic-bezier(0.215, 0.61, 0.355, 1)" },
    { offset: 0.6, opacity: "1", transform: "translate3d(0, 25px, 0) scaleY(0.9)" },
    { offset: 0.6, easing: "cubic-bezier(0.215, 0.61, 0.355, 1)" },
    { offset: 0.75, transform: "translate3d(0, -10px, 0) scaleY(0.95)" },
    { offset: 0.75, easing: "cubic-bezier(0.215, 0.61, 0.355, 1)" },
    { offset: 0.9, transform: "translate3d(0, 5px, 0) scaleY(0.985)" },
    { offset: 0.9, easing: "cubic-bezier(0.215, 0.61, 0.355, 1)" },
    { offset: 1, transform: "translate3d(0, 0, 0)" },
    { offset: 1, easing: "cubic-bezier(0.215, 0.61, 0.355, 1)" }
  ];

  // node_modules/@shoelace-style/animations/dist/bouncing_entrances/bounceInLeft.js
  var bounceInLeft = [
    { offset: 0, opacity: "0", transform: "translate3d(-3000px, 0, 0) scaleX(3)" },
    { offset: 0, easing: "cubic-bezier(0.215, 0.61, 0.355, 1)" },
    { offset: 0.6, opacity: "1", transform: "translate3d(25px, 0, 0) scaleX(1)" },
    { offset: 0.6, easing: "cubic-bezier(0.215, 0.61, 0.355, 1)" },
    { offset: 0.75, transform: "translate3d(-10px, 0, 0) scaleX(0.98)" },
    { offset: 0.75, easing: "cubic-bezier(0.215, 0.61, 0.355, 1)" },
    { offset: 0.9, transform: "translate3d(5px, 0, 0) scaleX(0.995)" },
    { offset: 0.9, easing: "cubic-bezier(0.215, 0.61, 0.355, 1)" },
    { offset: 1, transform: "translate3d(0, 0, 0)" },
    { offset: 1, easing: "cubic-bezier(0.215, 0.61, 0.355, 1)" }
  ];

  // node_modules/@shoelace-style/animations/dist/bouncing_entrances/bounceInRight.js
  var bounceInRight = [
    { offset: 0, opacity: "0", transform: "translate3d(3000px, 0, 0) scaleX(3)" },
    { offset: 0, easing: "cubic-bezier(0.215, 0.61, 0.355, 1)" },
    { offset: 0.6, opacity: "1", transform: "translate3d(-25px, 0, 0) scaleX(1)" },
    { offset: 0.6, easing: "cubic-bezier(0.215, 0.61, 0.355, 1)" },
    { offset: 0.75, transform: "translate3d(10px, 0, 0) scaleX(0.98)" },
    { offset: 0.75, easing: "cubic-bezier(0.215, 0.61, 0.355, 1)" },
    { offset: 0.9, transform: "translate3d(-5px, 0, 0) scaleX(0.995)" },
    { offset: 0.9, easing: "cubic-bezier(0.215, 0.61, 0.355, 1)" },
    { offset: 1, transform: "translate3d(0, 0, 0)" },
    { offset: 1, easing: "cubic-bezier(0.215, 0.61, 0.355, 1)" }
  ];

  // node_modules/@shoelace-style/animations/dist/bouncing_entrances/bounceInUp.js
  var bounceInUp = [
    { offset: 0, opacity: "0", transform: "translate3d(0, 3000px, 0) scaleY(5)" },
    { offset: 0, easing: "cubic-bezier(0.215, 0.61, 0.355, 1)" },
    { offset: 0.6, opacity: "1", transform: "translate3d(0, -20px, 0) scaleY(0.9)" },
    { offset: 0.6, easing: "cubic-bezier(0.215, 0.61, 0.355, 1)" },
    { offset: 0.75, transform: "translate3d(0, 10px, 0) scaleY(0.95)" },
    { offset: 0.75, easing: "cubic-bezier(0.215, 0.61, 0.355, 1)" },
    { offset: 0.9, transform: "translate3d(0, -5px, 0) scaleY(0.985)" },
    { offset: 0.9, easing: "cubic-bezier(0.215, 0.61, 0.355, 1)" },
    { offset: 1, transform: "translate3d(0, 0, 0)" },
    { offset: 1, easing: "cubic-bezier(0.215, 0.61, 0.355, 1)" }
  ];

  // node_modules/@shoelace-style/animations/dist/bouncing_exits/bounceOut.js
  var bounceOut = [
    { offset: 0.2, transform: "scale3d(0.9, 0.9, 0.9)" },
    { offset: 0.5, opacity: "1", transform: "scale3d(1.1, 1.1, 1.1)" },
    { offset: 0.55, opacity: "1", transform: "scale3d(1.1, 1.1, 1.1)" },
    { offset: 1, opacity: "0", transform: "scale3d(0.3, 0.3, 0.3)" }
  ];

  // node_modules/@shoelace-style/animations/dist/bouncing_exits/bounceOutDown.js
  var bounceOutDown = [
    { offset: 0.2, transform: "translate3d(0, 10px, 0) scaleY(0.985)" },
    { offset: 0.4, opacity: "1", transform: "translate3d(0, -20px, 0) scaleY(0.9)" },
    { offset: 0.45, opacity: "1", transform: "translate3d(0, -20px, 0) scaleY(0.9)" },
    { offset: 1, opacity: "0", transform: "translate3d(0, 2000px, 0) scaleY(3)" }
  ];

  // node_modules/@shoelace-style/animations/dist/bouncing_exits/bounceOutLeft.js
  var bounceOutLeft = [
    { offset: 0.2, opacity: "1", transform: "translate3d(20px, 0, 0) scaleX(0.9)" },
    { offset: 1, opacity: "0", transform: "translate3d(-2000px, 0, 0) scaleX(2)" }
  ];

  // node_modules/@shoelace-style/animations/dist/bouncing_exits/bounceOutRight.js
  var bounceOutRight = [
    { offset: 0.2, opacity: "1", transform: "translate3d(-20px, 0, 0) scaleX(0.9)" },
    { offset: 1, opacity: "0", transform: "translate3d(2000px, 0, 0) scaleX(2)" }
  ];

  // node_modules/@shoelace-style/animations/dist/bouncing_exits/bounceOutUp.js
  var bounceOutUp = [
    { offset: 0.2, transform: "translate3d(0, -10px, 0) scaleY(0.985)" },
    { offset: 0.4, opacity: "1", transform: "translate3d(0, 20px, 0) scaleY(0.9)" },
    { offset: 0.45, opacity: "1", transform: "translate3d(0, 20px, 0) scaleY(0.9)" },
    { offset: 1, opacity: "0", transform: "translate3d(0, -2000px, 0) scaleY(3)" }
  ];

  // node_modules/@shoelace-style/animations/dist/fading_entrances/fadeIn.js
  var fadeIn = [
    { offset: 0, opacity: "0" },
    { offset: 1, opacity: "1" }
  ];

  // node_modules/@shoelace-style/animations/dist/fading_entrances/fadeInBottomLeft.js
  var fadeInBottomLeft = [
    { offset: 0, opacity: "0", transform: "translate3d(-100%, 100%, 0)" },
    { offset: 1, opacity: "1", transform: "translate3d(0, 0, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/fading_entrances/fadeInBottomRight.js
  var fadeInBottomRight = [
    { offset: 0, opacity: "0", transform: "translate3d(100%, 100%, 0)" },
    { offset: 1, opacity: "1", transform: "translate3d(0, 0, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/fading_entrances/fadeInDown.js
  var fadeInDown = [
    { offset: 0, opacity: "0", transform: "translate3d(0, -100%, 0)" },
    { offset: 1, opacity: "1", transform: "translate3d(0, 0, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/fading_entrances/fadeInDownBig.js
  var fadeInDownBig = [
    { offset: 0, opacity: "0", transform: "translate3d(0, -2000px, 0)" },
    { offset: 1, opacity: "1", transform: "translate3d(0, 0, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/fading_entrances/fadeInLeft.js
  var fadeInLeft = [
    { offset: 0, opacity: "0", transform: "translate3d(-100%, 0, 0)" },
    { offset: 1, opacity: "1", transform: "translate3d(0, 0, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/fading_entrances/fadeInLeftBig.js
  var fadeInLeftBig = [
    { offset: 0, opacity: "0", transform: "translate3d(-2000px, 0, 0)" },
    { offset: 1, opacity: "1", transform: "translate3d(0, 0, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/fading_entrances/fadeInRight.js
  var fadeInRight = [
    { offset: 0, opacity: "0", transform: "translate3d(100%, 0, 0)" },
    { offset: 1, opacity: "1", transform: "translate3d(0, 0, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/fading_entrances/fadeInRightBig.js
  var fadeInRightBig = [
    { offset: 0, opacity: "0", transform: "translate3d(2000px, 0, 0)" },
    { offset: 1, opacity: "1", transform: "translate3d(0, 0, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/fading_entrances/fadeInTopLeft.js
  var fadeInTopLeft = [
    { offset: 0, opacity: "0", transform: "translate3d(-100%, -100%, 0)" },
    { offset: 1, opacity: "1", transform: "translate3d(0, 0, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/fading_entrances/fadeInTopRight.js
  var fadeInTopRight = [
    { offset: 0, opacity: "0", transform: "translate3d(100%, -100%, 0)" },
    { offset: 1, opacity: "1", transform: "translate3d(0, 0, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/fading_entrances/fadeInUp.js
  var fadeInUp = [
    { offset: 0, opacity: "0", transform: "translate3d(0, 100%, 0)" },
    { offset: 1, opacity: "1", transform: "translate3d(0, 0, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/fading_entrances/fadeInUpBig.js
  var fadeInUpBig = [
    { offset: 0, opacity: "0", transform: "translate3d(0, 2000px, 0)" },
    { offset: 1, opacity: "1", transform: "translate3d(0, 0, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/fading_exits/fadeOut.js
  var fadeOut = [
    { offset: 0, opacity: "1" },
    { offset: 1, opacity: "0" }
  ];

  // node_modules/@shoelace-style/animations/dist/fading_exits/fadeOutBottomLeft.js
  var fadeOutBottomLeft = [
    { offset: 0, opacity: "1", transform: "translate3d(0, 0, 0)" },
    { offset: 1, opacity: "0", transform: "translate3d(-100%, 100%, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/fading_exits/fadeOutBottomRight.js
  var fadeOutBottomRight = [
    { offset: 0, opacity: "1", transform: "translate3d(0, 0, 0)" },
    { offset: 1, opacity: "0", transform: "translate3d(100%, 100%, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/fading_exits/fadeOutDown.js
  var fadeOutDown = [
    { offset: 0, opacity: "1" },
    { offset: 1, opacity: "0", transform: "translate3d(0, 100%, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/fading_exits/fadeOutDownBig.js
  var fadeOutDownBig = [
    { offset: 0, opacity: "1" },
    { offset: 1, opacity: "0", transform: "translate3d(0, 2000px, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/fading_exits/fadeOutLeft.js
  var fadeOutLeft = [
    { offset: 0, opacity: "1" },
    { offset: 1, opacity: "0", transform: "translate3d(-100%, 0, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/fading_exits/fadeOutLeftBig.js
  var fadeOutLeftBig = [
    { offset: 0, opacity: "1" },
    { offset: 1, opacity: "0", transform: "translate3d(-2000px, 0, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/fading_exits/fadeOutRight.js
  var fadeOutRight = [
    { offset: 0, opacity: "1" },
    { offset: 1, opacity: "0", transform: "translate3d(100%, 0, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/fading_exits/fadeOutRightBig.js
  var fadeOutRightBig = [
    { offset: 0, opacity: "1" },
    { offset: 1, opacity: "0", transform: "translate3d(2000px, 0, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/fading_exits/fadeOutTopLeft.js
  var fadeOutTopLeft = [
    { offset: 0, opacity: "1", transform: "translate3d(0, 0, 0)" },
    { offset: 1, opacity: "0", transform: "translate3d(-100%, -100%, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/fading_exits/fadeOutTopRight.js
  var fadeOutTopRight = [
    { offset: 0, opacity: "1", transform: "translate3d(0, 0, 0)" },
    { offset: 1, opacity: "0", transform: "translate3d(100%, -100%, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/fading_exits/fadeOutUp.js
  var fadeOutUp = [
    { offset: 0, opacity: "1" },
    { offset: 1, opacity: "0", transform: "translate3d(0, -100%, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/fading_exits/fadeOutUpBig.js
  var fadeOutUpBig = [
    { offset: 0, opacity: "1" },
    { offset: 1, opacity: "0", transform: "translate3d(0, -2000px, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/flippers/flip.js
  var flip = [
    {
      offset: 0,
      transform: "perspective(400px) scale3d(1, 1, 1) translate3d(0, 0, 0) rotate3d(0, 1, 0, -360deg)",
      easing: "ease-out"
    },
    {
      offset: 0.4,
      transform: "perspective(400px) scale3d(1, 1, 1) translate3d(0, 0, 150px)\n      rotate3d(0, 1, 0, -190deg)",
      easing: "ease-out"
    },
    {
      offset: 0.5,
      transform: "perspective(400px) scale3d(1, 1, 1) translate3d(0, 0, 150px)\n      rotate3d(0, 1, 0, -170deg)",
      easing: "ease-in"
    },
    {
      offset: 0.8,
      transform: "perspective(400px) scale3d(0.95, 0.95, 0.95) translate3d(0, 0, 0)\n      rotate3d(0, 1, 0, 0deg)",
      easing: "ease-in"
    },
    {
      offset: 1,
      transform: "perspective(400px) scale3d(1, 1, 1) translate3d(0, 0, 0) rotate3d(0, 1, 0, 0deg)",
      easing: "ease-in"
    }
  ];

  // node_modules/@shoelace-style/animations/dist/flippers/flipInX.js
  var flipInX = [
    { offset: 0, transform: "perspective(400px) rotate3d(1, 0, 0, 90deg)", easing: "ease-in", opacity: "0" },
    { offset: 0.4, transform: "perspective(400px) rotate3d(1, 0, 0, -20deg)", easing: "ease-in" },
    { offset: 0.6, transform: "perspective(400px) rotate3d(1, 0, 0, 10deg)", opacity: "1" },
    { offset: 0.8, transform: "perspective(400px) rotate3d(1, 0, 0, -5deg)" },
    { offset: 1, transform: "perspective(400px)" }
  ];

  // node_modules/@shoelace-style/animations/dist/flippers/flipInY.js
  var flipInY = [
    { offset: 0, transform: "perspective(400px) rotate3d(0, 1, 0, 90deg)", easing: "ease-in", opacity: "0" },
    { offset: 0.4, transform: "perspective(400px) rotate3d(0, 1, 0, -20deg)", easing: "ease-in" },
    { offset: 0.6, transform: "perspective(400px) rotate3d(0, 1, 0, 10deg)", opacity: "1" },
    { offset: 0.8, transform: "perspective(400px) rotate3d(0, 1, 0, -5deg)" },
    { offset: 1, transform: "perspective(400px)" }
  ];

  // node_modules/@shoelace-style/animations/dist/flippers/flipOutX.js
  var flipOutX = [
    { offset: 0, transform: "perspective(400px)" },
    { offset: 0.3, transform: "perspective(400px) rotate3d(1, 0, 0, -20deg)", opacity: "1" },
    { offset: 1, transform: "perspective(400px) rotate3d(1, 0, 0, 90deg)", opacity: "0" }
  ];

  // node_modules/@shoelace-style/animations/dist/flippers/flipOutY.js
  var flipOutY = [
    { offset: 0, transform: "perspective(400px)" },
    { offset: 0.3, transform: "perspective(400px) rotate3d(0, 1, 0, -15deg)", opacity: "1" },
    { offset: 1, transform: "perspective(400px) rotate3d(0, 1, 0, 90deg)", opacity: "0" }
  ];

  // node_modules/@shoelace-style/animations/dist/lightspeed/lightSpeedInLeft.js
  var lightSpeedInLeft = [
    { offset: 0, transform: "translate3d(-100%, 0, 0) skewX(30deg)", opacity: "0" },
    { offset: 0.6, transform: "skewX(-20deg)", opacity: "1" },
    { offset: 0.8, transform: "skewX(5deg)" },
    { offset: 1, transform: "translate3d(0, 0, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/lightspeed/lightSpeedInRight.js
  var lightSpeedInRight = [
    { offset: 0, transform: "translate3d(100%, 0, 0) skewX(-30deg)", opacity: "0" },
    { offset: 0.6, transform: "skewX(20deg)", opacity: "1" },
    { offset: 0.8, transform: "skewX(-5deg)" },
    { offset: 1, transform: "translate3d(0, 0, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/lightspeed/lightSpeedOutLeft.js
  var lightSpeedOutLeft = [
    { offset: 0, opacity: "1" },
    { offset: 1, transform: "translate3d(-100%, 0, 0) skewX(-30deg)", opacity: "0" }
  ];

  // node_modules/@shoelace-style/animations/dist/lightspeed/lightSpeedOutRight.js
  var lightSpeedOutRight = [
    { offset: 0, opacity: "1" },
    { offset: 1, transform: "translate3d(100%, 0, 0) skewX(30deg)", opacity: "0" }
  ];

  // node_modules/@shoelace-style/animations/dist/rotating_entrances/rotateIn.js
  var rotateIn = [
    { offset: 0, transform: "rotate3d(0, 0, 1, -200deg)", opacity: "0" },
    { offset: 1, transform: "translate3d(0, 0, 0)", opacity: "1" }
  ];

  // node_modules/@shoelace-style/animations/dist/rotating_entrances/rotateInDownLeft.js
  var rotateInDownLeft = [
    { offset: 0, transform: "rotate3d(0, 0, 1, -45deg)", opacity: "0" },
    { offset: 1, transform: "translate3d(0, 0, 0)", opacity: "1" }
  ];

  // node_modules/@shoelace-style/animations/dist/rotating_entrances/rotateInDownRight.js
  var rotateInDownRight = [
    { offset: 0, transform: "rotate3d(0, 0, 1, 45deg)", opacity: "0" },
    { offset: 1, transform: "translate3d(0, 0, 0)", opacity: "1" }
  ];

  // node_modules/@shoelace-style/animations/dist/rotating_entrances/rotateInUpLeft.js
  var rotateInUpLeft = [
    { offset: 0, transform: "rotate3d(0, 0, 1, 45deg)", opacity: "0" },
    { offset: 1, transform: "translate3d(0, 0, 0)", opacity: "1" }
  ];

  // node_modules/@shoelace-style/animations/dist/rotating_entrances/rotateInUpRight.js
  var rotateInUpRight = [
    { offset: 0, transform: "rotate3d(0, 0, 1, -90deg)", opacity: "0" },
    { offset: 1, transform: "translate3d(0, 0, 0)", opacity: "1" }
  ];

  // node_modules/@shoelace-style/animations/dist/rotating_exits/rotateOut.js
  var rotateOut = [
    { offset: 0, opacity: "1" },
    { offset: 1, transform: "rotate3d(0, 0, 1, 200deg)", opacity: "0" }
  ];

  // node_modules/@shoelace-style/animations/dist/rotating_exits/rotateOutDownLeft.js
  var rotateOutDownLeft = [
    { offset: 0, opacity: "1" },
    { offset: 1, transform: "rotate3d(0, 0, 1, 45deg)", opacity: "0" }
  ];

  // node_modules/@shoelace-style/animations/dist/rotating_exits/rotateOutDownRight.js
  var rotateOutDownRight = [
    { offset: 0, opacity: "1" },
    { offset: 1, transform: "rotate3d(0, 0, 1, -45deg)", opacity: "0" }
  ];

  // node_modules/@shoelace-style/animations/dist/rotating_exits/rotateOutUpLeft.js
  var rotateOutUpLeft = [
    { offset: 0, opacity: "1" },
    { offset: 1, transform: "rotate3d(0, 0, 1, -45deg)", opacity: "0" }
  ];

  // node_modules/@shoelace-style/animations/dist/rotating_exits/rotateOutUpRight.js
  var rotateOutUpRight = [
    { offset: 0, opacity: "1" },
    { offset: 1, transform: "rotate3d(0, 0, 1, 90deg)", opacity: "0" }
  ];

  // node_modules/@shoelace-style/animations/dist/sliding_entrances/slideInDown.js
  var slideInDown = [
    { offset: 0, transform: "translate3d(0, -100%, 0)", visibility: "visible" },
    { offset: 1, transform: "translate3d(0, 0, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/sliding_entrances/slideInLeft.js
  var slideInLeft = [
    { offset: 0, transform: "translate3d(-100%, 0, 0)", visibility: "visible" },
    { offset: 1, transform: "translate3d(0, 0, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/sliding_entrances/slideInRight.js
  var slideInRight = [
    { offset: 0, transform: "translate3d(100%, 0, 0)", visibility: "visible" },
    { offset: 1, transform: "translate3d(0, 0, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/sliding_entrances/slideInUp.js
  var slideInUp = [
    { offset: 0, transform: "translate3d(0, 100%, 0)", visibility: "visible" },
    { offset: 1, transform: "translate3d(0, 0, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/sliding_exits/slideOutDown.js
  var slideOutDown = [
    { offset: 0, transform: "translate3d(0, 0, 0)" },
    { offset: 1, visibility: "hidden", transform: "translate3d(0, 100%, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/sliding_exits/slideOutLeft.js
  var slideOutLeft = [
    { offset: 0, transform: "translate3d(0, 0, 0)" },
    { offset: 1, visibility: "hidden", transform: "translate3d(-100%, 0, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/sliding_exits/slideOutRight.js
  var slideOutRight = [
    { offset: 0, transform: "translate3d(0, 0, 0)" },
    { offset: 1, visibility: "hidden", transform: "translate3d(100%, 0, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/sliding_exits/slideOutUp.js
  var slideOutUp = [
    { offset: 0, transform: "translate3d(0, 0, 0)" },
    { offset: 1, visibility: "hidden", transform: "translate3d(0, -100%, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/specials/hinge.js
  var hinge = [
    { offset: 0, easing: "ease-in-out" },
    { offset: 0.2, transform: "rotate3d(0, 0, 1, 80deg)", easing: "ease-in-out" },
    { offset: 0.4, transform: "rotate3d(0, 0, 1, 60deg)", easing: "ease-in-out", opacity: "1" },
    { offset: 0.6, transform: "rotate3d(0, 0, 1, 80deg)", easing: "ease-in-out" },
    { offset: 0.8, transform: "rotate3d(0, 0, 1, 60deg)", easing: "ease-in-out", opacity: "1" },
    { offset: 1, transform: "translate3d(0, 700px, 0)", opacity: "0" }
  ];

  // node_modules/@shoelace-style/animations/dist/specials/jackInTheBox.js
  var jackInTheBox = [
    { offset: 0, opacity: "0", transform: "scale(0.1) rotate(30deg)", "transform-origin": "center bottom" },
    { offset: 0.5, transform: "rotate(-10deg)" },
    { offset: 0.7, transform: "rotate(3deg)" },
    { offset: 1, opacity: "1", transform: "scale(1)" }
  ];

  // node_modules/@shoelace-style/animations/dist/specials/rollIn.js
  var rollIn = [
    { offset: 0, opacity: "0", transform: "translate3d(-100%, 0, 0) rotate3d(0, 0, 1, -120deg)" },
    { offset: 1, opacity: "1", transform: "translate3d(0, 0, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/specials/rollOut.js
  var rollOut = [
    { offset: 0, opacity: "1" },
    { offset: 1, opacity: "0", transform: "translate3d(100%, 0, 0) rotate3d(0, 0, 1, 120deg)" }
  ];

  // node_modules/@shoelace-style/animations/dist/zooming_entrances/zoomIn.js
  var zoomIn = [
    { offset: 0, opacity: "0", transform: "scale3d(0.3, 0.3, 0.3)" },
    { offset: 0.5, opacity: "1" }
  ];

  // node_modules/@shoelace-style/animations/dist/zooming_entrances/zoomInDown.js
  var zoomInDown = [
    {
      offset: 0,
      opacity: "0",
      transform: "scale3d(0.1, 0.1, 0.1) translate3d(0, -1000px, 0)",
      easing: "cubic-bezier(0.55, 0.055, 0.675, 0.19)"
    },
    {
      offset: 0.6,
      opacity: "1",
      transform: "scale3d(0.475, 0.475, 0.475) translate3d(0, 60px, 0)",
      easing: "cubic-bezier(0.175, 0.885, 0.32, 1)"
    }
  ];

  // node_modules/@shoelace-style/animations/dist/zooming_entrances/zoomInLeft.js
  var zoomInLeft = [
    {
      offset: 0,
      opacity: "0",
      transform: "scale3d(0.1, 0.1, 0.1) translate3d(-1000px, 0, 0)",
      easing: "cubic-bezier(0.55, 0.055, 0.675, 0.19)"
    },
    {
      offset: 0.6,
      opacity: "1",
      transform: "scale3d(0.475, 0.475, 0.475) translate3d(10px, 0, 0)",
      easing: "cubic-bezier(0.175, 0.885, 0.32, 1)"
    }
  ];

  // node_modules/@shoelace-style/animations/dist/zooming_entrances/zoomInRight.js
  var zoomInRight = [
    {
      offset: 0,
      opacity: "0",
      transform: "scale3d(0.1, 0.1, 0.1) translate3d(1000px, 0, 0)",
      easing: "cubic-bezier(0.55, 0.055, 0.675, 0.19)"
    },
    {
      offset: 0.6,
      opacity: "1",
      transform: "scale3d(0.475, 0.475, 0.475) translate3d(-10px, 0, 0)",
      easing: "cubic-bezier(0.175, 0.885, 0.32, 1)"
    }
  ];

  // node_modules/@shoelace-style/animations/dist/zooming_entrances/zoomInUp.js
  var zoomInUp = [
    {
      offset: 0,
      opacity: "0",
      transform: "scale3d(0.1, 0.1, 0.1) translate3d(0, 1000px, 0)",
      easing: "cubic-bezier(0.55, 0.055, 0.675, 0.19)"
    },
    {
      offset: 0.6,
      opacity: "1",
      transform: "scale3d(0.475, 0.475, 0.475) translate3d(0, -60px, 0)",
      easing: "cubic-bezier(0.175, 0.885, 0.32, 1)"
    }
  ];

  // node_modules/@shoelace-style/animations/dist/zooming_exits/zoomOut.js
  var zoomOut = [
    { offset: 0, opacity: "1" },
    { offset: 0.5, opacity: "0", transform: "scale3d(0.3, 0.3, 0.3)" },
    { offset: 1, opacity: "0" }
  ];

  // node_modules/@shoelace-style/animations/dist/zooming_exits/zoomOutDown.js
  var zoomOutDown = [
    {
      offset: 0.4,
      opacity: "1",
      transform: "scale3d(0.475, 0.475, 0.475) translate3d(0, -60px, 0)",
      easing: "cubic-bezier(0.55, 0.055, 0.675, 0.19)"
    },
    {
      offset: 1,
      opacity: "0",
      transform: "scale3d(0.1, 0.1, 0.1) translate3d(0, 2000px, 0)",
      easing: "cubic-bezier(0.175, 0.885, 0.32, 1)"
    }
  ];

  // node_modules/@shoelace-style/animations/dist/zooming_exits/zoomOutLeft.js
  var zoomOutLeft = [
    { offset: 0.4, opacity: "1", transform: "scale3d(0.475, 0.475, 0.475) translate3d(42px, 0, 0)" },
    { offset: 1, opacity: "0", transform: "scale(0.1) translate3d(-2000px, 0, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/zooming_exits/zoomOutRight.js
  var zoomOutRight = [
    { offset: 0.4, opacity: "1", transform: "scale3d(0.475, 0.475, 0.475) translate3d(-42px, 0, 0)" },
    { offset: 1, opacity: "0", transform: "scale(0.1) translate3d(2000px, 0, 0)" }
  ];

  // node_modules/@shoelace-style/animations/dist/zooming_exits/zoomOutUp.js
  var zoomOutUp = [
    {
      offset: 0.4,
      opacity: "1",
      transform: "scale3d(0.475, 0.475, 0.475) translate3d(0, 60px, 0)",
      easing: "cubic-bezier(0.55, 0.055, 0.675, 0.19)"
    },
    {
      offset: 1,
      opacity: "0",
      transform: "scale3d(0.1, 0.1, 0.1) translate3d(0, -2000px, 0)",
      easing: "cubic-bezier(0.175, 0.885, 0.32, 1)"
    }
  ];

  // node_modules/@shoelace-style/animations/dist/easings/easings.js
  var easings = {
    linear: "linear",
    ease: "ease",
    easeIn: "ease-in",
    easeOut: "ease-out",
    easeInOut: "ease-in-out",
    easeInSine: "cubic-bezier(0.47, 0, 0.745, 0.715)",
    easeOutSine: "cubic-bezier(0.39, 0.575, 0.565, 1)",
    easeInOutSine: "cubic-bezier(0.445, 0.05, 0.55, 0.95)",
    easeInQuad: "cubic-bezier(0.55, 0.085, 0.68, 0.53)",
    easeOutQuad: "cubic-bezier(0.25, 0.46, 0.45, 0.94)",
    easeInOutQuad: "cubic-bezier(0.455, 0.03, 0.515, 0.955)",
    easeInCubic: "cubic-bezier(0.55, 0.055, 0.675, 0.19)",
    easeOutCubic: "cubic-bezier(0.215, 0.61, 0.355, 1)",
    easeInOutCubic: "cubic-bezier(0.645, 0.045, 0.355, 1)",
    easeInQuart: "cubic-bezier(0.895, 0.03, 0.685, 0.22)",
    easeOutQuart: "cubic-bezier(0.165, 0.84, 0.44, 1)",
    easeInOutQuart: "cubic-bezier(0.77, 0, 0.175, 1)",
    easeInQuint: "cubic-bezier(0.755, 0.05, 0.855, 0.06)",
    easeOutQuint: "cubic-bezier(0.23, 1, 0.32, 1)",
    easeInOutQuint: "cubic-bezier(0.86, 0, 0.07, 1)",
    easeInExpo: "cubic-bezier(0.95, 0.05, 0.795, 0.035)",
    easeOutExpo: "cubic-bezier(0.19, 1, 0.22, 1)",
    easeInOutExpo: "cubic-bezier(1, 0, 0, 1)",
    easeInCirc: "cubic-bezier(0.6, 0.04, 0.98, 0.335)",
    easeOutCirc: "cubic-bezier(0.075, 0.82, 0.165, 1)",
    easeInOutCirc: "cubic-bezier(0.785, 0.135, 0.15, 0.86)",
    easeInBack: "cubic-bezier(0.6, -0.28, 0.735, 0.045)",
    easeOutBack: "cubic-bezier(0.175, 0.885, 0.32, 1.275)",
    easeInOutBack: "cubic-bezier(0.68, -0.55, 0.265, 1.55)"
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.XQWTILKM.js
  function getAnimationNames() {
    return Object.entries(dist_exports).filter(([name]) => name !== "easings").map(([name]) => name);
  }
  function getEasingNames() {
    return Object.entries(easings).map(([name]) => name);
  }

  // node_modules/@shoelace-style/localize/dist/index.js
  var connectedElements = /* @__PURE__ */ new Set();
  var translations = /* @__PURE__ */ new Map();
  var fallback;
  var documentDirection = "ltr";
  var documentLanguage = "en";
  var isClient = typeof MutationObserver !== "undefined" && typeof document !== "undefined" && typeof document.documentElement !== "undefined";
  if (isClient) {
    const documentElementObserver = new MutationObserver(update);
    documentDirection = document.documentElement.dir || "ltr";
    documentLanguage = document.documentElement.lang || navigator.language;
    documentElementObserver.observe(document.documentElement, {
      attributes: true,
      attributeFilter: ["dir", "lang"]
    });
  }
  function registerTranslation(...translation2) {
    translation2.map((t8) => {
      const code = t8.$code.toLowerCase();
      if (translations.has(code)) {
        translations.set(code, Object.assign(Object.assign({}, translations.get(code)), t8));
      } else {
        translations.set(code, t8);
      }
      if (!fallback) {
        fallback = t8;
      }
    });
    update();
  }
  function update() {
    if (isClient) {
      documentDirection = document.documentElement.dir || "ltr";
      documentLanguage = document.documentElement.lang || navigator.language;
    }
    [...connectedElements.keys()].map((el) => {
      if (typeof el.requestUpdate === "function") {
        el.requestUpdate();
      }
    });
  }
  var LocalizeController = class {
    constructor(host) {
      this.host = host;
      this.host.addController(this);
    }
    hostConnected() {
      connectedElements.add(this.host);
    }
    hostDisconnected() {
      connectedElements.delete(this.host);
    }
    dir() {
      return `${this.host.dir || documentDirection}`.toLowerCase();
    }
    lang() {
      const lang = `${this.host.lang || documentLanguage}`.toLowerCase().replace(/_/g, "-");
      try {
        new Intl.Locale(lang);
        return lang;
      } catch (_a) {
        return fallback ? fallback.$code.toLowerCase() : "en";
      }
    }
    getTranslationData(lang) {
      var _a, _b;
      let locale;
      try {
        locale = new Intl.Locale(lang.replace(/_/g, "-"));
      } catch (_c) {
        return { locale: void 0, language: "", region: "", primary: void 0, secondary: void 0 };
      }
      const language = locale.language.toLowerCase();
      const region = (_b = (_a = locale.region) === null || _a === void 0 ? void 0 : _a.toLowerCase()) !== null && _b !== void 0 ? _b : "";
      const primary = translations.get(`${language}-${region}`);
      const secondary = translations.get(language);
      return { locale, language, region, primary, secondary };
    }
    exists(key, options2) {
      var _a;
      const { primary, secondary } = this.getTranslationData((_a = options2.lang) !== null && _a !== void 0 ? _a : this.lang());
      options2 = Object.assign({ includeFallback: false }, options2);
      if (primary && primary[key] || secondary && secondary[key] || options2.includeFallback && fallback && fallback[key]) {
        return true;
      }
      return false;
    }
    term(key, ...args) {
      const { primary, secondary } = this.getTranslationData(this.lang());
      let term;
      if (primary && primary[key]) {
        term = primary[key];
      } else if (secondary && secondary[key]) {
        term = secondary[key];
      } else if (fallback && fallback[key]) {
        term = fallback[key];
      } else {
        console.error(`No translation found for: ${String(key)}`);
        return String(key);
      }
      if (typeof term === "function") {
        return term(...args);
      }
      return term;
    }
    date(dateToFormat, options2) {
      dateToFormat = new Date(dateToFormat);
      return new Intl.DateTimeFormat(this.lang(), options2).format(dateToFormat);
    }
    number(numberToFormat, options2) {
      numberToFormat = Number(numberToFormat);
      return isNaN(numberToFormat) ? "" : new Intl.NumberFormat(this.lang(), options2).format(numberToFormat);
    }
    relativeTime(value, unit, options2) {
      return new Intl.RelativeTimeFormat(this.lang(), options2).format(value, unit);
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.BUFZMJDN.js
  var translation = {
    $code: "en",
    $name: "English",
    $dir: "ltr",
    am: "AM",
    autosizeColumn: "Autosize column",
    captions: "Captions",
    carousel: "Carousel",
    chooseDate: "Choose date",
    chooseDecade: "Choose decade",
    chooseMonth: "Choose month",
    chooseTime: "Choose time",
    chooseYear: "Choose year",
    clearEntry: "Clear entry",
    clearFilter: "Clear filter",
    clearSort: "Clear sort",
    close: "Close",
    closeCalendar: "Close calendar",
    closeTimeInput: "Close time picker",
    collapseRow: "Collapse row",
    columnMenu: "Column options",
    columnMovedToPosition: (label, position, total) => `${label} moved to position ${position} of ${total}`,
    columns: "Columns",
    compactPageXOfY: (page, total) => `${page} of ${total}`,
    copied: "Copied",
    copy: "Copy",
    createOption: (value) => `Create "${value}"`,
    currentlyPlaying: "currently playing",
    currentValue: "Current value",
    date: "Date",
    datePickerKeyboardHelp: "Use arrow keys to change values; press Alt+Down Arrow to open the calendar.",
    day: "Day",
    dayPeriod: "AM/PM",
    decrement: "Decrement",
    deselectAllRows: "Deselect all rows",
    dropFileHere: "Drop file here or click to browse",
    dropFilesHere: "Drop files here or click to browse",
    empty: "Empty",
    endDate: "End date",
    enterFullscreen: "Enter fullscreen",
    error: "Error",
    exitFullscreen: "Exit fullscreen",
    expandRow: "Expand row",
    filterByColumn: (label) => `Filter by ${label}`,
    filterFrom: "From",
    filterMax: "Max",
    filterMin: "Min",
    filterTo: "To",
    firstPage: "First page",
    goToSlide: (slide, count) => `Go to slide ${slide} of ${count}`,
    hideColumn: "Hide column",
    hidePassword: "Hide password",
    hour: "Hour",
    incompleteDate: "Enter a valid date.",
    increment: "Increment",
    jumpBackwardX: (count) => `Jump back ${count} pages`,
    jumpForwardX: (count) => `Jump forward ${count} pages`,
    lastPage: "Last page",
    loading: "Loading",
    minute: "Minute",
    month: "Month",
    moreOptions: "More Options",
    mute: "Mute",
    nextDecade: "Next decade",
    nextMonth: "Next month",
    nextPage: "Next page",
    nextSlide: "Next slide",
    nextVideo: "Next Video",
    nextYear: "Next year",
    noData: "No data",
    noResults: "No matching results",
    now: "Now",
    numCharacters: (num) => {
      if (num === 1) {
        return "1 character";
      }
      return `${num} characters`;
    },
    numCharactersRemaining: (num) => {
      if (num === 1) {
        return "1 character remaining";
      }
      return `${num} characters remaining`;
    },
    numOptionsSelected: (num) => {
      if (num === 0) {
        return "No options selected";
      }
      if (num === 1) {
        return "1 option selected";
      }
      return `${num} options selected`;
    },
    numRowsCopied: (num) => num === 1 ? "1 row copied" : `${num} rows copied`,
    numRowsSelected: (num) => num === 1 ? "1 row selected" : `${num} rows selected`,
    pageXOfY: (page, total) => `Page ${page} of ${total}`,
    pagination: "Pagination",
    pause: "Pause",
    pauseAnimation: "Pause animation",
    pictureInPicture: "Picture in picture",
    pinLeft: "Pin left",
    pinRight: "Pin right",
    play: "Play",
    playAnimation: "Play animation",
    playbackSpeed: "Playback speed",
    playlist: "Playlist",
    pm: "PM",
    previousDecade: "Previous decade",
    previousMonth: "Previous month",
    previousPage: "Previous page",
    previousSlide: "Previous slide",
    previousVideo: "Previous video",
    previousYear: "Previous year",
    progress: "Progress",
    rangeTooLong: (max2) => {
      if (max2 === 1) {
        return "Select a range no longer than 1 day";
      }
      return `Select a range no longer than ${max2} days`;
    },
    rangeTooShort: (min2) => {
      if (min2 === 1) {
        return "Select a range at least 1 day long";
      }
      return `Select a range at least ${min2} days long`;
    },
    readonly: "Read-only",
    remove: "Remove",
    resetColumns: "Reset columns",
    resize: "Resize",
    resizeColumn: "Resize column",
    rowsPerPage: "Rows per page",
    scrollableRegion: "Scrollable region",
    scrollToEnd: "Scroll to end",
    scrollToStart: "Scroll to start",
    search: "Search",
    second: "Second",
    seek: "Seek",
    seekProgress: (current, duration) => `${current} of ${duration}`,
    selectAColorFromTheScreen: "Select a color from the screen",
    selectAllRows: "Select all rows",
    selected: "Selected",
    selectedDateLabel: (date) => `Selected: ${date}`,
    selectedRangeLabel: (range2) => `Selected range: ${range2}`,
    selectGroup: "Select group",
    selectionCleared: "Selection cleared",
    selectRow: "Select row",
    showingNofMRows: (shown, total) => `Showing ${shown} of ${total} rows`,
    showingXtoYofZ: (start, end, total) => `${start}\u2013${end} of ${total}`,
    showPassword: "Show password",
    slideNum: (slide) => `Slide ${slide}`,
    sortAscending: "Sort ascending",
    sortColumn: "Sort column",
    sortDescending: "Sort descending",
    startDate: "Start date",
    time: "Time",
    timeInputKeyboardHelp: "Use arrow keys to change values; press Alt+Down Arrow to open the time picker.",
    today: "Today",
    toggleColorFormat: "Toggle color format",
    unmute: "Unmute",
    unpin: "Unpin",
    unpinColumn: "Unpin column",
    videoPlayer: "Video player",
    volume: "Volume",
    year: "Year",
    zoomIn: "Zoom in",
    zoomOut: "Zoom out"
  };
  registerTranslation(translation);
  var en_default = translation;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.TFKBFNPO.js
  var LocalizeController2 = class extends LocalizeController {
    lang() {
      if (this.host.didSSR && !this.host.hasUpdated) {
        return this.host.lang || "en";
      }
      return super.lang();
    }
  };
  registerTranslation(en_default);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.5I2MJI4V.js
  function dataUri(svg) {
    return `data:image/svg+xml,${encodeURIComponent(svg)}`;
  }
  var icons = {
    angular: `<svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path d="M16.712 17.711H7.288l-1.204 2.916L12 24l5.916-3.373-1.204-2.916ZM14.692 0l7.832 16.855.814-12.856L14.692 0ZM9.308 0 .662 3.999l.814 12.856L9.308 0Zm-.405 13.93h6.198L12 6.396 8.903 13.93Z"/></svg>`,
    bluesky: `<svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path d="M5.202 2.857C7.954 4.922 10.913 9.11 12 11.358c1.087-2.247 4.046-6.436 6.798-8.501C20.783 1.366 24 .213 24 3.883c0 .732-.42 6.156-.667 7.037-.856 3.061-3.978 3.842-6.755 3.37 4.854.826 6.089 3.562 3.422 6.299-5.065 5.196-7.28-1.304-7.847-2.97-.104-.305-.152-.448-.153-.327 0-.121-.05.022-.153.327-.568 1.666-2.782 8.166-7.847 2.97-2.667-2.737-1.432-5.473 3.422-6.3-2.777.473-5.899-.308-6.755-3.369C.42 10.04 0 4.615 0 3.883c0-3.67 3.217-2.517 5.202-1.026"/></svg>`,
    discord: `<svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path d="M20.317 4.3698a19.7913 19.7913 0 00-4.8851-1.5152.0741.0741 0 00-.0785.0371c-.211.3753-.4447.8648-.6083 1.2495-1.8447-.2762-3.68-.2762-5.4868 0-.1636-.3933-.4058-.8742-.6177-1.2495a.077.077 0 00-.0785-.037 19.7363 19.7363 0 00-4.8852 1.515.0699.0699 0 00-.0321.0277C.5334 9.0458-.319 13.5799.0992 18.0578a.0824.0824 0 00.0312.0561c2.0528 1.5076 4.0413 2.4228 5.9929 3.0294a.0777.0777 0 00.0842-.0276c.4616-.6304.8731-1.2952 1.226-1.9942a.076.076 0 00-.0416-.1057c-.6528-.2476-1.2743-.5495-1.8722-.8923a.077.077 0 01-.0076-.1277c.1258-.0943.2517-.1923.3718-.2914a.0743.0743 0 01.0776-.0105c3.9278 1.7933 8.18 1.7933 12.0614 0a.0739.0739 0 01.0785.0095c.1202.099.246.1981.3728.2924a.077.077 0 01-.0066.1276 12.2986 12.2986 0 01-1.873.8914.0766.0766 0 00-.0407.1067c.3604.698.7719 1.3628 1.225 1.9932a.076.076 0 00.0842.0286c1.961-.6067 3.9495-1.5219 6.0023-3.0294a.077.077 0 00.0313-.0552c.5004-5.177-.8382-9.6739-3.5485-13.6604a.061.061 0 00-.0312-.0286zM8.02 15.3312c-1.1825 0-2.1569-1.0857-2.1569-2.419 0-1.3332.9555-2.4189 2.157-2.4189 1.2108 0 2.1757 1.0952 2.1568 2.419 0 1.3332-.9555 2.4189-2.1569 2.4189zm7.9748 0c-1.1825 0-2.1569-1.0857-2.1569-2.419 0-1.3332.9554-2.4189 2.1569-2.4189 1.2108 0 2.1757 1.0952 2.1568 2.419 0 1.3332-.946 2.4189-2.1568 2.4189Z"/></svg>`,
    "font-awesome": `<svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path d="M4.3934 4.5c.6837-.4317 1.1379-1.194 1.1379-2.0625C5.5313 1.0913 4.4398 0 3.0938 0 1.7475 0 .6563 1.0913.6563 2.4375c0 .7805.3668 1.4753.9375 1.9214V24h3v-3h17.5126c.6834 0 1.2373-.554 1.2373-1.2373a1.237 1.237 0 0 0-.1066-.5027l-2.8934-6.51 2.8934-6.51a1.237 1.237 0 0 0 .1066-.5026c0-.6834-.5539-1.2374-1.2373-1.2374Z"/></svg>`,
    github: `<svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path d="M12 .297c-6.63 0-12 5.373-12 12 0 5.303 3.438 9.8 8.205 11.385.6.113.82-.258.82-.577 0-.285-.01-1.04-.015-2.04-3.338.724-4.042-1.61-4.042-1.61C4.422 18.07 3.633 17.7 3.633 17.7c-1.087-.744.084-.729.084-.729 1.205.084 1.838 1.236 1.838 1.236 1.07 1.835 2.809 1.305 3.495.998.108-.776.417-1.305.76-1.605-2.665-.3-5.466-1.332-5.466-5.93 0-1.31.465-2.38 1.235-3.22-.135-.303-.54-1.523.105-3.176 0 0 1.005-.322 3.3 1.23.96-.267 1.98-.399 3-.405 1.02.006 2.04.138 3 .405 2.28-1.552 3.285-1.23 3.285-1.23.645 1.653.24 2.873.12 3.176.765.84 1.23 1.91 1.23 3.22 0 4.61-2.805 5.625-5.475 5.92.42.36.81 1.096.81 2.22 0 1.606-.015 2.896-.015 3.286 0 .315.21.69.825.57C20.565 22.092 24 17.592 24 12.297c0-6.627-5.373-12-12-12"/></svg>`,
    mastodon: `<svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path d="M23.268 5.313c-.35-2.578-2.617-4.61-5.304-5.004C17.51.242 15.792 0 11.813 0h-.03c-3.98 0-4.835.242-5.288.309C3.882.692 1.496 2.518.917 5.127.64 6.412.61 7.837.661 9.143c.074 1.874.088 3.745.26 5.611.118 1.24.325 2.47.62 3.68.55 2.237 2.777 4.098 4.96 4.857 2.336.792 4.849.923 7.256.38.265-.061.527-.132.786-.213.585-.184 1.27-.39 1.774-.753a.057.057 0 0 0 .023-.043v-1.809a.052.052 0 0 0-.02-.041.053.053 0 0 0-.046-.01 20.282 20.282 0 0 1-4.709.545c-2.73 0-3.463-1.284-3.674-1.818a5.593 5.593 0 0 1-.319-1.433.053.053 0 0 1 .066-.054c1.517.363 3.072.546 4.632.546.376 0 .75 0 1.125-.01 1.57-.044 3.224-.124 4.768-.422.038-.008.077-.015.11-.024 2.435-.464 4.753-1.92 4.989-5.604.008-.145.03-1.52.03-1.67.002-.512.167-3.63-.024-5.545zm-3.748 9.195h-2.561V8.29c0-1.309-.55-1.976-1.67-1.976-1.23 0-1.846.79-1.846 2.35v3.403h-2.546V8.663c0-1.56-.617-2.35-1.848-2.35-1.112 0-1.668.668-1.67 1.977v6.218H4.822V8.102c0-1.31.337-2.35 1.011-3.12.696-.77 1.608-1.164 2.74-1.164 1.311 0 2.302.5 2.962 1.498l.638 1.06.638-1.06c.66-.999 1.65-1.498 2.96-1.498 1.13 0 2.043.395 2.74 1.164.675.77 1.012 1.81 1.012 3.12z"/></svg>`,
    react: `<svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path d="M14.23 12.004a2.236 2.236 0 0 1-2.235 2.236 2.236 2.236 0 0 1-2.236-2.236 2.236 2.236 0 0 1 2.235-2.236 2.236 2.236 0 0 1 2.236 2.236zm2.648-10.69c-1.346 0-3.107.96-4.888 2.622-1.78-1.653-3.542-2.602-4.887-2.602-.41 0-.783.093-1.106.278-1.375.793-1.683 3.264-.973 6.365C1.98 8.917 0 10.42 0 12.004c0 1.59 1.99 3.097 5.043 4.03-.704 3.113-.39 5.588.988 6.38.32.187.69.275 1.102.275 1.345 0 3.107-.96 4.888-2.624 1.78 1.654 3.542 2.603 4.887 2.603.41 0 .783-.09 1.106-.275 1.374-.792 1.683-3.263.973-6.365C22.02 15.096 24 13.59 24 12.004c0-1.59-1.99-3.097-5.043-4.032.704-3.11.39-5.587-.988-6.38-.318-.184-.688-.277-1.092-.278zm-.005 1.09v.006c.225 0 .406.044.558.127.666.382.955 1.835.73 3.704-.054.46-.142.945-.25 1.44-.96-.236-2.006-.417-3.107-.534-.66-.905-1.345-1.727-2.035-2.447 1.592-1.48 3.087-2.292 4.105-2.295zm-9.77.02c1.012 0 2.514.808 4.11 2.28-.686.72-1.37 1.537-2.02 2.442-1.107.117-2.154.298-3.113.538-.112-.49-.195-.964-.254-1.42-.23-1.868.054-3.32.714-3.707.19-.09.4-.127.563-.132zm4.882 3.05c.455.468.91.992 1.36 1.564-.44-.02-.89-.034-1.345-.034-.46 0-.915.01-1.36.034.44-.572.895-1.096 1.345-1.565zM12 8.1c.74 0 1.477.034 2.202.093.406.582.802 1.203 1.183 1.86.372.64.71 1.29 1.018 1.946-.308.655-.646 1.31-1.013 1.95-.38.66-.773 1.288-1.18 1.87-.728.063-1.466.098-2.21.098-.74 0-1.477-.035-2.202-.093-.406-.582-.802-1.204-1.183-1.86-.372-.64-.71-1.29-1.018-1.946.303-.657.646-1.313 1.013-1.954.38-.66.773-1.286 1.18-1.868.728-.064 1.466-.098 2.21-.098zm-3.635.254c-.24.377-.48.763-.704 1.16-.225.39-.435.782-.635 1.174-.265-.656-.49-1.31-.676-1.947.64-.15 1.315-.283 2.015-.386zm7.26 0c.695.103 1.365.23 2.006.387-.18.632-.405 1.282-.66 1.933-.2-.39-.41-.783-.64-1.174-.225-.392-.465-.774-.705-1.146zm3.063.675c.484.15.944.317 1.375.498 1.732.74 2.852 1.708 2.852 2.476-.005.768-1.125 1.74-2.857 2.475-.42.18-.88.342-1.355.493-.28-.958-.646-1.956-1.1-2.98.45-1.017.81-2.01 1.085-2.964zm-13.395.004c.278.96.645 1.957 1.1 2.98-.45 1.017-.812 2.01-1.086 2.964-.484-.15-.944-.318-1.37-.5-1.732-.737-2.852-1.706-2.852-2.474 0-.768 1.12-1.742 2.852-2.476.42-.18.88-.342 1.356-.494zm11.678 4.28c.265.657.49 1.312.676 1.948-.64.157-1.316.29-2.016.39.24-.375.48-.762.705-1.158.225-.39.435-.788.636-1.18zm-9.945.02c.2.392.41.783.64 1.175.23.39.465.772.705 1.143-.695-.102-1.365-.23-2.006-.386.18-.63.406-1.282.66-1.933zM17.92 16.32c.112.493.2.968.254 1.423.23 1.868-.054 3.32-.714 3.708-.147.09-.338.128-.563.128-1.012 0-2.514-.807-4.11-2.28.686-.72 1.37-1.536 2.02-2.44 1.107-.118 2.154-.3 3.113-.54zm-11.83.01c.96.234 2.006.415 3.107.532.66.905 1.345 1.727 2.035 2.446-1.595 1.483-3.092 2.295-4.11 2.295-.22-.005-.406-.05-.553-.132-.666-.38-.955-1.834-.73-3.703.054-.46.142-.944.25-1.438zm4.56.64c.44.02.89.034 1.345.034.46 0 .915-.01 1.36-.034-.44.572-.895 1.095-1.345 1.565-.455-.47-.91-.993-1.36-1.565z"/></svg>`,
    svelte: `<svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path d="M10.354 21.125a4.44 4.44 0 0 1-4.765-1.767 4.109 4.109 0 0 1-.703-3.107 3.898 3.898 0 0 1 .134-.522l.105-.321.287.21a7.21 7.21 0 0 0 2.186 1.092l.208.063-.02.208a1.253 1.253 0 0 0 .226.83 1.337 1.337 0 0 0 1.435.533 1.231 1.231 0 0 0 .343-.15l5.59-3.562a1.164 1.164 0 0 0 .524-.778 1.242 1.242 0 0 0-.211-.937 1.338 1.338 0 0 0-1.435-.533 1.23 1.23 0 0 0-.343.15l-2.133 1.36a4.078 4.078 0 0 1-1.135.499 4.44 4.44 0 0 1-4.765-1.766 4.108 4.108 0 0 1-.702-3.108 3.855 3.855 0 0 1 1.742-2.582l5.589-3.563a4.072 4.072 0 0 1 1.135-.499 4.44 4.44 0 0 1 4.765 1.767 4.109 4.109 0 0 1 .703 3.107 3.943 3.943 0 0 1-.134.522l-.105.321-.286-.21a7.204 7.204 0 0 0-2.187-1.093l-.208-.063.02-.207a1.255 1.255 0 0 0-.226-.831 1.337 1.337 0 0 0-1.435-.532 1.231 1.231 0 0 0-.343.15L8.62 9.368a1.162 1.162 0 0 0-.524.778 1.24 1.24 0 0 0 .211.937 1.338 1.338 0 0 0 1.435.533 1.235 1.235 0 0 0 .344-.151l2.132-1.36a4.067 4.067 0 0 1 1.135-.498 4.44 4.44 0 0 1 4.765 1.766 4.108 4.108 0 0 1 .702 3.108 3.857 3.857 0 0 1-1.742 2.583l-5.589 3.562a4.072 4.072 0 0 1-1.135.499m10.358-17.95C18.484-.015 14.082-.96 10.9 1.068L5.31 4.63a6.412 6.412 0 0 0-2.896 4.295 6.753 6.753 0 0 0 .666 4.336 6.43 6.43 0 0 0-.96 2.396 6.833 6.833 0 0 0 1.168 5.167c2.229 3.19 6.63 4.135 9.812 2.108l5.59-3.562a6.41 6.41 0 0 0 2.896-4.295 6.756 6.756 0 0 0-.665-4.336 6.429 6.429 0 0 0 .958-2.396 6.831 6.831 0 0 0-1.167-5.168Z"/></svg>`,
    threads: `<svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path d="M18.263 11.097c-.03-3.486-1.92-5.586-5.111-5.586-2.13 0-3.922.963-4.863 2.499l2.062 1.438c.535-.843 1.272-1.543 2.628-1.543 1.528 0 2.318.85 2.544 2.431a15 15 0 0 0-2.236-.173c-4.125 0-6.068 1.867-6.068 4.336s1.943 3.99 4.804 3.99c3.139 0 5.013-2.115 5.781-4.735.798.361 1.348 1.204 1.348 2.47 0 3.387-3.907 5.232-7.22 5.232-4.885 0-8.077-3.207-8.077-8.424 0-6.392 4.223-10.487 9.9-10.487 3.808 0 5.69 1.671 6.97 3.914l2.108-1.475C21.44 2.078 18.331 0 13.663 0 6.227 0 1.168 5.277 1.168 12.934c0 7 4.953 11.066 10.856 11.066 4.878 0 9.809-2.846 9.809-7.716 0-2.545-1.46-4.231-3.569-5.187m-6.33 4.855c-1.077 0-2.026-.512-2.026-1.453 0-1.483 1.822-1.934 3.606-1.934.678 0 1.34.045 1.927.173-.422 1.927-1.671 3.215-3.508 3.214Z"/></svg>`,
    vuejs: `<svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path d="M24,1.61H14.06L12,5.16,9.94,1.61H0L12,22.39ZM12,14.08,5.16,2.23H9.59L12,6.41l2.41-4.18h4.43Z"/></svg>`,
    "x-twitter": `<svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path d="M14.234 10.162 22.977 0h-2.072l-7.591 8.824L7.251 0H.258l9.168 13.343L.258 24H2.33l8.016-9.318L16.749 24h6.993zm-2.837 3.299-.929-1.329L3.076 1.56h3.182l5.965 8.532.929 1.329 7.754 11.09h-3.182z"/></svg>`
  };
  var brandsLibrary = {
    name: "brands",
    resolver: (name) => {
      const svg = icons[name];
      return svg ? dataUri(svg) : "";
    },
    mutator: (svg) => {
      if (!svg.hasAttribute("fill")) {
        svg.setAttribute("fill", "currentColor");
      }
    }
  };
  var library_brands_default = brandsLibrary;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.WA2HPE5W.js
  var MS_VERSION = "0.46.0";
  var STYLES = ["sharp", "outlined", "rounded"];
  var WEIGHTS = [100, 200, 300, 400, 500, 600, 700];
  var DEFAULT_WEIGHT = 400;
  function getIconStyle(family) {
    return STYLES.includes(family) ? family : "sharp";
  }
  function getIconWeight(weight) {
    const value = Number(weight);
    if (!Number.isFinite(value)) {
      return DEFAULT_WEIGHT;
    }
    return WEIGHTS.reduce(
      (closest, candidate) => Math.abs(candidate - value) < Math.abs(closest - value) ? candidate : closest
    );
  }
  function getIconFileName(name, variant) {
    return variant === "fill" ? `${name}-fill` : name;
  }
  function getIconUrl(name, family, variant, weight) {
    const style = getIconStyle(family);
    const resolvedWeight = getIconWeight(weight);
    const fileName = getIconFileName(name, variant);
    const iconBase = getIconPath();
    if (iconBase) {
      return `${iconBase}/${resolvedWeight}/${style}/${fileName}.svg`;
    }
    return `https://cdn.jsdelivr.net/npm/@material-symbols/svg-${resolvedWeight}@${MS_VERSION}/${style}/${fileName}.svg`;
  }
  var library = {
    name: "default",
    resolver: (name, family = "sharp", variant = "regular", _autoWidth = false, weight = DEFAULT_WEIGHT) => {
      return getIconUrl(name, family, variant, weight);
    },
    mutator: (svg) => {
      if (!svg.hasAttribute("fill")) {
        svg.setAttribute("fill", "currentColor");
      }
    }
  };
  var library_default_default = library;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.KRPQCOJS.js
  function dataUri2(svg) {
    return `data:image/svg+xml,${encodeURIComponent(svg)}`;
  }
  var icons2 = {
    //
    // Regular (outlined) variant
    //
    regular: {
      add: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M450-450H200v-60h250v-250h60v250h250v60H510v250h-60v-250Z"/></svg>`,
      audio_file: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M427-192q41 0 70-28.31T526-290v-202h116v-60H486v187q-11-9-26.38-14-15.39-5-32.62-5-39.27 0-66.13 27.5Q334-329 334-289.5t26.87 68.5q26.86 29 66.13 29ZM160-80v-800h421l219 219v581H160Zm391-554v-186H220v680h520v-494H551ZM220-820v186-186 680-680Z"/></svg>`,
      calendar_today: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M120-80v-740h125v-60h65v60h340v-60h65v60h125v740H120Zm60-60h600v-430H180v430Zm0-490h600v-130H180v130Zm0 0v-130 130Z"/></svg>`,
      cancel: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="m330-288 150-150 150 150 42-42-150-150 150-150-42-42-150 150-150-150-42 42 150 150-150 150 42 42ZM480-80q-82 0-155-31.5t-127.5-86Q143-252 111.5-325T80-480q0-83 31.5-156t86-127Q252-817 325-848.5T480-880q83 0 156 31.5T763-763q54 54 85.5 127T880-480q0 82-31.5 155T763-197.5q-54 54.5-127 86T480-80Zm0-60q142 0 241-99.5T820-480q0-142-99-241t-241-99q-141 0-240.5 99T140-480q0 141 99.5 240.5T480-140Zm0-340Z"/></svg>`,
      check: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M378-246 154-470l43-43 181 181 384-384 43 43-427 427Z"/></svg>`,
      close: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="m249-207-42-42 231-231-231-231 42-42 231 231 231-231 42 42-231 231 231 231-42 42-231-231-231 231Z"/></svg>`,
      close_fullscreen: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="m122-80-42-42 298-298H160v-60h320v320h-60v-218L122-80Zm358-400v-320h60v218l298-298 42 42-298 298h218v60H480Z"/></svg>`,
      closed_caption: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M120-160v-640h720v640H120Zm60-60h600v-520H180v520Zm60-141h202v-72h-50v22H290v-138h102v22h50v-72H240v238Zm279 0h202v-72h-50v22H569v-138h102v22h50v-72H519v238ZM180-220v-520 520Z"/></svg>`,
      closed_caption_disabled: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="m361-599 50 50H290v138h102v-22h50v72H240v-238h121ZM246-800h594v594l-60-60v-474H306l-60-60Zm475 367v72h-36l-50-50h36v-22h50Zm-50-94v-22H569v72l-50-50v-72h202v72h-50Zm-128 24Zm-126 46ZM163-797l57 57h-40v520h474L26-848l43-43L876-84l-43 43-119-119H120v-637h43Z"/></svg>`,
      code_blocks: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="m379-343 44-44-93-93 92-92-44-44-136 136 137 137Zm202 0 137-137-137-137-44 44 93 93-93 93 44 44ZM120-120v-720h720v720H120Zm60-60h600v-600H180v600Zm0 0v-600 600Z"/></svg>`,
      colorize: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M120-120v-168l377-377-72-72 41-41 92 92 166-166 128 128-166 166 92 92-41 41-72-72-377 377H120Zm60-60h87l355-355-87-87-355 355v87Zm463-420 124-124-43-43-124 124 43 43Zm0 0-43-43 43 43Z"/></svg>`,
      content_copy: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M240-200v-680h560v680H240Zm60-60h440v-560H300v560ZM120-80v-680h60v620h500v60H120Zm180-180v-560 560Z"/></svg>`,
      description: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M319-250h322v-60H319v60Zm0-170h322v-60H319v60ZM160-80v-800h421l219 219v581H160Zm391-554v-186H220v680h520v-494H551ZM220-820v186-186 680-680Z"/></svg>`,
      draft: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M160-80v-800h421l219 219v581H160Zm391-554v-186H220v680h520v-494H551ZM220-820v186-186 680-680Z"/></svg>`,
      drag_indicator: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M349.91-160q-28.91 0-49.41-20.59-20.5-20.59-20.5-49.5t20.59-49.41q20.59-20.5 49.5-20.5t49.41 20.59q20.5 20.59 20.5 49.5t-20.59 49.41q-20.59 20.5-49.5 20.5Zm260 0q-28.91 0-49.41-20.59-20.5-20.59-20.5-49.5t20.59-49.41q20.59-20.5 49.5-20.5t49.41 20.59q20.5 20.59 20.5 49.5t-20.59 49.41q-20.59 20.5-49.5 20.5Zm-260-250q-28.91 0-49.41-20.59-20.5-20.59-20.5-49.5t20.59-49.41q20.59-20.5 49.5-20.5t49.41 20.59q20.5 20.59 20.5 49.5t-20.59 49.41q-20.59 20.5-49.5 20.5Zm260 0q-28.91 0-49.41-20.59-20.5-20.59-20.5-49.5t20.59-49.41q20.59-20.5 49.5-20.5t49.41 20.59q20.5 20.59 20.5 49.5t-20.59 49.41q-20.59 20.5-49.5 20.5Zm-260-250q-28.91 0-49.41-20.59-20.5-20.59-20.5-49.5t20.59-49.41q20.59-20.5 49.5-20.5t49.41 20.59q20.5 20.59 20.5 49.5t-20.59 49.41q-20.59 20.5-49.5 20.5Zm260 0q-28.91 0-49.41-20.59-20.5-20.59-20.5-49.5t20.59-49.41q20.59-20.5 49.5-20.5t49.41 20.59q20.5 20.59 20.5 49.5t-20.59 49.41q-20.59 20.5-49.5 20.5Z"/></svg>`,
      fast_forward: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M104-240v-480l346 240-346 240Zm407 0v-480l346 240-346 240ZM164-480Zm407 0ZM164-355l181-125-181-125v250Zm407 0 181-125-181-125v250Z"/></svg>`,
      fast_rewind: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M854-240 508-480l346-240v480Zm-402 0L106-480l346-240v480Zm-60-240Zm402 0ZM392-355v-250L211-480l181 125Zm402 0v-250L613-480l181 125Z"/></svg>`,
      folder_zip: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M640-496v-92h92v92h-92Zm0 92h-92v-92h92v92Zm0 92v-92h92v92h-92ZM456-680l-60-60H140v520h408v-92h92v92h180v-460H640v92h-92v-92h-92ZM80-160v-640h341l60 60h399v580H80Zm60-60v-520 520Z"/></svg>`,
      help: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M511-258q11-11 11-27t-11-27q-11-11-27-11t-27 11q-11 11-11 27t11 27q11 11 27 11t27-11Zm-62-135h59q0-26 6.5-47.5T555-490q31-26 44-51t13-55q0-53-34.5-85T486-713q-49 0-86.5 24.5T345-621l53 20q11-28 33-43.5t52-15.5q34 0 55 18.5t21 47.5q0 22-13 41.5T508-512q-30 26-44.5 51.5T449-393Zm31 313q-82 0-155-31.5t-127.5-86Q143-252 111.5-325T80-480q0-83 31.5-156t86-127Q252-817 325-848.5T480-880q83 0 156 31.5T763-763q54 54 85.5 127T880-480q0 82-31.5 155T763-197.5q-54 54.5-127 86T480-80Zm0-60q142 0 241-99.5T820-480q0-142-99-241t-241-99q-141 0-240.5 99T140-480q0 141 99.5 240.5T480-140Zm0-340Z"/></svg>`,
      horizontal_rule: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M160-450v-60h640v60H160Z"/></svg>`,
      image: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M236-277h489L578-473 446-302l-93-127-117 152ZM120-120v-720h720v720H120Zm60-60h600v-600H180v600Zm0 0v-600 600Z"/></svg>`,
      keyboard_arrow_down: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M480-344 240-584l43-43 197 197 197-197 43 43-240 240Z"/></svg>`,
      keyboard_arrow_left: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M561-240 320-481l241-241 43 43-198 198 198 198-43 43Z"/></svg>`,
      keyboard_arrow_right: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M530-481 332-679l43-43 241 241-241 241-43-43 198-198Z"/></svg>`,
      keyboard_double_arrow_left: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M453-241 213-481l240-240 42 42-198 198 198 198-42 42Zm253 0L466-481l240-240 42 42-198 198 198 198-42 42Z"/></svg>`,
      keyboard_double_arrow_right: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M411-481 213-679l42-42 240 240-240 240-42-42 198-198Zm253 0L466-679l42-42 240 240-240 240-42-42 198-198Z"/></svg>`,
      more_horiz: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M207.86-432Q188-432 174-446.14t-14-34Q160-500 174.14-514t34-14Q228-528 242-513.86t14 34Q256-460 241.86-446t-34 14Zm272 0Q460-432 446-446.14t-14-34Q432-500 446.14-514t34-14Q500-528 514-513.86t14 34Q528-460 513.86-446t-34 14Zm272 0Q732-432 718-446.14t-14-34Q704-500 718.14-514t34-14Q772-528 786-513.86t14 34Q800-460 785.86-446t-34 14Z"/></svg>`,
      more_vert: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M479.86-160Q460-160 446-174.14t-14-34Q432-228 446.14-242t34-14Q500-256 514-241.86t14 34Q528-188 513.86-174t-34 14Zm0-272Q460-432 446-446.14t-14-34Q432-500 446.14-514t34-14Q500-528 514-513.86t14 34Q528-460 513.86-446t-34 14Zm0-272Q460-704 446-718.14t-14-34Q432-772 446.14-786t34-14Q500-800 514-785.86t14 34Q528-732 513.86-718t-34 14Z"/></svg>`,
      open_in_full: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M120-120v-300h60v198l558-558H540v-60h300v300h-60v-198L222-180h198v60H120Z"/></svg>`,
      pause: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M525-200v-560h235v560H525Zm-325 0v-560h235v560H200Zm385-60h115v-440H585v440Zm-325 0h115v-440H260v440Zm0-440v440-440Zm325 0v440-440Z"/></svg>`,
      person: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M372-523q-42-42-42-108t42-108q42-42 108-42t108 42q42 42 42 108t-42 108q-42 42-108 42t-108-42ZM160-160v-94q0-38 19-65t49-41q67-30 128.5-45T480-420q62 0 123 15.5T731-360q31 14 50 41t19 65v94H160Zm60-60h520v-34q0-16-9.5-30.5T707-306q-64-31-117-42.5T480-360q-57 0-111 11.5T252-306q-14 7-23 21.5t-9 30.5v34Zm324.5-346.5Q570-592 570-631t-25.5-64.5Q519-721 480-721t-64.5 25.5Q390-670 390-631t25.5 64.5Q441-541 480-541t64.5-25.5ZM480-631Zm0 411Z"/></svg>`,
      picture_as_pdf: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M331-431h37v-83h70l15-15v-92l-15-15H331v205Zm37-120v-48h48v48h-48Zm129 120h106l15-15v-175l-15-15H497v205Zm37-37v-131h47v131h-47Zm133 37h37v-83h50v-37h-50v-48h50v-37h-87v205ZM200-200v-680h680v680H200Zm60-60h560v-560H260v560ZM80-80v-680h60v620h620v60H80Zm180-740v560-560Z"/></svg>`,
      picture_in_picture_alt: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M80-160v-640h800v640H80Zm60-60h680v-520H140v520Zm0 0v-520 520Zm306-45h335v-257H446v257Zm60-60v-137h215v137H506Z"/></svg>`,
      play_arrow: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M320-203v-560l440 280-440 280Zm60-280Zm0 171 269-171-269-171v342Z"/></svg>`,
      play_circle: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="m383-310 267-170-267-170v340Zm97 230q-82 0-155-31.5t-127.5-86Q143-252 111.5-325T80-480q0-83 31.5-156t86-127Q252-817 325-848.5T480-880q83 0 156 31.5T763-763q54 54 85.5 127T880-480q0 82-31.5 155T763-197.5q-54 54.5-127 86T480-80Zm0-60q142 0 241-99.5T820-480q0-142-99-241t-241-99q-141 0-240.5 99T140-480q0 141 99.5 240.5T480-140Zm0-340Z"/></svg>`,
      radio_button_unchecked: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M480-80q-82 0-155-31.5t-127.5-86Q143-252 111.5-325T80-480q0-83 31.5-156t86-127Q252-817 325-848.5T480-880q83 0 156 31.5T763-763q54 54 85.5 127T880-480q0 82-31.5 155T763-197.5q-54 54.5-127 86T480-80Zm0-60q142 0 241-99.5T820-480q0-142-99-241t-241-99q-141 0-240.5 99T140-480q0 141 99.5 240.5T480-140Z"/></svg>`,
      remove: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M200-450v-60h560v60H200Z"/></svg>`,
      schedule: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="m627-287 45-45-159-160v-201h-60v225l174 181ZM480-80q-82 0-155-31.5t-127.5-86Q143-252 111.5-325T80-480q0-82 31.5-155t86-127.5Q252-817 325-848.5T480-880q82 0 155 31.5t127.5 86Q817-708 848.5-635T880-480q0 82-31.5 155t-86 127.5Q708-143 635-111.5T480-80Zm0-400Zm0 340q140 0 240-100t100-240q0-140-100-240T480-820q-140 0-240 100T140-480q0 140 100 240t240 100Z"/></svg>`,
      settings: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="m388-80-20-126q-19-7-40-19t-37-25l-118 54-93-164 108-79q-2-9-2.5-20.5T185-480q0-9 .5-20.5T188-521L80-600l93-164 118 54q16-13 37-25t40-18l20-127h184l20 126q19 7 40.5 18.5T669-710l118-54 93 164-108 77q2 10 2.5 21.5t.5 21.5q0 10-.5 21t-2.5 21l108 78-93 164-118-54q-16 13-36.5 25.5T592-206L572-80H388Zm48-60h88l14-112q33-8 62.5-25t53.5-41l106 46 40-72-94-69q4-17 6.5-33.5T715-480q0-17-2-33.5t-7-33.5l94-69-40-72-106 46q-23-26-52-43.5T538-708l-14-112h-88l-14 112q-34 7-63.5 24T306-642l-106-46-40 72 94 69q-4 17-6.5 33.5T245-480q0 17 2.5 33.5T254-413l-94 69 40 72 106-46q24 24 53.5 41t62.5 25l14 112Zm44-210q54 0 92-38t38-92q0-54-38-92t-92-38q-54 0-92 38t-38 92q0 54 38 92t92 38Zm0-130Z"/></svg>`,
      skip_next: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M680-240v-480h60v480h-60Zm-460 0v-480l346 240-346 240Zm60-240Zm0 125 181-125-181-125v250Z"/></svg>`,
      skip_previous: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M220-240v-480h60v480h-60Zm520 0L394-480l346-240v480Zm-60-240Zm0 125v-250L499-480l181 125Z"/></svg>`,
      slideshow: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="m387-311 261-169-261-169v338ZM120-120v-720h720v720H120Zm60-60h600v-600H180v600Zm0 0v-600 600Z"/></svg>`,
      speed: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M473.5-303.5Q517-305 537-336l216-339-335 219q-30 20-32 64t21 67q23 23 66.5 21.5ZM478-799q57 0 119 18.5T716-717l-52 37q-45-30-96.5-44.5T477.98-739q-140.47 0-239.23 100.22Q140-538.57 140-396.02 140-351 152.5-305q12.5 46 35.5 85h579q22-36 35-84t13-94q0-42-12.5-90.5T758-578l39-52q38 56 57 112.5T875-404q2 60-12 113t-41 98q-12 23-25.5 28t-33.5 5H192q-17 0-33.5-8.5T134-193q-26-48-40-97.5T80-396q0-83 31.5-156.5t85.5-128Q251-735 323.68-767T478-799Zm-9 331Z"/></svg>`,
      star: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="m323-245 157-94 157 95-42-178 138-120-182-16-71-168-71 167-182 16 138 120-42 178Zm-90 125 65-281L80-590l288-25 112-265 112 265 288 25-218 189 65 281-247-149-247 149Zm247-355Z"/></svg>`,
      table_chart: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M120-121v-719h720v719H120Zm60-518h600v-141H180v141Zm0 458h130v-398H180v398Zm470 0h130v-398H650v398Zm-280 0h220v-398H370v398Z"/></svg>`,
      upload: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M450-313v-371L330-564l-43-43 193-193 193 193-43 43-120-120v371h-60ZM160-160v-203h60v143h520v-143h60v203H160Z"/></svg>`,
      video_file: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M317-240h245v-90l80 46v-152l-80 46v-90H317v240ZM160-80v-800h421l219 219v581H160Zm391-554v-186H220v680h520v-494H551ZM220-820v186-186 680-680Z"/></svg>`,
      visibility: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M600.5-379.5Q650-429 650-500t-49.5-120.5Q551-670 480-670t-120.5 49.5Q310-571 310-500t49.5 120.5Q409-330 480-330t120.5-49.5Zm-200-41Q368-453 368-500t32.5-79.5Q433-612 480-612t79.5 32.5Q592-547 592-500t-32.5 79.5Q527-388 480-388t-79.5-32.5ZM216-283Q98-366 40-500q58-134 176-217t264-83q146 0 264 83t176 217q-58 134-176 217t-264 83q-146 0-264-83Zm264-217Zm222.5 174.5Q804-391 857-500q-53-109-154.5-174.5T480-740q-121 0-222.5 65.5T102-500q54 109 155.5 174.5T480-260q121 0 222.5-65.5Z"/></svg>`,
      visibility_off: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="m629-419-44-44q26-71-27-118t-115-24l-44-44q17-11 38-16t43-5q71 0 120.5 49.5T650-500q0 22-5.5 43.5T629-419Zm129 129-40-40q49-36 85.5-80.5T857-500q-50-111-150-175.5T490-740q-42 0-86 8t-69 19l-46-47q35-16 89.5-28T485-800q143 0 261.5 81.5T920-500q-26 64-67 117t-95 93Zm58 226L648-229q-35 14-79 21.5t-89 7.5q-146 0-265-81.5T40-500q20-52 55.5-101.5T182-696L56-822l42-43 757 757-39 44ZM223-654q-37 27-71.5 71T102-500q51 111 153.5 175.5T488-260q33 0 65-4t48-12l-64-64q-11 5-27 7.5t-30 2.5q-70 0-120-49t-50-121q0-15 2.5-30t7.5-27l-97-97Zm305 142Zm-116 58Z"/></svg>`,
      volume_down: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M200-360v-240h160l200-200v640L360-360H200Zm420 48v-337q54 17 87 64t33 105q0 59-33 105t-87 63ZM500-648 387-540H260v120h127l113 109v-337ZM378-480Z"/></svg>`,
      volume_off: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M813-56 681-188q-28 20-60.5 34.5T553-131v-62q23-7 44.5-15.5T638-231L473-397v237L273-360H113v-240h156L49-820l43-43 764 763-43 44Zm-36-232-43-43q20-34 29.5-71.92Q773-440.85 773-481q0-103.32-60-184.66T553-769v-62q124 28 202 125.5T833-481q0 51-14 100t-42 93ZM643-422l-90-90v-130q47 22 73.5 66t26.5 96q0 15-2.5 29.5T643-422ZM473-592 369-696l104-104v208Zm-60 286v-150l-84-84H173v120h126l114 114Zm-42-192Z"/></svg>`,
      volume_up: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M560-131v-62q97-28 158.5-107.5T780-481q0-101-61-181T560-769v-62q124 28 202 125.5T840-481q0 127-78 224.5T560-131ZM120-360v-240h160l200-200v640L280-360H120Zm420 48v-337q55 17 87.5 64T660-480q0 57-33 104t-87 64ZM420-648 307-540H180v120h127l113 109v-337Zm-94 168Z"/></svg>`
    },
    //
    // Fill variant — only icons whose filled cut differs from the outlined one
    //
    fill: {
      audio_file: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M427-192q41 0 70-28.31T526-290v-202h116v-60H486v187q-11-9-26.38-14-15.39-5-32.62-5-39.27 0-66.13 27.5Q334-329 334-289.5t26.87 68.5q26.86 29 66.13 29ZM160-80v-800h421l219 219v581H160Zm391-554h189L551-820v186Z"/></svg>`,
      calendar_today: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M120-80v-740h125v-60h65v60h340v-60h65v60h125v740H120Zm60-60h600v-430H180v430Z"/></svg>`,
      cancel: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="m330-288 150-150 150 150 42-42-150-150 150-150-42-42-150 150-150-150-42 42 150 150-150 150 42 42ZM480-80q-82 0-155-31.5t-127.5-86Q143-252 111.5-325T80-480q0-83 31.5-156t86-127Q252-817 325-848.5T480-880q83 0 156 31.5T763-763q54 54 85.5 127T880-480q0 82-31.5 155T763-197.5q-54 54.5-127 86T480-80Z"/></svg>`,
      closed_caption: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M120-160v-640h720v640H120Zm120-201h202v-72h-50v22H290v-138h102v22h50v-72H240v238Zm279 0h202v-72h-50v22H569v-138h102v22h50v-72H519v238Z"/></svg>`,
      closed_caption_disabled: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M120-160v-595l-94-93 43-43L876-84l-43 43-119-119H120Zm720-47L686-361h35v-72h-50v22h-36l-66-66v-72h102v22h50v-72H519v72L246-800h594v593ZM240-361h202v-72h-50v22H290v-138h35l-47-47h-38v235Z"/></svg>`,
      code_blocks: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="m379-343 44-44-93-93 92-92-44-44-136 136 137 137Zm202 0 137-137-137-137-44 44 93 93-93 93 44 44ZM120-120v-720h720v720H120Z"/></svg>`,
      colorize: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M120-120v-168l377-377-72-72 41-41 92 92 166-166 128 128-166 166 92 92-41 41-72-72-377 377H120Zm60-60h87l355-355-87-87-355 355v87Z"/></svg>`,
      content_copy: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M240-200v-680h560v680H240ZM120-80v-680h60v620h500v60H120Z"/></svg>`,
      description: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M319-250h322v-60H319v60Zm0-170h322v-60H319v60ZM160-80v-800h421l219 219v581H160Zm391-554h189L551-820v186Z"/></svg>`,
      draft: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M160-80v-800h421l219 219v581H160Zm391-554h189L551-820v186Z"/></svg>`,
      drag_indicator: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M358.41-160Q326-160 303-183.29t-23-56Q280-272 303.23-295t55.86-23q31.91 0 55.41 23.29 23.5 23.29 23.5 56T414.41-183q-23.59 23-56 23Zm241 0Q567-160 544-183.29t-23-56Q521-272 544.23-295t55.86-23q31.91 0 55.41 23.29 23.5 23.29 23.5 56T655.41-183q-23.59 23-56 23Zm-241-241Q326-401 303-423.09t-23-57q0-32.62 23.23-55.77Q326.46-559 359.09-559q31.91 0 55.41 23.23 23.5 23.23 23.5 55.86Q438-445 414.41-423t-56 22Zm241 0Q567-401 544-423.09t-23-57q0-32.62 23.23-55.77Q567.46-559 600.09-559q31.91 0 55.41 23.23 23.5 23.23 23.5 55.86Q679-445 655.41-423t-56 22Zm-241-241Q326-642 303-665.29t-23-56Q280-754 303.23-777t55.86-23q31.91 0 55.41 23.29 23.5 23.29 23.5 56T414.41-665q-23.59 23-56 23Zm241 0Q567-642 544-665.29t-23-56Q521-754 544.23-777t55.86-23q31.91 0 55.41 23.29 23.5 23.29 23.5 56T655.41-665q-23.59 23-56 23Z"/></svg>`,
      fast_forward: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M104-240v-480l346 240-346 240Zm407 0v-480l346 240-346 240Z"/></svg>`,
      fast_rewind: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M854-240 508-480l346-240v480Zm-402 0L106-480l346-240v480Z"/></svg>`,
      folder_zip: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M80-160v-640h341l60 60h399v580H80Zm468-60h92v-92h92v-92h-92v-92h92v-92h-92v-92h-92v92h92v92h-92v92h92v92h-92v92Z"/></svg>`,
      help: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M511-258q11-11 11-27t-11-27q-11-11-27-11t-27 11q-11 11-11 27t11 27q11 11 27 11t27-11Zm-62-135h59q0-26 6.5-47.5T555-490q31-26 44-51t13-55q0-53-34.5-85T486-713q-49 0-86.5 24.5T345-621l53 20q11-28 33-43.5t52-15.5q34 0 55 18.5t21 47.5q0 22-13 41.5T508-512q-30 26-44.5 51.5T449-393Zm31 313q-82 0-155-31.5t-127.5-86Q143-252 111.5-325T80-480q0-83 31.5-156t86-127Q252-817 325-848.5T480-880q83 0 156 31.5T763-763q54 54 85.5 127T880-480q0 82-31.5 155T763-197.5q-54 54.5-127 86T480-80Z"/></svg>`,
      image: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M236-277h489L578-473 446-302l-93-127-117 152ZM120-120v-720h720v720H120Z"/></svg>`,
      pause: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M555-200v-560h175v560H555Zm-325 0v-560h175v560H230Z"/></svg>`,
      person: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M372-523q-42-42-42-108t42-108q42-42 108-42t108 42q42 42 42 108t-42 108q-42 42-108 42t-108-42ZM160-160v-94q0-38 19-65t49-41q67-30 128.5-45T480-420q62 0 123 15.5T731-360q31 14 50 41t19 65v94H160Z"/></svg>`,
      picture_as_pdf: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M331-431h37v-83h70l15-15v-92l-15-15H331v205Zm37-120v-48h48v48h-48Zm129 120h106l15-15v-175l-15-15H497v205Zm37-37v-131h47v131h-47Zm133 37h37v-83h50v-37h-50v-48h50v-37h-87v205ZM200-200v-680h680v680H200ZM80-80v-680h60v620h620v60H80Z"/></svg>`,
      picture_in_picture_alt: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M80-160v-640h800v640H80Zm366-105h335v-257H446v257Zm168-129Z"/></svg>`,
      play_arrow: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M320-203v-560l440 280-440 280Z"/></svg>`,
      play_circle: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="m383-310 267-170-267-170v340Zm97 230q-82 0-155-31.5t-127.5-86Q143-252 111.5-325T80-480q0-83 31.5-156t86-127Q252-817 325-848.5T480-880q83 0 156 31.5T763-763q54 54 85.5 127T880-480q0 82-31.5 155T763-197.5q-54 54.5-127 86T480-80Z"/></svg>`,
      schedule: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="m627-287 45-45-159-160v-201h-60v225l174 181ZM480-80q-82 0-155-31.5t-127.5-86Q143-252 111.5-325T80-480q0-82 31.5-155t86-127.5Q252-817 325-848.5T480-880q82 0 155 31.5t127.5 86Q817-708 848.5-635T880-480q0 82-31.5 155t-86 127.5Q708-143 635-111.5T480-80Z"/></svg>`,
      settings: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="m388-80-20-126q-19-7-40-19t-37-25l-118 54-93-164 108-79q-2-9-2.5-20.5T185-480q0-9 .5-20.5T188-521L80-600l93-164 118 54q16-13 37-25t40-18l20-127h184l20 126q19 7 40.5 18.5T669-710l118-54 93 164-108 77q2 10 2.5 21.5t.5 21.5q0 10-.5 21t-2.5 21l108 78-93 164-118-54q-16 13-36.5 25.5T592-206L572-80H388Zm92-270q54 0 92-38t38-92q0-54-38-92t-92-38q-54 0-92 38t-38 92q0 54 38 92t92 38Z"/></svg>`,
      skip_next: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M680-240v-480h60v480h-60Zm-460 0v-480l346 240-346 240Z"/></svg>`,
      skip_previous: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M220-240v-480h60v480h-60Zm520 0L394-480l346-240v480Z"/></svg>`,
      slideshow: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="m387-311 261-169-261-169v338ZM120-120v-720h720v720H120Z"/></svg>`,
      speed: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M418-340q25 25 63 23.5t55-27.5l221-333-333 221q-26 18-28.5 54.5T418-340ZM192-160q-18 0-34-8.5T134-193q-26-48-40-100T80-399q0-83 31.5-156T197-682.5q54-54.5 126.5-86T478-800q83 0 156.5 31.5t128 86Q817-628 848.5-555T880-399q0 54-13 106.5T827-193q-9 16-25 24.5t-34 8.5H192Z"/></svg>`,
      star: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="m233-120 65-281L80-590l288-25 112-265 112 265 288 25-218 189 65 281-247-149-247 149Z"/></svg>`,
      table_chart: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M120-639v-201h720v201H120Zm0 518v-458h190v458H120Zm530 0v-458h190v458H650Zm-280 0v-458h220v458H370Z"/></svg>`,
      video_file: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M160-80v-800h421l219 219v581H160Zm391-554h189L551-820v186ZM317-240h245v-90l80 46v-152l-80 46v-90H317v240Z"/></svg>`,
      visibility: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M600.5-379.5Q650-429 650-500t-49.5-120.5Q551-670 480-670t-120.5 49.5Q310-571 310-500t49.5 120.5Q409-330 480-330t120.5-49.5Zm-200-41Q368-453 368-500t32.5-79.5Q433-612 480-612t79.5 32.5Q592-547 592-500t-32.5 79.5Q527-388 480-388t-79.5-32.5ZM216-283Q98-366 40-500q58-134 176-217t264-83q146 0 264 83t176 217q-58 134-176 217t-264 83q-146 0-264-83Z"/></svg>`,
      visibility_off: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M816-64 648-229q-35 14-79 21.5t-89 7.5q-146 0-265-81.5T40-500q20-52 55.5-101.5T182-696L56-822l42-43 757 757-39 44ZM480-330q14 0 30-2.5t27-7.5L320-557q-5 12-7.5 27t-2.5 30q0 72 50 121t120 49Zm278 40L629-419q10-16 15.5-37.5T650-500q0-71-49.5-120.5T480-670q-22 0-43 5t-38 16L289-760q35-16 89.5-28T485-800q143 0 261.5 81.5T920-500q-26 64-67 117t-95 93ZM585-463 443-605q29-11 60-4.5t54 28.5q23 23 32 51.5t-4 66.5Z"/></svg>`,
      volume_down: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M200-360v-240h160l200-200v640L360-360H200Zm420 48v-337q54 17 87 64t33 105q0 59-33 105t-87 63Z"/></svg>`,
      volume_off: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M813-56 681-188q-28 20-60.5 34.5T553-131v-62q23-7 44.5-15.5T638-231L473-397v237L273-360H113v-240h156L49-820l43-43 764 763-43 44Zm-36-232-43-43q20-34 29.5-72t9.5-78q0-103-60-184.5T553-769v-62q124 28 202 125.5T833-481q0 51-14 100t-42 93ZM643-422l-90-90v-130q47 22 73.5 66t26.5 96q0 15-2.5 29.5T643-422ZM473-592 369-696l104-104v208Z"/></svg>`,
      volume_up: `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M560-131v-62q97-28 158.5-107.5T780-481q0-101-61-181T560-769v-62q124 28 202 125.5T840-481q0 127-78 224.5T560-131ZM120-360v-240h160l200-200v640L280-360H120Zm420 48v-337q55 17 87.5 64T660-480q0 57-33 104t-87 64Z"/></svg>`
    }
  };
  var systemLibrary = {
    name: "system",
    resolver: (name, _family = "sharp", variant = "regular") => {
      const collection = icons2[variant] ?? icons2.regular;
      const svg = collection[name] ?? icons2.regular[name] ?? icons2.regular.help;
      if (svg) {
        return dataUri2(svg);
      }
      return "";
    },
    mutator: (svg) => {
      if (!svg.hasAttribute("fill")) {
        svg.setAttribute("fill", "currentColor");
      }
    }
  };
  var library_system_default = systemLibrary;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.U4ZB4RTC.js
  var defaultIconFamily = "sharp";
  var registry = [library_default_default, library_system_default, library_brands_default];
  var watchedIcons = /* @__PURE__ */ new Set();
  function watchIcon(icon) {
    watchedIcons.add(icon);
  }
  function unwatchIcon(icon) {
    watchedIcons.delete(icon);
  }
  function getIconLibrary(name) {
    return registry.find((lib) => lib.name === name);
  }
  function registerIconLibrary(name, options2) {
    unregisterIconLibrary(name);
    registry.push({
      name,
      resolver: options2.resolver,
      mutator: options2.mutator,
      spriteSheet: options2.spriteSheet
    });
    watchedIcons.forEach((icon) => {
      if (icon.library === name) {
        icon.setIcon();
      }
    });
  }
  function unregisterIconLibrary(name) {
    registry = registry.filter((lib) => lib.name !== name);
  }
  function setDefaultIconFamily(family) {
    defaultIconFamily = family;
    watchedIcons.forEach((icon) => icon.setIcon());
  }
  function getDefaultIconFamily() {
    return defaultIconFamily;
  }

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.HBAVB7LX.js
  var __defProp2 = Object.defineProperty;
  var __getOwnPropDesc2 = Object.getOwnPropertyDescriptor;
  var __typeError = (msg) => {
    throw TypeError(msg);
  };
  var __decorateClass = (decorators, target, key, kind) => {
    var result = kind > 1 ? void 0 : kind ? __getOwnPropDesc2(target, key) : target;
    for (var i10 = decorators.length - 1, decorator; i10 >= 0; i10--)
      if (decorator = decorators[i10])
        result = (kind ? decorator(target, key, result) : decorator(result)) || result;
    if (kind && result) __defProp2(target, key, result);
    return result;
  };
  var __accessCheck = (obj, member, msg) => member.has(obj) || __typeError("Cannot " + msg);
  var __privateGet = (obj, member, getter) => (__accessCheck(obj, member, "read from private field"), getter ? getter.call(obj) : member.get(obj));
  var __privateAdd = (obj, member, value) => member.has(obj) ? __typeError("Cannot add the same private member more than once") : member instanceof WeakSet ? member.add(obj) : member.set(obj, value);
  var __privateSet = (obj, member, value, setter) => (__accessCheck(obj, member, "write to private field"), setter ? setter.call(obj, value) : member.set(obj, value), value);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.QNZZU272.js
  var CsAccordionAfterExpandEvent = class extends Event {
    constructor(detail) {
      super("cs-accordion-after-expand", { bubbles: true, cancelable: false, composed: true });
      this.detail = detail;
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.25QWNZW6.js
  var CsAccordionCollapseEvent = class extends Event {
    constructor(detail) {
      super("cs-accordion-collapse", { bubbles: true, cancelable: true, composed: true });
      this.detail = detail;
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.ZTDXG7TX.js
  var CsAccordionExpandEvent = class extends Event {
    constructor(detail) {
      super("cs-accordion-expand", { bubbles: true, cancelable: true, composed: true });
      this.detail = detail;
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.XSNXTL3J.js
  var CsAccordionAfterCollapseEvent = class extends Event {
    constructor(detail) {
      super("cs-accordion-after-collapse", { bubbles: true, cancelable: false, composed: true });
      this.detail = detail;
    }
  };

  // node_modules/@lit/reactive-element/css-tag.js
  var t = globalThis;
  var e = t.ShadowRoot && (void 0 === t.ShadyCSS || t.ShadyCSS.nativeShadow) && "adoptedStyleSheets" in Document.prototype && "replace" in CSSStyleSheet.prototype;
  var s = /* @__PURE__ */ Symbol();
  var o = /* @__PURE__ */ new WeakMap();
  var n = class {
    constructor(t8, e12, o13) {
      if (this._$cssResult$ = true, o13 !== s) throw Error("CSSResult is not constructable. Use `unsafeCSS` or `css` instead.");
      this.cssText = t8, this.t = e12;
    }
    get styleSheet() {
      let t8 = this.o;
      const s7 = this.t;
      if (e && void 0 === t8) {
        const e12 = void 0 !== s7 && 1 === s7.length;
        e12 && (t8 = o.get(s7)), void 0 === t8 && ((this.o = t8 = new CSSStyleSheet()).replaceSync(this.cssText), e12 && o.set(s7, t8));
      }
      return t8;
    }
    toString() {
      return this.cssText;
    }
  };
  var r = (t8) => new n("string" == typeof t8 ? t8 : t8 + "", void 0, s);
  var i = (t8, ...e12) => {
    const o13 = 1 === t8.length ? t8[0] : e12.reduce((e13, s7, o14) => e13 + ((t9) => {
      if (true === t9._$cssResult$) return t9.cssText;
      if ("number" == typeof t9) return t9;
      throw Error("Value passed to 'css' function must be a 'css' function result: " + t9 + ". Use 'unsafeCSS' to pass non-literal values, but take care to ensure page security.");
    })(s7) + t8[o14 + 1], t8[0]);
    return new n(o13, t8, s);
  };
  var S = (s7, o13) => {
    if (e) s7.adoptedStyleSheets = o13.map((t8) => t8 instanceof CSSStyleSheet ? t8 : t8.styleSheet);
    else for (const e12 of o13) {
      const o14 = document.createElement("style"), n9 = t.litNonce;
      void 0 !== n9 && o14.setAttribute("nonce", n9), o14.textContent = e12.cssText, s7.appendChild(o14);
    }
  };
  var c = e ? (t8) => t8 : (t8) => t8 instanceof CSSStyleSheet ? ((t9) => {
    let e12 = "";
    for (const s7 of t9.cssRules) e12 += s7.cssText;
    return r(e12);
  })(t8) : t8;

  // node_modules/@lit/reactive-element/reactive-element.js
  var { is: i2, defineProperty: e2, getOwnPropertyDescriptor: h, getOwnPropertyNames: r2, getOwnPropertySymbols: o2, getPrototypeOf: n2 } = Object;
  var a = globalThis;
  var c2 = a.trustedTypes;
  var l = c2 ? c2.emptyScript : "";
  var p = a.reactiveElementPolyfillSupport;
  var d = (t8, s7) => t8;
  var u = { toAttribute(t8, s7) {
    switch (s7) {
      case Boolean:
        t8 = t8 ? l : null;
        break;
      case Object:
      case Array:
        t8 = null == t8 ? t8 : JSON.stringify(t8);
    }
    return t8;
  }, fromAttribute(t8, s7) {
    let i10 = t8;
    switch (s7) {
      case Boolean:
        i10 = null !== t8;
        break;
      case Number:
        i10 = null === t8 ? null : Number(t8);
        break;
      case Object:
      case Array:
        try {
          i10 = JSON.parse(t8);
        } catch (t9) {
          i10 = null;
        }
    }
    return i10;
  } };
  var f = (t8, s7) => !i2(t8, s7);
  var b = { attribute: true, type: String, converter: u, reflect: false, useDefault: false, hasChanged: f };
  Symbol.metadata ??= /* @__PURE__ */ Symbol("metadata"), a.litPropertyMetadata ??= /* @__PURE__ */ new WeakMap();
  var y = class extends HTMLElement {
    static addInitializer(t8) {
      this._$Ei(), (this.l ??= []).push(t8);
    }
    static get observedAttributes() {
      return this.finalize(), this._$Eh && [...this._$Eh.keys()];
    }
    static createProperty(t8, s7 = b) {
      if (s7.state && (s7.attribute = false), this._$Ei(), this.prototype.hasOwnProperty(t8) && ((s7 = Object.create(s7)).wrapped = true), this.elementProperties.set(t8, s7), !s7.noAccessor) {
        const i10 = /* @__PURE__ */ Symbol(), h3 = this.getPropertyDescriptor(t8, i10, s7);
        void 0 !== h3 && e2(this.prototype, t8, h3);
      }
    }
    static getPropertyDescriptor(t8, s7, i10) {
      const { get: e12, set: r9 } = h(this.prototype, t8) ?? { get() {
        return this[s7];
      }, set(t9) {
        this[s7] = t9;
      } };
      return { get: e12, set(s8) {
        const h3 = e12?.call(this);
        r9?.call(this, s8), this.requestUpdate(t8, h3, i10);
      }, configurable: true, enumerable: true };
    }
    static getPropertyOptions(t8) {
      return this.elementProperties.get(t8) ?? b;
    }
    static _$Ei() {
      if (this.hasOwnProperty(d("elementProperties"))) return;
      const t8 = n2(this);
      t8.finalize(), void 0 !== t8.l && (this.l = [...t8.l]), this.elementProperties = new Map(t8.elementProperties);
    }
    static finalize() {
      if (this.hasOwnProperty(d("finalized"))) return;
      if (this.finalized = true, this._$Ei(), this.hasOwnProperty(d("properties"))) {
        const t9 = this.properties, s7 = [...r2(t9), ...o2(t9)];
        for (const i10 of s7) this.createProperty(i10, t9[i10]);
      }
      const t8 = this[Symbol.metadata];
      if (null !== t8) {
        const s7 = litPropertyMetadata.get(t8);
        if (void 0 !== s7) for (const [t9, i10] of s7) this.elementProperties.set(t9, i10);
      }
      this._$Eh = /* @__PURE__ */ new Map();
      for (const [t9, s7] of this.elementProperties) {
        const i10 = this._$Eu(t9, s7);
        void 0 !== i10 && this._$Eh.set(i10, t9);
      }
      this.elementStyles = this.finalizeStyles(this.styles);
    }
    static finalizeStyles(s7) {
      const i10 = [];
      if (Array.isArray(s7)) {
        const e12 = new Set(s7.flat(1 / 0).reverse());
        for (const s8 of e12) i10.unshift(c(s8));
      } else void 0 !== s7 && i10.push(c(s7));
      return i10;
    }
    static _$Eu(t8, s7) {
      const i10 = s7.attribute;
      return false === i10 ? void 0 : "string" == typeof i10 ? i10 : "string" == typeof t8 ? t8.toLowerCase() : void 0;
    }
    constructor() {
      super(), this._$Ep = void 0, this.isUpdatePending = false, this.hasUpdated = false, this._$Em = null, this._$Ev();
    }
    _$Ev() {
      this._$ES = new Promise((t8) => this.enableUpdating = t8), this._$AL = /* @__PURE__ */ new Map(), this._$E_(), this.requestUpdate(), this.constructor.l?.forEach((t8) => t8(this));
    }
    addController(t8) {
      (this._$EO ??= /* @__PURE__ */ new Set()).add(t8), void 0 !== this.renderRoot && this.isConnected && t8.hostConnected?.();
    }
    removeController(t8) {
      this._$EO?.delete(t8);
    }
    _$E_() {
      const t8 = /* @__PURE__ */ new Map(), s7 = this.constructor.elementProperties;
      for (const i10 of s7.keys()) this.hasOwnProperty(i10) && (t8.set(i10, this[i10]), delete this[i10]);
      t8.size > 0 && (this._$Ep = t8);
    }
    createRenderRoot() {
      const t8 = this.shadowRoot ?? this.attachShadow(this.constructor.shadowRootOptions);
      return S(t8, this.constructor.elementStyles), t8;
    }
    connectedCallback() {
      this.renderRoot ??= this.createRenderRoot(), this.enableUpdating(true), this._$EO?.forEach((t8) => t8.hostConnected?.());
    }
    enableUpdating(t8) {
    }
    disconnectedCallback() {
      this._$EO?.forEach((t8) => t8.hostDisconnected?.());
    }
    attributeChangedCallback(t8, s7, i10) {
      this._$AK(t8, i10);
    }
    _$ET(t8, s7) {
      const i10 = this.constructor.elementProperties.get(t8), e12 = this.constructor._$Eu(t8, i10);
      if (void 0 !== e12 && true === i10.reflect) {
        const h3 = (void 0 !== i10.converter?.toAttribute ? i10.converter : u).toAttribute(s7, i10.type);
        this._$Em = t8, null == h3 ? this.removeAttribute(e12) : this.setAttribute(e12, h3), this._$Em = null;
      }
    }
    _$AK(t8, s7) {
      const i10 = this.constructor, e12 = i10._$Eh.get(t8);
      if (void 0 !== e12 && this._$Em !== e12) {
        const t9 = i10.getPropertyOptions(e12), h3 = "function" == typeof t9.converter ? { fromAttribute: t9.converter } : void 0 !== t9.converter?.fromAttribute ? t9.converter : u;
        this._$Em = e12;
        const r9 = h3.fromAttribute(s7, t9.type);
        this[e12] = r9 ?? this._$Ej?.get(e12) ?? r9, this._$Em = null;
      }
    }
    requestUpdate(t8, s7, i10, e12 = false, h3) {
      if (void 0 !== t8) {
        const r9 = this.constructor;
        if (false === e12 && (h3 = this[t8]), i10 ??= r9.getPropertyOptions(t8), !((i10.hasChanged ?? f)(h3, s7) || i10.useDefault && i10.reflect && h3 === this._$Ej?.get(t8) && !this.hasAttribute(r9._$Eu(t8, i10)))) return;
        this.C(t8, s7, i10);
      }
      false === this.isUpdatePending && (this._$ES = this._$EP());
    }
    C(t8, s7, { useDefault: i10, reflect: e12, wrapped: h3 }, r9) {
      i10 && !(this._$Ej ??= /* @__PURE__ */ new Map()).has(t8) && (this._$Ej.set(t8, r9 ?? s7 ?? this[t8]), true !== h3 || void 0 !== r9) || (this._$AL.has(t8) || (this.hasUpdated || i10 || (s7 = void 0), this._$AL.set(t8, s7)), true === e12 && this._$Em !== t8 && (this._$Eq ??= /* @__PURE__ */ new Set()).add(t8));
    }
    async _$EP() {
      this.isUpdatePending = true;
      try {
        await this._$ES;
      } catch (t9) {
        Promise.reject(t9);
      }
      const t8 = this.scheduleUpdate();
      return null != t8 && await t8, !this.isUpdatePending;
    }
    scheduleUpdate() {
      return this.performUpdate();
    }
    performUpdate() {
      if (!this.isUpdatePending) return;
      if (!this.hasUpdated) {
        if (this.renderRoot ??= this.createRenderRoot(), this._$Ep) {
          for (const [t10, s8] of this._$Ep) this[t10] = s8;
          this._$Ep = void 0;
        }
        const t9 = this.constructor.elementProperties;
        if (t9.size > 0) for (const [s8, i10] of t9) {
          const { wrapped: t10 } = i10, e12 = this[s8];
          true !== t10 || this._$AL.has(s8) || void 0 === e12 || this.C(s8, void 0, i10, e12);
        }
      }
      let t8 = false;
      const s7 = this._$AL;
      try {
        t8 = this.shouldUpdate(s7), t8 ? (this.willUpdate(s7), this._$EO?.forEach((t9) => t9.hostUpdate?.()), this.update(s7)) : this._$EM();
      } catch (s8) {
        throw t8 = false, this._$EM(), s8;
      }
      t8 && this._$AE(s7);
    }
    willUpdate(t8) {
    }
    _$AE(t8) {
      this._$EO?.forEach((t9) => t9.hostUpdated?.()), this.hasUpdated || (this.hasUpdated = true, this.firstUpdated(t8)), this.updated(t8);
    }
    _$EM() {
      this._$AL = /* @__PURE__ */ new Map(), this.isUpdatePending = false;
    }
    get updateComplete() {
      return this.getUpdateComplete();
    }
    getUpdateComplete() {
      return this._$ES;
    }
    shouldUpdate(t8) {
      return true;
    }
    update(t8) {
      this._$Eq &&= this._$Eq.forEach((t9) => this._$ET(t9, this[t9])), this._$EM();
    }
    updated(t8) {
    }
    firstUpdated(t8) {
    }
  };
  y.elementStyles = [], y.shadowRootOptions = { mode: "open" }, y[d("elementProperties")] = /* @__PURE__ */ new Map(), y[d("finalized")] = /* @__PURE__ */ new Map(), p?.({ ReactiveElement: y }), (a.reactiveElementVersions ??= []).push("2.1.2");

  // node_modules/lit-html/lit-html.js
  var t2 = globalThis;
  var i3 = (t8) => t8;
  var s2 = t2.trustedTypes;
  var e3 = s2 ? s2.createPolicy("lit-html", { createHTML: (t8) => t8 }) : void 0;
  var h2 = "$lit$";
  var o3 = `lit$${Math.random().toFixed(9).slice(2)}$`;
  var n3 = "?" + o3;
  var r3 = `<${n3}>`;
  var l2 = document;
  var c3 = () => l2.createComment("");
  var a2 = (t8) => null === t8 || "object" != typeof t8 && "function" != typeof t8;
  var u2 = Array.isArray;
  var d2 = (t8) => u2(t8) || "function" == typeof t8?.[Symbol.iterator];
  var f2 = "[ 	\n\f\r]";
  var v = /<(?:(!--|\/[^a-zA-Z])|(\/?[a-zA-Z][^>\s]*)|(\/?$))/g;
  var _ = /-->/g;
  var m = />/g;
  var p2 = RegExp(`>|${f2}(?:([^\\s"'>=/]+)(${f2}*=${f2}*(?:[^ 	
\f\r"'\`<>=]|("|')|))|$)`, "g");
  var g = /'/g;
  var $ = /"/g;
  var y2 = /^(?:script|style|textarea|title)$/i;
  var x = (t8) => (i10, ...s7) => ({ _$litType$: t8, strings: i10, values: s7 });
  var b2 = x(1);
  var w = x(2);
  var T = x(3);
  var E = /* @__PURE__ */ Symbol.for("lit-noChange");
  var A = /* @__PURE__ */ Symbol.for("lit-nothing");
  var C = /* @__PURE__ */ new WeakMap();
  var P = l2.createTreeWalker(l2, 129);
  function V(t8, i10) {
    if (!u2(t8) || !t8.hasOwnProperty("raw")) throw Error("invalid template strings array");
    return void 0 !== e3 ? e3.createHTML(i10) : i10;
  }
  var N = (t8, i10) => {
    const s7 = t8.length - 1, e12 = [];
    let n9, l6 = 2 === i10 ? "<svg>" : 3 === i10 ? "<math>" : "", c6 = v;
    for (let i11 = 0; i11 < s7; i11++) {
      const s8 = t8[i11];
      let a4, u4, d3 = -1, f3 = 0;
      for (; f3 < s8.length && (c6.lastIndex = f3, u4 = c6.exec(s8), null !== u4); ) f3 = c6.lastIndex, c6 === v ? "!--" === u4[1] ? c6 = _ : void 0 !== u4[1] ? c6 = m : void 0 !== u4[2] ? (y2.test(u4[2]) && (n9 = RegExp("</" + u4[2], "g")), c6 = p2) : void 0 !== u4[3] && (c6 = p2) : c6 === p2 ? ">" === u4[0] ? (c6 = n9 ?? v, d3 = -1) : void 0 === u4[1] ? d3 = -2 : (d3 = c6.lastIndex - u4[2].length, a4 = u4[1], c6 = void 0 === u4[3] ? p2 : '"' === u4[3] ? $ : g) : c6 === $ || c6 === g ? c6 = p2 : c6 === _ || c6 === m ? c6 = v : (c6 = p2, n9 = void 0);
      const x2 = c6 === p2 && t8[i11 + 1].startsWith("/>") ? " " : "";
      l6 += c6 === v ? s8 + r3 : d3 >= 0 ? (e12.push(a4), s8.slice(0, d3) + h2 + s8.slice(d3) + o3 + x2) : s8 + o3 + (-2 === d3 ? i11 : x2);
    }
    return [V(t8, l6 + (t8[s7] || "<?>") + (2 === i10 ? "</svg>" : 3 === i10 ? "</math>" : "")), e12];
  };
  var S2 = class _S {
    constructor({ strings: t8, _$litType$: i10 }, e12) {
      let r9;
      this.parts = [];
      let l6 = 0, a4 = 0;
      const u4 = t8.length - 1, d3 = this.parts, [f3, v2] = N(t8, i10);
      if (this.el = _S.createElement(f3, e12), P.currentNode = this.el.content, 2 === i10 || 3 === i10) {
        const t9 = this.el.content.firstChild;
        t9.replaceWith(...t9.childNodes);
      }
      for (; null !== (r9 = P.nextNode()) && d3.length < u4; ) {
        if (1 === r9.nodeType) {
          if (r9.hasAttributes()) for (const t9 of r9.getAttributeNames()) if (t9.endsWith(h2)) {
            const i11 = v2[a4++], s7 = r9.getAttribute(t9).split(o3), e13 = /([.?@])?(.*)/.exec(i11);
            d3.push({ type: 1, index: l6, name: e13[2], strings: s7, ctor: "." === e13[1] ? I : "?" === e13[1] ? L : "@" === e13[1] ? z : H }), r9.removeAttribute(t9);
          } else t9.startsWith(o3) && (d3.push({ type: 6, index: l6 }), r9.removeAttribute(t9));
          if (y2.test(r9.tagName)) {
            const t9 = r9.textContent.split(o3), i11 = t9.length - 1;
            if (i11 > 0) {
              r9.textContent = s2 ? s2.emptyScript : "";
              for (let s7 = 0; s7 < i11; s7++) r9.append(t9[s7], c3()), P.nextNode(), d3.push({ type: 2, index: ++l6 });
              r9.append(t9[i11], c3());
            }
          }
        } else if (8 === r9.nodeType) if (r9.data === n3) d3.push({ type: 2, index: l6 });
        else {
          let t9 = -1;
          for (; -1 !== (t9 = r9.data.indexOf(o3, t9 + 1)); ) d3.push({ type: 7, index: l6 }), t9 += o3.length - 1;
        }
        l6++;
      }
    }
    static createElement(t8, i10) {
      const s7 = l2.createElement("template");
      return s7.innerHTML = t8, s7;
    }
  };
  function M(t8, i10, s7 = t8, e12) {
    if (i10 === E) return i10;
    let h3 = void 0 !== e12 ? s7._$Co?.[e12] : s7._$Cl;
    const o13 = a2(i10) ? void 0 : i10._$litDirective$;
    return h3?.constructor !== o13 && (h3?._$AO?.(false), void 0 === o13 ? h3 = void 0 : (h3 = new o13(t8), h3._$AT(t8, s7, e12)), void 0 !== e12 ? (s7._$Co ??= [])[e12] = h3 : s7._$Cl = h3), void 0 !== h3 && (i10 = M(t8, h3._$AS(t8, i10.values), h3, e12)), i10;
  }
  var R = class {
    constructor(t8, i10) {
      this._$AV = [], this._$AN = void 0, this._$AD = t8, this._$AM = i10;
    }
    get parentNode() {
      return this._$AM.parentNode;
    }
    get _$AU() {
      return this._$AM._$AU;
    }
    u(t8) {
      const { el: { content: i10 }, parts: s7 } = this._$AD, e12 = (t8?.creationScope ?? l2).importNode(i10, true);
      P.currentNode = e12;
      let h3 = P.nextNode(), o13 = 0, n9 = 0, r9 = s7[0];
      for (; void 0 !== r9; ) {
        if (o13 === r9.index) {
          let i11;
          2 === r9.type ? i11 = new k(h3, h3.nextSibling, this, t8) : 1 === r9.type ? i11 = new r9.ctor(h3, r9.name, r9.strings, this, t8) : 6 === r9.type && (i11 = new Z(h3, this, t8)), this._$AV.push(i11), r9 = s7[++n9];
        }
        o13 !== r9?.index && (h3 = P.nextNode(), o13++);
      }
      return P.currentNode = l2, e12;
    }
    p(t8) {
      let i10 = 0;
      for (const s7 of this._$AV) void 0 !== s7 && (void 0 !== s7.strings ? (s7._$AI(t8, s7, i10), i10 += s7.strings.length - 2) : s7._$AI(t8[i10])), i10++;
    }
  };
  var k = class _k {
    get _$AU() {
      return this._$AM?._$AU ?? this._$Cv;
    }
    constructor(t8, i10, s7, e12) {
      this.type = 2, this._$AH = A, this._$AN = void 0, this._$AA = t8, this._$AB = i10, this._$AM = s7, this.options = e12, this._$Cv = e12?.isConnected ?? true;
    }
    get parentNode() {
      let t8 = this._$AA.parentNode;
      const i10 = this._$AM;
      return void 0 !== i10 && 11 === t8?.nodeType && (t8 = i10.parentNode), t8;
    }
    get startNode() {
      return this._$AA;
    }
    get endNode() {
      return this._$AB;
    }
    _$AI(t8, i10 = this) {
      t8 = M(this, t8, i10), a2(t8) ? t8 === A || null == t8 || "" === t8 ? (this._$AH !== A && this._$AR(), this._$AH = A) : t8 !== this._$AH && t8 !== E && this._(t8) : void 0 !== t8._$litType$ ? this.$(t8) : void 0 !== t8.nodeType ? this.T(t8) : d2(t8) ? this.k(t8) : this._(t8);
    }
    O(t8) {
      return this._$AA.parentNode.insertBefore(t8, this._$AB);
    }
    T(t8) {
      this._$AH !== t8 && (this._$AR(), this._$AH = this.O(t8));
    }
    _(t8) {
      this._$AH !== A && a2(this._$AH) ? this._$AA.nextSibling.data = t8 : this.T(l2.createTextNode(t8)), this._$AH = t8;
    }
    $(t8) {
      const { values: i10, _$litType$: s7 } = t8, e12 = "number" == typeof s7 ? this._$AC(t8) : (void 0 === s7.el && (s7.el = S2.createElement(V(s7.h, s7.h[0]), this.options)), s7);
      if (this._$AH?._$AD === e12) this._$AH.p(i10);
      else {
        const t9 = new R(e12, this), s8 = t9.u(this.options);
        t9.p(i10), this.T(s8), this._$AH = t9;
      }
    }
    _$AC(t8) {
      let i10 = C.get(t8.strings);
      return void 0 === i10 && C.set(t8.strings, i10 = new S2(t8)), i10;
    }
    k(t8) {
      u2(this._$AH) || (this._$AH = [], this._$AR());
      const i10 = this._$AH;
      let s7, e12 = 0;
      for (const h3 of t8) e12 === i10.length ? i10.push(s7 = new _k(this.O(c3()), this.O(c3()), this, this.options)) : s7 = i10[e12], s7._$AI(h3), e12++;
      e12 < i10.length && (this._$AR(s7 && s7._$AB.nextSibling, e12), i10.length = e12);
    }
    _$AR(t8 = this._$AA.nextSibling, s7) {
      for (this._$AP?.(false, true, s7); t8 !== this._$AB; ) {
        const s8 = i3(t8).nextSibling;
        i3(t8).remove(), t8 = s8;
      }
    }
    setConnected(t8) {
      void 0 === this._$AM && (this._$Cv = t8, this._$AP?.(t8));
    }
  };
  var H = class {
    get tagName() {
      return this.element.tagName;
    }
    get _$AU() {
      return this._$AM._$AU;
    }
    constructor(t8, i10, s7, e12, h3) {
      this.type = 1, this._$AH = A, this._$AN = void 0, this.element = t8, this.name = i10, this._$AM = e12, this.options = h3, s7.length > 2 || "" !== s7[0] || "" !== s7[1] ? (this._$AH = Array(s7.length - 1).fill(new String()), this.strings = s7) : this._$AH = A;
    }
    _$AI(t8, i10 = this, s7, e12) {
      const h3 = this.strings;
      let o13 = false;
      if (void 0 === h3) t8 = M(this, t8, i10, 0), o13 = !a2(t8) || t8 !== this._$AH && t8 !== E, o13 && (this._$AH = t8);
      else {
        const e13 = t8;
        let n9, r9;
        for (t8 = h3[0], n9 = 0; n9 < h3.length - 1; n9++) r9 = M(this, e13[s7 + n9], i10, n9), r9 === E && (r9 = this._$AH[n9]), o13 ||= !a2(r9) || r9 !== this._$AH[n9], r9 === A ? t8 = A : t8 !== A && (t8 += (r9 ?? "") + h3[n9 + 1]), this._$AH[n9] = r9;
      }
      o13 && !e12 && this.j(t8);
    }
    j(t8) {
      t8 === A ? this.element.removeAttribute(this.name) : this.element.setAttribute(this.name, t8 ?? "");
    }
  };
  var I = class extends H {
    constructor() {
      super(...arguments), this.type = 3;
    }
    j(t8) {
      this.element[this.name] = t8 === A ? void 0 : t8;
    }
  };
  var L = class extends H {
    constructor() {
      super(...arguments), this.type = 4;
    }
    j(t8) {
      this.element.toggleAttribute(this.name, !!t8 && t8 !== A);
    }
  };
  var z = class extends H {
    constructor(t8, i10, s7, e12, h3) {
      super(t8, i10, s7, e12, h3), this.type = 5;
    }
    _$AI(t8, i10 = this) {
      if ((t8 = M(this, t8, i10, 0) ?? A) === E) return;
      const s7 = this._$AH, e12 = t8 === A && s7 !== A || t8.capture !== s7.capture || t8.once !== s7.once || t8.passive !== s7.passive, h3 = t8 !== A && (s7 === A || e12);
      e12 && this.element.removeEventListener(this.name, this, s7), h3 && this.element.addEventListener(this.name, this, t8), this._$AH = t8;
    }
    handleEvent(t8) {
      "function" == typeof this._$AH ? this._$AH.call(this.options?.host ?? this.element, t8) : this._$AH.handleEvent(t8);
    }
  };
  var Z = class {
    constructor(t8, i10, s7) {
      this.element = t8, this.type = 6, this._$AN = void 0, this._$AM = i10, this.options = s7;
    }
    get _$AU() {
      return this._$AM._$AU;
    }
    _$AI(t8) {
      M(this, t8);
    }
  };
  var j = { M: h2, P: o3, A: n3, C: 1, L: N, R, D: d2, V: M, I: k, H, N: L, U: z, B: I, F: Z };
  var B = t2.litHtmlPolyfillSupport;
  B?.(S2, k), (t2.litHtmlVersions ??= []).push("3.3.3");
  var D = (t8, i10, s7) => {
    const e12 = s7?.renderBefore ?? i10;
    let h3 = e12._$litPart$;
    if (void 0 === h3) {
      const t9 = s7?.renderBefore ?? null;
      e12._$litPart$ = h3 = new k(i10.insertBefore(c3(), t9), t9, void 0, s7 ?? {});
    }
    return h3._$AI(t8), h3;
  };

  // node_modules/lit-element/lit-element.js
  var s3 = globalThis;
  var i4 = class extends y {
    constructor() {
      super(...arguments), this.renderOptions = { host: this }, this._$Do = void 0;
    }
    createRenderRoot() {
      const t8 = super.createRenderRoot();
      return this.renderOptions.renderBefore ??= t8.firstChild, t8;
    }
    update(t8) {
      const r9 = this.render();
      this.hasUpdated || (this.renderOptions.isConnected = this.isConnected), super.update(t8), this._$Do = D(r9, this.renderRoot, this.renderOptions);
    }
    connectedCallback() {
      super.connectedCallback(), this._$Do?.setConnected(true);
    }
    disconnectedCallback() {
      super.disconnectedCallback(), this._$Do?.setConnected(false);
    }
    render() {
      return E;
    }
  };
  i4._$litElement$ = true, i4["finalized"] = true, s3.litElementHydrateSupport?.({ LitElement: i4 });
  var o4 = s3.litElementPolyfillSupport;
  o4?.({ LitElement: i4 });
  (s3.litElementVersions ??= []).push("4.2.2");

  // node_modules/lit-html/is-server.js
  var o5 = false;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.PSI3YOEP.js
  var accordion_styles_default = i`
  @layer cs-component {
    :host {
      display: block;
      background-color: var(--cs-color-surface-default);
      border: var(--cs-panel-border-width) var(--cs-panel-border-style) var(--cs-color-surface-border);
      border-radius: var(--cs-panel-border-radius);
      overflow: hidden;
    }

    /* Appearance modifiers */
    :host([appearance='outlined']) {
      background-color: var(--cs-color-surface-default);
      border-color: var(--cs-color-surface-border);
    }

    :host([appearance='filled']) {
      border-color: transparent;
    }

    :host([appearance='filled-outlined']) {
      background-color: var(--cs-color-neutral-fill-quiet);
      border-color: var(--cs-color-neutral-border-quiet);
    }

    :host([appearance='plain']) {
      background-color: transparent;
      border-color: transparent;
      border-radius: 0;
    }
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.3QN4KTE6.js
  function watch(propertyName, options2) {
    const resolvedOptions = {
      waitUntilFirstUpdate: false,
      ...options2
    };
    return (proto, decoratedFnName) => {
      const { update: update2 } = proto;
      const watchedProperties = Array.isArray(propertyName) ? propertyName : [propertyName];
      proto.update = function(changedProps) {
        watchedProperties.forEach((property) => {
          const key = property;
          if (changedProps.has(key)) {
            const oldValue = changedProps.get(key);
            const newValue = this[key];
            if (oldValue !== newValue) {
              if (!resolvedOptions.waitUntilFirstUpdate || this.hasUpdated) {
                this[decoratedFnName](oldValue, newValue);
              }
            }
          }
        });
        update2.call(this, changedProps);
      };
    };
  }

  // node_modules/@lit/reactive-element/decorators/custom-element.js
  var t3 = (t8) => (e12, o13) => {
    void 0 !== o13 ? o13.addInitializer(() => {
      customElements.define(t8, e12);
    }) : customElements.define(t8, e12);
  };

  // node_modules/@lit/reactive-element/decorators/property.js
  var o6 = { attribute: true, type: String, converter: u, reflect: false, hasChanged: f };
  var r4 = (t8 = o6, e12, r9) => {
    const { kind: n9, metadata: i10 } = r9;
    let s7 = globalThis.litPropertyMetadata.get(i10);
    if (void 0 === s7 && globalThis.litPropertyMetadata.set(i10, s7 = /* @__PURE__ */ new Map()), "setter" === n9 && ((t8 = Object.create(t8)).wrapped = true), s7.set(r9.name, t8), "accessor" === n9) {
      const { name: o13 } = r9;
      return { set(r10) {
        const n10 = e12.get.call(this);
        e12.set.call(this, r10), this.requestUpdate(o13, n10, t8, true, r10);
      }, init(e13) {
        return void 0 !== e13 && this.C(o13, void 0, t8, e13), e13;
      } };
    }
    if ("setter" === n9) {
      const { name: o13 } = r9;
      return function(r10) {
        const n10 = this[o13];
        e12.call(this, r10), this.requestUpdate(o13, n10, t8, true, r10);
      };
    }
    throw Error("Unsupported decorator location: " + n9);
  };
  function n4(t8) {
    return (e12, o13) => "object" == typeof o13 ? r4(t8, e12, o13) : ((t9, e13, o14) => {
      const r9 = e13.hasOwnProperty(o14);
      return e13.constructor.createProperty(o14, t9), r9 ? Object.getOwnPropertyDescriptor(e13, o14) : void 0;
    })(t8, e12, o13);
  }

  // node_modules/@lit/reactive-element/decorators/state.js
  function r5(r9) {
    return n4({ ...r9, state: true, attribute: false });
  }

  // node_modules/@lit/reactive-element/decorators/event-options.js
  function t4(t8) {
    return (n9, o13) => {
      const c6 = "function" == typeof n9 ? n9 : n9[o13];
      Object.assign(c6, t8);
    };
  }

  // node_modules/@lit/reactive-element/decorators/base.js
  var e4 = (e12, t8, c6) => (c6.configurable = true, c6.enumerable = true, Reflect.decorate && "object" != typeof t8 && Object.defineProperty(e12, t8, c6), c6);

  // node_modules/@lit/reactive-element/decorators/query.js
  function e5(e12, r9) {
    return (n9, s7, i10) => {
      const o13 = (t8) => t8.renderRoot?.querySelector(e12) ?? null;
      if (r9) {
        const { get: e13, set: r10 } = "object" == typeof s7 ? n9 : i10 ?? /* @__PURE__ */ (() => {
          const t8 = /* @__PURE__ */ Symbol();
          return { get() {
            return this[t8];
          }, set(e14) {
            this[t8] = e14;
          } };
        })();
        return e4(n9, s7, { get() {
          let t8 = e13.call(this);
          return void 0 === t8 && (t8 = o13(this), (null !== t8 || this.hasUpdated) && r10.call(this, t8)), t8;
        } });
      }
      return e4(n9, s7, { get() {
        return o13(this);
      } });
    };
  }

  // node_modules/@lit/reactive-element/decorators/query-async.js
  function r6(r9) {
    return (n9, e12) => e4(n9, e12, { async get() {
      return await this.updateComplete, this.renderRoot?.querySelector(r9) ?? null;
    } });
  }

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.4H5TQFVZ.js
  var host_styles_default = i`
  :host {
    box-sizing: border-box;
  }

  :host *,
  :host *::before,
  :host *::after {
    box-sizing: inherit;
  }

  [hidden],
  :host([hidden]) {
    display: none !important;
  }
`;
  var HAS_ENDING_COLON = /;\s+$/;
  function camelToKebab(str) {
    return str.replace(/[A-Z]/g, (c6) => `-${c6.toLowerCase()}`);
  }
  function buildStyleAttribute(options2) {
    const { property: property2, value, element } = options2;
    if (value) {
      let style = element.getAttribute("style") || "";
      if (style) {
        if (!style.match(HAS_ENDING_COLON)) {
          style += ";";
        }
        style += " ";
      }
      const str = `${property2}: ${value}`;
      if (style.includes(str)) {
        return;
      }
      return `${style}${str};`;
    }
    return null;
  }
  var _hasRecordedInitialProperties;
  var CornerstoneElement = class extends i4 {
    constructor() {
      super();
      __privateAdd(this, _hasRecordedInitialProperties, false);
      this.initialReflectedProperties = /* @__PURE__ */ new Map();
      this.didSSR = o5 || Boolean(this.shadowRoot);
      this.customStates = {
        /** Adds or removes the specified custom state. */
        set: (customState, active) => {
          if (!this.internals?.states) {
            return;
          }
          try {
            if (active) {
              this.internals.states.add(customState);
            } else {
              this.internals.states.delete(customState);
            }
          } catch (e12) {
            if (String(e12).includes("must start with '--'")) {
              console.error("Your browser implements an outdated version of CustomStateSet. Consider using a polyfill");
            } else {
              throw e12;
            }
          }
        },
        /** Determines whether or not the element currently has the specified state. */
        has: (customState) => {
          if (!this.internals?.states) {
            return false;
          }
          try {
            return this.internals.states.has(customState);
          } catch {
            return false;
          }
        }
      };
      try {
        this.internals = this.attachInternals();
      } catch {
        console.error("Element internals are not supported in your browser. Consider using a polyfill");
      }
      this.customStates.set("cs-defined", true);
      const Self = this.constructor;
      for (const [property2, spec] of Self.elementProperties) {
        if (spec.default === "inherit" && spec.initial !== void 0 && typeof property2 === "string") {
          this.customStates.set(`initial-${property2}-${spec.initial}`, true);
        }
      }
    }
    /** Prepends host styles to the component's styles. */
    static get styles() {
      const styles = Array.isArray(this.css) ? this.css : this.css ? [this.css] : [];
      return [host_styles_default, ...styles];
    }
    connectedCallback() {
      super.connectedCallback();
      if (!this.didSSR) {
        this.shadowRoot?.prepend(
          document.createComment(
            ` Cornerstone: https://cruglobal.github.io/cornerstone-design-system/components/${this.localName.replace("cs-", "")} `
          )
        );
      }
      if (this.didSSR) {
        this.updateComplete.then(() => {
          this.shadowRoot?.prepend(
            document.createComment(
              ` Cornerstone: https://cruglobal.github.io/cornerstone-design-system/components/${this.localName.replace("cs-", "")} `
            )
          );
        });
      }
    }
    attributeChangedCallback(name, oldValue, newValue) {
      if (!__privateGet(this, _hasRecordedInitialProperties)) {
        this.constructor.elementProperties.forEach(
          (obj, prop) => {
            if (obj.reflect && this[prop] != null) {
              this.initialReflectedProperties.set(prop, this[prop]);
            }
          }
        );
        __privateSet(this, _hasRecordedInitialProperties, true);
      }
      super.attributeChangedCallback(name, oldValue, newValue);
    }
    willUpdate(changedProperties) {
      super.willUpdate(changedProperties);
      this.initialReflectedProperties.forEach((value, prop) => {
        if (changedProperties.has(prop) && this[prop] == null) {
          this[prop] = value;
        }
      });
    }
    firstUpdated(changedProperties) {
      super.firstUpdated(changedProperties);
      if (this.didSSR) {
        this.shadowRoot?.querySelectorAll("slot").forEach((slotElement) => {
          slotElement.dispatchEvent(new Event("slotchange", { bubbles: true, composed: false, cancelable: false }));
        });
      }
    }
    update(changedProperties) {
      try {
        super.update(changedProperties);
      } catch (e12) {
        if (this.didSSR && !this.hasUpdated) {
          const event = new Event("lit-hydration-error", { bubbles: true, composed: true, cancelable: false });
          event.error = e12;
          this.dispatchEvent(event);
        }
        throw e12;
      }
    }
    /**
     * @internal
     * Internal way to set styles across both client and server
     */
    setStyle(property2, value) {
      if (!this.style) {
        const str = buildStyleAttribute({
          // because this is going to be serialized to an HTML style attribute, need to transform the casing.
          property: camelToKebab(property2),
          value,
          element: this
        });
        if (str) {
          this.setAttribute("style", str);
        }
        return;
      }
      this.style[property2] = value;
    }
    /**
     * @internal
     * Internal way to set a CSS custom property across both client and server.
     */
    setStyleProperty(property2, value) {
      if (!this.style) {
        const str = buildStyleAttribute({
          // because this is going to be serialized to an HTML style attribute, need to transform the casing.
          property: property2,
          value,
          element: this
        });
        if (str) {
          this.setAttribute("style", str);
        }
        return;
      }
      this.style.setProperty(property2, value);
    }
    /**
     * @internal Given a native event, this function cancels it and dispatches it again from the host element using the desired
     * event options.
     */
    relayNativeEvent(event, eventOptions) {
      event.stopImmediatePropagation();
      this.dispatchEvent(
        new event.constructor(event.type, {
          ...event,
          ...eventOptions
        })
      );
    }
  };
  _hasRecordedInitialProperties = /* @__PURE__ */ new WeakMap();
  __decorateClass([
    n4()
  ], CornerstoneElement.prototype, "dir", 2);
  __decorateClass([
    n4()
  ], CornerstoneElement.prototype, "lang", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true, attribute: "did-ssr" })
  ], CornerstoneElement.prototype, "didSSR", 2);
  var thisBuild = true ? "unbundled" : "unknown";
  var REGISTRY_KEY = /* @__PURE__ */ Symbol.for("cornerstone.registeringBuilds");
  function registeringBuilds() {
    const global = globalThis;
    const existing = global[REGISTRY_KEY];
    if (existing) {
      return existing;
    }
    const created = /* @__PURE__ */ new Map();
    global[REGISTRY_KEY] = created;
    return created;
  }
  function customElement(tagName) {
    return (cls) => {
      if (customElements.get(tagName)) {
        const firstBuild = registeringBuilds().get(tagName) ?? "unknown";
        console.warn(
          `Cornerstone Components: <${tagName}> is already defined, so this definition was skipped. The existing one came from the ${firstBuild} build; this one is from the ${thisBuild} build. Import each component from one build only \u2014 mixing them doubles the bundle, and if the two are different versions the page runs whichever registered first.`
        );
        return;
      }
      registeringBuilds().set(tagName, thisBuild);
      t3(tagName)(cls);
    };
  }

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.ZWFNEA25.js
  var CsAccordion = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.mode = "multiple";
      this.iconPlacement = "end";
      this.headingLevel = "3";
      this.appearance = "outlined";
    }
    getAllItems() {
      return this.defaultSlot.assignedElements({ flatten: true }).filter((el) => el.tagName.toLowerCase() === "cs-accordion-item");
    }
    getFocusableItems() {
      return this.getAllItems().filter((item) => !item.disabled);
    }
    ownsItem(item) {
      return item.closest("cs-accordion") === this;
    }
    initRovingTabIndex() {
      this.getFocusableItems().forEach((item, index) => {
        item.isTabbable = index === 0;
      });
    }
    handleSlotChange() {
      if (this.didSSR) {
        const promises = [];
        this.getAllItems().forEach((item) => {
          if (item.didSSR && !item.hasUpdated) {
            promises.push(item.updateComplete);
          }
        });
        if (promises.length > 0) {
          Promise.allSettled(promises).then(() => {
            this.handleSlotChange();
          });
          return;
        }
      }
      this.syncIconPlacement();
      this.syncHeadingLevel();
      this.syncAppearance();
      this.initRovingTabIndex();
    }
    handleFocusIn(event) {
      const items = this.getFocusableItems();
      const path = event.composedPath();
      const closestItem = path.find(
        (el) => el instanceof Element && el.tagName.toLowerCase() === "cs-accordion-item"
      );
      if (!closestItem || !this.ownsItem(closestItem)) {
        return;
      }
      const focusedItem = items.find((item) => item === closestItem);
      if (!focusedItem) {
        return;
      }
      items.forEach((item) => item.isTabbable = item === focusedItem);
    }
    handleKeyDown(event) {
      const items = this.getFocusableItems();
      if (!items.length) {
        return;
      }
      const path = event.composedPath();
      const closestItem = path.find(
        (el) => el instanceof Element && el.tagName.toLowerCase() === "cs-accordion-item"
      );
      if (!closestItem || !this.ownsItem(closestItem)) {
        return;
      }
      const currentIndex = items.findIndex((item) => item.isTabbable);
      let nextIndex = currentIndex;
      switch (event.key) {
        case "ArrowDown":
          event.preventDefault();
          nextIndex = (currentIndex + 1) % items.length;
          break;
        case "ArrowUp":
          event.preventDefault();
          nextIndex = (currentIndex - 1 + items.length) % items.length;
          break;
        case "Home":
          event.preventDefault();
          nextIndex = 0;
          break;
        case "End":
          event.preventDefault();
          nextIndex = items.length - 1;
          break;
        default:
          return;
      }
      items.forEach((item, index) => item.isTabbable = index === nextIndex);
      items[nextIndex].focus();
    }
    syncIconPlacement() {
      this.getAllItems().forEach((item) => item.iconPlacement = this.iconPlacement);
    }
    syncHeadingLevel() {
      this.getAllItems().forEach((item) => item.headingLevel = this.headingLevel);
    }
    syncAppearance() {
      this.getAllItems().forEach((item) => item.appearance = this.appearance);
    }
    async handleItemTrigger(event) {
      const { item } = event.detail;
      if (!this.ownsItem(item)) {
        return;
      }
      event.stopPropagation();
      if (item.disabled) {
        return;
      }
      if (item.expanded) {
        if (this.mode === "single") {
          return;
        }
        const csCollapse = new CsAccordionCollapseEvent({ item });
        this.dispatchEvent(csCollapse);
        if (csCollapse.defaultPrevented) {
          return;
        }
        await item.collapse();
        this.dispatchEvent(new CsAccordionAfterCollapseEvent({ item }));
      } else {
        if (this.mode === "single" || this.mode === "single-collapsible") {
          this.getAllItems().filter((i10) => i10 !== item && i10.expanded).forEach((i10) => i10.collapse());
        }
        const csExpand = new CsAccordionExpandEvent({ item });
        this.dispatchEvent(csExpand);
        if (csExpand.defaultPrevented) {
          return;
        }
        await item.expand();
        this.dispatchEvent(new CsAccordionAfterExpandEvent({ item }));
      }
    }
    /** Expands all accordion items. No-op when `mode` is `single` or `single-collapsible`. */
    expandAll() {
      if (this.mode === "single" || this.mode === "single-collapsible") {
        return;
      }
      this.getAllItems().filter((item) => !item.disabled && !item.expanded).forEach((item) => item.expand());
    }
    /** Collapses all accordion items. */
    collapseAll() {
      this.getAllItems().filter((item) => item.expanded).forEach((item) => item.collapse());
    }
    render() {
      return b2`
      <slot
        @slotchange=${this.handleSlotChange}
        @cs-accordion-item-trigger=${this.handleItemTrigger}
        @focusin=${this.handleFocusIn}
        @keydown=${this.handleKeyDown}
      ></slot>
    `;
    }
  };
  CsAccordion.css = accordion_styles_default;
  __decorateClass([
    e5("slot")
  ], CsAccordion.prototype, "defaultSlot", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsAccordion.prototype, "mode", 2);
  __decorateClass([
    n4({ attribute: "icon-placement", reflect: true })
  ], CsAccordion.prototype, "iconPlacement", 2);
  __decorateClass([
    n4({ attribute: "heading-level", reflect: true })
  ], CsAccordion.prototype, "headingLevel", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsAccordion.prototype, "appearance", 2);
  __decorateClass([
    watch("iconPlacement", { waitUntilFirstUpdate: true })
  ], CsAccordion.prototype, "syncIconPlacement", 1);
  __decorateClass([
    watch("headingLevel", { waitUntilFirstUpdate: true })
  ], CsAccordion.prototype, "syncHeadingLevel", 1);
  __decorateClass([
    watch("appearance", { waitUntilFirstUpdate: true })
  ], CsAccordion.prototype, "syncAppearance", 1);
  CsAccordion = __decorateClass([
    customElement("cs-accordion")
  ], CsAccordion);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.XJ36J6OZ.js
  var CsAccordionItemCollapsedEvent = class extends Event {
    constructor() {
      super("cs-accordion-item-collapsed", { bubbles: false, cancelable: false, composed: false });
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.2MQX2VB4.js
  var CsAccordionItemExpandedEvent = class extends Event {
    constructor() {
      super("cs-accordion-item-expanded", { bubbles: false, cancelable: false, composed: false });
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.3YQX52WM.js
  var CsAccordionItemTriggerEvent = class extends Event {
    constructor(detail) {
      super("cs-accordion-item-trigger", { bubbles: true, cancelable: false, composed: true });
      this.detail = detail;
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.EVAGGOC2.js
  function waitForEvent(el, eventName, timeout = 5e3) {
    return new Promise((resolve) => {
      function settle() {
        clearTimeout(timer);
        el.removeEventListener(eventName, done);
        resolve();
      }
      function done(event) {
        if (event.target === el) {
          settle();
        }
      }
      el.addEventListener(eventName, done);
      const timer = setTimeout(settle, timeout);
    });
  }

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.HC2QZ77X.js
  async function animate(el, keyframes, options2) {
    return el.animate(keyframes, options2).finished.catch(() => {
    });
  }
  function animateWithClass(el, className) {
    return new Promise((resolve) => {
      const controller = new AbortController();
      const { signal } = controller;
      if (el.classList.contains(className)) {
        resolve();
        return;
      }
      el.classList.add(className);
      let resolved = false;
      const onEnd = () => {
        if (resolved) {
          return;
        }
        resolved = true;
        el.classList.remove(className);
        resolve();
        controller.abort();
      };
      el.addEventListener("animationend", onEnd, { once: true, signal });
      el.addEventListener("animationcancel", onEnd, { once: true, signal });
      requestAnimationFrame(() => {
        if (!resolved && el.getAnimations().length === 0) {
          onEnd();
        }
      });
    });
  }
  function parseDuration(duration) {
    duration = duration.toString().toLowerCase();
    if (duration.indexOf("ms") > -1) {
      return parseFloat(duration) || 0;
    }
    if (duration.indexOf("s") > -1) {
      return (parseFloat(duration) || 0) * 1e3;
    }
    return parseFloat(duration) || 0;
  }
  function prefersReducedMotion() {
    const query = window.matchMedia("(prefers-reduced-motion: reduce)");
    return query.matches;
  }

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.IIB4U4YE.js
  var accordion_item_styles_default = i`
  @layer cs-component {
    :host {
      --spacing: var(--cs-space-m);
      --show-duration: var(--cs-transition-normal);
      --hide-duration: var(--cs-transition-normal);
      --easing: var(--cs-transition-easing);

      display: block;
    }

    :host(:not(:first-child)) {
      border-top: var(--cs-panel-border-width) var(--cs-panel-border-style) var(--cs-color-surface-border);
    }

    :host([appearance='filled']) {
      background-color: var(--cs-color-neutral-fill-quiet);
    }

    :host([appearance='filled']:not(:first-child)) {
      margin-block-start: var(--cs-panel-border-width);
      border-top: none;
    }

    [part~='heading'] {
      margin: 0;
      font: inherit;
    }

    [part~='button'] {
      display: flex;
      align-items: center;
      gap: var(--spacing);
      padding: var(--spacing);
      width: 100%;
      background: none;
      border: none;
      cursor: pointer;
      text-align: start;
      color: var(--cs-color-text-normal);
      font: inherit;
      font-weight: var(--cs-font-weight-semibold);

      &:focus {
        outline: none;
      }

      &:focus-visible {
        outline: var(--cs-focus-ring);
        /* Inset by the full ring width + offset so the parent's overflow:hidden doesn't clip it */
        outline-offset: calc(0px - var(--cs-focus-ring-width) - var(--cs-focus-ring-offset));
      }
    }

    /* Icon at end (default) */
    :host([icon-placement='end']) [part~='button'] {
      justify-content: space-between;
    }

    /* Icon at start */
    :host([icon-placement='start']) [part~='button'] {
      flex-direction: row-reverse;
      justify-content: flex-end;
    }

    :host([disabled]) {
      opacity: 0.5;
      cursor: not-allowed;
    }

    :host([disabled]) [part~='button'] {
      cursor: not-allowed;
      pointer-events: none;
    }

    :host(:first-child) [part~='button'] {
      border-top-left-radius: var(--cs-panel-border-radius);
      border-top-right-radius: var(--cs-panel-border-radius);
    }

    :host(:last-child:not([expanded])) [part~='button'] {
      border-bottom-left-radius: var(--cs-panel-border-radius);
      border-bottom-right-radius: var(--cs-panel-border-radius);
    }

    [part~='icon'] {
      flex: 0 0 auto;
      display: flex;
      align-items: center;
      color: var(--cs-color-text-quiet);
      transition: rotate var(--hide-duration) var(--easing);
    }

    :host([expanded]) [part~='icon'] {
      rotate: 90deg;
      transition-duration: var(--show-duration);
    }

    :host([expanded]:dir(rtl)) [part~='icon'] {
      rotate: -90deg;
    }

    .body {
      overflow: hidden;
      color: var(--cs-color-text-quiet);
    }

    :host([expanded]) .body:not(.animating) {
      overflow: visible;
    }

    .content {
      display: block;
      padding: 0 var(--spacing) var(--spacing);
    }
  }
`;

  // node_modules/lit-html/directive.js
  var t5 = { ATTRIBUTE: 1, CHILD: 2, PROPERTY: 3, BOOLEAN_ATTRIBUTE: 4, EVENT: 5, ELEMENT: 6 };
  var e6 = (t8) => (...e12) => ({ _$litDirective$: t8, values: e12 });
  var i5 = class {
    constructor(t8) {
    }
    get _$AU() {
      return this._$AM._$AU;
    }
    _$AT(t8, e12, i10) {
      this._$Ct = t8, this._$AM = e12, this._$Ci = i10;
    }
    _$AS(t8, e12) {
      return this.update(t8, e12);
    }
    update(t8, e12) {
      return this.render(...e12);
    }
  };

  // node_modules/lit-html/directives/class-map.js
  var e7 = e6(class extends i5 {
    constructor(t8) {
      if (super(t8), t8.type !== t5.ATTRIBUTE || "class" !== t8.name || t8.strings?.length > 2) throw Error("`classMap()` can only be used in the `class` attribute and must be the only part in the attribute.");
    }
    render(t8) {
      return " " + Object.keys(t8).filter((s7) => t8[s7]).join(" ") + " ";
    }
    update(s7, [i10]) {
      if (void 0 === this.st) {
        this.st = /* @__PURE__ */ new Set(), void 0 !== s7.strings && (this.nt = new Set(s7.strings.join(" ").split(/\s/).filter((t8) => "" !== t8)));
        for (const t8 in i10) i10[t8] && !this.nt?.has(t8) && this.st.add(t8);
        return this.render(i10);
      }
      const r9 = s7.element.classList;
      for (const t8 of this.st) t8 in i10 || (r9.remove(t8), this.st.delete(t8));
      for (const t8 in i10) {
        const s8 = !!i10[t8];
        s8 === this.st.has(t8) || this.nt?.has(t8) || (s8 ? (r9.add(t8), this.st.add(t8)) : (r9.remove(t8), this.st.delete(t8)));
      }
      return E;
    }
  });

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.AQDE3K3E.js
  var CsAccordionItem = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.animationGeneration = 0;
      this.localize = new LocalizeController2(this);
      this.isAnimating = false;
      this.label = "";
      this.expanded = false;
      this.disabled = false;
      this.headingLevel = "3";
      this.isTabbable = true;
      this.iconPlacement = "end";
      this.appearance = "outlined";
    }
    firstUpdated(changedProperties) {
      super.firstUpdated(changedProperties);
      this.body.style.height = this.expanded ? "auto" : "0";
    }
    updated() {
      this.customStates.set("animating", this.isAnimating);
    }
    handleTriggerClick() {
      if (this.disabled) {
        return;
      }
      this.dispatchEvent(new CsAccordionItemTriggerEvent({ item: this }));
    }
    handleTriggerKeyDown(event) {
      if (event.key === "Enter" || event.key === " ") {
        event.preventDefault();
        this.handleTriggerClick();
      }
    }
    async handleExpandedChange() {
      this.animationGeneration++;
      const generation = this.animationGeneration;
      if (this.expanded) {
        this.isAnimating = true;
        const duration = parseDuration(getComputedStyle(this.body).getPropertyValue("--show-duration") || "200ms");
        const easing = getComputedStyle(this.body).getPropertyValue("--easing") || "ease";
        await animate(
          this.body,
          [
            { height: "0", opacity: "0" },
            { height: `${this.body.scrollHeight}px`, opacity: "1" }
          ],
          { duration, easing }
        );
        if (this.animationGeneration !== generation) {
          return;
        }
        this.body.style.height = "auto";
        this.isAnimating = false;
        this.dispatchEvent(new CsAccordionItemExpandedEvent());
      } else {
        this.isAnimating = true;
        const duration = parseDuration(getComputedStyle(this.body).getPropertyValue("--hide-duration") || "200ms");
        const easing = getComputedStyle(this.body).getPropertyValue("--easing") || "ease";
        await animate(
          this.body,
          [
            { height: `${this.body.scrollHeight}px`, opacity: "1" },
            { height: "0", opacity: "0" }
          ],
          { duration, easing }
        );
        if (this.animationGeneration !== generation) {
          return;
        }
        this.body.style.height = "0";
        this.isAnimating = false;
        this.dispatchEvent(new CsAccordionItemCollapsedEvent());
      }
    }
    /** Expands the accordion item with animation. */
    async expand() {
      if (this.expanded || this.disabled) {
        return;
      }
      this.expanded = true;
      return waitForEvent(this, "cs-accordion-item-expanded");
    }
    /** Collapses the accordion item with animation. */
    async collapse() {
      if (!this.expanded || this.disabled) {
        return;
      }
      this.expanded = false;
      return waitForEvent(this, "cs-accordion-item-collapsed");
    }
    /** Toggles the accordion item's expanded state. */
    async toggle() {
      return this.expanded ? this.collapse() : this.expand();
    }
    /** Focuses the accordion item's trigger button. */
    focus(options2) {
      this.triggerButton?.focus(options2);
    }
    renderHeadingWrapper(content) {
      const level = parseInt(this.headingLevel, 10);
      switch (level >= 1 && level <= 6 ? level : 3) {
        case 1:
          return b2`<h1 part="heading">${content}</h1>`;
        case 2:
          return b2`<h2 part="heading">${content}</h2>`;
        case 4:
          return b2`<h4 part="heading">${content}</h4>`;
        case 5:
          return b2`<h5 part="heading">${content}</h5>`;
        case 6:
          return b2`<h6 part="heading">${content}</h6>`;
        default:
          return b2`<h3 part="heading">${content}</h3>`;
      }
    }
    render() {
      const isRtl = !this.hasUpdated ? this.dir === "rtl" : this.localize.dir() === "rtl";
      const button = b2`
      <button
        part="button"
        type="button"
        id="trigger"
        aria-expanded=${this.expanded ? "true" : "false"}
        aria-controls="panel"
        aria-disabled=${this.disabled ? "true" : "false"}
        tabindex=${this.disabled || !this.isTabbable ? "-1" : "0"}
        @click=${this.handleTriggerClick}
        @keydown=${this.handleTriggerKeyDown}
      >
        <slot name="label" part="label">${this.label}</slot>
        <span part="icon">
          <slot name="icon">
            <cs-icon library="system" name=${isRtl ? "keyboard_arrow_left" : "keyboard_arrow_right"}></cs-icon>
          </slot>
        </span>
      </button>
    `;
      return b2`
      <div part="accordion-item">
        ${this.headingLevel === "none" ? button : this.renderHeadingWrapper(button)}
        <div
          part="panel"
          id="panel"
          class=${e7({ body: true, animating: this.isAnimating })}
          role="region"
          aria-labelledby="trigger"
        >
          <slot part="content" class="content"></slot>
        </div>
      </div>
    `;
    }
  };
  CsAccordionItem.css = accordion_item_styles_default;
  __decorateClass([
    e5(".body")
  ], CsAccordionItem.prototype, "body", 2);
  __decorateClass([
    e5('[part~="button"]')
  ], CsAccordionItem.prototype, "triggerButton", 2);
  __decorateClass([
    r5()
  ], CsAccordionItem.prototype, "isAnimating", 2);
  __decorateClass([
    n4()
  ], CsAccordionItem.prototype, "label", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsAccordionItem.prototype, "expanded", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsAccordionItem.prototype, "disabled", 2);
  __decorateClass([
    n4({ attribute: "heading-level", reflect: true })
  ], CsAccordionItem.prototype, "headingLevel", 2);
  __decorateClass([
    n4({ type: Boolean, attribute: false })
  ], CsAccordionItem.prototype, "isTabbable", 2);
  __decorateClass([
    n4({ attribute: "icon-placement", reflect: true })
  ], CsAccordionItem.prototype, "iconPlacement", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsAccordionItem.prototype, "appearance", 2);
  __decorateClass([
    watch("expanded", { waitUntilFirstUpdate: true })
  ], CsAccordionItem.prototype, "handleExpandedChange", 1);
  CsAccordionItem = __decorateClass([
    customElement("cs-accordion-item")
  ], CsAccordionItem);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.WAVBO5QN.js
  var CsErrorEvent = class extends Event {
    constructor() {
      super("cs-error", { bubbles: true, cancelable: false, composed: true });
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.VVV5GJL4.js
  var CsLoadEvent = class extends Event {
    constructor() {
      super("cs-load", { bubbles: true, cancelable: false, composed: true });
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.LZDXEAHA.js
  var icon_styles_default = i`
  :host {
    --rotate-angle: 0deg;

    box-sizing: content-box;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    vertical-align: -0.125em;
  }

  /* #region Canvas — the box the icon is centered within. Orthogonal to font-size. */

  /* Fixed width (default): 1.25em × 1em (20 × 16px) */
  :host(:not([canvas])),
  :host([canvas='fixed']) {
    width: 1.25em;
    height: 1em;
    min-width: 1.25em; /* <-- this is what Safari respects for intrinsic */
    min-height: 1em;
  }

  /* Auto: hug the icon's width. \`auto-width\` is the deprecated alias for canvas="auto". */
  :host([canvas='auto']),
  :host([auto-width]:not([canvas])) {
    width: auto;
    height: 1em;
  }

  /* Square: 1.25em × 1.25em (20 × 20px) */
  :host([canvas='square']) {
    width: 1.25em;
    height: 1.25em;
    min-width: 1.25em;
    min-height: 1.25em;
  }

  /* Roomy: 1.5em × 1.5em (24 × 24px) */
  :host([canvas='roomy']) {
    width: 1.5em;
    height: 1.5em;
    min-width: 1.5em;
    min-height: 1.5em;
  }

  /* #endregion */

  svg {
    /* NOTE: Avoid setting fill here. A stylesheet rule beats SVG presentation attributes, breaking stroke-based
       libraries like Lucide (fill="none" stroke="currentColor") and attribute-based mutators (issue #1733). The default
       library applies fill="currentColor" in its mutator instead. */
    height: 1em;
    overflow: visible;
    width: auto;

    /*
     * Material Symbols draws every icon inside a 24dp box with a 20dp live area, so at a given font-size its ink lands
     * about 17% smaller than ink-cropped artwork would. 24 / 20 = 1.2 puts it back without moving the canvas, so no
     * layout reflows. Set --icon-scale to 1 to render the artwork at its native size.
     *
     * The default lives in the var() fallback rather than on :host so an ancestor can set it — a declaration on :host
     * comes from this tree and beats a value inherited from the outer one, which would leave the property tunable
     * only on the element itself.
     */
    scale: var(--icon-scale, 1.2);
  }

  /* Rotation */
  :host([rotate]) {
    transform: rotate(var(--rotate-angle, 0deg));
  }

  /* Flipping */
  :host([flip='x']) {
    transform: scaleX(-1);
  }
  :host([flip='y']) {
    transform: scaleY(-1);
  }
  :host([flip='both']) {
    transform: scale(-1, -1);
  }

  /* Rotation and Flipping combined */
  :host([rotate][flip='x']) {
    transform: rotate(var(--rotate-angle, 0deg)) scaleX(-1);
  }
  :host([rotate][flip='y']) {
    transform: rotate(var(--rotate-angle, 0deg)) scaleY(-1);
  }
  :host([rotate][flip='both']) {
    transform: rotate(var(--rotate-angle, 0deg)) scale(-1, -1);
  }

  /* #region Animations — ported from Font Awesome 7.3 (--fa-* props mapped to cs-icon's --* names) */

  :host([animation='beat']) {
    animation-name: beat;
    animation-delay: var(--animation-delay, 0s);
    animation-direction: var(--animation-direction, normal);
    animation-duration: var(--animation-duration, 1s);
    animation-iteration-count: var(--animation-iteration-count, infinite);
    animation-timing-function: var(--animation-timing, ease-in-out);
  }

  :host([animation='bounce']) {
    animation-name: bounce;
    animation-delay: var(--animation-delay, 0s);
    animation-direction: var(--animation-direction, normal);
    animation-duration: var(--animation-duration, 1s);
    animation-iteration-count: var(--animation-iteration-count, infinite);
    animation-timing-function: var(--animation-timing, cubic-bezier(0.28, 0.84, 0.42, 1));
  }

  :host([animation='fade']) {
    animation-name: fade;
    animation-delay: var(--animation-delay, 0s);
    animation-direction: var(--animation-direction, normal);
    animation-duration: var(--animation-duration, 1s);
    animation-iteration-count: var(--animation-iteration-count, infinite);
    animation-timing-function: var(--animation-timing, ease-in-out);
  }

  :host([animation='beat-fade']) {
    animation-name: beat-fade;
    animation-delay: var(--animation-delay, 0s);
    animation-direction: var(--animation-direction, normal);
    animation-duration: var(--animation-duration, 1s);
    animation-iteration-count: var(--animation-iteration-count, infinite);
    animation-timing-function: var(--animation-timing, ease-in-out);
  }

  :host([animation='flip']) {
    animation-name: flip;
    animation-delay: var(--animation-delay, 0s);
    animation-direction: var(--animation-direction, normal);
    animation-duration: var(--animation-duration, 1.5s);
    animation-iteration-count: var(--animation-iteration-count, infinite);
    animation-timing-function: var(--animation-timing, ease-in-out);
  }

  :host([animation='flip-360']) {
    animation-name: flip-360;
    animation-delay: var(--animation-delay, 0s);
    animation-direction: var(--animation-direction, normal);
    animation-duration: var(--animation-duration, 1s);
    animation-iteration-count: var(--animation-iteration-count, infinite);
    animation-timing-function: var(--animation-timing, ease-in-out);
  }

  :host([animation='shake']) {
    animation-name: shake;
    animation-delay: var(--animation-delay, 0s);
    animation-direction: var(--animation-direction, normal);
    animation-duration: var(--animation-duration, 0.75s);
    animation-iteration-count: var(--animation-iteration-count, infinite);
    animation-timing-function: var(--animation-timing, ease-in-out);
  }

  :host([animation='spin']) {
    animation-name: spin;
    animation-delay: var(--animation-delay, 0s);
    animation-direction: var(--animation-direction, normal);
    animation-duration: var(--animation-duration, 2s);
    animation-iteration-count: var(--animation-iteration-count, infinite);
    animation-timing-function: var(--animation-timing, linear);
  }

  :host([animation='spin-pulse']) {
    animation-name: spin;
    animation-delay: var(--animation-delay, 0s);
    animation-direction: var(--animation-direction, normal);
    animation-duration: var(--animation-duration, 1s);
    animation-iteration-count: var(--animation-iteration-count, infinite);
    animation-timing-function: var(--animation-timing, steps(8));
  }

  /* spin-reverse is FA's reverse modifier expressed as a standalone value; reverse any spin via --animation-direction: reverse */
  :host([animation='spin-reverse']) {
    animation-name: spin;
    animation-delay: var(--animation-delay, 0s);
    animation-direction: var(--animation-direction, reverse);
    animation-duration: var(--animation-duration, 2s);
    animation-iteration-count: var(--animation-iteration-count, infinite);
    animation-timing-function: var(--animation-timing, linear);
  }

  :host([animation='spin-snap']) {
    animation-name: spin-snap;
    animation-delay: var(--animation-delay, 0s);
    animation-direction: var(--animation-direction, normal);
    animation-duration: var(--animation-duration, 3s);
    animation-iteration-count: var(--animation-iteration-count, infinite);
    animation-timing-function: var(--animation-timing, linear);
  }

  :host([animation='spin-snap-4']) {
    animation-name: spin-snap-4;
    animation-delay: var(--animation-delay, 0s);
    animation-direction: var(--animation-direction, normal);
    animation-duration: var(--animation-duration, 2.4s);
    animation-iteration-count: var(--animation-iteration-count, infinite);
    animation-timing-function: var(--animation-timing, linear);
  }

  :host([animation='spin-snap-8']) {
    animation-name: spin-snap-8;
    animation-delay: var(--animation-delay, 0s);
    animation-direction: var(--animation-direction, normal);
    animation-duration: var(--animation-duration, 4s);
    animation-iteration-count: var(--animation-iteration-count, infinite);
    animation-timing-function: var(--animation-timing, linear);
  }

  :host([animation='buzz']) {
    animation-name: buzz;
    animation-delay: var(--animation-delay, 0s);
    animation-direction: var(--animation-direction, normal);
    animation-duration: var(--animation-duration, 0.6s);
    animation-iteration-count: var(--animation-iteration-count, infinite);
    animation-timing-function: var(--animation-timing, linear);
  }

  :host([animation='wag']) {
    animation-name: wag;
    animation-delay: var(--animation-delay, 0s);
    animation-direction: var(--animation-direction, normal);
    animation-duration: var(--animation-duration, 0.9s);
    animation-iteration-count: var(--animation-iteration-count, infinite);
    animation-timing-function: var(--animation-timing, ease-out);
    transform-origin: bottom center;
  }

  :host([animation='float']) {
    animation-name: float;
    animation-delay: var(--animation-delay, 0s);
    animation-direction: var(--animation-direction, normal);
    animation-duration: var(--animation-duration, 3s);
    animation-iteration-count: var(--animation-iteration-count, infinite);
    animation-timing-function: var(--animation-timing, ease-in-out);
    will-change: transform;
  }

  :host([animation='swing']) {
    animation-name: swing;
    animation-delay: var(--animation-delay, 0s);
    animation-direction: var(--animation-direction, normal);
    animation-duration: var(--animation-duration, 1.2s);
    animation-iteration-count: var(--animation-iteration-count, infinite);
    animation-timing-function: var(--animation-timing, ease-out);
    transform-origin: top center;
  }

  :host([animation='jello']) {
    animation-name: jello;
    animation-delay: var(--animation-delay, 0s);
    animation-direction: var(--animation-direction, normal);
    animation-duration: var(--animation-duration, 0.9s);
    animation-iteration-count: var(--animation-iteration-count, infinite);
    animation-timing-function: var(--animation-timing, ease-out);
  }

  @media (prefers-reduced-motion: reduce) {
    :host([animation='beat']),
    :host([animation='bounce']),
    :host([animation='fade']),
    :host([animation='beat-fade']),
    :host([animation='flip']),
    :host([animation='flip-360']),
    :host([animation='shake']),
    :host([animation='spin']),
    :host([animation='spin-pulse']),
    :host([animation='spin-reverse']),
    :host([animation='spin-snap']),
    :host([animation='spin-snap-4']),
    :host([animation='spin-snap-8']),
    :host([animation='buzz']),
    :host([animation='wag']),
    :host([animation='float']),
    :host([animation='swing']),
    :host([animation='jello']) {
      animation: none !important;
      transition: none !important;
    }
  }

  /* #endregion */

  /* #region Keyframes — ported verbatim from Font Awesome 7.3 */

  @keyframes beat {
    0% {
      transform: scale(1);
    }
    25% {
      transform: scale(calc(1.25 * var(--beat-scale, 1.25)));
    }
    45% {
      transform: scale(calc(1.22 * var(--beat-scale, 1.22)));
    }
    65% {
      transform: scale(calc(1.25 * var(--beat-scale, 1.25)));
    }
    90% {
      transform: scale(1);
    }
  }

  @keyframes bounce {
    0% {
      transform: scale(1, 1) translateY(0);
      /* No fallback by design (ported from FA 7.3): the first segment uses the user's --animation-timing or the CSS
         initial ease, while the explicit cubic-beziers on later stops drive the bounce physics. */
      animation-timing-function: var(--animation-timing);
    }
    14% {
      transform: scale(var(--bounce-start-scale-x, 1.06), var(--bounce-start-scale-y, 0.94))
        translateY(var(--bounce-anticipation, 3px));
      animation-timing-function: cubic-bezier(0.33, 0, 0.66, 0.33);
    }
    32% {
      transform: scale(var(--bounce-jump-scale-x, 0.94), var(--bounce-jump-scale-y, 1.12))
        translateY(calc(-1 * var(--bounce-height, 0.5em)));
      animation-timing-function: cubic-bezier(0.33, 0.66, 0.66, 1);
    }
    52% {
      transform: scale(1, 1) translateY(calc(-1 * var(--bounce-height, 0.5em) * 1.1));
      animation-timing-function: cubic-bezier(0.5, 0, 1, 0.5);
    }
    70% {
      transform: scale(var(--bounce-land-scale-x, 1.06), var(--bounce-land-scale-y, 0.92)) translateY(0);
      animation-timing-function: cubic-bezier(0.33, 0.33, 0.66, 1);
    }
    85% {
      transform: scale(0.98, 1.04) translateY(calc(-2px * var(--bounce-rebound, 1)));
      animation-timing-function: cubic-bezier(0.33, 0, 0.66, 1);
    }
    100% {
      transform: scale(1, 1) translateY(0);
    }
  }

  @keyframes fade {
    0% {
      opacity: 1;
      transform: scale(1);
      animation-timing-function: cubic-bezier(0.2, 0, 0.4, 1);
    }
    40% {
      opacity: var(--fade-opacity, 0.4);
      transform: scale(0.98);
      animation-timing-function: cubic-bezier(0.4, 0, 0.6, 1);
    }
    100% {
      opacity: 1;
      transform: scale(1);
    }
  }

  @keyframes beat-fade {
    0% {
      opacity: var(--beat-fade-opacity, 0.4);
      transform: scale(1);
      animation-timing-function: cubic-bezier(0.2, 0, 0.4, 1);
    }
    25% {
      opacity: calc(var(--beat-fade-opacity, 0.4) + 0.4);
      transform: scale(var(--beat-fade-scale, 1.28));
      animation-timing-function: cubic-bezier(0.4, 0, 0.6, 1);
    }
    45% {
      opacity: 1;
      transform: scale(var(--beat-fade-scale, 1.25));
      animation-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
    }
    65% {
      opacity: calc(var(--beat-fade-opacity, 0.4) + 0.4);
      transform: scale(var(--beat-fade-scale, 1.28));
      animation-timing-function: cubic-bezier(0.4, 0, 0.6, 1);
    }
    100% {
      opacity: var(--beat-fade-opacity, 0.4);
      transform: scale(1);
    }
  }

  @keyframes flip {
    0% {
      transform: perspective(2em) scale(1) rotate3d(var(--flip-x, 0), var(--flip-y, 1), var(--flip-z, 0), 0deg);
      animation-timing-function: cubic-bezier(0.2, 0, 0.4, 1);
    }
    8% {
      transform: perspective(2em) scale(var(--flip-anticipation-scale, 0.95))
        rotate3d(var(--flip-x, 0), var(--flip-y, 1), var(--flip-z, 0), 0deg);
      animation-timing-function: cubic-bezier(0.33, 0, 0.66, 0.33);
    }
    35% {
      transform: perspective(2em) scale(1)
        rotate3d(var(--flip-x, 0), var(--flip-y, 1), var(--flip-z, 0), calc(var(--flip-angle, -360deg) * 0.6));
      animation-timing-function: linear;
    }
    65% {
      transform: perspective(2em) scale(1)
        rotate3d(var(--flip-x, 0), var(--flip-y, 1), var(--flip-z, 0), calc(var(--flip-angle, -360deg) * 0.5));
      animation-timing-function: cubic-bezier(0.33, 0.66, 0.66, 1);
    }
    92% {
      transform: perspective(2em) scale(1)
        rotate3d(
          var(--flip-x, 0),
          var(--flip-y, 1),
          var(--flip-z, 0),
          calc(var(--flip-angle, -360deg) * var(--flip-overshoot, 1.04))
        );
      animation-timing-function: cubic-bezier(0.33, 0, 0.66, 1);
    }
    100% {
      transform: perspective(2em) scale(1)
        rotate3d(var(--flip-x, 0), var(--flip-y, 1), var(--flip-z, 0), var(--flip-angle, -360deg));
    }
  }

  @keyframes flip-360 {
    0% {
      transform: perspective(2em) scale(1) rotate3d(var(--flip-x, 0), var(--flip-y, 1), var(--flip-z, 0), 0deg);
      animation-timing-function: cubic-bezier(0.2, 0, 0.4, 1);
    }
    8% {
      transform: perspective(2em) scale(var(--flip-anticipation-scale, 0.95))
        rotate3d(var(--flip-x, 0), var(--flip-y, 1), var(--flip-z, 0), 0deg);
      animation-timing-function: cubic-bezier(0.33, 0, 0.66, 0.33);
    }
    50% {
      transform: perspective(2em) scale(1)
        rotate3d(var(--flip-x, 0), var(--flip-y, 1), var(--flip-z, 0), calc(var(--flip-angle, -360deg) * 0.6));
      animation-timing-function: cubic-bezier(0.33, 0.66, 0.66, 1);
    }
    80% {
      transform: perspective(2em) scale(1)
        rotate3d(
          var(--flip-x, 0),
          var(--flip-y, 1),
          var(--flip-z, 0),
          calc(var(--flip-angle, -360deg) * var(--flip-overshoot, 1.04))
        );
      animation-timing-function: cubic-bezier(0.33, 0, 0.66, 1);
    }
    100% {
      transform: perspective(2em) scale(1)
        rotate3d(var(--flip-x, 0), var(--flip-y, 1), var(--flip-z, 0), var(--flip-angle, -360deg));
    }
  }

  @keyframes shake {
    0% {
      transform: rotate(0deg);
      animation-timing-function: cubic-bezier(0.2, 0, 0.8, 1);
    }
    8% {
      transform: rotate(35deg) translateX(1px);
      animation-timing-function: cubic-bezier(0.3, 0, 0.7, 1);
    }
    20% {
      transform: rotate(-22deg) translateX(-1px);
      animation-timing-function: cubic-bezier(0.3, 0, 0.7, 1);
    }
    35% {
      transform: rotate(15deg) translateX(1px);
      animation-timing-function: cubic-bezier(0.3, 0, 0.7, 1);
    }
    50% {
      transform: rotate(-9deg);
      animation-timing-function: cubic-bezier(0.4, 0, 0.6, 1);
    }
    65% {
      transform: rotate(5deg);
      animation-timing-function: cubic-bezier(0.4, 0, 0.6, 1);
    }
    78% {
      transform: rotate(-3deg);
      animation-timing-function: cubic-bezier(0.4, 0, 0.6, 1);
    }
    90% {
      transform: rotate(1deg);
      animation-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
    }
    100% {
      transform: rotate(0deg);
    }
  }

  @keyframes spin {
    0% {
      transform: rotate(0deg);
    }
    100% {
      transform: rotate(360deg);
    }
  }

  @keyframes spin-snap {
    0% {
      transform: rotate(0deg);
      animation-timing-function: cubic-bezier(0, 0, 0.2, 1);
    }
    12% {
      transform: rotate(60deg);
      animation-timing-function: cubic-bezier(0.8, 0, 1, 1);
    }
    16.67% {
      transform: rotate(60deg);
      animation-timing-function: cubic-bezier(0, 0, 0.2, 1);
    }
    28.67% {
      transform: rotate(120deg);
      animation-timing-function: cubic-bezier(0.8, 0, 1, 1);
    }
    33.33% {
      transform: rotate(120deg);
      animation-timing-function: cubic-bezier(0, 0, 0.2, 1);
    }
    45.33% {
      transform: rotate(180deg);
      animation-timing-function: cubic-bezier(0.8, 0, 1, 1);
    }
    50% {
      transform: rotate(180deg);
      animation-timing-function: cubic-bezier(0, 0, 0.2, 1);
    }
    62% {
      transform: rotate(240deg);
      animation-timing-function: cubic-bezier(0.8, 0, 1, 1);
    }
    66.67% {
      transform: rotate(240deg);
      animation-timing-function: cubic-bezier(0, 0, 0.2, 1);
    }
    78.67% {
      transform: rotate(300deg);
      animation-timing-function: cubic-bezier(0.8, 0, 1, 1);
    }
    83.33% {
      transform: rotate(300deg);
      animation-timing-function: cubic-bezier(0, 0, 0.2, 1);
    }
    95.33% {
      transform: rotate(360deg);
      animation-timing-function: cubic-bezier(0.8, 0, 1, 1);
    }
    100% {
      transform: rotate(360deg);
    }
  }

  @keyframes spin-snap-4 {
    0% {
      transform: rotate(0deg);
      animation-timing-function: cubic-bezier(0, 0, 0.2, 1);
    }
    15% {
      transform: rotate(90deg);
      animation-timing-function: cubic-bezier(0.8, 0, 1, 1);
    }
    25% {
      transform: rotate(90deg);
      animation-timing-function: cubic-bezier(0, 0, 0.2, 1);
    }
    40% {
      transform: rotate(180deg);
      animation-timing-function: cubic-bezier(0.8, 0, 1, 1);
    }
    50% {
      transform: rotate(180deg);
      animation-timing-function: cubic-bezier(0, 0, 0.2, 1);
    }
    65% {
      transform: rotate(270deg);
      animation-timing-function: cubic-bezier(0.8, 0, 1, 1);
    }
    75% {
      transform: rotate(270deg);
      animation-timing-function: cubic-bezier(0, 0, 0.2, 1);
    }
    90% {
      transform: rotate(360deg);
      animation-timing-function: cubic-bezier(0.8, 0, 1, 1);
    }
    100% {
      transform: rotate(360deg);
    }
  }

  @keyframes spin-snap-8 {
    0% {
      transform: rotate(0deg);
      animation-timing-function: cubic-bezier(0, 0, 0.2, 1);
    }
    9% {
      transform: rotate(45deg);
      animation-timing-function: cubic-bezier(0.8, 0, 1, 1);
    }
    12.5% {
      transform: rotate(45deg);
      animation-timing-function: cubic-bezier(0, 0, 0.2, 1);
    }
    21.5% {
      transform: rotate(90deg);
      animation-timing-function: cubic-bezier(0.8, 0, 1, 1);
    }
    25% {
      transform: rotate(90deg);
      animation-timing-function: cubic-bezier(0, 0, 0.2, 1);
    }
    34% {
      transform: rotate(135deg);
      animation-timing-function: cubic-bezier(0.8, 0, 1, 1);
    }
    37.5% {
      transform: rotate(135deg);
      animation-timing-function: cubic-bezier(0, 0, 0.2, 1);
    }
    46.5% {
      transform: rotate(180deg);
      animation-timing-function: cubic-bezier(0.8, 0, 1, 1);
    }
    50% {
      transform: rotate(180deg);
      animation-timing-function: cubic-bezier(0, 0, 0.2, 1);
    }
    59% {
      transform: rotate(225deg);
      animation-timing-function: cubic-bezier(0.8, 0, 1, 1);
    }
    62.5% {
      transform: rotate(225deg);
      animation-timing-function: cubic-bezier(0, 0, 0.2, 1);
    }
    71.5% {
      transform: rotate(270deg);
      animation-timing-function: cubic-bezier(0.8, 0, 1, 1);
    }
    75% {
      transform: rotate(270deg);
      animation-timing-function: cubic-bezier(0, 0, 0.2, 1);
    }
    84% {
      transform: rotate(315deg);
      animation-timing-function: cubic-bezier(0.8, 0, 1, 1);
    }
    87.5% {
      transform: rotate(315deg);
      animation-timing-function: cubic-bezier(0, 0, 0.2, 1);
    }
    96.5% {
      transform: rotate(360deg);
      animation-timing-function: cubic-bezier(0.8, 0, 1, 1);
    }
    100% {
      transform: rotate(360deg);
    }
  }

  @keyframes buzz {
    0% {
      transform: translateX(0) rotate(0deg);
      animation-timing-function: cubic-bezier(0.1, 0, 0.9, 1);
    }
    5% {
      transform: translateX(var(--buzz-distance, 4px)) rotate(0.5deg);
    }
    10% {
      transform: translateX(calc(-1 * var(--buzz-distance, 4px))) rotate(-0.5deg);
    }
    15% {
      transform: translateX(var(--buzz-distance, 4px)) rotate(0.3deg);
    }
    20% {
      transform: translateX(calc(-1 * var(--buzz-distance, 4px))) rotate(-0.3deg);
    }
    25% {
      transform: translateX(calc(var(--buzz-distance, 4px) * 0.7)) rotate(0.2deg);
    }
    30% {
      transform: translateX(calc(-1 * var(--buzz-distance, 4px) * 0.7)) rotate(-0.2deg);
    }
    35% {
      transform: translateX(calc(var(--buzz-distance, 4px) * 0.4)) rotate(0.1deg);
    }
    40% {
      transform: translateX(0) rotate(0deg);
    }
    100% {
      transform: translateX(0) rotate(0deg);
    }
  }

  @keyframes wag {
    0% {
      transform: rotate(0deg);
      animation-timing-function: cubic-bezier(0.2, 0, 0.6, 1);
    }
    12% {
      transform: rotate(var(--wag-angle, 12deg));
      animation-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
    }
    24% {
      transform: rotate(2deg);
      animation-timing-function: cubic-bezier(0.2, 0, 0.6, 1);
    }
    36% {
      transform: rotate(calc(var(--wag-angle, 12deg) * 0.85));
      animation-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
    }
    48% {
      transform: rotate(1deg);
      animation-timing-function: cubic-bezier(0.2, 0, 0.6, 1);
    }
    58% {
      transform: rotate(calc(var(--wag-angle, 12deg) * 0.6));
      animation-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
    }
    68% {
      transform: rotate(0deg);
    }
    100% {
      transform: rotate(0deg);
    }
  }

  @keyframes float {
    0% {
      transform: translateY(0) translateX(0) rotate(0deg)
        scale(var(--float-squash-x, 1.02), var(--float-squash-y, 0.98));
      animation-timing-function: cubic-bezier(0.33, 0, 0.66, 0.33);
    }
    15% {
      transform: translateY(calc(-0.4 * var(--float-height, 6px))) translateX(var(--float-drift, 1px))
        rotate(var(--float-tilt, 1deg)) scale(1, 1);
      animation-timing-function: cubic-bezier(0.33, 0.66, 0.66, 1);
    }
    35% {
      transform: translateY(calc(-1 * var(--float-height, 6px))) translateX(0) rotate(0deg)
        scale(var(--float-stretch-x, 0.98), var(--float-stretch-y, 1.03));
      animation-timing-function: cubic-bezier(0.5, 0, 0.5, 0);
    }
    50% {
      transform: translateY(calc(-0.92 * var(--float-height, 6px))) translateX(calc(-0.5 * var(--float-drift, 1px)))
        rotate(calc(-0.5 * var(--float-tilt, 1deg))) scale(0.995, 1.01);
      animation-timing-function: cubic-bezier(0.33, 0, 0.66, 0.33);
    }
    70% {
      transform: translateY(calc(-0.3 * var(--float-height, 6px))) translateX(calc(-1 * var(--float-drift, 1px)))
        rotate(calc(-1 * var(--float-tilt, 1deg))) scale(1, 1);
      animation-timing-function: cubic-bezier(0.33, 0.66, 0.66, 1);
    }
    90% {
      transform: translateY(calc(0.05 * var(--float-height, 6px))) translateX(0) rotate(0deg)
        scale(var(--float-squash-x, 1.02), var(--float-squash-y, 0.98));
      animation-timing-function: cubic-bezier(0.33, 0, 0.66, 1);
    }
    100% {
      transform: translateY(0) translateX(0) rotate(0deg)
        scale(var(--float-squash-x, 1.02), var(--float-squash-y, 0.98));
    }
  }

  @keyframes swing {
    0% {
      transform: rotate(0deg);
      animation-timing-function: cubic-bezier(0.2, 0, 0.8, 1);
    }
    8% {
      transform: rotate(var(--swing-angle, 22deg));
      animation-timing-function: cubic-bezier(0.3, 0, 0.7, 1);
    }
    18% {
      transform: rotate(calc(-1 * var(--swing-angle, 22deg) * 0.85));
      animation-timing-function: cubic-bezier(0.3, 0, 0.7, 1);
    }
    28% {
      transform: rotate(calc(var(--swing-angle, 22deg) * 0.65));
      animation-timing-function: cubic-bezier(0.35, 0, 0.65, 1);
    }
    38% {
      transform: rotate(calc(-1 * var(--swing-angle, 22deg) * 0.45));
      animation-timing-function: cubic-bezier(0.4, 0, 0.6, 1);
    }
    48% {
      transform: rotate(calc(var(--swing-angle, 22deg) * 0.25));
      animation-timing-function: cubic-bezier(0.4, 0, 0.6, 1);
    }
    56% {
      transform: rotate(calc(-1 * var(--swing-angle, 22deg) * 0.1));
      animation-timing-function: cubic-bezier(0.4, 0, 0.6, 1);
    }
    64% {
      transform: rotate(0deg);
    }
    100% {
      transform: rotate(0deg);
    }
  }

  @keyframes jello {
    0% {
      transform: scale(1, 1);
      animation-timing-function: cubic-bezier(0.2, 0, 0.8, 1);
    }
    12% {
      transform: scale(var(--jello-scale-x, 1.15), calc(2 - var(--jello-scale-x, 1.15)));
      animation-timing-function: cubic-bezier(0.3, 0, 0.7, 1);
    }
    24% {
      transform: scale(calc(2 - var(--jello-scale-y, 1.12)), var(--jello-scale-y, 1.12));
      animation-timing-function: cubic-bezier(0.3, 0, 0.7, 1);
    }
    36% {
      transform: scale(
        calc(1 + (var(--jello-scale-x, 1.15) - 1) * 0.5),
        calc(2 - (1 + (var(--jello-scale-x, 1.15) - 1) * 0.5))
      );
      animation-timing-function: cubic-bezier(0.4, 0, 0.6, 1);
    }
    48% {
      transform: scale(
        calc(2 - (1 + (var(--jello-scale-y, 1.12) - 1) * 0.3)),
        calc(1 + (var(--jello-scale-y, 1.12) - 1) * 0.3)
      );
      animation-timing-function: cubic-bezier(0.4, 0, 0.6, 1);
    }
    58% {
      transform: scale(1.02, 0.98);
      animation-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
    }
    68% {
      transform: scale(1, 1);
    }
    100% {
      transform: scale(1, 1);
    }
  }

  /* #endregion */
`;

  // node_modules/lit-html/directive-helpers.js
  var { I: t6 } = j;
  var l3 = (o13, t8) => void 0 === t8 ? void 0 !== o13?._$litType$ : o13?._$litType$ === t8;
  var r7 = (o13) => void 0 === o13.strings;
  var m2 = {};
  var p3 = (o13, t8 = m2) => o13._$AH = t8;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.WOYNK4PY.js
  var CACHEABLE_ERROR = /* @__PURE__ */ Symbol();
  var RETRYABLE_ERROR = /* @__PURE__ */ Symbol();
  var parser;
  var iconCache = /* @__PURE__ */ new Map();
  var CsIcon = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.svg = null;
      this.weight = 400;
      this.autoWidth = false;
      this.label = "";
      this.library = "default";
      this.rotate = 0;
      this.resolveIcon = async (url, library2) => {
        let fileData;
        if (library2?.spriteSheet) {
          if (!this.hasUpdated) {
            await this.updateComplete;
          }
          this.svg = b2`<svg part="svg">
        <use part="use" href="${url}"></use>
      </svg>`;
          await this.updateComplete;
          const svg = this.shadowRoot.querySelector("[part='svg']");
          if (typeof library2.mutator === "function") {
            library2.mutator(svg, this);
          }
          return this.svg;
        }
        try {
          fileData = await fetch(url, { mode: "cors" });
          if (!fileData.ok) {
            return fileData.status === 410 ? CACHEABLE_ERROR : RETRYABLE_ERROR;
          }
        } catch {
          return RETRYABLE_ERROR;
        }
        try {
          const div = document.createElement("div");
          div.innerHTML = await fileData.text();
          const svg = div.firstElementChild;
          if (svg?.tagName.toLowerCase() !== "svg") {
            return CACHEABLE_ERROR;
          }
          if (!parser) {
            parser = new DOMParser();
          }
          const doc = parser.parseFromString(svg.outerHTML, "text/html");
          const svgEl = doc.body.querySelector("svg");
          if (!svgEl) {
            return CACHEABLE_ERROR;
          }
          svgEl.part.add("svg");
          return document.adoptNode(svgEl);
        } catch {
          return CACHEABLE_ERROR;
        }
      };
    }
    connectedCallback() {
      super.connectedCallback();
      watchIcon(this);
    }
    firstUpdated(changedProperties) {
      super.firstUpdated(changedProperties);
      if (this.hasAttribute("rotate")) {
        this.style.setProperty("--rotate-angle", `${this.rotate}deg`);
      }
      this.setIcon();
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      unwatchIcon(this);
    }
    async getIconSource() {
      const library2 = getIconLibrary(this.library);
      const family = this.family || getDefaultIconFamily();
      if (this.name && library2) {
        const autoWidth = this.canvas === "auto" || this.autoWidth;
        let url;
        try {
          url = await library2.resolver(this.name, family, this.variant, autoWidth, this.weight);
        } catch {
          url = void 0;
        }
        return { url, fromLibrary: true };
      }
      return {
        url: this.src,
        fromLibrary: false
      };
    }
    handleLabelChange() {
      const hasLabel = typeof this.label === "string" && this.label.length > 0;
      if (hasLabel) {
        this.setAttribute("role", "img");
        this.setAttribute("aria-label", this.label);
        this.removeAttribute("aria-hidden");
      } else {
        this.removeAttribute("role");
        this.removeAttribute("aria-label");
        this.setAttribute("aria-hidden", "true");
      }
    }
    async setIcon() {
      const { url, fromLibrary } = await this.getIconSource();
      const library2 = fromLibrary ? getIconLibrary(this.library) : void 0;
      if (!url) {
        this.svg = null;
        return;
      }
      let iconResolver = iconCache.get(url);
      if (!iconResolver) {
        iconResolver = this.resolveIcon(url, library2);
        iconCache.set(url, iconResolver);
      }
      const svg = await iconResolver;
      if (svg === RETRYABLE_ERROR) {
        iconCache.delete(url);
      }
      const sourceAfterFetch = await this.getIconSource();
      if (url !== sourceAfterFetch.url) {
        return;
      }
      if (l3(svg)) {
        this.svg = svg;
        return;
      }
      switch (svg) {
        case RETRYABLE_ERROR:
        case CACHEABLE_ERROR:
          this.svg = null;
          this.dispatchEvent(new CsErrorEvent());
          break;
        default:
          this.svg = svg.cloneNode(true);
          library2?.mutator?.(this.svg, this);
          this.dispatchEvent(new CsLoadEvent());
      }
    }
    willUpdate(changedProperties) {
      if (!this.style) {
        this.setStyleProperty("--rotate-angle", `${this.rotate}deg`);
      }
      return super.willUpdate(changedProperties);
    }
    updated(changedProperties) {
      super.updated(changedProperties);
      const library2 = getIconLibrary(this.library);
      if (this.hasAttribute("rotate")) {
        this.style.setProperty("--rotate-angle", `${this.rotate}deg`);
      }
      const svg = this.shadowRoot?.querySelector("svg");
      if (svg) {
        library2?.mutator?.(svg, this);
      }
    }
    render() {
      if (this.hasUpdated) {
        return this.svg;
      }
      return b2`<svg part="svg" width="16" height="16" viewBox="0 0 16 16"></svg>`;
    }
  };
  CsIcon.css = icon_styles_default;
  __decorateClass([
    r5()
  ], CsIcon.prototype, "svg", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsIcon.prototype, "name", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsIcon.prototype, "family", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsIcon.prototype, "variant", 2);
  __decorateClass([
    n4({ type: Number, reflect: true })
  ], CsIcon.prototype, "weight", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsIcon.prototype, "canvas", 2);
  __decorateClass([
    n4({ attribute: "auto-width", type: Boolean, reflect: true })
  ], CsIcon.prototype, "autoWidth", 2);
  __decorateClass([
    n4()
  ], CsIcon.prototype, "src", 2);
  __decorateClass([
    n4()
  ], CsIcon.prototype, "label", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsIcon.prototype, "library", 2);
  __decorateClass([
    n4({ type: Number, reflect: true })
  ], CsIcon.prototype, "rotate", 2);
  __decorateClass([
    n4({ type: String, reflect: true })
  ], CsIcon.prototype, "flip", 2);
  __decorateClass([
    n4({ type: String, reflect: true })
  ], CsIcon.prototype, "animation", 2);
  __decorateClass([
    watch("label")
  ], CsIcon.prototype, "handleLabelChange", 1);
  __decorateClass([
    watch(["family", "name", "library", "variant", "weight", "src", "autoWidth", "canvas"], {
      waitUntilFirstUpdate: true
    })
  ], CsIcon.prototype, "setIcon", 1);
  CsIcon = __decorateClass([
    customElement("cs-icon")
  ], CsIcon);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.BWP7Q3QU.js
  var animated_image_styles_default = i`
  :host {
    --control-box-size: 3rem;
    --icon-size: calc(var(--control-box-size) * 0.625);

    display: inline-flex;
    position: relative;
    cursor: pointer;
  }

  img {
    display: block;
    width: 100%;
    height: 100%;
  }

  img[aria-hidden='true'] {
    display: none;
  }

  .control-box {
    display: flex;
    position: absolute;
    align-items: center;
    justify-content: center;
    top: calc(50% - var(--control-box-size) / 2);
    right: calc(50% - var(--control-box-size) / 2);
    width: var(--control-box-size);
    height: var(--control-box-size);
    font-size: calc(var(--icon-size) * 0.75);
    background: none;
    border: solid var(--cs-border-width-s) currentColor;
    background-color: rgb(0 0 0 / 50%);
    border-radius: var(--cs-border-radius-circle);
    color: white;
    pointer-events: none;
    transition: opacity var(--cs-transition-normal) var(--cs-transition-easing);
  }

  @media (hover: hover) {
    :host([play]:hover) .control-box {
      opacity: 1;
    }
  }

  :where(:host([play]:not(:hover))) .control-box {
    opacity: 0;
  }

  :host([play]) slot[name='play-icon'],
  :host(:not([play])) slot[name='pause-icon'] {
    display: none;
  }

  /* Show control box on keyboard focus */
  .animated-image {
    &:focus {
      outline: none;
    }

    &:focus-visible .control-box {
      opacity: 1;
      outline: var(--cs-focus-ring);
      outline-offset: var(--cs-focus-ring-offset);
    }
  }
`;

  // node_modules/lit-html/directives/style-map.js
  var n5 = "important";
  var i6 = " !" + n5;
  var o7 = e6(class extends i5 {
    constructor(t8) {
      if (super(t8), t8.type !== t5.ATTRIBUTE || "style" !== t8.name || t8.strings?.length > 2) throw Error("The `styleMap` directive must be used in the `style` attribute and must be the only part in the attribute.");
    }
    render(t8) {
      return Object.keys(t8).reduce((e12, r9) => {
        const s7 = t8[r9];
        return null == s7 ? e12 : e12 + `${r9 = r9.includes("-") ? r9 : r9.replace(/(?:^(webkit|moz|ms|o)|)(?=[A-Z])/g, "-$&").toLowerCase()}:${s7};`;
      }, "");
    }
    update(e12, [r9]) {
      const { style: s7 } = e12.element;
      if (void 0 === this.ft) return this.ft = new Set(Object.keys(r9)), this.render(r9);
      for (const t8 of this.ft) null == r9[t8] && (this.ft.delete(t8), t8.includes("-") ? s7.removeProperty(t8) : s7[t8] = null);
      for (const t8 in r9) {
        const e13 = r9[t8];
        if (null != e13) {
          this.ft.add(t8);
          const r10 = "string" == typeof e13 && e13.endsWith(i6);
          t8.includes("-") || r10 ? s7.setProperty(t8, r10 ? e13.slice(0, -11) : e13, r10 ? n5 : "") : s7[t8] = e13;
        }
      }
      return E;
    }
  });

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.UFR3AR65.js
  var CsAnimatedImage = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.localize = new LocalizeController2(this);
      this.isLoaded = false;
    }
    handleClick() {
      this.play = !this.play;
    }
    handleKeyDown(event) {
      if (event.key === "Enter" || event.key === " ") {
        event.preventDefault();
        this.play = !this.play;
      }
    }
    firstUpdated(changedProperties) {
      if (this.didSSR) {
        const img = this.animatedImage;
        if (img && img.complete) {
          if (img.naturalWidth > 0) {
            img.dispatchEvent(new Event("load"));
          } else {
            img.dispatchEvent(new Event("error"));
          }
        }
      }
      super.firstUpdated(changedProperties);
    }
    handleLoad() {
      const canvas = document.createElement("canvas");
      const { width, height } = this.animatedImage;
      canvas.width = width;
      canvas.height = height;
      canvas.getContext("2d").drawImage(this.animatedImage, 0, 0, width, height);
      this.frozenFrame = canvas.toDataURL("image/gif");
      if (!this.isLoaded) {
        this.dispatchEvent(new CsLoadEvent());
        this.isLoaded = true;
      }
    }
    handleError() {
      this.dispatchEvent(new CsErrorEvent());
    }
    handlePlayChange() {
      if (this.play) {
        this.animatedImage.src = "";
        this.animatedImage.src = this.src;
      }
    }
    handleSrcChange() {
      this.isLoaded = false;
    }
    render() {
      const verb = this.localize.term(this.play ? "pauseAnimation" : "playAnimation");
      const label = `${verb} ${this.alt}`;
      const shouldShow = this.didSSR && !this.hasUpdated || this.play;
      return b2`
      <div
        class="animated-image"
        tabindex="0"
        role="button"
        aria-pressed=${this.play ? "true" : "false"}
        aria-label=${label}
        @click=${this.handleClick}
        @keydown=${this.handleKeyDown}
      >
        <img
          class="animated"
          src=${this.src}
          alt=${this.alt}
          crossorigin="anonymous"
          aria-hidden=${shouldShow ? "false" : "true"}
          style="visibility: hidden;"
          role="presentation"
          @load=${this.handleLoad}
          @error=${this.handleError}
        />

        ${this.isLoaded ? b2`
                <img
                  class="frozen"
                  src=${this.frozenFrame}
                  alt=${this.alt}
                  aria-hidden=${this.play ? "true" : "false"}
                  role="presentation"
                />

                <div part="control-box" class="control-box" aria-hidden="true">
                  <slot name="play-icon">
                    <cs-icon
                      name="play_arrow"
                      library="system"
                      class="default"
                      style=${o7({ "margin-inline-start": "3px" })}
                    ></cs-icon>
                  </slot>
                  <slot name="pause-icon">
                    <cs-icon name="pause" library="system" variant="fill" class="default"></cs-icon>
                  </slot>
                </div>
              ` : ""}
      </div>
    `;
    }
  };
  CsAnimatedImage.css = animated_image_styles_default;
  __decorateClass([
    e5(".animated")
  ], CsAnimatedImage.prototype, "animatedImage", 2);
  __decorateClass([
    r5()
  ], CsAnimatedImage.prototype, "frozenFrame", 2);
  __decorateClass([
    r5()
  ], CsAnimatedImage.prototype, "isLoaded", 2);
  __decorateClass([
    n4()
  ], CsAnimatedImage.prototype, "src", 2);
  __decorateClass([
    n4()
  ], CsAnimatedImage.prototype, "alt", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsAnimatedImage.prototype, "play", 2);
  __decorateClass([
    watch("play", { waitUntilFirstUpdate: true })
  ], CsAnimatedImage.prototype, "handlePlayChange", 1);
  __decorateClass([
    watch("src")
  ], CsAnimatedImage.prototype, "handleSrcChange", 1);
  CsAnimatedImage = __decorateClass([
    customElement("cs-animated-image")
  ], CsAnimatedImage);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.HYZIRQPO.js
  var CsStartEvent = class extends Event {
    constructor() {
      super("cs-start", { bubbles: true, cancelable: false, composed: true });
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.GIMFLC2P.js
  var CsFinishEvent = class extends Event {
    constructor() {
      super("cs-finish", { bubbles: true, cancelable: false, composed: true });
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.JWIY4LQX.js
  var CsCancelEvent = class extends Event {
    constructor() {
      super("cs-cancel", { bubbles: true, cancelable: false, composed: true });
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.A4RQJLJW.js
  var animation_styles_default = i`
  :host {
    display: contents;
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.FZGW55TH.js
  var CsAnimation = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.hasStarted = false;
      this.name = "none";
      this.play = false;
      this.delay = 0;
      this.direction = "normal";
      this.duration = 1e3;
      this.easing = "linear";
      this.endDelay = 0;
      this.fill = "auto";
      this.iterations = Infinity;
      this.iterationStart = 0;
      this.playbackRate = 1;
      this.handleAnimationFinish = () => {
        this.play = false;
        this.hasStarted = false;
        this.dispatchEvent(new CsFinishEvent());
      };
      this.handleAnimationCancel = () => {
        this.play = false;
        this.hasStarted = false;
        this.dispatchEvent(new CsCancelEvent());
      };
    }
    /** Gets and sets the current animation time. */
    get currentTime() {
      return this.animation?.currentTime ?? 0;
    }
    set currentTime(time) {
      if (this.animation) {
        this.animation.currentTime = time;
      }
    }
    connectedCallback() {
      super.connectedCallback();
      if ("animate" in this) {
        this.createAnimation();
      }
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      if ("animate" in this) {
        this.destroyAnimation();
      }
    }
    handleSlotChange() {
      this.destroyAnimation();
      this.createAnimation();
    }
    async createAnimation() {
      const easing = dist_exports.easings[this.easing] ?? this.easing;
      const keyframes = this.keyframes ?? dist_exports[this.name];
      const slot = await this.defaultSlot;
      const element = slot.assignedElements()[0];
      if (!element || !keyframes) {
        return false;
      }
      this.destroyAnimation();
      this.animation = element.animate(keyframes, {
        delay: this.delay,
        direction: this.direction,
        duration: this.duration,
        easing,
        endDelay: this.endDelay,
        fill: this.fill,
        iterationStart: this.iterationStart,
        iterations: this.iterations
      });
      this.animation.playbackRate = this.playbackRate;
      this.animation.addEventListener("cancel", this.handleAnimationCancel);
      this.animation.addEventListener("finish", this.handleAnimationFinish);
      if (this.play) {
        this.hasStarted = true;
        this.dispatchEvent(new CsStartEvent());
      } else {
        this.animation.pause();
      }
      return true;
    }
    destroyAnimation() {
      if (this.animation) {
        this.animation.cancel();
        this.animation.removeEventListener("cancel", this.handleAnimationCancel);
        this.animation.removeEventListener("finish", this.handleAnimationFinish);
        this.hasStarted = false;
      }
    }
    handleAnimationChange() {
      if (!this.hasUpdated) {
        return;
      }
      this.createAnimation();
    }
    handlePlayChange() {
      if (this.animation) {
        if (this.play && !this.hasStarted) {
          this.hasStarted = true;
          this.dispatchEvent(new CsStartEvent());
        }
        if (this.play) {
          this.animation.play();
        } else {
          this.animation.pause();
        }
        return true;
      }
      return false;
    }
    handlePlaybackRateChange() {
      if (this.animation) {
        this.animation.playbackRate = this.playbackRate;
      }
    }
    /** Clears all keyframe effects caused by this animation and aborts its playback. */
    cancel() {
      this.animation?.cancel();
    }
    /** Sets the playback time to the end of the animation corresponding to the current playback direction. */
    finish() {
      this.animation?.finish();
    }
    render() {
      return b2` <slot @slotchange=${this.handleSlotChange}></slot> `;
    }
  };
  CsAnimation.css = animation_styles_default;
  __decorateClass([
    r6("slot")
  ], CsAnimation.prototype, "defaultSlot", 2);
  __decorateClass([
    n4()
  ], CsAnimation.prototype, "name", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsAnimation.prototype, "play", 2);
  __decorateClass([
    n4({ type: Number })
  ], CsAnimation.prototype, "delay", 2);
  __decorateClass([
    n4()
  ], CsAnimation.prototype, "direction", 2);
  __decorateClass([
    n4({ type: Number })
  ], CsAnimation.prototype, "duration", 2);
  __decorateClass([
    n4()
  ], CsAnimation.prototype, "easing", 2);
  __decorateClass([
    n4({ attribute: "end-delay", type: Number })
  ], CsAnimation.prototype, "endDelay", 2);
  __decorateClass([
    n4()
  ], CsAnimation.prototype, "fill", 2);
  __decorateClass([
    n4({ type: Number })
  ], CsAnimation.prototype, "iterations", 2);
  __decorateClass([
    n4({ attribute: "iteration-start", type: Number })
  ], CsAnimation.prototype, "iterationStart", 2);
  __decorateClass([
    n4({ attribute: false })
  ], CsAnimation.prototype, "keyframes", 2);
  __decorateClass([
    n4({ attribute: "playback-rate", type: Number })
  ], CsAnimation.prototype, "playbackRate", 2);
  __decorateClass([
    watch([
      "name",
      "delay",
      "direction",
      "duration",
      "easing",
      "endDelay",
      "fill",
      "iterations",
      "iterationsStart",
      "keyframes"
    ])
  ], CsAnimation.prototype, "handleAnimationChange", 1);
  __decorateClass([
    watch("play")
  ], CsAnimation.prototype, "handlePlayChange", 1);
  __decorateClass([
    watch("playbackRate")
  ], CsAnimation.prototype, "handlePlaybackRateChange", 1);
  CsAnimation = __decorateClass([
    customElement("cs-animation")
  ], CsAnimation);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.INLPCBCX.js
  var avatar_styles_default = i`
  :host {
    --size: 3rem;

    display: inline-flex;
    align-items: center;
    justify-content: center;
    position: relative;
    width: var(--size);
    height: var(--size);
    color: var(--cs-color-neutral-on-normal);
    font: inherit;
    font-size: calc(var(--size) * 0.4);
    vertical-align: middle;
    background-color: var(--cs-color-neutral-fill-normal);
    border-radius: var(--cs-border-radius-circle);
    user-select: none;
    -webkit-user-select: none;
  }

  :host([shape='square']) {
    border-radius: 0;
  }

  :host([shape='rounded']) {
    border-radius: var(--cs-border-radius-m);
  }

  .icon {
    display: flex;
    align-items: center;
    justify-content: center;
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
  }

  .initials {
    line-height: 1;
    text-transform: uppercase;
  }

  .image {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    overflow: hidden;
    border-radius: inherit;
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.CXR5VX3Q.js
  var CsAvatar = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.hasError = false;
      this.image = "";
      this.label = "";
      this.initials = "";
      this.loading = "eager";
      this.shape = "circle";
    }
    handleImageChange() {
      this.hasError = false;
    }
    handleImageLoadError() {
      this.hasError = true;
      this.dispatchEvent(new CsErrorEvent());
    }
    connectedCallback() {
      super.connectedCallback();
      if (this.didSSR) {
        const img = this.shadowRoot?.querySelector?.("img");
        if (img && img.complete && img.naturalWidth <= 0) {
          this.updateComplete.then(() => {
            this.handleImageLoadError();
          });
        }
      }
    }
    render() {
      const avatarWithImage = b2`
      <img
        part="image"
        class="image"
        src="${this.image}"
        loading="${this.loading}"
        role="img"
        aria-label=${this.label}
        @error="${this.handleImageLoadError}"
      />
    `;
      let avatarWithoutImage;
      if (this.initials) {
        avatarWithoutImage = b2`<div part="initials" class="initials" role="img" aria-label=${this.label}>
        ${this.initials}
      </div>`;
      } else {
        avatarWithoutImage = b2`
        <slot name="icon" part="icon" class="icon" role="img" aria-label=${this.label}>
          <cs-icon name="person" library="system"></cs-icon>
        </slot>
      `;
      }
      return b2` ${this.image && !this.hasError ? avatarWithImage : avatarWithoutImage} `;
    }
  };
  CsAvatar.css = avatar_styles_default;
  __decorateClass([
    r5()
  ], CsAvatar.prototype, "hasError", 2);
  __decorateClass([
    n4()
  ], CsAvatar.prototype, "image", 2);
  __decorateClass([
    n4()
  ], CsAvatar.prototype, "label", 2);
  __decorateClass([
    n4()
  ], CsAvatar.prototype, "initials", 2);
  __decorateClass([
    n4()
  ], CsAvatar.prototype, "loading", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsAvatar.prototype, "shape", 2);
  __decorateClass([
    watch("image")
  ], CsAvatar.prototype, "handleImageChange", 1);
  CsAvatar = __decorateClass([
    customElement("cs-avatar")
  ], CsAvatar);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.IANKKS7L.js
  var variants_styles_default = i`
  :where(:root),
  .cs-neutral,
  :host([variant='neutral']) {
    --cs-color-fill-loud: var(--cs-color-neutral-fill-loud);
    --cs-color-fill-normal: var(--cs-color-neutral-fill-normal);
    --cs-color-fill-quiet: var(--cs-color-neutral-fill-quiet);
    --cs-color-border-loud: var(--cs-color-neutral-border-loud);
    --cs-color-border-normal: var(--cs-color-neutral-border-normal);
    --cs-color-border-quiet: var(--cs-color-neutral-border-quiet);
    --cs-color-on-loud: var(--cs-color-neutral-on-loud);
    --cs-color-on-normal: var(--cs-color-neutral-on-normal);
    --cs-color-on-quiet: var(--cs-color-neutral-on-quiet);
  }

  .cs-brand,
  :host([variant='brand']) {
    --cs-color-fill-loud: var(--cs-color-brand-fill-loud);
    --cs-color-fill-normal: var(--cs-color-brand-fill-normal);
    --cs-color-fill-quiet: var(--cs-color-brand-fill-quiet);
    --cs-color-border-loud: var(--cs-color-brand-border-loud);
    --cs-color-border-normal: var(--cs-color-brand-border-normal);
    --cs-color-border-quiet: var(--cs-color-brand-border-quiet);
    --cs-color-on-loud: var(--cs-color-brand-on-loud);
    --cs-color-on-normal: var(--cs-color-brand-on-normal);
    --cs-color-on-quiet: var(--cs-color-brand-on-quiet);
  }

  .cs-success,
  :host([variant='success']) {
    --cs-color-fill-loud: var(--cs-color-success-fill-loud);
    --cs-color-fill-normal: var(--cs-color-success-fill-normal);
    --cs-color-fill-quiet: var(--cs-color-success-fill-quiet);
    --cs-color-border-loud: var(--cs-color-success-border-loud);
    --cs-color-border-normal: var(--cs-color-success-border-normal);
    --cs-color-border-quiet: var(--cs-color-success-border-quiet);
    --cs-color-on-loud: var(--cs-color-success-on-loud);
    --cs-color-on-normal: var(--cs-color-success-on-normal);
    --cs-color-on-quiet: var(--cs-color-success-on-quiet);
  }

  .cs-warning,
  :host([variant='warning']) {
    --cs-color-fill-loud: var(--cs-color-warning-fill-loud);
    --cs-color-fill-normal: var(--cs-color-warning-fill-normal);
    --cs-color-fill-quiet: var(--cs-color-warning-fill-quiet);
    --cs-color-border-loud: var(--cs-color-warning-border-loud);
    --cs-color-border-normal: var(--cs-color-warning-border-normal);
    --cs-color-border-quiet: var(--cs-color-warning-border-quiet);
    --cs-color-on-loud: var(--cs-color-warning-on-loud);
    --cs-color-on-normal: var(--cs-color-warning-on-normal);
    --cs-color-on-quiet: var(--cs-color-warning-on-quiet);
  }

  .cs-highlight,
  :host([variant='highlight']) {
    --cs-color-fill-loud: var(--cs-color-highlight-fill-loud);
    --cs-color-fill-normal: var(--cs-color-highlight-fill-normal);
    --cs-color-fill-quiet: var(--cs-color-highlight-fill-quiet);
    --cs-color-border-loud: var(--cs-color-highlight-border-loud);
    --cs-color-border-normal: var(--cs-color-highlight-border-normal);
    --cs-color-border-quiet: var(--cs-color-highlight-border-quiet);
    --cs-color-on-loud: var(--cs-color-highlight-on-loud);
    --cs-color-on-normal: var(--cs-color-highlight-on-normal);
    --cs-color-on-quiet: var(--cs-color-highlight-on-quiet);
  }

  .cs-information,
  :host([variant='information']) {
    --cs-color-fill-loud: var(--cs-color-information-fill-loud);
    --cs-color-fill-normal: var(--cs-color-information-fill-normal);
    --cs-color-fill-quiet: var(--cs-color-information-fill-quiet);
    --cs-color-border-loud: var(--cs-color-information-border-loud);
    --cs-color-border-normal: var(--cs-color-information-border-normal);
    --cs-color-border-quiet: var(--cs-color-information-border-quiet);
    --cs-color-on-loud: var(--cs-color-information-on-loud);
    --cs-color-on-normal: var(--cs-color-information-on-normal);
    --cs-color-on-quiet: var(--cs-color-information-on-quiet);
  }

  .cs-danger,
  :host([variant='danger']) {
    --cs-color-fill-loud: var(--cs-color-danger-fill-loud);
    --cs-color-fill-normal: var(--cs-color-danger-fill-normal);
    --cs-color-fill-quiet: var(--cs-color-danger-fill-quiet);
    --cs-color-border-loud: var(--cs-color-danger-border-loud);
    --cs-color-border-normal: var(--cs-color-danger-border-normal);
    --cs-color-border-quiet: var(--cs-color-danger-border-quiet);
    --cs-color-on-loud: var(--cs-color-danger-on-loud);
    --cs-color-on-normal: var(--cs-color-danger-on-normal);
    --cs-color-on-quiet: var(--cs-color-danger-on-quiet);
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.OE2PJV7Z.js
  var badge_styles_default = i`
  :host {
    --pulse-color: var(--cs-color-fill-loud, var(--cs-color-brand-fill-loud));

    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.375em 0.625em;
    color: var(--cs-color-on-loud, var(--cs-color-brand-on-loud));
    font-size: max(var(--cs-font-size-3xs), 0.75em);
    font-weight: var(--cs-font-weight-semibold);
    line-height: 1;
    vertical-align: middle;
    white-space: nowrap;
    background-color: var(--cs-color-fill-loud, var(--cs-color-brand-fill-loud));
    border-color: transparent;
    border-radius: var(--cs-border-radius-s);
    border-style: var(--cs-border-style);
    border-width: var(--cs-border-width-s);
    user-select: none;
    -webkit-user-select: none;
    cursor: inherit;

    min-width: 1.25em; /* <-- this is what Safari respects for intrinsic */
    min-height: 1em;
  }

  /* Appearance modifiers */
  :host([appearance='outlined']) {
    --pulse-color: var(--cs-color-border-loud, var(--cs-color-brand-border-loud));

    color: var(--cs-color-on-quiet, var(--cs-color-brand-on-quiet));
    background-color: transparent;
    border-color: var(--cs-color-border-loud, var(--cs-color-brand-border-loud));
  }

  :host([appearance='filled']) {
    --pulse-color: var(--cs-color-fill-normal, var(--cs-color-brand-fill-normal));

    color: var(--cs-color-on-normal, var(--cs-color-brand-on-normal));
    background-color: var(--cs-color-fill-normal, var(--cs-color-brand-fill-normal));
    border-color: transparent;
  }

  :host([appearance='filled-outlined']) {
    --pulse-color: var(--cs-color-border-normal, var(--cs-color-brand-border-normal));

    color: var(--cs-color-on-normal, var(--cs-color-brand-on-normal));
    background-color: var(--cs-color-fill-normal, var(--cs-color-brand-fill-normal));
    border-color: var(--cs-color-border-normal, var(--cs-color-brand-border-normal));
  }

  :host([appearance='accent']) {
    --pulse-color: var(--cs-color-fill-loud, var(--cs-color-brand-fill-loud));

    color: var(--cs-color-on-loud, var(--cs-color-brand-on-loud));
    background-color: var(--cs-color-fill-loud, var(--cs-color-brand-fill-loud));
    border-color: transparent;
  }

  /* Pill modifier */
  :host([pill]) {
    border-radius: var(--cs-border-radius-pill);
  }

  /* Pulse attention */
  :host([attention='pulse']) {
    animation: pulse 1.5s infinite;
  }

  @keyframes pulse {
    0% {
      box-shadow: 0 0 0 0 var(--pulse-color);
    }
    70% {
      box-shadow: 0 0 0 0.5rem transparent;
    }
    100% {
      box-shadow: 0 0 0 0 transparent;
    }
  }

  /* Bounce attention */
  :host([attention='bounce']) {
    animation: bounce 1s cubic-bezier(0.28, 0.84, 0.42, 1) infinite;
  }

  @keyframes bounce {
    0%,
    20%,
    50%,
    80%,
    100% {
      transform: translateY(0);
    }
    40% {
      transform: translateY(-5px);
    }
    60% {
      transform: translateY(-2px);
    }
  }

  /* Prevents vertical space when icons with vertical-align are slotted in - https://github.com/shoelace-style/webawesome/issues/2280 */
  [part='start'],
  [part='end'] {
    line-height: 0;
  }

  /* These are !important because a document-level universal margin reset would otherwise beat them:
     a slotted element belongs to the outer tree. See callout.styles.ts for the full note. */
  slot[name='start']::slotted(*) {
    margin-inline-end: 0.375em !important;
  }

  slot[name='end']::slotted(*) {
    margin-inline-start: 0.375em !important;
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.4ZCTA2FF.js
  var CsBadge = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.variant = "brand";
      this.appearance = "accent";
      this.pill = false;
      this.attention = "none";
    }
    render() {
      return b2`
      <span part="start">
        <slot name="start"></slot>
      </span>

      <span part="badge" role="status">
        <slot></slot>
      </span>

      <span part="end">
        <slot name="end"></slot>
      </span>
    `;
    }
  };
  CsBadge.css = [variants_styles_default, badge_styles_default];
  __decorateClass([
    n4({ reflect: true })
  ], CsBadge.prototype, "variant", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsBadge.prototype, "appearance", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsBadge.prototype, "pill", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsBadge.prototype, "attention", 2);
  CsBadge = __decorateClass([
    customElement("cs-badge")
  ], CsBadge);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.CGEM72FY.js
  var breadcrumb_styles_default = i`
  .breadcrumb {
    display: flex;
    align-items: center;
    flex-wrap: wrap;
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.RTTAZ4FU.js
  var CsBreadcrumb = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.localize = new LocalizeController2(this);
      this.separatorDir = this.localize.dir();
      this.label = "";
    }
    // Generates a clone of the separator element to use for each breadcrumb item
    getSeparator() {
      const separator = this.separatorSlot.assignedElements({ flatten: true })[0];
      const clone = separator.cloneNode(true);
      [clone, ...clone.querySelectorAll("[id]")].forEach((el) => el.removeAttribute("id"));
      clone.setAttribute("data-default", "");
      clone.slot = "separator";
      return clone;
    }
    handleSlotChange() {
      const items = [...this.defaultSlot.assignedElements({ flatten: true })].filter(
        (item) => item.tagName.toLowerCase() === "cs-breadcrumb-item"
      );
      items.forEach((item, index) => {
        const separator = item.querySelector('[slot="separator"]');
        if (separator === null) {
          item.append(this.getSeparator());
        } else if (separator.hasAttribute("data-default")) {
          separator.replaceWith(this.getSeparator());
        } else {
        }
        if (index === items.length - 1) {
          item.setAttribute("aria-current", "page");
        } else {
          item.removeAttribute("aria-current");
        }
      });
    }
    render() {
      if (this.separatorDir !== this.localize.dir()) {
        this.separatorDir = this.localize.dir();
        this.updateComplete.then(() => this.handleSlotChange());
      }
      return b2`
      <nav part="breadcrumb" class="breadcrumb" aria-label=${this.label}>
        <slot @slotchange=${this.handleSlotChange}></slot>
      </nav>

      <span hidden aria-hidden="true">
        <slot name="separator">
          <cs-icon
            name=${this.localize.dir() === "rtl" ? "keyboard_arrow_left" : "keyboard_arrow_right"}
            library="system"
          ></cs-icon>
        </slot>
      </span>
    `;
    }
  };
  CsBreadcrumb.css = breadcrumb_styles_default;
  __decorateClass([
    e5("slot")
  ], CsBreadcrumb.prototype, "defaultSlot", 2);
  __decorateClass([
    e5('slot[name="separator"]')
  ], CsBreadcrumb.prototype, "separatorSlot", 2);
  __decorateClass([
    n4()
  ], CsBreadcrumb.prototype, "label", 2);
  CsBreadcrumb = __decorateClass([
    customElement("cs-breadcrumb")
  ], CsBreadcrumb);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.PDGRJMP3.js
  var breadcrumb_item_styles_default = i`
  :host {
    color: var(--cs-color-text-link);
    display: inline-flex;
    align-items: center;
    font: inherit;
    font-weight: var(--cs-font-weight-action);
    line-height: var(--cs-line-height-normal);
    white-space: nowrap;
  }

  :host(:last-of-type) {
    color: var(--cs-color-text-quiet);
  }

  .label {
    display: inline-block;
    font: inherit;
    text-decoration: none;
    color: currentColor;
    background: none;
    border: none;
    border-radius: var(--cs-border-radius-m);
    padding: 0;
    margin: 0;
    cursor: pointer;
    transition: color var(--cs-transition-normal) var(--cs-transition-easing);
  }

  @media (hover: hover) {
    :host(:not(:last-of-type)) .label:hover {
      color: color-mix(in oklab, currentColor, var(--cs-color-mix-hover));
    }
  }

  :host(:not(:last-of-type)) .label:active {
    color: color-mix(in oklab, currentColor, var(--cs-color-mix-active));
  }

  .label:focus {
    outline: none;
  }

  .label:focus-visible {
    outline: var(--cs-focus-ring);
    outline-offset: var(--cs-focus-ring-offset);
  }

  .start,
  .end {
    display: none;
    flex: 0 0 auto;
    display: flex;
    align-items: center;
  }

  .start,
  .end {
    display: inline-flex;
    color: var(--cs-color-text-quiet);
  }

  /* These are !important because a document-level universal margin reset would otherwise beat them:
     a slotted element belongs to the outer tree. See callout.styles.ts for the full note. */
  ::slotted([slot='start']) {
    margin-inline-end: var(--cs-space-s) !important;
  }

  ::slotted([slot='end']) {
    margin-inline-start: var(--cs-space-s) !important;
  }

  :host(:last-of-type) .separator {
    display: none;
  }

  .separator {
    color: var(--cs-color-text-quiet);
    display: inline-flex;
    align-items: center;
    margin: 0 var(--cs-space-s);
    user-select: none;
    -webkit-user-select: none;
  }
`;

  // node_modules/lit-html/directives/if-defined.js
  var o8 = (o13) => o13 ?? A;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.SHF5YPMV.js
  var CsBreadcrumbItem = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.renderType = "button";
      this.rel = "noreferrer noopener";
    }
    setRenderType() {
      const hasDropdown = this.defaultSlot.assignedElements({ flatten: true }).filter((i10) => i10.tagName.toLowerCase() === "cs-dropdown").length > 0;
      if (typeof this.href === "string") {
        this.renderType = "link";
        return;
      }
      if (hasDropdown) {
        this.renderType = "dropdown";
        return;
      }
      this.renderType = "button";
    }
    hrefChanged() {
      this.setRenderType();
    }
    handleSlotChange() {
      this.setRenderType();
    }
    render() {
      return b2`
      <span part="start" class="start">
        <slot name="start"></slot>
      </span>

      ${this.renderType === "link" ? b2`
              <a
                part="label"
                class="label label-link"
                href="${this.href}"
                target="${o8(this.target ? this.target : void 0)}"
                rel=${o8(this.target ? this.rel : void 0)}
              >
                <slot></slot>
              </a>
            ` : ""}
      ${this.renderType === "button" ? b2`
              <button part="label" type="button" class="label label-button">
                <slot @slotchange=${this.handleSlotChange}></slot>
              </button>
            ` : ""}
      ${this.renderType === "dropdown" ? b2`
              <div part="label" class="label label-dropdown">
                <slot @slotchange=${this.handleSlotChange}></slot>
              </div>
            ` : ""}

      <span part="end" class="end">
        <slot name="end"></slot>
      </span>

      <span part="separator" class="separator" aria-hidden="true">
        <slot name="separator"></slot>
      </span>
    `;
    }
  };
  CsBreadcrumbItem.css = breadcrumb_item_styles_default;
  __decorateClass([
    e5("slot:not([name])")
  ], CsBreadcrumbItem.prototype, "defaultSlot", 2);
  __decorateClass([
    r5()
  ], CsBreadcrumbItem.prototype, "renderType", 2);
  __decorateClass([
    n4()
  ], CsBreadcrumbItem.prototype, "href", 2);
  __decorateClass([
    n4()
  ], CsBreadcrumbItem.prototype, "target", 2);
  __decorateClass([
    n4()
  ], CsBreadcrumbItem.prototype, "rel", 2);
  __decorateClass([
    watch("href", { waitUntilFirstUpdate: true })
  ], CsBreadcrumbItem.prototype, "hrefChanged", 1);
  CsBreadcrumbItem = __decorateClass([
    customElement("cs-breadcrumb-item")
  ], CsBreadcrumbItem);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.6WQS6DWD.js
  var MirrorValidator = () => {
    return {
      checkValidity(element) {
        const formControl = element.input;
        const validity = {
          message: "",
          isValid: true,
          invalidKeys: []
        };
        if (!formControl) {
          return validity;
        }
        let isValid = true;
        if ("checkValidity" in formControl) {
          isValid = formControl.checkValidity();
        }
        if (isValid) {
          return validity;
        }
        validity.isValid = false;
        if ("validationMessage" in formControl) {
          validity.message = formControl.validationMessage;
        }
        if (!("validity" in formControl)) {
          validity.invalidKeys.push("customError");
          return validity;
        }
        for (const key in formControl.validity) {
          if (key === "valid") {
            continue;
          }
          const checkedKey = key;
          if (formControl.validity[checkedKey]) {
            validity.invalidKeys.push(checkedKey);
          }
        }
        return validity;
      }
    };
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.NYORTQAI.js
  var CsInvalidEvent = class extends Event {
    constructor() {
      super("cs-invalid", { bubbles: true, cancelable: false, composed: true });
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.ITWO4C4O.js
  var CustomErrorValidator = () => {
    return {
      observedAttributes: ["custom-error"],
      checkValidity(element) {
        const validity = {
          message: "",
          isValid: true,
          invalidKeys: []
        };
        if (element.customError) {
          validity.message = element.customError;
          validity.isValid = false;
          validity.invalidKeys = ["customError"];
        }
        return validity;
      }
    };
  };
  var CornerstoneFormAssociatedElement = class extends CornerstoneElement {
    constructor() {
      super();
      this.name = null;
      this.disabled = false;
      this.required = false;
      this.assumeInteractionOn = ["input"];
      this.validators = [];
      this.valueHasChanged = false;
      this.hasInteracted = false;
      this.customError = null;
      this.emittedEvents = [];
      this.emitInvalid = (e12) => {
        if (e12.target !== this) {
          return;
        }
        this.hasInteracted = true;
        this.dispatchEvent(new CsInvalidEvent());
      };
      this.handleInteraction = (event) => {
        const emittedEvents = this.emittedEvents;
        if (!emittedEvents.includes(event.type)) {
          emittedEvents.push(event.type);
        }
        if (emittedEvents.length === this.assumeInteractionOn?.length) {
          this.hasInteracted = true;
        }
      };
      if ("addEventListener" in this) {
        this.addEventListener("invalid", this.emitInvalid);
      }
    }
    /**
     * Validators are static because they have `observedAttributes`, essentially attributes to "watch"
     * for changes. Whenever these attributes change, we want to be notified and update the validator.
     */
    static get validators() {
      return o5 ? [] : [CustomErrorValidator()];
    }
    // Append all Validator "observedAttributes" into the "observedAttributes" so they can run.
    static get observedAttributes() {
      const parentAttrs = new Set(super.observedAttributes || []);
      for (const validator of this.validators) {
        if (!validator.observedAttributes) {
          continue;
        }
        for (const attr of validator.observedAttributes) {
          parentAttrs.add(attr);
        }
      }
      return [...parentAttrs];
    }
    connectedCallback() {
      super.connectedCallback();
      if (this.didSSR && !this.hasUpdated) {
        this.updateComplete.then(() => {
          this.updateValidity();
        });
      } else {
        this.updateValidity();
      }
      this.assumeInteractionOn.forEach((event) => {
        this.addEventListener?.(event, this.handleInteraction);
      });
    }
    firstUpdated(...args) {
      super.firstUpdated(...args);
      this.updateValidity();
    }
    willUpdate(changedProperties) {
      if (!o5 && changedProperties.has("customError")) {
        if (!this.customError) {
          this.customError = null;
        }
        this.setCustomValidity(this.customError || "");
      }
      if (changedProperties.has("value") || changedProperties.has("disabled") || changedProperties.has("defaultValue")) {
        const value = this.value;
        this.updateFormValue(value);
      }
      if (changedProperties.has("disabled")) {
        this.customStates.set("disabled", this.disabled);
        if (this.hasAttribute("disabled") || !o5 && !this.matches(":disabled")) {
          this.toggleAttribute("disabled", this.disabled);
        }
      }
      super.willUpdate(changedProperties);
      if (this.didSSR && !this.hasUpdated) {
        this.updateComplete.then(() => this.updateValidity());
      } else {
        this.updateValidity();
      }
    }
    /**
     * @internal
     */
    updateFormValue(value) {
      if (Array.isArray(value)) {
        if (this.name) {
          const formData = new FormData();
          for (const val of value) {
            formData.append(this.name, val);
          }
          this.setValue(formData, formData);
        }
      } else {
        this.setValue(value, value);
      }
    }
    get labels() {
      return this.internals.labels;
    }
    getForm() {
      return this.internals.form;
    }
    /**
     * By default, form controls are associated with the nearest containing `<form>` element. This attribute allows you
     * to place the form control outside of a form and associate it with the form that has this `id`. The form must be in
     * the same document or shadow root for this to work.
     */
    set form(val) {
      if (val) {
        this.setAttribute("form", val);
      } else {
        this.removeAttribute("form");
      }
    }
    get form() {
      return this.internals.form;
    }
    get validity() {
      return this.internals.validity;
    }
    // Not sure if this supports `novalidate`. Will need to test.
    get willValidate() {
      return this.internals.willValidate;
    }
    get validationMessage() {
      return this.internals.validationMessage;
    }
    checkValidity() {
      this.updateValidity();
      return this.internals.checkValidity();
    }
    reportValidity() {
      this.updateValidity();
      this.hasInteracted = true;
      return this.internals.reportValidity();
    }
    /**
     * Override this to change where constraint validation popups are anchored.
     */
    get validationTarget() {
      return this.input || void 0;
    }
    setValidity(...args) {
      const flags = args[0];
      const message = args[1];
      let anchor = args[2];
      if (!anchor) {
        anchor = this.validationTarget;
      }
      this.internals.setValidity(flags, message, anchor || void 0);
      this.requestUpdate("validity");
      this.setCustomStates();
    }
    setCustomStates() {
      const required = Boolean(this.required);
      const isValid = this.internals.validity.valid;
      const hasInteracted = this.hasInteracted;
      this.customStates.set("required", required);
      this.customStates.set("optional", !required);
      this.customStates.set("invalid", !isValid);
      this.customStates.set("valid", isValid);
      this.customStates.set("user-invalid", !isValid && hasInteracted);
      this.customStates.set("user-valid", isValid && hasInteracted);
    }
    /**
     * Do not use this when creating a "Validator". This is intended for end users of components.
     * We track manually defined custom errors so we don't clear them on accident in our validators.
     *
     */
    setCustomValidity(message) {
      if (!message) {
        this.customError = null;
        this.setValidity({});
        return;
      }
      this.customError = message;
      this.setValidity({ customError: true }, message, this.validationTarget);
    }
    formResetCallback() {
      this.resetValidity();
      this.hasInteracted = false;
      this.valueHasChanged = false;
      this.emittedEvents = [];
      this.updateValidity();
    }
    formDisabledCallback(isDisabled) {
      this.disabled = isDisabled;
      this.updateValidity();
    }
    /**
     * Called when the browser is trying to restore element’s state to state in which case reason is "restore", or when
     * the browser is trying to fulfill autofill on behalf of user in which case reason is "autocomplete". In the case of
     * "restore", state is a string, File, or FormData object previously set as the second argument to setFormValue.
     */
    formStateRestoreCallback(state, reason) {
      if (this.didSSR && !this.hasUpdated) {
        this.updateComplete.then(() => {
          this.value = state;
          if (reason === "restore") {
            this.resetValidity();
          }
          this.updateValidity();
        });
      } else {
        this.value = state;
        if (reason === "restore") {
          this.resetValidity();
        }
        this.updateValidity();
      }
    }
    setValue(...args) {
      const [value, state] = args;
      this.internals.setFormValue(value, state);
    }
    get allValidators() {
      const staticValidators = this.constructor.validators || [];
      const validators = this.validators || [];
      return [...staticValidators, ...validators];
    }
    /**
     * Reset validity is a way of removing manual custom errors and native validation.
     */
    resetValidity() {
      this.setCustomValidity("");
      this.setValidity({});
    }
    updateValidity() {
      if (this.disabled || this.hasAttribute("disabled") || !this.willValidate) {
        this.resetValidity();
        return;
      }
      const validators = this.allValidators;
      if (!validators?.length) {
        return;
      }
      const flags = {
        // Don't trust custom errors from the Browser. Safari breaks the spec.
        customError: Boolean(this.customError)
      };
      const formControl = this.validationTarget || this.input || void 0;
      let finalMessage = "";
      for (const validator of validators) {
        const { isValid, message, invalidKeys } = validator.checkValidity(this);
        if (isValid) {
          continue;
        }
        if (!finalMessage) {
          finalMessage = message;
        }
        if (invalidKeys?.length >= 0) {
          invalidKeys.forEach((str) => flags[str] = true);
        }
      }
      if (!finalMessage) {
        finalMessage = this.validationMessage;
      }
      this.setValidity(flags, finalMessage, formControl);
    }
  };
  CornerstoneFormAssociatedElement.formAssociated = true;
  __decorateClass([
    n4({ reflect: true })
  ], CornerstoneFormAssociatedElement.prototype, "name", 2);
  __decorateClass([
    n4({ type: Boolean })
  ], CornerstoneFormAssociatedElement.prototype, "disabled", 2);
  __decorateClass([
    n4({ state: true, attribute: false })
  ], CornerstoneFormAssociatedElement.prototype, "valueHasChanged", 2);
  __decorateClass([
    n4({ state: true, attribute: false })
  ], CornerstoneFormAssociatedElement.prototype, "hasInteracted", 2);
  __decorateClass([
    n4({ attribute: "custom-error", reflect: true })
  ], CornerstoneFormAssociatedElement.prototype, "customError", 2);
  __decorateClass([
    n4({ attribute: false, state: true, type: Object })
  ], CornerstoneFormAssociatedElement.prototype, "validity", 1);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.CXKOXFXU.js
  var size_styles_default = i`
  :host([size='xs']) {
    font-size: var(--cs-font-size-xs);
  }

  :host([size='s']) {
    font-size: var(--cs-font-size-s);
  }

  :host([size='m']) {
    font-size: var(--cs-font-size-m);
  }

  :host([size='l']) {
    font-size: var(--cs-font-size-l);
  }

  :host([size='xl']) {
    font-size: var(--cs-font-size-xl);
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.OYECECOT.js
  var button_styles_default = i`
  @layer cs-component {
    :host {
      display: inline-block;

      /* Workaround because Chrome doesn't like :host(:has()) below
       * https://issues.chromium.org/issues/40062355
       * Firefox doesn't like this nested rule, so both are needed */
      &:has(cs-badge) {
        position: relative;
      }
    }

    /* Apply relative positioning only when needed to position cs-badge
     * This avoids creating a new stacking context for every button */
    :host(:has(cs-badge)) {
      position: relative;
    }
  }

  .button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    text-decoration: none;
    user-select: none;
    -webkit-user-select: none;
    white-space: nowrap;
    vertical-align: middle;
    transition-property: background, border, box-shadow, color, opacity, transform;
    transition-duration: var(--cs-transition-fast);
    transition-timing-function: var(--cs-transition-easing);
    transform-origin: center;
    cursor: pointer;
    padding: 0 var(--cs-form-control-padding-inline);
    font-family: inherit;
    font-size: inherit;
    font-weight: var(--cs-font-weight-action);
    height: var(--cs-form-control-height);
    width: 100%;

    background-color: var(--cs-color-fill-loud, var(--cs-color-neutral-fill-loud));

    border-color: transparent;
    color: var(--cs-color-on-loud, var(--cs-color-neutral-on-loud));
    border-start-start-radius: var(--_button-start-start-radius, var(--cs-form-control-border-radius));
    border-start-end-radius: var(--_button-start-end-radius, var(--cs-form-control-border-radius));
    border-end-start-radius: var(--_button-end-start-radius, var(--cs-form-control-border-radius));
    border-end-end-radius: var(--_button-end-end-radius, var(--cs-form-control-border-radius));
    border-style: var(--cs-form-control-border-style);
    border-width: var(--cs-form-control-border-width);
  }

  /* Hover and active transforms */
  .button:not(.disabled):not(.loading) {
    @media (hover: hover) {
      &:hover {
        transform: var(--cs-button-transform-hover);
      }
    }
    &:active {
      transform: var(--cs-button-transform-active);
    }

    @media (prefers-reduced-motion: reduce) {
      &:hover,
      &:active {
        transform: none;
      }
    }
  }

  /* Appearance modifiers */
  :host([appearance='plain']) {
    /* Indentation overrides for grouping */
    margin-inline-start: var(--_button-horizontal-indent);
    margin-block-start: var(--_button-vertical-indent);

    .button {
      color: var(--cs-color-on-quiet, var(--cs-color-neutral-on-quiet));
      background-color: transparent;
      border-color: transparent;
    }
    @media (hover: hover) {
      .button:not(.disabled):not(.loading):hover {
        color: var(--cs-color-on-quiet, var(--cs-color-neutral-on-quiet));
        background-color: var(--cs-color-fill-quiet, var(--cs-color-neutral-fill-quiet));
      }
    }
    .button:not(.disabled):not(.loading):active {
      color: var(--cs-color-on-quiet, var(--cs-color-neutral-on-quiet));
      background-color: color-mix(
        in oklab,
        var(--cs-color-fill-quiet, var(--cs-color-neutral-fill-quiet)),
        var(--cs-color-mix-active)
      );
    }
  }

  :host([appearance='outlined']) {
    /* Indentation overrides for grouping outlined */
    margin-inline-start: var(--_button-horizontal-indent-outlined);
    margin-block-start: var(--_button-vertical-indent-outlined);

    .button {
      color: var(--cs-color-on-quiet, var(--cs-color-neutral-on-quiet));
      background-color: transparent;
      border-color: var(--cs-color-border-loud, var(--cs-color-neutral-border-loud));
    }
    @media (hover: hover) {
      .button:not(.disabled):not(.loading):hover {
        color: var(--cs-color-on-quiet, var(--cs-color-neutral-on-quiet));
        background-color: var(--cs-color-fill-quiet, var(--cs-color-neutral-fill-quiet));
      }
    }
    .button:not(.disabled):not(.loading):active {
      color: var(--cs-color-on-quiet, var(--cs-color-neutral-on-quiet));
      background-color: color-mix(
        in oklab,
        var(--cs-color-fill-quiet, var(--cs-color-neutral-fill-quiet)),
        var(--cs-color-mix-active)
      );
    }
  }

  :host([appearance='filled']) {
    /* Indentation overrides for grouping */
    margin-inline-start: var(--_button-horizontal-indent);
    margin-block-start: var(--_button-vertical-indent);

    .button {
      color: var(--cs-color-on-normal, var(--cs-color-neutral-on-normal));
      background-color: var(--cs-color-fill-normal, var(--cs-color-neutral-fill-normal));
      border-color: transparent;
    }
    @media (hover: hover) {
      .button:not(.disabled):not(.loading):hover {
        color: var(--cs-color-on-normal, var(--cs-color-neutral-on-normal));
        background-color: color-mix(
          in oklab,
          var(--cs-color-fill-normal, var(--cs-color-neutral-fill-normal)),
          var(--cs-color-mix-hover)
        );
      }
    }
    .button:not(.disabled):not(.loading):active {
      color: var(--cs-color-on-normal, var(--cs-color-neutral-on-normal));
      background-color: color-mix(
        in oklab,
        var(--cs-color-fill-normal, var(--cs-color-neutral-fill-normal)),
        var(--cs-color-mix-active)
      );
    }
  }

  :host([appearance='filled-outlined']) {
    /* Indentation overrides for grouping outlined */
    margin-inline-start: var(--_button-horizontal-indent-outlined);
    margin-block-start: var(--_button-vertical-indent-outlined);

    .button {
      color: var(--cs-color-on-normal, var(--cs-color-neutral-on-normal));
      background-color: var(--cs-color-fill-normal, var(--cs-color-neutral-fill-normal));
      border-color: var(--cs-color-border-normal, var(--cs-color-neutral-border-normal));
    }
    @media (hover: hover) {
      .button:not(.disabled):not(.loading):hover {
        color: var(--cs-color-on-normal, var(--cs-color-neutral-on-normal));
        background-color: color-mix(
          in oklab,
          var(--cs-color-fill-normal, var(--cs-color-neutral-fill-normal)),
          var(--cs-color-mix-hover)
        );
      }
    }
    .button:not(.disabled):not(.loading):active {
      color: var(--cs-color-on-normal, var(--cs-color-neutral-on-normal));
      background-color: color-mix(
        in oklab,
        var(--cs-color-fill-normal, var(--cs-color-neutral-fill-normal)),
        var(--cs-color-mix-active)
      );
    }
  }

  :host([appearance='accent']) {
    /* Indentation overrides for grouping */
    margin-inline-start: var(--_button-horizontal-indent);
    margin-block-start: var(--_button-vertical-indent);

    .button {
      color: var(--cs-color-on-loud, var(--cs-color-neutral-on-loud));
      background-color: var(--cs-color-fill-loud, var(--cs-color-neutral-fill-loud));
      border-color: transparent;
    }
    @media (hover: hover) {
      .button:not(.disabled):not(.loading):hover {
        background-color: color-mix(
          in oklab,
          var(--cs-color-fill-loud, var(--cs-color-neutral-fill-loud)),
          var(--cs-color-mix-hover)
        );
      }
    }
    .button:not(.disabled):not(.loading):active {
      background-color: color-mix(
        in oklab,
        var(--cs-color-fill-loud, var(--cs-color-neutral-fill-loud)),
        var(--cs-color-mix-active)
      );
    }
  }

  /* Focus states */
  .button:focus {
    outline: none;
  }

  .button:focus-visible {
    outline: var(--cs-focus-ring);
    outline-offset: var(--cs-focus-ring-offset);
  }

  /* Disabled state */
  :host([disabled]) {
    opacity: 0.5;
    cursor: not-allowed;

    /* When disabled, prevent mouse events from bubbling up from children */
    .button {
      pointer-events: none;
    }
  }

  /* Keep it last so Safari doesn't stop parsing this block */
  .button::-moz-focus-inner {
    border: 0;
  }

  /* Icon buttons */
  .button.is-icon-button {
    outline-offset: 2px;
    width: var(--cs-form-control-height);
    aspect-ratio: 1;
  }

  /* Icon buttons with a caret need to grow to fit both the icon and the caret */
  .button.is-icon-button.caret {
    width: auto;
    aspect-ratio: auto;
    min-width: var(--cs-form-control-height);
  }

  /* Pill modifier */
  :host([pill]) .button {
    border-start-start-radius: var(--_button-start-start-radius, var(--cs-border-radius-pill));
    border-start-end-radius: var(--_button-start-end-radius, var(--cs-border-radius-pill));
    border-end-start-radius: var(--_button-end-start-radius, var(--cs-border-radius-pill));
    border-end-end-radius: var(--_button-end-end-radius, var(--cs-border-radius-pill));
  }

  /*
   * Label
   */

  .start,
  .end {
    flex: 0 0 auto;
    display: flex;
    align-items: center;
    pointer-events: none;
  }

  .label {
    display: inline-block;
  }

  .is-icon-button .label {
    display: flex;
    justify-content: center;
  }

  .label::slotted(cs-icon) {
    align-self: center;
  }

  /*
   * Caret modifier
   */

  cs-icon[part='caret'] {
    display: flex;
    align-self: center;
    align-items: center;

    &::part(svg) {
      width: 0.875em;
      height: 0.875em;
    }

    .button:has(&) .end {
      display: none;
    }
  }

  /*
   * Loading modifier
   */

  .loading {
    position: relative;
    cursor: wait;

    .start,
    .label,
    .end,
    .caret {
      visibility: hidden;
    }

    cs-spinner {
      --indicator-color: currentColor;
      --track-color: color-mix(in oklab, currentColor, transparent 90%);

      position: absolute;
      font-size: 1em;
      height: 1em;
      width: 1em;
      top: calc(50% - 0.5em);
      left: calc(50% - 0.5em);
    }
  }

  /*
   * Badges
   */

  .button ::slotted(cs-badge) {
    border-color: var(--cs-color-surface-default);
    position: absolute;
    inset-block-start: 0;
    inset-inline-end: 0;
    translate: 50% -50%;
    pointer-events: none;
  }

  :host(:dir(rtl)) ::slotted(cs-badge) {
    translate: -50% -50%;
  }

  /*
  * Button spacing
  */

  /* These are !important because a document-level universal margin reset would otherwise beat them:
     a slotted element belongs to the outer tree. See callout.styles.ts for the full note. */
  slot[name='start']::slotted(*) {
    margin-inline-end: 0.75em !important;
  }

  slot[name='end']::slotted(*),
  .button:not(.visually-hidden-label) [part='caret'] {
    margin-inline-start: 0.75em !important;
  }
`;

  // node_modules/lit-html/static.js
  var a3 = /* @__PURE__ */ Symbol.for("");
  var o9 = (t8) => {
    if (t8?.r === a3) return t8?._$litStatic$;
  };
  var i7 = (t8, ...r9) => ({ _$litStatic$: r9.reduce((r10, e12, a4) => r10 + ((t9) => {
    if (void 0 !== t9._$litStatic$) return t9._$litStatic$;
    throw Error(`Value passed to 'literal' function must be a 'literal' result: ${t9}. Use 'unsafeStatic' to pass non-literal values, but
            take care to ensure page security.`);
  })(e12) + t8[a4 + 1], t8[0]), r: a3 });
  var l4 = /* @__PURE__ */ new Map();
  var n6 = (t8) => (r9, ...e12) => {
    const a4 = e12.length;
    let s7, i10;
    const n9 = [], u4 = [];
    let c6, $3 = 0, f3 = false;
    for (; $3 < a4; ) {
      for (c6 = r9[$3]; $3 < a4 && void 0 !== (i10 = e12[$3], s7 = o9(i10)); ) c6 += s7 + r9[++$3], f3 = true;
      $3 !== a4 && u4.push(i10), n9.push(c6), $3++;
    }
    if ($3 === a4 && n9.push(r9[a4]), f3) {
      const t9 = n9.join("$$lit$$");
      void 0 === (r9 = l4.get(t9)) && (n9.raw = n9, l4.set(t9, r9 = n9)), e12 = u4;
    }
    return t8(r9, ...e12);
  };
  var u3 = n6(b2);
  var c4 = n6(w);
  var $2 = n6(T);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.X44IALPO.js
  var CsButton = class extends CornerstoneFormAssociatedElement {
    constructor() {
      super(...arguments);
      this.assumeInteractionOn = ["click"];
      this.localize = new LocalizeController2(this);
      this.invalid = false;
      this.isIconButton = false;
      this.title = "";
      this.variant = "neutral";
      this.appearance = "accent";
      this.size = "m";
      this.withCaret = false;
      this.disabled = false;
      this.loading = false;
      this.pill = false;
      this.type = "button";
    }
    static get validators() {
      return [...super.validators, MirrorValidator()];
    }
    constructLightDOMButton() {
      const button = document.createElement("button");
      for (const attribute of this.attributes) {
        if (attribute.name === "style") {
          continue;
        }
        button.setAttribute(attribute.name, attribute.value);
      }
      button.type = this.type;
      button.style.position = "absolute !important";
      button.style.width = "0 !important";
      button.style.height = "0 !important";
      button.style.clipPath = "inset(50%) !important";
      button.style.overflow = "hidden !important";
      button.style.whiteSpace = "nowrap !important";
      if (this.name) {
        button.name = this.name;
      }
      button.value = this.value || "";
      return button;
    }
    handleClick(event) {
      if (this.disabled || this.loading) {
        event.preventDefault();
        event.stopImmediatePropagation();
        return;
      }
      if (this.type !== "submit" && this.type !== "reset") {
        return;
      }
      const form = this.getForm();
      if (!form) {
        return;
      }
      const lightDOMButton = this.constructLightDOMButton();
      this.parentElement?.append(lightDOMButton);
      lightDOMButton.click();
      lightDOMButton.remove();
    }
    handleInvalid() {
      this.dispatchEvent(new CsInvalidEvent());
    }
    handleLabelSlotChange() {
      const nodes = this.labelSlot.assignedNodes({ flatten: true });
      let hasIconLabel = false;
      let hasIcon = false;
      let hasText = false;
      let hasOtherElements = false;
      [...nodes].forEach((node) => {
        if (node.nodeType === Node.ELEMENT_NODE) {
          const element = node;
          if (element.localName === "cs-icon") {
            hasIcon = true;
            if (!hasIconLabel) {
              const iconLabel = element.label;
              hasIconLabel = typeof iconLabel === "string" && iconLabel.length > 0;
            }
          } else {
            hasOtherElements = true;
          }
        } else if (node.nodeType === Node.TEXT_NODE) {
          const text = node.textContent?.trim() || "";
          if (text.length > 0) {
            hasText = true;
          }
        }
      });
      this.isIconButton = hasIcon && !hasText && !hasOtherElements;
      this.customStates.set("icon-button", this.isIconButton);
      if (this.isIconButton && !hasIconLabel) {
        console.warn(
          'Icon buttons must have a label for screen readers. Add <cs-icon label="..."> to remove this warning.',
          this
        );
      }
    }
    isButton() {
      return this.href ? false : true;
    }
    isLink() {
      return this.href ? true : false;
    }
    handleDisabledChange() {
      this.customStates.set("disabled", this.disabled);
      this.updateValidity();
    }
    handleHrefChange() {
      this.customStates.set("link", this.isLink());
    }
    handleLoadingChange() {
      this.customStates.set("loading", this.loading);
    }
    setValue(..._args) {
    }
    /** Simulates a click on the button. */
    click() {
      this.button.click();
    }
    /** Sets focus on the button. */
    focus(options2) {
      this.button.focus(options2);
    }
    /** Removes focus from the button. */
    blur() {
      this.button.blur();
    }
    render() {
      const isLink = this.isLink();
      const tag2 = isLink ? i7`a` : i7`button`;
      return u3`
      <${tag2}
        part="button"
        class=${e7({
        button: true,
        caret: this.withCaret,
        disabled: this.disabled,
        loading: this.loading,
        rtl: this.localize.dir() === "rtl",
        "is-icon-button": this.isIconButton
      })}
        ?disabled=${o8(isLink ? void 0 : this.disabled)}
        type=${o8(isLink ? void 0 : this.type)}
        title=${this.title}
        name=${o8(isLink ? void 0 : this.name)}
        value=${o8(isLink ? void 0 : this.value)}
        href=${o8(isLink ? this.href : void 0)}
        target=${o8(isLink ? this.target : void 0)}
        download=${o8(isLink ? this.download : void 0)}
        rel=${o8(isLink && this.rel ? this.rel : void 0)}
        role=${o8(isLink ? void 0 : "button")}
        aria-disabled=${o8(isLink && this.disabled ? "true" : void 0)}
        tabindex=${this.disabled ? "-1" : "0"}
        @invalid=${this.isButton() ? this.handleInvalid : null}
        @click=${this.handleClick}
      >
        <slot name="start" part="start" class="start"></slot>
        <slot part="label" class="label" @slotchange=${this.handleLabelSlotChange}></slot>
        <slot name="end" part="end" class="end"></slot>
        ${this.withCaret ? u3` <cs-icon part="caret" class="caret" library="system" name="keyboard_arrow_down"></cs-icon> ` : ""}
        ${this.loading ? u3`<cs-spinner part="spinner"></cs-spinner>` : ""}
      </${tag2}>
    `;
    }
  };
  CsButton.shadowRootOptions = { ...CornerstoneFormAssociatedElement.shadowRootOptions, delegatesFocus: true };
  CsButton.css = [button_styles_default, variants_styles_default, size_styles_default];
  __decorateClass([
    e5(".button")
  ], CsButton.prototype, "button", 2);
  __decorateClass([
    e5("slot:not([name])")
  ], CsButton.prototype, "labelSlot", 2);
  __decorateClass([
    r5()
  ], CsButton.prototype, "invalid", 2);
  __decorateClass([
    r5()
  ], CsButton.prototype, "isIconButton", 2);
  __decorateClass([
    n4()
  ], CsButton.prototype, "title", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsButton.prototype, "variant", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsButton.prototype, "appearance", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsButton.prototype, "size", 2);
  __decorateClass([
    n4({ attribute: "with-caret", type: Boolean, reflect: true })
  ], CsButton.prototype, "withCaret", 2);
  __decorateClass([
    n4({ type: Boolean })
  ], CsButton.prototype, "disabled", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsButton.prototype, "loading", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsButton.prototype, "pill", 2);
  __decorateClass([
    n4()
  ], CsButton.prototype, "type", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsButton.prototype, "name", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsButton.prototype, "value", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsButton.prototype, "href", 2);
  __decorateClass([
    n4()
  ], CsButton.prototype, "target", 2);
  __decorateClass([
    n4()
  ], CsButton.prototype, "rel", 2);
  __decorateClass([
    n4()
  ], CsButton.prototype, "download", 2);
  __decorateClass([
    n4({ attribute: "formaction" })
  ], CsButton.prototype, "formAction", 2);
  __decorateClass([
    n4({ attribute: "formenctype" })
  ], CsButton.prototype, "formEnctype", 2);
  __decorateClass([
    n4({ attribute: "formmethod" })
  ], CsButton.prototype, "formMethod", 2);
  __decorateClass([
    n4({ attribute: "formnovalidate", type: Boolean })
  ], CsButton.prototype, "formNoValidate", 2);
  __decorateClass([
    n4({ attribute: "formtarget" })
  ], CsButton.prototype, "formTarget", 2);
  __decorateClass([
    watch("disabled", { waitUntilFirstUpdate: true })
  ], CsButton.prototype, "handleDisabledChange", 1);
  __decorateClass([
    watch("href")
  ], CsButton.prototype, "handleHrefChange", 1);
  __decorateClass([
    watch("loading", { waitUntilFirstUpdate: true })
  ], CsButton.prototype, "handleLoadingChange", 1);
  CsButton = __decorateClass([
    customElement("cs-button")
  ], CsButton);
  CsButton.disableWarning?.("change-in-update");

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.W4L7GVHR.js
  var spinner_styles_default = i`
  :host {
    --track-width: 2px;
    --track-color: var(--cs-color-neutral-fill-normal);
    --indicator-color: var(--cs-color-brand-fill-loud);
    --speed: 2s;
    --size: 1em;

    /*
      Resizing a spinner element using anything but font-size will break the animation because the animation uses em
      units. Therefore, if a spinner is used in a flex container without \`flex: none\` applied, the spinner can
      grow/shrink and break the animation. The use of \`flex: none\` on the host element prevents this by always having
      the spinner sized according to its actual dimensions.
    */
    flex: none;
    display: inline-flex;
    width: var(--size);
    height: var(--size);
  }

  svg {
    width: 100%;
    height: 100%;
    aspect-ratio: 1;
    animation: spin var(--speed) linear infinite;
  }

  .track,
  .indicator {
    --radius: calc(var(--size) / 2 - var(--track-width) / 2);
    --circumference: calc(var(--radius) * 2 * 3.141592654);

    cx: calc(var(--size) / 2);
    cy: calc(var(--size) / 2);
    r: var(--radius);
    fill: none;
    stroke-width: var(--track-width);
  }

  .track {
    stroke: var(--track-color);
  }

  .indicator {
    stroke: var(--indicator-color);
    stroke-linecap: round;
    stroke-dasharray: calc(0.597 * var(--circumference)), calc(0.796 * var(--circumference));
    stroke-dashoffset: calc(-0.04 * var(--circumference));
    animation: dash 1.5s ease-in-out infinite;
  }

  @keyframes spin {
    0% {
      transform: rotate(0deg);
    }
    100% {
      transform: rotate(360deg);
    }
  }

  @keyframes dash {
    0% {
      stroke-dasharray: calc(0.008 * var(--circumference)), calc(1.194 * var(--circumference));
      stroke-dashoffset: 0;
    }
    50% {
      stroke-dasharray: calc(0.716 * var(--circumference)), calc(1.194 * var(--circumference));
      stroke-dashoffset: calc(-0.278 * var(--circumference));
    }
    100% {
      stroke-dasharray: calc(0.716 * var(--circumference)), calc(1.194 * var(--circumference));
      stroke-dashoffset: calc(-0.987 * var(--circumference));
    }
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.GF7JC3ON.js
  var CsSpinner = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.localize = new LocalizeController2(this);
    }
    render() {
      return b2`
      <svg
        part="spinner"
        role="progressbar"
        aria-label=${this.localize.term("loading")}
        fill="none"
        xmlns="http://www.w3.org/2000/svg"
      >
        <circle class="track" />
        <circle class="indicator" />
      </svg>
    `;
    }
  };
  CsSpinner.css = spinner_styles_default;
  CsSpinner = __decorateClass([
    customElement("cs-spinner")
  ], CsSpinner);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.METBWRR6.js
  var button_group_styles_default = i`
  :host {
    display: inline-flex;
  }

  .button-group {
    display: flex;
    position: relative;
    isolation: isolate;
    flex-wrap: var(--_button-group-flex-wrap, wrap);

    @media (hover: hover) {
      > :hover,
      &::slotted(:hover) {
        z-index: 1;
      }
    }

    /* Focus and checked are always on top */
    > :focus,
    &::slotted(:focus),
    > [aria-checked='true'],
    &::slotted([aria-checked='true']),
    > [checked],
    &::slotted([checked]) {
      z-index: 2 !important;
    }

    :host([orientation='horizontal']) & {
      flex-direction: row;
    }

    :host([orientation='vertical']) & {
      flex-direction: column;
    }
  }

  /* Set custom properties to be inherited by slotted buttons */
  :host([orientation='horizontal']) {
    --_button-horizontal-indent: var(--cs-form-control-border-width);
    --_button-horizontal-indent-outlined: calc(var(--cs-form-control-border-width) * -1);

    ::slotted(:first-child) {
      --_button-horizontal-indent: 0;
      --_button-horizontal-indent-outlined: 0;
    }
  }

  :host([orientation='vertical']) {
    --_button-vertical-indent: var(--cs-form-control-border-width);
    --_button-vertical-indent-outlined: calc(var(--cs-form-control-border-width) * -1);

    ::slotted(:first-child) {
      --_button-vertical-indent: 0;
      --_button-vertical-indent-outlined: 0;
    }
  }

  /* All buttons that are not in front or at the end get their border radius removed */
  ::slotted(:not(:first-child):not(:last-child)) {
    --_button-start-start-radius: 0;
    --_button-start-end-radius: 0;
    --_button-end-start-radius: 0;
    --_button-end-end-radius: 0;
  }

  /* Remove leading and trailing buttons border radius individually */
  :host([orientation='horizontal']) {
    ::slotted(:first-child:not(:last-child)) {
      --_button-start-end-radius: 0;
      --_button-end-end-radius: 0;
    }

    ::slotted(:last-child:not(:first-child)) {
      --_button-start-start-radius: 0;
      --_button-end-start-radius: 0;
    }
  }

  :host([orientation='vertical']) {
    ::slotted(:first-child:not(:last-child)) {
      --_button-end-start-radius: 0;
      --_button-end-end-radius: 0;
    }

    ::slotted(:last-child:not(:first-child)) {
      --_button-start-start-radius: 0;
      --_button-start-end-radius: 0;
    }
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.5WGXQAN7.js
  var CsButtonGroup = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.disableRole = false;
      this.hasOutlined = false;
      this.label = "";
      this.orientation = "horizontal";
    }
    updated(changedProperties) {
      super.updated(changedProperties);
      if (changedProperties.has("orientation")) {
        this.setAttribute("aria-orientation", this.orientation);
      }
    }
    handleFocus(event) {
      const button = findButton(event.target);
      button?.classList.add("button-focus");
    }
    handleBlur(event) {
      const button = findButton(event.target);
      button?.classList.remove("button-focus");
    }
    handleMouseOver(event) {
      const button = findButton(event.target);
      button?.classList.add("button-hover");
    }
    handleMouseOut(event) {
      const button = findButton(event.target);
      button?.classList.remove("button-hover");
    }
    render() {
      return b2`
      <slot
        class="button-group"
        role="${this.disableRole ? "presentation" : "group"}"
        aria-label=${this.label}
        aria-orientation=${this.orientation}
        @focusout=${this.handleBlur}
        @focusin=${this.handleFocus}
        @mouseover=${this.handleMouseOver}
        @mouseout=${this.handleMouseOut}
      ></slot>
    `;
    }
  };
  CsButtonGroup.css = [button_group_styles_default];
  __decorateClass([
    e5("slot")
  ], CsButtonGroup.prototype, "defaultSlot", 2);
  __decorateClass([
    r5()
  ], CsButtonGroup.prototype, "disableRole", 2);
  __decorateClass([
    r5()
  ], CsButtonGroup.prototype, "hasOutlined", 2);
  __decorateClass([
    n4()
  ], CsButtonGroup.prototype, "label", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsButtonGroup.prototype, "orientation", 2);
  CsButtonGroup = __decorateClass([
    customElement("cs-button-group")
  ], CsButtonGroup);
  function findButton(el) {
    const selector = "cs-button, cs-radio-button";
    return el.closest(selector) ?? el.querySelector(selector);
  }

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.WOD7WYR2.js
  var callout_styles_default = i`
  :host {
    display: flex;
    position: relative;
    align-items: stretch;
    border-radius: var(--cs-panel-border-radius);
    background-color: var(--cs-color-fill-quiet, var(--cs-color-brand-fill-quiet));
    border-color: var(--cs-color-border-quiet, var(--cs-color-brand-border-quiet));
    border-style: var(--cs-panel-border-style);
    border-width: var(--cs-panel-border-width);
    color: var(--cs-color-text-normal);
    padding: 1em;
  }

  /* Appearance modifiers */
  :host([appearance~='plain']) {
    background-color: transparent;
    border-color: transparent;
  }

  :host([appearance~='outlined']) {
    background-color: transparent;
    border-color: var(--cs-color-border-loud, var(--cs-color-brand-border-loud));
  }

  :host([appearance~='filled']) {
    background-color: var(--cs-color-fill-quiet, var(--cs-color-brand-fill-quiet));
    border-color: transparent;
  }

  :host([appearance~='filled-outlined']) {
    border-color: var(--cs-color-border-quiet, var(--cs-color-brand-border-quiet));
  }

  :host([appearance~='accent']) {
    color: var(--cs-color-on-loud, var(--cs-color-brand-on-loud));
    background-color: var(--cs-color-fill-loud, var(--cs-color-brand-fill-loud));
    border-color: transparent;

    [part~='icon'] {
      color: currentColor;
    }
  }

  [part~='icon'] {
    flex: 0 0 auto;
    display: flex;
    align-items: center;
    color: var(--cs-color-on-quiet);
    font-size: 1.25em;
  }

  /*
   * The gap between the icon and the message, and why it is !important.
   *
   * A slotted element belongs to the outer tree, so per the shadow cascade a *normal* declaration from the
   * outer tree beats one from the inner tree whatever the specificity: any document-level universal margin
   * reset -- Starlight's, Tailwind's Preflight, any of the classic ones -- silently collapsed this gap to zero.
   * The same cascade rule inverts for important declarations, where the inner tree wins, which is the mechanism
   * a component has to defend its own internals against a consumer's reset.
   *
   * Two alternatives were tried and do not work. Moving the margin to the icon container puts it out of reach
   * of outer rules, but the container renders whether or not anything is slotted, so an icon-less callout gains
   * the gap as dead space -- reacting to what is actually slotted is the thing ::slotted gets for free. Gating
   * that with :host(:has(> [slot='icon'])) does not match at all: :host() takes a compound selector, and :has()
   * with a relative selector inside it is not matched.
   */
  ::slotted([slot='icon']) {
    margin-inline-end: var(--cs-form-control-padding-inline) !important;
  }

  [part~='message'] {
    flex: 1 1 auto;
    display: block;
    overflow: hidden;
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.NJ2ZFVIU.js
  var CsCallout = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.variant = "brand";
      this.size = "m";
    }
    render() {
      return b2`
      <div part="icon">
        <slot name="icon"></slot>
      </div>

      <div part="message">
        <slot></slot>
      </div>
    `;
    }
  };
  CsCallout.css = [callout_styles_default, variants_styles_default, size_styles_default];
  __decorateClass([
    n4({ reflect: true })
  ], CsCallout.prototype, "variant", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsCallout.prototype, "appearance", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsCallout.prototype, "size", 2);
  CsCallout = __decorateClass([
    customElement("cs-callout")
  ], CsCallout);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.25L55TBI.js
  var HasSlotController = class {
    constructor(host, ...slotNames) {
      this.slotNames = [];
      this.handleSlotChange = (event) => {
        const slot = event.target;
        if (this.slotNames.includes("[default]") && !slot.name || slot.name && this.slotNames.includes(slot.name)) {
          this.host.requestUpdate();
        }
      };
      (this.host = host).addController(this);
      this.slotNames = slotNames;
    }
    hasDefaultSlot() {
      if (!this.host.childNodes) {
        return false;
      }
      return [...this.host.childNodes].some((node) => {
        if (node.nodeType === Node.TEXT_NODE && node.textContent.trim() !== "") {
          return true;
        }
        if (node.nodeType === Node.ELEMENT_NODE) {
          const el = node;
          const tagName = el.tagName.toLowerCase();
          if (tagName === "cs-visually-hidden") {
            return false;
          }
          if (!el.hasAttribute("slot")) {
            return true;
          }
        }
        return false;
      });
    }
    hasNamedSlot(name) {
      return Boolean(this.host.querySelector?.(`:scope > [slot="${name}"]`));
    }
    /**
     * @param slotName     - Name of the slot to look for
     * @param propertyName - Generally we infer via the matching `ssr*` property on the host, but in cases where its different, you can specify a manual property name.
     */
    test(slotName, propertyName) {
      if (propertyName && this.host.didSSR && !this.host.hasUpdated) {
        return Boolean(this.host[propertyName]);
      }
      return slotName === "[default]" ? this.hasDefaultSlot() : this.hasNamedSlot(slotName);
    }
    hostConnected() {
      const shadowRoot = this.host.shadowRoot;
      if (shadowRoot && "addEventListener" in shadowRoot) {
        shadowRoot.addEventListener("slotchange", this.handleSlotChange);
      }
    }
    hostDisconnected() {
      const shadowRoot = this.host.shadowRoot;
      if (shadowRoot && "removeEventListener" in shadowRoot) {
        shadowRoot.removeEventListener("slotchange", this.handleSlotChange);
      }
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.AESWDHHO.js
  var card_styles_default = i`
  :host {
    --spacing: var(--cs-space-l);

    /* Internal calculated properties */
    --inner-border-radius: calc(var(--cs-panel-border-radius) - var(--cs-panel-border-width));

    display: flex;
    flex-direction: column;
    background-color: var(--cs-color-surface-default);
    border-color: var(--cs-color-surface-border);
    border-radius: var(--cs-panel-border-radius);
    border-style: var(--cs-panel-border-style);
    box-shadow: var(--cs-shadow-s);
    border-width: var(--cs-panel-border-width);
    color: var(--cs-color-text-normal);
  }

  /* Appearance modifiers */
  :host([appearance='plain']) {
    background-color: transparent;
    border-color: transparent;
    box-shadow: none;
  }

  :host([appearance='outlined']) {
    background-color: var(--cs-color-surface-default);
    border-color: var(--cs-color-surface-border);
  }

  :host([appearance='filled']) {
    background-color: var(--cs-color-neutral-fill-quiet);
    border-color: transparent;
  }

  :host([appearance='filled-outlined']) {
    background-color: var(--cs-color-neutral-fill-quiet);
    border-color: var(--cs-color-surface-border);
  }

  :host([appearance='accent']) {
    color: var(--cs-color-neutral-on-loud);
    background-color: var(--cs-color-neutral-fill-loud);
    border-color: transparent;
  }

  /* Take care of top and bottom radii */
  .media,
  :host(:not([has-media])) .header,
  :host(:not([has-media], [has-header])) .body {
    border-start-start-radius: var(--inner-border-radius);
    border-start-end-radius: var(--inner-border-radius);
  }

  :host(:not([has-footer])) .body,
  .footer {
    border-end-start-radius: var(--inner-border-radius);
    border-end-end-radius: var(--inner-border-radius);
  }

  .media {
    display: flex;
    overflow: hidden;

    &::slotted(*) {
      display: block;
      width: 100%;
      border-radius: 0 !important;
    }
  }

  /* Round all corners for plain appearance */
  :host([appearance='plain']) .media {
    border-radius: var(--inner-border-radius);

    &::slotted(*) {
      border-radius: inherit !important;
    }
  }

  .header {
    display: block;
    border-block-end-style: inherit;
    border-block-end-color: var(--cs-color-surface-border);
    border-block-end-width: var(--cs-panel-border-width);
    padding: calc(var(--spacing) / 2) var(--spacing);
  }

  .body {
    display: block;
    padding: var(--spacing);
  }

  .footer {
    display: block;
    border-block-start-style: inherit;
    border-block-start-color: var(--cs-color-surface-border);
    border-block-start-width: var(--cs-panel-border-width);
    padding: var(--spacing);
  }

  /* Push slots to sides when the action slots renders */
  .has-actions {
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  :host(:not([has-header])) .header,
  :host(:not([has-footer])) .footer,
  :host(:not([has-media])) .media {
    display: none;
  }

  /* Orientation Styles */
  :host([orientation='horizontal']) {
    flex-direction: row;

    .media {
      border-start-start-radius: var(--inner-border-radius);
      border-end-start-radius: var(--inner-border-radius);
      border-start-end-radius: 0;

      &::slotted(*) {
        block-size: 100%;
        inline-size: 100%;
        object-fit: cover;
      }
    }
  }

  :host([orientation='horizontal']) .body slot::slotted(*) {
    display: block;
    height: 100%;
    margin: 0;
  }

  :host([orientation='horizontal']) slot[name='actions']::slotted(*) {
    display: flex;
    align-items: center;
    padding: var(--spacing);
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.N5LLQKQL.js
  var CsCard = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.hasSlotController = new HasSlotController(
        this,
        "footer",
        "header",
        "media",
        "header-actions",
        "footer-actions"
      );
      this.appearance = "outlined";
      this.hasHeader = false;
      this.hasMedia = false;
      this.hasFooter = false;
      this.ssrHeaderActions = false;
      this.ssrFooterActions = false;
      this.orientation = "vertical";
    }
    willUpdate(changedProperties) {
      this.hasHeader = this.hasSlotController.test("header", "hasHeader");
      this.hasMedia = this.hasSlotController.test("media", "hasMedia");
      this.hasFooter = this.hasSlotController.test("footer", "hasFooter");
      super.willUpdate(changedProperties);
    }
    render() {
      if (this.orientation === "horizontal") {
        return b2`
        <slot name="media" part="media" class="media"></slot>
        <div part="body" class="body"><slot></slot></div>
        <slot name="actions" part="actions" class="actions"></slot>
      `;
      }
      const hasHeaderActions = this.hasSlotController.test("header-actions", "ssrHeaderActions");
      const hasFooterActions = this.hasSlotController.test("footer-actions", "ssrFooterActions");
      return b2`
      <slot name="media" part="media" class="media"></slot>

      <header
        part="header"
        class=${e7({
        header: true,
        "has-actions": hasHeaderActions
      })}
      >
        <slot name="header"></slot>
        <slot name="header-actions"></slot>
      </header>

      <div part="body" class="body"><slot></slot></div>

      <footer
        part="footer"
        class=${e7({
        footer: true,
        "has-actions": hasFooterActions
      })}
      >
        <slot name="footer"></slot>
        <slot name="footer-actions"></slot>
      </footer>
    `;
    }
  };
  CsCard.css = [size_styles_default, card_styles_default];
  __decorateClass([
    n4({ reflect: true })
  ], CsCard.prototype, "appearance", 2);
  __decorateClass([
    n4({ attribute: "has-header", type: Boolean, reflect: true })
  ], CsCard.prototype, "hasHeader", 2);
  __decorateClass([
    n4({ attribute: "has-media", type: Boolean, reflect: true })
  ], CsCard.prototype, "hasMedia", 2);
  __decorateClass([
    n4({ attribute: "has-footer", type: Boolean, reflect: true })
  ], CsCard.prototype, "hasFooter", 2);
  __decorateClass([
    n4({ attribute: "ssr-header-actions", type: Boolean, reflect: true })
  ], CsCard.prototype, "ssrHeaderActions", 2);
  __decorateClass([
    n4({ attribute: "ssr-footer-actions", type: Boolean, reflect: true })
  ], CsCard.prototype, "ssrFooterActions", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsCard.prototype, "orientation", 2);
  CsCard = __decorateClass([
    customElement("cs-card")
  ], CsCard);
  CsCard.disableWarning?.("change-in-update");

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.XLSW3DZZ.js
  var CsSlideChangeEvent = class extends Event {
    constructor(detail) {
      super("cs-slide-change", { bubbles: true, cancelable: false, composed: true });
      this.detail = detail;
    }
  };

  // node_modules/nanoid/url-alphabet/index.js
  var urlAlphabet = "useandom-26T198340PX75pxJACKVERYMINDBUSHWOLF_GQZbfghjklqvwyzrict";

  // node_modules/nanoid/index.browser.js
  var nanoid = (size3 = 21) => {
    let id3 = "";
    let bytes = crypto.getRandomValues(new Uint8Array(size3 |= 0));
    while (size3--) {
      id3 += urlAlphabet[bytes[size3] & 63];
    }
    return id3;
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.7BPV545Z.js
  function clamp(value, min2, max2) {
    const noNegativeZero = (n9) => Object.is(n9, -0) ? 0 : n9;
    if (value < min2) {
      return noNegativeZero(min2);
    }
    if (value > max2) {
      return noNegativeZero(max2);
    }
    return noNegativeZero(value);
  }
  function uniqueId(prefix = "") {
    return `${prefix}${nanoid()}`;
  }

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.ZHHK42LK.js
  var AutoplayController = class {
    constructor(host, tickCallback) {
      this.timerId = 0;
      this.activeInteractions = 0;
      this.paused = false;
      this.stopped = true;
      this.pause = () => {
        if (!this.activeInteractions++) {
          this.paused = true;
          this.host.requestUpdate();
        }
      };
      this.resume = () => {
        if (!--this.activeInteractions) {
          this.paused = false;
          this.host.requestUpdate();
        }
      };
      host.addController(this);
      this.host = host;
      this.tickCallback = tickCallback;
    }
    hostConnected() {
      this.host.addEventListener("mouseenter", this.pause);
      this.host.addEventListener("mouseleave", this.resume);
      this.host.addEventListener("focusin", this.pause);
      this.host.addEventListener("focusout", this.resume);
      this.host.addEventListener("touchstart", this.pause, { passive: true });
      this.host.addEventListener("touchend", this.resume);
    }
    hostDisconnected() {
      this.stop();
      this.host.removeEventListener("mouseenter", this.pause);
      this.host.removeEventListener("mouseleave", this.resume);
      this.host.removeEventListener("focusin", this.pause);
      this.host.removeEventListener("focusout", this.resume);
      this.host.removeEventListener("touchstart", this.pause);
      this.host.removeEventListener("touchend", this.resume);
    }
    start(interval) {
      this.stop();
      this.stopped = false;
      this.timerId = window.setInterval(() => {
        if (!this.paused) {
          this.tickCallback();
        }
      }, interval);
    }
    stop() {
      clearInterval(this.timerId);
      this.stopped = true;
      this.host.requestUpdate();
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.4LWSQYB5.js
  var carousel_styles_default = i`
  :host {
    --aspect-ratio: 16 / 9;
    --scroll-hint: 0px;
    --slide-gap: var(--cs-space-m, 1rem); /* fallback value is necessary */

    display: flex;
  }

  .carousel {
    display: grid;
    grid-template-columns: min-content 1fr min-content;
    grid-template-rows: 1fr min-content;
    grid-template-areas:
      '. slides .'
      '. pagination .';
    gap: var(--cs-space-m);
    align-items: center;
    min-height: 100%;
    min-width: 100%;
    position: relative;
  }

  .pagination {
    grid-area: pagination;
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: var(--cs-space-s);
    padding-block: var(--cs-space-3xs);
  }

  .slides {
    grid-area: slides;

    display: grid;
    height: 100%;
    width: 100%;
    align-items: center;
    justify-items: center;
    overflow: auto;
    overscroll-behavior-x: contain;
    scrollbar-width: none;
    aspect-ratio: calc(var(--aspect-ratio) * var(--slides-per-page));
    border-radius: var(--cs-border-radius-m);

    --slide-size: calc((100% - (var(--slides-per-page) - 1) * var(--slide-gap)) / var(--slides-per-page));
  }

  /*
   * While a looping carousel that initialized inside a hidden container waits to scroll past its leading clones, hide
   * the slides and pagination to avoid flashing the wrong slide and active dot, then fade them in once the carousel has
   * positioned itself.
   */
  .slides,
  .pagination {
    transition: opacity var(--cs-transition-fast) ease;
  }

  .slides-awaiting-position,
  .pagination-awaiting-position {
    opacity: 0;
    transition: none;
  }

  @media (prefers-reduced-motion) {
    :where(.slides) {
      scroll-behavior: auto;
    }
  }

  .slides-horizontal {
    grid-auto-flow: column;
    grid-auto-columns: var(--slide-size);
    grid-auto-rows: 100%;
    column-gap: var(--slide-gap);
    scroll-snap-type: x mandatory;
    scroll-padding-inline: var(--scroll-hint);
    padding-inline: var(--scroll-hint);
    overflow-y: hidden;
  }

  .slides-vertical {
    grid-auto-flow: row;
    grid-auto-columns: 100%;
    grid-auto-rows: var(--slide-size);
    row-gap: var(--slide-gap);
    scroll-snap-type: y mandatory;
    scroll-padding-block: var(--scroll-hint);
    padding-block: var(--scroll-hint);
    overflow-x: hidden;
  }

  :host([vertical]) ::slotted(cs-carousel-item) {
    height: 100%;
  }

  .slides::-webkit-scrollbar {
    display: none;
  }

  .navigation {
    grid-area: navigation;
    display: contents;
    font-size: var(--cs-font-size-l);
  }

  .navigation-button {
    flex: 0 0 auto;
    display: flex;
    align-items: center;
    background: none;
    border: none;
    border-radius: var(--cs-border-radius-m);
    font-size: inherit;
    color: var(--cs-color-text-quiet);
    padding: var(--cs-space-xs);
    cursor: pointer;
    transition: var(--cs-transition-normal) color;
    appearance: none;
  }

  .navigation-button-disabled {
    opacity: 0.5;
    cursor: not-allowed;
  }

  .navigation-button-previous {
    grid-column: 1;
    grid-row: 1;
  }

  .navigation-button-next {
    grid-column: 3;
    grid-row: 1;
  }

  .pagination-item {
    display: block;
    cursor: pointer;
    background: none;
    border: 0;
    border-radius: var(--cs-border-radius-circle);
    width: var(--cs-space-s);
    height: var(--cs-space-s);
    background-color: var(--cs-color-neutral-fill-normal);
    padding: 0;
    margin: 0;
    transition: transform var(--cs-transition-slow);
  }

  .pagination-item-active {
    background-color: var(--cs-form-control-activated-color);
    transform: scale(1.25);
  }

  /* Focus styles */
  .slides:focus-visible,
  .navigation-button:focus-visible,
  .pagination-item:focus-visible {
    outline: var(--cs-focus-ring);
    outline-offset: var(--cs-focus-ring-offset);
  }
`;

  // node_modules/lit-html/directives/map.js
  function* o10(o13, f3) {
    if (void 0 !== o13) {
      let i10 = 0;
      for (const t8 of o13) yield f3(t8, i10++);
    }
  }

  // node_modules/lit-html/directives/range.js
  function* o11(o13, t8, e12 = 1) {
    const i10 = void 0 === t8 ? 0 : o13;
    t8 ??= o13;
    for (let o14 = i10; e12 > 0 ? o14 < t8 : t8 < o14; o14 += e12) yield o14;
  }

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.QLABDH6E.js
  (() => {
    if (o5) {
      return;
    }
    const debounce = (fn, delay) => {
      let timerId = 0;
      return function(...args) {
        window.clearTimeout(timerId);
        timerId = window.setTimeout(() => {
          fn.call(this, ...args);
        }, delay);
      };
    };
    const decorate = (proto, method, decorateFn) => {
      const superFn = proto[method];
      proto[method] = function(...args) {
        superFn.call(this, ...args);
        decorateFn.call(this, superFn, ...args);
      };
    };
    const isSupported = "onscrollend" in window;
    if (!isSupported) {
      const pointers = /* @__PURE__ */ new Set();
      const scrollHandlers = /* @__PURE__ */ new WeakMap();
      const handlePointerDown = (event) => {
        pointers.add(event.pointerId);
      };
      const handlePointerUp = (event) => {
        pointers.delete(event.pointerId);
      };
      document.addEventListener("pointerdown", handlePointerDown);
      document.addEventListener("pointerup", handlePointerUp);
      decorate(EventTarget.prototype, "addEventListener", function(addEventListener, type) {
        if (type !== "scroll") {
          return;
        }
        const handleScrollEnd = debounce(() => {
          if (!pointers.size) {
            this.dispatchEvent(new Event("scrollend"));
          } else {
            handleScrollEnd();
          }
        }, 100);
        addEventListener.call(this, "scroll", handleScrollEnd, { passive: true });
        scrollHandlers.set(this, handleScrollEnd);
      });
      decorate(EventTarget.prototype, "removeEventListener", function(removeEventListener, type) {
        if (type !== "scroll") {
          return;
        }
        const scrollHandler = scrollHandlers.get(this);
        if (scrollHandler) {
          removeEventListener.call(this, "scroll", scrollHandler, { passive: true });
        }
      });
    }
  })();
  var CsCarousel = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.loop = false;
      this.slides = 0;
      this.currentSlide = 0;
      this.navigation = false;
      this.pagination = false;
      this.autoplay = false;
      this.autoplayInterval = 3e3;
      this.slidesPerPage = 1;
      this.slidesPerMove = 1;
      this.orientation = "horizontal";
      this.mouseDragging = false;
      this.activeSlide = 0;
      this.scrolling = false;
      this.dragging = false;
      this.awaitingInitialPosition = false;
      this.autoplayController = new AutoplayController(this, () => this.next());
      this.dragStartPosition = [-1, -1];
      this.localize = new LocalizeController2(this);
      this.pendingSlideChange = false;
      this.handleMouseDrag = (event) => {
        if (!this.dragging) {
          this.scrollContainer.style.setProperty("scroll-snap-type", "none");
          this.dragging = true;
          this.dragStartPosition = [event.clientX, event.clientY];
        }
        this.scrollContainer.scrollBy({
          left: -event.movementX,
          top: -event.movementY,
          behavior: "instant"
        });
      };
      this.handleMouseDragEnd = () => {
        const scrollContainer = this.scrollContainer;
        document.removeEventListener("pointermove", this.handleMouseDrag, { capture: true });
        const startLeft = scrollContainer.scrollLeft;
        const startTop = scrollContainer.scrollTop;
        scrollContainer.style.removeProperty("scroll-snap-type");
        scrollContainer.style.setProperty("overflow", "hidden");
        const finalLeft = scrollContainer.scrollLeft;
        const finalTop = scrollContainer.scrollTop;
        scrollContainer.style.removeProperty("overflow");
        scrollContainer.style.setProperty("scroll-snap-type", "none");
        scrollContainer.scrollTo({ left: startLeft, top: startTop, behavior: "instant" });
        requestAnimationFrame(async () => {
          if (startLeft !== finalLeft || startTop !== finalTop) {
            scrollContainer.scrollTo({
              left: finalLeft,
              top: finalTop,
              behavior: prefersReducedMotion() ? "auto" : "smooth"
            });
            await waitForEvent(scrollContainer, "scrollend");
          }
          scrollContainer.style.removeProperty("scroll-snap-type");
          this.dragging = false;
          this.dragStartPosition = [-1, -1];
          this.handleScrollEnd();
        });
      };
      this.handleSlotChange = (mutations) => {
        const needsInitialization = mutations.some(
          (mutation) => [...mutation.addedNodes, ...mutation.removedNodes].some(
            (el) => this.isCarouselItem(el) && !el.hasAttribute("data-clone")
          )
        );
        if (needsInitialization) {
          this.initializeSlides();
        }
        this.requestUpdate();
      };
    }
    connectedCallback() {
      super.connectedCallback();
      if (!o5) {
        this.setAttribute("role", "region");
        this.setAttribute("aria-label", this.localize.term("carousel"));
      }
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this.mutationObserver?.disconnect();
      this.resizeObserver?.disconnect();
    }
    firstUpdated(changedProperties) {
      super.firstUpdated(changedProperties);
      this.initializeSlides();
      this.mutationObserver = new MutationObserver(this.handleSlotChange);
      this.mutationObserver.observe(this, {
        childList: true,
        subtree: true
      });
      const startedHidden = this.loop && !this.scrollContainer?.clientWidth && !this.scrollContainer?.clientHeight;
      if (startedHidden) {
        this.awaitingInitialPosition = true;
      }
      this.resizeObserver = new ResizeObserver(() => {
        if (this.scrollContainer?.clientWidth || this.scrollContainer?.clientHeight) {
          this.goToSlide(this.activeSlide, "auto");
          this.synchronizeSlides();
          this.resizeObserver?.disconnect();
          this.resizeObserver = void 0;
          if (this.awaitingInitialPosition) {
            requestAnimationFrame(() => {
              requestAnimationFrame(() => {
                this.awaitingInitialPosition = false;
              });
            });
          }
        }
      });
      this.resizeObserver.observe(this);
    }
    willUpdate(changedProperties) {
      if (changedProperties.has("slidesPerMove") || changedProperties.has("slidesPerPage")) {
        this.slidesPerMove = Math.min(this.slidesPerMove, this.slidesPerPage);
      }
    }
    getPageCount() {
      const slidesCount = this.getSlides().length;
      const { slidesPerPage, slidesPerMove, loop } = this;
      const pages = loop ? slidesCount / slidesPerMove : (slidesCount - slidesPerPage) / slidesPerMove + 1;
      return Math.ceil(pages);
    }
    getCurrentPage() {
      return Math.ceil(this.activeSlide / this.slidesPerMove);
    }
    canScrollNext() {
      return this.loop || this.getCurrentPage() < this.getPageCount() - 1;
    }
    canScrollPrev() {
      return this.loop || this.getCurrentPage() > 0;
    }
    /** @internal Gets all carousel items. */
    getSlides({ excludeClones = true } = {}) {
      return [...this.children].filter(
        (el) => this.isCarouselItem(el) && (!excludeClones || !el.hasAttribute("data-clone"))
      );
    }
    handleClick(event) {
      if (this.dragging && this.dragStartPosition[0] > 0 && this.dragStartPosition[1] > 0) {
        const deltaX = Math.abs(this.dragStartPosition[0] - event.clientX);
        const deltaY = Math.abs(this.dragStartPosition[1] - event.clientY);
        const delta = Math.sqrt(deltaX * deltaX + deltaY * deltaY);
        if (delta >= 10) {
          event.preventDefault();
        }
      }
    }
    handleKeyDown(event) {
      if (["ArrowLeft", "ArrowRight", "ArrowUp", "ArrowDown", "Home", "End"].includes(event.key)) {
        const target = event.target;
        const isRtl = this.localize.dir() === "rtl";
        const isFocusInPagination = target.closest('[part~="pagination-item"]') !== null;
        const isNext = event.key === "ArrowDown" || !isRtl && event.key === "ArrowRight" || isRtl && event.key === "ArrowLeft";
        const isPrevious = event.key === "ArrowUp" || !isRtl && event.key === "ArrowLeft" || isRtl && event.key === "ArrowRight";
        event.preventDefault();
        if (isPrevious) {
          this.previous();
        }
        if (isNext) {
          this.next();
        }
        if (event.key === "Home") {
          this.goToSlide(0);
        }
        if (event.key === "End") {
          this.goToSlide(this.getSlides().length - 1);
        }
        if (isFocusInPagination) {
          this.updateComplete.then(() => {
            const activePaginationItem = this.shadowRoot?.querySelector(
              '[part~="pagination-item-active"]'
            );
            if (activePaginationItem) {
              activePaginationItem.focus();
            }
          });
        }
      }
    }
    handleMouseDragStart(event) {
      const canDrag = this.mouseDragging && event.button === 0;
      if (canDrag) {
        event.preventDefault();
        document.addEventListener("pointermove", this.handleMouseDrag, { capture: true, passive: true });
        document.addEventListener("pointerup", this.handleMouseDragEnd, { capture: true, once: true });
      }
    }
    handleScroll() {
      this.scrolling = true;
      if (!this.pendingSlideChange) {
        this.synchronizeSlides();
      }
    }
    /** @internal Synchronizes the slides with the IntersectionObserver API. */
    synchronizeSlides() {
      const io = new IntersectionObserver(
        (entries) => {
          io.disconnect();
          for (const entry of entries) {
            const slide = entry.target;
            slide.toggleAttribute("inert", !entry.isIntersecting);
            slide.classList.toggle("--in-view", entry.isIntersecting);
            slide.setAttribute("aria-hidden", entry.isIntersecting ? "false" : "true");
          }
          const firstIntersecting = entries.find((entry) => entry.isIntersecting);
          if (!firstIntersecting) {
            return;
          }
          const slidesWithClones = this.getSlides({ excludeClones: false });
          const slidesCount = this.getSlides().length;
          const slideIndex = slidesWithClones.indexOf(firstIntersecting.target);
          const normalizedIndex = this.loop ? slideIndex - this.slidesPerPage : slideIndex;
          this.activeSlide = (Math.ceil(normalizedIndex / this.slidesPerMove) * this.slidesPerMove + slidesCount) % slidesCount;
          if (!this.scrolling && !this.pendingSlideChange) {
            if (this.loop && firstIntersecting.target.hasAttribute("data-clone")) {
              const clonePosition = Number(firstIntersecting.target.getAttribute("data-clone"));
              this.goToSlide(clonePosition, "instant");
            }
          }
        },
        {
          root: this.scrollContainer,
          threshold: 0.6
        }
      );
      this.getSlides({ excludeClones: false }).forEach((slide) => {
        io.observe(slide);
      });
    }
    handleScrollEnd() {
      if (!this.scrolling || this.dragging) {
        return;
      }
      this.synchronizeSlides();
      this.scrolling = false;
      this.pendingSlideChange = false;
      this.synchronizeSlides();
    }
    isCarouselItem(node) {
      return node instanceof Element && node.tagName.toLowerCase() === "cs-carousel-item";
    }
    initializeSlides() {
      this.getSlides({ excludeClones: false }).forEach((slide, index) => {
        slide.classList.remove("--in-view");
        slide.classList.remove("--is-active");
        slide.setAttribute("aria-label", this.localize.term("slideNum", index + 1));
        if (slide.hasAttribute("data-clone")) {
          slide.remove();
        }
      });
      this.updateSlidesSnap();
      if (this.loop) {
        this.createClones();
      }
      this.goToSlide(this.activeSlide, "auto");
      this.synchronizeSlides();
    }
    createClones() {
      const slides = this.getSlides();
      const slidesPerPage = this.slidesPerPage;
      const lastSlides = slides.slice(-slidesPerPage);
      const firstSlides = slides.slice(0, slidesPerPage);
      lastSlides.reverse().forEach((slide, i10) => {
        const clone = slide.cloneNode(true);
        clone.setAttribute("data-clone", String(slides.length - i10 - 1));
        this.prepend(clone);
      });
      firstSlides.forEach((slide, i10) => {
        const clone = slide.cloneNode(true);
        clone.setAttribute("data-clone", String(i10));
        this.append(clone);
      });
    }
    handleSlideChange() {
      const slides = this.getSlides();
      slides.forEach((slide, i10) => {
        slide.classList.toggle("--is-active", i10 === this.activeSlide);
      });
      if (this.hasUpdated) {
        this.dispatchEvent(
          new CsSlideChangeEvent({
            index: this.activeSlide,
            slide: slides[this.activeSlide]
          })
        );
      }
    }
    updateSlidesSnap() {
      const slides = this.getSlides();
      const slidesPerMove = this.slidesPerMove;
      slides.forEach((slide, i10) => {
        const shouldSnap = (i10 + slidesPerMove) % slidesPerMove === 0;
        if (shouldSnap) {
          slide.style.removeProperty("scroll-snap-align");
        } else {
          slide.style.setProperty("scroll-snap-align", "none");
        }
      });
    }
    handleAutoplayChange() {
      this.autoplayController.stop();
      if (this.autoplay) {
        this.autoplayController.start(this.autoplayInterval);
      }
    }
    /**
     * Move the carousel backward by `slides-per-move` slides.
     *
     * @param behavior - The behavior used for scrolling.
     */
    previous(behavior = "smooth") {
      this.goToSlide(this.activeSlide - this.slidesPerMove, behavior);
    }
    /**
     * Move the carousel forward by `slides-per-move` slides.
     *
     * @param behavior - The behavior used for scrolling.
     */
    next(behavior = "smooth") {
      this.goToSlide(this.activeSlide + this.slidesPerMove, behavior);
    }
    /** Adds a carousel item as the last real slide. */
    addSlide(slide) {
      if (!this.isCarouselItem(slide)) {
        throw new TypeError("addSlide() expects a <cs-carousel-item>.");
      }
      if (slide.hasAttribute("data-clone")) {
        throw new TypeError("addSlide() cannot add a cloned carousel item.");
      }
      const slides = this.getSlides();
      const lastSlide = slides[slides.length - 1];
      this.insertBefore(slide, lastSlide?.nextElementSibling ?? null);
    }
    /** Removes the real slide at the specified index. */
    removeSlide(index) {
      if (!Number.isInteger(index)) {
        return;
      }
      const slides = this.getSlides();
      const slide = slides[index];
      if (!slide) {
        return;
      }
      const lastIndexAfterRemoval = Math.max(0, slides.length - 2);
      if (index < this.activeSlide) {
        this.activeSlide = Math.max(0, this.activeSlide - 1);
      } else if (index === this.activeSlide) {
        this.activeSlide = clamp(this.activeSlide, 0, lastIndexAfterRemoval);
      }
      slide.remove();
    }
    /**
     * Scrolls the carousel to the slide specified by `index`.
     *
     * @param index - The slide index.
     * @param behavior - The behavior used for scrolling.
     */
    goToSlide(index, behavior = "smooth") {
      const { slidesPerPage, loop } = this;
      const slides = this.getSlides();
      const slidesWithClones = this.getSlides({ excludeClones: false });
      if (!slides.length) {
        return;
      }
      const newActiveSlide = loop ? (index + slides.length) % slides.length : clamp(index, 0, slides.length - slidesPerPage);
      this.activeSlide = newActiveSlide;
      const isRtl = this.localize.dir() === "rtl";
      const nextSlideIndex = clamp(
        index + (loop ? slidesPerPage : 0) + (isRtl ? slidesPerPage - 1 : 0),
        0,
        slidesWithClones.length - 1
      );
      const nextSlide = slidesWithClones[nextSlideIndex];
      this.scrollToSlide(nextSlide, prefersReducedMotion() ? "auto" : behavior);
    }
    scrollToSlide(slide, behavior = "smooth") {
      this.pendingSlideChange = true;
      window.requestAnimationFrame(() => {
        if (!this.scrollContainer) {
          return;
        }
        const scrollContainer = this.scrollContainer;
        const scrollContainerRect = scrollContainer.getBoundingClientRect();
        const nextSlideRect = slide.getBoundingClientRect();
        const nextLeft = nextSlideRect.left - scrollContainerRect.left;
        const nextTop = nextSlideRect.top - scrollContainerRect.top;
        if (nextLeft || nextTop) {
          this.pendingSlideChange = true;
          scrollContainer.scrollTo({
            left: nextLeft + scrollContainer.scrollLeft,
            top: nextTop + scrollContainer.scrollTop,
            behavior
          });
        } else {
          this.pendingSlideChange = false;
        }
      });
    }
    render() {
      const { slidesPerMove, scrolling } = this;
      let pagesCount = 0;
      let currentPage = 0;
      let prevEnabled = false;
      let nextEnabled = false;
      if (this.hasUpdated) {
        pagesCount = this.getPageCount();
        currentPage = this.getCurrentPage();
        prevEnabled = this.canScrollPrev();
        nextEnabled = this.canScrollNext();
      }
      const isRTL2 = o5 ? this.dir === "rtl" : this.localize.dir() === "rtl";
      return b2`
      <div part="carousel" class="carousel">
        <div
          id="scroll-container"
          part="scroll-container"
          class="${e7({
        slides: true,
        "slides-horizontal": this.orientation === "horizontal",
        "slides-vertical": this.orientation === "vertical",
        "slides-dragging": this.dragging,
        "slides-awaiting-position": this.awaitingInitialPosition
      })}"
          style=${o7({ "--slides-per-page": this.slidesPerPage })}
          aria-busy="${scrolling ? "true" : "false"}"
          aria-atomic="true"
          tabindex="0"
          @keydown=${this.handleKeyDown}
          @mousedown="${this.handleMouseDragStart}"
          @scroll="${this.handleScroll}"
          @scrollend=${this.handleScrollEnd}
          @click=${this.handleClick}
        >
          <slot @slotchange=${() => this.requestUpdate()}></slot>
        </div>

        ${this.navigation ? b2`
                <div part="navigation" class="navigation">
                  <button
                    part="navigation-button navigation-button-previous"
                    class="${e7({
        "navigation-button": true,
        "navigation-button-previous": true,
        "navigation-button-disabled": !prevEnabled
      })}"
                    aria-label="${this.localize.term("previousSlide")}"
                    aria-controls="scroll-container"
                    aria-disabled="${prevEnabled ? "false" : "true"}"
                    @click=${prevEnabled ? () => this.previous() : null}
                  >
                    <slot name="previous-icon">
                      <cs-icon
                        library="system"
                        name="${isRTL2 ? "keyboard_arrow_right" : "keyboard_arrow_left"}"
                      ></cs-icon>
                    </slot>
                  </button>

                  <button
                    part="navigation-button navigation-button-next"
                    class=${e7({
        "navigation-button": true,
        "navigation-button-next": true,
        "navigation-button-disabled": !nextEnabled
      })}
                    aria-label="${this.localize.term("nextSlide")}"
                    aria-controls="scroll-container"
                    aria-disabled="${nextEnabled ? "false" : "true"}"
                    @click=${nextEnabled ? () => this.next() : null}
                  >
                    <slot name="next-icon">
                      <cs-icon
                        library="system"
                        name="${isRTL2 ? "keyboard_arrow_left" : "keyboard_arrow_right"}"
                      ></cs-icon>
                    </slot>
                  </button>
                </div>
              ` : ""}
        ${this.pagination ? b2`
                <div
                  part="pagination"
                  role="tablist"
                  class="${e7({
        pagination: true,
        "pagination-awaiting-position": this.awaitingInitialPosition
      })}"
                  aria-controls="scroll-container"
                >
                  ${o10(o11(pagesCount), (index) => {
        const isActive = index === currentPage;
        return b2`
                      <button
                        part="pagination-item ${isActive ? "pagination-item-active" : ""}"
                        class="${e7({
          "pagination-item": true,
          "pagination-item-active": isActive
        })}"
                        role="tab"
                        aria-selected="${isActive ? "true" : "false"}"
                        aria-label="${this.localize.term("goToSlide", index + 1, pagesCount)}"
                        tabindex=${isActive ? "0" : "-1"}
                        @click=${() => this.goToSlide(index * slidesPerMove)}
                        @keydown=${this.handleKeyDown}
                      ></button>
                    `;
      })}
                </div>
              ` : b2``}
      </div>
    `;
    }
  };
  CsCarousel.css = carousel_styles_default;
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsCarousel.prototype, "loop", 2);
  __decorateClass([
    n4({ type: Number, reflect: true })
  ], CsCarousel.prototype, "slides", 2);
  __decorateClass([
    n4({ type: Number, reflect: true })
  ], CsCarousel.prototype, "currentSlide", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsCarousel.prototype, "navigation", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsCarousel.prototype, "pagination", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsCarousel.prototype, "autoplay", 2);
  __decorateClass([
    n4({ type: Number, attribute: "autoplay-interval" })
  ], CsCarousel.prototype, "autoplayInterval", 2);
  __decorateClass([
    n4({ type: Number, attribute: "slides-per-page" })
  ], CsCarousel.prototype, "slidesPerPage", 2);
  __decorateClass([
    n4({ type: Number, attribute: "slides-per-move" })
  ], CsCarousel.prototype, "slidesPerMove", 2);
  __decorateClass([
    n4()
  ], CsCarousel.prototype, "orientation", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true, attribute: "mouse-dragging" })
  ], CsCarousel.prototype, "mouseDragging", 2);
  __decorateClass([
    e5(".slides")
  ], CsCarousel.prototype, "scrollContainer", 2);
  __decorateClass([
    e5(".pagination")
  ], CsCarousel.prototype, "paginationContainer", 2);
  __decorateClass([
    r5()
  ], CsCarousel.prototype, "activeSlide", 2);
  __decorateClass([
    r5()
  ], CsCarousel.prototype, "scrolling", 2);
  __decorateClass([
    r5()
  ], CsCarousel.prototype, "dragging", 2);
  __decorateClass([
    r5()
  ], CsCarousel.prototype, "awaitingInitialPosition", 2);
  __decorateClass([
    t4({ passive: true })
  ], CsCarousel.prototype, "handleScroll", 1);
  __decorateClass([
    watch("loop", { waitUntilFirstUpdate: true }),
    watch("slidesPerPage", { waitUntilFirstUpdate: true })
  ], CsCarousel.prototype, "initializeSlides", 1);
  __decorateClass([
    watch("activeSlide")
  ], CsCarousel.prototype, "handleSlideChange", 1);
  __decorateClass([
    watch("slidesPerMove")
  ], CsCarousel.prototype, "updateSlidesSnap", 1);
  __decorateClass([
    watch("autoplay")
  ], CsCarousel.prototype, "handleAutoplayChange", 1);
  CsCarousel = __decorateClass([
    customElement("cs-carousel")
  ], CsCarousel);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.V5YKGPBS.js
  var carousel_item_styles_default = i`
  :host {
    --aspect-ratio: inherit;

    display: flex;
    align-items: center;
    justify-content: center;
    flex-direction: column;
    width: 100%;
    max-height: 100%;
    aspect-ratio: var(--aspect-ratio);
    scroll-snap-align: start;
    scroll-snap-stop: always;
  }

  ::slotted(img) {
    width: 100% !important;
    height: 100% !important;
    object-fit: cover;
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.BZY5MZVJ.js
  var CsCarouselItem = class extends CornerstoneElement {
    connectedCallback() {
      super.connectedCallback();
      this.setAttribute("role", "group");
    }
    render() {
      return b2` <slot></slot> `;
    }
  };
  CsCarouselItem.css = carousel_item_styles_default;
  CsCarouselItem = __decorateClass([
    customElement("cs-carousel-item")
  ], CsCarouselItem);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.TE6IBKE4.js
  var checkbox_styles_default = i`
  :host {
    --checked-icon-color: var(--cs-color-brand-on-loud);
    --checked-icon-scale: 0.8;

    display: inline-flex;
    color: var(--cs-form-control-value-color);
    font-family: inherit;
    font-weight: var(--cs-form-control-value-font-weight);
    line-height: var(--cs-form-control-value-line-height);
    user-select: none;
    -webkit-user-select: none;
  }

  [part~='control'] {
    display: inline-flex;
    flex: 0 0 auto;
    position: relative;
    align-items: center;
    justify-content: center;
    width: var(--cs-form-control-toggle-size);
    height: var(--cs-form-control-toggle-size);
    border-color: var(--cs-form-control-border-color);
    border-radius: min(
      calc(var(--cs-form-control-toggle-size) * 0.375),
      var(--cs-border-radius-s)
    ); /* min prevents entirely circular checkbox */
    border-style: var(--cs-border-style);
    border-width: var(--cs-form-control-border-width);
    background-color: var(--cs-form-control-background-color);
    transition:
      background var(--cs-transition-normal),
      border-color var(--cs-transition-fast),
      box-shadow var(--cs-transition-fast),
      color var(--cs-transition-fast);
    transition-timing-function: var(--cs-transition-easing);

    margin-inline-end: 0.5em;
  }

  [part~='checkbox'] {
    display: flex;
    align-items: flex-start;
    position: relative;
    color: currentColor;
    vertical-align: middle;
    cursor: pointer;
  }

  [part~='label'] {
    display: inline;
  }

  /* Checked */
  [part~='control']:has(:checked, :indeterminate) {
    color: var(--checked-icon-color);
    border-color: var(--cs-form-control-activated-color);
    background-color: var(--cs-form-control-activated-color);
  }

  /* Focus */
  [part~='control']:has(> input:focus-visible:not(:disabled)) {
    outline: var(--cs-focus-ring);
    outline-offset: var(--cs-focus-ring-offset);
  }

  /* Disabled */
  :host [part~='checkbox']:has(input:disabled) {
    opacity: 0.5;
    cursor: not-allowed;
  }

  input {
    position: absolute;
    padding: 0;
    margin: 0;
    height: 100%;
    width: 100%;
    opacity: 0;
    pointer-events: none;
  }

  [part~='icon'] {
    display: flex;
    scale: var(--checked-icon-scale);

    /* Without this, Safari renders the icon slightly to the left */
    &::part(svg) {
      translate: 0.0009765625em;
    }

    input:not(:checked, :indeterminate) + & {
      visibility: hidden;
    }
  }

  :host([required]) [part~='label']::after {
    content: var(--cs-form-control-required-content);
    color: var(--cs-form-control-required-content-color);
    margin-inline-start: var(--cs-form-control-required-content-offset);
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.BWRQEKED.js
  var RequiredValidator = (options2 = {}) => {
    const { validationProperty = "value" } = options2;
    const canCreateElement = typeof document !== "undefined" && "createElement" in document;
    const provided = options2.validationElement;
    let validationElement;
    if (typeof provided === "function") {
      if (canCreateElement) {
        validationElement = provided();
      }
    } else if (provided) {
      validationElement = provided;
    } else if (canCreateElement) {
      validationElement = Object.assign(document.createElement("input"), { required: true });
    }
    const obj = {
      observedAttributes: ["required"],
      message: validationElement?.validationMessage,
      // @TODO: Add a translation.
      checkValidity(element) {
        const validity = {
          message: "",
          isValid: true,
          invalidKeys: []
        };
        const isRequired = element.required ?? element.hasAttribute("required");
        if (!isRequired) {
          return validity;
        }
        const value = element[validationProperty];
        const isEmpty = !value;
        if (isEmpty) {
          validity.message = typeof obj.message === "function" ? obj.message(element) : obj.message || "";
          validity.isValid = false;
          validity.invalidKeys.push("valueMissing");
        }
        return validity;
      }
    };
    return obj;
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.FGFWM4HV.js
  var form_control_styles_default = i`
  :host {
    display: flex;
    flex-direction: column;
  }

  /* Treat wrapped labels, inputs, and hints as direct children of the host element */
  [part~='form-control'] {
    display: contents;
  }

  /* Label */
  :is([part~='form-control-label'], [part~='label']):has(*:not(:empty)),
  :is([part~='form-control-label'], [part~='label']).has-label {
    display: inline-flex;
    color: var(--cs-form-control-label-color);
    font-weight: var(--cs-form-control-label-font-weight);
    line-height: var(--cs-form-control-label-line-height);
    margin-block-end: 0.5em;
  }

  :host([required]) :is([part~='form-control-label'], [part~='label'])::after {
    content: var(--cs-form-control-required-content);
    margin-inline-start: var(--cs-form-control-required-content-offset);
    color: var(--cs-form-control-required-content-color);
  }

  /* Help text */
  [part~='hint'] {
    display: block;
    color: var(--cs-form-control-hint-color);
    font-weight: var(--cs-form-control-hint-font-weight);
    line-height: var(--cs-form-control-hint-line-height);
    margin-block-start: 0.5em;
    font-size: var(--cs-font-size-smaller);

    &:not(.has-slotted, .has-hint, .has-count) {
      display: none;
    }
  }
`;

  // node_modules/lit-html/directives/live.js
  var l5 = e6(class extends i5 {
    constructor(r9) {
      if (super(r9), r9.type !== t5.PROPERTY && r9.type !== t5.ATTRIBUTE && r9.type !== t5.BOOLEAN_ATTRIBUTE) throw Error("The `live` directive is not allowed on child or event bindings");
      if (!r7(r9)) throw Error("`live` bindings can only contain a single expression");
    }
    render(r9) {
      return r9;
    }
    update(i10, [t8]) {
      if (t8 === E || t8 === A) return t8;
      const o13 = i10.element, l6 = i10.name;
      if (i10.type === t5.PROPERTY) {
        if (t8 === o13[l6]) return E;
      } else if (i10.type === t5.BOOLEAN_ATTRIBUTE) {
        if (!!t8 === o13.hasAttribute(l6)) return E;
      } else if (i10.type === t5.ATTRIBUTE && o13.getAttribute(l6) === t8 + "") return E;
      return p3(i10), t8;
    }
  });

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.ODLQFTLV.js
  var CsCheckbox = class extends CornerstoneFormAssociatedElement {
    constructor() {
      super(...arguments);
      this.hasSlotController = new HasSlotController(this, "hint");
      this.title = "";
      this._value = this.getAttribute("value") ?? null;
      this.size = "m";
      this.disabled = false;
      this.indeterminate = false;
      this._checked = null;
      this.defaultChecked = this.hasAttribute("checked");
      this.required = false;
      this.hint = "";
      this.ssrHint = false;
    }
    static get validators() {
      return [
        ...super.validators,
        RequiredValidator({
          validationProperty: "checked",
          // Use a checkbox so we get "free" translation strings.
          validationElement: () => Object.assign(document.createElement("input"), {
            type: "checkbox",
            required: true
          })
        })
      ];
    }
    /** The value of the checkbox, submitted as a name/value pair with form data. */
    get value() {
      return this._value ?? "on";
    }
    set value(val) {
      this._value = val;
    }
    get checked() {
      if (this.valueHasChanged) {
        return Boolean(this._checked);
      }
      return this._checked ?? this.defaultChecked;
    }
    set checked(val) {
      this._checked = Boolean(val);
      this.valueHasChanged = true;
    }
    handleClick() {
      this.hasInteracted = true;
      this.checked = !this.checked;
      this.indeterminate = false;
      this.updateComplete.then(() => {
        this.dispatchEvent(new Event("change", { bubbles: true, composed: true }));
      });
    }
    connectedCallback() {
      super.connectedCallback();
      if (this.didSSR && !this.hasUpdated) {
        this.updateComplete.then(() => {
          this.handleDefaultCheckedChange();
        });
        return;
      }
      this.handleDefaultCheckedChange();
    }
    handleDefaultCheckedChange() {
      this.handleValueOrCheckedChange();
    }
    handleValueOrCheckedChange() {
      if (this.didSSR && !this.hasUpdated) {
        this.updateComplete.then(() => {
          this.handleValueOrCheckedChange();
        });
        return;
      }
      this.setValue(this.checked ? this.value : null, this._value);
      this.updateValidity();
    }
    handleStateChange() {
      if (this.hasUpdated) {
        this.input.checked = this.checked;
        this.input.indeterminate = this.indeterminate;
      }
      this.customStates.set("checked", this.checked);
      this.customStates.set("indeterminate", this.indeterminate);
      this.updateValidity();
    }
    handleDisabledChange() {
      this.customStates.set("disabled", this.disabled);
    }
    willUpdate(changedProperties) {
      super.willUpdate(changedProperties);
      if (changedProperties.has("value") || changedProperties.has("checked") || changedProperties.has("defaultChecked") || changedProperties.has("disabled")) {
        this.handleValueOrCheckedChange();
      }
    }
    formResetCallback() {
      this._checked = null;
      super.formResetCallback();
      this.handleValueOrCheckedChange();
    }
    /** Simulates a click on the checkbox. */
    click() {
      this.input.click();
    }
    /** Sets focus on the checkbox. */
    focus(options2) {
      this.input.focus(options2);
    }
    /** Removes focus from the checkbox. */
    blur() {
      this.input.blur();
    }
    render() {
      const hasHintSlot = this.hasSlotController.test("hint", "ssrHint");
      const hasHint = this.hint ? true : !!hasHintSlot;
      const isIndeterminate = !this.checked && this.indeterminate;
      const iconName = isIndeterminate ? "horizontal_rule" : "check";
      const stateIconPart = isIndeterminate ? "indeterminate-icon" : "checked-icon";
      const checkedAttribute = this.didSSR && !this.hasUpdated ? this.checked : this.defaultChecked;
      const checkedProperty = this.didSSR && !this.hasUpdated ? null : l5(this.checked);
      return b2`
      <label part="checkbox">
        <span part="control">
          <input
            class="input"
            type="checkbox"
            title=${this.title}
            name=${o8(this.name)}
            value=${o8(this.value)}
            .indeterminate=${l5(this.indeterminate)}
            .checked=${o8(checkedProperty)}
            ?checked=${checkedAttribute}
            ?disabled=${this.disabled}
            ?required=${this.required}
            aria-checked=${this.indeterminate ? "mixed" : this.checked ? "true" : "false"}
            aria-describedby="hint"
            @click=${this.handleClick}
          />

          <cs-icon part="${stateIconPart} icon" library="system" name=${iconName}></cs-icon>
        </span>

        <slot part="label"></slot>
      </label>

      <slot
        id="hint"
        part="hint"
        name="hint"
        aria-hidden=${hasHint ? "false" : "true"}
        class="${e7({ "has-slotted": hasHint })}"
      >
        ${this.hint}
      </slot>
    `;
    }
  };
  CsCheckbox.css = [form_control_styles_default, size_styles_default, checkbox_styles_default];
  CsCheckbox.shadowRootOptions = { ...CornerstoneFormAssociatedElement.shadowRootOptions, delegatesFocus: true };
  __decorateClass([
    e5('input[type="checkbox"]')
  ], CsCheckbox.prototype, "input", 2);
  __decorateClass([
    n4()
  ], CsCheckbox.prototype, "title", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsCheckbox.prototype, "value", 1);
  __decorateClass([
    n4({ reflect: true })
  ], CsCheckbox.prototype, "size", 2);
  __decorateClass([
    n4({ type: Boolean })
  ], CsCheckbox.prototype, "disabled", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsCheckbox.prototype, "indeterminate", 2);
  __decorateClass([
    n4({ type: Boolean, attribute: false })
  ], CsCheckbox.prototype, "checked", 1);
  __decorateClass([
    n4({ type: Boolean, reflect: true, attribute: "checked" })
  ], CsCheckbox.prototype, "defaultChecked", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsCheckbox.prototype, "required", 2);
  __decorateClass([
    n4()
  ], CsCheckbox.prototype, "hint", 2);
  __decorateClass([
    n4({ attribute: "ssr-hint", type: Boolean })
  ], CsCheckbox.prototype, "ssrHint", 2);
  __decorateClass([
    watch(["checked", "defaultChecked"])
  ], CsCheckbox.prototype, "handleDefaultCheckedChange", 1);
  __decorateClass([
    watch(["checked", "indeterminate"])
  ], CsCheckbox.prototype, "handleStateChange", 1);
  __decorateClass([
    watch("disabled")
  ], CsCheckbox.prototype, "handleDisabledChange", 1);
  CsCheckbox = __decorateClass([
    customElement("cs-checkbox")
  ], CsCheckbox);
  CsCheckbox.disableWarning?.("change-in-update");

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.ZQ5PR7MV.js
  var checkbox_group_styles_default = i`
  :host {
    --gap: 0.5em;

    display: block;
  }

  :host([orientation='horizontal']) {
    --gap: 1em;
  }

  .form-control {
    position: relative;
    border: none;
    padding: 0;
    margin: 0;
  }

  .label {
    padding: 0;
  }

  .checkbox-group-required .label::after {
    content: var(--cs-form-control-required-content);
    margin-inline-start: var(--cs-form-control-required-content-offset);
  }

  /* The group of checkboxes */
  [part~='form-control-input'] {
    display: flex;
    flex-direction: column;
    /* Keep items sized to their content so the clickable label doesn't span the full width */
    align-items: start;
    gap: var(--gap);
    margin-block-start: 0.5em;
  }

  /* Horizontal */
  :host([orientation='horizontal']) [part~='form-control-input'] {
    flex-direction: row;
    flex-wrap: wrap;
    align-items: center;
  }

  /* Hint */
  [part~='hint'] {
    margin-block-start: 0.5em;
  }

  /* Hide the required asterisk on individual controls; the group's label carries the indicator instead. */
  ::slotted(cs-checkbox[required]),
  ::slotted(cs-switch[required]) {
    --cs-form-control-required-content: '';
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.5XTELNRF.js
  var CsCheckboxGroup = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.hasSlotController = new HasSlotController(this, "hint", "label");
      this.label = "";
      this.hint = "";
      this.orientation = "vertical";
      this.required = false;
      this.ssrLabel = false;
      this.ssrHint = false;
      this.syncCheckboxElements = () => {
        if (!this.size) {
          return;
        }
        for (const checkbox of this.getAllCheckboxes()) {
          checkbox.setAttribute("size", this.size);
        }
      };
    }
    updated(changedProperties) {
      if (changedProperties.has("size")) {
        this.syncCheckboxElements();
      }
    }
    /** Returns all grouped checkbox and switch elements. */
    getAllCheckboxes() {
      return [...this.querySelectorAll(":is(cs-checkbox, cs-switch)")];
    }
    render() {
      const hasLabelSlot = this.hasSlotController.test("label", "ssrLabel");
      const hasHintSlot = this.hasSlotController.test("hint", "ssrHint");
      const hasLabel = this.label ? true : !!hasLabelSlot;
      const hasHint = this.hint ? true : !!hasHintSlot;
      return b2`
      <fieldset
        part="form-control"
        class=${e7({
        "form-control": true,
        "checkbox-group-required": this.required,
        "form-control-has-label": hasLabel
      })}
      >
        <label
          part="form-control-label"
          id="label"
          class=${e7({
        label: true,
        "has-label": hasLabel
      })}
          aria-hidden=${hasLabel ? "false" : "true"}
        >
          <slot name="label">${this.label}</slot>
        </label>

        <div part="form-control-input" role="group" aria-labelledby="label" aria-describedby="hint">
          <slot @slotchange=${this.syncCheckboxElements}></slot>
        </div>

        <slot
          id="hint"
          name="hint"
          part="hint"
          class=${e7({
        "has-slotted": hasHint
      })}
          aria-hidden=${hasHint ? "false" : "true"}
          >${this.hint}</slot
        >
      </fieldset>
    `;
    }
  };
  CsCheckboxGroup.css = [size_styles_default, form_control_styles_default, checkbox_group_styles_default];
  __decorateClass([
    n4()
  ], CsCheckboxGroup.prototype, "label", 2);
  __decorateClass([
    n4({ attribute: "hint" })
  ], CsCheckboxGroup.prototype, "hint", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsCheckboxGroup.prototype, "orientation", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsCheckboxGroup.prototype, "size", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsCheckboxGroup.prototype, "required", 2);
  __decorateClass([
    n4({ type: Boolean, attribute: "ssr-label" })
  ], CsCheckboxGroup.prototype, "ssrLabel", 2);
  __decorateClass([
    n4({ type: Boolean, attribute: "ssr-hint" })
  ], CsCheckboxGroup.prototype, "ssrHint", 2);
  CsCheckboxGroup = __decorateClass([
    customElement("cs-checkbox-group")
  ], CsCheckboxGroup);
  CsCheckboxGroup.disableWarning?.("change-in-update");

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.CIU2UIYH.js
  function drag(container, options2) {
    function move(pointerEvent) {
      const dims = container.getBoundingClientRect();
      const defaultView = container.ownerDocument.defaultView;
      const offsetX = dims.left + defaultView.pageXOffset;
      const offsetY = dims.top + defaultView.pageYOffset;
      const x2 = pointerEvent.pageX - offsetX;
      const y3 = pointerEvent.pageY - offsetY;
      if (options2?.onMove) {
        options2.onMove(x2, y3);
      }
    }
    function stop() {
      document.removeEventListener("pointermove", move);
      document.removeEventListener("pointerup", stop);
      if (options2?.onStop) {
        options2.onStop();
      }
    }
    document.addEventListener("pointermove", move, { passive: true });
    document.addEventListener("pointerup", stop);
    if (options2?.initialEvent instanceof PointerEvent) {
      move(options2.initialEvent);
    }
  }
  var supportsTouch = typeof window !== "undefined" && "ontouchstart" in window;
  var DraggableElement = class {
    constructor(el, options2) {
      this.isActive = false;
      this.isDragging = false;
      this.handleDragStart = (event) => {
        const clientX = "touches" in event ? event.touches[0].clientX : event.clientX;
        const clientY = "touches" in event ? event.touches[0].clientY : event.clientY;
        if (this.isDragging || // Prevent right-clicks from triggering drags
        !supportsTouch && event.buttons > 1) {
          return;
        }
        this.isDragging = true;
        document.addEventListener("pointerup", this.handleDragStop);
        document.addEventListener("pointermove", this.handleDragMove);
        document.addEventListener("pointercancel", this.handleDragStop);
        document.addEventListener("touchend", this.handleDragStop);
        document.addEventListener("touchmove", this.handleDragMove);
        document.addEventListener("touchcancel", this.handleDragStop);
        this.options.start(clientX, clientY);
      };
      this.handleDragStop = (event) => {
        const clientX = "changedTouches" in event ? event.changedTouches[0].clientX : event.clientX;
        const clientY = "changedTouches" in event ? event.changedTouches[0].clientY : event.clientY;
        this.isDragging = false;
        document.removeEventListener("pointerup", this.handleDragStop);
        document.removeEventListener("pointermove", this.handleDragMove);
        document.removeEventListener("pointercancel", this.handleDragStop);
        document.removeEventListener("touchend", this.handleDragStop);
        document.removeEventListener("touchmove", this.handleDragMove);
        document.removeEventListener("touchcancel", this.handleDragStop);
        this.options.stop(clientX, clientY);
      };
      this.handleDragMove = (event) => {
        const clientX = "touches" in event ? event.touches[0].clientX : event.clientX;
        const clientY = "touches" in event ? event.touches[0].clientY : event.clientY;
        window.getSelection()?.removeAllRanges();
        this.options.move(clientX, clientY);
      };
      this.element = el;
      this.options = {
        start: () => void 0,
        stop: () => void 0,
        move: () => void 0,
        ...options2
      };
      this.start();
    }
    /** Start listening to drags. */
    start() {
      if (!this.isActive) {
        this.element.addEventListener("pointerdown", this.handleDragStart);
        if (supportsTouch) {
          this.element.addEventListener("touchstart", this.handleDragStart);
        }
        this.isActive = true;
      }
    }
    /** Stop listening to drags. */
    stop() {
      document.removeEventListener("pointerup", this.handleDragStop);
      document.removeEventListener("pointermove", this.handleDragMove);
      document.removeEventListener("pointercancel", this.handleDragStop);
      document.removeEventListener("touchend", this.handleDragStop);
      document.removeEventListener("touchmove", this.handleDragMove);
      document.removeEventListener("touchcancel", this.handleDragStop);
      this.element.removeEventListener("pointerdown", this.handleDragStart);
      if (supportsTouch) {
        this.element.removeEventListener("touchstart", this.handleDragStart);
      }
      this.isActive = false;
      this.isDragging = false;
    }
    /** Starts or stops the drag listeners. */
    toggle(isActive) {
      const isGoingToBeActive = isActive !== void 0 ? isActive : !this.isActive;
      if (isGoingToBeActive) {
        this.start();
      } else {
        this.stop();
      }
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.UVNKJQB6.js
  var visually_hidden_styles_default = i`
  .cs-visually-hidden:not(:focus-within),
  .cs-visually-hidden-force,
  .cs-visually-hidden-hint::part(hint),
  .cs-visually-hidden-label::part(label),
  .cs-visually-hidden-label::part(form-control-label) {
    position: absolute !important;
    width: 1px !important;
    height: 1px !important;
    clip: rect(0 0 0 0) !important;
    clip-path: inset(50%) !important;
    border: none !important;
    overflow: hidden !important;
    white-space: nowrap !important;
    padding: 0 !important;
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.OW5I3LRS.js
  var dismissibleStack = [];
  function registerDismissible(key) {
    dismissibleStack.push(key);
  }
  function unregisterDismissible(key) {
    for (let i10 = dismissibleStack.length - 1; i10 >= 0; i10--) {
      if (dismissibleStack[i10] === key) {
        dismissibleStack.splice(i10, 1);
        break;
      }
    }
  }
  function isTopDismissible(key) {
    return dismissibleStack.length > 0 && dismissibleStack[dismissibleStack.length - 1] === key;
  }

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.4XTOBUQA.js
  var color_picker_styles_default = i`
  :host {
    --grid-width: 17em;
    --grid-height: 12em;
    --grid-handle-size: 1.25em;
    --slider-height: 1em;
    --slider-handle-size: calc(var(--slider-height) + 0.25em);
  }

  .color-picker {
    background-color: var(--cs-color-surface-raised);
    border-radius: var(--cs-border-radius-m);
    border-style: var(--cs-border-style);
    border-width: var(--cs-border-width-s);
    border-color: var(--cs-color-surface-border);
    box-shadow: var(--cs-shadow-m);
    color: var(--color);
    font: inherit;
    font-size: inherit;
    user-select: none;
    width: var(--grid-width);
    -webkit-user-select: none;
  }

  .grid {
    position: relative;
    height: var(--grid-height);
    background-image:
      linear-gradient(to bottom, rgba(0, 0, 0, 0) 0%, rgba(0, 0, 0, 1) 100%),
      linear-gradient(to right, #fff 0%, rgba(255, 255, 255, 0) 100%);
    border-top-left-radius: calc(var(--cs-border-radius-m) - var(--cs-border-width-s));
    border-top-right-radius: calc(var(--cs-border-radius-m) - var(--cs-border-width-s));
    cursor: crosshair;
    forced-color-adjust: none;
  }

  .grid-handle {
    position: absolute;
    width: var(--grid-handle-size);
    height: var(--grid-handle-size);
    border-radius: var(--cs-border-radius-circle);
    box-shadow: 0 0 0 0.0625rem rgba(0, 0, 0, 0.2);
    border: solid 0.125rem white;
    margin-top: calc(var(--grid-handle-size) / -2);
    margin-left: calc(var(--grid-handle-size) / -2);
    transition: scale var(--cs-transition-normal) var(--cs-transition-easing);
  }

  .grid-handle-dragging {
    cursor: none;
    scale: 1.5;
  }

  .grid-handle:focus-visible {
    outline: var(--cs-focus-ring);
  }

  .controls {
    padding: 0.75em;
    display: flex;
    align-items: center;
  }

  .sliders {
    flex: 1 1 auto;
  }

  .slider {
    position: relative;
    height: var(--slider-height);
    border-radius: var(--cs-border-radius-s);
    box-shadow: inset 0 0 0 0.0625rem rgba(0, 0, 0, 0.2);
    forced-color-adjust: none;
  }

  .slider:not(:last-of-type) {
    margin-bottom: 0.75em;
  }

  .slider-handle {
    position: absolute;
    top: calc(50% - var(--slider-handle-size) / 2);
    width: var(--slider-handle-size);
    height: var(--slider-handle-size);
    border-radius: var(--cs-border-radius-circle);
    border: solid 0.125rem white;
    box-shadow: 0 0 0 0.0625rem rgba(0, 0, 0, 0.2);
    margin-left: calc(var(--slider-handle-size) / -2);
  }

  .slider-handle:focus-visible {
    outline: var(--cs-focus-ring);
  }

  .hue {
    background-image: linear-gradient(
      to right,
      rgb(255, 0, 0) 0%,
      rgb(255, 255, 0) 17%,
      rgb(0, 255, 0) 33%,
      rgb(0, 255, 255) 50%,
      rgb(0, 0, 255) 67%,
      rgb(255, 0, 255) 83%,
      rgb(255, 0, 0) 100%
    );
  }

  .alpha .alpha-gradient {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    border-radius: inherit;
  }

  .preview {
    flex: 0 0 auto;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    position: relative;
    width: 3em;
    height: 3em;
    border: none;
    border-radius: var(--cs-border-radius-circle);
    background: none;
    font-size: inherit;
    margin-inline-start: 0.75em;
    cursor: copy;
    forced-color-adjust: none;
  }

  .preview:before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    border-radius: inherit;
    box-shadow: inset 0 0 0 0.0625rem rgba(0, 0, 0, 0.2);

    /* We use a custom property in lieu of currentColor because of https://bugs.webkit.org/show_bug.cgi?id=216780 */
    background-color: var(--preview-color);
  }

  .preview:focus-visible {
    outline: var(--cs-focus-ring);
    outline-offset: var(--cs-focus-ring-offset);
  }

  .preview-color {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    border: solid 0.0625rem rgba(0, 0, 0, 0.125);
  }

  .preview-color-copied {
    animation: pulse 850ms;
  }

  @keyframes pulse {
    0% {
      box-shadow: 0 0 0 0 var(--cs-color-brand-fill-loud);
    }
    70% {
      box-shadow: 0 0 0 0.5rem transparent;
    }
    100% {
      box-shadow: 0 0 0 0 transparent;
    }
  }

  .user-input {
    display: flex;
    align-items: center;
    padding: 0 0.75em 0.75em 0.75em;
  }

  .user-input cs-input {
    min-width: 0; /* fix input width in Safari */
    flex: 1 1 auto;

    &::part(form-control-label) {
      /* Visually hidden */
      position: absolute !important;
      width: 1px !important;
      height: 1px !important;
      clip: rect(0 0 0 0) !important;
      clip-path: inset(50%) !important;
      border: none !important;
      overflow: hidden !important;
      white-space: nowrap !important;
      padding: 0 !important;
    }
  }

  .user-input cs-button-group {
    margin-inline-start: 0.75em;
    --_button-group-flex-wrap: nowrap;
  }

  .user-input cs-button:first-of-type {
    min-width: 3em;
    max-width: 3em;
  }

  .swatches {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(min(1.5em, 100%), 1fr));
    grid-gap: 0.5em;
    justify-items: center;
    border-block-start: var(--cs-form-control-border-style) var(--cs-form-control-border-width)
      var(--cs-color-surface-border);
    padding: 0.5em;
    forced-color-adjust: none;
  }

  .swatch {
    position: relative;
    aspect-ratio: 1 / 1;
    width: 100%;
    border-radius: var(--cs-border-radius-s);
  }

  .swatch .swatch-color {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    border: solid 0.0625rem rgba(0, 0, 0, 0.125);
    border-radius: inherit;
    cursor: pointer;
  }

  .swatch:focus-visible {
    outline: var(--cs-focus-ring);
    outline-offset: var(--cs-focus-ring-offset);
  }

  .transparent-bg {
    background-image:
      linear-gradient(45deg, var(--cs-color-neutral-fill-normal) 25%, transparent 25%),
      linear-gradient(45deg, transparent 75%, var(--cs-color-neutral-fill-normal) 75%),
      linear-gradient(45deg, transparent 75%, var(--cs-color-neutral-fill-normal) 75%),
      linear-gradient(45deg, var(--cs-color-neutral-fill-normal) 25%, transparent 25%);
    background-size: 0.5rem 0.5rem;
    background-position:
      0 0,
      0 0,
      -0.25rem -0.25rem,
      0.25rem 0.25rem;
  }

  :host([disabled]) {
    opacity: 0.5;
    cursor: not-allowed;

    .grid,
    .grid-handle,
    .slider,
    .slider-handle,
    .preview,
    .swatch,
    .swatch-color {
      pointer-events: none;
    }
  }

  /*
   * Color dropdown
   */

  .color-dropdown {
    display: contents;
  }

  .color-dropdown::part(panel) {
    max-height: none;
    background-color: var(--cs-color-surface-raised);
    border: var(--cs-border-style) var(--cs-border-width-s) var(--cs-color-surface-border);
    border-radius: var(--cs-border-radius-m);
    overflow: visible;
  }

  .trigger {
    display: block;
    position: relative;
    background-color: transparent;
    border: none;
    cursor: pointer;
    font-size: inherit;
    forced-color-adjust: none;
    width: var(--cs-form-control-height);
    height: var(--cs-form-control-height);
    border-radius: var(--cs-form-control-border-radius);
  }

  .trigger:before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    border-radius: inherit;
    background-color: currentColor;
    box-shadow:
      inset 0 0 0 var(--cs-form-control-border-width) var(--cs-form-control-border-color),
      inset 0 0 0 calc(var(--cs-form-control-border-width) * 3) var(--cs-color-surface-default);
  }

  .trigger-empty:before {
    background-color: transparent;
  }

  .trigger:focus-visible {
    outline: none;
  }

  .trigger:focus-visible:not(.trigger:disabled) {
    outline: var(--cs-focus-ring);
    outline-offset: var(--cs-focus-ring-offset);
  }

  :host([disabled]) :is(.label, .trigger) {
    opacity: 0.5;
    cursor: not-allowed;
  }

  .form-control.form-control-has-label .label {
    cursor: pointer;
    display: inline-block;
  }
`;

  // node_modules/@ctrl/tinycolor/dist/module/util.js
  function bound01(n9, max2) {
    if (isOnePointZero(n9)) {
      n9 = "100%";
    }
    const isPercent = isPercentage(n9);
    n9 = max2 === 360 ? n9 : Math.min(max2, Math.max(0, parseFloat(n9)));
    if (isPercent) {
      n9 = parseInt(String(n9 * max2), 10) / 100;
    }
    if (Math.abs(n9 - max2) < 1e-6) {
      return 1;
    }
    if (max2 === 360) {
      n9 = (n9 < 0 ? n9 % max2 + max2 : n9 % max2) / parseFloat(String(max2));
    } else {
      n9 = n9 % max2 / parseFloat(String(max2));
    }
    return n9;
  }
  function clamp01(val) {
    return Math.min(1, Math.max(0, val));
  }
  function isOnePointZero(n9) {
    return typeof n9 === "string" && n9.indexOf(".") !== -1 && parseFloat(n9) === 1;
  }
  function isPercentage(n9) {
    return typeof n9 === "string" && n9.indexOf("%") !== -1;
  }
  function boundAlpha(a4) {
    a4 = parseFloat(a4);
    if (isNaN(a4) || a4 < 0 || a4 > 1) {
      a4 = 1;
    }
    return a4;
  }
  function convertToPercentage(n9) {
    if (Number(n9) <= 1) {
      return `${Number(n9) * 100}%`;
    }
    return n9;
  }
  function pad2(c6) {
    return c6.length === 1 ? "0" + c6 : String(c6);
  }

  // node_modules/@ctrl/tinycolor/dist/module/conversion.js
  function rgbToRgb(r9, g2, b3) {
    return {
      r: bound01(r9, 255) * 255,
      g: bound01(g2, 255) * 255,
      b: bound01(b3, 255) * 255
    };
  }
  function rgbToHsl(r9, g2, b3) {
    r9 = bound01(r9, 255);
    g2 = bound01(g2, 255);
    b3 = bound01(b3, 255);
    const max2 = Math.max(r9, g2, b3);
    const min2 = Math.min(r9, g2, b3);
    let h3 = 0;
    let s7 = 0;
    const l6 = (max2 + min2) / 2;
    if (max2 === min2) {
      s7 = 0;
      h3 = 0;
    } else {
      const d3 = max2 - min2;
      s7 = l6 > 0.5 ? d3 / (2 - max2 - min2) : d3 / (max2 + min2);
      switch (max2) {
        case r9:
          h3 = (g2 - b3) / d3 + (g2 < b3 ? 6 : 0);
          break;
        case g2:
          h3 = (b3 - r9) / d3 + 2;
          break;
        case b3:
          h3 = (r9 - g2) / d3 + 4;
          break;
        default:
          break;
      }
      h3 /= 6;
    }
    return { h: h3, s: s7, l: l6 };
  }
  function hue2rgb(p4, q, t8) {
    if (t8 < 0) {
      t8 += 1;
    }
    if (t8 > 1) {
      t8 -= 1;
    }
    if (t8 < 1 / 6) {
      return p4 + (q - p4) * (6 * t8);
    }
    if (t8 < 1 / 2) {
      return q;
    }
    if (t8 < 2 / 3) {
      return p4 + (q - p4) * (2 / 3 - t8) * 6;
    }
    return p4;
  }
  function hslToRgb(h3, s7, l6) {
    let r9;
    let g2;
    let b3;
    h3 = bound01(h3, 360);
    s7 = bound01(s7, 100);
    l6 = bound01(l6, 100);
    if (s7 === 0) {
      g2 = l6;
      b3 = l6;
      r9 = l6;
    } else {
      const q = l6 < 0.5 ? l6 * (1 + s7) : l6 + s7 - l6 * s7;
      const p4 = 2 * l6 - q;
      r9 = hue2rgb(p4, q, h3 + 1 / 3);
      g2 = hue2rgb(p4, q, h3);
      b3 = hue2rgb(p4, q, h3 - 1 / 3);
    }
    return { r: r9 * 255, g: g2 * 255, b: b3 * 255 };
  }
  function rgbToHsv(r9, g2, b3) {
    r9 = bound01(r9, 255);
    g2 = bound01(g2, 255);
    b3 = bound01(b3, 255);
    const max2 = Math.max(r9, g2, b3);
    const min2 = Math.min(r9, g2, b3);
    let h3 = 0;
    const v2 = max2;
    const d3 = max2 - min2;
    const s7 = max2 === 0 ? 0 : d3 / max2;
    if (max2 === min2) {
      h3 = 0;
    } else {
      switch (max2) {
        case r9:
          h3 = (g2 - b3) / d3 + (g2 < b3 ? 6 : 0);
          break;
        case g2:
          h3 = (b3 - r9) / d3 + 2;
          break;
        case b3:
          h3 = (r9 - g2) / d3 + 4;
          break;
        default:
          break;
      }
      h3 /= 6;
    }
    return { h: h3, s: s7, v: v2 };
  }
  function hsvToRgb(h3, s7, v2) {
    h3 = bound01(h3, 360) * 6;
    s7 = bound01(s7, 100);
    v2 = bound01(v2, 100);
    const i10 = Math.floor(h3);
    const f3 = h3 - i10;
    const p4 = v2 * (1 - s7);
    const q = v2 * (1 - f3 * s7);
    const t8 = v2 * (1 - (1 - f3) * s7);
    const mod = i10 % 6;
    const r9 = [v2, q, p4, p4, t8, v2][mod];
    const g2 = [t8, v2, v2, q, p4, p4][mod];
    const b3 = [p4, p4, t8, v2, v2, q][mod];
    return { r: r9 * 255, g: g2 * 255, b: b3 * 255 };
  }
  function rgbToHex(r9, g2, b3, allow3Char) {
    const hex = [
      pad2(Math.round(r9).toString(16)),
      pad2(Math.round(g2).toString(16)),
      pad2(Math.round(b3).toString(16))
    ];
    if (allow3Char && hex[0].startsWith(hex[0].charAt(1)) && hex[1].startsWith(hex[1].charAt(1)) && hex[2].startsWith(hex[2].charAt(1))) {
      return hex[0].charAt(0) + hex[1].charAt(0) + hex[2].charAt(0);
    }
    return hex.join("");
  }
  function rgbaToHex(r9, g2, b3, a4, allow4Char) {
    const hex = [
      pad2(Math.round(r9).toString(16)),
      pad2(Math.round(g2).toString(16)),
      pad2(Math.round(b3).toString(16)),
      pad2(convertDecimalToHex(a4))
    ];
    if (allow4Char && hex[0].startsWith(hex[0].charAt(1)) && hex[1].startsWith(hex[1].charAt(1)) && hex[2].startsWith(hex[2].charAt(1)) && hex[3].startsWith(hex[3].charAt(1))) {
      return hex[0].charAt(0) + hex[1].charAt(0) + hex[2].charAt(0) + hex[3].charAt(0);
    }
    return hex.join("");
  }
  function cmykToRgb(c6, m3, y3, k2) {
    const cConv = c6 / 100;
    const mConv = m3 / 100;
    const yConv = y3 / 100;
    const kConv = k2 / 100;
    const r9 = 255 * (1 - cConv) * (1 - kConv);
    const g2 = 255 * (1 - mConv) * (1 - kConv);
    const b3 = 255 * (1 - yConv) * (1 - kConv);
    return { r: r9, g: g2, b: b3 };
  }
  function rgbToCmyk(r9, g2, b3) {
    let c6 = 1 - r9 / 255;
    let m3 = 1 - g2 / 255;
    let y3 = 1 - b3 / 255;
    let k2 = Math.min(c6, m3, y3);
    if (k2 === 1) {
      c6 = 0;
      m3 = 0;
      y3 = 0;
    } else {
      c6 = (c6 - k2) / (1 - k2) * 100;
      m3 = (m3 - k2) / (1 - k2) * 100;
      y3 = (y3 - k2) / (1 - k2) * 100;
    }
    k2 *= 100;
    return {
      c: Math.round(c6),
      m: Math.round(m3),
      y: Math.round(y3),
      k: Math.round(k2)
    };
  }
  function convertDecimalToHex(d3) {
    return Math.round(parseFloat(d3) * 255).toString(16);
  }
  function convertHexToDecimal(h3) {
    return parseIntFromHex(h3) / 255;
  }
  function parseIntFromHex(val) {
    return parseInt(val, 16);
  }
  function numberInputToObject(color) {
    return {
      r: color >> 16,
      g: (color & 65280) >> 8,
      b: color & 255
    };
  }

  // node_modules/@ctrl/tinycolor/dist/module/css-color-names.js
  var names = {
    aliceblue: "#f0f8ff",
    antiquewhite: "#faebd7",
    aqua: "#00ffff",
    aquamarine: "#7fffd4",
    azure: "#f0ffff",
    beige: "#f5f5dc",
    bisque: "#ffe4c4",
    black: "#000000",
    blanchedalmond: "#ffebcd",
    blue: "#0000ff",
    blueviolet: "#8a2be2",
    brown: "#a52a2a",
    burlywood: "#deb887",
    cadetblue: "#5f9ea0",
    chartreuse: "#7fff00",
    chocolate: "#d2691e",
    coral: "#ff7f50",
    cornflowerblue: "#6495ed",
    cornsilk: "#fff8dc",
    crimson: "#dc143c",
    cyan: "#00ffff",
    darkblue: "#00008b",
    darkcyan: "#008b8b",
    darkgoldenrod: "#b8860b",
    darkgray: "#a9a9a9",
    darkgreen: "#006400",
    darkgrey: "#a9a9a9",
    darkkhaki: "#bdb76b",
    darkmagenta: "#8b008b",
    darkolivegreen: "#556b2f",
    darkorange: "#ff8c00",
    darkorchid: "#9932cc",
    darkred: "#8b0000",
    darksalmon: "#e9967a",
    darkseagreen: "#8fbc8f",
    darkslateblue: "#483d8b",
    darkslategray: "#2f4f4f",
    darkslategrey: "#2f4f4f",
    darkturquoise: "#00ced1",
    darkviolet: "#9400d3",
    deeppink: "#ff1493",
    deepskyblue: "#00bfff",
    dimgray: "#696969",
    dimgrey: "#696969",
    dodgerblue: "#1e90ff",
    firebrick: "#b22222",
    floralwhite: "#fffaf0",
    forestgreen: "#228b22",
    fuchsia: "#ff00ff",
    gainsboro: "#dcdcdc",
    ghostwhite: "#f8f8ff",
    goldenrod: "#daa520",
    gold: "#ffd700",
    gray: "#808080",
    green: "#008000",
    greenyellow: "#adff2f",
    grey: "#808080",
    honeydew: "#f0fff0",
    hotpink: "#ff69b4",
    indianred: "#cd5c5c",
    indigo: "#4b0082",
    ivory: "#fffff0",
    khaki: "#f0e68c",
    lavenderblush: "#fff0f5",
    lavender: "#e6e6fa",
    lawngreen: "#7cfc00",
    lemonchiffon: "#fffacd",
    lightblue: "#add8e6",
    lightcoral: "#f08080",
    lightcyan: "#e0ffff",
    lightgoldenrodyellow: "#fafad2",
    lightgray: "#d3d3d3",
    lightgreen: "#90ee90",
    lightgrey: "#d3d3d3",
    lightpink: "#ffb6c1",
    lightsalmon: "#ffa07a",
    lightseagreen: "#20b2aa",
    lightskyblue: "#87cefa",
    lightslategray: "#778899",
    lightslategrey: "#778899",
    lightsteelblue: "#b0c4de",
    lightyellow: "#ffffe0",
    lime: "#00ff00",
    limegreen: "#32cd32",
    linen: "#faf0e6",
    magenta: "#ff00ff",
    maroon: "#800000",
    mediumaquamarine: "#66cdaa",
    mediumblue: "#0000cd",
    mediumorchid: "#ba55d3",
    mediumpurple: "#9370db",
    mediumseagreen: "#3cb371",
    mediumslateblue: "#7b68ee",
    mediumspringgreen: "#00fa9a",
    mediumturquoise: "#48d1cc",
    mediumvioletred: "#c71585",
    midnightblue: "#191970",
    mintcream: "#f5fffa",
    mistyrose: "#ffe4e1",
    moccasin: "#ffe4b5",
    navajowhite: "#ffdead",
    navy: "#000080",
    oldlace: "#fdf5e6",
    olive: "#808000",
    olivedrab: "#6b8e23",
    orange: "#ffa500",
    orangered: "#ff4500",
    orchid: "#da70d6",
    palegoldenrod: "#eee8aa",
    palegreen: "#98fb98",
    paleturquoise: "#afeeee",
    palevioletred: "#db7093",
    papayawhip: "#ffefd5",
    peachpuff: "#ffdab9",
    peru: "#cd853f",
    pink: "#ffc0cb",
    plum: "#dda0dd",
    powderblue: "#b0e0e6",
    purple: "#800080",
    rebeccapurple: "#663399",
    red: "#ff0000",
    rosybrown: "#bc8f8f",
    royalblue: "#4169e1",
    saddlebrown: "#8b4513",
    salmon: "#fa8072",
    sandybrown: "#f4a460",
    seagreen: "#2e8b57",
    seashell: "#fff5ee",
    sienna: "#a0522d",
    silver: "#c0c0c0",
    skyblue: "#87ceeb",
    slateblue: "#6a5acd",
    slategray: "#708090",
    slategrey: "#708090",
    snow: "#fffafa",
    springgreen: "#00ff7f",
    steelblue: "#4682b4",
    tan: "#d2b48c",
    teal: "#008080",
    thistle: "#d8bfd8",
    tomato: "#ff6347",
    turquoise: "#40e0d0",
    violet: "#ee82ee",
    wheat: "#f5deb3",
    white: "#ffffff",
    whitesmoke: "#f5f5f5",
    yellow: "#ffff00",
    yellowgreen: "#9acd32"
  };

  // node_modules/@ctrl/tinycolor/dist/module/format-input.js
  function inputToRGB(color) {
    let rgb = { r: 0, g: 0, b: 0 };
    let a4 = 1;
    let s7 = null;
    let v2 = null;
    let l6 = null;
    let ok = false;
    let format = false;
    if (typeof color === "string") {
      color = stringInputToObject(color);
    }
    if (typeof color === "object") {
      if (isValidCSSUnit(color.r) && isValidCSSUnit(color.g) && isValidCSSUnit(color.b)) {
        rgb = rgbToRgb(color.r, color.g, color.b);
        ok = true;
        format = String(color.r).substr(-1) === "%" ? "prgb" : "rgb";
      } else if (isValidCSSUnit(color.h) && isValidCSSUnit(color.s) && isValidCSSUnit(color.v)) {
        s7 = convertToPercentage(color.s);
        v2 = convertToPercentage(color.v);
        rgb = hsvToRgb(color.h, s7, v2);
        ok = true;
        format = "hsv";
      } else if (isValidCSSUnit(color.h) && isValidCSSUnit(color.s) && isValidCSSUnit(color.l)) {
        s7 = convertToPercentage(color.s);
        l6 = convertToPercentage(color.l);
        rgb = hslToRgb(color.h, s7, l6);
        ok = true;
        format = "hsl";
      } else if (isValidCSSUnit(color.c) && isValidCSSUnit(color.m) && isValidCSSUnit(color.y) && isValidCSSUnit(color.k)) {
        rgb = cmykToRgb(color.c, color.m, color.y, color.k);
        ok = true;
        format = "cmyk";
      }
      if (Object.prototype.hasOwnProperty.call(color, "a")) {
        a4 = color.a;
      }
    }
    a4 = boundAlpha(a4);
    return {
      ok,
      format: color.format || format,
      r: Math.min(255, Math.max(rgb.r, 0)),
      g: Math.min(255, Math.max(rgb.g, 0)),
      b: Math.min(255, Math.max(rgb.b, 0)),
      a: a4
    };
  }
  var CSS_INTEGER = "[-\\+]?\\d+%?";
  var CSS_NUMBER = "[-\\+]?\\d*\\.\\d+%?";
  var CSS_UNIT = "(?:" + CSS_NUMBER + ")|(?:" + CSS_INTEGER + ")";
  var PERMISSIVE_MATCH3 = "[\\s|\\(]+(" + CSS_UNIT + ")[,|\\s]+(" + CSS_UNIT + ")[,|\\s]+(" + CSS_UNIT + ")\\s*\\)?";
  var PERMISSIVE_MATCH4 = (
    // eslint-disable-next-line prettier/prettier
    "[\\s|\\(]+(" + CSS_UNIT + ")[,|\\s]+(" + CSS_UNIT + ")[,|\\s]+(" + CSS_UNIT + ")[,|\\s]+(" + CSS_UNIT + ")\\s*\\)?"
  );
  var matchers = {
    CSS_UNIT: new RegExp(CSS_UNIT),
    rgb: new RegExp("rgb" + PERMISSIVE_MATCH3),
    rgba: new RegExp("rgba" + PERMISSIVE_MATCH4),
    hsl: new RegExp("hsl" + PERMISSIVE_MATCH3),
    hsla: new RegExp("hsla" + PERMISSIVE_MATCH4),
    hsv: new RegExp("hsv" + PERMISSIVE_MATCH3),
    hsva: new RegExp("hsva" + PERMISSIVE_MATCH4),
    cmyk: new RegExp("cmyk" + PERMISSIVE_MATCH4),
    hex3: /^#?([0-9a-fA-F]{1})([0-9a-fA-F]{1})([0-9a-fA-F]{1})$/,
    hex6: /^#?([0-9a-fA-F]{2})([0-9a-fA-F]{2})([0-9a-fA-F]{2})$/,
    hex4: /^#?([0-9a-fA-F]{1})([0-9a-fA-F]{1})([0-9a-fA-F]{1})([0-9a-fA-F]{1})$/,
    hex8: /^#?([0-9a-fA-F]{2})([0-9a-fA-F]{2})([0-9a-fA-F]{2})([0-9a-fA-F]{2})$/
  };
  function stringInputToObject(color) {
    color = color.trim().toLowerCase();
    if (color.length === 0) {
      return false;
    }
    let named = false;
    if (names[color]) {
      color = names[color];
      named = true;
    } else if (color === "transparent") {
      return { r: 0, g: 0, b: 0, a: 0, format: "name" };
    }
    let match = matchers.rgb.exec(color);
    if (match) {
      return { r: match[1], g: match[2], b: match[3] };
    }
    match = matchers.rgba.exec(color);
    if (match) {
      return { r: match[1], g: match[2], b: match[3], a: match[4] };
    }
    match = matchers.hsl.exec(color);
    if (match) {
      return { h: match[1], s: match[2], l: match[3] };
    }
    match = matchers.hsla.exec(color);
    if (match) {
      return { h: match[1], s: match[2], l: match[3], a: match[4] };
    }
    match = matchers.hsv.exec(color);
    if (match) {
      return { h: match[1], s: match[2], v: match[3] };
    }
    match = matchers.hsva.exec(color);
    if (match) {
      return { h: match[1], s: match[2], v: match[3], a: match[4] };
    }
    match = matchers.cmyk.exec(color);
    if (match) {
      return {
        c: match[1],
        m: match[2],
        y: match[3],
        k: match[4]
      };
    }
    match = matchers.hex8.exec(color);
    if (match) {
      return {
        r: parseIntFromHex(match[1]),
        g: parseIntFromHex(match[2]),
        b: parseIntFromHex(match[3]),
        a: convertHexToDecimal(match[4]),
        format: named ? "name" : "hex8"
      };
    }
    match = matchers.hex6.exec(color);
    if (match) {
      return {
        r: parseIntFromHex(match[1]),
        g: parseIntFromHex(match[2]),
        b: parseIntFromHex(match[3]),
        format: named ? "name" : "hex"
      };
    }
    match = matchers.hex4.exec(color);
    if (match) {
      return {
        r: parseIntFromHex(match[1] + match[1]),
        g: parseIntFromHex(match[2] + match[2]),
        b: parseIntFromHex(match[3] + match[3]),
        a: convertHexToDecimal(match[4] + match[4]),
        format: named ? "name" : "hex8"
      };
    }
    match = matchers.hex3.exec(color);
    if (match) {
      return {
        r: parseIntFromHex(match[1] + match[1]),
        g: parseIntFromHex(match[2] + match[2]),
        b: parseIntFromHex(match[3] + match[3]),
        format: named ? "name" : "hex"
      };
    }
    return false;
  }
  function isValidCSSUnit(color) {
    if (typeof color === "number") {
      return !Number.isNaN(color);
    }
    return matchers.CSS_UNIT.test(color);
  }

  // node_modules/@ctrl/tinycolor/dist/module/index.js
  var TinyColor = class _TinyColor {
    constructor(color = "", opts = {}) {
      if (color instanceof _TinyColor) {
        return color;
      }
      if (typeof color === "number") {
        color = numberInputToObject(color);
      }
      this.originalInput = color;
      const rgb = inputToRGB(color);
      this.originalInput = color;
      this.r = rgb.r;
      this.g = rgb.g;
      this.b = rgb.b;
      this.a = rgb.a;
      this.roundA = Math.round(100 * this.a) / 100;
      this.format = opts.format ?? rgb.format;
      this.gradientType = opts.gradientType;
      if (this.r < 1) {
        this.r = Math.round(this.r);
      }
      if (this.g < 1) {
        this.g = Math.round(this.g);
      }
      if (this.b < 1) {
        this.b = Math.round(this.b);
      }
      this.isValid = rgb.ok;
    }
    isDark() {
      return this.getBrightness() < 128;
    }
    isLight() {
      return !this.isDark();
    }
    /**
     * Returns the perceived brightness of the color, from 0-255.
     */
    getBrightness() {
      const rgb = this.toRgb();
      return (rgb.r * 299 + rgb.g * 587 + rgb.b * 114) / 1e3;
    }
    /**
     * Returns the perceived luminance of a color, from 0-1.
     */
    getLuminance() {
      const rgb = this.toRgb();
      let R2;
      let G;
      let B2;
      const RsRGB = rgb.r / 255;
      const GsRGB = rgb.g / 255;
      const BsRGB = rgb.b / 255;
      if (RsRGB <= 0.03928) {
        R2 = RsRGB / 12.92;
      } else {
        R2 = Math.pow((RsRGB + 0.055) / 1.055, 2.4);
      }
      if (GsRGB <= 0.03928) {
        G = GsRGB / 12.92;
      } else {
        G = Math.pow((GsRGB + 0.055) / 1.055, 2.4);
      }
      if (BsRGB <= 0.03928) {
        B2 = BsRGB / 12.92;
      } else {
        B2 = Math.pow((BsRGB + 0.055) / 1.055, 2.4);
      }
      return 0.2126 * R2 + 0.7152 * G + 0.0722 * B2;
    }
    /**
     * Returns the alpha value of a color, from 0-1.
     */
    getAlpha() {
      return this.a;
    }
    /**
     * Sets the alpha value on the current color.
     *
     * @param alpha - The new alpha value. The accepted range is 0-1.
     */
    setAlpha(alpha) {
      this.a = boundAlpha(alpha);
      this.roundA = Math.round(100 * this.a) / 100;
      return this;
    }
    /**
     * Returns whether the color is monochrome.
     */
    isMonochrome() {
      const { s: s7 } = this.toHsl();
      return s7 === 0;
    }
    /**
     * Returns the object as a HSVA object.
     */
    toHsv() {
      const hsv = rgbToHsv(this.r, this.g, this.b);
      return { h: hsv.h * 360, s: hsv.s, v: hsv.v, a: this.a };
    }
    /**
     * Returns the hsva values interpolated into a string with the following format:
     * "hsva(xxx, xxx, xxx, xx)".
     */
    toHsvString() {
      const hsv = rgbToHsv(this.r, this.g, this.b);
      const h3 = Math.round(hsv.h * 360);
      const s7 = Math.round(hsv.s * 100);
      const v2 = Math.round(hsv.v * 100);
      return this.a === 1 ? `hsv(${h3}, ${s7}%, ${v2}%)` : `hsva(${h3}, ${s7}%, ${v2}%, ${this.roundA})`;
    }
    /**
     * Returns the object as a HSLA object.
     */
    toHsl() {
      const hsl = rgbToHsl(this.r, this.g, this.b);
      return { h: hsl.h * 360, s: hsl.s, l: hsl.l, a: this.a };
    }
    /**
     * Returns the hsla values interpolated into a string with the following format:
     * "hsla(xxx, xxx, xxx, xx)".
     */
    toHslString() {
      const hsl = rgbToHsl(this.r, this.g, this.b);
      const h3 = Math.round(hsl.h * 360);
      const s7 = Math.round(hsl.s * 100);
      const l6 = Math.round(hsl.l * 100);
      return this.a === 1 ? `hsl(${h3}, ${s7}%, ${l6}%)` : `hsla(${h3}, ${s7}%, ${l6}%, ${this.roundA})`;
    }
    /**
     * Returns the hex value of the color.
     * @param allow3Char will shorten hex value to 3 char if possible
     */
    toHex(allow3Char = false) {
      return rgbToHex(this.r, this.g, this.b, allow3Char);
    }
    /**
     * Returns the hex value of the color -with a # prefixed.
     * @param allow3Char will shorten hex value to 3 char if possible
     */
    toHexString(allow3Char = false) {
      return "#" + this.toHex(allow3Char);
    }
    /**
     * Returns the hex 8 value of the color.
     * @param allow4Char will shorten hex value to 4 char if possible
     */
    toHex8(allow4Char = false) {
      return rgbaToHex(this.r, this.g, this.b, this.a, allow4Char);
    }
    /**
     * Returns the hex 8 value of the color -with a # prefixed.
     * @param allow4Char will shorten hex value to 4 char if possible
     */
    toHex8String(allow4Char = false) {
      return "#" + this.toHex8(allow4Char);
    }
    /**
     * Returns the shorter hex value of the color depends on its alpha -with a # prefixed.
     * @param allowShortChar will shorten hex value to 3 or 4 char if possible
     */
    toHexShortString(allowShortChar = false) {
      return this.a === 1 ? this.toHexString(allowShortChar) : this.toHex8String(allowShortChar);
    }
    /**
     * Returns the object as a RGBA object.
     */
    toRgb() {
      return {
        r: Math.round(this.r),
        g: Math.round(this.g),
        b: Math.round(this.b),
        a: this.a
      };
    }
    /**
     * Returns the RGBA values interpolated into a string with the following format:
     * "RGBA(xxx, xxx, xxx, xx)".
     */
    toRgbString() {
      const r9 = Math.round(this.r);
      const g2 = Math.round(this.g);
      const b3 = Math.round(this.b);
      return this.a === 1 ? `rgb(${r9}, ${g2}, ${b3})` : `rgba(${r9}, ${g2}, ${b3}, ${this.roundA})`;
    }
    /**
     * Returns the object as a RGBA object.
     */
    toPercentageRgb() {
      const fmt = (x2) => `${Math.round(bound01(x2, 255) * 100)}%`;
      return {
        r: fmt(this.r),
        g: fmt(this.g),
        b: fmt(this.b),
        a: this.a
      };
    }
    /**
     * Returns the RGBA relative values interpolated into a string
     */
    toPercentageRgbString() {
      const rnd = (x2) => Math.round(bound01(x2, 255) * 100);
      return this.a === 1 ? `rgb(${rnd(this.r)}%, ${rnd(this.g)}%, ${rnd(this.b)}%)` : `rgba(${rnd(this.r)}%, ${rnd(this.g)}%, ${rnd(this.b)}%, ${this.roundA})`;
    }
    toCmyk() {
      return {
        ...rgbToCmyk(this.r, this.g, this.b)
      };
    }
    toCmykString() {
      const { c: c6, m: m3, y: y3, k: k2 } = rgbToCmyk(this.r, this.g, this.b);
      return `cmyk(${c6}, ${m3}, ${y3}, ${k2})`;
    }
    /**
     * The 'real' name of the color -if there is one.
     */
    toName() {
      if (this.a === 0) {
        return "transparent";
      }
      if (this.a < 1) {
        return false;
      }
      const hex = "#" + rgbToHex(this.r, this.g, this.b, false);
      for (const [key, value] of Object.entries(names)) {
        if (hex === value) {
          return key;
        }
      }
      return false;
    }
    toString(format) {
      const formatSet = Boolean(format);
      format = format ?? this.format;
      let formattedString = false;
      const hasAlpha = this.a < 1 && this.a >= 0;
      const needsAlphaFormat = !formatSet && hasAlpha && (format.startsWith("hex") || format === "name");
      if (needsAlphaFormat) {
        if (format === "name" && this.a === 0) {
          return this.toName();
        }
        return this.toRgbString();
      }
      if (format === "rgb") {
        formattedString = this.toRgbString();
      }
      if (format === "prgb") {
        formattedString = this.toPercentageRgbString();
      }
      if (format === "hex" || format === "hex6") {
        formattedString = this.toHexString();
      }
      if (format === "hex3") {
        formattedString = this.toHexString(true);
      }
      if (format === "hex4") {
        formattedString = this.toHex8String(true);
      }
      if (format === "hex8") {
        formattedString = this.toHex8String();
      }
      if (format === "name") {
        formattedString = this.toName();
      }
      if (format === "hsl") {
        formattedString = this.toHslString();
      }
      if (format === "hsv") {
        formattedString = this.toHsvString();
      }
      if (format === "cmyk") {
        formattedString = this.toCmykString();
      }
      return formattedString || this.toHexString();
    }
    toNumber() {
      return (Math.round(this.r) << 16) + (Math.round(this.g) << 8) + Math.round(this.b);
    }
    clone() {
      return new _TinyColor(this.toString());
    }
    /**
     * Lighten the color a given amount. Providing 100 will always return white.
     * @param amount - valid between 1-100
     */
    lighten(amount = 10) {
      const hsl = this.toHsl();
      hsl.l += amount / 100;
      hsl.l = clamp01(hsl.l);
      return new _TinyColor(hsl);
    }
    /**
     * Brighten the color a given amount, from 0 to 100.
     * @param amount - valid between 1-100
     */
    brighten(amount = 10) {
      const rgb = this.toRgb();
      rgb.r = Math.max(0, Math.min(255, rgb.r - Math.round(255 * -(amount / 100))));
      rgb.g = Math.max(0, Math.min(255, rgb.g - Math.round(255 * -(amount / 100))));
      rgb.b = Math.max(0, Math.min(255, rgb.b - Math.round(255 * -(amount / 100))));
      return new _TinyColor(rgb);
    }
    /**
     * Darken the color a given amount, from 0 to 100.
     * Providing 100 will always return black.
     * @param amount - valid between 1-100
     */
    darken(amount = 10) {
      const hsl = this.toHsl();
      hsl.l -= amount / 100;
      hsl.l = clamp01(hsl.l);
      return new _TinyColor(hsl);
    }
    /**
     * Mix the color with pure white, from 0 to 100.
     * Providing 0 will do nothing, providing 100 will always return white.
     * @param amount - valid between 1-100
     */
    tint(amount = 10) {
      return this.mix("white", amount);
    }
    /**
     * Mix the color with pure black, from 0 to 100.
     * Providing 0 will do nothing, providing 100 will always return black.
     * @param amount - valid between 1-100
     */
    shade(amount = 10) {
      return this.mix("black", amount);
    }
    /**
     * Desaturate the color a given amount, from 0 to 100.
     * Providing 100 will is the same as calling greyscale
     * @param amount - valid between 1-100
     */
    desaturate(amount = 10) {
      const hsl = this.toHsl();
      hsl.s -= amount / 100;
      hsl.s = clamp01(hsl.s);
      return new _TinyColor(hsl);
    }
    /**
     * Saturate the color a given amount, from 0 to 100.
     * @param amount - valid between 1-100
     */
    saturate(amount = 10) {
      const hsl = this.toHsl();
      hsl.s += amount / 100;
      hsl.s = clamp01(hsl.s);
      return new _TinyColor(hsl);
    }
    /**
     * Completely desaturates a color into greyscale.
     * Same as calling `desaturate(100)`
     */
    greyscale() {
      return this.desaturate(100);
    }
    /**
     * Spin takes a positive or negative amount within [-360, 360] indicating the change of hue.
     * Values outside of this range will be wrapped into this range.
     */
    spin(amount) {
      const hsl = this.toHsl();
      const hue = (hsl.h + amount) % 360;
      hsl.h = hue < 0 ? 360 + hue : hue;
      return new _TinyColor(hsl);
    }
    /**
     * Mix the current color a given amount with another color, from 0 to 100.
     * 0 means no mixing (return current color).
     */
    mix(color, amount = 50) {
      const rgb1 = this.toRgb();
      const rgb2 = new _TinyColor(color).toRgb();
      const p4 = amount / 100;
      const rgba = {
        r: (rgb2.r - rgb1.r) * p4 + rgb1.r,
        g: (rgb2.g - rgb1.g) * p4 + rgb1.g,
        b: (rgb2.b - rgb1.b) * p4 + rgb1.b,
        a: (rgb2.a - rgb1.a) * p4 + rgb1.a
      };
      return new _TinyColor(rgba);
    }
    analogous(results = 6, slices = 30) {
      const hsl = this.toHsl();
      const part = 360 / slices;
      const ret = [this];
      for (hsl.h = (hsl.h - (part * results >> 1) + 720) % 360; --results; ) {
        hsl.h = (hsl.h + part) % 360;
        ret.push(new _TinyColor(hsl));
      }
      return ret;
    }
    /**
     * taken from https://github.com/infusion/jQuery-xcolor/blob/master/jquery.xcolor.js
     */
    complement() {
      const hsl = this.toHsl();
      hsl.h = (hsl.h + 180) % 360;
      return new _TinyColor(hsl);
    }
    monochromatic(results = 6) {
      const hsv = this.toHsv();
      const { h: h3 } = hsv;
      const { s: s7 } = hsv;
      let { v: v2 } = hsv;
      const res = [];
      const modification = 1 / results;
      while (results--) {
        res.push(new _TinyColor({ h: h3, s: s7, v: v2 }));
        v2 = (v2 + modification) % 1;
      }
      return res;
    }
    splitcomplement() {
      const hsl = this.toHsl();
      const { h: h3 } = hsl;
      return [
        this,
        new _TinyColor({ h: (h3 + 72) % 360, s: hsl.s, l: hsl.l }),
        new _TinyColor({ h: (h3 + 216) % 360, s: hsl.s, l: hsl.l })
      ];
    }
    /**
     * Compute how the color would appear on a background
     */
    onBackground(background) {
      const fg = this.toRgb();
      const bg = new _TinyColor(background).toRgb();
      const alpha = fg.a + bg.a * (1 - fg.a);
      return new _TinyColor({
        r: (fg.r * fg.a + bg.r * bg.a * (1 - fg.a)) / alpha,
        g: (fg.g * fg.a + bg.g * bg.a * (1 - fg.a)) / alpha,
        b: (fg.b * fg.a + bg.b * bg.a * (1 - fg.a)) / alpha,
        a: alpha
      });
    }
    /**
     * Alias for `polyad(3)`
     */
    triad() {
      return this.polyad(3);
    }
    /**
     * Alias for `polyad(4)`
     */
    tetrad() {
      return this.polyad(4);
    }
    /**
     * Get polyad colors, like (for 1, 2, 3, 4, 5, 6, 7, 8, etc...)
     * monad, dyad, triad, tetrad, pentad, hexad, heptad, octad, etc...
     */
    polyad(n9) {
      const hsl = this.toHsl();
      const { h: h3 } = hsl;
      const result = [this];
      const increment = 360 / n9;
      for (let i10 = 1; i10 < n9; i10++) {
        result.push(new _TinyColor({ h: (h3 + i10 * increment) % 360, s: hsl.s, l: hsl.l }));
      }
      return result;
    }
    /**
     * compare color vs current color
     */
    equals(color) {
      const comparedColor = new _TinyColor(color);
      if (this.format === "cmyk" || comparedColor.format === "cmyk") {
        return this.toCmykString() === comparedColor.toCmykString();
      }
      return this.toRgbString() === comparedColor.toRgbString();
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.GAOBQZI3.js
  var CsColorPicker = class extends CornerstoneFormAssociatedElement {
    constructor() {
      super();
      this.hasSlotController = new HasSlotController(this, "hint", "label");
      this.isSafeValue = false;
      this.localize = new LocalizeController2(this);
      this.hasFocus = false;
      this.isDraggingGridHandle = false;
      this.inputValue = "";
      this.hue = 0;
      this.isEmpty = true;
      this.saturation = 100;
      this.brightness = 100;
      this.alpha = 100;
      this._value = null;
      this.defaultValue = this.getAttribute("value") || null;
      this.ssrLabel = false;
      this.ssrHint = false;
      this.hasEyeDropper = false;
      this.label = "";
      this.hint = "";
      this.format = "hex";
      this.size = "m";
      this.placement = "bottom-start";
      this.withoutFormatToggle = false;
      this.name = null;
      this.disabled = false;
      this.open = false;
      this.opacity = false;
      this.uppercase = false;
      this.swatches = "";
      this.required = false;
      this.handleFocusIn = () => {
        this.hasFocus = true;
      };
      this.handleFocusOut = () => {
        this.hasFocus = false;
      };
      this.reportValidityAfterShow = () => {
        this.removeEventListener("invalid", this.emitInvalid);
        this.reportValidity();
        this.addEventListener("invalid", this.emitInvalid);
      };
      this.handleKeyDown = (event) => {
        if (this.open && event.key === "Escape" && isTopDismissible(this)) {
          event.stopPropagation();
          this.hide();
          this.focus();
        }
      };
      this.handleDocumentKeyDown = (event) => {
        if (event.key === "Escape" && this.open && isTopDismissible(this)) {
          event.stopPropagation();
          this.focus();
          this.hide();
          return;
        }
        if (event.key === "Tab") {
          setTimeout(() => {
            const activeElement = this.getRootNode() instanceof ShadowRoot ? document.activeElement?.shadowRoot?.activeElement : document.activeElement;
            if (activeElement?.closest(this.tagName.toLowerCase()) !== this) {
              this.hide();
            }
          });
        }
      };
      this.handleDocumentMouseDown = (event) => {
        const path = event.composedPath();
        const isInsideRelevantArea = path.some(
          (element) => element instanceof Element && (element.closest(".color-picker") || element === this.trigger)
        );
        if (!isInsideRelevantArea) {
          this.hide();
        }
      };
      if (!o5) {
        this.addEventListener("focusin", this.handleFocusIn);
        this.addEventListener("focusout", this.handleFocusOut);
      }
      this.opacity = this.hasAttribute("opacity");
      this.uppercase = this.hasAttribute("uppercase");
      const format = this.getAttribute("format");
      if (format === "rgb" || format === "hsl" || format === "hsv") {
        this.format = format;
      }
      this.handleValueChange("", this.value || "");
    }
    static get validators() {
      return [...super.validators, RequiredValidator()];
    }
    // @TODO: This is a hacky way to show the "Please fill out this field", do we want the old behavior where it opens the dropdown?
    //   or is the new behavior okay?
    get validationTarget() {
      if (this.popup?.active) {
        return this.input;
      }
      return this.trigger;
    }
    /** The current value of the input, submitted as a name/value pair with form data. */
    get value() {
      if (this.valueHasChanged) {
        return this._value;
      }
      return this._value ?? this.defaultValue;
    }
    set value(val) {
      if (this._value === val) {
        return;
      }
      this.valueHasChanged = true;
      this._value = val;
    }
    /**
     * @internal
     */
    updateFormValue(value) {
      if (value == null) {
        this.setValue("", null);
        return;
      }
      super.updateFormValue(value);
    }
    handleCopy() {
      this.input.select();
      document.execCommand("copy");
      this.previewButton.focus();
      this.previewButton.classList.add("preview-color-copied");
      this.previewButton.addEventListener("animationend", () => {
        this.previewButton.classList.remove("preview-color-copied");
      });
    }
    handleFormatToggle() {
      const formats = ["hex", "rgb", "hsl", "hsv"];
      const nextIndex = (formats.indexOf(this.format) + 1) % formats.length;
      this.format = formats[nextIndex];
      this.setColor(this.value || "");
      this.updateComplete.then(() => {
        this.dispatchEvent(new Event("change", { bubbles: true, composed: true }));
        this.dispatchEvent(new InputEvent("input", { bubbles: true, composed: true }));
      });
    }
    handleAlphaDrag(event) {
      const container = this.shadowRoot.querySelector(".slider.alpha");
      const handle = container.querySelector(".slider-handle");
      const { width } = container.getBoundingClientRect();
      let initialValue = this.value;
      let currentValue = this.value;
      handle.focus();
      event.preventDefault();
      drag(container, {
        onMove: (x2) => {
          this.alpha = clamp(x2 / width * 100, 0, 100);
          this.syncValues();
          if (this.value !== currentValue) {
            currentValue = this.value;
            this.updateComplete.then(() => {
              this.dispatchEvent(new InputEvent("input", { bubbles: true, composed: true }));
            });
          }
        },
        onStop: () => {
          if (this.value !== initialValue) {
            initialValue = this.value;
            this.updateComplete.then(() => {
              this.dispatchEvent(new Event("change", { bubbles: true, composed: true }));
            });
          }
        },
        initialEvent: event
      });
    }
    handleHueDrag(event) {
      const container = this.shadowRoot.querySelector(".slider.hue");
      const handle = container.querySelector(".slider-handle");
      const { width } = container.getBoundingClientRect();
      let initialValue = this.value;
      let currentValue = this.value;
      handle.focus();
      event.preventDefault();
      drag(container, {
        onMove: (x2) => {
          this.hue = clamp(x2 / width * 360, 0, 360);
          this.syncValues();
          if (this.value !== currentValue) {
            currentValue = this.value;
            this.updateComplete.then(() => {
              this.dispatchEvent(new InputEvent("input"));
            });
          }
        },
        onStop: () => {
          if (this.value !== initialValue) {
            initialValue = this.value;
            this.updateComplete.then(() => {
              this.dispatchEvent(new Event("change", { bubbles: true, composed: true }));
            });
          }
        },
        initialEvent: event
      });
    }
    handleGridDrag(event) {
      const grid = this.shadowRoot.querySelector(".grid");
      const handle = grid.querySelector(".grid-handle");
      const { width, height } = grid.getBoundingClientRect();
      let initialValue = this.value;
      let currentValue = this.value;
      handle.focus();
      event.preventDefault();
      this.isDraggingGridHandle = true;
      drag(grid, {
        onMove: (x2, y3) => {
          this.saturation = clamp(x2 / width * 100, 0, 100);
          this.brightness = clamp(100 - y3 / height * 100, 0, 100);
          this.syncValues();
          if (this.value !== currentValue) {
            currentValue = this.value;
            this.updateComplete.then(() => {
              this.dispatchEvent(new InputEvent("input", { bubbles: true, composed: true }));
            });
          }
        },
        onStop: () => {
          this.isDraggingGridHandle = false;
          if (this.value !== initialValue) {
            initialValue = this.value;
            this.updateComplete.then(() => {
              this.dispatchEvent(new Event("change", { bubbles: true, composed: true }));
            });
          }
        },
        initialEvent: event
      });
    }
    handleAlphaKeyDown(event) {
      const increment = event.shiftKey ? 10 : 1;
      const oldValue = this.value;
      if (event.key === "ArrowLeft") {
        event.preventDefault();
        this.alpha = clamp(this.alpha - increment, 0, 100);
        this.syncValues();
      }
      if (event.key === "ArrowRight") {
        event.preventDefault();
        this.alpha = clamp(this.alpha + increment, 0, 100);
        this.syncValues();
      }
      if (event.key === "Home") {
        event.preventDefault();
        this.alpha = 0;
        this.syncValues();
      }
      if (event.key === "End") {
        event.preventDefault();
        this.alpha = 100;
        this.syncValues();
      }
      if (this.value !== oldValue) {
        this.updateComplete.then(() => {
          this.dispatchEvent(new InputEvent("input", { bubbles: true, composed: true }));
          this.dispatchEvent(new Event("change", { bubbles: true, composed: true }));
        });
      }
    }
    handleHueKeyDown(event) {
      const increment = event.shiftKey ? 10 : 1;
      const oldValue = this.value;
      if (event.key === "ArrowLeft") {
        event.preventDefault();
        this.hue = clamp(this.hue - increment, 0, 360);
        this.syncValues();
      }
      if (event.key === "ArrowRight") {
        event.preventDefault();
        this.hue = clamp(this.hue + increment, 0, 360);
        this.syncValues();
      }
      if (event.key === "Home") {
        event.preventDefault();
        this.hue = 0;
        this.syncValues();
      }
      if (event.key === "End") {
        event.preventDefault();
        this.hue = 360;
        this.syncValues();
      }
      if (this.value !== oldValue) {
        this.updateComplete.then(() => {
          this.dispatchEvent(new InputEvent("input", { bubbles: true, composed: true }));
          this.dispatchEvent(new Event("change", { bubbles: true, composed: true }));
        });
      }
    }
    handleGridKeyDown(event) {
      const increment = event.shiftKey ? 10 : 1;
      const oldValue = this.value;
      if (event.key === "ArrowLeft") {
        event.preventDefault();
        this.saturation = clamp(this.saturation - increment, 0, 100);
        this.syncValues();
      }
      if (event.key === "ArrowRight") {
        event.preventDefault();
        this.saturation = clamp(this.saturation + increment, 0, 100);
        this.syncValues();
      }
      if (event.key === "ArrowUp") {
        event.preventDefault();
        this.brightness = clamp(this.brightness + increment, 0, 100);
        this.syncValues();
      }
      if (event.key === "ArrowDown") {
        event.preventDefault();
        this.brightness = clamp(this.brightness - increment, 0, 100);
        this.syncValues();
      }
      if (this.value !== oldValue) {
        this.updateComplete.then(() => {
          this.dispatchEvent(new InputEvent("input", { bubbles: true, composed: true }));
          this.dispatchEvent(new Event("change", { bubbles: true, composed: true }));
        });
      }
    }
    handleInputChange(event) {
      const target = event.target;
      const oldValue = this.value;
      event.stopPropagation();
      if (this.input.value) {
        this.setColor(target.value);
        target.value = this.value || "";
      } else {
        this.value = "";
      }
      if (this.value !== oldValue) {
        this.updateComplete.then(() => {
          this.dispatchEvent(new InputEvent("input", { bubbles: true, composed: true }));
          this.dispatchEvent(new Event("change", { bubbles: true, composed: true }));
        });
      }
    }
    handleInputInput(event) {
      this.updateValidity();
      event.stopPropagation();
    }
    handleInputKeyDown(event) {
      if (event.key === "Enter") {
        const oldValue = this.value;
        if (this.input.value) {
          this.setColor(this.input.value);
          this.input.value = this.value;
          if (this.value !== oldValue) {
            this.updateComplete.then(() => {
              this.dispatchEvent(new InputEvent("input", { bubbles: true, composed: true }));
              this.dispatchEvent(new Event("change", { bubbles: true, composed: true }));
            });
          }
          setTimeout(() => this.input.select());
        } else {
          this.hue = 0;
        }
      }
    }
    handleTouchMove(event) {
      event.preventDefault();
    }
    parseColor(colorString) {
      if (!colorString || colorString.trim() === "") {
        return null;
      }
      const color = new TinyColor(colorString);
      if (!color.isValid) {
        return null;
      }
      const hslColor = color.toHsl();
      const rgb = color.toRgb();
      const hsvColor = color.toHsv();
      if (!rgb || rgb.r == null || rgb.g == null || rgb.b == null) {
        return null;
      }
      const hsl = {
        h: hslColor.h || 0,
        s: (hslColor.s || 0) * 100,
        l: (hslColor.l || 0) * 100,
        a: hslColor.a || 0
      };
      const hex = color.toHexString();
      const hexa = color.toHex8String();
      const hsv = {
        h: hsvColor.h || 0,
        s: (hsvColor.s || 0) * 100,
        v: (hsvColor.v || 0) * 100,
        a: hsvColor.a || 0
      };
      return {
        hsl: {
          h: hsl.h,
          s: hsl.s,
          l: hsl.l,
          string: this.setLetterCase(`hsl(${Math.round(hsl.h)}, ${Math.round(hsl.s)}%, ${Math.round(hsl.l)}%)`)
        },
        hsla: {
          h: hsl.h,
          s: hsl.s,
          l: hsl.l,
          a: hsl.a,
          string: this.setLetterCase(
            `hsla(${Math.round(hsl.h)}, ${Math.round(hsl.s)}%, ${Math.round(hsl.l)}%, ${hsl.a.toFixed(2).toString()})`
          )
        },
        hsv: {
          h: hsv.h,
          s: hsv.s,
          v: hsv.v,
          string: this.setLetterCase(`hsv(${Math.round(hsv.h)}, ${Math.round(hsv.s)}%, ${Math.round(hsv.v)}%)`)
        },
        hsva: {
          h: hsv.h,
          s: hsv.s,
          v: hsv.v,
          a: hsv.a,
          string: this.setLetterCase(
            `hsva(${Math.round(hsv.h)}, ${Math.round(hsv.s)}%, ${Math.round(hsv.v)}%, ${hsv.a.toFixed(2).toString()})`
          )
        },
        rgb: {
          r: rgb.r,
          g: rgb.g,
          b: rgb.b,
          string: this.setLetterCase(`rgb(${Math.round(rgb.r)}, ${Math.round(rgb.g)}, ${Math.round(rgb.b)})`)
        },
        rgba: {
          r: rgb.r,
          g: rgb.g,
          b: rgb.b,
          a: rgb.a || 0,
          string: this.setLetterCase(
            `rgba(${Math.round(rgb.r)}, ${Math.round(rgb.g)}, ${Math.round(rgb.b)}, ${(rgb.a || 0).toFixed(2).toString()})`
          )
        },
        hex: this.setLetterCase(hex),
        hexa: this.setLetterCase(hexa)
      };
    }
    setColor(colorString) {
      const newColor = this.parseColor(colorString);
      if (newColor === null) {
        return false;
      }
      this.hue = newColor.hsva.h;
      this.saturation = newColor.hsva.s;
      this.brightness = newColor.hsva.v;
      this.alpha = this.opacity ? newColor.hsva.a * 100 : 100;
      this.syncValues();
      return true;
    }
    setLetterCase(string) {
      if (typeof string !== "string") {
        return "";
      }
      return this.uppercase ? string.toUpperCase() : string.toLowerCase();
    }
    async syncValues() {
      const currentColor = this.parseColor(
        `hsva(${this.hue}, ${this.saturation}%, ${this.brightness}%, ${this.alpha / 100})`
      );
      if (currentColor === null) {
        return;
      }
      if (this.format === "hsl") {
        this.inputValue = this.opacity ? currentColor.hsla.string : currentColor.hsl.string;
      } else if (this.format === "rgb") {
        this.inputValue = this.opacity ? currentColor.rgba.string : currentColor.rgb.string;
      } else if (this.format === "hsv") {
        this.inputValue = this.opacity ? currentColor.hsva.string : currentColor.hsv.string;
      } else {
        this.inputValue = this.opacity ? currentColor.hexa : currentColor.hex;
      }
      this.isSafeValue = true;
      this.value = this.inputValue;
      await this.updateComplete;
      this.isSafeValue = false;
    }
    handleAfterHide() {
      this.previewButton.classList.remove("preview-color-copied");
      this.updateValidity();
    }
    handleAfterShow() {
      this.updateValidity();
    }
    handleEyeDropper() {
      if (!this.hasEyeDropper) {
        return;
      }
      const eyeDropper = new EyeDropper();
      eyeDropper.open().then((colorSelectionResult) => {
        const oldValue = this.value;
        this.setColor(colorSelectionResult.sRGBHex);
        if (this.value !== oldValue) {
          this.updateComplete.then(() => {
            this.dispatchEvent(new InputEvent("input", { bubbles: true, composed: true }));
            this.dispatchEvent(new Event("change", { bubbles: true, composed: true }));
          });
        }
      }).catch(() => {
      });
    }
    selectSwatch(color) {
      const oldValue = this.value;
      if (!this.disabled) {
        this.setColor(color);
        if (this.value !== oldValue) {
          this.updateComplete.then(() => {
            this.dispatchEvent(new InputEvent("input", { bubbles: true, composed: true }));
            this.dispatchEvent(new Event("change", { bubbles: true, composed: true }));
          });
        }
      }
    }
    /** Generates a hex string from HSV values. Hue must be 0-360. All other arguments must be 0-100. */
    getHexString(hue, saturation, brightness, alpha = 100) {
      const color = new TinyColor(`hsva(${hue}, ${saturation}%, ${brightness}%, ${alpha / 100})`);
      if (!color.isValid) {
        return "";
      }
      return color.toHex8String();
    }
    // Prevents nested components from leaking events
    stopNestedEventPropagation(event) {
      event.stopImmediatePropagation();
    }
    handleFormatChange() {
      this.syncValues();
    }
    handleOpacityChange() {
      this.alpha = 100;
    }
    willUpdate(changedProperties) {
      if (changedProperties.has("value") || changedProperties.has("defaultValue")) {
        this.handleValueChange(changedProperties.get("value") || "", this.value || "");
      }
      super.willUpdate(changedProperties);
    }
    handleValueChange(oldValue, newValue) {
      this.isEmpty = !newValue;
      if (!newValue) {
        this.hue = 0;
        this.saturation = 0;
        this.brightness = 100;
        this.alpha = 100;
      }
      if (!this.isSafeValue) {
        const newColor = this.parseColor(newValue);
        if (newColor !== null) {
          this.inputValue = this.value || "";
          this.hue = newColor.hsva.h;
          this.saturation = newColor.hsva.s;
          this.brightness = newColor.hsva.v;
          this.alpha = this.opacity ? newColor.hsva.a * 100 : 100;
          this.syncValues();
        } else {
          this.inputValue = oldValue ?? "";
        }
      }
      this.requestUpdate();
    }
    /** Sets focus on the color picker. */
    focus(options2) {
      this.trigger.focus(options2);
    }
    /** Removes focus from the color picker. */
    blur() {
      const elementToBlur = this.trigger;
      if (this.hasFocus) {
        elementToBlur.focus({ preventScroll: true });
        elementToBlur.blur();
      }
      if (this.popup?.active) {
        this.hide();
      }
    }
    /** Returns the current value as a string in the specified format. */
    getFormattedValue(format = "hex") {
      const currentColor = this.parseColor(
        `hsva(${this.hue}, ${this.saturation}%, ${this.brightness}%, ${this.alpha / 100})`
      );
      if (currentColor === null) {
        return "";
      }
      switch (format) {
        case "hex":
          return currentColor.hex;
        case "hexa":
          return currentColor.hexa;
        case "rgb":
          return currentColor.rgb.string;
        case "rgba":
          return currentColor.rgba.string;
        case "hsl":
          return currentColor.hsl.string;
        case "hsla":
          return currentColor.hsla.string;
        case "hsv":
          return currentColor.hsv.string;
        case "hsva":
          return currentColor.hsva.string;
        default:
          return "";
      }
    }
    /** Checks for validity and shows the browser's validation message if the control is invalid. */
    reportValidity() {
      if (!this.validity.valid && !this.open) {
        this.addEventListener("cs-after-show", this.reportValidityAfterShow, { once: true });
        this.show();
        if (!this.disabled) {
          this.dispatchEvent(new CsInvalidEvent());
        }
        return false;
      }
      return super.reportValidity();
    }
    formResetCallback() {
      this.value = this.defaultValue;
      super.formResetCallback();
    }
    firstUpdated(changedProperties) {
      super.firstUpdated(changedProperties);
      this.hasEyeDropper = "EyeDropper" in window;
    }
    handleTriggerClick() {
      if (this.open) {
        this.hide();
      } else {
        this.show();
        this.focus();
      }
    }
    async handleTriggerKeyDown(event) {
      if ([" ", "Enter"].includes(event.key)) {
        event.preventDefault();
        this.handleTriggerClick();
        return;
      }
    }
    handleTriggerKeyUp(event) {
      if (event.key === " ") {
        event.preventDefault();
      }
    }
    updateAccessibleTrigger() {
      const accessibleTrigger = this.trigger;
      if (accessibleTrigger) {
        accessibleTrigger.setAttribute("aria-haspopup", "true");
        accessibleTrigger.setAttribute("aria-expanded", this.open ? "true" : "false");
      }
    }
    /** Shows the color picker panel. */
    async show() {
      if (this.open) {
        return void 0;
      }
      this.open = true;
      return waitForEvent(this, "cs-after-show");
    }
    /** Hides the color picker panel */
    async hide() {
      if (!this.open) {
        return void 0;
      }
      this.open = false;
      return waitForEvent(this, "cs-after-hide");
    }
    addOpenListeners() {
      this.base.addEventListener("keydown", this.handleKeyDown);
      document.addEventListener("keydown", this.handleDocumentKeyDown);
      document.addEventListener("mousedown", this.handleDocumentMouseDown);
      registerDismissible(this);
    }
    removeOpenListeners() {
      if (this.base) {
        this.base.removeEventListener("keydown", this.handleKeyDown);
      }
      document.removeEventListener("keydown", this.handleDocumentKeyDown);
      document.removeEventListener("mousedown", this.handleDocumentMouseDown);
      unregisterDismissible(this);
    }
    async handleOpenChange() {
      if (this.disabled) {
        this.open = false;
        return;
      }
      this.updateAccessibleTrigger();
      if (this.open) {
        this.dispatchEvent(new CustomEvent("cs-show"));
        this.addOpenListeners();
        await this.updateComplete;
        this.base.hidden = false;
        this.popup.active = true;
        await animateWithClass(this.popup.popup, "show-with-scale");
        this.dispatchEvent(new CustomEvent("cs-after-show"));
      } else {
        this.dispatchEvent(new CustomEvent("cs-hide"));
        this.removeOpenListeners();
        await animateWithClass(this.popup.popup, "hide-with-scale");
        this.base.hidden = true;
        this.popup.active = false;
        this.dispatchEvent(new CustomEvent("cs-after-hide"));
      }
    }
    render() {
      const isEmpty = this.isEmpty;
      const hasLabelSlot = this.hasSlotController.test("label", "ssrLabel");
      const hasHintSlot = this.hasSlotController.test("hint", "ssrHint");
      const hasLabel = this.label ? true : !!hasLabelSlot;
      const hasHint = this.hint ? true : !!hasHintSlot;
      const gridHandleX = this.saturation;
      const gridHandleY = 100 - this.brightness;
      const normalizedSwatches = Array.isArray(this.swatches) ? this.swatches.map((s7) => typeof s7 === "string" ? { color: s7, label: s7 } : s7) : this.swatches.split(";").filter((color) => color.trim() !== "").map((color) => ({ color: color.trim(), label: color.trim() }));
      const colorPicker = b2`
      <div
        part="color-picker"
        class=${e7({
        "color-picker": true
      })}
        aria-disabled=${this.disabled ? "true" : "false"}
        tabindex="-1"
      >
        <div
          part="grid"
          class="grid"
          style=${o7({ backgroundColor: this.getHexString(this.hue, 100, 100) })}
          @pointerdown=${this.handleGridDrag}
          @touchmove=${this.handleTouchMove}
        >
          <span
            part="grid-handle"
            class=${e7({
        "grid-handle": true,
        "grid-handle-dragging": this.isDraggingGridHandle
      })}
            style=${o7({
        top: `${gridHandleY}%`,
        left: `${gridHandleX}%`,
        backgroundColor: this.getHexString(this.hue, this.saturation, this.brightness, this.alpha)
      })}
            role="application"
            aria-label="HSV"
            tabindex=${o8(this.disabled ? void 0 : "0")}
            @keydown=${this.handleGridKeyDown}
          ></span>
        </div>

        <div class="controls">
          <div class="sliders">
            <div
              part="slider hue-slider"
              class="hue slider"
              @pointerdown=${this.handleHueDrag}
              @touchmove=${this.handleTouchMove}
            >
              <span
                part="slider-handle hue-slider-handle"
                class="slider-handle"
                style=${o7({
        left: `${this.hue === 0 ? 0 : 100 / (360 / this.hue)}%`,
        backgroundColor: this.getHexString(this.hue, 100, 100)
      })}
                role="slider"
                aria-label="hue"
                aria-orientation="horizontal"
                aria-valuemin="0"
                aria-valuemax="360"
                aria-valuenow=${`${Math.round(this.hue)}`}
                tabindex=${o8(this.disabled ? void 0 : "0")}
                @keydown=${this.handleHueKeyDown}
              ></span>
            </div>

            ${this.opacity ? b2`
                    <div
                      part="slider opacity-slider"
                      class="alpha slider transparent-bg"
                      @pointerdown="${this.handleAlphaDrag}"
                      @touchmove=${this.handleTouchMove}
                    >
                      <div
                        class="alpha-gradient"
                        style=${o7({
        backgroundImage: `linear-gradient(
                          to right,
                          ${this.getHexString(this.hue, this.saturation, this.brightness, 0)} 0%,
                          ${this.getHexString(this.hue, this.saturation, this.brightness, 100)} 100%
                        )`
      })}
                      ></div>
                      <span
                        part="slider-handle opacity-slider-handle"
                        class="slider-handle"
                        style=${o7({
        left: `${this.alpha}%`,
        backgroundColor: this.getHexString(this.hue, this.saturation, this.brightness, this.alpha)
      })}
                        role="slider"
                        aria-label="alpha"
                        aria-orientation="horizontal"
                        aria-valuemin="0"
                        aria-valuemax="100"
                        aria-valuenow=${Math.round(this.alpha)}
                        tabindex=${o8(this.disabled ? void 0 : "0")}
                        @keydown=${this.handleAlphaKeyDown}
                      ></span>
                    </div>
                  ` : ""}
          </div>

          <button
            type="button"
            part="preview"
            class="preview transparent-bg"
            aria-label=${this.localize.term("copy")}
            style=${o7({
        "--preview-color": this.getHexString(this.hue, this.saturation, this.brightness, this.alpha)
      })}
            @click=${this.handleCopy}
          ></button>
        </div>

        <div class="user-input" aria-live="polite">
          <cs-input
            part="input"
            type="text"
            name=${this.name}
            size="s"
            autocomplete="off"
            autocorrect="off"
            autocapitalize="off"
            spellcheck="false"
            .value=${isEmpty ? "" : this.inputValue}
            value=${isEmpty ? "" : this.inputValue}
            ?required=${this.required}
            ?disabled=${this.disabled}
            aria-label=${this.localize.term("currentValue")}
            @keydown=${this.handleInputKeyDown}
            @change=${this.handleInputChange}
            @input=${this.handleInputInput}
            @blur=${this.stopNestedEventPropagation}
            @focus=${this.stopNestedEventPropagation}
          ></cs-input>

          <cs-button-group>
            ${!this.withoutFormatToggle ? b2`
                    <cs-button
                      part="format-button"
                      size="s"
                      appearance="outlined"
                      aria-label=${this.localize.term("toggleColorFormat")}
                      exportparts="
                      button:format-button__button,
                      start:format-button__start,
                      label:format-button__label,
                      end:format-button__end,
                      caret:format-button__caret
                    "
                      @click=${this.handleFormatToggle}
                      @blur=${this.stopNestedEventPropagation}
                      @focus=${this.stopNestedEventPropagation}
                    >
                      ${this.setLetterCase(this.format)}
                    </cs-button>
                  ` : ""}
            ${this.hasEyeDropper ? b2`
                    <cs-button
                      part="eyedropper-button"
                      size="s"
                      appearance="outlined"
                      exportparts="
                      button:eyedropper-button__button,
                      start:eyedropper-button__start,
                      label:eyedropper-button__label,
                      end:eyedropper-button__end,
                      caret:eyedropper-button__caret
                    "
                      @click=${this.handleEyeDropper}
                      @blur=${this.stopNestedEventPropagation}
                      @focus=${this.stopNestedEventPropagation}
                    >
                      <cs-icon
                        library="system"
                        name="colorize"
                        label=${this.localize.term("selectAColorFromTheScreen")}
                      ></cs-icon>
                    </cs-button>
                  ` : ""}
          </cs-button-group>
        </div>

        ${normalizedSwatches.length > 0 ? b2`
                <div part="swatches" class="swatches">
                  ${normalizedSwatches.map((swatch) => {
        const parsedColor = this.parseColor(swatch.color);
        if (!parsedColor) {
          return "";
        }
        return b2`
                      <div
                        part="swatch"
                        class="swatch transparent-bg"
                        tabindex=${o8(this.disabled ? void 0 : "0")}
                        role="button"
                        aria-label=${swatch.label}
                        @click=${() => this.selectSwatch(swatch.color)}
                        @keydown=${(event) => {
          if (event.key === "Enter" || event.key === " ") {
            event.preventDefault();
            this.selectSwatch(swatch.color);
          }
        }}
                      >
                        <div class="swatch-color" style=${o7({ backgroundColor: parsedColor.hexa })}></div>
                      </div>
                    `;
      })}
                </div>
              ` : ""}
      </div>
    `;
      return b2`
      <div
        class=${e7({
        container: true,
        "form-control": true,
        "form-control-has-label": hasLabel
      })}
        part="trigger-container form-control"
      >
        <div
          part="form-control-label"
          class=${e7({
        label: true,
        "has-label": hasLabel
      })}
          id="form-control-label"
        >
          <slot name="label">${this.label}</slot>
        </div>

        <button
          id="trigger"
          part="trigger form-control-input"
          class=${e7({
        trigger: true,
        "trigger-empty": isEmpty,
        "transparent-bg": true,
        "form-control-input": true
      })}
          style=${o7({
        color: this.getHexString(this.hue, this.saturation, this.brightness, this.alpha)
      })}
          type="button"
          aria-labelledby="form-control-label"
          aria-describedby="hint"
          .disabled=${this.disabled}
          @click=${this.handleTriggerClick}
          @keydown=${this.handleTriggerKeyDown}
          @keyup=${this.handleTriggerKeyUp}
        ></button>

        <slot
          id="hint"
          name="hint"
          part="hint"
          class=${e7({
        "has-slotted": hasHint
      })}
          >${this.hint}</slot
        >
      </div>

      <cs-popup
        class="color-popup"
        anchor="trigger"
        placement=${this.placement}
        distance="0"
        skidding="0"
        flip
        flip-fallback-strategy="best-fit"
        shift
        shift-padding="10"
        aria-disabled=${this.disabled ? "true" : "false"}
        @cs-after-show=${this.handleAfterShow}
        @cs-after-hide=${this.handleAfterHide}
      >
        ${colorPicker}
      </cs-popup>
    `;
    }
  };
  CsColorPicker.css = [visually_hidden_styles_default, size_styles_default, form_control_styles_default, color_picker_styles_default];
  CsColorPicker.shadowRootOptions = { ...CornerstoneFormAssociatedElement.shadowRootOptions, delegatesFocus: true };
  __decorateClass([
    e5('[part~="color-picker"]')
  ], CsColorPicker.prototype, "base", 2);
  __decorateClass([
    e5('[part~="input"]')
  ], CsColorPicker.prototype, "input", 2);
  __decorateClass([
    e5('[part~="form-control-label"]')
  ], CsColorPicker.prototype, "triggerLabel", 2);
  __decorateClass([
    e5('[part~="form-control-input"]')
  ], CsColorPicker.prototype, "triggerButton", 2);
  __decorateClass([
    e5(".color-popup")
  ], CsColorPicker.prototype, "popup", 2);
  __decorateClass([
    e5('[part~="preview"]')
  ], CsColorPicker.prototype, "previewButton", 2);
  __decorateClass([
    e5('[part~="trigger"]')
  ], CsColorPicker.prototype, "trigger", 2);
  __decorateClass([
    r5()
  ], CsColorPicker.prototype, "hasFocus", 2);
  __decorateClass([
    r5()
  ], CsColorPicker.prototype, "isDraggingGridHandle", 2);
  __decorateClass([
    r5()
  ], CsColorPicker.prototype, "inputValue", 2);
  __decorateClass([
    r5()
  ], CsColorPicker.prototype, "hue", 2);
  __decorateClass([
    r5()
  ], CsColorPicker.prototype, "isEmpty", 2);
  __decorateClass([
    r5()
  ], CsColorPicker.prototype, "saturation", 2);
  __decorateClass([
    r5()
  ], CsColorPicker.prototype, "brightness", 2);
  __decorateClass([
    r5()
  ], CsColorPicker.prototype, "alpha", 2);
  __decorateClass([
    r5()
  ], CsColorPicker.prototype, "value", 1);
  __decorateClass([
    n4({ attribute: "value", reflect: true })
  ], CsColorPicker.prototype, "defaultValue", 2);
  __decorateClass([
    n4({ attribute: "ssr-label", reflect: true, type: Boolean })
  ], CsColorPicker.prototype, "ssrLabel", 2);
  __decorateClass([
    n4({ attribute: "ssr-hint", reflect: true, type: Boolean })
  ], CsColorPicker.prototype, "ssrHint", 2);
  __decorateClass([
    r5()
  ], CsColorPicker.prototype, "hasEyeDropper", 2);
  __decorateClass([
    n4()
  ], CsColorPicker.prototype, "label", 2);
  __decorateClass([
    n4({ attribute: "hint" })
  ], CsColorPicker.prototype, "hint", 2);
  __decorateClass([
    n4()
  ], CsColorPicker.prototype, "format", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsColorPicker.prototype, "size", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsColorPicker.prototype, "placement", 2);
  __decorateClass([
    n4({ attribute: "without-format-toggle", type: Boolean })
  ], CsColorPicker.prototype, "withoutFormatToggle", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsColorPicker.prototype, "name", 2);
  __decorateClass([
    n4({ type: Boolean })
  ], CsColorPicker.prototype, "disabled", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsColorPicker.prototype, "open", 2);
  __decorateClass([
    n4({ type: Boolean })
  ], CsColorPicker.prototype, "opacity", 2);
  __decorateClass([
    n4({ type: Boolean })
  ], CsColorPicker.prototype, "uppercase", 2);
  __decorateClass([
    n4()
  ], CsColorPicker.prototype, "swatches", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsColorPicker.prototype, "required", 2);
  __decorateClass([
    t4({ passive: false })
  ], CsColorPicker.prototype, "handleTouchMove", 1);
  __decorateClass([
    watch("format", { waitUntilFirstUpdate: true })
  ], CsColorPicker.prototype, "handleFormatChange", 1);
  __decorateClass([
    watch("opacity", { waitUntilFirstUpdate: true })
  ], CsColorPicker.prototype, "handleOpacityChange", 1);
  __decorateClass([
    watch("value")
  ], CsColorPicker.prototype, "handleValueChange", 1);
  __decorateClass([
    watch("open", { waitUntilFirstUpdate: true })
  ], CsColorPicker.prototype, "handleOpenChange", 1);
  CsColorPicker = __decorateClass([
    customElement("cs-color-picker")
  ], CsColorPicker);
  CsColorPicker.disableWarning?.("change-in-update");

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.NLPSVSUU.js
  var CsClearEvent = class extends Event {
    constructor() {
      super("cs-clear", { bubbles: true, cancelable: false, composed: true });
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.I3GI4VWI.js
  function submitOnEnter(event, el) {
    const hasModifier = event.metaKey || event.ctrlKey || event.shiftKey || event.altKey;
    if (event.key === "Enter" && !hasModifier) {
      setTimeout(() => {
        if (!event.defaultPrevented && !event.isComposing) {
          submitForm(el);
        }
      });
    }
  }
  function submitForm(el) {
    let form = null;
    if ("form" in el) {
      form = el.form;
    }
    if (!form && "getForm" in el) {
      form = el.getForm();
    }
    if (!form) {
      return;
    }
    const formElements = [...form.elements];
    if (formElements.length === 1) {
      form.requestSubmit(null);
      return;
    }
    const button = formElements.find((el2) => el2.type === "submit" && !el2.matches(":disabled"));
    if (!button) {
      return;
    }
    if (["input", "button"].includes(button.localName)) {
      form.requestSubmit(button);
    } else {
      button.click();
    }
  }

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.JMP3G2VM.js
  var input_styles_default = i`
  :host {
    border-width: 0;
  }

  :host(:focus) {
    outline: none;
  }

  .text-field {
    display: flex;
    align-items: stretch;
    justify-content: start;
    position: relative;
    transition: inherit;
    height: var(--cs-form-control-height);
    border-color: var(--cs-form-control-border-color);
    border-radius: var(--cs-form-control-border-radius);
    border-style: var(--cs-form-control-border-style);
    border-width: var(--cs-form-control-border-width);
    cursor: text;
    color: var(--cs-form-control-value-color);
    font-size: inherit;
    font-family: inherit;
    font-weight: var(--cs-form-control-value-font-weight);
    line-height: var(--cs-form-control-value-line-height);
    vertical-align: middle;
    width: 100%;
    transition:
      background-color var(--cs-transition-normal),
      border-color var(--cs-transition-normal),
      outline-color var(--cs-transition-fast);
    transition-timing-function: var(--cs-transition-easing);
    background-color: var(--cs-form-control-background-color);
    box-shadow: var(--box-shadow);
    padding: 0 var(--cs-form-control-padding-inline);
    outline: var(--cs-focus-ring-style) var(--cs-focus-ring-width) transparent;
    outline-offset: var(--cs-focus-ring-offset);

    &:focus-within {
      outline-color: var(--cs-color-focus);
    }

    /* Style disabled inputs */
    &:has(:disabled) {
      cursor: not-allowed;
      opacity: 0.5;
    }
  }

  /* Appearance modifiers */
  :host([appearance='outlined']) .text-field {
    background-color: var(--cs-form-control-background-color);
    border-color: var(--cs-form-control-border-color);
  }

  :host([appearance='filled']) .text-field {
    background-color: var(--cs-color-neutral-fill-quiet);
    border-color: var(--cs-color-neutral-fill-quiet);
  }

  :host([appearance='filled-outlined']) .text-field {
    background-color: var(--cs-color-neutral-fill-quiet);
    border-color: var(--cs-form-control-border-color);
  }

  :host([pill]) .text-field {
    border-radius: var(--cs-border-radius-pill) !important;
  }

  .text-field {
    /* Show autofill styles over the entire text field, not just the native <input> */
    &:has(:autofill),
    &:has(:-webkit-autofill) {
      background-color: var(--cs-color-brand-fill-quiet) !important;
    }

    input,
    textarea {
      /*
      Fixes an alignment issue with placeholders.
      https://github.com/shoelace-style/webawesome/issues/342
    */
      height: 100%;

      padding: 0;
      border: none;
      outline: none;
      box-shadow: none;
      margin: 0;
      cursor: inherit;
      -webkit-appearance: none;
      font: inherit;

      /* Turn off Safari's autofill styles */
      &:-webkit-autofill,
      &:-webkit-autofill:hover,
      &:-webkit-autofill:focus,
      &:-webkit-autofill:active {
        -webkit-background-clip: text;
        background-color: transparent;
        -webkit-text-fill-color: inherit;
      }
    }
  }

  input {
    flex: 1 1 auto;
    min-width: 0;
    height: 100%;
    transition: inherit;

    /* prettier-ignore */
    background-color: rgb(118 118 118 / 0); /* ensures proper placeholder styles in webkit's date input */
    height: calc(var(--cs-form-control-height) - var(--border-width) * 2);
    padding-block: 0;
    color: inherit;

    &:autofill {
      &,
      &:hover,
      &:focus,
      &:active {
        box-shadow: none;
        caret-color: var(--cs-form-control-value-color);
      }
    }

    &::placeholder {
      color: var(--cs-form-control-placeholder-color);
      user-select: none;
      -webkit-user-select: none;
    }

    &::-webkit-search-decoration,
    &::-webkit-search-cancel-button,
    &::-webkit-search-results-button,
    &::-webkit-search-results-decoration {
      -webkit-appearance: none;
    }

    &:focus {
      outline: none;
    }
  }

  textarea {
    &:autofill {
      &,
      &:hover,
      &:focus,
      &:active {
        box-shadow: none;
        caret-color: var(--cs-form-control-value-color);
      }
    }

    &::placeholder {
      color: var(--cs-form-control-placeholder-color);
      user-select: none;
      -webkit-user-select: none;
    }
  }

  .start,
  .end {
    display: inline-flex;
    flex: 0 0 auto;
    align-items: center;
    cursor: default;

    &::slotted(cs-icon) {
      color: var(--cs-color-neutral-on-quiet);
    }
  }

  /* These are !important because a document-level universal margin reset would otherwise beat them:
     a slotted element belongs to the outer tree. See callout.styles.ts for the full note. */
  .start::slotted(*) {
    margin-inline-end: var(--cs-form-control-padding-inline) !important;
  }

  .end::slotted(*) {
    margin-inline-start: var(--cs-form-control-padding-inline) !important;
  }

  /*
   * Clearable + Password Toggle
   */

  .clear,
  .password-toggle {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    font-size: inherit;
    color: var(--cs-color-neutral-on-quiet);
    border: none;
    background: none;
    padding: 0;
    transition: var(--cs-transition-normal) color;
    cursor: pointer;
    margin-inline-start: var(--cs-form-control-padding-inline);

    @media (hover: hover) {
      &:hover {
        color: color-mix(in oklab, currentColor, var(--cs-color-mix-hover));
      }
    }

    &:active {
      color: color-mix(in oklab, currentColor, var(--cs-color-mix-active));
    }

    &:focus {
      outline: none;
    }
  }

  /* Don't show the browser's password toggle in Edge */
  ::-ms-reveal {
    display: none;
  }

  /* Hide the built-in number spinner */
  :host([without-spin-buttons]) input[type='number'] {
    -moz-appearance: textfield;

    &::-webkit-outer-spin-button,
    &::-webkit-inner-spin-button {
      -webkit-appearance: none;
      display: none;
    }
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.MGBHMNHW.js
  var CsInput = class extends CornerstoneFormAssociatedElement {
    constructor() {
      super(...arguments);
      this.assumeInteractionOn = ["blur", "input"];
      this.hasSlotController = new HasSlotController(this, "hint", "label");
      this.localize = new LocalizeController2(this);
      this.title = "";
      this.type = "text";
      this._value = null;
      this.defaultValue = this.getAttribute("value") || null;
      this.size = "m";
      this.appearance = "outlined";
      this.pill = false;
      this.label = "";
      this.hint = "";
      this.withClear = false;
      this.placeholder = "";
      this.readonly = false;
      this.passwordToggle = false;
      this.passwordVisible = false;
      this.withoutSpinButtons = false;
      this.required = false;
      this.spellcheck = true;
      this.ssrLabel = false;
      this.ssrHint = false;
    }
    static get validators() {
      return [...super.validators, MirrorValidator()];
    }
    /** The current value of the input, submitted as a name/value pair with form data. */
    get value() {
      if (this.valueHasChanged) {
        return this._value;
      }
      return this._value ?? this.defaultValue;
    }
    set value(val) {
      if (this._value === val) {
        return;
      }
      this.valueHasChanged = true;
      this._value = val;
    }
    /**
     * @internal
     */
    updateFormValue(value) {
      if (value == null) {
        this.setValue("", null);
        return;
      }
      super.updateFormValue(value);
    }
    handleChange(event) {
      this.value = this.input.value;
      this.relayNativeEvent(event, { bubbles: true, composed: true });
    }
    handleClearClick(event) {
      event.preventDefault();
      if (this.value !== "") {
        this.value = "";
        this.updateComplete.then(() => {
          this.dispatchEvent(new CsClearEvent());
          this.dispatchEvent(new InputEvent("input", { bubbles: true, composed: true }));
          this.dispatchEvent(new Event("change", { bubbles: true, composed: true }));
        });
      }
      this.input.focus();
    }
    handleInput() {
      this.value = this.input.value;
    }
    handleKeyDown(event) {
      submitOnEnter(event, this);
    }
    handlePasswordToggle() {
      this.passwordVisible = !this.passwordVisible;
    }
    updated(changedProperties) {
      super.updated(changedProperties);
      if (changedProperties.has("value") || changedProperties.has("defaultValue") || changedProperties.has("type")) {
        const sanitizingTypes = ["number", "date", "time", "datetime-local"];
        if (this.input && sanitizingTypes.includes(this.type) && this.value && this.input.value !== this.value) {
          this._value = this.input.value;
        }
        this.customStates.set("blank", !this.value);
        this.updateValidity();
      }
    }
    handleStepChange() {
      this.input.step = String(this.step);
      this.updateValidity();
    }
    /** Sets focus on the input. */
    focus(options2) {
      this.input.focus(options2);
    }
    /** Removes focus from the input. */
    blur() {
      this.input.blur();
    }
    /** Selects all the text in the input. */
    select() {
      this.input.select();
    }
    /** Sets the start and end positions of the text selection (0-based). */
    setSelectionRange(selectionStart, selectionEnd, selectionDirection = "none") {
      this.input.setSelectionRange(selectionStart, selectionEnd, selectionDirection);
    }
    /** Replaces a range of text with a new string. */
    setRangeText(replacement, start, end, selectMode = "preserve") {
      const selectionStart = start ?? this.input.selectionStart;
      const selectionEnd = end ?? this.input.selectionEnd;
      this.input.setRangeText(replacement, selectionStart, selectionEnd, selectMode);
      if (this.value !== this.input.value) {
        this.value = this.input.value;
      }
    }
    /** Displays the browser picker for an input element (only works if the browser supports it for the input type). */
    showPicker() {
      if ("showPicker" in HTMLInputElement.prototype) {
        this.input.showPicker();
      }
    }
    /** Increments the value of a numeric input type by the value of the step attribute. */
    stepUp() {
      this.input.stepUp();
      if (this.value !== this.input.value) {
        this.value = this.input.value;
      }
    }
    /** Decrements the value of a numeric input type by the value of the step attribute. */
    stepDown() {
      this.input.stepDown();
      if (this.value !== this.input.value) {
        this.value = this.input.value;
      }
    }
    formResetCallback() {
      this.value = null;
      if (this.input) {
        this.input.value = this.value;
      }
      super.formResetCallback();
    }
    render() {
      const hasLabelSlot = this.hasSlotController.test("label", "ssrLabel");
      const hasHintSlot = this.hasSlotController.test("hint", "ssrHint");
      const hasLabel = this.label ? true : !!hasLabelSlot;
      const hasHint = this.hint ? true : !!hasHintSlot;
      const hasClearIcon = this.withClear && !this.disabled && !this.readonly;
      const isClearIconVisible = (
        // prevents hydration mismatch errors.
        (!this.didSSR || this.hasUpdated) && hasClearIcon && (typeof this.value === "number" || this.value && this.value.length > 0)
      );
      return b2`
      <label
        part="form-control-label"
        class=${e7({
        label: true,
        "has-label": hasLabel
      })}
        for="input"
        aria-hidden=${hasLabel ? "false" : "true"}
      >
        <slot name="label">${this.label}</slot>
      </label>

      <div part="input-wrapper" class="text-field">
        <slot name="start" part="start" class="start"></slot>

        <input
          part="input"
          id="input"
          class="control"
          type=${this.type === "password" && this.passwordVisible ? "text" : this.type}
          title=${this.title}
          name=${o8(this.name)}
          ?disabled=${this.disabled}
          ?readonly=${this.readonly}
          ?required=${this.required}
          placeholder=${o8(this.placeholder)}
          minlength=${o8(this.minlength)}
          maxlength=${o8(this.maxlength)}
          min=${o8(this.min)}
          max=${o8(this.max)}
          step=${o8(this.step)}
          .value=${l5(this.value ?? "")}
          autocapitalize=${o8(this.autocapitalize)}
          autocomplete=${o8(this.autocomplete)}
          autocorrect=${this.autocorrect ? "on" : "off"}
          ?autofocus=${this.autofocus}
          spellcheck=${this.spellcheck}
          pattern=${o8(this.pattern)}
          enterkeyhint=${o8(this.enterkeyhint)}
          inputmode=${o8(this.inputmode)}
          aria-describedby="hint"
          @change=${this.handleChange}
          @input=${this.handleInput}
          @keydown=${this.handleKeyDown}
        />

        ${isClearIconVisible ? b2`
                <button
                  part="clear-button"
                  class="clear"
                  type="button"
                  aria-label=${this.localize.term("clearEntry")}
                  @click=${this.handleClearClick}
                  tabindex="-1"
                >
                  <slot name="clear-icon">
                    <cs-icon name="cancel" library="system"></cs-icon>
                  </slot>
                </button>
              ` : ""}
        ${this.passwordToggle && !this.disabled ? b2`
                <button
                  part="password-toggle-button"
                  class="password-toggle"
                  type="button"
                  aria-label=${this.localize.term(this.passwordVisible ? "hidePassword" : "showPassword")}
                  @click=${this.handlePasswordToggle}
                  tabindex="-1"
                >
                  ${!this.passwordVisible ? b2`
                          <slot name="show-password-icon">
                            <cs-icon name="visibility" library="system"></cs-icon>
                          </slot>
                        ` : b2`
                          <slot name="hide-password-icon">
                            <cs-icon name="visibility_off" library="system"></cs-icon>
                          </slot>
                        `}
                </button>
              ` : ""}

        <slot name="end" part="end" class="end"></slot>
      </div>

      <slot
        id="hint"
        part="hint"
        name="hint"
        class=${e7({
        "has-slotted": hasHint
      })}
        aria-hidden=${hasHint ? "false" : "true"}
        >${this.hint}</slot
      >
    `;
    }
  };
  CsInput.css = [size_styles_default, form_control_styles_default, input_styles_default];
  CsInput.shadowRootOptions = { ...CornerstoneFormAssociatedElement.shadowRootOptions, delegatesFocus: true };
  __decorateClass([
    e5("input")
  ], CsInput.prototype, "input", 2);
  __decorateClass([
    n4()
  ], CsInput.prototype, "title", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsInput.prototype, "type", 2);
  __decorateClass([
    r5()
  ], CsInput.prototype, "value", 1);
  __decorateClass([
    n4({ attribute: "value", reflect: true })
  ], CsInput.prototype, "defaultValue", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsInput.prototype, "size", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsInput.prototype, "appearance", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsInput.prototype, "pill", 2);
  __decorateClass([
    n4()
  ], CsInput.prototype, "label", 2);
  __decorateClass([
    n4({ attribute: "hint" })
  ], CsInput.prototype, "hint", 2);
  __decorateClass([
    n4({ attribute: "with-clear", type: Boolean })
  ], CsInput.prototype, "withClear", 2);
  __decorateClass([
    n4()
  ], CsInput.prototype, "placeholder", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsInput.prototype, "readonly", 2);
  __decorateClass([
    n4({ attribute: "password-toggle", type: Boolean })
  ], CsInput.prototype, "passwordToggle", 2);
  __decorateClass([
    n4({ attribute: "password-visible", type: Boolean })
  ], CsInput.prototype, "passwordVisible", 2);
  __decorateClass([
    n4({ attribute: "without-spin-buttons", type: Boolean, reflect: true })
  ], CsInput.prototype, "withoutSpinButtons", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsInput.prototype, "required", 2);
  __decorateClass([
    n4()
  ], CsInput.prototype, "pattern", 2);
  __decorateClass([
    n4({ type: Number })
  ], CsInput.prototype, "minlength", 2);
  __decorateClass([
    n4({ type: Number })
  ], CsInput.prototype, "maxlength", 2);
  __decorateClass([
    n4()
  ], CsInput.prototype, "min", 2);
  __decorateClass([
    n4()
  ], CsInput.prototype, "max", 2);
  __decorateClass([
    n4()
  ], CsInput.prototype, "step", 2);
  __decorateClass([
    n4()
  ], CsInput.prototype, "autocapitalize", 2);
  __decorateClass([
    n4({
      type: Boolean,
      converter: {
        fromAttribute: (value) => !value || value === "off" ? false : true,
        toAttribute: (value) => value ? "on" : "off"
      }
    })
  ], CsInput.prototype, "autocorrect", 2);
  __decorateClass([
    n4()
  ], CsInput.prototype, "autocomplete", 2);
  __decorateClass([
    n4({ type: Boolean })
  ], CsInput.prototype, "autofocus", 2);
  __decorateClass([
    n4()
  ], CsInput.prototype, "enterkeyhint", 2);
  __decorateClass([
    n4({
      type: Boolean,
      converter: {
        // Allow "true|false" attribute values but keep the property boolean
        fromAttribute: (value) => !value || value === "false" ? false : true,
        toAttribute: (value) => value ? "true" : "false"
      }
    })
  ], CsInput.prototype, "spellcheck", 2);
  __decorateClass([
    n4()
  ], CsInput.prototype, "inputmode", 2);
  __decorateClass([
    n4({ attribute: "ssr-label", type: Boolean })
  ], CsInput.prototype, "ssrLabel", 2);
  __decorateClass([
    n4({ attribute: "ssr-hint", type: Boolean })
  ], CsInput.prototype, "ssrHint", 2);
  __decorateClass([
    watch("step", { waitUntilFirstUpdate: true })
  ], CsInput.prototype, "handleStepChange", 1);
  CsInput = __decorateClass([
    customElement("cs-input")
  ], CsInput);
  CsInput.disableWarning?.("change-in-update");

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.REJ5RFTA.js
  var CsRepositionEvent = class extends Event {
    constructor() {
      super("cs-reposition", { bubbles: true, cancelable: false, composed: true });
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.Q2BQ3P5Q.js
  var popup_styles_default = i`
  :host {
    --arrow-color: black;
    --arrow-size: var(--cs-tooltip-arrow-size);
    --popup-border-width: 0px;
    --show-duration: var(--cs-transition-fast);
    --hide-duration: var(--cs-transition-fast);

    /*
     * These properties are computed to account for the arrow's dimensions after being rotated 45º. The constant
     * 0.7071 is derived from sin(45) to calculate the length of the arrow after rotation.
     *
     * The diamond will be translated inward by --arrow-base-offset, the border thickness, to centralise it on
     * the inner edge of the popup border. This also means we need to increase the size of the arrow by the
     * same amount to compensate.
     *
     * A diamond shaped clipping mask is used to avoid overlap of popup content. This extends slightly inward so
     * the popup border is covered with no sub-pixel rounding artifacts. The diamond corners are mitred at 22.5º
     * to properly merge any arrow border with the popup border. The constant 1.4142 is derived from 1 + tan(22.5).
     *
     */
    --arrow-base-offset: var(--popup-border-width);
    --arrow-size-diagonal: calc((var(--arrow-size) + var(--arrow-base-offset)) * 0.7071);
    --arrow-padding-offset: calc(var(--arrow-size-diagonal) - var(--arrow-size));
    --arrow-size-div: calc(var(--arrow-size-diagonal) * 2);
    --arrow-clipping-corner: calc(var(--arrow-base-offset) * 1.4142);

    display: contents;
  }

  .popup {
    position: absolute;
    isolation: isolate;
    max-width: var(--auto-size-available-width, none);
    max-height: var(--auto-size-available-height, none);

    /* Clear UA styles for [popover] */
    :where(&) {
      inset: unset;
      padding: unset;
      margin: unset;
      width: unset;
      height: unset;
      color: unset;
      background: unset;
      border: unset;
      overflow: unset;
    }
  }

  .popup-fixed {
    position: fixed;
  }

  .popup:not(.popup-active) {
    display: none;
  }

  .arrow {
    position: absolute;
    width: var(--arrow-size-div);
    height: var(--arrow-size-div);
    background: var(--arrow-color);
    z-index: 3;
    clip-path: polygon(
      var(--arrow-clipping-corner) 100%,
      var(--arrow-base-offset) calc(100% - var(--arrow-base-offset)),
      calc(var(--arrow-base-offset) - 2px) calc(100% - var(--arrow-base-offset)),
      calc(100% - var(--arrow-base-offset)) calc(var(--arrow-base-offset) - 2px),
      calc(100% - var(--arrow-base-offset)) var(--arrow-base-offset),
      100% var(--arrow-clipping-corner),
      100% 100%
    );
    rotate: 45deg;
  }

  :host([data-current-placement|='left']) .arrow {
    rotate: -45deg;
  }

  :host([data-current-placement|='right']) .arrow {
    rotate: 135deg;
  }

  :host([data-current-placement|='bottom']) .arrow {
    rotate: 225deg;
  }

  /* Hover bridge */
  .popup-hover-bridge:not(.popup-hover-bridge-visible) {
    display: none;
  }

  .popup-hover-bridge {
    position: fixed;
    z-index: 899;
    top: 0;
    right: 0;
    bottom: 0;
    left: 0;
    clip-path: polygon(
      var(--hover-bridge-top-left-x, 0) var(--hover-bridge-top-left-y, 0),
      var(--hover-bridge-top-right-x, 0) var(--hover-bridge-top-right-y, 0),
      var(--hover-bridge-bottom-right-x, 0) var(--hover-bridge-bottom-right-y, 0),
      var(--hover-bridge-bottom-left-x, 0) var(--hover-bridge-bottom-left-y, 0)
    );
  }

  /* Built-in animations */
  .show {
    animation: show var(--show-duration) ease;
  }

  .hide {
    animation: show var(--hide-duration) ease reverse;
  }

  @keyframes show {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }

  .show-with-scale {
    animation: show-with-scale var(--show-duration) ease;
  }

  .hide-with-scale {
    animation: show-with-scale var(--hide-duration) ease reverse;
  }

  @keyframes show-with-scale {
    from {
      opacity: 0;
      scale: 0.8;
    }
    to {
      opacity: 1;
      scale: 1;
    }
  }
`;

  // node_modules/@floating-ui/utils/dist/floating-ui.utils.mjs
  var min = Math.min;
  var max = Math.max;
  var round = Math.round;
  var floor = Math.floor;
  var createCoords = (v2) => ({
    x: v2,
    y: v2
  });
  var oppositeSideMap = {
    left: "right",
    right: "left",
    bottom: "top",
    top: "bottom"
  };
  function clamp2(start, value, end) {
    return max(start, min(value, end));
  }
  function evaluate(value, param) {
    return typeof value === "function" ? value(param) : value;
  }
  function getSide(placement) {
    return placement.split("-")[0];
  }
  function getAlignment(placement) {
    return placement.split("-")[1];
  }
  function getOppositeAxis(axis) {
    return axis === "x" ? "y" : "x";
  }
  function getAxisLength(axis) {
    return axis === "y" ? "height" : "width";
  }
  function getSideAxis(placement) {
    const firstChar = placement[0];
    return firstChar === "t" || firstChar === "b" ? "y" : "x";
  }
  function getAlignmentAxis(placement) {
    return getOppositeAxis(getSideAxis(placement));
  }
  function getAlignmentSides(placement, rects, rtl) {
    if (rtl === void 0) {
      rtl = false;
    }
    const alignment = getAlignment(placement);
    const alignmentAxis = getAlignmentAxis(placement);
    const length = getAxisLength(alignmentAxis);
    let mainAlignmentSide = alignmentAxis === "x" ? alignment === (rtl ? "end" : "start") ? "right" : "left" : alignment === "start" ? "bottom" : "top";
    if (rects.reference[length] > rects.floating[length]) {
      mainAlignmentSide = getOppositePlacement(mainAlignmentSide);
    }
    return [mainAlignmentSide, getOppositePlacement(mainAlignmentSide)];
  }
  function getExpandedPlacements(placement) {
    const oppositePlacement = getOppositePlacement(placement);
    return [getOppositeAlignmentPlacement(placement), oppositePlacement, getOppositeAlignmentPlacement(oppositePlacement)];
  }
  function getOppositeAlignmentPlacement(placement) {
    return placement.includes("start") ? placement.replace("start", "end") : placement.replace("end", "start");
  }
  var lrPlacement = ["left", "right"];
  var rlPlacement = ["right", "left"];
  var tbPlacement = ["top", "bottom"];
  var btPlacement = ["bottom", "top"];
  function getSideList(side, isStart, rtl) {
    switch (side) {
      case "top":
      case "bottom":
        if (rtl) return isStart ? rlPlacement : lrPlacement;
        return isStart ? lrPlacement : rlPlacement;
      case "left":
      case "right":
        return isStart ? tbPlacement : btPlacement;
      default:
        return [];
    }
  }
  function getOppositeAxisPlacements(placement, flipAlignment, direction, rtl) {
    const alignment = getAlignment(placement);
    let list2 = getSideList(getSide(placement), direction === "start", rtl);
    if (alignment) {
      list2 = list2.map((side) => side + "-" + alignment);
      if (flipAlignment) {
        list2 = list2.concat(list2.map(getOppositeAlignmentPlacement));
      }
    }
    return list2;
  }
  function getOppositePlacement(placement) {
    const side = getSide(placement);
    return oppositeSideMap[side] + placement.slice(side.length);
  }
  function expandPaddingObject(padding) {
    var _padding$top, _padding$right, _padding$bottom, _padding$left;
    return {
      top: (_padding$top = padding.top) != null ? _padding$top : 0,
      right: (_padding$right = padding.right) != null ? _padding$right : 0,
      bottom: (_padding$bottom = padding.bottom) != null ? _padding$bottom : 0,
      left: (_padding$left = padding.left) != null ? _padding$left : 0
    };
  }
  function getPaddingObject(padding) {
    return typeof padding !== "number" ? expandPaddingObject(padding) : {
      top: padding,
      right: padding,
      bottom: padding,
      left: padding
    };
  }
  function rectToClientRect(rect) {
    const {
      x: x2,
      y: y3,
      width,
      height
    } = rect;
    return {
      width,
      height,
      top: y3,
      left: x2,
      right: x2 + width,
      bottom: y3 + height,
      x: x2,
      y: y3
    };
  }

  // node_modules/@floating-ui/core/dist/floating-ui.core.mjs
  function computeCoordsFromPlacement(_ref, placement, rtl) {
    let {
      reference,
      floating
    } = _ref;
    const sideAxis = getSideAxis(placement);
    const alignmentAxis = getAlignmentAxis(placement);
    const alignLength = getAxisLength(alignmentAxis);
    const side = getSide(placement);
    const isVertical = sideAxis === "y";
    const commonX = reference.x + reference.width / 2 - floating.width / 2;
    const commonY = reference.y + reference.height / 2 - floating.height / 2;
    const commonAlign = reference[alignLength] / 2 - floating[alignLength] / 2;
    let coords;
    switch (side) {
      case "top":
        coords = {
          x: commonX,
          y: reference.y - floating.height
        };
        break;
      case "bottom":
        coords = {
          x: commonX,
          y: reference.y + reference.height
        };
        break;
      case "right":
        coords = {
          x: reference.x + reference.width,
          y: commonY
        };
        break;
      case "left":
        coords = {
          x: reference.x - floating.width,
          y: commonY
        };
        break;
      default:
        coords = {
          x: reference.x,
          y: reference.y
        };
    }
    const alignment = getAlignment(placement);
    if (alignment) {
      coords[alignmentAxis] += commonAlign * (alignment === "end" ? 1 : -1) * (rtl && isVertical ? -1 : 1);
    }
    return coords;
  }
  async function detectOverflow(state, options2) {
    var _await$platform$isEle;
    if (options2 === void 0) {
      options2 = {};
    }
    const {
      x: x2,
      y: y3,
      platform: platform2,
      rects,
      elements,
      strategy
    } = state;
    const {
      boundary = "clippingAncestors",
      rootBoundary = "viewport",
      elementContext = "floating",
      altBoundary = false,
      padding = 0
    } = evaluate(options2, state);
    const paddingObject = getPaddingObject(padding);
    const altContext = elementContext === "floating" ? "reference" : "floating";
    const element = elements[altBoundary ? altContext : elementContext];
    const clippingClientRect = rectToClientRect(await platform2.getClippingRect({
      element: ((_await$platform$isEle = await (platform2.isElement == null ? void 0 : platform2.isElement(element))) != null ? _await$platform$isEle : true) ? element : element.contextElement || await (platform2.getDocumentElement == null ? void 0 : platform2.getDocumentElement(elements.floating)),
      boundary,
      rootBoundary,
      strategy
    }));
    const rect = elementContext === "floating" ? {
      x: x2,
      y: y3,
      width: rects.floating.width,
      height: rects.floating.height
    } : rects.reference;
    const offsetParent = await (platform2.getOffsetParent == null ? void 0 : platform2.getOffsetParent(elements.floating));
    const offsetScale = await (platform2.isElement == null ? void 0 : platform2.isElement(offsetParent)) && await (platform2.getScale == null ? void 0 : platform2.getScale(offsetParent)) || {
      x: 1,
      y: 1
    };
    const elementClientRect = rectToClientRect(platform2.convertOffsetParentRelativeRectToViewportRelativeRect ? await platform2.convertOffsetParentRelativeRectToViewportRelativeRect({
      elements,
      rect,
      offsetParent,
      strategy
    }) : rect);
    return {
      top: (clippingClientRect.top - elementClientRect.top + paddingObject.top) / offsetScale.y,
      bottom: (elementClientRect.bottom - clippingClientRect.bottom + paddingObject.bottom) / offsetScale.y,
      left: (clippingClientRect.left - elementClientRect.left + paddingObject.left) / offsetScale.x,
      right: (elementClientRect.right - clippingClientRect.right + paddingObject.right) / offsetScale.x
    };
  }
  var MAX_RESET_COUNT = 50;
  var computePosition = async (reference, floating, config) => {
    const {
      placement = "bottom",
      strategy = "absolute",
      middleware = [],
      platform: platform2
    } = config;
    const platformWithDetectOverflow = platform2.detectOverflow ? platform2 : {
      ...platform2,
      detectOverflow
    };
    const rtl = await (platform2.isRTL == null ? void 0 : platform2.isRTL(floating));
    let rects = await platform2.getElementRects({
      reference,
      floating,
      strategy
    });
    let {
      x: x2,
      y: y3
    } = computeCoordsFromPlacement(rects, placement, rtl);
    let statefulPlacement = placement;
    let resetCount = 0;
    const middlewareData = {};
    for (let i10 = 0; i10 < middleware.length; i10++) {
      const currentMiddleware = middleware[i10];
      if (!currentMiddleware) {
        continue;
      }
      const {
        name,
        fn
      } = currentMiddleware;
      const {
        x: nextX,
        y: nextY,
        data,
        reset
      } = await fn({
        x: x2,
        y: y3,
        initialPlacement: placement,
        placement: statefulPlacement,
        strategy,
        middlewareData,
        rects,
        platform: platformWithDetectOverflow,
        elements: {
          reference,
          floating
        }
      });
      x2 = nextX != null ? nextX : x2;
      y3 = nextY != null ? nextY : y3;
      middlewareData[name] = {
        ...middlewareData[name],
        ...data
      };
      if (reset && resetCount < MAX_RESET_COUNT) {
        resetCount++;
        if (typeof reset === "object") {
          if (reset.placement) {
            statefulPlacement = reset.placement;
          }
          if (reset.rects) {
            rects = reset.rects === true ? await platform2.getElementRects({
              reference,
              floating,
              strategy
            }) : reset.rects;
          }
          ({
            x: x2,
            y: y3
          } = computeCoordsFromPlacement(rects, statefulPlacement, rtl));
        }
        i10 = -1;
      }
    }
    return {
      x: x2,
      y: y3,
      placement: statefulPlacement,
      strategy,
      middlewareData
    };
  };
  var arrow = (options2) => ({
    name: "arrow",
    options: options2,
    async fn(state) {
      const {
        x: x2,
        y: y3,
        placement,
        rects,
        platform: platform2,
        elements,
        middlewareData
      } = state;
      const {
        element,
        padding = 0
      } = evaluate(options2, state) || {};
      if (element == null) {
        return {};
      }
      const paddingObject = getPaddingObject(padding);
      const coords = {
        x: x2,
        y: y3
      };
      const axis = getAlignmentAxis(placement);
      const length = getAxisLength(axis);
      const arrowDimensions = await platform2.getDimensions(element);
      const isYAxis = axis === "y";
      const minProp = isYAxis ? "top" : "left";
      const maxProp = isYAxis ? "bottom" : "right";
      const clientProp = isYAxis ? "clientHeight" : "clientWidth";
      const endDiff = rects.reference[length] + rects.reference[axis] - coords[axis] - rects.floating[length];
      const startDiff = coords[axis] - rects.reference[axis];
      const arrowOffsetParent = await (platform2.getOffsetParent == null ? void 0 : platform2.getOffsetParent(element));
      let clientSize = arrowOffsetParent ? arrowOffsetParent[clientProp] : 0;
      if (!clientSize || !await (platform2.isElement == null ? void 0 : platform2.isElement(arrowOffsetParent))) {
        clientSize = elements.floating[clientProp] || rects.floating[length];
      }
      const centerToReference = endDiff / 2 - startDiff / 2;
      const largestPossiblePadding = clientSize / 2 - arrowDimensions[length] / 2 - 1;
      const minPadding = min(paddingObject[minProp], largestPossiblePadding);
      const maxPadding = min(paddingObject[maxProp], largestPossiblePadding);
      const max2 = clientSize - arrowDimensions[length] - maxPadding;
      const center = clientSize / 2 - arrowDimensions[length] / 2 + centerToReference;
      const offset3 = clamp2(minPadding, center, max2);
      const shouldAddOffset = !middlewareData.arrow && getAlignment(placement) != null && center !== offset3 && rects.reference[length] / 2 - (center < minPadding ? minPadding : maxPadding) - arrowDimensions[length] / 2 < 0;
      const alignmentOffset = shouldAddOffset ? center < minPadding ? center - minPadding : center - max2 : 0;
      return {
        [axis]: coords[axis] + alignmentOffset,
        data: {
          [axis]: offset3,
          centerOffset: center - offset3 - alignmentOffset,
          ...shouldAddOffset && {
            alignmentOffset
          }
        },
        reset: shouldAddOffset
      };
    }
  });
  var flip2 = function(options2) {
    if (options2 === void 0) {
      options2 = {};
    }
    return {
      name: "flip",
      options: options2,
      async fn(state) {
        var _middlewareData$arrow, _middlewareData$flip;
        const {
          placement,
          middlewareData,
          rects,
          initialPlacement,
          platform: platform2,
          elements
        } = state;
        const {
          mainAxis: checkMainAxis = true,
          crossAxis: checkCrossAxis = true,
          fallbackPlacements: specifiedFallbackPlacements,
          fallbackStrategy = "bestFit",
          fallbackAxisSideDirection = "none",
          flipAlignment = true,
          ...detectOverflowOptions
        } = evaluate(options2, state);
        if ((_middlewareData$arrow = middlewareData.arrow) != null && _middlewareData$arrow.alignmentOffset) {
          return {};
        }
        const side = getSide(placement);
        const initialSideAxis = getSideAxis(initialPlacement);
        const isBasePlacement = getSide(initialPlacement) === initialPlacement;
        const rtl = await (platform2.isRTL == null ? void 0 : platform2.isRTL(elements.floating));
        const fallbackPlacements = specifiedFallbackPlacements || (isBasePlacement || !flipAlignment ? [getOppositePlacement(initialPlacement)] : getExpandedPlacements(initialPlacement));
        const hasFallbackAxisSideDirection = fallbackAxisSideDirection !== "none";
        if (!specifiedFallbackPlacements && hasFallbackAxisSideDirection) {
          fallbackPlacements.push(...getOppositeAxisPlacements(initialPlacement, flipAlignment, fallbackAxisSideDirection, rtl));
        }
        const placements2 = [initialPlacement, ...fallbackPlacements];
        const overflow = await platform2.detectOverflow(state, detectOverflowOptions);
        const overflows = [];
        let overflowsData = ((_middlewareData$flip = middlewareData.flip) == null ? void 0 : _middlewareData$flip.overflows) || [];
        if (checkMainAxis) {
          overflows.push(overflow[side]);
        }
        if (checkCrossAxis) {
          const sides2 = getAlignmentSides(placement, rects, rtl);
          overflows.push(overflow[sides2[0]], overflow[sides2[1]]);
        }
        overflowsData = [...overflowsData, {
          placement,
          overflows
        }];
        if (!overflows.every((side2) => side2 <= 0)) {
          var _middlewareData$flip2, _overflowsData$filter;
          const nextIndex = (((_middlewareData$flip2 = middlewareData.flip) == null ? void 0 : _middlewareData$flip2.index) || 0) + 1;
          const nextPlacement = placements2[nextIndex];
          if (nextPlacement) {
            const ignoreCrossAxisOverflow = checkCrossAxis === "alignment" ? initialSideAxis !== getSideAxis(nextPlacement) : false;
            if (!ignoreCrossAxisOverflow || // We leave the current main axis only if every placement on that axis
            // overflows the main axis.
            overflowsData.every((d3) => getSideAxis(d3.placement) === initialSideAxis ? d3.overflows[0] > 0 : true)) {
              return {
                data: {
                  index: nextIndex,
                  overflows: overflowsData
                },
                reset: {
                  placement: nextPlacement
                }
              };
            }
          }
          let resetPlacement = (_overflowsData$filter = overflowsData.filter((d3) => d3.overflows[0] <= 0).sort((a4, b3) => a4.overflows[1] - b3.overflows[1])[0]) == null ? void 0 : _overflowsData$filter.placement;
          if (!resetPlacement) {
            switch (fallbackStrategy) {
              case "bestFit": {
                var _overflowsData$filter2;
                const placement2 = (_overflowsData$filter2 = overflowsData.filter((d3) => {
                  if (hasFallbackAxisSideDirection) {
                    const currentSideAxis = getSideAxis(d3.placement);
                    return currentSideAxis === initialSideAxis || // Create a bias to the `y` side axis due to horizontal
                    // reading directions favoring greater width.
                    currentSideAxis === "y";
                  }
                  return true;
                }).map((d3) => [d3.placement, d3.overflows.filter((overflow2) => overflow2 > 0).reduce((acc, overflow2) => acc + overflow2, 0)]).sort((a4, b3) => a4[1] - b3[1])[0]) == null ? void 0 : _overflowsData$filter2[0];
                if (placement2) {
                  resetPlacement = placement2;
                }
                break;
              }
              case "initialPlacement":
                resetPlacement = initialPlacement;
                break;
            }
          }
          if (placement !== resetPlacement) {
            return {
              reset: {
                placement: resetPlacement
              }
            };
          }
        }
        return {};
      }
    };
  };
  var originSides = /* @__PURE__ */ new Set(["left", "top"]);
  async function convertValueToCoords(state, options2) {
    const {
      placement,
      platform: platform2,
      elements
    } = state;
    const rtl = await (platform2.isRTL == null ? void 0 : platform2.isRTL(elements.floating));
    const side = getSide(placement);
    const alignment = getAlignment(placement);
    const isVertical = getSideAxis(placement) === "y";
    const mainAxisMulti = originSides.has(side) ? -1 : 1;
    const crossAxisMulti = rtl && isVertical ? -1 : 1;
    const rawValue = evaluate(options2, state);
    let {
      mainAxis,
      crossAxis,
      alignmentAxis
    } = typeof rawValue === "number" ? {
      mainAxis: rawValue,
      crossAxis: 0,
      alignmentAxis: null
    } : {
      mainAxis: rawValue.mainAxis || 0,
      crossAxis: rawValue.crossAxis || 0,
      alignmentAxis: rawValue.alignmentAxis
    };
    if (alignment && typeof alignmentAxis === "number") {
      crossAxis = alignment === "end" ? alignmentAxis * -1 : alignmentAxis;
    }
    return isVertical ? {
      x: crossAxis * crossAxisMulti,
      y: mainAxis * mainAxisMulti
    } : {
      x: mainAxis * mainAxisMulti,
      y: crossAxis * crossAxisMulti
    };
  }
  var offset = function(options2) {
    if (options2 === void 0) {
      options2 = 0;
    }
    return {
      name: "offset",
      options: options2,
      async fn(state) {
        var _middlewareData$offse, _middlewareData$arrow;
        const {
          x: x2,
          y: y3,
          placement,
          middlewareData
        } = state;
        const diffCoords = await convertValueToCoords(state, options2);
        if (placement === ((_middlewareData$offse = middlewareData.offset) == null ? void 0 : _middlewareData$offse.placement) && (_middlewareData$arrow = middlewareData.arrow) != null && _middlewareData$arrow.alignmentOffset) {
          return {};
        }
        return {
          x: x2 + diffCoords.x,
          y: y3 + diffCoords.y,
          data: {
            ...diffCoords,
            placement
          }
        };
      }
    };
  };
  var shift = function(options2) {
    if (options2 === void 0) {
      options2 = {};
    }
    return {
      name: "shift",
      options: options2,
      async fn(state) {
        const {
          x: x2,
          y: y3,
          placement,
          platform: platform2
        } = state;
        const {
          mainAxis: checkMainAxis = true,
          crossAxis: checkCrossAxis = false,
          limiter = {
            fn: (_ref) => {
              let {
                x: x3,
                y: y4
              } = _ref;
              return {
                x: x3,
                y: y4
              };
            }
          },
          ...detectOverflowOptions
        } = evaluate(options2, state);
        const coords = {
          x: x2,
          y: y3
        };
        const overflow = await platform2.detectOverflow(state, detectOverflowOptions);
        const crossAxis = getSideAxis(placement);
        const mainAxis = getOppositeAxis(crossAxis);
        let mainAxisCoord = coords[mainAxis];
        let crossAxisCoord = coords[crossAxis];
        const clampCoord = (axis, coord) => clamp2(coord + overflow[axis === "y" ? "top" : "left"], coord, coord - overflow[axis === "y" ? "bottom" : "right"]);
        if (checkMainAxis) {
          mainAxisCoord = clampCoord(mainAxis, mainAxisCoord);
        }
        if (checkCrossAxis) {
          crossAxisCoord = clampCoord(crossAxis, crossAxisCoord);
        }
        const limitedCoords = limiter.fn({
          ...state,
          [mainAxis]: mainAxisCoord,
          [crossAxis]: crossAxisCoord
        });
        return {
          ...limitedCoords,
          data: {
            x: limitedCoords.x - x2,
            y: limitedCoords.y - y3,
            enabled: {
              [mainAxis]: checkMainAxis,
              [crossAxis]: checkCrossAxis
            }
          }
        };
      }
    };
  };
  var size = function(options2) {
    if (options2 === void 0) {
      options2 = {};
    }
    return {
      name: "size",
      options: options2,
      async fn(state) {
        const {
          placement,
          rects,
          platform: platform2,
          elements
        } = state;
        const {
          apply = () => {
          },
          ...detectOverflowOptions
        } = evaluate(options2, state);
        const overflow = await platform2.detectOverflow(state, detectOverflowOptions);
        const side = getSide(placement);
        const alignment = getAlignment(placement);
        const isYAxis = getSideAxis(placement) === "y";
        const {
          width,
          height
        } = rects.floating;
        let heightSide;
        let widthSide;
        if (side === "top" || side === "bottom") {
          heightSide = side;
          widthSide = alignment === (await (platform2.isRTL == null ? void 0 : platform2.isRTL(elements.floating)) ? "start" : "end") ? "left" : "right";
        } else {
          widthSide = side;
          heightSide = alignment === "end" ? "top" : "bottom";
        }
        const maximumClippingHeight = height - overflow.top - overflow.bottom;
        const maximumClippingWidth = width - overflow.left - overflow.right;
        const overflowAvailableHeight = min(height - overflow[heightSide], maximumClippingHeight);
        const overflowAvailableWidth = min(width - overflow[widthSide], maximumClippingWidth);
        const shiftData = state.middlewareData.shift;
        const noShift = !shiftData;
        let availableHeight = overflowAvailableHeight;
        let availableWidth = overflowAvailableWidth;
        if (shiftData != null && shiftData.enabled.x) {
          availableWidth = maximumClippingWidth;
        }
        if (shiftData != null && shiftData.enabled.y) {
          availableHeight = maximumClippingHeight;
        }
        if (noShift && !alignment) {
          if (isYAxis) {
            availableWidth = width - 2 * max(overflow.left, overflow.right);
          } else {
            availableHeight = height - 2 * max(overflow.top, overflow.bottom);
          }
        }
        await apply({
          ...state,
          availableWidth,
          availableHeight
        });
        const nextDimensions = await platform2.getDimensions(elements.floating);
        if (width !== nextDimensions.width || height !== nextDimensions.height) {
          return {
            reset: {
              rects: true
            }
          };
        }
        return {};
      }
    };
  };

  // node_modules/@floating-ui/utils/dist/floating-ui.utils.dom.mjs
  function hasWindow() {
    return typeof window !== "undefined";
  }
  function getNodeName(node) {
    if (isNode(node)) {
      return (node.nodeName || "").toLowerCase();
    }
    return "#document";
  }
  function getWindow(node) {
    var _node$ownerDocument;
    return (node == null || (_node$ownerDocument = node.ownerDocument) == null ? void 0 : _node$ownerDocument.defaultView) || window;
  }
  function getDocumentElement(node) {
    var _ref;
    return (_ref = (isNode(node) ? node.ownerDocument : node.document) || window.document) == null ? void 0 : _ref.documentElement;
  }
  function isNode(value) {
    if (!hasWindow()) {
      return false;
    }
    return value instanceof Node || value instanceof getWindow(value).Node;
  }
  function isElement(value) {
    if (!hasWindow()) {
      return false;
    }
    return value instanceof Element || value instanceof getWindow(value).Element;
  }
  function isHTMLElement(value) {
    if (!hasWindow()) {
      return false;
    }
    return value instanceof HTMLElement || value instanceof getWindow(value).HTMLElement;
  }
  function isShadowRoot(value) {
    if (!hasWindow() || typeof ShadowRoot === "undefined") {
      return false;
    }
    return value instanceof ShadowRoot || value instanceof getWindow(value).ShadowRoot;
  }
  function isOverflowElement(element) {
    const {
      overflow,
      overflowX,
      overflowY,
      display
    } = getComputedStyle2(element);
    return /auto|scroll|overlay|hidden|clip/.test(overflow + overflowY + overflowX) && display !== "inline" && display !== "contents";
  }
  function isTableElement(element) {
    return /^(table|td|th)$/.test(getNodeName(element));
  }
  function isTopLayer(element) {
    try {
      if (element.matches(":popover-open")) {
        return true;
      }
    } catch (_e) {
    }
    try {
      return element.matches(":modal");
    } catch (_e) {
      return false;
    }
  }
  var willChangeRe = /transform|translate|scale|rotate|perspective|filter/;
  var containRe = /paint|layout|strict|content/;
  var isNotNone = (value) => !!value && value !== "none";
  var isWebKitValue;
  function isContainingBlock(elementOrCss) {
    const css = isElement(elementOrCss) ? getComputedStyle2(elementOrCss) : elementOrCss;
    return isNotNone(css.transform) || isNotNone(css.translate) || isNotNone(css.scale) || isNotNone(css.rotate) || isNotNone(css.perspective) || !isWebKit() && (isNotNone(css.backdropFilter) || isNotNone(css.filter)) || willChangeRe.test(css.willChange || "") || containRe.test(css.contain || "");
  }
  function getContainingBlock(element) {
    let currentNode = getParentNode(element);
    while (isHTMLElement(currentNode) && !isLastTraversableNode(currentNode)) {
      if (isContainingBlock(currentNode)) {
        return currentNode;
      } else if (isTopLayer(currentNode)) {
        return null;
      }
      currentNode = getParentNode(currentNode);
    }
    return null;
  }
  function isWebKit() {
    if (isWebKitValue == null) {
      isWebKitValue = typeof CSS !== "undefined" && CSS.supports && CSS.supports("-webkit-backdrop-filter", "none");
    }
    return isWebKitValue;
  }
  function isLastTraversableNode(node) {
    return /^(html|body|#document)$/.test(getNodeName(node));
  }
  function getComputedStyle2(element) {
    return getWindow(element).getComputedStyle(element);
  }
  function getNodeScroll(element) {
    if (isElement(element)) {
      return {
        scrollLeft: element.scrollLeft,
        scrollTop: element.scrollTop
      };
    }
    return {
      scrollLeft: element.scrollX,
      scrollTop: element.scrollY
    };
  }
  function getParentNode(node) {
    if (getNodeName(node) === "html") {
      return node;
    }
    const result = (
      // Step into the shadow DOM of the parent of a slotted node.
      node.assignedSlot || // DOM Element detected.
      node.parentNode || // ShadowRoot detected.
      isShadowRoot(node) && node.host || // Fallback.
      getDocumentElement(node)
    );
    return isShadowRoot(result) ? result.host : result;
  }
  function getNearestOverflowAncestor(node) {
    const parentNode = getParentNode(node);
    if (isLastTraversableNode(parentNode)) {
      return (node.ownerDocument || node).body;
    }
    if (isHTMLElement(parentNode) && isOverflowElement(parentNode)) {
      return parentNode;
    }
    return getNearestOverflowAncestor(parentNode);
  }
  function getOverflowAncestors(node, list2, traverseIframes) {
    var _node$ownerDocument2;
    if (list2 === void 0) {
      list2 = [];
    }
    if (traverseIframes === void 0) {
      traverseIframes = true;
    }
    const scrollableAncestor = getNearestOverflowAncestor(node);
    const isBody = scrollableAncestor === ((_node$ownerDocument2 = node.ownerDocument) == null ? void 0 : _node$ownerDocument2.body);
    const win = getWindow(scrollableAncestor);
    if (isBody) {
      const frameElement = getFrameElement(win);
      return list2.concat(win, win.visualViewport || [], isOverflowElement(scrollableAncestor) ? scrollableAncestor : [], frameElement && traverseIframes ? getOverflowAncestors(frameElement) : []);
    } else {
      return list2.concat(scrollableAncestor, getOverflowAncestors(scrollableAncestor, [], traverseIframes));
    }
  }
  function getFrameElement(win) {
    return win.parent && Object.getPrototypeOf(win.parent) ? win.frameElement : null;
  }

  // node_modules/@floating-ui/dom/dist/floating-ui.dom.mjs
  function getCssDimensions(element) {
    const css = getComputedStyle2(element);
    let width = parseFloat(css.width) || 0;
    let height = parseFloat(css.height) || 0;
    const hasOffset = isHTMLElement(element);
    const offsetWidth = hasOffset ? element.offsetWidth : width;
    const offsetHeight = hasOffset ? element.offsetHeight : height;
    const shouldFallback = round(width) !== offsetWidth || round(height) !== offsetHeight;
    if (shouldFallback) {
      width = offsetWidth;
      height = offsetHeight;
    }
    return {
      width,
      height,
      $: shouldFallback
    };
  }
  function unwrapElement(element) {
    return !isElement(element) ? element.contextElement : element;
  }
  function getScale(element) {
    const domElement = unwrapElement(element);
    if (!isHTMLElement(domElement)) {
      return createCoords(1);
    }
    const rect = domElement.getBoundingClientRect();
    const {
      width,
      height,
      $: $3
    } = getCssDimensions(domElement);
    let x2 = ($3 ? round(rect.width) : rect.width) / width;
    let y3 = ($3 ? round(rect.height) : rect.height) / height;
    if (!x2 || !Number.isFinite(x2)) {
      x2 = 1;
    }
    if (!y3 || !Number.isFinite(y3)) {
      y3 = 1;
    }
    return {
      x: x2,
      y: y3
    };
  }
  var noOffsets = /* @__PURE__ */ createCoords(0);
  function getVisualOffsets(element) {
    const win = getWindow(element);
    if (!isWebKit() || !win.visualViewport) {
      return noOffsets;
    }
    return {
      x: win.visualViewport.offsetLeft,
      y: win.visualViewport.offsetTop
    };
  }
  function shouldAddVisualOffsets(element, isFixed, floatingOffsetParent) {
    if (isFixed === void 0) {
      isFixed = false;
    }
    return !!floatingOffsetParent && isFixed && floatingOffsetParent === getWindow(element);
  }
  function getBoundingClientRect(element, includeScale, isFixedStrategy, offsetParent) {
    if (includeScale === void 0) {
      includeScale = false;
    }
    if (isFixedStrategy === void 0) {
      isFixedStrategy = false;
    }
    const clientRect = element.getBoundingClientRect();
    const domElement = unwrapElement(element);
    let scale = createCoords(1);
    if (includeScale) {
      if (offsetParent) {
        if (isElement(offsetParent)) {
          scale = getScale(offsetParent);
        }
      } else {
        scale = getScale(element);
      }
    }
    const visualOffsets = shouldAddVisualOffsets(domElement, isFixedStrategy, offsetParent) ? getVisualOffsets(domElement) : createCoords(0);
    let x2 = (clientRect.left + visualOffsets.x) / scale.x;
    let y3 = (clientRect.top + visualOffsets.y) / scale.y;
    let width = clientRect.width / scale.x;
    let height = clientRect.height / scale.y;
    if (domElement && offsetParent) {
      const win = getWindow(domElement);
      const offsetWin = isElement(offsetParent) ? getWindow(offsetParent) : offsetParent;
      let currentWin = win;
      let currentIFrame = getFrameElement(currentWin);
      while (currentIFrame && offsetWin !== currentWin) {
        const iframeScale = getScale(currentIFrame);
        const iframeRect = currentIFrame.getBoundingClientRect();
        const css = getComputedStyle2(currentIFrame);
        const left = iframeRect.left + (currentIFrame.clientLeft + parseFloat(css.paddingLeft)) * iframeScale.x;
        const top = iframeRect.top + (currentIFrame.clientTop + parseFloat(css.paddingTop)) * iframeScale.y;
        x2 *= iframeScale.x;
        y3 *= iframeScale.y;
        width *= iframeScale.x;
        height *= iframeScale.y;
        x2 += left;
        y3 += top;
        currentWin = getWindow(currentIFrame);
        currentIFrame = getFrameElement(currentWin);
      }
    }
    return rectToClientRect({
      width,
      height,
      x: x2,
      y: y3
    });
  }
  function getWindowScrollBarX(element, rect) {
    const leftScroll = getNodeScroll(element).scrollLeft;
    if (!rect) {
      return getBoundingClientRect(getDocumentElement(element)).left + leftScroll;
    }
    return rect.left + leftScroll;
  }
  function getHTMLOffset(documentElement, scroll) {
    const htmlRect = documentElement.getBoundingClientRect();
    const x2 = htmlRect.left + scroll.scrollLeft - getWindowScrollBarX(documentElement, htmlRect);
    const y3 = htmlRect.top + scroll.scrollTop;
    return {
      x: x2,
      y: y3
    };
  }
  function convertOffsetParentRelativeRectToViewportRelativeRect(_ref) {
    let {
      elements,
      rect,
      offsetParent,
      strategy
    } = _ref;
    const isFixed = strategy === "fixed";
    const documentElement = getDocumentElement(offsetParent);
    const topLayer = elements ? isTopLayer(elements.floating) : false;
    if (offsetParent === documentElement || topLayer && isFixed) {
      return rect;
    }
    let scroll = {
      scrollLeft: 0,
      scrollTop: 0
    };
    let scale = createCoords(1);
    const offsets = createCoords(0);
    const isOffsetParentAnElement = isHTMLElement(offsetParent);
    if (isOffsetParentAnElement || !isFixed) {
      if (getNodeName(offsetParent) !== "body" || isOverflowElement(documentElement)) {
        scroll = getNodeScroll(offsetParent);
      }
      if (isOffsetParentAnElement) {
        const offsetRect = getBoundingClientRect(offsetParent);
        scale = getScale(offsetParent);
        offsets.x = offsetRect.x + offsetParent.clientLeft;
        offsets.y = offsetRect.y + offsetParent.clientTop;
      }
    }
    const htmlOffset = documentElement && !isOffsetParentAnElement && !isFixed ? getHTMLOffset(documentElement, scroll) : createCoords(0);
    return {
      width: rect.width * scale.x,
      height: rect.height * scale.y,
      x: rect.x * scale.x - scroll.scrollLeft * scale.x + offsets.x + htmlOffset.x,
      y: rect.y * scale.y - scroll.scrollTop * scale.y + offsets.y + htmlOffset.y
    };
  }
  function getClientRects(element) {
    return element.getClientRects ? Array.from(element.getClientRects()) : [];
  }
  function getDocumentRect(html2) {
    const scroll = getNodeScroll(html2);
    const body = html2.ownerDocument.body;
    const width = max(html2.scrollWidth, html2.clientWidth, body.scrollWidth, body.clientWidth);
    const height = max(html2.scrollHeight, html2.clientHeight, body.scrollHeight, body.clientHeight);
    let x2 = -scroll.scrollLeft + getWindowScrollBarX(html2);
    const y3 = -scroll.scrollTop;
    if (getComputedStyle2(body).direction === "rtl") {
      x2 += max(html2.clientWidth, body.clientWidth) - width;
    }
    return {
      width,
      height,
      x: x2,
      y: y3
    };
  }
  var SCROLLBAR_MAX = 25;
  function getViewportRect(element, strategy, rootBoundary) {
    if (rootBoundary === void 0) {
      rootBoundary = "viewport";
    }
    const isLayoutViewport = rootBoundary === "layoutViewport";
    const win = getWindow(element);
    const html2 = getDocumentElement(element);
    const visualViewport = win.visualViewport;
    let width = html2.clientWidth;
    let height = html2.clientHeight;
    let x2 = 0;
    let y3 = 0;
    if (visualViewport) {
      const layoutRelativeClientCoords = !isWebKit() || strategy === "fixed";
      if (isLayoutViewport) {
        if (!layoutRelativeClientCoords) {
          x2 = -visualViewport.offsetLeft;
          y3 = -visualViewport.offsetTop;
        }
      } else {
        width = visualViewport.width;
        height = visualViewport.height;
        if (layoutRelativeClientCoords) {
          x2 = visualViewport.offsetLeft;
          y3 = visualViewport.offsetTop;
        }
      }
    }
    const windowScrollbarX = getWindowScrollBarX(html2);
    if (windowScrollbarX <= 0) {
      const doc = html2.ownerDocument;
      const body = doc.body;
      const bodyStyles = getComputedStyle(body);
      const bodyMarginInline = doc.compatMode === "CSS1Compat" ? parseFloat(bodyStyles.marginLeft) + parseFloat(bodyStyles.marginRight) || 0 : 0;
      const reservedWidth = Math.abs(html2.clientWidth - body.clientWidth - bodyMarginInline);
      const gutter = getComputedStyle(html2).scrollbarGutter === "stable both-edges" ? reservedWidth / 2 : reservedWidth;
      if (gutter <= SCROLLBAR_MAX) {
        width -= gutter;
      }
    }
    return {
      width,
      height,
      x: x2,
      y: y3
    };
  }
  function getInnerBoundingClientRect(element, strategy) {
    const clientRect = getBoundingClientRect(element, true, strategy === "fixed");
    const top = clientRect.top + element.clientTop;
    const left = clientRect.left + element.clientLeft;
    const scale = getScale(element);
    const width = element.clientWidth * scale.x;
    const height = element.clientHeight * scale.y;
    const x2 = left * scale.x;
    const y3 = top * scale.y;
    return {
      width,
      height,
      x: x2,
      y: y3
    };
  }
  function getClientRectFromClippingAncestor(element, clippingAncestor, strategy) {
    let rect;
    if (clippingAncestor === "viewport" || clippingAncestor === "layoutViewport") {
      rect = getViewportRect(element, strategy, clippingAncestor);
    } else if (clippingAncestor === "document") {
      rect = getDocumentRect(getDocumentElement(element));
    } else if (isElement(clippingAncestor)) {
      rect = getInnerBoundingClientRect(clippingAncestor, strategy);
    } else {
      const visualOffsets = getVisualOffsets(element);
      rect = {
        x: clippingAncestor.x - visualOffsets.x,
        y: clippingAncestor.y - visualOffsets.y,
        width: clippingAncestor.width,
        height: clippingAncestor.height
      };
    }
    return rectToClientRect(rect);
  }
  function getClippingElementAncestors(element, cache) {
    const cachedResult = cache.get(element);
    if (cachedResult) {
      return cachedResult;
    }
    let result = getOverflowAncestors(element, [], false).filter((el) => isElement(el) && getNodeName(el) !== "body");
    let lastKeptComputedStyle = null;
    const elementIsFixed = getComputedStyle2(element).position === "fixed";
    let currentNode = elementIsFixed ? getParentNode(element) : element;
    while (isElement(currentNode) && !isLastTraversableNode(currentNode)) {
      const computedStyle = getComputedStyle2(currentNode);
      const currentNodeIsContaining = isContainingBlock(currentNode);
      const lastPosition = lastKeptComputedStyle ? lastKeptComputedStyle.position : elementIsFixed ? "fixed" : "";
      const shouldDropCurrentNode = !currentNodeIsContaining && (lastPosition === "fixed" || lastPosition === "absolute" && computedStyle.position === "static");
      if (shouldDropCurrentNode) {
        result = result.filter((ancestor) => ancestor !== currentNode);
      } else {
        lastKeptComputedStyle = computedStyle;
      }
      currentNode = getParentNode(currentNode);
    }
    cache.set(element, result);
    return result;
  }
  function getClippingRect(_ref) {
    let {
      element,
      boundary,
      rootBoundary,
      strategy
    } = _ref;
    const elementClippingAncestors = boundary === "clippingAncestors" ? isTopLayer(element) ? [] : getClippingElementAncestors(element, this._c) : [].concat(boundary);
    const clippingAncestors = [...elementClippingAncestors, rootBoundary];
    const firstRect = getClientRectFromClippingAncestor(element, clippingAncestors[0], strategy);
    let top = firstRect.top;
    let right = firstRect.right;
    let bottom = firstRect.bottom;
    let left = firstRect.left;
    for (let i10 = 1; i10 < clippingAncestors.length; i10++) {
      const rect = getClientRectFromClippingAncestor(element, clippingAncestors[i10], strategy);
      top = max(rect.top, top);
      right = min(rect.right, right);
      bottom = min(rect.bottom, bottom);
      left = max(rect.left, left);
    }
    return {
      width: right - left,
      height: bottom - top,
      x: left,
      y: top
    };
  }
  function getDimensions(element) {
    const {
      width,
      height
    } = getCssDimensions(element);
    return {
      width,
      height
    };
  }
  function getRectRelativeToOffsetParent(element, offsetParent, strategy) {
    const isOffsetParentAnElement = isHTMLElement(offsetParent);
    const documentElement = getDocumentElement(offsetParent);
    const isFixed = strategy === "fixed";
    const rect = getBoundingClientRect(element, true, isFixed, offsetParent);
    let scroll = {
      scrollLeft: 0,
      scrollTop: 0
    };
    const offsets = createCoords(0);
    if (isOffsetParentAnElement || !isFixed) {
      if (getNodeName(offsetParent) !== "body" || isOverflowElement(documentElement)) {
        scroll = getNodeScroll(offsetParent);
      }
      if (isOffsetParentAnElement) {
        const offsetRect = getBoundingClientRect(offsetParent, true, isFixed, offsetParent);
        offsets.x = offsetRect.x + offsetParent.clientLeft;
        offsets.y = offsetRect.y + offsetParent.clientTop;
      }
    }
    if (!isOffsetParentAnElement && documentElement) {
      offsets.x = getWindowScrollBarX(documentElement);
    }
    const htmlOffset = documentElement && !isOffsetParentAnElement && !isFixed ? getHTMLOffset(documentElement, scroll) : createCoords(0);
    const x2 = rect.left + scroll.scrollLeft - offsets.x - htmlOffset.x;
    const y3 = rect.top + scroll.scrollTop - offsets.y - htmlOffset.y;
    return {
      x: x2,
      y: y3,
      width: rect.width,
      height: rect.height
    };
  }
  function isStaticPositioned(element) {
    return getComputedStyle2(element).position === "static";
  }
  function getTrueOffsetParent(element, polyfill) {
    if (!isHTMLElement(element) || getComputedStyle2(element).position === "fixed") {
      return null;
    }
    if (polyfill) {
      return polyfill(element);
    }
    let rawOffsetParent = element.offsetParent;
    if (getDocumentElement(element) === rawOffsetParent) {
      rawOffsetParent = rawOffsetParent.ownerDocument.body;
    }
    return rawOffsetParent;
  }
  function getOffsetParent(element, polyfill) {
    const win = getWindow(element);
    if (isTopLayer(element)) {
      return win;
    }
    if (!isHTMLElement(element)) {
      let svgOffsetParent = getParentNode(element);
      while (svgOffsetParent && !isLastTraversableNode(svgOffsetParent)) {
        if (isElement(svgOffsetParent) && !isStaticPositioned(svgOffsetParent)) {
          return svgOffsetParent;
        }
        svgOffsetParent = getParentNode(svgOffsetParent);
      }
      return win;
    }
    let offsetParent = getTrueOffsetParent(element, polyfill);
    while (offsetParent && isTableElement(offsetParent) && isStaticPositioned(offsetParent)) {
      offsetParent = getTrueOffsetParent(offsetParent, polyfill);
    }
    if (offsetParent && isLastTraversableNode(offsetParent) && isStaticPositioned(offsetParent) && !isContainingBlock(offsetParent)) {
      return win;
    }
    return offsetParent || getContainingBlock(element) || win;
  }
  var getElementRects = async function(data) {
    const getOffsetParentFn = this.getOffsetParent || getOffsetParent;
    const getDimensionsFn = this.getDimensions;
    const floatingDimensions = await getDimensionsFn(data.floating);
    return {
      reference: getRectRelativeToOffsetParent(data.reference, await getOffsetParentFn(data.floating), data.strategy),
      floating: {
        x: 0,
        y: 0,
        width: floatingDimensions.width,
        height: floatingDimensions.height
      }
    };
  };
  function isRTL(element) {
    return getComputedStyle2(element).direction === "rtl";
  }
  var platform = {
    convertOffsetParentRelativeRectToViewportRelativeRect,
    getDocumentElement,
    getClippingRect,
    getOffsetParent,
    getElementRects,
    getClientRects,
    getDimensions,
    getScale,
    isElement,
    isRTL
  };
  function rectsAreEqual(a4, b3) {
    return a4.x === b3.x && a4.y === b3.y && a4.width === b3.width && a4.height === b3.height;
  }
  function observeMove(element, onMove, ancestorResize) {
    let io = null;
    let timeoutId;
    const root = getDocumentElement(element);
    function cleanup() {
      var _io;
      clearTimeout(timeoutId);
      (_io = io) == null || _io.disconnect();
      io = null;
    }
    function refresh(skip, threshold) {
      if (skip === void 0) {
        skip = false;
      }
      if (threshold === void 0) {
        threshold = 1;
      }
      cleanup();
      const elementRectForRootMargin = element.getBoundingClientRect();
      const {
        left,
        top,
        width,
        height
      } = elementRectForRootMargin;
      if (!skip) {
        onMove();
      }
      if (!width || !height) {
        return;
      }
      const insetTop = floor(top);
      const insetRight = floor(root.clientWidth - (left + width));
      const insetBottom = floor(root.clientHeight - (top + height));
      const insetLeft = floor(left);
      const rootMargin = -insetTop + "px " + -insetRight + "px " + -insetBottom + "px " + -insetLeft + "px";
      const options2 = {
        rootMargin,
        threshold: max(0, min(1, threshold)) || 1
      };
      let isFirstUpdate = true;
      function handleObserve(entries) {
        const ratio = entries[0].intersectionRatio;
        if (!rectsAreEqual(elementRectForRootMargin, element.getBoundingClientRect())) {
          return refresh();
        }
        if (ratio !== threshold) {
          if (!isFirstUpdate) {
            return refresh();
          }
          if (!ratio) {
            timeoutId = setTimeout(() => {
              refresh(false, 1e-7);
            }, 1e3);
          } else {
            refresh(false, ratio);
          }
        }
        isFirstUpdate = false;
      }
      try {
        io = new IntersectionObserver(handleObserve, {
          ...options2,
          // Handle <iframe>s
          root: root.ownerDocument
        });
      } catch (_e) {
        io = new IntersectionObserver(handleObserve, options2);
      }
      io.observe(element);
    }
    const win = getWindow(element);
    const handleResize = () => refresh(ancestorResize);
    win.addEventListener("resize", handleResize);
    refresh(true);
    return () => {
      win.removeEventListener("resize", handleResize);
      cleanup();
    };
  }
  function autoUpdate(reference, floating, update2, options2) {
    if (options2 === void 0) {
      options2 = {};
    }
    const {
      ancestorScroll = true,
      ancestorResize = true,
      elementResize = typeof ResizeObserver === "function",
      layoutShift = typeof IntersectionObserver === "function",
      animationFrame = false
    } = options2;
    const referenceEl = unwrapElement(reference);
    const ancestors = ancestorScroll || ancestorResize ? [...referenceEl ? getOverflowAncestors(referenceEl) : [], ...floating ? getOverflowAncestors(floating) : []] : [];
    ancestors.forEach((ancestor) => {
      ancestorScroll && ancestor.addEventListener("scroll", update2);
      ancestorResize && ancestor.addEventListener("resize", update2);
    });
    const cleanupIo = referenceEl && layoutShift ? observeMove(referenceEl, update2, ancestorResize) : null;
    let reobserveFrame = -1;
    let resizeObserver = null;
    if (elementResize) {
      resizeObserver = new ResizeObserver((_ref) => {
        let [firstEntry] = _ref;
        if (firstEntry && firstEntry.target === referenceEl && resizeObserver && floating) {
          resizeObserver.unobserve(floating);
          cancelAnimationFrame(reobserveFrame);
          reobserveFrame = requestAnimationFrame(() => {
            var _resizeObserver;
            (_resizeObserver = resizeObserver) == null || _resizeObserver.observe(floating);
          });
        }
        update2();
      });
      if (referenceEl && !animationFrame) {
        resizeObserver.observe(referenceEl);
      }
      if (floating) {
        resizeObserver.observe(floating);
      }
    }
    let frameId;
    let prevRefRect = animationFrame ? getBoundingClientRect(reference) : null;
    if (animationFrame) {
      frameLoop();
    }
    function frameLoop() {
      const nextRefRect = getBoundingClientRect(reference);
      if (prevRefRect && !rectsAreEqual(prevRefRect, nextRefRect)) {
        update2();
      }
      prevRefRect = nextRefRect;
      frameId = requestAnimationFrame(frameLoop);
    }
    update2();
    return () => {
      var _resizeObserver2;
      ancestors.forEach((ancestor) => {
        ancestorScroll && ancestor.removeEventListener("scroll", update2);
        ancestorResize && ancestor.removeEventListener("resize", update2);
      });
      cleanupIo == null || cleanupIo();
      (_resizeObserver2 = resizeObserver) == null || _resizeObserver2.disconnect();
      resizeObserver = null;
      if (animationFrame) {
        cancelAnimationFrame(frameId);
      }
    };
  }
  var offset2 = offset;
  var shift2 = shift;
  var flip3 = flip2;
  var size2 = size;
  var arrow2 = arrow;
  var computePosition2 = (reference, floating, options2) => {
    const cache = /* @__PURE__ */ new Map();
    const mergedOptions = options2 != null ? options2 : {};
    const platformWithCache = {
      ...platform,
      ...mergedOptions.platform,
      _c: cache
    };
    return computePosition(reference, floating, {
      ...mergedOptions,
      platform: platformWithCache
    });
  };

  // node_modules/composed-offset-position/dist/composed-offset-position.browser.min.mjs
  function e8(t8) {
    return i8(t8);
  }
  function r8(t8) {
    return t8.assignedSlot ? t8.assignedSlot : t8.parentNode instanceof ShadowRoot ? t8.parentNode.host : t8.parentNode;
  }
  function i8(e12) {
    for (let t8 = e12; t8; t8 = r8(t8)) if (t8 instanceof Element && "none" === getComputedStyle(t8).display) return null;
    for (let n9 = r8(e12); n9; n9 = r8(n9)) {
      if (!(n9 instanceof Element)) continue;
      const e13 = getComputedStyle(n9);
      if ("contents" !== e13.display) {
        if ("static" !== e13.position || isContainingBlock(e13)) return n9;
        if ("BODY" === n9.tagName) return n9;
      }
    }
    return null;
  }

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.7QB3EXDD.js
  function isVirtualElement(e12) {
    return e12 !== null && typeof e12 === "object" && "getBoundingClientRect" in e12 && ("contextElement" in e12 ? e12 instanceof Element : true);
  }
  var SUPPORTS_POPOVER = Boolean(
    globalThis?.HTMLElement?.prototype && Object.hasOwn(globalThis.HTMLElement.prototype, "popover")
  );
  var CsPopup = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.localize = new LocalizeController2(this);
      this.SUPPORTS_POPOVER = false;
      this.active = false;
      this.placement = "top";
      this.boundary = "viewport";
      this.distance = 0;
      this.skidding = 0;
      this.arrow = false;
      this.arrowPlacement = "anchor";
      this.arrowPadding = 10;
      this.flip = false;
      this.flipFallbackPlacements = "";
      this.flipFallbackStrategy = "best-fit";
      this.flipPadding = 0;
      this.shift = false;
      this.shiftPadding = 0;
      this.autoSizePadding = 0;
      this.hoverBridge = false;
      this.updateHoverBridge = () => {
        if (this.hoverBridge && this.anchorEl && this.popup) {
          const anchorRect = this.anchorEl.getBoundingClientRect();
          const popupRect = this.popup.getBoundingClientRect();
          const isVertical = this.placement.includes("top") || this.placement.includes("bottom");
          let topLeftX;
          let topLeftY;
          let topRightX;
          let topRightY;
          let bottomLeftX;
          let bottomLeftY;
          let bottomRightX;
          let bottomRightY;
          if (isVertical) {
            if (anchorRect.top < popupRect.top) {
              topLeftX = anchorRect.left;
              topLeftY = anchorRect.bottom;
              topRightX = anchorRect.right;
              topRightY = anchorRect.bottom;
              bottomLeftX = popupRect.left;
              bottomLeftY = popupRect.top;
              bottomRightX = popupRect.right;
              bottomRightY = popupRect.top;
            } else {
              topLeftX = popupRect.left;
              topLeftY = popupRect.bottom;
              topRightX = popupRect.right;
              topRightY = popupRect.bottom;
              bottomLeftX = anchorRect.left;
              bottomLeftY = anchorRect.top;
              bottomRightX = anchorRect.right;
              bottomRightY = anchorRect.top;
            }
          } else {
            if (anchorRect.left < popupRect.left) {
              topLeftX = anchorRect.right;
              topLeftY = anchorRect.top;
              topRightX = popupRect.left;
              topRightY = popupRect.top;
              bottomLeftX = anchorRect.right;
              bottomLeftY = anchorRect.bottom;
              bottomRightX = popupRect.left;
              bottomRightY = popupRect.bottom;
            } else {
              topLeftX = popupRect.right;
              topLeftY = popupRect.top;
              topRightX = anchorRect.left;
              topRightY = anchorRect.top;
              bottomLeftX = popupRect.right;
              bottomLeftY = popupRect.bottom;
              bottomRightX = anchorRect.left;
              bottomRightY = anchorRect.bottom;
            }
          }
          this.style.setProperty("--hover-bridge-top-left-x", `${topLeftX}px`);
          this.style.setProperty("--hover-bridge-top-left-y", `${topLeftY}px`);
          this.style.setProperty("--hover-bridge-top-right-x", `${topRightX}px`);
          this.style.setProperty("--hover-bridge-top-right-y", `${topRightY}px`);
          this.style.setProperty("--hover-bridge-bottom-left-x", `${bottomLeftX}px`);
          this.style.setProperty("--hover-bridge-bottom-left-y", `${bottomLeftY}px`);
          this.style.setProperty("--hover-bridge-bottom-right-x", `${bottomRightX}px`);
          this.style.setProperty("--hover-bridge-bottom-right-y", `${bottomRightY}px`);
        }
      };
    }
    async connectedCallback() {
      super.connectedCallback();
      await this.updateComplete;
      this.SUPPORTS_POPOVER = SUPPORTS_POPOVER;
      this.start();
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this.stop();
    }
    async updated(changedProperties) {
      super.updated(changedProperties);
      if (changedProperties.has("active")) {
        if (this.active) {
          this.start();
        } else {
          this.stop();
        }
      }
      if (changedProperties.has("anchor")) {
        this.handleAnchorChange();
      }
      if (this.active) {
        await this.updateComplete;
        this.reposition();
      }
    }
    async handleAnchorChange() {
      await this.stop();
      if (this.anchor && typeof this.anchor === "string") {
        const root = this.getRootNode();
        this.anchorEl = root.getElementById(this.anchor);
      } else if (this.anchor instanceof Element || isVirtualElement(this.anchor)) {
        this.anchorEl = this.anchor;
      } else {
        this.anchorEl = this.querySelector('[slot="anchor"]');
      }
      if (this.anchorEl instanceof HTMLSlotElement) {
        this.anchorEl = this.anchorEl.assignedElements({ flatten: true })[0];
      }
      if (this.anchorEl) {
        this.start();
      }
    }
    start() {
      if (!this.anchorEl || !this.active || !this.isConnected) {
        return;
      }
      this.popup?.showPopover?.();
      this.cleanup = autoUpdate(this.anchorEl, this.popup, () => {
        this.reposition();
      });
    }
    async stop() {
      return new Promise((resolve) => {
        this.popup?.hidePopover?.();
        if (this.cleanup) {
          this.cleanup();
          this.cleanup = void 0;
          this.removeAttribute("data-current-placement");
          this.style.removeProperty("--auto-size-available-width");
          this.style.removeProperty("--auto-size-available-height");
          requestAnimationFrame(() => resolve());
        } else {
          resolve();
        }
      });
    }
    /** Forces the popup to recalculate and reposition itself. */
    reposition() {
      if (!this.active || !this.anchorEl || !this.popup) {
        return;
      }
      const middleware = [
        // The offset middleware goes first
        offset2({ mainAxis: this.distance, crossAxis: this.skidding })
      ];
      if (this.sync) {
        middleware.push(
          size2({
            apply: ({ rects }) => {
              const syncWidth = this.sync === "width" || this.sync === "both";
              const syncHeight = this.sync === "height" || this.sync === "both";
              this.popup.style.width = syncWidth ? `${rects.reference.width}px` : "";
              this.popup.style.height = syncHeight ? `${rects.reference.height}px` : "";
            }
          })
        );
      } else {
        this.popup.style.width = "";
        this.popup.style.height = "";
      }
      let defaultBoundary;
      if (this.SUPPORTS_POPOVER && !isVirtualElement(this.anchor) && this.boundary === "scroll") {
        defaultBoundary = getOverflowAncestors(this.anchorEl).filter((el) => el instanceof Element);
      }
      if (this.flip) {
        middleware.push(
          flip3({
            boundary: this.flipBoundary || defaultBoundary,
            // @ts-expect-error - We're converting a string attribute to an array here
            fallbackPlacements: this.flipFallbackPlacements,
            fallbackStrategy: this.flipFallbackStrategy === "best-fit" ? "bestFit" : "initialPlacement",
            padding: this.flipPadding
          })
        );
      }
      if (this.shift) {
        middleware.push(
          shift2({
            boundary: this.shiftBoundary || defaultBoundary,
            padding: this.shiftPadding
          })
        );
      }
      if (this.autoSize) {
        middleware.push(
          size2({
            boundary: this.autoSizeBoundary || defaultBoundary,
            padding: this.autoSizePadding,
            apply: ({ availableWidth, availableHeight }) => {
              if (this.autoSize === "vertical" || this.autoSize === "both") {
                this.style.setProperty("--auto-size-available-height", `${availableHeight}px`);
              } else {
                this.style.removeProperty("--auto-size-available-height");
              }
              if (this.autoSize === "horizontal" || this.autoSize === "both") {
                this.style.setProperty("--auto-size-available-width", `${availableWidth}px`);
              } else {
                this.style.removeProperty("--auto-size-available-width");
              }
            }
          })
        );
      } else {
        this.style.removeProperty("--auto-size-available-width");
        this.style.removeProperty("--auto-size-available-height");
      }
      if (this.arrow) {
        middleware.push(
          arrow2({
            element: this.arrowEl,
            padding: this.arrowPadding
          })
        );
      }
      const getOffsetParent2 = this.SUPPORTS_POPOVER ? (element) => platform.getOffsetParent(element, e8) : platform.getOffsetParent;
      computePosition2(this.anchorEl, this.popup, {
        placement: this.placement,
        middleware,
        strategy: this.SUPPORTS_POPOVER ? "absolute" : "fixed",
        platform: {
          ...platform,
          getOffsetParent: getOffsetParent2
        }
      }).then(({ x: x2, y: y3, middlewareData, placement }) => {
        const isRtl = this.localize.dir() === "rtl";
        const staticSide = { top: "bottom", right: "left", bottom: "top", left: "right" }[placement.split("-")[0]];
        this.setAttribute("data-current-placement", placement);
        Object.assign(this.popup.style, {
          left: `${x2}px`,
          top: `${y3}px`
        });
        if (this.arrow) {
          const arrowX = middlewareData.arrow.x;
          const arrowY = middlewareData.arrow.y;
          let top = "";
          let right = "";
          let bottom = "";
          let left;
          if (this.arrowPlacement === "start") {
            const value = typeof arrowX === "number" ? `calc(${this.arrowPadding}px - var(--arrow-padding-offset))` : "";
            top = typeof arrowY === "number" ? `calc(${this.arrowPadding}px - var(--arrow-padding-offset))` : "";
            right = isRtl ? value : "";
            left = isRtl ? "" : value;
          } else if (this.arrowPlacement === "end") {
            const value = typeof arrowX === "number" ? `calc(${this.arrowPadding}px - var(--arrow-padding-offset))` : "";
            right = isRtl ? "" : value;
            left = isRtl ? value : "";
            bottom = typeof arrowY === "number" ? `calc(${this.arrowPadding}px - var(--arrow-padding-offset))` : "";
          } else if (this.arrowPlacement === "center") {
            left = typeof arrowX === "number" ? `calc(50% - var(--arrow-size-diagonal))` : "";
            top = typeof arrowY === "number" ? `calc(50% - var(--arrow-size-diagonal))` : "";
          } else {
            left = typeof arrowX === "number" ? `${arrowX}px` : "";
            top = typeof arrowY === "number" ? `${arrowY}px` : "";
          }
          Object.assign(this.arrowEl.style, {
            top,
            right,
            bottom,
            left,
            [staticSide]: "calc(var(--arrow-base-offset) - var(--arrow-size-diagonal))"
          });
        }
      });
      requestAnimationFrame(() => this.updateHoverBridge());
      this.dispatchEvent(new CsRepositionEvent());
    }
    render() {
      return b2`
      <slot name="anchor" @slotchange=${this.handleAnchorChange}></slot>

      <span
        part="hover-bridge"
        class=${e7({
        "popup-hover-bridge": true,
        "popup-hover-bridge-visible": this.hoverBridge && this.active
      })}
      ></span>

      <div
        popover="manual"
        part="popup"
        class=${e7({
        popup: true,
        "popup-active": this.active,
        "popup-fixed": !this.SUPPORTS_POPOVER,
        "popup-has-arrow": this.arrow
      })}
      >
        <slot></slot>
        ${this.arrow ? b2`<div part="arrow" class="arrow" role="presentation"></div>` : ""}
      </div>
    `;
    }
  };
  CsPopup.css = popup_styles_default;
  __decorateClass([
    e5(".popup")
  ], CsPopup.prototype, "popup", 2);
  __decorateClass([
    e5(".arrow")
  ], CsPopup.prototype, "arrowEl", 2);
  __decorateClass([
    n4({ attribute: false, type: Boolean })
  ], CsPopup.prototype, "SUPPORTS_POPOVER", 2);
  __decorateClass([
    n4()
  ], CsPopup.prototype, "anchor", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsPopup.prototype, "active", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsPopup.prototype, "placement", 2);
  __decorateClass([
    n4()
  ], CsPopup.prototype, "boundary", 2);
  __decorateClass([
    n4({ type: Number })
  ], CsPopup.prototype, "distance", 2);
  __decorateClass([
    n4({ type: Number })
  ], CsPopup.prototype, "skidding", 2);
  __decorateClass([
    n4({ type: Boolean })
  ], CsPopup.prototype, "arrow", 2);
  __decorateClass([
    n4({ attribute: "arrow-placement" })
  ], CsPopup.prototype, "arrowPlacement", 2);
  __decorateClass([
    n4({ attribute: "arrow-padding", type: Number })
  ], CsPopup.prototype, "arrowPadding", 2);
  __decorateClass([
    n4({ type: Boolean })
  ], CsPopup.prototype, "flip", 2);
  __decorateClass([
    n4({
      attribute: "flip-fallback-placements",
      converter: {
        fromAttribute: (value) => {
          return value.split(" ").map((p4) => p4.trim()).filter((p4) => p4 !== "");
        },
        toAttribute: (value) => {
          return value.join(" ");
        }
      }
    })
  ], CsPopup.prototype, "flipFallbackPlacements", 2);
  __decorateClass([
    n4({ attribute: "flip-fallback-strategy" })
  ], CsPopup.prototype, "flipFallbackStrategy", 2);
  __decorateClass([
    n4({ type: Object })
  ], CsPopup.prototype, "flipBoundary", 2);
  __decorateClass([
    n4({ attribute: "flip-padding", type: Number })
  ], CsPopup.prototype, "flipPadding", 2);
  __decorateClass([
    n4({ type: Boolean })
  ], CsPopup.prototype, "shift", 2);
  __decorateClass([
    n4({ type: Object })
  ], CsPopup.prototype, "shiftBoundary", 2);
  __decorateClass([
    n4({ attribute: "shift-padding", type: Number })
  ], CsPopup.prototype, "shiftPadding", 2);
  __decorateClass([
    n4({ attribute: "auto-size" })
  ], CsPopup.prototype, "autoSize", 2);
  __decorateClass([
    n4()
  ], CsPopup.prototype, "sync", 2);
  __decorateClass([
    n4({ type: Object })
  ], CsPopup.prototype, "autoSizeBoundary", 2);
  __decorateClass([
    n4({ attribute: "auto-size-padding", type: Number })
  ], CsPopup.prototype, "autoSizePadding", 2);
  __decorateClass([
    n4({ attribute: "hover-bridge", type: Boolean })
  ], CsPopup.prototype, "hoverBridge", 2);
  CsPopup = __decorateClass([
    customElement("cs-popup")
  ], CsPopup);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.FZIQFJ3S.js
  var comparison_styles_default = i`
  :host {
    --divider-width: 0.125rem;
    --handle-size: 2.5rem;

    display: block;
    position: relative;
    max-width: 100%;
    max-height: 100%;
    overflow: hidden;
  }

  .before,
  .after {
    display: block;

    &::slotted(img),
    &::slotted(svg) {
      display: block;
      max-width: 100% !important;
      height: auto;
    }

    &::slotted(:not(img, svg)) {
      isolation: isolate;
    }
  }

  .after {
    position: absolute;
    top: 0;
    left: 0;
    height: 100%;
    width: 100%;
  }

  /* Disable pointer-events while dragging. This is especially important for iframes. */
  :host(:state(dragging)) {
    .before,
    .after {
      pointer-events: none;
    }
  }

  .divider {
    display: flex;
    align-items: center;
    justify-content: center;
    position: absolute;
    top: 0;
    width: var(--divider-width);
    height: 100%;
    background-color: var(--cs-color-surface-default);
    translate: calc(var(--divider-width) / -2);
    cursor: ew-resize;
  }

  .handle {
    display: flex;
    align-items: center;
    justify-content: center;
    position: absolute;
    top: calc(50% - (var(--handle-size) / 2));
    width: var(--handle-size);
    height: var(--handle-size);
    background-color: var(--cs-color-surface-default);
    border-radius: var(--cs-border-radius-circle);
    font-size: calc(var(--handle-size) * 0.4);
    color: var(--cs-color-neutral-on-quiet);
    cursor: inherit;
    z-index: 10;
  }

  .handle:focus-visible {
    outline: var(--cs-focus-ring);
    outline-offset: var(--cs-focus-ring-offset);
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.O4ZFCG4U.js
  var CsComparison = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.localize = new LocalizeController2(this);
      this.position = 50;
    }
    handleDrag(event) {
      const { width } = this.getBoundingClientRect();
      const isRtl = this.localize.dir() === "rtl";
      event.preventDefault();
      drag(this, {
        onMove: (x2) => {
          this.customStates.set("dragging", true);
          this.position = parseFloat(clamp(x2 / width * 100, 0, 100).toFixed(2));
          if (isRtl) {
            this.position = 100 - this.position;
          }
        },
        onStop: () => {
          this.customStates.set("dragging", false);
        },
        initialEvent: event
      });
    }
    handleKeyDown(event) {
      const isLtr = this.matches(":dir(ltr)");
      const isRtl = this.localize.dir() === "rtl";
      if (["ArrowLeft", "ArrowRight", "Home", "End"].includes(event.key)) {
        const incr = event.shiftKey ? 10 : 1;
        let newPosition = this.position;
        event.preventDefault();
        if (isLtr && event.key === "ArrowLeft" || isRtl && event.key === "ArrowRight") {
          newPosition -= incr;
        }
        if (isLtr && event.key === "ArrowRight" || isRtl && event.key === "ArrowLeft") {
          newPosition += incr;
        }
        if (event.key === "Home") {
          newPosition = 0;
        }
        if (event.key === "End") {
          newPosition = 100;
        }
        newPosition = clamp(newPosition, 0, 100);
        this.position = newPosition;
      }
    }
    handlePositionChange() {
      this.dispatchEvent(new Event("change", { bubbles: true, composed: true }));
    }
    render() {
      const isRtl = this.hasUpdated ? this.localize.dir() === "rtl" : this.dir === "rtl";
      return b2`
      <div id="comparison" class="image" part="comparison">
        <div part="before" class="before">
          <slot name="before"></slot>
        </div>

        <div
          part="after"
          class="after"
          style=${o7({
        clipPath: isRtl ? `inset(0 0 0 ${100 - this.position}%)` : `inset(0 ${100 - this.position}% 0 0)`
      })}
        >
          <slot name="after"></slot>
        </div>
      </div>

      <div
        part="divider"
        class="divider"
        style=${o7({
        left: isRtl ? `${100 - this.position}%` : `${this.position}%`
      })}
        @keydown=${this.handleKeyDown}
        @mousedown=${this.handleDrag}
        @touchstart=${this.handleDrag}
      >
        <div
          part="handle"
          class="handle"
          role="scrollbar"
          aria-valuenow=${this.position}
          aria-valuemin="0"
          aria-valuemax="100"
          aria-controls="comparison"
          tabindex="0"
        >
          <slot name="handle">
            <cs-icon library="system" name="drag_indicator"></cs-icon>
          </slot>
        </div>
      </div>
    `;
    }
  };
  CsComparison.css = comparison_styles_default;
  __decorateClass([
    e5(".handle")
  ], CsComparison.prototype, "handle", 2);
  __decorateClass([
    n4({ type: Number, reflect: true })
  ], CsComparison.prototype, "position", 2);
  __decorateClass([
    watch("position", { waitUntilFirstUpdate: true })
  ], CsComparison.prototype, "handlePositionChange", 1);
  CsComparison = __decorateClass([
    customElement("cs-comparison")
  ], CsComparison);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.DWYWP5LN.js
  var CsCopyEvent = class extends Event {
    constructor(detail) {
      super("cs-copy", { bubbles: true, cancelable: false, composed: true });
      this.detail = detail;
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.PFH4TBPZ.js
  var politeLog = null;
  var assertiveLog = null;
  var CLEAR_DELAY = 7e3;
  function createLog(politeness) {
    const log = document.createElement("div");
    log.setAttribute("role", "log");
    log.setAttribute("aria-live", politeness);
    log.setAttribute("aria-relevant", "additions");
    Object.assign(log.style, {
      position: "absolute",
      width: "1px",
      height: "1px",
      margin: "-1px",
      padding: "0",
      border: "0",
      overflow: "hidden",
      clip: "rect(0 0 0 0)",
      clipPath: "inset(50%)",
      whiteSpace: "nowrap"
    });
    return log;
  }
  function getLog(politeness) {
    if (politeness === "assertive") {
      assertiveLog ?? (assertiveLog = document.body.appendChild(createLog("assertive")));
      return assertiveLog;
    }
    politeLog ?? (politeLog = document.body.appendChild(createLog("polite")));
    return politeLog;
  }
  function announce(message, politeness = "polite") {
    if (!message) {
      return;
    }
    const log = getLog(politeness);
    const node = document.createElement("div");
    node.textContent = message;
    log.appendChild(node);
    setTimeout(() => node.remove(), CLEAR_DELAY);
  }

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.JPMNEQRT.js
  var copy_button_styles_default = i`
  :host {
    display: inline-block;
    color: var(--cs-color-neutral-on-quiet);
  }

  .copy-button__trigger {
    position: relative;
  }

  .button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    background-color: transparent;
    border: none;
    border-radius: var(--cs-form-control-border-radius);
    color: inherit;
    font-size: inherit;
    height: calc(var(--cs-form-control-height) * 0.8);
    aspect-ratio: 1;
    cursor: pointer;
    transition-property: background-color, color;
    transition-duration: var(--cs-transition-fast);
    transition-timing-function: var(--cs-transition-easing);
  }

  @media (hover: hover) {
    .button:hover:not([disabled]) {
      background-color: var(--cs-color-neutral-fill-quiet);
      color: color-mix(in oklab, currentColor, var(--cs-color-mix-hover));
    }
  }

  .button:focus-visible:not([disabled]) {
    background-color: var(--cs-color-neutral-fill-quiet);
    color: color-mix(in oklab, currentColor, var(--cs-color-mix-hover));
  }

  .button:active:not([disabled]) {
    color: color-mix(in oklab, currentColor, var(--cs-color-mix-active));
  }

  .button:focus-visible {
    outline: var(--cs-focus-ring);
    outline-offset: var(--cs-focus-ring-offset);
  }

  .button[disabled] {
    opacity: 0.5;
    cursor: not-allowed !important;
  }

  slot {
    display: inline-flex;
  }

  /* Icon swap animation */
  .show {
    animation: copy-button-icon-show var(--cs-transition-fast) var(--cs-transition-easing);
  }

  .hide {
    animation: copy-button-icon-show var(--cs-transition-fast) var(--cs-transition-easing) reverse;
  }

  @keyframes copy-button-icon-show {
    from {
      scale: 0.25;
      opacity: 0.25;
    }
    to {
      scale: 1;
      opacity: 1;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .show,
    .hide {
      animation-duration: 1ms;
    }
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.FAIFU5LH.js
  var INTERNAL_TOOLTIP_SLOT = "cs-internal-tooltip";
  var ASSIGNED_ID_PROP = "__waCopyButtonAssignedId";
  var CsCopyButton = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.localize = new LocalizeController2(this);
      this.isCopying = false;
      this.status = "rest";
      this.hasCustomTrigger = false;
      this.customTriggerEl = null;
      this.lightTooltip = null;
      this.feedbackTimeout = null;
      this.value = "";
      this.from = "";
      this.disabled = false;
      this.copyLabel = "";
      this.successLabel = "";
      this.errorLabel = "";
      this.feedbackDuration = 1e3;
      this.tooltipPlacement = "top";
      this.tooltip = "full";
      this.handleDefaultSlotChange = () => {
        const assigned = this.defaultSlot?.assignedElements({ flatten: true }) ?? [];
        const trigger = assigned.find((el) => el instanceof HTMLElement) ?? null;
        if (trigger !== this.customTriggerEl) {
          this.releaseAssignedId(this.customTriggerEl);
          this.customTriggerEl = trigger;
        }
        this.hasCustomTrigger = trigger !== null;
        if (trigger && this.tooltip !== "none") {
          if (!trigger.id) {
            trigger.id = uniqueId("cs-copy-button-trigger-");
            trigger[ASSIGNED_ID_PROP] = true;
          }
          this.ensureLightTooltip();
        } else {
          this.removeLightTooltip();
        }
      };
    }
    get activeTooltip() {
      return this.lightTooltip ?? this.shadowTooltip ?? null;
    }
    get currentLabel() {
      if (this.status === "success") {
        return this.successLabel || this.localize.term("copied");
      }
      if (this.status === "error") {
        return this.errorLabel || this.localize.term("error");
      }
      return this.copyLabel || this.localize.term("copy");
    }
    firstUpdated(changedProperties) {
      super.firstUpdated(changedProperties);
      if (this.didSSR) {
        this.updateComplete.then(() => {
          this.handleDefaultSlotChange();
        });
      } else {
        this.handleDefaultSlotChange();
      }
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this.removeLightTooltip();
    }
    handleStatusChange() {
      this.customStates.set("success", this.status === "success");
      this.customStates.set("error", this.status === "error");
      this.syncTooltipText();
      if (this.status === "success" || this.status === "error") {
        announce(this.currentLabel, "polite");
      }
    }
    handleLabelChange() {
      this.syncTooltipText();
    }
    handleTooltipOptionsChange() {
      if (this.lightTooltip) {
        this.lightTooltip.placement = this.tooltipPlacement;
        this.lightTooltip.disabled = this.disabled;
      }
    }
    handleTooltipModeChange(oldValue) {
      if (this.tooltip === "none") {
        this.removeLightTooltip();
      } else if (oldValue === "none") {
        this.handleDefaultSlotChange();
      } else if (this.lightTooltip) {
        this.lightTooltip.setAttribute("trigger", this.tooltip === "copy" ? "manual" : "hover focus");
      }
    }
    releaseAssignedId(el) {
      if (el && el[ASSIGNED_ID_PROP]) {
        el.removeAttribute("id");
        delete el[ASSIGNED_ID_PROP];
      }
    }
    ensureLightTooltip() {
      if (!this.customTriggerEl) {
        return;
      }
      const triggerValue = this.tooltip === "copy" ? "manual" : "hover focus";
      if (!this.lightTooltip) {
        const tooltip = document.createElement("cs-tooltip");
        tooltip.setAttribute("slot", INTERNAL_TOOLTIP_SLOT);
        tooltip.setAttribute("part", "feedback");
        tooltip.setAttribute("trigger", triggerValue);
        tooltip.dataset.copyButtonTooltip = "";
        tooltip.setAttribute("for", this.customTriggerEl.id);
        tooltip.placement = this.tooltipPlacement;
        tooltip.disabled = this.disabled;
        tooltip.textContent = this.currentLabel;
        this.appendChild(tooltip);
        this.lightTooltip = tooltip;
      } else {
        this.lightTooltip.setAttribute("for", this.customTriggerEl.id);
        this.lightTooltip.setAttribute("trigger", triggerValue);
        this.lightTooltip.placement = this.tooltipPlacement;
        this.lightTooltip.disabled = this.disabled;
        this.lightTooltip.textContent = this.currentLabel;
      }
    }
    removeLightTooltip() {
      if (this.lightTooltip) {
        this.releaseAssignedId(this.customTriggerEl);
        this.lightTooltip.remove();
        this.lightTooltip = null;
      }
    }
    syncTooltipText() {
      if (this.lightTooltip) {
        this.lightTooltip.textContent = this.currentLabel;
      }
    }
    async handleCopy() {
      if (this.disabled || this.isCopying) {
        return;
      }
      this.isCopying = true;
      let valueToCopy = this.value;
      if (this.from) {
        const root = this.getRootNode();
        const isProperty = this.from.includes(".");
        const isAttribute = this.from.includes("[") && this.from.includes("]");
        let id3 = this.from;
        let field = "";
        if (isProperty) {
          [id3, field] = this.from.trim().split(".");
        } else if (isAttribute) {
          [id3, field] = this.from.trim().replace(/\]$/, "").split("[");
        }
        const target = "getElementById" in root ? root.getElementById(id3) : null;
        if (target) {
          if (isAttribute) {
            valueToCopy = target.getAttribute(field) || "";
          } else if (isProperty) {
            valueToCopy = target[field] || "";
          } else {
            valueToCopy = target.textContent || "";
          }
        } else {
          this.showStatus("error");
          this.dispatchEvent(new CsErrorEvent());
        }
      }
      if (!valueToCopy) {
        this.showStatus("error");
        this.dispatchEvent(new CsErrorEvent());
      } else {
        try {
          await navigator.clipboard.writeText(valueToCopy);
          this.showStatus("success");
          this.dispatchEvent(new CsCopyEvent({ value: valueToCopy }));
        } catch {
          this.showStatus("error");
          this.dispatchEvent(new CsErrorEvent());
        }
      }
    }
    async showStatus(status) {
      this.status = status;
      if (this.copyIcon) {
        const iconToShow = status === "success" ? this.successIcon : this.errorIcon;
        await animateWithClass(this.copyIcon, "hide");
        this.copyIcon.hidden = true;
        iconToShow.hidden = false;
        await animateWithClass(iconToShow, "show");
      }
      await this.updateComplete;
      const tooltip = this.tooltip === "none" ? null : this.activeTooltip;
      let earlyClose = null;
      if (tooltip) {
        tooltip.show();
        earlyClose = new Promise((resolve) => {
          tooltip.addEventListener(
            "cs-after-hide",
            () => {
              if (this.feedbackTimeout !== null) {
                clearTimeout(this.feedbackTimeout);
                this.feedbackTimeout = null;
              }
              resolve();
            },
            { once: true }
          );
        });
        this.feedbackTimeout = window.setTimeout(async () => {
          this.feedbackTimeout = null;
          await tooltip.hide();
        }, this.feedbackDuration);
      }
      setTimeout(async () => {
        if (earlyClose) {
          await earlyClose;
        }
        if (this.copyIcon) {
          const iconToShow = status === "success" ? this.successIcon : this.errorIcon;
          await animateWithClass(iconToShow, "hide");
          iconToShow.hidden = true;
          this.copyIcon.hidden = false;
          await animateWithClass(this.copyIcon, "show");
        }
        this.status = "rest";
        this.isCopying = false;
      }, this.feedbackDuration);
    }
    render() {
      const hasCustomTrigger = this.hasCustomTrigger;
      let showTooltip = !hasCustomTrigger && this.tooltip !== "none";
      const triggerValue = this.tooltip === "copy" ? "manual" : "hover focus";
      if (this.didSSR && !this.hasUpdated) {
        showTooltip = false;
      }
      return b2`
      <div class="copy-button__trigger" @click=${this.handleCopy}>
        <slot @slotchange=${this.handleDefaultSlotChange}></slot>
        <button
          class="button"
          part="button"
          type="button"
          id="copy-button"
          aria-label=${this.currentLabel}
          ?disabled=${this.disabled}
          ?hidden=${this.hasCustomTrigger}
        >
          <slot part="copy-icon" name="copy-icon">
            <cs-icon library="system" name="content_copy"></cs-icon>
          </slot>
          <slot part="success-icon" name="success-icon" hidden>
            <cs-icon library="system" name="check"></cs-icon>
          </slot>
          <slot part="error-icon" name="error-icon" hidden>
            <cs-icon library="system" name="close"></cs-icon>
          </slot>
        </button>

        ${showTooltip ? b2`
                <cs-tooltip
                  part="feedback"
                  for="copy-button"
                  placement=${this.tooltipPlacement}
                  trigger=${triggerValue}
                  class=${e7({
        "copy-button-tooltip": true,
        "copy-button-tooltip-success": this.status === "success",
        "copy-button-tooltip-error": this.status === "error"
      })}
                  ?disabled=${this.disabled}
                  >${this.currentLabel}</cs-tooltip
                >
              ` : ""}
        <slot name="${INTERNAL_TOOLTIP_SLOT}"></slot>
      </div>
    `;
    }
  };
  CsCopyButton.css = [host_styles_default, visually_hidden_styles_default, copy_button_styles_default];
  __decorateClass([
    e5('slot[name="copy-icon"]')
  ], CsCopyButton.prototype, "copyIcon", 2);
  __decorateClass([
    e5('slot[name="success-icon"]')
  ], CsCopyButton.prototype, "successIcon", 2);
  __decorateClass([
    e5('slot[name="error-icon"]')
  ], CsCopyButton.prototype, "errorIcon", 2);
  __decorateClass([
    e5("slot:not([name])")
  ], CsCopyButton.prototype, "defaultSlot", 2);
  __decorateClass([
    e5('cs-tooltip[part="feedback"]')
  ], CsCopyButton.prototype, "shadowTooltip", 2);
  __decorateClass([
    r5()
  ], CsCopyButton.prototype, "isCopying", 2);
  __decorateClass([
    r5()
  ], CsCopyButton.prototype, "status", 2);
  __decorateClass([
    r5()
  ], CsCopyButton.prototype, "hasCustomTrigger", 2);
  __decorateClass([
    n4()
  ], CsCopyButton.prototype, "value", 2);
  __decorateClass([
    n4()
  ], CsCopyButton.prototype, "from", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsCopyButton.prototype, "disabled", 2);
  __decorateClass([
    n4({ attribute: "copy-label" })
  ], CsCopyButton.prototype, "copyLabel", 2);
  __decorateClass([
    n4({ attribute: "success-label" })
  ], CsCopyButton.prototype, "successLabel", 2);
  __decorateClass([
    n4({ attribute: "error-label" })
  ], CsCopyButton.prototype, "errorLabel", 2);
  __decorateClass([
    n4({ attribute: "feedback-duration", type: Number })
  ], CsCopyButton.prototype, "feedbackDuration", 2);
  __decorateClass([
    n4({ attribute: "tooltip-placement", reflect: true })
  ], CsCopyButton.prototype, "tooltipPlacement", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsCopyButton.prototype, "tooltip", 2);
  __decorateClass([
    watch("status")
  ], CsCopyButton.prototype, "handleStatusChange", 1);
  __decorateClass([
    watch(["copyLabel", "successLabel", "errorLabel"])
  ], CsCopyButton.prototype, "handleLabelChange", 1);
  __decorateClass([
    watch(["tooltipPlacement", "disabled"], { waitUntilFirstUpdate: true })
  ], CsCopyButton.prototype, "handleTooltipOptionsChange", 1);
  __decorateClass([
    watch("tooltip", { waitUntilFirstUpdate: true })
  ], CsCopyButton.prototype, "handleTooltipModeChange", 1);
  CsCopyButton = __decorateClass([
    customElement("cs-copy-button")
  ], CsCopyButton);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.UCKV5SOL.js
  var tooltip_styles_default = i`
  :host {
    --max-width: 30ch;

    /** These styles are added so we don't interfere in the DOM. */
    display: inline-block;
    position: absolute;

    /** Defaults for inherited CSS properties */
    color: var(--cs-tooltip-content-color);
    font-size: var(--cs-tooltip-font-size);
    line-height: var(--cs-tooltip-line-height);
    text-align: start;
    white-space: normal;
  }

  .tooltip {
    --arrow-size: var(--cs-tooltip-arrow-size);
    --arrow-color: var(--cs-tooltip-background-color);
  }

  .tooltip::part(popup) {
    z-index: 1000;
  }

  .tooltip[placement^='top']::part(popup) {
    transform-origin: bottom;
  }

  .tooltip[placement^='bottom']::part(popup) {
    transform-origin: top;
  }

  .tooltip[placement^='left']::part(popup) {
    transform-origin: right;
  }

  .tooltip[placement^='right']::part(popup) {
    transform-origin: left;
  }

  .body {
    display: block;
    width: max-content;
    max-width: var(--max-width);
    border-radius: var(--cs-tooltip-border-radius);
    background-color: var(--cs-tooltip-background-color);
    border: var(--cs-tooltip-border-width) var(--cs-tooltip-border-style) var(--cs-tooltip-border-color);
    padding: 0.25em 0.5em;
    user-select: none;
    -webkit-user-select: none;
  }

  .tooltip {
    --popup-border-width: var(--cs-tooltip-border-width);

    /* Inset box-shadow, not a border: Safari seams a clip-path edge that runs along a border. */
    &::part(arrow) {
      box-shadow: inset calc(-1 * var(--cs-tooltip-border-width)) calc(-1 * var(--cs-tooltip-border-width)) 0 0
        var(--cs-tooltip-border-color);
    }
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.3ZOOG3MW.js
  var CsShowEvent = class extends Event {
    constructor() {
      super("cs-show", { bubbles: true, cancelable: true, composed: true });
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.4UXBEDUP.js
  var CsHideEvent = class extends Event {
    constructor(detail) {
      super("cs-hide", { bubbles: true, cancelable: true, composed: true });
      this.detail = detail;
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.NTVMNT4T.js
  var CsAfterHideEvent = class extends Event {
    constructor() {
      super("cs-after-hide", { bubbles: true, cancelable: false, composed: true });
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.UKTDLFCP.js
  var CsAfterShowEvent = class extends Event {
    constructor() {
      super("cs-after-show", { bubbles: true, cancelable: false, composed: true });
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.TT7CEBMQ.js
  var CsTooltip = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.placement = "top";
      this.disabled = false;
      this.distance = 8;
      this.open = false;
      this.skidding = 0;
      this.showDelay = 150;
      this.hideDelay = 0;
      this.trigger = "hover focus";
      this.withoutArrow = false;
      this.for = null;
      this.anchor = null;
      this.eventController = new AbortController();
      this.handleBlur = () => {
        if (this.hasTrigger("focus")) {
          this.hide();
        }
      };
      this.handleClick = () => {
        if (this.hasTrigger("click")) {
          if (this.open) {
            this.hide();
          } else {
            this.show();
          }
        }
      };
      this.handleFocus = () => {
        if (this.hasTrigger("focus")) {
          this.show();
        }
      };
      this.handleDocumentKeyDown = (event) => {
        if (event.key === "Escape" && this.open && isTopDismissible(this)) {
          event.preventDefault();
          event.stopPropagation();
          this.hide();
        }
      };
      this.handleMouseOver = () => {
        if (this.hasTrigger("hover")) {
          clearTimeout(this.hoverTimeout);
          this.hoverTimeout = window.setTimeout(() => this.show(), this.showDelay);
        }
      };
      this.handleMouseOut = (event) => {
        if (this.hasTrigger("hover")) {
          const relatedTarget = event.relatedTarget;
          const movedIntoAnchor = Boolean(relatedTarget && this.anchor?.contains(relatedTarget));
          const movedIntoTooltip = Boolean(relatedTarget && this.contains(relatedTarget));
          if (movedIntoAnchor || movedIntoTooltip) {
            return;
          }
          clearTimeout(this.hoverTimeout);
          this.hoverTimeout = window.setTimeout(() => {
            this.hide();
          }, this.hideDelay);
        }
      };
    }
    connectedCallback() {
      super.connectedCallback();
      const isClient2 = typeof document !== "undefined";
      if (isClient2) {
        if (this.eventController.signal.aborted) {
          this.eventController = new AbortController();
        }
        this.addEventListener("mouseout", this.handleMouseOut);
        if (this.open) {
          this.open = false;
          this.updateComplete.then(() => {
            this.open = true;
          });
        }
        if (!this.id) {
          this.id = uniqueId("cs-tooltip-");
        }
        if (this.for && this.anchor) {
          this.anchor = null;
          this.handleForChange();
        } else if (this.for) {
          this.handleForChange();
        }
      }
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      document.removeEventListener("keydown", this.handleDocumentKeyDown);
      unregisterDismissible(this);
      this.eventController.abort();
      if (this.anchor) {
        this.removeFromAriaLabelledBy(this.anchor, this.id);
      }
    }
    firstUpdated(changedProperties) {
      this.body.hidden = !this.open;
      if (this.open) {
        this.popup.active = true;
        this.popup.reposition();
      }
      super.firstUpdated(changedProperties);
    }
    hasTrigger(triggerType) {
      const triggers = this.trigger.split(" ");
      return triggers.includes(triggerType);
    }
    /** Adds the tooltip ID to the aria-labelledby attribute */
    addToAriaLabelledBy(element, id3) {
      const currentLabel = element.getAttribute("aria-labelledby") || "";
      const labels = currentLabel.split(/\s+/).filter(Boolean);
      if (!labels.includes(id3)) {
        labels.push(id3);
        element.setAttribute("aria-labelledby", labels.join(" "));
      }
    }
    /** Removes the tooltip ID from the aria-labelledby attribute */
    removeFromAriaLabelledBy(element, id3) {
      const currentLabel = element.getAttribute("aria-labelledby") || "";
      const labels = currentLabel.split(/\s+/).filter(Boolean);
      const filteredLabels = labels.filter((label) => label !== id3);
      if (filteredLabels.length > 0) {
        element.setAttribute("aria-labelledby", filteredLabels.join(" "));
      } else {
        element.removeAttribute("aria-labelledby");
      }
    }
    async handleOpenChange() {
      if (this.open) {
        if (this.disabled) {
          return;
        }
        const csShowEvent = new CsShowEvent();
        this.dispatchEvent(csShowEvent);
        if (csShowEvent.defaultPrevented) {
          this.open = false;
          return;
        }
        document.addEventListener("keydown", this.handleDocumentKeyDown, { signal: this.eventController.signal });
        registerDismissible(this);
        this.body.hidden = false;
        this.popup.active = true;
        await animateWithClass(this.popup.popup, "show-with-scale");
        this.popup.reposition();
        this.dispatchEvent(new CsAfterShowEvent());
      } else {
        const csHideEvent = new CsHideEvent();
        this.dispatchEvent(csHideEvent);
        if (csHideEvent.defaultPrevented) {
          this.open = false;
          return;
        }
        document.removeEventListener("keydown", this.handleDocumentKeyDown);
        unregisterDismissible(this);
        await animateWithClass(this.popup.popup, "hide-with-scale");
        this.popup.active = false;
        this.body.hidden = true;
        this.dispatchEvent(new CsAfterHideEvent());
      }
    }
    handleForChange() {
      const rootNode = this.getRootNode?.();
      if (!rootNode) {
        return;
      }
      const newAnchor = this.for ? rootNode.getElementById?.(this.for) : null;
      const oldAnchor = this.anchor;
      if (newAnchor === oldAnchor) {
        return;
      }
      const { signal } = this.eventController;
      if (newAnchor) {
        this.addToAriaLabelledBy(newAnchor, this.id);
        newAnchor.addEventListener("blur", this.handleBlur, { capture: true, signal });
        newAnchor.addEventListener("focus", this.handleFocus, { capture: true, signal });
        newAnchor.addEventListener("click", this.handleClick, { signal });
        newAnchor.addEventListener("mouseover", this.handleMouseOver, { signal });
        newAnchor.addEventListener("mouseout", this.handleMouseOut, { signal });
      }
      if (oldAnchor) {
        this.removeFromAriaLabelledBy(oldAnchor, this.id);
        oldAnchor.removeEventListener("blur", this.handleBlur, { capture: true });
        oldAnchor.removeEventListener("focus", this.handleFocus, { capture: true });
        oldAnchor.removeEventListener("click", this.handleClick);
        oldAnchor.removeEventListener("mouseover", this.handleMouseOver);
        oldAnchor.removeEventListener("mouseout", this.handleMouseOut);
      }
      this.anchor = newAnchor;
    }
    async handleOptionsChange() {
      if (this.hasUpdated) {
        await this.updateComplete;
        this.popup.reposition();
      }
    }
    handleDisabledChange() {
      if (this.disabled && this.open) {
        this.hide();
      }
    }
    /** Shows the tooltip. */
    async show() {
      if (this.open) {
        return void 0;
      }
      this.open = true;
      return waitForEvent(this, "cs-after-show");
    }
    /** Hides the tooltip */
    async hide() {
      if (!this.open) {
        return void 0;
      }
      this.open = false;
      return waitForEvent(this, "cs-after-hide");
    }
    render() {
      return b2`
      <cs-popup
        part="tooltip"
        exportparts="
          popup:tooltip__popup,
          arrow:tooltip__arrow
        "
        class=${e7({
        tooltip: true,
        "tooltip-open": this.open
      })}
        placement=${this.placement}
        distance=${this.distance}
        skidding=${this.skidding}
        flip
        shift
        ?arrow=${!this.withoutArrow}
        hover-bridge
        .anchor=${this.anchor}
      >
        <div part="body" class="body">
          <slot></slot>
        </div>
      </cs-popup>
    `;
    }
  };
  CsTooltip.css = tooltip_styles_default;
  CsTooltip.dependencies = { "cs-popup": CsPopup };
  __decorateClass([
    e5("slot:not([name])")
  ], CsTooltip.prototype, "defaultSlot", 2);
  __decorateClass([
    e5(".body")
  ], CsTooltip.prototype, "body", 2);
  __decorateClass([
    e5("cs-popup")
  ], CsTooltip.prototype, "popup", 2);
  __decorateClass([
    n4()
  ], CsTooltip.prototype, "placement", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsTooltip.prototype, "disabled", 2);
  __decorateClass([
    n4({ type: Number })
  ], CsTooltip.prototype, "distance", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsTooltip.prototype, "open", 2);
  __decorateClass([
    n4({ type: Number })
  ], CsTooltip.prototype, "skidding", 2);
  __decorateClass([
    n4({ attribute: "show-delay", type: Number })
  ], CsTooltip.prototype, "showDelay", 2);
  __decorateClass([
    n4({ attribute: "hide-delay", type: Number })
  ], CsTooltip.prototype, "hideDelay", 2);
  __decorateClass([
    n4()
  ], CsTooltip.prototype, "trigger", 2);
  __decorateClass([
    n4({ attribute: "without-arrow", type: Boolean, reflect: true })
  ], CsTooltip.prototype, "withoutArrow", 2);
  __decorateClass([
    n4()
  ], CsTooltip.prototype, "for", 2);
  __decorateClass([
    r5()
  ], CsTooltip.prototype, "anchor", 2);
  __decorateClass([
    watch("open", { waitUntilFirstUpdate: true })
  ], CsTooltip.prototype, "handleOpenChange", 1);
  __decorateClass([
    watch("for")
  ], CsTooltip.prototype, "handleForChange", 1);
  __decorateClass([
    watch(["distance", "placement", "skidding"])
  ], CsTooltip.prototype, "handleOptionsChange", 1);
  __decorateClass([
    watch("disabled")
  ], CsTooltip.prototype, "handleDisabledChange", 1);
  CsTooltip = __decorateClass([
    customElement("cs-tooltip")
  ], CsTooltip);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.BA6B2LF7.js
  var details_styles_default = i`
  :host {
    --spacing: var(--cs-space-m);
    --show-duration: var(--cs-transition-normal);
    --hide-duration: var(--cs-transition-normal);

    display: block;
  }

  details {
    display: block;
    overflow-anchor: none;
    border: var(--cs-panel-border-width) var(--cs-color-surface-border) var(--cs-panel-border-style);
    background-color: var(--cs-color-surface-default);
    border-radius: var(--cs-panel-border-radius);
    color: var(--cs-color-text-normal);

    /* Print styles */
    @media print {
      background: none;
      border: solid var(--cs-border-width-s) var(--cs-color-surface-border);

      summary {
        list-style: none;
      }
    }
  }

  /* Appearance modifiers */
  :host([appearance='plain']) details {
    background-color: transparent;
    border-color: transparent;
    border-radius: 0;
  }

  :host([appearance='outlined']) details {
    background-color: var(--cs-color-surface-default);
    border-color: var(--cs-color-surface-border);
  }

  :host([appearance='filled']) details {
    background-color: var(--cs-color-neutral-fill-quiet);
    border-color: transparent;
  }

  :host([appearance='filled-outlined']) details {
    background-color: var(--cs-color-neutral-fill-quiet);
    border-color: var(--cs-color-neutral-border-quiet);
  }

  :host([disabled]) details {
    opacity: 0.5;
    cursor: not-allowed;
  }

  summary {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--spacing);
    padding: var(--spacing); /* Add padding here */
    border-radius: calc(var(--cs-panel-border-radius) - var(--cs-panel-border-width));
    user-select: none;
    -webkit-user-select: none;
    cursor: pointer;

    &::marker,
    &::-webkit-details-marker {
      display: none;
    }

    &:focus {
      outline: none;
    }

    &:focus-visible {
      outline: var(--cs-focus-ring);
      outline-offset: calc(var(--cs-panel-border-width) + var(--cs-focus-ring-offset));
    }
  }

  :host([open]) summary {
    border-end-start-radius: 0;
    border-end-end-radius: 0;
  }

  /* 'Start' icon placement */
  :host([icon-placement='start']) summary {
    flex-direction: row-reverse;
    justify-content: start;
  }

  [part~='icon'] {
    flex: 0 0 auto;
    display: flex;
    align-items: center;
    color: var(--cs-color-text-quiet);
    transition: rotate var(--cs-transition-normal) var(--cs-transition-easing);
  }

  :host([open]) [part~='icon'] {
    rotate: 90deg;
  }

  :host([open]:dir(rtl)) [part~='icon'] {
    rotate: -90deg;
  }

  :host([open]) slot[name='expand-icon'],
  :host(:not([open])) slot[name='collapse-icon'] {
    display: none;
  }

  .body.animating {
    overflow: hidden;
  }

  .content {
    display: block;
    box-sizing: border-box; /* Ensure contents don't overflow */
    padding-block-start: var(--spacing);
    padding-inline: var(--spacing); /* Add horizontal padding */
    padding-block-end: var(--spacing); /* Add bottom padding */
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.RCGGQL7K.js
  var CsDetails = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.localize = new LocalizeController2(this);
      this.animationGeneration = 0;
      this.isAnimating = false;
      this.open = false;
      this.disabled = false;
      this.appearance = "outlined";
      this.iconPlacement = "end";
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this.detailsObserver?.disconnect();
    }
    firstUpdated(changedProperties) {
      super.firstUpdated(changedProperties);
      this.body.style.height = this.open ? "auto" : "0";
      if (this.open) {
        this.details.open = true;
      }
      this.detailsObserver = new MutationObserver((changes) => {
        for (const change of changes) {
          if (change.type === "attributes" && change.attributeName === "open") {
            if (this.details.open) {
              this.show();
            } else {
              this.hide();
            }
          }
        }
      });
      this.detailsObserver.observe(this.details, { attributes: true });
    }
    updated(changedProperties) {
      if (changedProperties.has("isAnimating")) {
        this.customStates.set("animating", this.isAnimating);
      }
    }
    handleSummaryClick(event) {
      const eventPath = event.composedPath();
      const hasInteractiveElement = eventPath.some((element) => {
        if (!(element instanceof HTMLElement)) {
          return false;
        }
        const tagName = element.tagName.toLowerCase();
        if (["a", "button", "input", "textarea", "select"].includes(tagName)) {
          return true;
        }
        if (element instanceof CornerstoneFormAssociatedElement) {
          return !("disabled" in element) || !element.disabled;
        }
        return false;
      });
      if (hasInteractiveElement) {
        return;
      }
      event.preventDefault();
      if (!this.disabled) {
        if (this.open) {
          this.hide();
        } else {
          this.show();
        }
        this.header.focus();
      }
    }
    handleSummaryKeyDown(event) {
      if (event.key === "Enter" || event.key === " ") {
        event.preventDefault();
        if (this.open) {
          this.hide();
        } else {
          this.show();
        }
      }
      if (event.key === "ArrowUp" || event.key === "ArrowLeft") {
        event.preventDefault();
        this.hide();
      }
      if (event.key === "ArrowDown" || event.key === "ArrowRight") {
        event.preventDefault();
        this.show();
      }
    }
    /** Closes other <cs-details> elements in the same document when they have the same name. */
    closeOthersWithSameName() {
      if (!this.name) {
        return;
      }
      const root = this.getRootNode();
      const otherDetails = root.querySelectorAll(`cs-details[name="${this.name}"]`);
      otherDetails.forEach((detail) => {
        if (detail !== this && detail.open) {
          detail.open = false;
        }
      });
    }
    async handleOpenChange() {
      this.animationGeneration++;
      const generation = this.animationGeneration;
      if (this.open) {
        this.details.open = true;
        const csShow = new CsShowEvent();
        this.dispatchEvent(csShow);
        if (csShow.defaultPrevented) {
          this.open = false;
          this.details.open = false;
          return;
        }
        this.closeOthersWithSameName();
        this.isAnimating = true;
        const duration = parseDuration(getComputedStyle(this.body).getPropertyValue("--show-duration"));
        await animate(
          this.body,
          [
            { height: "0", opacity: "0" },
            { height: `${this.body.scrollHeight}px`, opacity: "1" }
          ],
          {
            duration,
            easing: "linear"
          }
        );
        if (this.animationGeneration !== generation) {
          return;
        }
        this.body.style.height = "auto";
        this.isAnimating = false;
        this.dispatchEvent(new CsAfterShowEvent());
      } else {
        const csHide = new CsHideEvent();
        this.dispatchEvent(csHide);
        if (csHide.defaultPrevented) {
          this.details.open = true;
          this.open = true;
          return;
        }
        this.isAnimating = true;
        const duration = parseDuration(getComputedStyle(this.body).getPropertyValue("--hide-duration"));
        await animate(
          this.body,
          [
            { height: `${this.body.scrollHeight}px`, opacity: "1" },
            { height: "0", opacity: "0" }
          ],
          { duration, easing: "linear" }
        );
        if (this.animationGeneration !== generation) {
          return;
        }
        this.body.style.height = "0";
        this.isAnimating = false;
        this.details.open = false;
        this.dispatchEvent(new CsAfterHideEvent());
      }
    }
    /** Shows the details. */
    async show() {
      if (this.open || this.disabled) {
        return void 0;
      }
      this.open = true;
      return waitForEvent(this, "cs-after-show");
    }
    /** Hides the details */
    async hide() {
      if (!this.open || this.disabled) {
        return void 0;
      }
      this.open = false;
      return waitForEvent(this, "cs-after-hide");
    }
    render() {
      const isRtl = !this.hasUpdated ? this.dir === "rtl" : this.localize.dir() === "rtl";
      return b2`
      <details part="details">
        <summary
          part="header"
          role="button"
          aria-expanded=${this.open ? "true" : "false"}
          aria-controls="content"
          aria-disabled=${this.disabled ? "true" : "false"}
          tabindex=${this.disabled ? "-1" : "0"}
          @click=${this.handleSummaryClick}
          @keydown=${this.handleSummaryKeyDown}
        >
          <slot name="summary" part="summary">${this.summary}</slot>

          <span part="icon">
            <slot name="expand-icon">
              <cs-icon library="system" name=${isRtl ? "keyboard_arrow_left" : "keyboard_arrow_right"}></cs-icon>
            </slot>
            <slot name="collapse-icon">
              <cs-icon library="system" name=${isRtl ? "keyboard_arrow_left" : "keyboard_arrow_right"}></cs-icon>
            </slot>
          </span>
        </summary>

        <div
          class=${e7({
        body: true,
        animating: this.isAnimating
      })}
          role="region"
          aria-labelledby="header"
        >
          <slot part="content" id="content" class="content"></slot>
        </div>
      </details>
    `;
    }
  };
  CsDetails.css = details_styles_default;
  __decorateClass([
    e5("details")
  ], CsDetails.prototype, "details", 2);
  __decorateClass([
    e5("summary")
  ], CsDetails.prototype, "header", 2);
  __decorateClass([
    e5(".body")
  ], CsDetails.prototype, "body", 2);
  __decorateClass([
    e5(".expand-icon-slot")
  ], CsDetails.prototype, "expandIconSlot", 2);
  __decorateClass([
    r5()
  ], CsDetails.prototype, "isAnimating", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsDetails.prototype, "open", 2);
  __decorateClass([
    n4()
  ], CsDetails.prototype, "summary", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsDetails.prototype, "name", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsDetails.prototype, "disabled", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsDetails.prototype, "appearance", 2);
  __decorateClass([
    n4({ attribute: "icon-placement", reflect: true })
  ], CsDetails.prototype, "iconPlacement", 2);
  __decorateClass([
    watch("open", { waitUntilFirstUpdate: true })
  ], CsDetails.prototype, "handleOpenChange", 1);
  CsDetails = __decorateClass([
    customElement("cs-details")
  ], CsDetails);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.MDQ3IO6L.js
  var RenderedWatcher = class {
    constructor(element, callback) {
      this.element = element;
      this.callback = callback;
    }
    /**
     * Starts watching and reports the current state on the next frame. Additional targets also act as change signals,
     * e.g. an internal element that always has a non-zero box when rendered.
     */
    start(...additionalTargets) {
      if (o5) {
        return;
      }
      this.observer ?? (this.observer = new ResizeObserver(() => this.check()));
      this.observer.observe(this.element);
      for (const target of additionalTargets) {
        this.observer.observe(target);
      }
      this.initialCheckHandle ?? (this.initialCheckHandle = requestAnimationFrame(() => {
        this.initialCheckHandle = void 0;
        this.check();
      }));
    }
    /** Stops watching. */
    stop() {
      if (this.initialCheckHandle !== void 0) {
        cancelAnimationFrame(this.initialCheckHandle);
        this.initialCheckHandle = void 0;
      }
      this.observer?.disconnect();
    }
    check() {
      this.callback(this.element.getClientRects().length > 0);
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.I467WJ3G.js
  function getOffset(element, parent) {
    return {
      top: Math.round(element.getBoundingClientRect().top - parent.getBoundingClientRect().top),
      left: Math.round(element.getBoundingClientRect().left - parent.getBoundingClientRect().left)
    };
  }
  var locks = /* @__PURE__ */ new Set();
  function getScrollbarWidth() {
    const documentWidth = document.documentElement.clientWidth;
    return Math.abs(window.innerWidth - documentWidth);
  }
  function getExistingBodyPadding() {
    const padding = Number(getComputedStyle(document.body).paddingRight.replace(/px/, ""));
    if (isNaN(padding) || !padding) {
      return 0;
    }
    return padding;
  }
  function lockBodyScrolling(lockingEl) {
    locks.add(lockingEl);
    if (!document.documentElement.classList.contains("cs-scroll-lock")) {
      const scrollbarWidth = getScrollbarWidth() + getExistingBodyPadding();
      let scrollbarGutterProperty = getComputedStyle(document.documentElement).scrollbarGutter;
      if (!scrollbarGutterProperty || scrollbarGutterProperty === "auto") {
        scrollbarGutterProperty = "stable";
      }
      if (scrollbarWidth < 2) {
        scrollbarGutterProperty = "";
      }
      document.documentElement.style.setProperty("--cs-scroll-lock-gutter", scrollbarGutterProperty);
      document.documentElement.classList.add("cs-scroll-lock");
      document.documentElement.style.setProperty("--cs-scroll-lock-size", `${scrollbarWidth}px`);
    }
  }
  function unlockBodyScrolling(lockingEl) {
    locks.delete(lockingEl);
    if (locks.size === 0) {
      document.documentElement.classList.remove("cs-scroll-lock");
      document.documentElement.style.removeProperty("--cs-scroll-lock-size");
    }
  }
  function scrollIntoView(element, container, direction = "vertical", behavior = "smooth") {
    const offset3 = getOffset(element, container);
    const offsetTop = offset3.top + container.scrollTop;
    const offsetLeft = offset3.left + container.scrollLeft;
    const minX = container.scrollLeft;
    const maxX = container.scrollLeft + container.offsetWidth;
    const minY = container.scrollTop;
    const maxY = container.scrollTop + container.offsetHeight;
    if (direction === "horizontal" || direction === "both") {
      if (offsetLeft < minX) {
        container.scrollTo({ left: offsetLeft, behavior });
      } else if (offsetLeft + element.clientWidth > maxX) {
        container.scrollTo({ left: offsetLeft - container.offsetWidth + element.clientWidth, behavior });
      }
    }
    if (direction === "vertical" || direction === "both") {
      if (offsetTop < minY) {
        container.scrollTo({ top: offsetTop, behavior });
      } else if (offsetTop + element.clientHeight > maxY) {
        container.scrollTo({ top: offsetTop - container.offsetHeight + element.clientHeight, behavior });
      }
    }
  }

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.QVMFBQ5Z.js
  function parseSpaceDelimitedTokens(input) {
    return input.split(" ").map((token) => token.trim()).filter((token) => token !== "");
  }

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.R66PMNHL.js
  var dialog_styles_default = i`
  :host {
    --width: 31rem;
    --spacing: var(--cs-space-l);
    --backdrop-filter: none;
    --show-duration: var(--cs-transition-normal);
    --hide-duration: var(--cs-transition-normal);

    display: none;
  }

  /*
   * Out of flow, so opening the dialog cannot move anything around it. The visible panel is a native dialog element
   * opened with showModal(), which lives in the top layer and is positioned against the viewport, so the host's own
   * box paints nothing — but while it was in flow it was still a flex/grid item wherever the host sat, and opening
   * the dialog added a gap that nudged its siblings.
   *
   * The width stays full-viewport rather than zero. RenderedWatcher spots third-party CSS hiding the dialog through
   * ResizeObserver size changes, and the panel's max-width resolves against this box, so collapsing it would flatten
   * both of those signals and the modal would never resume. Height is zero because nothing here paints.
   */
  :host([open]) {
    display: block;
    position: fixed;
    inset-inline: 0;
    height: 0;
  }

  .dialog {
    display: flex;
    flex-direction: column;
    top: 0;
    right: 0;
    bottom: 0;
    left: 0;
    width: var(--width);
    max-width: calc(100% - var(--cs-space-2xl));
    max-height: calc(100% - var(--cs-space-2xl));
    color: inherit;
    background-color: var(--cs-color-surface-raised);
    border-radius: var(--cs-panel-border-radius);
    border: none;
    box-shadow: var(--cs-shadow-l);
    padding: 0;
    margin: auto;

    &.show {
      animation: show-dialog var(--show-duration) ease;

      &::backdrop {
        animation: show-backdrop var(--show-duration, 200ms) ease;
      }
    }

    &.hide {
      animation: show-dialog var(--hide-duration) ease reverse;

      &::backdrop {
        animation: show-backdrop var(--hide-duration, 200ms) ease reverse;
      }
    }

    &.pulse {
      animation: pulse 250ms ease;
    }
  }

  .dialog:focus {
    outline: none;
  }

  /* Ensure there's enough vertical padding for phones that don't update vh when chrome appears (e.g. iPhone) */
  @media screen and (max-width: 420px) {
    .dialog {
      max-height: 80vh;
    }
  }

  .open {
    display: flex;
    opacity: 1;
  }

  .header {
    flex: 0 0 auto;
    display: flex;
    flex-wrap: nowrap;

    padding-inline-start: var(--spacing);
    padding-block-end: 0;

    /* Subtract the close button's padding so that the X is visually aligned with the edges of the dialog content */
    padding-inline-end: calc(var(--spacing) - var(--cs-form-control-padding-block));
    padding-block-start: calc(var(--spacing) - var(--cs-form-control-padding-block));
  }

  .title {
    align-self: center;
    flex: 1 1 auto;
    font-family: inherit;
    font-size: var(--cs-font-size-l);
    font-weight: var(--cs-font-weight-heading);
    line-height: var(--cs-line-height-condensed);
    margin: 0;
  }

  .header-actions {
    align-self: start;
    display: flex;
    flex-shrink: 0;
    flex-wrap: wrap;
    justify-content: end;
    gap: var(--cs-space-2xs);
    padding-inline-start: var(--spacing);
  }

  .header-actions cs-button,
  .header-actions ::slotted(cs-button) {
    flex: 0 0 auto;
    display: flex;
    align-items: center;
  }

  .body {
    flex: 1 1 auto;
    display: block;
    padding: var(--spacing);
    overflow: auto;
    -webkit-overflow-scrolling: touch;

    &:focus {
      outline: none;
    }

    &:focus-visible {
      outline: var(--cs-focus-ring);
      outline-offset: var(--cs-focus-ring-offset);
    }
  }

  .footer {
    flex: 0 0 auto;
    display: flex;
    flex-wrap: wrap;
    gap: var(--cs-space-xs);
    justify-content: end;
    padding: var(--spacing);
    padding-block-start: 0;
  }

  .footer ::slotted(cs-button:not(:first-of-type)) {
    margin-inline-start: var(--cs-space-xs);
  }

  .dialog::backdrop {
    /*
      NOTE: the ::backdrop element doesn't inherit properly in Safari yet, but it will in 17.4! At that time, we can
      remove the fallback values here.
    */
    background-color: var(--cs-color-overlay-modal, rgb(0 0 0 / 0.25));
    backdrop-filter: var(--backdrop-filter);
  }

  @keyframes pulse {
    0% {
      scale: 1;
    }
    50% {
      scale: 1.02;
    }
    100% {
      scale: 1;
    }
  }

  @keyframes show-dialog {
    from {
      opacity: 0;
      scale: 0.8;
    }
    to {
      opacity: 1;
      scale: 1;
    }
  }

  @keyframes show-backdrop {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }

  @media (forced-colors: active) {
    .dialog {
      border: solid 1px white;
    }
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.DRUEC5M4.js
  var CsDialog = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.localize = new LocalizeController2(this);
      this.hasSlotController = new HasSlotController(this, "footer");
      this.renderedWatcher = new RenderedWatcher(this, (isRendered) => this.handleRenderedChange(isRendered));
      this.open = false;
      this.label = "";
      this.withoutHeader = false;
      this.lightDismiss = false;
      this.ssrFooter = false;
      this.handleDocumentKeyDown = (event) => {
        if (event.key === "Escape" && this.open && isTopDismissible(this)) {
          event.preventDefault();
          event.stopPropagation();
          this.requestClose(this.dialog);
        }
      };
    }
    firstUpdated(changedProperties) {
      super.firstUpdated(changedProperties);
      if (this.open) {
        this.addOpenListeners();
        this.dialog.showModal();
        lockBodyScrolling(this);
        this.renderedWatcher.start(this.dialog);
      }
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this.renderedWatcher.stop();
      unlockBodyScrolling(this);
      this.removeOpenListeners();
    }
    async requestClose(source) {
      const csHideEvent = new CsHideEvent({ source });
      this.dispatchEvent(csHideEvent);
      if (csHideEvent.defaultPrevented) {
        this.open = true;
        animateWithClass(this.dialog, "pulse");
        return;
      }
      this.removeOpenListeners();
      await animateWithClass(this.dialog, "hide");
      this.open = false;
      this.dialog.close();
      unlockBodyScrolling(this);
      this.renderedWatcher.stop();
      const trigger = this.originalTrigger;
      if (typeof trigger?.focus === "function") {
        setTimeout(() => trigger.focus());
      }
      this.dispatchEvent(new CsAfterHideEvent());
    }
    addOpenListeners() {
      document.addEventListener("keydown", this.handleDocumentKeyDown);
      registerDismissible(this);
    }
    removeOpenListeners() {
      document.removeEventListener("keydown", this.handleDocumentKeyDown);
      unregisterDismissible(this);
    }
    handleDialogCancel(event) {
      event.preventDefault();
      if (!this.dialog.classList.contains("hide") && event.target === this.dialog && isTopDismissible(this)) {
        this.requestClose(this.dialog);
      }
    }
    handleDialogClick(event) {
      const target = event.target;
      const button = target.closest('[data-dialog="close"]');
      if (button) {
        event.stopPropagation();
        this.requestClose(button);
      }
    }
    async handleDialogPointerDown(event) {
      if (event.target === this.dialog) {
        if (this.lightDismiss) {
          this.requestClose(this.dialog);
        } else {
          await animateWithClass(this.dialog, "pulse");
        }
      }
    }
    /**
     * Suspends the modal when third-party CSS (e.g. cookie banner blockers) hides an open dialog, so the page isn't
     * left scroll locked and inert. "open" stays true so the modal resumes if the dialog is rendered again.
     */
    handleRenderedChange(isRendered) {
      if (!this.open) {
        this.renderedWatcher.stop();
        return;
      }
      if (!isRendered && this.dialog.open) {
        this.removeOpenListeners();
        this.dialog.close();
        unlockBodyScrolling(this);
      } else if (isRendered && !this.dialog.open) {
        this.addOpenListeners();
        this.dialog.showModal();
        lockBodyScrolling(this);
      }
    }
    handleOpenChange() {
      if (this.open && !this.dialog.open) {
        this.show();
      } else if (!this.open && this.dialog.open) {
        this.open = true;
        this.requestClose(this.dialog);
      } else if (!this.open) {
        this.renderedWatcher.stop();
      }
    }
    /** Shows the dialog. */
    async show() {
      const csShowEvent = new CsShowEvent();
      this.dispatchEvent(csShowEvent);
      if (csShowEvent.defaultPrevented) {
        this.open = false;
        return;
      }
      this.addOpenListeners();
      this.originalTrigger = document.activeElement;
      this.open = true;
      this.dialog.showModal();
      lockBodyScrolling(this);
      this.renderedWatcher.start(this.dialog);
      requestAnimationFrame(() => {
        const elementToFocus = this.querySelector("[autofocus]");
        if (elementToFocus && typeof elementToFocus.focus === "function") {
          elementToFocus.focus();
        } else {
          this.dialog.focus();
        }
      });
      await animateWithClass(this.dialog, "show");
      this.dispatchEvent(new CsAfterShowEvent());
    }
    render() {
      const hasHeader = !this.withoutHeader;
      const labelledBy = hasHeader ? "title" : void 0;
      const hasFooter = this.hasSlotController.test("footer", "ssrFooter");
      return b2`
      <dialog
        part="dialog"
        aria-labelledby=${o8(labelledBy)}
        class=${e7({
        dialog: true,
        open: this.open
      })}
        @cancel=${this.handleDialogCancel}
        @click=${this.handleDialogClick}
        @pointerdown=${this.handleDialogPointerDown}
      >
        ${hasHeader ? b2`
                <header part="header" class="header">
                  <h2 part="title" class="title" id="title">
                    \x3C!-- If there's no label, use an invisible character to prevent the header from collapsing -->
                    <slot name="label"> ${this.label.length > 0 ? this.label : String.fromCharCode(8203)} </slot>
                  </h2>
                  <div part="header-actions" class="header-actions">
                    <slot name="header-actions"></slot>
                    <cs-button
                      part="close-button"
                      exportparts="button:close-button__button"
                      class="close"
                      appearance="plain"
                      @click="${(event) => this.requestClose(event.target)}"
                    >
                      <cs-icon name="close" label=${this.localize.term("close")} library="system"></cs-icon>
                    </cs-button>
                  </div>
                </header>
              ` : ""}

        <div part="body" class="body"><slot></slot></div>

        \x3C!-- Use a hidden element so we still get "slotchange" events. -->
        <footer part="footer" class="footer" ?hidden=${!hasFooter}>
          <slot name="footer"></slot>
        </footer>
      </dialog>
    `;
    }
  };
  CsDialog.css = dialog_styles_default;
  __decorateClass([
    e5(".dialog")
  ], CsDialog.prototype, "dialog", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsDialog.prototype, "open", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsDialog.prototype, "label", 2);
  __decorateClass([
    n4({ attribute: "without-header", type: Boolean, reflect: true })
  ], CsDialog.prototype, "withoutHeader", 2);
  __decorateClass([
    n4({ attribute: "light-dismiss", type: Boolean })
  ], CsDialog.prototype, "lightDismiss", 2);
  __decorateClass([
    n4({ attribute: "ssr-footer", type: Boolean })
  ], CsDialog.prototype, "ssrFooter", 2);
  __decorateClass([
    watch("open", { waitUntilFirstUpdate: true })
  ], CsDialog.prototype, "handleOpenChange", 1);
  CsDialog = __decorateClass([
    customElement("cs-dialog")
  ], CsDialog);
  if (!o5) {
    document.addEventListener("click", (event) => {
      const dialogAttrEl = event.target.closest("[data-dialog]");
      if (dialogAttrEl instanceof Element) {
        const [command, id3] = parseSpaceDelimitedTokens(dialogAttrEl.getAttribute("data-dialog") || "");
        if (command === "open" && id3?.length) {
          const doc = dialogAttrEl.getRootNode();
          const dialog = doc.getElementById(id3);
          if (dialog?.localName === "cs-dialog") {
            dialog.open = true;
          } else {
            console.warn(`A dialog with an ID of "${id3}" could not be found in this document.`);
          }
        }
      }
    });
    document.addEventListener("pointerdown", () => {
    });
  }

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.6RDQB523.js
  var divider_styles_default = i`
  :host {
    --color: var(--cs-color-surface-border);
    --width: var(--cs-border-width-s);
    --spacing: var(--cs-space-m);
  }

  :host(:not([orientation='vertical'])) {
    display: block;
    border-top: solid var(--width) var(--color);
    margin: var(--spacing) 0;
  }

  :host([orientation='vertical']) {
    display: inline-block;
    height: 100%;
    border-inline-start: solid var(--width) var(--color);
    margin: 0 var(--spacing);
    min-block-size: 1lh;
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.JXDCQNHM.js
  var CsDivider = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.orientation = "horizontal";
    }
    connectedCallback() {
      super.connectedCallback();
      this.setAttribute("role", "separator");
    }
    handleVerticalChange() {
      this.setAttribute("aria-orientation", this.orientation);
    }
  };
  CsDivider.css = divider_styles_default;
  __decorateClass([
    n4({ reflect: true })
  ], CsDivider.prototype, "orientation", 2);
  __decorateClass([
    watch("orientation")
  ], CsDivider.prototype, "handleVerticalChange", 1);
  CsDivider = __decorateClass([
    customElement("cs-divider")
  ], CsDivider);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.FV7MGNZY.js
  var drawer_styles_default = i`
  :host {
    --size: 25rem;
    --spacing: var(--cs-space-l);
    --backdrop-filter: none;
    --show-duration: var(--cs-transition-normal);
    --hide-duration: var(--cs-transition-normal);

    display: none;
  }

  /*
   * Out of flow, so opening the dialog cannot move anything around it. The visible panel is a native dialog element
   * opened with showModal(), which lives in the top layer and is positioned against the viewport, so the host's own
   * box paints nothing — but while it was in flow it was still a flex/grid item wherever the host sat, and opening
   * the dialog added a gap that nudged its siblings.
   *
   * The width stays full-viewport rather than zero. RenderedWatcher spots third-party CSS hiding the dialog through
   * ResizeObserver size changes, and the panel's max-width resolves against this box, so collapsing it would flatten
   * both of those signals and the modal would never resume. Height is zero because nothing here paints.
   */
  :host([open]) {
    display: block;
    position: fixed;
    inset-inline: 0;
    height: 0;
  }

  .drawer {
    display: flex;
    flex-direction: column;
    top: 0;
    inset-inline-start: 0;
    width: 100%;
    height: 100%;
    max-width: 100%;
    max-height: 100%;
    overflow: hidden;
    color: inherit;
    background-color: var(--cs-color-surface-raised);
    border: none;
    box-shadow: var(--cs-shadow-l);
    overflow: auto;
    padding: 0;
    margin: 0;
    animation-duration: var(--show-duration);
    animation-timing-function: ease;

    &.show::backdrop {
      animation: show-backdrop var(--show-duration, 200ms) ease;
    }

    &.hide::backdrop {
      animation: show-backdrop var(--hide-duration, 200ms) ease reverse;
    }

    &.show.top {
      animation: show-drawer-from-top var(--show-duration) ease;
    }

    &.hide.top {
      animation: show-drawer-from-top var(--hide-duration) ease reverse;
    }

    &.show.end {
      animation: show-drawer-from-end var(--show-duration) ease;

      &:dir(rtl) {
        animation-name: show-drawer-from-start;
      }
    }

    &.hide.end {
      animation: show-drawer-from-end var(--hide-duration) ease reverse;

      &:dir(rtl) {
        animation-name: show-drawer-from-start;
      }
    }

    &.show.bottom {
      animation: show-drawer-from-bottom var(--show-duration) ease;
    }

    &.hide.bottom {
      animation: show-drawer-from-bottom var(--hide-duration) ease reverse;
    }

    &.show.start {
      animation: show-drawer-from-start var(--show-duration) ease;

      &:dir(rtl) {
        animation-name: show-drawer-from-end;
      }
    }

    &.hide.start {
      animation: show-drawer-from-start var(--hide-duration) ease reverse;

      &:dir(rtl) {
        animation-name: show-drawer-from-end;
      }
    }

    &.pulse {
      animation: pulse 250ms ease;
    }
  }

  .drawer:focus {
    outline: none;
  }

  .top {
    top: 0;
    inset-inline-end: auto;
    bottom: auto;
    inset-inline-start: 0;
    width: 100%;
    height: var(--size);
  }

  .end {
    top: 0;
    inset-inline-end: 0;
    bottom: auto;
    inset-inline-start: auto;
    width: var(--size);
    height: 100%;
  }

  .bottom {
    top: auto;
    inset-inline-end: auto;
    bottom: 0;
    inset-inline-start: 0;
    width: 100%;
    height: var(--size);
  }

  .start {
    top: 0;
    inset-inline-end: auto;
    bottom: auto;
    inset-inline-start: 0;
    width: var(--size);
    height: 100%;
  }

  .header {
    display: flex;
    flex-wrap: nowrap;
    padding-inline-start: var(--spacing);
    padding-block-end: 0;

    /* Subtract the close button's padding so that the X is visually aligned with the edges of the dialog content */
    padding-inline-end: calc(var(--spacing) - var(--cs-form-control-padding-block));
    padding-block-start: calc(var(--spacing) - var(--cs-form-control-padding-block));
  }

  .title {
    align-self: center;
    flex: 1 1 auto;
    font: inherit;
    font-size: var(--cs-font-size-l);
    font-weight: var(--cs-font-weight-heading);
    line-height: var(--cs-line-height-condensed);
    margin: 0;
  }

  .header-actions {
    align-self: start;
    display: flex;
    flex-shrink: 0;
    flex-wrap: wrap;
    justify-content: end;
    gap: var(--cs-space-2xs);
    padding-inline-start: var(--spacing);
  }

  .header-actions cs-button,
  .header-actions ::slotted(cs-button) {
    flex: 0 0 auto;
    display: flex;
    align-items: center;
  }

  .body {
    flex: 1 1 auto;
    display: block;
    padding: var(--spacing);
    overflow: auto;
    -webkit-overflow-scrolling: touch;

    &:focus {
      outline: none;
    }

    &:focus-visible {
      outline: var(--cs-focus-ring);
      outline-offset: var(--cs-focus-ring-offset);
    }
  }

  .footer {
    display: flex;
    flex-wrap: wrap;
    gap: var(--cs-space-xs);
    justify-content: end;
    padding: var(--spacing);
    padding-block-start: 0;
  }

  .footer ::slotted(cs-button:not(:last-of-type)) {
    margin-inline-end: var(--cs-space-xs);
  }

  .drawer::backdrop {
    /*
        NOTE: the ::backdrop element doesn't inherit properly in Safari yet, but it will in 17.4! At that time, we can
        remove the fallback values here.
      */
    background-color: var(--cs-color-overlay-modal, rgb(0 0 0 / 0.25));
    backdrop-filter: var(--backdrop-filter);
  }

  @keyframes pulse {
    0% {
      scale: 1;
    }
    50% {
      scale: 1.01;
    }
    100% {
      scale: 1;
    }
  }

  @keyframes show-drawer {
    from {
      opacity: 0;
      scale: 0.8;
    }
    to {
      opacity: 1;
      scale: 1;
    }
  }

  @keyframes show-drawer-from-top {
    from {
      opacity: 0;
      translate: 0 -100%;
    }
    to {
      opacity: 1;
      translate: 0 0;
    }
  }

  @keyframes show-drawer-from-end {
    from {
      opacity: 0;
      translate: 100%;
    }
    to {
      opacity: 1;
      translate: 0 0;
    }
  }

  @keyframes show-drawer-from-bottom {
    from {
      opacity: 0;
      translate: 0 100%;
    }
    to {
      opacity: 1;
      translate: 0 0;
    }
  }

  @keyframes show-drawer-from-start {
    from {
      opacity: 0;
      translate: -100% 0;
    }
    to {
      opacity: 1;
      translate: 0 0;
    }
  }

  @keyframes show-backdrop {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }

  @media (forced-colors: active) {
    .drawer {
      border: solid 1px white;
    }
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.5XIAQ2PB.js
  var CsDrawer = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.localize = new LocalizeController2(this);
      this.hasSlotController = new HasSlotController(this, "footer");
      this.renderedWatcher = new RenderedWatcher(this, (isRendered) => this.handleRenderedChange(isRendered));
      this.open = false;
      this.label = "";
      this.placement = "end";
      this.withoutHeader = false;
      this.lightDismiss = false;
      this.ssrFooter = false;
      this.handleDocumentKeyDown = (event) => {
        if (event.key === "Escape" && this.open && isTopDismissible(this)) {
          event.preventDefault();
          event.stopPropagation();
          this.requestClose(this.drawer);
        }
      };
    }
    firstUpdated(changedProperties) {
      super.firstUpdated(changedProperties);
      if (this.open) {
        this.addOpenListeners();
        this.drawer.showModal();
        lockBodyScrolling(this);
        this.renderedWatcher.start(this.drawer);
      }
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this.renderedWatcher.stop();
      unlockBodyScrolling(this);
      this.removeOpenListeners();
    }
    async requestClose(source) {
      const csHideEvent = new CsHideEvent({ source });
      this.dispatchEvent(csHideEvent);
      if (csHideEvent.defaultPrevented) {
        this.open = true;
        animateWithClass(this.drawer, "pulse");
        return;
      }
      this.removeOpenListeners();
      await animateWithClass(this.drawer, "hide");
      this.open = false;
      this.drawer.close();
      unlockBodyScrolling(this);
      this.renderedWatcher.stop();
      const trigger = this.originalTrigger;
      if (typeof trigger?.focus === "function") {
        setTimeout(() => trigger.focus());
      }
      this.dispatchEvent(new CsAfterHideEvent());
    }
    addOpenListeners() {
      document.addEventListener("keydown", this.handleDocumentKeyDown);
      registerDismissible(this);
    }
    removeOpenListeners() {
      document.removeEventListener("keydown", this.handleDocumentKeyDown);
      unregisterDismissible(this);
    }
    handleDialogCancel(event) {
      event.preventDefault();
      if (!this.drawer.classList.contains("hide") && event.target === this.drawer && isTopDismissible(this)) {
        this.requestClose(this.drawer);
      }
    }
    handleDialogClick(event) {
      const target = event.target;
      const button = target.closest('[data-drawer="close"]');
      if (button) {
        event.stopPropagation();
        this.requestClose(button);
      }
    }
    async handleDialogPointerDown(event) {
      if (event.target === this.drawer) {
        if (this.lightDismiss) {
          this.requestClose(this.drawer);
        } else {
          await animateWithClass(this.drawer, "pulse");
        }
      }
    }
    /**
     * Suspends the modal when third-party CSS (e.g. cookie banner blockers) hides an open drawer, so the page isn't
     * left scroll locked and inert. "open" stays true so the modal resumes if the drawer is rendered again.
     */
    handleRenderedChange(isRendered) {
      if (!this.open) {
        this.renderedWatcher.stop();
        return;
      }
      if (!isRendered && this.drawer.open) {
        this.removeOpenListeners();
        this.drawer.close();
        unlockBodyScrolling(this);
      } else if (isRendered && !this.drawer.open) {
        this.addOpenListeners();
        this.drawer.showModal();
        lockBodyScrolling(this);
      }
    }
    handleOpenChange() {
      if (this.open && !this.drawer.open) {
        this.show();
      } else if (this.drawer.open) {
        this.open = true;
        this.requestClose(this.drawer);
      } else if (!this.open) {
        this.renderedWatcher.stop();
      }
    }
    /** Shows the drawer. */
    async show() {
      const csShowEvent = new CsShowEvent();
      this.dispatchEvent(csShowEvent);
      if (csShowEvent.defaultPrevented) {
        this.open = false;
        return;
      }
      this.addOpenListeners();
      this.originalTrigger = document.activeElement;
      this.open = true;
      this.drawer.showModal();
      lockBodyScrolling(this);
      this.renderedWatcher.start(this.drawer);
      requestAnimationFrame(() => {
        const elementToFocus = this.querySelector("[autofocus]");
        if (elementToFocus && typeof elementToFocus.focus === "function") {
          elementToFocus.focus();
        } else {
          this.drawer.focus();
        }
      });
      await animateWithClass(this.drawer, "show");
      this.dispatchEvent(new CsAfterShowEvent());
    }
    render() {
      const hasHeader = !this.withoutHeader;
      const labelledBy = hasHeader ? "title" : void 0;
      const hasFooter = this.hasSlotController.test("footer", "ssrFooter");
      return b2`
      <dialog
        part="dialog"
        aria-labelledby=${o8(labelledBy)}
        class=${e7({
        drawer: true,
        open: this.open,
        top: this.placement === "top",
        end: this.placement === "end",
        bottom: this.placement === "bottom",
        start: this.placement === "start"
      })}
        @cancel=${this.handleDialogCancel}
        @click=${this.handleDialogClick}
        @pointerdown=${this.handleDialogPointerDown}
      >
        ${hasHeader ? b2`
                <header part="header" class="header">
                  <h2 part="title" class="title" id="title">
                    \x3C!-- If there's no label, use an invisible character to prevent the header from collapsing -->
                    <slot name="label"> ${this.label.length > 0 ? this.label : String.fromCharCode(8203)} </slot>
                  </h2>
                  <div part="header-actions" class="header-actions">
                    <slot name="header-actions"></slot>
                    <cs-button
                      part="close-button"
                      exportparts="button:close-button__button"
                      class="close"
                      appearance="plain"
                      @click="${(event) => this.requestClose(event.target)}"
                    >
                      <cs-icon name="close" label=${this.localize.term("close")} library="system"></cs-icon>
                    </cs-button>
                  </div>
                </header>
              ` : ""}

        <div part="body" class="body"><slot></slot></div>

        <footer part="footer" class="footer" ?hidden=${!hasFooter}>
          <slot name="footer"></slot>
        </footer>
      </dialog>
    `;
    }
  };
  CsDrawer.css = drawer_styles_default;
  __decorateClass([
    e5(".drawer")
  ], CsDrawer.prototype, "drawer", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsDrawer.prototype, "open", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsDrawer.prototype, "label", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsDrawer.prototype, "placement", 2);
  __decorateClass([
    n4({ attribute: "without-header", type: Boolean, reflect: true })
  ], CsDrawer.prototype, "withoutHeader", 2);
  __decorateClass([
    n4({ attribute: "light-dismiss", type: Boolean })
  ], CsDrawer.prototype, "lightDismiss", 2);
  __decorateClass([
    n4({ attribute: "ssr-footer", type: Boolean })
  ], CsDrawer.prototype, "ssrFooter", 2);
  __decorateClass([
    watch("open", { waitUntilFirstUpdate: true })
  ], CsDrawer.prototype, "handleOpenChange", 1);
  CsDrawer = __decorateClass([
    customElement("cs-drawer")
  ], CsDrawer);
  if (!o5) {
    document.addEventListener("click", (event) => {
      const drawerAttrEl = event.target.closest("[data-drawer]");
      if (drawerAttrEl instanceof Element) {
        const [command, id3] = parseSpaceDelimitedTokens(drawerAttrEl.getAttribute("data-drawer") || "");
        if (command === "open" && id3?.length) {
          const doc = drawerAttrEl.getRootNode();
          const drawer = doc.getElementById(id3);
          if (drawer?.localName === "cs-drawer") {
            drawer.open = true;
          } else {
            console.warn(`A drawer with an ID of "${id3}" could not be found in this document.`);
          }
        }
      }
    });
    document.addEventListener("pointerdown", () => {
    });
  }

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.6X5DGRFL.js
  var CsSelectEvent = class extends Event {
    constructor(detail) {
      super("cs-select", { bubbles: true, cancelable: true, composed: true });
      this.detail = detail;
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.HWIJ6II3.js
  function* activeElements(activeElement = document.activeElement) {
    if (activeElement === null || activeElement === void 0) {
      return;
    }
    yield activeElement;
    if ("shadowRoot" in activeElement && activeElement.shadowRoot && activeElement.shadowRoot.mode !== "closed") {
      yield* activeElements(activeElement.shadowRoot.activeElement);
    }
  }
  function getDeepestActiveElement() {
    return [...activeElements()].pop();
  }

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.HIULTBEP.js
  var dropdown_styles_default = i`
  :host {
    --show-duration: var(--cs-transition-fast);
    --hide-duration: var(--cs-transition-fast);
    display: contents;
  }

  #menu {
    display: flex;
    flex-direction: column;
    width: max-content;
    margin: 0;
    padding: 0.25em;
    border: var(--cs-border-style) var(--cs-border-width-s) var(--cs-color-surface-border);
    border-radius: var(--cs-border-radius-m);
    background-color: var(--cs-color-surface-raised);
    box-shadow: var(--cs-shadow-m);
    color: var(--cs-color-text-normal);
    text-align: start;
    user-select: none;
    overflow: auto;
    max-width: var(--auto-size-available-width) !important;
    max-height: var(--auto-size-available-height) !important;

    &.show {
      animation: show var(--show-duration) ease;
    }

    &.hide {
      animation: show var(--hide-duration) ease reverse;
    }

    ::slotted(h1),
    ::slotted(h2),
    ::slotted(h3),
    ::slotted(h4),
    ::slotted(h5),
    ::slotted(h6) {
      display: block !important;
      margin: 0.25em 0 !important;
      padding: 0.25em 0.75em !important;
      color: var(--cs-color-text-quiet);
      font-family: var(--cs-font-family-body) !important;
      font-weight: var(--cs-font-weight-semibold) !important;
      font-size: var(--cs-font-size-smaller) !important;
    }

    ::slotted(cs-divider) {
      --spacing: 0.25em; /* Component-specific, left as-is */
    }
  }

  cs-popup[data-current-placement^='top'] #menu {
    transform-origin: bottom;
  }

  cs-popup[data-current-placement^='bottom'] #menu {
    transform-origin: top;
  }

  cs-popup[data-current-placement^='left'] #menu {
    transform-origin: right;
  }

  cs-popup[data-current-placement^='right'] #menu {
    transform-origin: left;
  }

  cs-popup[data-current-placement='left-start'] #menu {
    transform-origin: right top;
  }

  cs-popup[data-current-placement='left-end'] #menu {
    transform-origin: right bottom;
  }

  cs-popup[data-current-placement='right-start'] #menu {
    transform-origin: left top;
  }

  cs-popup[data-current-placement='right-end'] #menu {
    transform-origin: left bottom;
  }

  @keyframes show {
    from {
      scale: 0.9;
      opacity: 0;
    }
    to {
      scale: 1;
      opacity: 1;
    }
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.PDNUBFDC.js
  var openDropdowns = /* @__PURE__ */ new Set();
  var CsDropdown = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.submenuCleanups = /* @__PURE__ */ new Map();
      this.localize = new LocalizeController2(this);
      this.userTypedQuery = "";
      this.openSubmenuStack = [];
      this.open = false;
      this.size = "m";
      this.placement = "bottom-start";
      this.distance = 0;
      this.skidding = 0;
      this.handleDocumentKeyDown = async (event) => {
        const isRtl = this.localize.dir() === "rtl";
        if (event.key === "Escape" && this.open && isTopDismissible(this)) {
          const trigger = this.getTrigger();
          event.preventDefault();
          event.stopPropagation();
          this.open = false;
          trigger?.focus({ preventScroll: true });
          return;
        }
        const activeElement = [...activeElements()].find((el) => el.localName === "cs-dropdown-item");
        const isFocusedOnItem = activeElement?.localName === "cs-dropdown-item";
        const currentSubmenuItem = this.getCurrentSubmenuItem();
        const isInSubmenu = !!currentSubmenuItem;
        let items;
        let activeItem;
        let activeItemIndex;
        if (isInSubmenu) {
          items = this.getSubmenuItems(currentSubmenuItem);
          activeItem = items.find((item) => item.active || item === activeElement);
          activeItemIndex = activeItem ? items.indexOf(activeItem) : -1;
        } else {
          items = this.getItems();
          activeItem = items.find((item) => item.active || item === activeElement);
          activeItemIndex = activeItem ? items.indexOf(activeItem) : -1;
        }
        let itemToSelect;
        if (event.key === "ArrowUp") {
          event.preventDefault();
          event.stopPropagation();
          if (activeItemIndex > 0) {
            itemToSelect = items[activeItemIndex - 1];
          } else {
            itemToSelect = items[items.length - 1];
          }
        }
        if (event.key === "ArrowDown") {
          event.preventDefault();
          event.stopPropagation();
          if (activeItemIndex !== -1 && activeItemIndex < items.length - 1) {
            itemToSelect = items[activeItemIndex + 1];
          } else {
            itemToSelect = items[0];
          }
        }
        if (event.key === (isRtl ? "ArrowLeft" : "ArrowRight") && isFocusedOnItem && activeItem) {
          if (activeItem.hasSubmenu) {
            event.preventDefault();
            event.stopPropagation();
            activeItem.submenuOpen = true;
            this.addToSubmenuStack(activeItem);
            setTimeout(() => {
              const submenuItems = this.getSubmenuItems(activeItem);
              if (submenuItems.length > 0) {
                submenuItems.forEach((item, index) => item.active = index === 0);
                submenuItems[0].focus({ preventScroll: true });
              }
            }, 0);
            return;
          }
        }
        if (event.key === (isRtl ? "ArrowRight" : "ArrowLeft") && isInSubmenu) {
          event.preventDefault();
          event.stopPropagation();
          const removedItem = this.removeFromSubmenuStack();
          if (removedItem) {
            removedItem.submenuOpen = false;
            setTimeout(() => {
              removedItem.focus({ preventScroll: true });
              removedItem.active = true;
              const parentItems = removedItem.slot === "submenu" ? this.getSubmenuItems(removedItem.parentElement) : this.getItems();
              parentItems.forEach((item) => {
                if (item !== removedItem) {
                  item.active = false;
                }
              });
            }, 0);
          }
          return;
        }
        if (event.key === "Home" || event.key === "End") {
          event.preventDefault();
          event.stopPropagation();
          itemToSelect = event.key === "Home" ? items[0] : items[items.length - 1];
        }
        if (event.key === "Tab") {
          await this.hideMenu();
        }
        if (event.key.length === 1 && !(event.metaKey || event.ctrlKey || event.altKey) && !(event.key === " " && this.userTypedQuery === "")) {
          clearTimeout(this.userTypedTimeout);
          this.userTypedTimeout = setTimeout(() => {
            this.userTypedQuery = "";
          }, 1e3);
          this.userTypedQuery += event.key;
          items.some((item) => {
            const label = (item.textContent || "").trim().toLowerCase();
            const selectionQuery = this.userTypedQuery.trim().toLowerCase();
            if (label.startsWith(selectionQuery)) {
              itemToSelect = item;
              return true;
            }
            return false;
          });
        }
        if (itemToSelect) {
          event.preventDefault();
          event.stopPropagation();
          items.forEach((item) => item.active = item === itemToSelect);
          itemToSelect.focus({ preventScroll: true });
          itemToSelect.scrollIntoView({ block: "nearest" });
          return;
        }
        if ((event.key === "Enter" || event.key === " " && this.userTypedQuery === "") && isFocusedOnItem && activeItem) {
          event.preventDefault();
          event.stopPropagation();
          if (activeItem.hasSubmenu) {
            activeItem.submenuOpen = true;
            this.addToSubmenuStack(activeItem);
            setTimeout(() => {
              const submenuItems = this.getSubmenuItems(activeItem);
              if (submenuItems.length > 0) {
                submenuItems.forEach((item, index) => item.active = index === 0);
                submenuItems[0].focus({ preventScroll: true });
              }
            }, 0);
          } else {
            this.makeSelection(activeItem);
          }
        }
      };
      this.handleDocumentPointerDown = (event) => {
        const path = event.composedPath();
        const isInDropdownHierarchy = path.some((el) => {
          if (el instanceof HTMLElement) {
            return el === this || el.closest('cs-dropdown, [part="submenu"]');
          }
          return false;
        });
        if (!isInDropdownHierarchy) {
          this.open = false;
        }
      };
      this.handleGlobalMouseMove = (event) => {
        const currentSubmenuItem = this.getCurrentSubmenuItem();
        if (!currentSubmenuItem?.submenuOpen || !currentSubmenuItem.submenuElement) {
          return;
        }
        const submenuRect = currentSubmenuItem.submenuElement.getBoundingClientRect();
        const isRtl = this.localize.dir() === "rtl";
        const submenuEdgeX = isRtl ? submenuRect.right : submenuRect.left;
        const constrainedX = isRtl ? Math.max(event.clientX, submenuEdgeX) : Math.min(event.clientX, submenuEdgeX);
        const constrainedY = Math.max(submenuRect.top, Math.min(event.clientY, submenuRect.bottom));
        currentSubmenuItem.submenuElement.style.setProperty("--safe-triangle-cursor-x", `${constrainedX}px`);
        currentSubmenuItem.submenuElement.style.setProperty("--safe-triangle-cursor-y", `${constrainedY}px`);
        const composedPath = event.composedPath();
        const submenuItemHovered = currentSubmenuItem.matches(":hover");
        const submenuElementHovered = Boolean(currentSubmenuItem.submenuElement?.matches(":hover"));
        const isOverItem = submenuItemHovered || !!composedPath.find((el) => el === currentSubmenuItem);
        const isOverSubmenu = submenuElementHovered || !!composedPath.find(
          (el) => el instanceof HTMLElement && el.closest('[part="submenu"]') === currentSubmenuItem.submenuElement
        );
        if (!isOverItem && !isOverSubmenu) {
          setTimeout(() => {
            const stillOverItem = currentSubmenuItem.matches(":hover");
            const stillOverSubmenu = Boolean(currentSubmenuItem.submenuElement?.matches(":hover"));
            if (!stillOverItem && !stillOverSubmenu) {
              currentSubmenuItem.submenuOpen = false;
            }
          }, 100);
        }
      };
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      clearInterval(this.userTypedTimeout);
      this.closeAllSubmenus();
      this.submenuCleanups.forEach((cleanup) => cleanup());
      this.submenuCleanups.clear();
      document.removeEventListener("mousemove", this.handleGlobalMouseMove);
      document.removeEventListener("keydown", this.handleDocumentKeyDown);
      document.removeEventListener("pointerdown", this.handleDocumentPointerDown);
      unregisterDismissible(this);
    }
    firstUpdated(changedProperties) {
      super.firstUpdated(changedProperties);
      this.syncAriaAttributes();
    }
    async updated(changedProperties) {
      if (changedProperties.has("open")) {
        const previousOpen = changedProperties.get("open");
        if (previousOpen === this.open) {
          return;
        }
        if (previousOpen === void 0 && this.open === false) {
          return;
        }
        this.customStates.set("open", this.open);
        if (this.open) {
          await this.showMenu();
        } else {
          this.closeAllSubmenus();
          await this.hideMenu();
        }
      }
      if (changedProperties.has("size")) {
        this.syncItemSizes();
      }
    }
    /** Gets all dropdown items slotted in the menu. */
    getItems(includeDisabled = false) {
      const items = (this.defaultSlot?.assignedElements({ flatten: true }) ?? []).filter(
        (el) => el.localName === "cs-dropdown-item"
      );
      return includeDisabled ? items : items.filter((item) => !item.disabled);
    }
    /** Gets all dropdown items in a specific submenu. */
    getSubmenuItems(parentItem, includeDisabled = false) {
      const submenuSlot = parentItem.shadowRoot?.querySelector('slot[name="submenu"]') || parentItem.querySelector('slot[name="submenu"]');
      if (!submenuSlot) {
        return [];
      }
      const items = submenuSlot.assignedElements({ flatten: true }).filter((el) => el.localName === "cs-dropdown-item");
      return includeDisabled ? items : items.filter((item) => !item.disabled);
    }
    /** Syncs item sizes with the dropdown's size property. */
    syncItemSizes() {
      const items = (this.defaultSlot?.assignedElements({ flatten: true }) ?? []).filter(
        (el) => el.localName === "cs-dropdown-item"
      );
      items.forEach((item) => item.size = this.size);
    }
    /** Handles the submenu navigation stack */
    addToSubmenuStack(item) {
      const index = this.openSubmenuStack.indexOf(item);
      if (index !== -1) {
        this.openSubmenuStack = this.openSubmenuStack.slice(0, index + 1);
      } else {
        this.openSubmenuStack.push(item);
      }
    }
    /** Removes the last item from the submenu stack */
    removeFromSubmenuStack() {
      return this.openSubmenuStack.pop();
    }
    /** Gets the current active submenu item */
    getCurrentSubmenuItem() {
      return this.openSubmenuStack.length > 0 ? this.openSubmenuStack[this.openSubmenuStack.length - 1] : void 0;
    }
    /** Closes all submenus in the dropdown. */
    closeAllSubmenus() {
      const items = this.getItems(true);
      items.forEach((item) => {
        item.submenuOpen = false;
      });
      this.openSubmenuStack = [];
    }
    /** Closes sibling submenus at the same level as the specified item. */
    closeSiblingSubmenus(item) {
      const parentDropdownItem = item.closest('cs-dropdown-item:not([slot="submenu"])');
      let siblingItems;
      if (parentDropdownItem) {
        siblingItems = this.getSubmenuItems(parentDropdownItem, true);
      } else {
        siblingItems = this.getItems(true);
      }
      siblingItems.forEach((siblingItem) => {
        if (siblingItem !== item && siblingItem.submenuOpen) {
          siblingItem.submenuOpen = false;
        }
      });
      if (!this.openSubmenuStack.includes(item)) {
        this.openSubmenuStack.push(item);
      }
    }
    /** Get the slotted trigger button, a <cs-button> or <button> element */
    getTrigger() {
      return this.querySelector('[slot="trigger"]');
    }
    /** Shows the dropdown menu. This should only be called from within updated(). */
    async showMenu() {
      const anchor = this.getTrigger();
      if (!anchor || !this.popup || !this.menu) {
        return;
      }
      const showEvent = new CsShowEvent();
      this.dispatchEvent(showEvent);
      if (showEvent.defaultPrevented) {
        this.open = false;
        return;
      }
      if (this.popup.active) {
        return;
      }
      openDropdowns.forEach((dropdown) => dropdown.open = false);
      this.popup.active = true;
      this.open = true;
      openDropdowns.add(this);
      registerDismissible(this);
      this.syncAriaAttributes();
      document.addEventListener("keydown", this.handleDocumentKeyDown);
      document.addEventListener("pointerdown", this.handleDocumentPointerDown);
      document.addEventListener("mousemove", this.handleGlobalMouseMove);
      this.menu.classList.remove("hide");
      await animateWithClass(this.menu, "show");
      const items = this.getItems();
      if (items.length > 0) {
        items.forEach((item, index) => item.active = index === 0);
        items[0].focus({ preventScroll: true });
      }
      this.dispatchEvent(new CsAfterShowEvent());
    }
    /** Hides the dropdown menu. This should only be called from within updated(). */
    async hideMenu() {
      if (!this.popup || !this.menu) {
        return;
      }
      const hideEvent = new CsHideEvent({ source: this });
      this.dispatchEvent(hideEvent);
      if (hideEvent.defaultPrevented) {
        this.open = true;
        return;
      }
      this.open = false;
      openDropdowns.delete(this);
      unregisterDismissible(this);
      this.syncAriaAttributes();
      document.removeEventListener("keydown", this.handleDocumentKeyDown);
      document.removeEventListener("pointerdown", this.handleDocumentPointerDown);
      document.removeEventListener("mousemove", this.handleGlobalMouseMove);
      this.menu.classList.remove("show");
      await animateWithClass(this.menu, "hide");
      this.popup.active = this.open;
      this.dispatchEvent(new CsAfterHideEvent());
    }
    /** Handles clicks on the menu. */
    handleMenuClick(event) {
      const item = event.target.closest("cs-dropdown-item");
      if (!item || item.disabled) {
        return;
      }
      if (item.hasSubmenu) {
        if (!item.submenuOpen) {
          this.closeSiblingSubmenus(item);
          this.addToSubmenuStack(item);
          item.submenuOpen = true;
        }
        event.stopPropagation();
        return;
      }
      this.makeSelection(item);
    }
    /** Prepares dropdown items when they get added or removed */
    async handleMenuSlotChange() {
      const items = this.getItems(true);
      await Promise.all(items.map((item) => item.updateComplete));
      this.syncItemSizes();
      const hasCheckbox = items.some((item) => item.type === "checkbox");
      const hasSubmenu = items.some((item) => item.hasSubmenu);
      items.forEach((item, index) => {
        item.setAttribute("aria-posinset", String(index + 1));
        item.setAttribute("aria-setsize", String(items.length));
        item.active = index === 0;
        item.checkboxAdjacent = hasCheckbox;
        item.submenuAdjacent = hasSubmenu;
      });
    }
    /** Toggles the dropdown menu */
    handleTriggerClick() {
      this.open = !this.open;
    }
    /** Handles submenu opening events */
    handleSubmenuOpening(event) {
      const openingItem = event.detail.item;
      this.closeSiblingSubmenus(openingItem);
      this.addToSubmenuStack(openingItem);
      this.setupSubmenuPosition(openingItem);
      this.processSubmenuItems(openingItem);
    }
    /** Sets up submenu positioning with autoUpdate */
    setupSubmenuPosition(item) {
      if (!item.submenuElement) {
        return;
      }
      this.cleanupSubmenuPosition(item);
      const cleanup = autoUpdate(item, item.submenuElement, () => {
        this.positionSubmenu(item);
        this.updateSafeTriangleCoordinates(item);
      });
      this.submenuCleanups.set(item, cleanup);
      const submenuSlot = item.submenuElement.querySelector('slot[name="submenu"]');
      if (submenuSlot) {
        submenuSlot.removeEventListener("slotchange", CsDropdown.handleSubmenuSlotChange);
        submenuSlot.addEventListener("slotchange", CsDropdown.handleSubmenuSlotChange);
        CsDropdown.handleSubmenuSlotChange({ target: submenuSlot });
      }
    }
    static handleSubmenuSlotChange(event) {
      const slot = event.target;
      if (!slot) {
        return;
      }
      const items = slot.assignedElements().filter((el) => el.localName === "cs-dropdown-item");
      if (items.length === 0) {
        return;
      }
      const hasSubmenuItems = items.some((item) => item.hasSubmenu);
      const hasCheckboxItems = items.some((item) => item.type === "checkbox");
      items.forEach((item) => {
        item.submenuAdjacent = hasSubmenuItems;
        item.checkboxAdjacent = hasCheckboxItems;
      });
    }
    processSubmenuItems(item) {
      if (!item.submenuElement) {
        return;
      }
      const submenuItems = this.getSubmenuItems(item, true);
      const hasSubmenuItems = submenuItems.some((subItem) => subItem.hasSubmenu);
      submenuItems.forEach((subItem) => {
        subItem.submenuAdjacent = hasSubmenuItems;
      });
    }
    /** Cleans up submenu positioning */
    cleanupSubmenuPosition(item) {
      const cleanup = this.submenuCleanups.get(item);
      if (cleanup) {
        cleanup();
        this.submenuCleanups.delete(item);
      }
    }
    /** Positions a submenu relative to its parent item */
    positionSubmenu(item) {
      if (!item.submenuElement) {
        return;
      }
      const isRtl = this.localize.dir() === "rtl";
      const placement = isRtl ? "left-start" : "right-start";
      computePosition2(item, item.submenuElement, {
        placement,
        middleware: [
          offset2({
            mainAxis: 0,
            crossAxis: -5
          }),
          flip3({
            fallbackStrategy: "bestFit"
          }),
          shift2({
            padding: 8
          })
        ]
      }).then(({ x: x2, y: y3, placement: placement2 }) => {
        item.submenuElement.setAttribute("data-placement", placement2);
        Object.assign(item.submenuElement.style, {
          left: `${x2}px`,
          top: `${y3}px`
        });
      });
    }
    /** Updates the safe triangle coordinates for a submenu */
    updateSafeTriangleCoordinates(item) {
      if (!item.submenuElement || !item.submenuOpen) {
        return;
      }
      const isKeyboardNavigation = document.activeElement?.matches(":focus-visible");
      if (isKeyboardNavigation) {
        item.submenuElement.style.setProperty("--safe-triangle-visible", "none");
        return;
      }
      item.submenuElement.style.setProperty("--safe-triangle-visible", "block");
      const submenuRect = item.submenuElement.getBoundingClientRect();
      const isRtl = this.localize.dir() === "rtl";
      item.submenuElement.style.setProperty(
        "--safe-triangle-submenu-start-x",
        `${isRtl ? submenuRect.right : submenuRect.left}px`
      );
      item.submenuElement.style.setProperty("--safe-triangle-submenu-start-y", `${submenuRect.top}px`);
      item.submenuElement.style.setProperty(
        "--safe-triangle-submenu-end-x",
        `${isRtl ? submenuRect.right : submenuRect.left}px`
      );
      item.submenuElement.style.setProperty("--safe-triangle-submenu-end-y", `${submenuRect.bottom}px`);
    }
    /** Makes a selection, emits the cs-select event, and closes the dropdown. */
    makeSelection(item) {
      const trigger = this.getTrigger();
      if (item.disabled) {
        return;
      }
      if (item.type === "checkbox") {
        item.checked = !item.checked;
      }
      const selectEvent = new CsSelectEvent({ item });
      this.dispatchEvent(selectEvent);
      if (!selectEvent.defaultPrevented) {
        this.open = false;
        trigger?.focus({ preventScroll: true });
      }
    }
    /** Syncs aria attributes on the slotted trigger element and the menu based on the dropdown's current state */
    async syncAriaAttributes() {
      const trigger = this.getTrigger();
      let nativeButton;
      if (!trigger) {
        return;
      }
      if (trigger.localName === "cs-button") {
        await customElements.whenDefined("cs-button");
        await trigger.updateComplete;
        nativeButton = trigger.shadowRoot.querySelector('[part~="button"]');
      } else {
        nativeButton = trigger;
      }
      if (!nativeButton.hasAttribute("id")) {
        nativeButton.setAttribute("id", uniqueId("cs-dropdown-trigger-"));
      }
      nativeButton.setAttribute("aria-haspopup", "menu");
      nativeButton.setAttribute("aria-expanded", this.open ? "true" : "false");
      this.menu?.setAttribute("aria-expanded", "false");
    }
    render() {
      const active = this.didSSR && !this.hasUpdated ? this.open : this.popup?.active;
      return b2`
      <cs-popup
        placement=${this.placement}
        distance=${this.distance}
        skidding=${this.skidding}
        ?active=${active}
        flip
        flip-fallback-strategy="best-fit"
        shift
        shift-padding="10"
        auto-size="vertical"
        auto-size-padding="10"
      >
        <slot
          name="trigger"
          slot="anchor"
          @click=${this.handleTriggerClick}
          @slotchange=${this.syncAriaAttributes}
        ></slot>
        <div
          id="menu"
          part="menu"
          role="menu"
          tabindex="-1"
          aria-orientation="vertical"
          @click=${this.handleMenuClick}
          @submenu-opening=${this.handleSubmenuOpening}
        >
          <slot @slotchange=${this.handleMenuSlotChange}></slot>
        </div>
      </cs-popup>
    `;
    }
  };
  CsDropdown.css = [size_styles_default, dropdown_styles_default];
  __decorateClass([
    e5("slot:not([name])")
  ], CsDropdown.prototype, "defaultSlot", 2);
  __decorateClass([
    e5("#menu")
  ], CsDropdown.prototype, "menu", 2);
  __decorateClass([
    e5("cs-popup")
  ], CsDropdown.prototype, "popup", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsDropdown.prototype, "open", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsDropdown.prototype, "size", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsDropdown.prototype, "placement", 2);
  __decorateClass([
    n4({ type: Number })
  ], CsDropdown.prototype, "distance", 2);
  __decorateClass([
    n4({ type: Number })
  ], CsDropdown.prototype, "skidding", 2);
  CsDropdown = __decorateClass([
    customElement("cs-dropdown")
  ], CsDropdown);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.MJEPIX6K.js
  var dropdown_item_styles_default = i`
  :host {
    display: flex;
    position: relative;
    align-items: center;
    padding: 0.5em 1em;
    border-radius: var(--cs-border-radius-s);
    isolation: isolate;
    color: var(--cs-color-text-normal);
    line-height: var(--cs-line-height-condensed);
    cursor: pointer;
    transition:
      var(--cs-transition-fast) background-color var(--cs-transition-easing),
      var(--cs-transition-fast) color var(--cs-transition-easing);
  }

  @media (hover: hover) {
    :host(:hover:not(:state(disabled))) {
      background-color: var(--cs-color-neutral-fill-normal);
    }
  }

  :host(:state(submenu-open)) {
    background-color: var(--cs-color-neutral-fill-normal);
  }

  :host(:focus-visible) {
    z-index: 1;
    outline: var(--cs-focus-ring);
    background-color: var(--cs-color-neutral-fill-normal);
  }

  :host(:state(disabled)),
  :host([disabled]) {
    opacity: 0.5;
    cursor: not-allowed;
  }

  /* Danger variant */
  :host([variant='danger']),
  :host([variant='danger']) #details {
    color: var(--cs-color-danger-on-quiet);
  }

  @media (hover: hover) {
    :host([variant='danger']:hover) {
      background-color: var(--cs-color-danger-fill-normal);
      color: var(--cs-color-danger-on-normal);
    }
  }

  :host([variant='danger']:state(submenu-open)),
  :host([variant='danger']:focus-visible) {
    background-color: var(--cs-color-danger-fill-normal);
    color: var(--cs-color-danger-on-normal);
  }

  :host([checkbox-adjacent]) {
    padding-inline-start: 2em;
  }

  /* Only add padding when item actually has a submenu */
  :host([submenu-adjacent]:not(:state(has-submenu))) #details {
    padding-inline-end: 0;
  }

  :host(:state(has-submenu)[submenu-adjacent]) #details {
    padding-inline-end: 1.75em;
  }

  #check {
    visibility: hidden;
    margin-inline-start: -1.5em;
    margin-inline-end: 0.5em;
    font-size: var(--cs-font-size-smaller);
  }

  :host(:state(checked)) #check {
    visibility: visible;
  }

  #icon ::slotted(*) {
    display: flex;
    flex: 0 0 auto;
    align-items: center;
    margin-inline-end: 0.75em !important;
    font-size: var(--cs-font-size-smaller);
  }

  #label {
    flex: 1 1 auto;
    min-width: 0;
  }

  #details {
    display: flex;
    flex: 0 0 auto;
    align-items: center;
    justify-content: end;
    color: var(--cs-color-text-quiet);
    font-size: var(--cs-font-size-smaller) !important;
  }

  #details ::slotted(*) {
    margin-inline-start: 2em !important;
  }

  /* Submenu indicator icon */
  #submenu-indicator {
    position: absolute;
    inset-inline-end: 1em;
    color: var(--cs-color-neutral-on-quiet);
    font-size: var(--cs-font-size-smaller);
  }

  /* Flip chevron icon when RTL */
  :host(:dir(rtl)) #submenu-indicator {
    transform: scaleX(-1);
  }

  /* Submenu styles */
  #submenu {
    display: flex;
    z-index: 10;
    position: absolute;
    top: 0;
    left: 0;
    flex-direction: column;
    width: max-content;
    margin: 0;
    padding: 0.25em;
    border: var(--cs-border-style) var(--cs-border-width-s) var(--cs-color-surface-border);
    border-radius: var(--cs-border-radius-m);
    background-color: var(--cs-color-surface-raised);
    box-shadow: var(--cs-shadow-m);
    color: var(--cs-color-text-normal);
    text-align: start;
    user-select: none;

    /* Override default popover styles */
    &[popover] {
      margin: 0;
      inset: auto;
      padding: 0.25em;
      overflow: visible;
      border-radius: var(--cs-border-radius-m);
    }

    &.show {
      animation: submenu-show var(--show-duration, var(--cs-transition-fast)) ease;
    }

    &.hide {
      animation: submenu-show var(--show-duration, var(--cs-transition-fast)) ease reverse;
    }

    /* Submenu placement transform origins */
    &[data-placement^='top'] {
      transform-origin: bottom;
    }

    &[data-placement^='bottom'] {
      transform-origin: top;
    }

    &[data-placement^='left'] {
      transform-origin: right;
    }

    &[data-placement^='right'] {
      transform-origin: left;
    }

    &[data-placement='left-start'] {
      transform-origin: right top;
    }

    &[data-placement='left-end'] {
      transform-origin: right bottom;
    }

    &[data-placement='right-start'] {
      transform-origin: left top;
    }

    &[data-placement='right-end'] {
      transform-origin: left bottom;
    }

    /* Safe triangle styling */
    &::before {
      display: none;
      z-index: 9;
      position: fixed;
      top: 0;
      right: 0;
      bottom: 0;
      left: 0;
      background-color: transparent;
      content: '';
      clip-path: polygon(
        var(--safe-triangle-cursor-x, 0) var(--safe-triangle-cursor-y, 0),
        var(--safe-triangle-submenu-start-x, 0) var(--safe-triangle-submenu-start-y, 0),
        var(--safe-triangle-submenu-end-x, 0) var(--safe-triangle-submenu-end-y, 0)
      );
      pointer-events: auto; /* Enable mouse events on the triangle */
    }

    &[data-visible]::before {
      display: block;
    }
  }

  ::slotted(cs-dropdown-item) {
    font-size: inherit;
  }

  ::slotted(cs-divider) {
    --spacing: 0.25em;
  }

  @keyframes submenu-show {
    from {
      scale: 0.9;
      opacity: 0;
    }
    to {
      scale: 1;
      opacity: 1;
    }
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.O4PTQTCV.js
  var CsDropdownItem = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.hasSlotController = new HasSlotController(this, "[default]", "start", "end");
      this.active = false;
      this.variant = "neutral";
      this.size = "m";
      this.checkboxAdjacent = false;
      this.submenuAdjacent = false;
      this.type = "normal";
      this.checked = false;
      this.disabled = false;
      this.submenuOpen = false;
      this.hasSubmenu = false;
      this.handleSlotChange = () => {
        this.hasSubmenu = this.hasSlotController.test("submenu");
        this.updateHasSubmenuState();
        if (this.hasSubmenu) {
          this.setAttribute("aria-haspopup", "menu");
          this.setAttribute("aria-expanded", this.submenuOpen ? "true" : "false");
        } else {
          this.removeAttribute("aria-haspopup");
          this.removeAttribute("aria-expanded");
        }
      };
      this.handleHostClick = (event) => {
        if (this.disabled) {
          event.preventDefault();
          event.stopImmediatePropagation();
        }
      };
      this.handleClick = (event) => {
        if (this.disabled) {
          event.preventDefault();
          event.stopImmediatePropagation();
        }
      };
    }
    connectedCallback() {
      super.connectedCallback();
      this.addEventListener?.("click", this.handleHostClick);
      this.addEventListener?.("mouseenter", this.handleMouseEnter.bind(this));
      this.shadowRoot?.addEventListener?.("click", this.handleClick, { capture: true });
      this.shadowRoot?.addEventListener?.("slotchange", this.handleSlotChange);
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this.closeSubmenu();
      this.removeEventListener?.("click", this.handleHostClick);
      this.removeEventListener?.("mouseenter", this.handleMouseEnter);
      this.shadowRoot?.removeEventListener?.("click", this.handleClick, { capture: true });
      this.shadowRoot?.removeEventListener?.("slotchange", this.handleSlotChange);
    }
    firstUpdated(changedProperties) {
      super.firstUpdated(changedProperties);
      this.setAttribute("tabindex", "-1");
      this.hasSubmenu = this.hasSlotController.test("submenu");
      this.updateHasSubmenuState();
    }
    updated(changedProperties) {
      if (changedProperties.has("active")) {
        this.setAttribute("tabindex", this.active ? "0" : "-1");
        this.customStates.set("active", this.active);
      }
      if (changedProperties.has("checked")) {
        if (this.type === "checkbox") {
          this.setAttribute("aria-checked", this.checked ? "true" : "false");
        } else {
          this.removeAttribute("aria-checked");
        }
        this.customStates.set("checked", this.checked);
      }
      if (changedProperties.has("disabled")) {
        this.setAttribute("aria-disabled", this.disabled ? "true" : "false");
        this.customStates.set("disabled", this.disabled);
      }
      if (changedProperties.has("type")) {
        if (this.type === "checkbox") {
          this.setAttribute("role", "menuitemcheckbox");
          this.setAttribute("aria-checked", this.checked ? "true" : "false");
        } else {
          this.setAttribute("role", "menuitem");
          this.removeAttribute("aria-checked");
        }
      }
      if (changedProperties.has("submenuOpen")) {
        this.customStates.set("submenu-open", this.submenuOpen);
        if (this.submenuOpen) {
          this.openSubmenu();
        } else {
          this.closeSubmenu();
        }
      }
    }
    /** Update the has-submenu custom state */
    updateHasSubmenuState() {
      this.customStates.set("has-submenu", this.hasSubmenu);
    }
    /** Opens the submenu. */
    async openSubmenu() {
      const submenu = this.submenuElement;
      if (!this.hasSubmenu || !submenu || !this.isConnected) {
        return;
      }
      this.notifyParentOfOpening();
      submenu.showPopover?.();
      submenu.hidden = false;
      submenu.setAttribute("data-visible", "");
      this.submenuOpen = true;
      this.setAttribute("aria-expanded", "true");
      await animateWithClass(submenu, "show");
      setTimeout(() => {
        const items = this.getSubmenuItems();
        if (items.length > 0) {
          items.forEach((item, index) => item.active = index === 0);
          items[0].focus({ preventScroll: true });
        }
      }, 0);
    }
    /** Notifies the parent dropdown that this item is opening its submenu */
    notifyParentOfOpening() {
      const event = new CustomEvent("submenu-opening", {
        bubbles: true,
        composed: true,
        detail: { item: this }
      });
      this.dispatchEvent(event);
      const parent = this.parentElement;
      if (parent) {
        const siblings = [...parent.children].filter(
          (el) => el !== this && el.localName === "cs-dropdown-item" && el.getAttribute("slot") === this.getAttribute("slot") && el.submenuOpen
        );
        siblings.forEach((sibling) => {
          sibling.submenuOpen = false;
        });
      }
    }
    /** Closes the submenu. */
    async closeSubmenu() {
      const submenu = this.submenuElement;
      if (!this.hasSubmenu || !submenu) {
        return;
      }
      this.submenuOpen = false;
      this.setAttribute("aria-expanded", "false");
      if (!submenu.hidden) {
        await animateWithClass(submenu, "hide");
        if (submenu.isConnected) {
          submenu.hidden = true;
          submenu.removeAttribute("data-visible");
          submenu.hidePopover?.();
        }
      }
    }
    /** Gets all dropdown items in the submenu. */
    getSubmenuItems() {
      return [...this.children].filter(
        (el) => el.localName === "cs-dropdown-item" && el.getAttribute("slot") === "submenu" && !el.hasAttribute("disabled")
      );
    }
    /** Handles mouse enter to open the submenu */
    handleMouseEnter() {
      if (this.hasSubmenu && !this.disabled) {
        this.notifyParentOfOpening();
        this.submenuOpen = true;
      }
    }
    render() {
      return b2`
      ${this.type === "checkbox" ? b2`
              <cs-icon
                id="check"
                part="checkmark"
                exportparts="svg:checkmark__svg"
                library="system"
                name="check"
              ></cs-icon>
            ` : ""}

      <span id="icon" part="icon">
        <slot name="icon"></slot>
      </span>

      <span id="label" part="label">
        <slot></slot>
      </span>

      <span id="details" part="details">
        <slot name="details"></slot>
      </span>

      ${this.hasSubmenu ? b2`
              <cs-icon
                id="submenu-indicator"
                part="submenu-icon"
                exportparts="svg:submenu-icon__svg"
                library="system"
                name="keyboard_arrow_right"
              ></cs-icon>
            ` : ""}
      ${this.hasSubmenu ? b2`
              <div
                id="submenu"
                part="submenu"
                popover="manual"
                role="menu"
                tabindex="-1"
                aria-orientation="vertical"
                hidden
              >
                <slot name="submenu"></slot>
              </div>
            ` : ""}
    `;
    }
  };
  CsDropdownItem.css = dropdown_item_styles_default;
  __decorateClass([
    e5("#submenu")
  ], CsDropdownItem.prototype, "submenuElement", 2);
  __decorateClass([
    n4({ type: Boolean })
  ], CsDropdownItem.prototype, "active", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsDropdownItem.prototype, "variant", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsDropdownItem.prototype, "size", 2);
  __decorateClass([
    n4({ attribute: "checkbox-adjacent", type: Boolean, reflect: true })
  ], CsDropdownItem.prototype, "checkboxAdjacent", 2);
  __decorateClass([
    n4({ attribute: "submenu-adjacent", type: Boolean, reflect: true })
  ], CsDropdownItem.prototype, "submenuAdjacent", 2);
  __decorateClass([
    n4()
  ], CsDropdownItem.prototype, "value", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsDropdownItem.prototype, "type", 2);
  __decorateClass([
    n4({ type: Boolean })
  ], CsDropdownItem.prototype, "checked", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsDropdownItem.prototype, "disabled", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsDropdownItem.prototype, "submenuOpen", 2);
  __decorateClass([
    r5()
  ], CsDropdownItem.prototype, "hasSubmenu", 2);
  CsDropdownItem = __decorateClass([
    customElement("cs-dropdown-item")
  ], CsDropdownItem);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.3JXJ6KH6.js
  var CsFormatBytes = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.localize = new LocalizeController2(this);
      this.value = 0;
      this.unit = "byte";
      this.display = "short";
    }
    static get styles() {
      return [];
    }
    render() {
      if (isNaN(this.value)) {
        return "";
      }
      const bitPrefixes = ["", "kilo", "mega", "giga", "tera"];
      const bytePrefixes = ["", "kilo", "mega", "giga", "tera", "peta"];
      const prefix = this.unit === "bit" ? bitPrefixes : bytePrefixes;
      const index = Math.max(0, Math.min(Math.floor(Math.log10(this.value) / 3), prefix.length - 1));
      const unit = prefix[index] + this.unit;
      const valueToFormat = parseFloat((this.value / Math.pow(1e3, index)).toPrecision(3));
      return this.localize.number(valueToFormat, {
        style: "unit",
        unit,
        unitDisplay: this.display
      });
    }
  };
  __decorateClass([
    n4({ type: Number })
  ], CsFormatBytes.prototype, "value", 2);
  __decorateClass([
    n4()
  ], CsFormatBytes.prototype, "unit", 2);
  __decorateClass([
    n4()
  ], CsFormatBytes.prototype, "display", 2);
  CsFormatBytes = __decorateClass([
    customElement("cs-format-bytes")
  ], CsFormatBytes);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.SZH276KU.js
  var CsFormatDate = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.localize = new LocalizeController2(this);
      this.date = /* @__PURE__ */ new Date();
      this.hourFormat = "auto";
    }
    static get styles() {
      return [];
    }
    render() {
      const date = new Date(this.date);
      const hour12 = this.hourFormat === "auto" ? void 0 : this.hourFormat === "12";
      if (isNaN(date.getMilliseconds())) {
        return void 0;
      }
      const displayDate = this.localize.date(date, {
        weekday: this.weekday,
        era: this.era,
        year: this.year,
        month: this.month,
        day: this.day,
        hour: this.hour,
        minute: this.minute,
        second: this.second,
        timeZoneName: this.timeZoneName,
        timeZone: this.timeZone,
        hour12
      });
      return b2`<time datetime=${date.toISOString()}>${displayDate}</time>`;
    }
  };
  __decorateClass([
    n4()
  ], CsFormatDate.prototype, "date", 2);
  __decorateClass([
    n4()
  ], CsFormatDate.prototype, "weekday", 2);
  __decorateClass([
    n4()
  ], CsFormatDate.prototype, "era", 2);
  __decorateClass([
    n4()
  ], CsFormatDate.prototype, "year", 2);
  __decorateClass([
    n4()
  ], CsFormatDate.prototype, "month", 2);
  __decorateClass([
    n4()
  ], CsFormatDate.prototype, "day", 2);
  __decorateClass([
    n4()
  ], CsFormatDate.prototype, "hour", 2);
  __decorateClass([
    n4()
  ], CsFormatDate.prototype, "minute", 2);
  __decorateClass([
    n4()
  ], CsFormatDate.prototype, "second", 2);
  __decorateClass([
    n4({ attribute: "time-zone-name" })
  ], CsFormatDate.prototype, "timeZoneName", 2);
  __decorateClass([
    n4({ attribute: "time-zone" })
  ], CsFormatDate.prototype, "timeZone", 2);
  __decorateClass([
    n4({ attribute: "hour-format" })
  ], CsFormatDate.prototype, "hourFormat", 2);
  CsFormatDate = __decorateClass([
    customElement("cs-format-date")
  ], CsFormatDate);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.KLDSUYP3.js
  var CsFormatNumber = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.localize = new LocalizeController2(this);
      this.value = 0;
      this.type = "decimal";
      this.withoutGrouping = false;
      this.currency = "USD";
      this.currencyDisplay = "symbol";
    }
    static get styles() {
      return [];
    }
    render() {
      if (isNaN(this.value)) {
        return "";
      }
      return this.localize.number(this.value, {
        style: this.type,
        currency: this.currency,
        currencyDisplay: this.currencyDisplay,
        useGrouping: !this.withoutGrouping,
        minimumIntegerDigits: this.minimumIntegerDigits,
        minimumFractionDigits: this.minimumFractionDigits,
        maximumFractionDigits: this.maximumFractionDigits,
        minimumSignificantDigits: this.minimumSignificantDigits,
        maximumSignificantDigits: this.maximumSignificantDigits
      });
    }
  };
  __decorateClass([
    n4({ type: Number })
  ], CsFormatNumber.prototype, "value", 2);
  __decorateClass([
    n4()
  ], CsFormatNumber.prototype, "type", 2);
  __decorateClass([
    n4({ attribute: "without-grouping", type: Boolean })
  ], CsFormatNumber.prototype, "withoutGrouping", 2);
  __decorateClass([
    n4()
  ], CsFormatNumber.prototype, "currency", 2);
  __decorateClass([
    n4({ attribute: "currency-display" })
  ], CsFormatNumber.prototype, "currencyDisplay", 2);
  __decorateClass([
    n4({ attribute: "minimum-integer-digits", type: Number })
  ], CsFormatNumber.prototype, "minimumIntegerDigits", 2);
  __decorateClass([
    n4({ attribute: "minimum-fraction-digits", type: Number })
  ], CsFormatNumber.prototype, "minimumFractionDigits", 2);
  __decorateClass([
    n4({ attribute: "maximum-fraction-digits", type: Number })
  ], CsFormatNumber.prototype, "maximumFractionDigits", 2);
  __decorateClass([
    n4({ attribute: "minimum-significant-digits", type: Number })
  ], CsFormatNumber.prototype, "minimumSignificantDigits", 2);
  __decorateClass([
    n4({ attribute: "maximum-significant-digits", type: Number })
  ], CsFormatNumber.prototype, "maximumSignificantDigits", 2);
  CsFormatNumber = __decorateClass([
    customElement("cs-format-number")
  ], CsFormatNumber);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.PPR6LABH.js
  var CsIncludeErrorEvent = class extends Event {
    constructor(detail) {
      super("cs-include-error", { bubbles: true, cancelable: false, composed: true });
      this.detail = detail;
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.ZPQHUNDD.js
  var include_styles_default = i`
  :host {
    display: block;
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.LRMM7ZSN.js
  var includeFiles = /* @__PURE__ */ new Map();
  function requestInclude(src, mode = "cors") {
    const prev = includeFiles.get(src);
    if (prev !== void 0) {
      return Promise.resolve(prev);
    }
    const fileDataPromise = fetch(src, { mode }).then(async (response) => {
      const res = {
        ok: response.ok,
        status: response.status,
        html: await response.text()
      };
      includeFiles.set(src, res);
      return res;
    });
    includeFiles.set(src, fileDataPromise);
    return fileDataPromise;
  }

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.MK6J2BOJ.js
  var CsInclude = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.mode = "cors";
      this.allowScripts = false;
    }
    executeScript(script) {
      const newScript = document.createElement("script");
      [...script.attributes].forEach((attr) => newScript.setAttribute(attr.name, attr.value));
      newScript.textContent = script.textContent;
      script.parentNode.replaceChild(newScript, script);
    }
    /** Clones the contents of an element — a template's `content`, or any other element's children — for insertion. */
    cloneFragment(element, ownerDocument) {
      const content = element.localName === "template" ? element.content : this.childNodesToFragment(element);
      return ownerDocument.importNode(content, true);
    }
    childNodesToFragment(element) {
      const fragment = element.ownerDocument.createDocumentFragment();
      element.childNodes.forEach((child) => fragment.append(child.cloneNode(true)));
      return fragment;
    }
    async handleSrcChange() {
      try {
        const src = this.src;
        const url = new URL(src, document.baseURI);
        const fragmentId = url.hash.slice(1);
        if (src.startsWith("#")) {
          const element = fragmentId ? document.getElementById(decodeURIComponent(fragmentId)) : null;
          if (element) {
            this.replaceChildren(this.cloneFragment(element, document));
          } else {
            this.replaceChildren();
          }
          this.dispatchEvent(new CsLoadEvent());
          return;
        }
        let fetchSrc = src;
        if (fragmentId) {
          url.hash = "";
          fetchSrc = url.href;
        }
        const file = await requestInclude(fetchSrc, this.mode);
        if (src !== this.src) {
          return;
        }
        if (!file.ok) {
          this.dispatchEvent(new CsIncludeErrorEvent({ status: file.status }));
          return;
        }
        if (fragmentId) {
          const doc = new DOMParser().parseFromString(file.html, "text/html");
          const element = doc.getElementById(decodeURIComponent(fragmentId));
          if (!element) {
            this.dispatchEvent(new CsIncludeErrorEvent({ status: file.status }));
            return;
          }
          this.replaceChildren(this.cloneFragment(element, document));
        } else {
          this.innerHTML = file.html;
        }
        if (this.allowScripts) {
          [...this.querySelectorAll("script")].forEach((script) => this.executeScript(script));
        }
        this.dispatchEvent(new CsLoadEvent());
      } catch {
        this.dispatchEvent(new CsIncludeErrorEvent({ status: -1 }));
      }
    }
    render() {
      return b2`<slot></slot>`;
    }
  };
  CsInclude.css = include_styles_default;
  __decorateClass([
    n4()
  ], CsInclude.prototype, "src", 2);
  __decorateClass([
    n4()
  ], CsInclude.prototype, "mode", 2);
  __decorateClass([
    n4({ attribute: "allow-scripts", type: Boolean })
  ], CsInclude.prototype, "allowScripts", 2);
  __decorateClass([
    watch("src")
  ], CsInclude.prototype, "handleSrcChange", 1);
  CsInclude = __decorateClass([
    customElement("cs-include")
  ], CsInclude);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.Y75774KV.js
  var CsIntersectEvent = class extends Event {
    constructor(detail) {
      super("cs-intersect", { bubbles: true, cancelable: false, composed: true });
      this.detail = detail;
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.PISVMULO.js
  var intersection_observer_styles_default = i`
  :host {
    display: contents;
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.R7SQD33I.js
  var CsIntersectionObserver = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.intersectionObserver = null;
      this.observedElements = /* @__PURE__ */ new Map();
      this.root = null;
      this.rootMargin = "0px";
      this.threshold = "0";
      this.intersectClass = "";
      this.once = false;
      this.disabled = false;
    }
    connectedCallback() {
      super.connectedCallback();
      if (!this.disabled) {
        this.updateComplete.then(() => {
          this.startObserver();
        });
      }
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this.stopObserver();
    }
    handleSlotChange() {
      if (!this.disabled) {
        this.startObserver();
      }
    }
    /** Converts threshold property string into numeric array. */
    parseThreshold() {
      const tokens = parseSpaceDelimitedTokens(this.threshold);
      return tokens.map((token) => {
        const num = parseFloat(token);
        return isNaN(num) ? 0 : clamp(num, 0, 1);
      });
    }
    /** Locates and returns the root element using the specified ID. */
    resolveRoot() {
      if (!this.root) {
        return null;
      }
      try {
        const doc = this.getRootNode();
        const target = doc.getElementById(this.root);
        if (!target) {
          console.warn(`Root element with ID "${this.root}" could not be found.`, this);
        }
        return target;
      } catch {
        console.warn(`Invalid selector for root: "${this.root}"`, this);
        return null;
      }
    }
    /** Initializes or reinitializes the intersection observer instance. */
    startObserver() {
      this.stopObserver();
      if (this.disabled) {
        return;
      }
      const threshold = this.parseThreshold();
      const rootElement = this.resolveRoot();
      this.intersectionObserver = new IntersectionObserver(
        (entries) => {
          entries.forEach((entry) => {
            const wasIntersecting = this.observedElements.get(entry.target) ?? false;
            const isIntersecting = entry.isIntersecting;
            this.observedElements.set(entry.target, isIntersecting);
            if (this.intersectClass) {
              if (isIntersecting) {
                entry.target.classList.add(this.intersectClass);
              } else {
                entry.target.classList.remove(this.intersectClass);
              }
            }
            const changeEvent = new CsIntersectEvent({ entry });
            this.dispatchEvent(changeEvent);
            if (isIntersecting && !wasIntersecting) {
              if (this.once) {
                this.intersectionObserver?.unobserve(entry.target);
                this.observedElements.delete(entry.target);
              }
            }
          });
        },
        {
          root: rootElement,
          rootMargin: this.rootMargin,
          threshold
        }
      );
      const slot = this.shadowRoot.querySelector("slot");
      if (slot !== null) {
        const elements = slot.assignedElements({ flatten: true });
        elements.forEach((element) => {
          this.intersectionObserver?.observe(element);
          this.observedElements.set(element, false);
        });
      }
    }
    /** Halts the intersection observer and cleans up. */
    stopObserver() {
      if (this.intersectClass) {
        this.observedElements.forEach((_2, element) => {
          element.classList.remove(this.intersectClass);
        });
      }
      this.intersectionObserver?.disconnect();
      this.intersectionObserver = null;
      this.observedElements.clear();
    }
    handleDisabledChange() {
      if (this.disabled) {
        this.stopObserver();
      } else {
        this.startObserver();
      }
    }
    handleOptionsChange() {
      this.startObserver();
    }
    render() {
      return b2` <slot @slotchange=${this.handleSlotChange}></slot> `;
    }
  };
  CsIntersectionObserver.css = intersection_observer_styles_default;
  __decorateClass([
    n4()
  ], CsIntersectionObserver.prototype, "root", 2);
  __decorateClass([
    n4({ attribute: "root-margin" })
  ], CsIntersectionObserver.prototype, "rootMargin", 2);
  __decorateClass([
    n4()
  ], CsIntersectionObserver.prototype, "threshold", 2);
  __decorateClass([
    n4({ attribute: "intersect-class" })
  ], CsIntersectionObserver.prototype, "intersectClass", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsIntersectionObserver.prototype, "once", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsIntersectionObserver.prototype, "disabled", 2);
  __decorateClass([
    watch("disabled", { waitUntilFirstUpdate: true })
  ], CsIntersectionObserver.prototype, "handleDisabledChange", 1);
  __decorateClass([
    watch("root", { waitUntilFirstUpdate: true }),
    watch("rootMargin", { waitUntilFirstUpdate: true }),
    watch("threshold", { waitUntilFirstUpdate: true })
  ], CsIntersectionObserver.prototype, "handleOptionsChange", 1);
  CsIntersectionObserver = __decorateClass([
    customElement("cs-intersection-observer")
  ], CsIntersectionObserver);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.SPKRX6EI.js
  var orderCache = /* @__PURE__ */ new Map();
  function localeFieldOrder(locale) {
    const key = locale || "en";
    const cached = orderCache.get(key);
    if (cached) {
      return cached;
    }
    const formatter = new Intl.DateTimeFormat(key, {
      year: "numeric",
      month: "2-digit",
      day: "2-digit",
      calendar: "gregory",
      numberingSystem: "latn"
    });
    const parts = formatter.formatToParts(new Date(2026, 0, 23));
    const order = [];
    for (const part of parts) {
      if (part.type === "year" || part.type === "month" || part.type === "day") {
        order.push(part.type);
      }
    }
    const resolved = order.length === 3 ? order : ["month", "day", "year"];
    orderCache.set(key, resolved);
    return resolved;
  }

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.3APUOJ36.js
  var PartialDateValidator = () => {
    return {
      checkValidity(element) {
        const host = element;
        const parts = host.parts;
        const empty = parts.day === "" && parts.month === "" && parts.year === "";
        if (empty) {
          return { isValid: true, invalidKeys: [], message: "" };
        }
        if (host.value === "") {
          const message = host.localize?.term("incompleteDate") || "Enter a valid date.";
          return { isValid: false, invalidKeys: ["badInput"], message };
        }
        return { isValid: true, invalidKeys: [], message: "" };
      }
    };
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.SIXRQME2.js
  var EMPTY_PARTS = { day: "", month: "", year: "" };
  function isComplete(parts) {
    return parts.day !== "" && parts.month !== "" && parts.year !== "";
  }
  function partsToIso(parts) {
    if (!isComplete(parts)) {
      return "";
    }
    const year = Number(parts.year);
    const month = Number(parts.month);
    const day = Number(parts.day);
    if (!Number.isInteger(year) || year < 1 || year > 9999) {
      return "";
    }
    if (!Number.isInteger(month) || month < 1 || month > 12) {
      return "";
    }
    if (!Number.isInteger(day) || day < 1 || day > 31) {
      return "";
    }
    const date = new Date(2e3, month - 1, day);
    date.setFullYear(year);
    if (date.getFullYear() !== year || date.getMonth() !== month - 1 || date.getDate() !== day) {
      return "";
    }
    return `${String(year).padStart(4, "0")}-${String(month).padStart(2, "0")}-${String(day).padStart(2, "0")}`;
  }
  function isoToParts(iso) {
    if (!iso) {
      return { ...EMPTY_PARTS };
    }
    const match = /^(\d{4})-(\d{2})-(\d{2})$/.exec(iso);
    if (!match) {
      return { ...EMPTY_PARTS };
    }
    return { year: match[1], month: match[2], day: match[3] };
  }

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.3E6Z6QTN.js
  var known_date_styles_default = i`
  :host {
    display: block;
    container-type: inline-size;
    container-name: known-date;
  }

  [part~='fieldset'],
  .fieldset {
    border: 0;
    padding: 0;
    margin: 0;
    min-inline-size: 0;
  }

  legend[part~='legend'] {
    padding: 0;
    display: block;
  }

  /* The legend's inner span carries the form-control-label part so the existing form-control styles
     (including the required asterisk) apply consistently across browsers. */
  .label {
    display: inline-block;
  }

  [part~='fields'] {
    display: flex;
    gap: var(--cs-space-xs);
    align-items: start;
    inline-size: 100%;
    min-inline-size: 0;
  }

  [part~='field'] {
    display: flex;
    flex-direction: column;
    flex: 1 1 0;
    min-inline-size: 0;
  }

  /* Day and month each hold two digits; year holds four. Bias the flex distribution so the year
     field gets roughly twice the share of the row but all three still grow and shrink together. */
  [part~='field-month'],
  [part~='field-day'] {
    min-inline-size: 2.5em;
  }

  [part~='field-year'] {
    flex-grow: 2;
    min-inline-size: 6em;
  }

  /* Per-field labels match the hint's typography and spacing exactly (the same 0.5em offset other
     form controls use between their input and hint) so the gap below each input reads as native. */
  [part~='field-label'] {
    color: var(--cs-form-control-hint-color);
    font-weight: var(--cs-form-control-hint-font-weight);
    line-height: var(--cs-form-control-hint-line-height);
    font-size: var(--cs-font-size-smaller);
    margin-block-start: 0.5em;
  }

  /* Each input is styled to match cs-input's .text-field wrapper directly — same border, height,
     padding, focus ring, and appearance variants. The host doesn't compose cs-input instances because
     we want three discrete native inputs (no clear/password slots, simpler DOM), but the visual contract
     is identical. */
  [part~='field-input'] {
    -webkit-appearance: none;
    appearance: none;
    box-sizing: border-box;
    height: var(--cs-form-control-height);
    inline-size: 100%;
    min-inline-size: 0;
    border-style: var(--cs-form-control-border-style);
    border-width: var(--cs-form-control-border-width);
    border-color: var(--cs-form-control-border-color);
    border-radius: var(--cs-form-control-border-radius);
    background-color: var(--cs-form-control-background-color);
    color: var(--cs-form-control-value-color);
    font-family: inherit;
    font-size: inherit;
    font-weight: var(--cs-form-control-value-font-weight);
    line-height: var(--cs-form-control-value-line-height);
    padding: 0 var(--cs-form-control-padding-inline);
    outline: var(--cs-focus-ring-style) var(--cs-focus-ring-width) transparent;
    outline-offset: var(--cs-focus-ring-offset);
    transition:
      background-color var(--cs-transition-normal),
      border-color var(--cs-transition-normal),
      outline-color var(--cs-transition-fast);
    transition-timing-function: var(--cs-transition-easing);
  }

  [part~='field-input']:focus {
    outline-color: var(--cs-color-focus);
  }

  /* When the fields row gets too narrow to comfortably hold three side-by-side inputs, stack them
     vertically. The threshold reflects the smallest width at which all three inputs still fit a
     four-digit year plus padding without truncation. */
  @container known-date (inline-size < 300px) {
    [part~='fields'] {
      flex-direction: column;
      align-items: stretch;
    }
  }

  /* Suppress the native number spin buttons so a paste that briefly looks like a number can't show them. */
  [part~='field-input']::-webkit-outer-spin-button,
  [part~='field-input']::-webkit-inner-spin-button {
    -webkit-appearance: none;
    margin: 0;
  }

  /* Hide the mirror used for native form-data + constraint validation. */
  .value-input {
    position: absolute;
    inline-size: 1px;
    block-size: 1px;
    opacity: 0;
    pointer-events: none;
    border: 0;
    padding: 0;
    margin: 0;
    clip: rect(0 0 0 0);
    overflow: hidden;
  }

  /* Appearances — mirror cs-input's .text-field appearance variants exactly. */
  :host([appearance='outlined']) [part~='field-input'] {
    background-color: var(--cs-form-control-background-color);
    border-color: var(--cs-form-control-border-color);
  }

  :host([appearance='filled']) [part~='field-input'] {
    background-color: var(--cs-color-neutral-fill-quiet);
    border-color: var(--cs-color-neutral-fill-quiet);
  }

  :host([appearance='filled-outlined']) [part~='field-input'] {
    background-color: var(--cs-color-neutral-fill-quiet);
    border-color: var(--cs-form-control-border-color);
  }

  :host([pill]) [part~='field-input'] {
    border-radius: var(--cs-border-radius-pill) !important;
  }

  /* Disabled — mirror cs-input's :has(:disabled) opacity treatment. */
  :host(:state(disabled)) [part~='field'],
  [part~='field-input']:disabled {
    cursor: not-allowed;
    opacity: 0.5;
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.UYDDFXXJ.js
  var generateId = () => uniqueId("cs-known-date-");
  var CsKnownDate = class extends CornerstoneFormAssociatedElement {
    constructor() {
      super(...arguments);
      this.assumeInteractionOn = ["input"];
      this.localize = new LocalizeController2(this);
      this.hasSlotController = new HasSlotController(this, "hint", "label");
      this.groupId = generateId();
      this.hintId = `${this.groupId}-hint`;
      this.lastEmittedValue = "";
      this.pendingValue = null;
      this.parts = { ...EMPTY_PARTS };
      this.name = "";
      this._value = "";
      this.defaultValue = this.getAttribute("value") ?? "";
      this.disabled = false;
      this.required = false;
      this.readonly = false;
      this.size = "m";
      this.appearance = "outlined";
      this.pill = false;
      this.label = "";
      this.hint = "";
      this.autocomplete = "";
      this.min = "";
      this.max = "";
      this.locale = "";
      this.ssrLabel = false;
      this.ssrHint = false;
      this.handleFieldInput = (event) => {
        if (this.readonly) {
          return;
        }
        const target = event.currentTarget;
        const field = target.dataset.field;
        const max2 = field === "year" ? 4 : 2;
        const sanitized = target.value.replace(/\D/g, "").slice(0, max2);
        if (sanitized !== target.value) {
          target.value = sanitized;
        }
        this.parts = { ...this.parts, [field]: sanitized };
        this.recomputeValue();
        this.requestUpdate();
      };
    }
    static get validators() {
      return [
        ...super.validators,
        // Order matters: the first failing validator's message wins. `PartialDateValidator` comes first so a partial
        // or out-of-range entry (day 32, Feb 30) reports "Enter a valid date" as `badInput`, rather than
        // `RequiredValidator`'s "Please fill out this field" (the mirror is empty whenever the entry doesn't
        // compose).
        PartialDateValidator(),
        RequiredValidator({
          validationElement: () => Object.assign(document.createElement("input"), { required: true })
        }),
        // Mirrors the hidden native `<input type="date">` so its min/max constraints surface as
        // rangeUnderflow/rangeOverflow validity.
        MirrorValidator()
      ];
    }
    /**
     * The committed value as an ISO `YYYY-MM-DD` string. The setter also accepts a `Date` or `null`. Reading
     * returns an empty string when the value is blank or any field is only partially filled.
     */
    get value() {
      if (this.valueHasChanged) {
        return this._value;
      }
      return this._value || this.defaultValue || "";
    }
    set value(val) {
      const normalized = this.normalizeIncomingValue(val);
      if (normalized === this._value) {
        return;
      }
      const oldValue = this._value;
      this._value = normalized;
      this.valueHasChanged = true;
      if (this.hasUpdated) {
        this.syncPartsFromCanonical();
      } else {
        this.pendingValue = this._value;
      }
      this.requestUpdate("value", oldValue);
    }
    //
    // Lifecycle
    //
    firstUpdated(changedProperties) {
      super.firstUpdated(changedProperties);
      if (this.pendingValue != null) {
        this._value = this.pendingValue;
        this.pendingValue = null;
      } else if (!this._value && this.defaultValue) {
        this._value = this.defaultValue;
      }
      this.syncPartsFromCanonical();
      this.input = this.valueInput;
      this.updateValidity();
      this.lastEmittedValue = this._value;
    }
    updated(changed) {
      super.updated(changed);
      if (changed.has("value")) {
        this.customStates.set("blank", !this._value);
      }
    }
    //
    // Public API
    //
    /** Focuses the first empty field, or the first field when all are filled. */
    focus(options2) {
      const target = this.firstFocusableInput();
      target?.focus(options2);
    }
    /** Removes focus from the known date. */
    blur() {
      const active = this.shadowRoot?.activeElement;
      active?.blur();
    }
    /** The committed value as a `Date`, or `null` when the value is empty/invalid. */
    get valueAsDate() {
      if (!this._value) {
        return null;
      }
      const match = /^(\d{4})-(\d{2})-(\d{2})$/.exec(this._value);
      if (!match) {
        return null;
      }
      return new Date(Number(match[1]), Number(match[2]) - 1, Number(match[3]));
    }
    /**
     * Anchor native validation popups on a real visible input. The hidden mirror handles form data, but
     * anchoring a popup on `display: none` content would render it at offset (0, 0).
     */
    get validationTarget() {
      if (!this.shadowRoot) {
        return void 0;
      }
      const inputs = Array.from(this.shadowRoot.querySelectorAll('input[part~="field-input"]'));
      if (inputs.length === 0) {
        return void 0;
      }
      const offending = this.firstInvalidField();
      if (offending) {
        const el = inputs.find((i10) => i10.dataset.field === offending);
        if (el) {
          return el;
        }
      }
      return inputs[0];
    }
    //
    // Form association
    //
    formResetCallback() {
      this._value = this.defaultValue;
      this.valueHasChanged = false;
      this.syncPartsFromCanonical();
      super.formResetCallback();
      this.lastEmittedValue = this._value;
      this.requestUpdate();
    }
    formStateRestoreCallback(state2) {
      if (typeof state2 === "string") {
        this.value = state2;
      }
      this.updateValidity();
    }
    //
    // Internal helpers
    //
    get resolvedLocale() {
      return this.locale || this.localize.lang() || "en";
    }
    fieldOrder() {
      return localeFieldOrder(this.resolvedLocale);
    }
    normalizeIncomingValue(val) {
      if (val == null) {
        return "";
      }
      if (val instanceof Date) {
        const y3 = String(val.getFullYear()).padStart(4, "0");
        const m3 = String(val.getMonth() + 1).padStart(2, "0");
        const d3 = String(val.getDate()).padStart(2, "0");
        return `${y3}-${m3}-${d3}`;
      }
      if (typeof val === "string") {
        const parts = isoToParts(val);
        return partsToIso(parts);
      }
      return "";
    }
    syncPartsFromCanonical() {
      this.parts = isoToParts(this._value);
      this.updateHiddenInput();
    }
    updateHiddenInput() {
      if (this.valueInput) {
        this.valueInput.value = this._value;
      }
      this.setValue(this._value || null);
    }
    recomputeValue() {
      const oldValue = this._value;
      const newValue = partsToIso(this.parts);
      if (newValue !== oldValue) {
        this._value = newValue;
        this.valueHasChanged = true;
        this.updateHiddenInput();
        this.updateValidity();
      }
      this.dispatchEvent(new InputEvent("input", { bubbles: true, composed: true }));
      if (newValue !== this.lastEmittedValue) {
        this.lastEmittedValue = newValue;
        this.dispatchEvent(new Event("change", { bubbles: true, composed: true }));
      }
    }
    firstFocusableInput() {
      if (!this.shadowRoot) {
        return void 0;
      }
      const inputs = Array.from(this.shadowRoot.querySelectorAll('input[part~="field-input"]'));
      for (const field of this.fieldOrder()) {
        if (this.parts[field] === "") {
          const el = inputs.find((i10) => i10.dataset.field === field);
          if (el) {
            return el;
          }
        }
      }
      return inputs[0];
    }
    /**
     * Returns the field to fix when the value doesn't compose, in locale order: the first empty field, else the first
     * out-of-range field (year < 1, month not 1–12, day not 1–31), else the day (e.g. Feb 30). Null when the value
     * composes cleanly.
     */
    firstInvalidField() {
      if (this._value) {
        return null;
      }
      const order = this.fieldOrder();
      const empty = order.find((field) => this.parts[field] === "");
      if (empty) {
        return empty;
      }
      const inRange = {
        year: (n9) => Number.isInteger(n9) && n9 >= 1 && n9 <= 9999,
        month: (n9) => Number.isInteger(n9) && n9 >= 1 && n9 <= 12,
        day: (n9) => Number.isInteger(n9) && n9 >= 1 && n9 <= 31
      };
      const outOfRange = order.find((field) => !inRange[field](Number(this.parts[field])));
      if (outOfRange) {
        return outOfRange;
      }
      return "day";
    }
    autocompleteFor(field) {
      const family = this.autocomplete.trim();
      if (!family) {
        return void 0;
      }
      if (family === "bday") {
        if (field === "day") {
          return "bday-day";
        }
        if (field === "month") {
          return "bday-month";
        }
        return "bday-year";
      }
      if (family === "off" || family === "on") {
        return family;
      }
      return field === "year" ? family : void 0;
    }
    //
    // Render
    //
    render() {
      const hasLabelSlot = this.hasSlotController.test("label", "ssrLabel");
      const hasHintSlot = this.hasSlotController.test("hint", "ssrHint");
      const hasLabel = !!this.label || !!hasLabelSlot;
      const hasHint = !!this.hint || !!hasHintSlot;
      const groupAriaLabel = this.label || this.localize.term("date") || "Date";
      const userInvalid = !o5 && this.customStates.has("user-invalid");
      const describedBy = hasHint ? this.hintId : "";
      const fields = this.fieldOrder().map((field) => this.renderField(field, describedBy, userInvalid));
      const groupContent = b2`
      <div part="known-date form-control-input fields" class="fields">${fields}</div>

      <slot
        name="hint"
        part="hint"
        id=${this.hintId}
        class=${e7({ hint: true, "has-slotted": hasHint })}
        aria-hidden=${hasHint ? "false" : "true"}
      >
        ${this.hint}
      </slot>
    `;
      return b2`
      <div
        part="form-control"
        class=${e7({
        "form-control": true,
        "form-control-has-label": hasLabel
      })}
      >
        ${hasLabel ? b2`<fieldset part="fieldset" class="fieldset">
                <legend part="legend">
                  <span part="form-control-label" class="label">
                    <slot name="label">${this.label}</slot>
                  </span>
                </legend>
                ${groupContent}
              </fieldset>` : b2`<div part="fieldset" class="fieldset" role="group" aria-label=${groupAriaLabel}>
                ${groupContent}
              </div>`}

        <input
          class="value-input"
          type="date"
          tabindex="-1"
          aria-hidden="true"
          .value=${this._value}
          min=${o8(this.min || void 0)}
          max=${o8(this.max || void 0)}
          ?disabled=${this.disabled}
          ?required=${this.required}
        />
      </div>
    `;
    }
    renderField(field, describedBy, userInvalid) {
      const fieldId = `${this.groupId}-${field}`;
      const value = this.parts[field];
      const autocompleteAttr = this.autocompleteFor(field);
      const ariaInvalid = userInvalid ? "true" : void 0;
      const fieldLabel = this.localize.term(field) || (field === "day" ? "Day" : field === "month" ? "Month" : "Year");
      return b2`
      <div part="field field-${field}" class=${e7({ field: true, [`field-${field}`]: true })}>
        <input
          id=${fieldId}
          part="field-input"
          class="field-input"
          type="text"
          inputmode="numeric"
          pattern="[0-9]*"
          maxlength=${field === "year" ? 4 : 2}
          data-field=${field}
          autocomplete=${o8(autocompleteAttr)}
          aria-describedby=${o8(describedBy || void 0)}
          aria-invalid=${o8(ariaInvalid)}
          aria-required=${this.required ? "true" : "false"}
          .value=${l5(value)}
          ?disabled=${this.disabled}
          ?readonly=${this.readonly}
          @input=${this.handleFieldInput}
        />
        <label part="field-label" class="field-label" for=${fieldId}>${fieldLabel}</label>
      </div>
    `;
    }
  };
  CsKnownDate.css = [size_styles_default, form_control_styles_default, known_date_styles_default];
  CsKnownDate.shadowRootOptions = {
    ...CornerstoneFormAssociatedElement.shadowRootOptions,
    delegatesFocus: true
  };
  __decorateClass([
    e5(".value-input")
  ], CsKnownDate.prototype, "valueInput", 2);
  __decorateClass([
    r5()
  ], CsKnownDate.prototype, "parts", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsKnownDate.prototype, "name", 2);
  __decorateClass([
    r5()
  ], CsKnownDate.prototype, "value", 1);
  __decorateClass([
    n4({ attribute: "value", reflect: true })
  ], CsKnownDate.prototype, "defaultValue", 2);
  __decorateClass([
    n4({ type: Boolean })
  ], CsKnownDate.prototype, "disabled", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsKnownDate.prototype, "required", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsKnownDate.prototype, "readonly", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsKnownDate.prototype, "size", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsKnownDate.prototype, "appearance", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsKnownDate.prototype, "pill", 2);
  __decorateClass([
    n4()
  ], CsKnownDate.prototype, "label", 2);
  __decorateClass([
    n4({ attribute: "hint" })
  ], CsKnownDate.prototype, "hint", 2);
  __decorateClass([
    n4()
  ], CsKnownDate.prototype, "autocomplete", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsKnownDate.prototype, "min", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsKnownDate.prototype, "max", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsKnownDate.prototype, "locale", 2);
  __decorateClass([
    n4({ attribute: "ssr-label", type: Boolean })
  ], CsKnownDate.prototype, "ssrLabel", 2);
  __decorateClass([
    n4({ attribute: "ssr-hint", type: Boolean })
  ], CsKnownDate.prototype, "ssrHint", 2);
  CsKnownDate = __decorateClass([
    customElement("cs-known-date")
  ], CsKnownDate);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.L3MARBHQ.js
  var markdown_styles_default = i`
  :host {
    display: contents;
  }
`;

  // node_modules/marked/lib/marked.esm.js
  function _getDefaults() {
    return {
      async: false,
      breaks: false,
      extensions: null,
      gfm: true,
      hooks: null,
      pedantic: false,
      renderer: null,
      silent: false,
      tokenizer: null,
      walkTokens: null
    };
  }
  var _defaults = _getDefaults();
  function changeDefaults(newDefaults) {
    _defaults = newDefaults;
  }
  var escapeTest = /[&<>"']/;
  var escapeReplace = new RegExp(escapeTest.source, "g");
  var escapeTestNoEncode = /[<>"']|&(?!(#\d{1,7}|#[Xx][a-fA-F0-9]{1,6}|\w+);)/;
  var escapeReplaceNoEncode = new RegExp(escapeTestNoEncode.source, "g");
  var escapeReplacements = {
    "&": "&amp;",
    "<": "&lt;",
    ">": "&gt;",
    '"': "&quot;",
    "'": "&#39;"
  };
  var getEscapeReplacement = (ch) => escapeReplacements[ch];
  function escape$1(html2, encode) {
    if (encode) {
      if (escapeTest.test(html2)) {
        return html2.replace(escapeReplace, getEscapeReplacement);
      }
    } else {
      if (escapeTestNoEncode.test(html2)) {
        return html2.replace(escapeReplaceNoEncode, getEscapeReplacement);
      }
    }
    return html2;
  }
  var unescapeTest = /&(#(?:\d+)|(?:#x[0-9A-Fa-f]+)|(?:\w+));?/ig;
  function unescape(html2) {
    return html2.replace(unescapeTest, (_2, n9) => {
      n9 = n9.toLowerCase();
      if (n9 === "colon")
        return ":";
      if (n9.charAt(0) === "#") {
        return n9.charAt(1) === "x" ? String.fromCharCode(parseInt(n9.substring(2), 16)) : String.fromCharCode(+n9.substring(1));
      }
      return "";
    });
  }
  var caret = /(^|[^\[])\^/g;
  function edit(regex, opt) {
    let source = typeof regex === "string" ? regex : regex.source;
    opt = opt || "";
    const obj = {
      replace: (name, val) => {
        let valSource = typeof val === "string" ? val : val.source;
        valSource = valSource.replace(caret, "$1");
        source = source.replace(name, valSource);
        return obj;
      },
      getRegex: () => {
        return new RegExp(source, opt);
      }
    };
    return obj;
  }
  function cleanUrl(href) {
    try {
      href = encodeURI(href).replace(/%25/g, "%");
    } catch (e12) {
      return null;
    }
    return href;
  }
  var noopTest = { exec: () => null };
  function splitCells(tableRow, count) {
    const row = tableRow.replace(/\|/g, (match, offset3, str) => {
      let escaped = false;
      let curr = offset3;
      while (--curr >= 0 && str[curr] === "\\")
        escaped = !escaped;
      if (escaped) {
        return "|";
      } else {
        return " |";
      }
    }), cells = row.split(/ \|/);
    let i10 = 0;
    if (!cells[0].trim()) {
      cells.shift();
    }
    if (cells.length > 0 && !cells[cells.length - 1].trim()) {
      cells.pop();
    }
    if (count) {
      if (cells.length > count) {
        cells.splice(count);
      } else {
        while (cells.length < count)
          cells.push("");
      }
    }
    for (; i10 < cells.length; i10++) {
      cells[i10] = cells[i10].trim().replace(/\\\|/g, "|");
    }
    return cells;
  }
  function rtrim(str, c6, invert) {
    const l6 = str.length;
    if (l6 === 0) {
      return "";
    }
    let suffLen = 0;
    while (suffLen < l6) {
      const currChar = str.charAt(l6 - suffLen - 1);
      if (currChar === c6 && !invert) {
        suffLen++;
      } else if (currChar !== c6 && invert) {
        suffLen++;
      } else {
        break;
      }
    }
    return str.slice(0, l6 - suffLen);
  }
  function findClosingBracket(str, b3) {
    if (str.indexOf(b3[1]) === -1) {
      return -1;
    }
    let level = 0;
    for (let i10 = 0; i10 < str.length; i10++) {
      if (str[i10] === "\\") {
        i10++;
      } else if (str[i10] === b3[0]) {
        level++;
      } else if (str[i10] === b3[1]) {
        level--;
        if (level < 0) {
          return i10;
        }
      }
    }
    return -1;
  }
  function outputLink(cap, link2, raw, lexer2) {
    const href = link2.href;
    const title = link2.title ? escape$1(link2.title) : null;
    const text = cap[1].replace(/\\([\[\]])/g, "$1");
    if (cap[0].charAt(0) !== "!") {
      lexer2.state.inLink = true;
      const token = {
        type: "link",
        raw,
        href,
        title,
        text,
        tokens: lexer2.inlineTokens(text)
      };
      lexer2.state.inLink = false;
      return token;
    }
    return {
      type: "image",
      raw,
      href,
      title,
      text: escape$1(text)
    };
  }
  function indentCodeCompensation(raw, text) {
    const matchIndentToCode = raw.match(/^(\s+)(?:```)/);
    if (matchIndentToCode === null) {
      return text;
    }
    const indentToCode = matchIndentToCode[1];
    return text.split("\n").map((node) => {
      const matchIndentInNode = node.match(/^\s+/);
      if (matchIndentInNode === null) {
        return node;
      }
      const [indentInNode] = matchIndentInNode;
      if (indentInNode.length >= indentToCode.length) {
        return node.slice(indentToCode.length);
      }
      return node;
    }).join("\n");
  }
  var _Tokenizer = class {
    options;
    rules;
    // set by the lexer
    lexer;
    // set by the lexer
    constructor(options2) {
      this.options = options2 || _defaults;
    }
    space(src) {
      const cap = this.rules.block.newline.exec(src);
      if (cap && cap[0].length > 0) {
        return {
          type: "space",
          raw: cap[0]
        };
      }
    }
    code(src) {
      const cap = this.rules.block.code.exec(src);
      if (cap) {
        const text = cap[0].replace(/^ {1,4}/gm, "");
        return {
          type: "code",
          raw: cap[0],
          codeBlockStyle: "indented",
          text: !this.options.pedantic ? rtrim(text, "\n") : text
        };
      }
    }
    fences(src) {
      const cap = this.rules.block.fences.exec(src);
      if (cap) {
        const raw = cap[0];
        const text = indentCodeCompensation(raw, cap[3] || "");
        return {
          type: "code",
          raw,
          lang: cap[2] ? cap[2].trim().replace(this.rules.inline.anyPunctuation, "$1") : cap[2],
          text
        };
      }
    }
    heading(src) {
      const cap = this.rules.block.heading.exec(src);
      if (cap) {
        let text = cap[2].trim();
        if (/#$/.test(text)) {
          const trimmed = rtrim(text, "#");
          if (this.options.pedantic) {
            text = trimmed.trim();
          } else if (!trimmed || / $/.test(trimmed)) {
            text = trimmed.trim();
          }
        }
        return {
          type: "heading",
          raw: cap[0],
          depth: cap[1].length,
          text,
          tokens: this.lexer.inline(text)
        };
      }
    }
    hr(src) {
      const cap = this.rules.block.hr.exec(src);
      if (cap) {
        return {
          type: "hr",
          raw: cap[0]
        };
      }
    }
    blockquote(src) {
      const cap = this.rules.block.blockquote.exec(src);
      if (cap) {
        const text = rtrim(cap[0].replace(/^ *>[ \t]?/gm, ""), "\n");
        const top = this.lexer.state.top;
        this.lexer.state.top = true;
        const tokens = this.lexer.blockTokens(text);
        this.lexer.state.top = top;
        return {
          type: "blockquote",
          raw: cap[0],
          tokens,
          text
        };
      }
    }
    list(src) {
      let cap = this.rules.block.list.exec(src);
      if (cap) {
        let bull = cap[1].trim();
        const isordered = bull.length > 1;
        const list2 = {
          type: "list",
          raw: "",
          ordered: isordered,
          start: isordered ? +bull.slice(0, -1) : "",
          loose: false,
          items: []
        };
        bull = isordered ? `\\d{1,9}\\${bull.slice(-1)}` : `\\${bull}`;
        if (this.options.pedantic) {
          bull = isordered ? bull : "[*+-]";
        }
        const itemRegex = new RegExp(`^( {0,3}${bull})((?:[	 ][^\\n]*)?(?:\\n|$))`);
        let raw = "";
        let itemContents = "";
        let endsWithBlankLine = false;
        while (src) {
          let endEarly = false;
          if (!(cap = itemRegex.exec(src))) {
            break;
          }
          if (this.rules.block.hr.test(src)) {
            break;
          }
          raw = cap[0];
          src = src.substring(raw.length);
          let line = cap[2].split("\n", 1)[0].replace(/^\t+/, (t8) => " ".repeat(3 * t8.length));
          let nextLine = src.split("\n", 1)[0];
          let indent = 0;
          if (this.options.pedantic) {
            indent = 2;
            itemContents = line.trimStart();
          } else {
            indent = cap[2].search(/[^ ]/);
            indent = indent > 4 ? 1 : indent;
            itemContents = line.slice(indent);
            indent += cap[1].length;
          }
          let blankLine = false;
          if (!line && /^ *$/.test(nextLine)) {
            raw += nextLine + "\n";
            src = src.substring(nextLine.length + 1);
            endEarly = true;
          }
          if (!endEarly) {
            const nextBulletRegex = new RegExp(`^ {0,${Math.min(3, indent - 1)}}(?:[*+-]|\\d{1,9}[.)])((?:[ 	][^\\n]*)?(?:\\n|$))`);
            const hrRegex = new RegExp(`^ {0,${Math.min(3, indent - 1)}}((?:- *){3,}|(?:_ *){3,}|(?:\\* *){3,})(?:\\n+|$)`);
            const fencesBeginRegex = new RegExp(`^ {0,${Math.min(3, indent - 1)}}(?:\`\`\`|~~~)`);
            const headingBeginRegex = new RegExp(`^ {0,${Math.min(3, indent - 1)}}#`);
            while (src) {
              const rawLine = src.split("\n", 1)[0];
              nextLine = rawLine;
              if (this.options.pedantic) {
                nextLine = nextLine.replace(/^ {1,4}(?=( {4})*[^ ])/g, "  ");
              }
              if (fencesBeginRegex.test(nextLine)) {
                break;
              }
              if (headingBeginRegex.test(nextLine)) {
                break;
              }
              if (nextBulletRegex.test(nextLine)) {
                break;
              }
              if (hrRegex.test(src)) {
                break;
              }
              if (nextLine.search(/[^ ]/) >= indent || !nextLine.trim()) {
                itemContents += "\n" + nextLine.slice(indent);
              } else {
                if (blankLine) {
                  break;
                }
                if (line.search(/[^ ]/) >= 4) {
                  break;
                }
                if (fencesBeginRegex.test(line)) {
                  break;
                }
                if (headingBeginRegex.test(line)) {
                  break;
                }
                if (hrRegex.test(line)) {
                  break;
                }
                itemContents += "\n" + nextLine;
              }
              if (!blankLine && !nextLine.trim()) {
                blankLine = true;
              }
              raw += rawLine + "\n";
              src = src.substring(rawLine.length + 1);
              line = nextLine.slice(indent);
            }
          }
          if (!list2.loose) {
            if (endsWithBlankLine) {
              list2.loose = true;
            } else if (/\n *\n *$/.test(raw)) {
              endsWithBlankLine = true;
            }
          }
          let istask = null;
          let ischecked;
          if (this.options.gfm) {
            istask = /^\[[ xX]\] /.exec(itemContents);
            if (istask) {
              ischecked = istask[0] !== "[ ] ";
              itemContents = itemContents.replace(/^\[[ xX]\] +/, "");
            }
          }
          list2.items.push({
            type: "list_item",
            raw,
            task: !!istask,
            checked: ischecked,
            loose: false,
            text: itemContents,
            tokens: []
          });
          list2.raw += raw;
        }
        list2.items[list2.items.length - 1].raw = raw.trimEnd();
        list2.items[list2.items.length - 1].text = itemContents.trimEnd();
        list2.raw = list2.raw.trimEnd();
        for (let i10 = 0; i10 < list2.items.length; i10++) {
          this.lexer.state.top = false;
          list2.items[i10].tokens = this.lexer.blockTokens(list2.items[i10].text, []);
          if (!list2.loose) {
            const spacers = list2.items[i10].tokens.filter((t8) => t8.type === "space");
            const hasMultipleLineBreaks = spacers.length > 0 && spacers.some((t8) => /\n.*\n/.test(t8.raw));
            list2.loose = hasMultipleLineBreaks;
          }
        }
        if (list2.loose) {
          for (let i10 = 0; i10 < list2.items.length; i10++) {
            list2.items[i10].loose = true;
          }
        }
        return list2;
      }
    }
    html(src) {
      const cap = this.rules.block.html.exec(src);
      if (cap) {
        const token = {
          type: "html",
          block: true,
          raw: cap[0],
          pre: cap[1] === "pre" || cap[1] === "script" || cap[1] === "style",
          text: cap[0]
        };
        return token;
      }
    }
    def(src) {
      const cap = this.rules.block.def.exec(src);
      if (cap) {
        const tag2 = cap[1].toLowerCase().replace(/\s+/g, " ");
        const href = cap[2] ? cap[2].replace(/^<(.*)>$/, "$1").replace(this.rules.inline.anyPunctuation, "$1") : "";
        const title = cap[3] ? cap[3].substring(1, cap[3].length - 1).replace(this.rules.inline.anyPunctuation, "$1") : cap[3];
        return {
          type: "def",
          tag: tag2,
          raw: cap[0],
          href,
          title
        };
      }
    }
    table(src) {
      const cap = this.rules.block.table.exec(src);
      if (!cap) {
        return;
      }
      if (!/[:|]/.test(cap[2])) {
        return;
      }
      const headers = splitCells(cap[1]);
      const aligns = cap[2].replace(/^\||\| *$/g, "").split("|");
      const rows = cap[3] && cap[3].trim() ? cap[3].replace(/\n[ \t]*$/, "").split("\n") : [];
      const item = {
        type: "table",
        raw: cap[0],
        header: [],
        align: [],
        rows: []
      };
      if (headers.length !== aligns.length) {
        return;
      }
      for (const align of aligns) {
        if (/^ *-+: *$/.test(align)) {
          item.align.push("right");
        } else if (/^ *:-+: *$/.test(align)) {
          item.align.push("center");
        } else if (/^ *:-+ *$/.test(align)) {
          item.align.push("left");
        } else {
          item.align.push(null);
        }
      }
      for (const header of headers) {
        item.header.push({
          text: header,
          tokens: this.lexer.inline(header)
        });
      }
      for (const row of rows) {
        item.rows.push(splitCells(row, item.header.length).map((cell) => {
          return {
            text: cell,
            tokens: this.lexer.inline(cell)
          };
        }));
      }
      return item;
    }
    lheading(src) {
      const cap = this.rules.block.lheading.exec(src);
      if (cap) {
        return {
          type: "heading",
          raw: cap[0],
          depth: cap[2].charAt(0) === "=" ? 1 : 2,
          text: cap[1],
          tokens: this.lexer.inline(cap[1])
        };
      }
    }
    paragraph(src) {
      const cap = this.rules.block.paragraph.exec(src);
      if (cap) {
        const text = cap[1].charAt(cap[1].length - 1) === "\n" ? cap[1].slice(0, -1) : cap[1];
        return {
          type: "paragraph",
          raw: cap[0],
          text,
          tokens: this.lexer.inline(text)
        };
      }
    }
    text(src) {
      const cap = this.rules.block.text.exec(src);
      if (cap) {
        return {
          type: "text",
          raw: cap[0],
          text: cap[0],
          tokens: this.lexer.inline(cap[0])
        };
      }
    }
    escape(src) {
      const cap = this.rules.inline.escape.exec(src);
      if (cap) {
        return {
          type: "escape",
          raw: cap[0],
          text: escape$1(cap[1])
        };
      }
    }
    tag(src) {
      const cap = this.rules.inline.tag.exec(src);
      if (cap) {
        if (!this.lexer.state.inLink && /^<a /i.test(cap[0])) {
          this.lexer.state.inLink = true;
        } else if (this.lexer.state.inLink && /^<\/a>/i.test(cap[0])) {
          this.lexer.state.inLink = false;
        }
        if (!this.lexer.state.inRawBlock && /^<(pre|code|kbd|script)(\s|>)/i.test(cap[0])) {
          this.lexer.state.inRawBlock = true;
        } else if (this.lexer.state.inRawBlock && /^<\/(pre|code|kbd|script)(\s|>)/i.test(cap[0])) {
          this.lexer.state.inRawBlock = false;
        }
        return {
          type: "html",
          raw: cap[0],
          inLink: this.lexer.state.inLink,
          inRawBlock: this.lexer.state.inRawBlock,
          block: false,
          text: cap[0]
        };
      }
    }
    link(src) {
      const cap = this.rules.inline.link.exec(src);
      if (cap) {
        const trimmedUrl = cap[2].trim();
        if (!this.options.pedantic && /^</.test(trimmedUrl)) {
          if (!/>$/.test(trimmedUrl)) {
            return;
          }
          const rtrimSlash = rtrim(trimmedUrl.slice(0, -1), "\\");
          if ((trimmedUrl.length - rtrimSlash.length) % 2 === 0) {
            return;
          }
        } else {
          const lastParenIndex = findClosingBracket(cap[2], "()");
          if (lastParenIndex > -1) {
            const start = cap[0].indexOf("!") === 0 ? 5 : 4;
            const linkLen = start + cap[1].length + lastParenIndex;
            cap[2] = cap[2].substring(0, lastParenIndex);
            cap[0] = cap[0].substring(0, linkLen).trim();
            cap[3] = "";
          }
        }
        let href = cap[2];
        let title = "";
        if (this.options.pedantic) {
          const link2 = /^([^'"]*[^\s])\s+(['"])(.*)\2/.exec(href);
          if (link2) {
            href = link2[1];
            title = link2[3];
          }
        } else {
          title = cap[3] ? cap[3].slice(1, -1) : "";
        }
        href = href.trim();
        if (/^</.test(href)) {
          if (this.options.pedantic && !/>$/.test(trimmedUrl)) {
            href = href.slice(1);
          } else {
            href = href.slice(1, -1);
          }
        }
        return outputLink(cap, {
          href: href ? href.replace(this.rules.inline.anyPunctuation, "$1") : href,
          title: title ? title.replace(this.rules.inline.anyPunctuation, "$1") : title
        }, cap[0], this.lexer);
      }
    }
    reflink(src, links) {
      let cap;
      if ((cap = this.rules.inline.reflink.exec(src)) || (cap = this.rules.inline.nolink.exec(src))) {
        const linkString = (cap[2] || cap[1]).replace(/\s+/g, " ");
        const link2 = links[linkString.toLowerCase()];
        if (!link2) {
          const text = cap[0].charAt(0);
          return {
            type: "text",
            raw: text,
            text
          };
        }
        return outputLink(cap, link2, cap[0], this.lexer);
      }
    }
    emStrong(src, maskedSrc, prevChar = "") {
      let match = this.rules.inline.emStrongLDelim.exec(src);
      if (!match)
        return;
      if (match[3] && prevChar.match(/[\p{L}\p{N}]/u))
        return;
      const nextChar = match[1] || match[2] || "";
      if (!nextChar || !prevChar || this.rules.inline.punctuation.exec(prevChar)) {
        const lLength = [...match[0]].length - 1;
        let rDelim, rLength, delimTotal = lLength, midDelimTotal = 0;
        const endReg = match[0][0] === "*" ? this.rules.inline.emStrongRDelimAst : this.rules.inline.emStrongRDelimUnd;
        endReg.lastIndex = 0;
        maskedSrc = maskedSrc.slice(-1 * src.length + lLength);
        while ((match = endReg.exec(maskedSrc)) != null) {
          rDelim = match[1] || match[2] || match[3] || match[4] || match[5] || match[6];
          if (!rDelim)
            continue;
          rLength = [...rDelim].length;
          if (match[3] || match[4]) {
            delimTotal += rLength;
            continue;
          } else if (match[5] || match[6]) {
            if (lLength % 3 && !((lLength + rLength) % 3)) {
              midDelimTotal += rLength;
              continue;
            }
          }
          delimTotal -= rLength;
          if (delimTotal > 0)
            continue;
          rLength = Math.min(rLength, rLength + delimTotal + midDelimTotal);
          const lastCharLength = [...match[0]][0].length;
          const raw = src.slice(0, lLength + match.index + lastCharLength + rLength);
          if (Math.min(lLength, rLength) % 2) {
            const text2 = raw.slice(1, -1);
            return {
              type: "em",
              raw,
              text: text2,
              tokens: this.lexer.inlineTokens(text2)
            };
          }
          const text = raw.slice(2, -2);
          return {
            type: "strong",
            raw,
            text,
            tokens: this.lexer.inlineTokens(text)
          };
        }
      }
    }
    codespan(src) {
      const cap = this.rules.inline.code.exec(src);
      if (cap) {
        let text = cap[2].replace(/\n/g, " ");
        const hasNonSpaceChars = /[^ ]/.test(text);
        const hasSpaceCharsOnBothEnds = /^ /.test(text) && / $/.test(text);
        if (hasNonSpaceChars && hasSpaceCharsOnBothEnds) {
          text = text.substring(1, text.length - 1);
        }
        text = escape$1(text, true);
        return {
          type: "codespan",
          raw: cap[0],
          text
        };
      }
    }
    br(src) {
      const cap = this.rules.inline.br.exec(src);
      if (cap) {
        return {
          type: "br",
          raw: cap[0]
        };
      }
    }
    del(src) {
      const cap = this.rules.inline.del.exec(src);
      if (cap) {
        return {
          type: "del",
          raw: cap[0],
          text: cap[2],
          tokens: this.lexer.inlineTokens(cap[2])
        };
      }
    }
    autolink(src) {
      const cap = this.rules.inline.autolink.exec(src);
      if (cap) {
        let text, href;
        if (cap[2] === "@") {
          text = escape$1(cap[1]);
          href = "mailto:" + text;
        } else {
          text = escape$1(cap[1]);
          href = text;
        }
        return {
          type: "link",
          raw: cap[0],
          text,
          href,
          tokens: [
            {
              type: "text",
              raw: text,
              text
            }
          ]
        };
      }
    }
    url(src) {
      let cap;
      if (cap = this.rules.inline.url.exec(src)) {
        let text, href;
        if (cap[2] === "@") {
          text = escape$1(cap[0]);
          href = "mailto:" + text;
        } else {
          let prevCapZero;
          do {
            prevCapZero = cap[0];
            cap[0] = this.rules.inline._backpedal.exec(cap[0])?.[0] ?? "";
          } while (prevCapZero !== cap[0]);
          text = escape$1(cap[0]);
          if (cap[1] === "www.") {
            href = "http://" + cap[0];
          } else {
            href = cap[0];
          }
        }
        return {
          type: "link",
          raw: cap[0],
          text,
          href,
          tokens: [
            {
              type: "text",
              raw: text,
              text
            }
          ]
        };
      }
    }
    inlineText(src) {
      const cap = this.rules.inline.text.exec(src);
      if (cap) {
        let text;
        if (this.lexer.state.inRawBlock) {
          text = cap[0];
        } else {
          text = escape$1(cap[0]);
        }
        return {
          type: "text",
          raw: cap[0],
          text
        };
      }
    }
  };
  var newline = /^(?: *(?:\n|$))+/;
  var blockCode = /^( {4}[^\n]+(?:\n(?: *(?:\n|$))*)?)+/;
  var fences = /^ {0,3}(`{3,}(?=[^`\n]*(?:\n|$))|~{3,})([^\n]*)(?:\n|$)(?:|([\s\S]*?)(?:\n|$))(?: {0,3}\1[~`]* *(?=\n|$)|$)/;
  var hr = /^ {0,3}((?:-[\t ]*){3,}|(?:_[ \t]*){3,}|(?:\*[ \t]*){3,})(?:\n+|$)/;
  var heading = /^ {0,3}(#{1,6})(?=\s|$)(.*)(?:\n+|$)/;
  var bullet = /(?:[*+-]|\d{1,9}[.)])/;
  var lheading = edit(/^(?!bull )((?:.|\n(?!\s*?\n|bull ))+?)\n {0,3}(=+|-+) *(?:\n+|$)/).replace(/bull/g, bullet).getRegex();
  var _paragraph = /^([^\n]+(?:\n(?!hr|heading|lheading|blockquote|fences|list|html|table| +\n)[^\n]+)*)/;
  var blockText = /^[^\n]+/;
  var _blockLabel = /(?!\s*\])(?:\\.|[^\[\]\\])+/;
  var def = edit(/^ {0,3}\[(label)\]: *(?:\n *)?([^<\s][^\s]*|<.*?>)(?:(?: +(?:\n *)?| *\n *)(title))? *(?:\n+|$)/).replace("label", _blockLabel).replace("title", /(?:"(?:\\"?|[^"\\])*"|'[^'\n]*(?:\n[^'\n]+)*\n?'|\([^()]*\))/).getRegex();
  var list = edit(/^( {0,3}bull)([ \t][^\n]+?)?(?:\n|$)/).replace(/bull/g, bullet).getRegex();
  var _tag = "address|article|aside|base|basefont|blockquote|body|caption|center|col|colgroup|dd|details|dialog|dir|div|dl|dt|fieldset|figcaption|figure|footer|form|frame|frameset|h[1-6]|head|header|hr|html|iframe|legend|li|link|main|menu|menuitem|meta|nav|noframes|ol|optgroup|option|p|param|section|source|summary|table|tbody|td|tfoot|th|thead|title|tr|track|ul";
  var _comment = /\x3C!--(?!-?>)[\s\S]*?(?:-->|$)/;
  var html = edit("^ {0,3}(?:<(script|pre|style|textarea)[\\s>][\\s\\S]*?(?:</\\1>[^\\n]*\\n+|$)|comment[^\\n]*(\\n+|$)|<\\?[\\s\\S]*?(?:\\?>\\n*|$)|<![A-Z][\\s\\S]*?(?:>\\n*|$)|<!\\[CDATA\\[[\\s\\S]*?(?:\\]\\]>\\n*|$)|</?(tag)(?: +|\\n|/?>)[\\s\\S]*?(?:(?:\\n *)+\\n|$)|<(?!script|pre|style|textarea)([a-z][\\w-]*)(?:attribute)*? */?>(?=[ \\t]*(?:\\n|$))[\\s\\S]*?(?:(?:\\n *)+\\n|$)|</(?!script|pre|style|textarea)[a-z][\\w-]*\\s*>(?=[ \\t]*(?:\\n|$))[\\s\\S]*?(?:(?:\\n *)+\\n|$))", "i").replace("comment", _comment).replace("tag", _tag).replace("attribute", / +[a-zA-Z:_][\w.:-]*(?: *= *"[^"\n]*"| *= *'[^'\n]*'| *= *[^\s"'=<>`]+)?/).getRegex();
  var paragraph = edit(_paragraph).replace("hr", hr).replace("heading", " {0,3}#{1,6}(?:\\s|$)").replace("|lheading", "").replace("|table", "").replace("blockquote", " {0,3}>").replace("fences", " {0,3}(?:`{3,}(?=[^`\\n]*\\n)|~{3,})[^\\n]*\\n").replace("list", " {0,3}(?:[*+-]|1[.)]) ").replace("html", "</?(?:tag)(?: +|\\n|/?>)|<(?:script|pre|style|textarea|!--)").replace("tag", _tag).getRegex();
  var blockquote = edit(/^( {0,3}> ?(paragraph|[^\n]*)(?:\n|$))+/).replace("paragraph", paragraph).getRegex();
  var blockNormal = {
    blockquote,
    code: blockCode,
    def,
    fences,
    heading,
    hr,
    html,
    lheading,
    list,
    newline,
    paragraph,
    table: noopTest,
    text: blockText
  };
  var gfmTable = edit("^ *([^\\n ].*)\\n {0,3}((?:\\| *)?:?-+:? *(?:\\| *:?-+:? *)*(?:\\| *)?)(?:\\n((?:(?! *\\n|hr|heading|blockquote|code|fences|list|html).*(?:\\n|$))*)\\n*|$)").replace("hr", hr).replace("heading", " {0,3}#{1,6}(?:\\s|$)").replace("blockquote", " {0,3}>").replace("code", " {4}[^\\n]").replace("fences", " {0,3}(?:`{3,}(?=[^`\\n]*\\n)|~{3,})[^\\n]*\\n").replace("list", " {0,3}(?:[*+-]|1[.)]) ").replace("html", "</?(?:tag)(?: +|\\n|/?>)|<(?:script|pre|style|textarea|!--)").replace("tag", _tag).getRegex();
  var blockGfm = {
    ...blockNormal,
    table: gfmTable,
    paragraph: edit(_paragraph).replace("hr", hr).replace("heading", " {0,3}#{1,6}(?:\\s|$)").replace("|lheading", "").replace("table", gfmTable).replace("blockquote", " {0,3}>").replace("fences", " {0,3}(?:`{3,}(?=[^`\\n]*\\n)|~{3,})[^\\n]*\\n").replace("list", " {0,3}(?:[*+-]|1[.)]) ").replace("html", "</?(?:tag)(?: +|\\n|/?>)|<(?:script|pre|style|textarea|!--)").replace("tag", _tag).getRegex()
  };
  var blockPedantic = {
    ...blockNormal,
    html: edit(`^ *(?:comment *(?:\\n|\\s*$)|<(tag)[\\s\\S]+?</\\1> *(?:\\n{2,}|\\s*$)|<tag(?:"[^"]*"|'[^']*'|\\s[^'"/>\\s]*)*?/?> *(?:\\n{2,}|\\s*$))`).replace("comment", _comment).replace(/tag/g, "(?!(?:a|em|strong|small|s|cite|q|dfn|abbr|data|time|code|var|samp|kbd|sub|sup|i|b|u|mark|ruby|rt|rp|bdi|bdo|span|br|wbr|ins|del|img)\\b)\\w+(?!:|[^\\w\\s@]*@)\\b").getRegex(),
    def: /^ *\[([^\]]+)\]: *<?([^\s>]+)>?(?: +(["(][^\n]+[")]))? *(?:\n+|$)/,
    heading: /^(#{1,6})(.*)(?:\n+|$)/,
    fences: noopTest,
    // fences not supported
    lheading: /^(.+?)\n {0,3}(=+|-+) *(?:\n+|$)/,
    paragraph: edit(_paragraph).replace("hr", hr).replace("heading", " *#{1,6} *[^\n]").replace("lheading", lheading).replace("|table", "").replace("blockquote", " {0,3}>").replace("|fences", "").replace("|list", "").replace("|html", "").replace("|tag", "").getRegex()
  };
  var escape = /^\\([!"#$%&'()*+,\-./:;<=>?@\[\]\\^_`{|}~])/;
  var inlineCode = /^(`+)([^`]|[^`][\s\S]*?[^`])\1(?!`)/;
  var br = /^( {2,}|\\)\n(?!\s*$)/;
  var inlineText = /^(`+|[^`])(?:(?= {2,}\n)|[\s\S]*?(?:(?=[\\<!\[`*_]|\b_|$)|[^ ](?= {2,}\n)))/;
  var _punctuation = "\\p{P}$+<=>`^|~";
  var punctuation = edit(/^((?![*_])[\spunctuation])/, "u").replace(/punctuation/g, _punctuation).getRegex();
  var blockSkip = /\[[^[\]]*?\]\([^\(\)]*?\)|`[^`]*?`|<[^<>]*?>/g;
  var emStrongLDelim = edit(/^(?:\*+(?:((?!\*)[punct])|[^\s*]))|^_+(?:((?!_)[punct])|([^\s_]))/, "u").replace(/punct/g, _punctuation).getRegex();
  var emStrongRDelimAst = edit("^[^_*]*?__[^_*]*?\\*[^_*]*?(?=__)|[^*]+(?=[^*])|(?!\\*)[punct](\\*+)(?=[\\s]|$)|[^punct\\s](\\*+)(?!\\*)(?=[punct\\s]|$)|(?!\\*)[punct\\s](\\*+)(?=[^punct\\s])|[\\s](\\*+)(?!\\*)(?=[punct])|(?!\\*)[punct](\\*+)(?!\\*)(?=[punct])|[^punct\\s](\\*+)(?=[^punct\\s])", "gu").replace(/punct/g, _punctuation).getRegex();
  var emStrongRDelimUnd = edit("^[^_*]*?\\*\\*[^_*]*?_[^_*]*?(?=\\*\\*)|[^_]+(?=[^_])|(?!_)[punct](_+)(?=[\\s]|$)|[^punct\\s](_+)(?!_)(?=[punct\\s]|$)|(?!_)[punct\\s](_+)(?=[^punct\\s])|[\\s](_+)(?!_)(?=[punct])|(?!_)[punct](_+)(?!_)(?=[punct])", "gu").replace(/punct/g, _punctuation).getRegex();
  var anyPunctuation = edit(/\\([punct])/, "gu").replace(/punct/g, _punctuation).getRegex();
  var autolink = edit(/^<(scheme:[^\s\x00-\x1f<>]*|email)>/).replace("scheme", /[a-zA-Z][a-zA-Z0-9+.-]{1,31}/).replace("email", /[a-zA-Z0-9.!#$%&'*+/=?^_`{|}~-]+(@)[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?(?:\.[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?)+(?![-_])/).getRegex();
  var _inlineComment = edit(_comment).replace("(?:-->|$)", "-->").getRegex();
  var tag = edit("^comment|^</[a-zA-Z][\\w:-]*\\s*>|^<[a-zA-Z][\\w-]*(?:attribute)*?\\s*/?>|^<\\?[\\s\\S]*?\\?>|^<![a-zA-Z]+\\s[\\s\\S]*?>|^<!\\[CDATA\\[[\\s\\S]*?\\]\\]>").replace("comment", _inlineComment).replace("attribute", /\s+[a-zA-Z:_][\w.:-]*(?:\s*=\s*"[^"]*"|\s*=\s*'[^']*'|\s*=\s*[^\s"'=<>`]+)?/).getRegex();
  var _inlineLabel = /(?:\[(?:\\.|[^\[\]\\])*\]|\\.|`[^`]*`|[^\[\]\\`])*?/;
  var link = edit(/^!?\[(label)\]\(\s*(href)(?:\s+(title))?\s*\)/).replace("label", _inlineLabel).replace("href", /<(?:\\.|[^\n<>\\])+>|[^\s\x00-\x1f]*/).replace("title", /"(?:\\"?|[^"\\])*"|'(?:\\'?|[^'\\])*'|\((?:\\\)?|[^)\\])*\)/).getRegex();
  var reflink = edit(/^!?\[(label)\]\[(ref)\]/).replace("label", _inlineLabel).replace("ref", _blockLabel).getRegex();
  var nolink = edit(/^!?\[(ref)\](?:\[\])?/).replace("ref", _blockLabel).getRegex();
  var reflinkSearch = edit("reflink|nolink(?!\\()", "g").replace("reflink", reflink).replace("nolink", nolink).getRegex();
  var inlineNormal = {
    _backpedal: noopTest,
    // only used for GFM url
    anyPunctuation,
    autolink,
    blockSkip,
    br,
    code: inlineCode,
    del: noopTest,
    emStrongLDelim,
    emStrongRDelimAst,
    emStrongRDelimUnd,
    escape,
    link,
    nolink,
    punctuation,
    reflink,
    reflinkSearch,
    tag,
    text: inlineText,
    url: noopTest
  };
  var inlinePedantic = {
    ...inlineNormal,
    link: edit(/^!?\[(label)\]\((.*?)\)/).replace("label", _inlineLabel).getRegex(),
    reflink: edit(/^!?\[(label)\]\s*\[([^\]]*)\]/).replace("label", _inlineLabel).getRegex()
  };
  var inlineGfm = {
    ...inlineNormal,
    escape: edit(escape).replace("])", "~|])").getRegex(),
    url: edit(/^((?:ftp|https?):\/\/|www\.)(?:[a-zA-Z0-9\-]+\.?)+[^\s<]*|^email/, "i").replace("email", /[A-Za-z0-9._+-]+(@)[a-zA-Z0-9-_]+(?:\.[a-zA-Z0-9-_]*[a-zA-Z0-9])+(?![-_])/).getRegex(),
    _backpedal: /(?:[^?!.,:;*_'"~()&]+|\([^)]*\)|&(?![a-zA-Z0-9]+;$)|[?!.,:;*_'"~)]+(?!$))+/,
    del: /^(~~?)(?=[^\s~])([\s\S]*?[^\s~])\1(?=[^~]|$)/,
    text: /^([`~]+|[^`~])(?:(?= {2,}\n)|(?=[a-zA-Z0-9.!#$%&'*+\/=?_`{\|}~-]+@)|[\s\S]*?(?:(?=[\\<!\[`*~_]|\b_|https?:\/\/|ftp:\/\/|www\.|$)|[^ ](?= {2,}\n)|[^a-zA-Z0-9.!#$%&'*+\/=?_`{\|}~-](?=[a-zA-Z0-9.!#$%&'*+\/=?_`{\|}~-]+@)))/
  };
  var inlineBreaks = {
    ...inlineGfm,
    br: edit(br).replace("{2,}", "*").getRegex(),
    text: edit(inlineGfm.text).replace("\\b_", "\\b_| {2,}\\n").replace(/\{2,\}/g, "*").getRegex()
  };
  var block = {
    normal: blockNormal,
    gfm: blockGfm,
    pedantic: blockPedantic
  };
  var inline2 = {
    normal: inlineNormal,
    gfm: inlineGfm,
    breaks: inlineBreaks,
    pedantic: inlinePedantic
  };
  var _Lexer = class __Lexer {
    tokens;
    options;
    state;
    tokenizer;
    inlineQueue;
    constructor(options2) {
      this.tokens = [];
      this.tokens.links = /* @__PURE__ */ Object.create(null);
      this.options = options2 || _defaults;
      this.options.tokenizer = this.options.tokenizer || new _Tokenizer();
      this.tokenizer = this.options.tokenizer;
      this.tokenizer.options = this.options;
      this.tokenizer.lexer = this;
      this.inlineQueue = [];
      this.state = {
        inLink: false,
        inRawBlock: false,
        top: true
      };
      const rules = {
        block: block.normal,
        inline: inline2.normal
      };
      if (this.options.pedantic) {
        rules.block = block.pedantic;
        rules.inline = inline2.pedantic;
      } else if (this.options.gfm) {
        rules.block = block.gfm;
        if (this.options.breaks) {
          rules.inline = inline2.breaks;
        } else {
          rules.inline = inline2.gfm;
        }
      }
      this.tokenizer.rules = rules;
    }
    /**
     * Expose Rules
     */
    static get rules() {
      return {
        block,
        inline: inline2
      };
    }
    /**
     * Static Lex Method
     */
    static lex(src, options2) {
      const lexer2 = new __Lexer(options2);
      return lexer2.lex(src);
    }
    /**
     * Static Lex Inline Method
     */
    static lexInline(src, options2) {
      const lexer2 = new __Lexer(options2);
      return lexer2.inlineTokens(src);
    }
    /**
     * Preprocessing
     */
    lex(src) {
      src = src.replace(/\r\n|\r/g, "\n");
      this.blockTokens(src, this.tokens);
      for (let i10 = 0; i10 < this.inlineQueue.length; i10++) {
        const next = this.inlineQueue[i10];
        this.inlineTokens(next.src, next.tokens);
      }
      this.inlineQueue = [];
      return this.tokens;
    }
    blockTokens(src, tokens = []) {
      if (this.options.pedantic) {
        src = src.replace(/\t/g, "    ").replace(/^ +$/gm, "");
      } else {
        src = src.replace(/^( *)(\t+)/gm, (_2, leading, tabs) => {
          return leading + "    ".repeat(tabs.length);
        });
      }
      let token;
      let lastToken;
      let cutSrc;
      let lastParagraphClipped;
      while (src) {
        if (this.options.extensions && this.options.extensions.block && this.options.extensions.block.some((extTokenizer) => {
          if (token = extTokenizer.call({ lexer: this }, src, tokens)) {
            src = src.substring(token.raw.length);
            tokens.push(token);
            return true;
          }
          return false;
        })) {
          continue;
        }
        if (token = this.tokenizer.space(src)) {
          src = src.substring(token.raw.length);
          if (token.raw.length === 1 && tokens.length > 0) {
            tokens[tokens.length - 1].raw += "\n";
          } else {
            tokens.push(token);
          }
          continue;
        }
        if (token = this.tokenizer.code(src)) {
          src = src.substring(token.raw.length);
          lastToken = tokens[tokens.length - 1];
          if (lastToken && (lastToken.type === "paragraph" || lastToken.type === "text")) {
            lastToken.raw += "\n" + token.raw;
            lastToken.text += "\n" + token.text;
            this.inlineQueue[this.inlineQueue.length - 1].src = lastToken.text;
          } else {
            tokens.push(token);
          }
          continue;
        }
        if (token = this.tokenizer.fences(src)) {
          src = src.substring(token.raw.length);
          tokens.push(token);
          continue;
        }
        if (token = this.tokenizer.heading(src)) {
          src = src.substring(token.raw.length);
          tokens.push(token);
          continue;
        }
        if (token = this.tokenizer.hr(src)) {
          src = src.substring(token.raw.length);
          tokens.push(token);
          continue;
        }
        if (token = this.tokenizer.blockquote(src)) {
          src = src.substring(token.raw.length);
          tokens.push(token);
          continue;
        }
        if (token = this.tokenizer.list(src)) {
          src = src.substring(token.raw.length);
          tokens.push(token);
          continue;
        }
        if (token = this.tokenizer.html(src)) {
          src = src.substring(token.raw.length);
          tokens.push(token);
          continue;
        }
        if (token = this.tokenizer.def(src)) {
          src = src.substring(token.raw.length);
          lastToken = tokens[tokens.length - 1];
          if (lastToken && (lastToken.type === "paragraph" || lastToken.type === "text")) {
            lastToken.raw += "\n" + token.raw;
            lastToken.text += "\n" + token.raw;
            this.inlineQueue[this.inlineQueue.length - 1].src = lastToken.text;
          } else if (!this.tokens.links[token.tag]) {
            this.tokens.links[token.tag] = {
              href: token.href,
              title: token.title
            };
          }
          continue;
        }
        if (token = this.tokenizer.table(src)) {
          src = src.substring(token.raw.length);
          tokens.push(token);
          continue;
        }
        if (token = this.tokenizer.lheading(src)) {
          src = src.substring(token.raw.length);
          tokens.push(token);
          continue;
        }
        cutSrc = src;
        if (this.options.extensions && this.options.extensions.startBlock) {
          let startIndex = Infinity;
          const tempSrc = src.slice(1);
          let tempStart;
          this.options.extensions.startBlock.forEach((getStartIndex) => {
            tempStart = getStartIndex.call({ lexer: this }, tempSrc);
            if (typeof tempStart === "number" && tempStart >= 0) {
              startIndex = Math.min(startIndex, tempStart);
            }
          });
          if (startIndex < Infinity && startIndex >= 0) {
            cutSrc = src.substring(0, startIndex + 1);
          }
        }
        if (this.state.top && (token = this.tokenizer.paragraph(cutSrc))) {
          lastToken = tokens[tokens.length - 1];
          if (lastParagraphClipped && lastToken.type === "paragraph") {
            lastToken.raw += "\n" + token.raw;
            lastToken.text += "\n" + token.text;
            this.inlineQueue.pop();
            this.inlineQueue[this.inlineQueue.length - 1].src = lastToken.text;
          } else {
            tokens.push(token);
          }
          lastParagraphClipped = cutSrc.length !== src.length;
          src = src.substring(token.raw.length);
          continue;
        }
        if (token = this.tokenizer.text(src)) {
          src = src.substring(token.raw.length);
          lastToken = tokens[tokens.length - 1];
          if (lastToken && lastToken.type === "text") {
            lastToken.raw += "\n" + token.raw;
            lastToken.text += "\n" + token.text;
            this.inlineQueue.pop();
            this.inlineQueue[this.inlineQueue.length - 1].src = lastToken.text;
          } else {
            tokens.push(token);
          }
          continue;
        }
        if (src) {
          const errMsg = "Infinite loop on byte: " + src.charCodeAt(0);
          if (this.options.silent) {
            console.error(errMsg);
            break;
          } else {
            throw new Error(errMsg);
          }
        }
      }
      this.state.top = true;
      return tokens;
    }
    inline(src, tokens = []) {
      this.inlineQueue.push({ src, tokens });
      return tokens;
    }
    /**
     * Lexing/Compiling
     */
    inlineTokens(src, tokens = []) {
      let token, lastToken, cutSrc;
      let maskedSrc = src;
      let match;
      let keepPrevChar, prevChar;
      if (this.tokens.links) {
        const links = Object.keys(this.tokens.links);
        if (links.length > 0) {
          while ((match = this.tokenizer.rules.inline.reflinkSearch.exec(maskedSrc)) != null) {
            if (links.includes(match[0].slice(match[0].lastIndexOf("[") + 1, -1))) {
              maskedSrc = maskedSrc.slice(0, match.index) + "[" + "a".repeat(match[0].length - 2) + "]" + maskedSrc.slice(this.tokenizer.rules.inline.reflinkSearch.lastIndex);
            }
          }
        }
      }
      while ((match = this.tokenizer.rules.inline.blockSkip.exec(maskedSrc)) != null) {
        maskedSrc = maskedSrc.slice(0, match.index) + "[" + "a".repeat(match[0].length - 2) + "]" + maskedSrc.slice(this.tokenizer.rules.inline.blockSkip.lastIndex);
      }
      while ((match = this.tokenizer.rules.inline.anyPunctuation.exec(maskedSrc)) != null) {
        maskedSrc = maskedSrc.slice(0, match.index) + "++" + maskedSrc.slice(this.tokenizer.rules.inline.anyPunctuation.lastIndex);
      }
      while (src) {
        if (!keepPrevChar) {
          prevChar = "";
        }
        keepPrevChar = false;
        if (this.options.extensions && this.options.extensions.inline && this.options.extensions.inline.some((extTokenizer) => {
          if (token = extTokenizer.call({ lexer: this }, src, tokens)) {
            src = src.substring(token.raw.length);
            tokens.push(token);
            return true;
          }
          return false;
        })) {
          continue;
        }
        if (token = this.tokenizer.escape(src)) {
          src = src.substring(token.raw.length);
          tokens.push(token);
          continue;
        }
        if (token = this.tokenizer.tag(src)) {
          src = src.substring(token.raw.length);
          lastToken = tokens[tokens.length - 1];
          if (lastToken && token.type === "text" && lastToken.type === "text") {
            lastToken.raw += token.raw;
            lastToken.text += token.text;
          } else {
            tokens.push(token);
          }
          continue;
        }
        if (token = this.tokenizer.link(src)) {
          src = src.substring(token.raw.length);
          tokens.push(token);
          continue;
        }
        if (token = this.tokenizer.reflink(src, this.tokens.links)) {
          src = src.substring(token.raw.length);
          lastToken = tokens[tokens.length - 1];
          if (lastToken && token.type === "text" && lastToken.type === "text") {
            lastToken.raw += token.raw;
            lastToken.text += token.text;
          } else {
            tokens.push(token);
          }
          continue;
        }
        if (token = this.tokenizer.emStrong(src, maskedSrc, prevChar)) {
          src = src.substring(token.raw.length);
          tokens.push(token);
          continue;
        }
        if (token = this.tokenizer.codespan(src)) {
          src = src.substring(token.raw.length);
          tokens.push(token);
          continue;
        }
        if (token = this.tokenizer.br(src)) {
          src = src.substring(token.raw.length);
          tokens.push(token);
          continue;
        }
        if (token = this.tokenizer.del(src)) {
          src = src.substring(token.raw.length);
          tokens.push(token);
          continue;
        }
        if (token = this.tokenizer.autolink(src)) {
          src = src.substring(token.raw.length);
          tokens.push(token);
          continue;
        }
        if (!this.state.inLink && (token = this.tokenizer.url(src))) {
          src = src.substring(token.raw.length);
          tokens.push(token);
          continue;
        }
        cutSrc = src;
        if (this.options.extensions && this.options.extensions.startInline) {
          let startIndex = Infinity;
          const tempSrc = src.slice(1);
          let tempStart;
          this.options.extensions.startInline.forEach((getStartIndex) => {
            tempStart = getStartIndex.call({ lexer: this }, tempSrc);
            if (typeof tempStart === "number" && tempStart >= 0) {
              startIndex = Math.min(startIndex, tempStart);
            }
          });
          if (startIndex < Infinity && startIndex >= 0) {
            cutSrc = src.substring(0, startIndex + 1);
          }
        }
        if (token = this.tokenizer.inlineText(cutSrc)) {
          src = src.substring(token.raw.length);
          if (token.raw.slice(-1) !== "_") {
            prevChar = token.raw.slice(-1);
          }
          keepPrevChar = true;
          lastToken = tokens[tokens.length - 1];
          if (lastToken && lastToken.type === "text") {
            lastToken.raw += token.raw;
            lastToken.text += token.text;
          } else {
            tokens.push(token);
          }
          continue;
        }
        if (src) {
          const errMsg = "Infinite loop on byte: " + src.charCodeAt(0);
          if (this.options.silent) {
            console.error(errMsg);
            break;
          } else {
            throw new Error(errMsg);
          }
        }
      }
      return tokens;
    }
  };
  var _Renderer = class {
    options;
    constructor(options2) {
      this.options = options2 || _defaults;
    }
    code(code, infostring, escaped) {
      const lang = (infostring || "").match(/^\S*/)?.[0];
      code = code.replace(/\n$/, "") + "\n";
      if (!lang) {
        return "<pre><code>" + (escaped ? code : escape$1(code, true)) + "</code></pre>\n";
      }
      return '<pre><code class="language-' + escape$1(lang) + '">' + (escaped ? code : escape$1(code, true)) + "</code></pre>\n";
    }
    blockquote(quote) {
      return `<blockquote>
${quote}</blockquote>
`;
    }
    html(html2, block2) {
      return html2;
    }
    heading(text, level, raw) {
      return `<h${level}>${text}</h${level}>
`;
    }
    hr() {
      return "<hr>\n";
    }
    list(body, ordered, start) {
      const type = ordered ? "ol" : "ul";
      const startatt = ordered && start !== 1 ? ' start="' + start + '"' : "";
      return "<" + type + startatt + ">\n" + body + "</" + type + ">\n";
    }
    listitem(text, task, checked) {
      return `<li>${text}</li>
`;
    }
    checkbox(checked) {
      return "<input " + (checked ? 'checked="" ' : "") + 'disabled="" type="checkbox">';
    }
    paragraph(text) {
      return `<p>${text}</p>
`;
    }
    table(header, body) {
      if (body)
        body = `<tbody>${body}</tbody>`;
      return "<table>\n<thead>\n" + header + "</thead>\n" + body + "</table>\n";
    }
    tablerow(content) {
      return `<tr>
${content}</tr>
`;
    }
    tablecell(content, flags) {
      const type = flags.header ? "th" : "td";
      const tag2 = flags.align ? `<${type} align="${flags.align}">` : `<${type}>`;
      return tag2 + content + `</${type}>
`;
    }
    /**
     * span level renderer
     */
    strong(text) {
      return `<strong>${text}</strong>`;
    }
    em(text) {
      return `<em>${text}</em>`;
    }
    codespan(text) {
      return `<code>${text}</code>`;
    }
    br() {
      return "<br>";
    }
    del(text) {
      return `<del>${text}</del>`;
    }
    link(href, title, text) {
      const cleanHref = cleanUrl(href);
      if (cleanHref === null) {
        return text;
      }
      href = cleanHref;
      let out = '<a href="' + href + '"';
      if (title) {
        out += ' title="' + title + '"';
      }
      out += ">" + text + "</a>";
      return out;
    }
    image(href, title, text) {
      const cleanHref = cleanUrl(href);
      if (cleanHref === null) {
        return text;
      }
      href = cleanHref;
      let out = `<img src="${href}" alt="${text}"`;
      if (title) {
        out += ` title="${title}"`;
      }
      out += ">";
      return out;
    }
    text(text) {
      return text;
    }
  };
  var _TextRenderer = class {
    // no need for block level renderers
    strong(text) {
      return text;
    }
    em(text) {
      return text;
    }
    codespan(text) {
      return text;
    }
    del(text) {
      return text;
    }
    html(text) {
      return text;
    }
    text(text) {
      return text;
    }
    link(href, title, text) {
      return "" + text;
    }
    image(href, title, text) {
      return "" + text;
    }
    br() {
      return "";
    }
  };
  var _Parser = class __Parser {
    options;
    renderer;
    textRenderer;
    constructor(options2) {
      this.options = options2 || _defaults;
      this.options.renderer = this.options.renderer || new _Renderer();
      this.renderer = this.options.renderer;
      this.renderer.options = this.options;
      this.textRenderer = new _TextRenderer();
    }
    /**
     * Static Parse Method
     */
    static parse(tokens, options2) {
      const parser3 = new __Parser(options2);
      return parser3.parse(tokens);
    }
    /**
     * Static Parse Inline Method
     */
    static parseInline(tokens, options2) {
      const parser3 = new __Parser(options2);
      return parser3.parseInline(tokens);
    }
    /**
     * Parse Loop
     */
    parse(tokens, top = true) {
      let out = "";
      for (let i10 = 0; i10 < tokens.length; i10++) {
        const token = tokens[i10];
        if (this.options.extensions && this.options.extensions.renderers && this.options.extensions.renderers[token.type]) {
          const genericToken = token;
          const ret = this.options.extensions.renderers[genericToken.type].call({ parser: this }, genericToken);
          if (ret !== false || !["space", "hr", "heading", "code", "table", "blockquote", "list", "html", "paragraph", "text"].includes(genericToken.type)) {
            out += ret || "";
            continue;
          }
        }
        switch (token.type) {
          case "space": {
            continue;
          }
          case "hr": {
            out += this.renderer.hr();
            continue;
          }
          case "heading": {
            const headingToken = token;
            out += this.renderer.heading(this.parseInline(headingToken.tokens), headingToken.depth, unescape(this.parseInline(headingToken.tokens, this.textRenderer)));
            continue;
          }
          case "code": {
            const codeToken = token;
            out += this.renderer.code(codeToken.text, codeToken.lang, !!codeToken.escaped);
            continue;
          }
          case "table": {
            const tableToken = token;
            let header = "";
            let cell = "";
            for (let j2 = 0; j2 < tableToken.header.length; j2++) {
              cell += this.renderer.tablecell(this.parseInline(tableToken.header[j2].tokens), { header: true, align: tableToken.align[j2] });
            }
            header += this.renderer.tablerow(cell);
            let body = "";
            for (let j2 = 0; j2 < tableToken.rows.length; j2++) {
              const row = tableToken.rows[j2];
              cell = "";
              for (let k2 = 0; k2 < row.length; k2++) {
                cell += this.renderer.tablecell(this.parseInline(row[k2].tokens), { header: false, align: tableToken.align[k2] });
              }
              body += this.renderer.tablerow(cell);
            }
            out += this.renderer.table(header, body);
            continue;
          }
          case "blockquote": {
            const blockquoteToken = token;
            const body = this.parse(blockquoteToken.tokens);
            out += this.renderer.blockquote(body);
            continue;
          }
          case "list": {
            const listToken = token;
            const ordered = listToken.ordered;
            const start = listToken.start;
            const loose = listToken.loose;
            let body = "";
            for (let j2 = 0; j2 < listToken.items.length; j2++) {
              const item = listToken.items[j2];
              const checked = item.checked;
              const task = item.task;
              let itemBody = "";
              if (item.task) {
                const checkbox = this.renderer.checkbox(!!checked);
                if (loose) {
                  if (item.tokens.length > 0 && item.tokens[0].type === "paragraph") {
                    item.tokens[0].text = checkbox + " " + item.tokens[0].text;
                    if (item.tokens[0].tokens && item.tokens[0].tokens.length > 0 && item.tokens[0].tokens[0].type === "text") {
                      item.tokens[0].tokens[0].text = checkbox + " " + item.tokens[0].tokens[0].text;
                    }
                  } else {
                    item.tokens.unshift({
                      type: "text",
                      text: checkbox + " "
                    });
                  }
                } else {
                  itemBody += checkbox + " ";
                }
              }
              itemBody += this.parse(item.tokens, loose);
              body += this.renderer.listitem(itemBody, task, !!checked);
            }
            out += this.renderer.list(body, ordered, start);
            continue;
          }
          case "html": {
            const htmlToken = token;
            out += this.renderer.html(htmlToken.text, htmlToken.block);
            continue;
          }
          case "paragraph": {
            const paragraphToken = token;
            out += this.renderer.paragraph(this.parseInline(paragraphToken.tokens));
            continue;
          }
          case "text": {
            let textToken = token;
            let body = textToken.tokens ? this.parseInline(textToken.tokens) : textToken.text;
            while (i10 + 1 < tokens.length && tokens[i10 + 1].type === "text") {
              textToken = tokens[++i10];
              body += "\n" + (textToken.tokens ? this.parseInline(textToken.tokens) : textToken.text);
            }
            out += top ? this.renderer.paragraph(body) : body;
            continue;
          }
          default: {
            const errMsg = 'Token with "' + token.type + '" type was not found.';
            if (this.options.silent) {
              console.error(errMsg);
              return "";
            } else {
              throw new Error(errMsg);
            }
          }
        }
      }
      return out;
    }
    /**
     * Parse Inline Tokens
     */
    parseInline(tokens, renderer) {
      renderer = renderer || this.renderer;
      let out = "";
      for (let i10 = 0; i10 < tokens.length; i10++) {
        const token = tokens[i10];
        if (this.options.extensions && this.options.extensions.renderers && this.options.extensions.renderers[token.type]) {
          const ret = this.options.extensions.renderers[token.type].call({ parser: this }, token);
          if (ret !== false || !["escape", "html", "link", "image", "strong", "em", "codespan", "br", "del", "text"].includes(token.type)) {
            out += ret || "";
            continue;
          }
        }
        switch (token.type) {
          case "escape": {
            const escapeToken = token;
            out += renderer.text(escapeToken.text);
            break;
          }
          case "html": {
            const tagToken = token;
            out += renderer.html(tagToken.text);
            break;
          }
          case "link": {
            const linkToken = token;
            out += renderer.link(linkToken.href, linkToken.title, this.parseInline(linkToken.tokens, renderer));
            break;
          }
          case "image": {
            const imageToken = token;
            out += renderer.image(imageToken.href, imageToken.title, imageToken.text);
            break;
          }
          case "strong": {
            const strongToken = token;
            out += renderer.strong(this.parseInline(strongToken.tokens, renderer));
            break;
          }
          case "em": {
            const emToken = token;
            out += renderer.em(this.parseInline(emToken.tokens, renderer));
            break;
          }
          case "codespan": {
            const codespanToken = token;
            out += renderer.codespan(codespanToken.text);
            break;
          }
          case "br": {
            out += renderer.br();
            break;
          }
          case "del": {
            const delToken = token;
            out += renderer.del(this.parseInline(delToken.tokens, renderer));
            break;
          }
          case "text": {
            const textToken = token;
            out += renderer.text(textToken.text);
            break;
          }
          default: {
            const errMsg = 'Token with "' + token.type + '" type was not found.';
            if (this.options.silent) {
              console.error(errMsg);
              return "";
            } else {
              throw new Error(errMsg);
            }
          }
        }
      }
      return out;
    }
  };
  var _Hooks = class {
    options;
    constructor(options2) {
      this.options = options2 || _defaults;
    }
    static passThroughHooks = /* @__PURE__ */ new Set([
      "preprocess",
      "postprocess",
      "processAllTokens"
    ]);
    /**
     * Process markdown before marked
     */
    preprocess(markdown) {
      return markdown;
    }
    /**
     * Process HTML after marked is finished
     */
    postprocess(html2) {
      return html2;
    }
    /**
     * Process all tokens before walk tokens
     */
    processAllTokens(tokens) {
      return tokens;
    }
  };
  var Marked = class {
    defaults = _getDefaults();
    options = this.setOptions;
    parse = this.#parseMarkdown(_Lexer.lex, _Parser.parse);
    parseInline = this.#parseMarkdown(_Lexer.lexInline, _Parser.parseInline);
    Parser = _Parser;
    Renderer = _Renderer;
    TextRenderer = _TextRenderer;
    Lexer = _Lexer;
    Tokenizer = _Tokenizer;
    Hooks = _Hooks;
    constructor(...args) {
      this.use(...args);
    }
    /**
     * Run callback for every token
     */
    walkTokens(tokens, callback) {
      let values = [];
      for (const token of tokens) {
        values = values.concat(callback.call(this, token));
        switch (token.type) {
          case "table": {
            const tableToken = token;
            for (const cell of tableToken.header) {
              values = values.concat(this.walkTokens(cell.tokens, callback));
            }
            for (const row of tableToken.rows) {
              for (const cell of row) {
                values = values.concat(this.walkTokens(cell.tokens, callback));
              }
            }
            break;
          }
          case "list": {
            const listToken = token;
            values = values.concat(this.walkTokens(listToken.items, callback));
            break;
          }
          default: {
            const genericToken = token;
            if (this.defaults.extensions?.childTokens?.[genericToken.type]) {
              this.defaults.extensions.childTokens[genericToken.type].forEach((childTokens) => {
                const tokens2 = genericToken[childTokens].flat(Infinity);
                values = values.concat(this.walkTokens(tokens2, callback));
              });
            } else if (genericToken.tokens) {
              values = values.concat(this.walkTokens(genericToken.tokens, callback));
            }
          }
        }
      }
      return values;
    }
    use(...args) {
      const extensions = this.defaults.extensions || { renderers: {}, childTokens: {} };
      args.forEach((pack) => {
        const opts = { ...pack };
        opts.async = this.defaults.async || opts.async || false;
        if (pack.extensions) {
          pack.extensions.forEach((ext) => {
            if (!ext.name) {
              throw new Error("extension name required");
            }
            if ("renderer" in ext) {
              const prevRenderer = extensions.renderers[ext.name];
              if (prevRenderer) {
                extensions.renderers[ext.name] = function(...args2) {
                  let ret = ext.renderer.apply(this, args2);
                  if (ret === false) {
                    ret = prevRenderer.apply(this, args2);
                  }
                  return ret;
                };
              } else {
                extensions.renderers[ext.name] = ext.renderer;
              }
            }
            if ("tokenizer" in ext) {
              if (!ext.level || ext.level !== "block" && ext.level !== "inline") {
                throw new Error("extension level must be 'block' or 'inline'");
              }
              const extLevel = extensions[ext.level];
              if (extLevel) {
                extLevel.unshift(ext.tokenizer);
              } else {
                extensions[ext.level] = [ext.tokenizer];
              }
              if (ext.start) {
                if (ext.level === "block") {
                  if (extensions.startBlock) {
                    extensions.startBlock.push(ext.start);
                  } else {
                    extensions.startBlock = [ext.start];
                  }
                } else if (ext.level === "inline") {
                  if (extensions.startInline) {
                    extensions.startInline.push(ext.start);
                  } else {
                    extensions.startInline = [ext.start];
                  }
                }
              }
            }
            if ("childTokens" in ext && ext.childTokens) {
              extensions.childTokens[ext.name] = ext.childTokens;
            }
          });
          opts.extensions = extensions;
        }
        if (pack.renderer) {
          const renderer = this.defaults.renderer || new _Renderer(this.defaults);
          for (const prop in pack.renderer) {
            if (!(prop in renderer)) {
              throw new Error(`renderer '${prop}' does not exist`);
            }
            if (prop === "options") {
              continue;
            }
            const rendererProp = prop;
            const rendererFunc = pack.renderer[rendererProp];
            const prevRenderer = renderer[rendererProp];
            renderer[rendererProp] = (...args2) => {
              let ret = rendererFunc.apply(renderer, args2);
              if (ret === false) {
                ret = prevRenderer.apply(renderer, args2);
              }
              return ret || "";
            };
          }
          opts.renderer = renderer;
        }
        if (pack.tokenizer) {
          const tokenizer = this.defaults.tokenizer || new _Tokenizer(this.defaults);
          for (const prop in pack.tokenizer) {
            if (!(prop in tokenizer)) {
              throw new Error(`tokenizer '${prop}' does not exist`);
            }
            if (["options", "rules", "lexer"].includes(prop)) {
              continue;
            }
            const tokenizerProp = prop;
            const tokenizerFunc = pack.tokenizer[tokenizerProp];
            const prevTokenizer = tokenizer[tokenizerProp];
            tokenizer[tokenizerProp] = (...args2) => {
              let ret = tokenizerFunc.apply(tokenizer, args2);
              if (ret === false) {
                ret = prevTokenizer.apply(tokenizer, args2);
              }
              return ret;
            };
          }
          opts.tokenizer = tokenizer;
        }
        if (pack.hooks) {
          const hooks = this.defaults.hooks || new _Hooks();
          for (const prop in pack.hooks) {
            if (!(prop in hooks)) {
              throw new Error(`hook '${prop}' does not exist`);
            }
            if (prop === "options") {
              continue;
            }
            const hooksProp = prop;
            const hooksFunc = pack.hooks[hooksProp];
            const prevHook = hooks[hooksProp];
            if (_Hooks.passThroughHooks.has(prop)) {
              hooks[hooksProp] = (arg) => {
                if (this.defaults.async) {
                  return Promise.resolve(hooksFunc.call(hooks, arg)).then((ret2) => {
                    return prevHook.call(hooks, ret2);
                  });
                }
                const ret = hooksFunc.call(hooks, arg);
                return prevHook.call(hooks, ret);
              };
            } else {
              hooks[hooksProp] = (...args2) => {
                let ret = hooksFunc.apply(hooks, args2);
                if (ret === false) {
                  ret = prevHook.apply(hooks, args2);
                }
                return ret;
              };
            }
          }
          opts.hooks = hooks;
        }
        if (pack.walkTokens) {
          const walkTokens2 = this.defaults.walkTokens;
          const packWalktokens = pack.walkTokens;
          opts.walkTokens = function(token) {
            let values = [];
            values.push(packWalktokens.call(this, token));
            if (walkTokens2) {
              values = values.concat(walkTokens2.call(this, token));
            }
            return values;
          };
        }
        this.defaults = { ...this.defaults, ...opts };
      });
      return this;
    }
    setOptions(opt) {
      this.defaults = { ...this.defaults, ...opt };
      return this;
    }
    lexer(src, options2) {
      return _Lexer.lex(src, options2 ?? this.defaults);
    }
    parser(tokens, options2) {
      return _Parser.parse(tokens, options2 ?? this.defaults);
    }
    #parseMarkdown(lexer2, parser3) {
      return (src, options2) => {
        const origOpt = { ...options2 };
        const opt = { ...this.defaults, ...origOpt };
        if (this.defaults.async === true && origOpt.async === false) {
          if (!opt.silent) {
            console.warn("marked(): The async option was set to true by an extension. The async: false option sent to parse will be ignored.");
          }
          opt.async = true;
        }
        const throwError = this.#onError(!!opt.silent, !!opt.async);
        if (typeof src === "undefined" || src === null) {
          return throwError(new Error("marked(): input parameter is undefined or null"));
        }
        if (typeof src !== "string") {
          return throwError(new Error("marked(): input parameter is of type " + Object.prototype.toString.call(src) + ", string expected"));
        }
        if (opt.hooks) {
          opt.hooks.options = opt;
        }
        if (opt.async) {
          return Promise.resolve(opt.hooks ? opt.hooks.preprocess(src) : src).then((src2) => lexer2(src2, opt)).then((tokens) => opt.hooks ? opt.hooks.processAllTokens(tokens) : tokens).then((tokens) => opt.walkTokens ? Promise.all(this.walkTokens(tokens, opt.walkTokens)).then(() => tokens) : tokens).then((tokens) => parser3(tokens, opt)).then((html2) => opt.hooks ? opt.hooks.postprocess(html2) : html2).catch(throwError);
        }
        try {
          if (opt.hooks) {
            src = opt.hooks.preprocess(src);
          }
          let tokens = lexer2(src, opt);
          if (opt.hooks) {
            tokens = opt.hooks.processAllTokens(tokens);
          }
          if (opt.walkTokens) {
            this.walkTokens(tokens, opt.walkTokens);
          }
          let html2 = parser3(tokens, opt);
          if (opt.hooks) {
            html2 = opt.hooks.postprocess(html2);
          }
          return html2;
        } catch (e12) {
          return throwError(e12);
        }
      };
    }
    #onError(silent, async) {
      return (e12) => {
        e12.message += "\nPlease report this to https://github.com/markedjs/marked.";
        if (silent) {
          const msg = "<p>An error occurred:</p><pre>" + escape$1(e12.message + "", true) + "</pre>";
          if (async) {
            return Promise.resolve(msg);
          }
          return msg;
        }
        if (async) {
          return Promise.reject(e12);
        }
        throw e12;
      };
    }
  };
  var markedInstance = new Marked();
  function marked(src, opt) {
    return markedInstance.parse(src, opt);
  }
  marked.options = marked.setOptions = function(options2) {
    markedInstance.setOptions(options2);
    marked.defaults = markedInstance.defaults;
    changeDefaults(marked.defaults);
    return marked;
  };
  marked.getDefaults = _getDefaults;
  marked.defaults = _defaults;
  marked.use = function(...args) {
    markedInstance.use(...args);
    marked.defaults = markedInstance.defaults;
    changeDefaults(marked.defaults);
    return marked;
  };
  marked.walkTokens = function(tokens, callback) {
    return markedInstance.walkTokens(tokens, callback);
  };
  marked.parseInline = markedInstance.parseInline;
  marked.Parser = _Parser;
  marked.parser = _Parser.parse;
  marked.Renderer = _Renderer;
  marked.TextRenderer = _TextRenderer;
  marked.Lexer = _Lexer;
  marked.lexer = _Lexer.lex;
  marked.Tokenizer = _Tokenizer;
  marked.Hooks = _Hooks;
  marked.parse = marked;
  var options = marked.options;
  var setOptions = marked.setOptions;
  var use = marked.use;
  var walkTokens = marked.walkTokens;
  var parseInline = marked.parseInline;
  var parser2 = _Parser.parse;
  var lexer = _Lexer.lex;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.Z2X4OKXM.js
  var sharedMarked = new Marked();
  var connectedInstances = /* @__PURE__ */ new Set();
  var CsMarkdown = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.renderGeneration = 0;
      this.suppressSlotChange = false;
      this.tabSize = 4;
    }
    /** Returns the shared Marked instance used by all `<cs-markdown>` components. */
    static getMarked() {
      return sharedMarked;
    }
    /** Re-renders all connected `<cs-markdown>` instances. Call this after changing the Marked configuration. */
    static updateAll() {
      for (const instance of connectedInstances) {
        instance.renderMarkdown();
      }
    }
    /** A reference to the shared Marked instance for convenience. Equivalent to `CsMarkdown.getMarked()`. */
    get marked() {
      return sharedMarked;
    }
    connectedCallback() {
      super.connectedCallback();
      connectedInstances.add(this);
    }
    disconnectedCallback() {
      connectedInstances.delete(this);
      super.disconnectedCallback();
    }
    /**
     * Normalizes whitespace in the given text by converting leading tabs to spaces, trimming leading/trailing blank
     * lines, and removing the common indentation prefix from all lines.
     */
    dedent(text) {
      const normalized = text.replace(/\r\n/g, "\n");
      const lines = normalized.split("\n").map((line) => {
        let expanded = "";
        let column = 0;
        for (let i10 = 0; i10 < line.length; i10++) {
          const char = line[i10];
          if (char === "	") {
            const spacesToNextStop = this.tabSize - column % this.tabSize;
            expanded += " ".repeat(spacesToNextStop);
            column += spacesToNextStop;
          } else if (char === " ") {
            expanded += " ";
            column++;
          } else {
            expanded += line.slice(i10);
            break;
          }
        }
        return expanded;
      });
      let start = 0;
      while (start < lines.length && lines[start].trim() === "") {
        start++;
      }
      let end = lines.length - 1;
      while (end >= start && lines[end].trim() === "") {
        end--;
      }
      const trimmedLines = lines.slice(start, end + 1);
      if (trimmedLines.length === 0) {
        return "";
      }
      let minIndent = Infinity;
      for (const line of trimmedLines) {
        if (line.trim() === "") {
          continue;
        }
        const match = line.match(/^( *)/);
        const leadingSpaces = match ? match[1].length : 0;
        minIndent = Math.min(minIndent, leadingSpaces);
      }
      if (minIndent === Infinity) {
        minIndent = 0;
      }
      const dedentedLines = trimmedLines.map((line) => {
        if (line.trim() === "") {
          return "";
        }
        return line.slice(minIndent);
      });
      return dedentedLines.join("\n");
    }
    /** Finds the `<script type="text/markdown">` source element inside this component. */
    getSourceScript() {
      return this.querySelector('script[type="text/markdown"]');
    }
    /** Reads the script content, normalizes whitespace, parses markdown, and injects the result. */
    renderMarkdown() {
      const script = this.getSourceScript();
      if (!script) {
        console.warn(
          'No <script type="text/markdown"> found. Provide markdown content inside a <script type="text/markdown"> element.',
          this
        );
        return;
      }
      const generation = ++this.renderGeneration;
      const raw = script.textContent ?? "";
      const dedented = this.dedent(raw);
      let result;
      try {
        result = sharedMarked.parse(dedented);
      } catch (error) {
        console.error("Failed to parse markdown content.", error, this);
        return;
      }
      const inject = (renderedHtml) => {
        if (generation !== this.renderGeneration) {
          return;
        }
        this.suppressSlotChange = true;
        for (const child of [...this.childNodes]) {
          if (child !== script) {
            child.remove();
          }
        }
        const fragment = document.createRange().createContextualFragment(renderedHtml);
        this.appendChild(fragment);
        queueMicrotask(() => {
          this.suppressSlotChange = false;
        });
      };
      if (typeof result === "string") {
        inject(result);
      } else {
        result.then(inject).catch((error) => {
          console.error("Failed to parse markdown content.", error, this);
        });
      }
    }
    handleSlotChange() {
      if (this.suppressSlotChange) {
        return;
      }
      if (this.didSSR && !this.hasUpdated) {
        return;
      }
      this.renderMarkdown();
    }
    render() {
      return b2`<slot @slotchange=${this.handleSlotChange}></slot>`;
    }
  };
  CsMarkdown.css = markdown_styles_default;
  __decorateClass([
    n4({ type: Number, attribute: "tab-size" })
  ], CsMarkdown.prototype, "tabSize", 2);
  CsMarkdown = __decorateClass([
    customElement("cs-markdown")
  ], CsMarkdown);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.TDSLPPI2.js
  var CsMutationEvent = class extends Event {
    constructor(detail) {
      super("cs-mutation", { bubbles: true, cancelable: false, composed: true });
      this.detail = detail;
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.WO77PDD5.js
  var mutation_observer_styles_default = i`
  :host {
    display: contents;
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.U66JN7EU.js
  var CsMutationObserver = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.attrOldValue = false;
      this.charData = false;
      this.charDataOldValue = false;
      this.childList = false;
      this.disabled = false;
      this.handleMutation = (mutationList) => {
        this.dispatchEvent(new CsMutationEvent({ mutationList }));
      };
    }
    connectedCallback() {
      super.connectedCallback();
      if (typeof MutationObserver !== "undefined") {
        this.mutationObserver = new MutationObserver(this.handleMutation);
        if (!this.disabled) {
          this.startObserver();
        }
      }
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this.stopObserver();
    }
    startObserver() {
      const observeAttributes = typeof this.attr === "string" && this.attr.length > 0;
      const attributeFilter = observeAttributes && this.attr !== "*" ? this.attr.split(" ") : void 0;
      try {
        this.mutationObserver.observe(this, {
          subtree: true,
          childList: this.childList,
          attributes: observeAttributes,
          attributeFilter,
          attributeOldValue: this.attrOldValue,
          characterData: this.charData,
          characterDataOldValue: this.charDataOldValue
        });
      } catch {
      }
    }
    stopObserver() {
      this.mutationObserver.disconnect();
    }
    handleDisabledChange() {
      if (this.disabled) {
        this.stopObserver();
      } else {
        this.startObserver();
      }
    }
    handleChange() {
      this.stopObserver();
      this.startObserver();
    }
    render() {
      return b2` <slot></slot> `;
    }
  };
  CsMutationObserver.css = mutation_observer_styles_default;
  __decorateClass([
    n4({ reflect: true })
  ], CsMutationObserver.prototype, "attr", 2);
  __decorateClass([
    n4({ attribute: "attr-old-value", type: Boolean, reflect: true })
  ], CsMutationObserver.prototype, "attrOldValue", 2);
  __decorateClass([
    n4({ attribute: "char-data", type: Boolean, reflect: true })
  ], CsMutationObserver.prototype, "charData", 2);
  __decorateClass([
    n4({ attribute: "char-data-old-value", type: Boolean, reflect: true })
  ], CsMutationObserver.prototype, "charDataOldValue", 2);
  __decorateClass([
    n4({ attribute: "child-list", type: Boolean, reflect: true })
  ], CsMutationObserver.prototype, "childList", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsMutationObserver.prototype, "disabled", 2);
  __decorateClass([
    watch("disabled")
  ], CsMutationObserver.prototype, "handleDisabledChange", 1);
  __decorateClass([
    watch("attr", { waitUntilFirstUpdate: true }),
    watch("attr-old-value", { waitUntilFirstUpdate: true }),
    watch("char-data", { waitUntilFirstUpdate: true }),
    watch("char-data-old-value", { waitUntilFirstUpdate: true }),
    watch("childList", { waitUntilFirstUpdate: true })
  ], CsMutationObserver.prototype, "handleChange", 1);
  CsMutationObserver = __decorateClass([
    customElement("cs-mutation-observer")
  ], CsMutationObserver);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.SMIHAHYP.js
  var number_input_styles_default = i`
  :host(:focus) {
    outline: none;
  }

  .number-field {
    display: flex;
    align-items: stretch;
    justify-content: start;
    position: relative;
    height: var(--cs-form-control-height);
    border-color: var(--cs-form-control-border-color);
    border-radius: var(--cs-form-control-border-radius);
    border-style: var(--cs-form-control-border-style);
    border-width: var(--cs-form-control-border-width);
    cursor: text;
    color: var(--cs-form-control-value-color);
    font-size: inherit;
    font-family: inherit;
    font-weight: var(--cs-form-control-value-font-weight);
    line-height: var(--cs-form-control-value-line-height);
    vertical-align: middle;
    width: 100%;
    transition:
      background-color var(--cs-transition-normal),
      border-color var(--cs-transition-normal),
      outline-color var(--cs-transition-fast);
    transition-timing-function: var(--cs-transition-easing);
    background-color: var(--cs-form-control-background-color);
    padding: 0;
    outline: var(--cs-focus-ring-style) var(--cs-focus-ring-width) transparent;
    outline-offset: var(--cs-focus-ring-offset);

    &:focus-within {
      outline-color: var(--cs-color-focus);
    }

    /* Style disabled inputs */
    &:has(input:disabled) {
      cursor: not-allowed;
      opacity: 0.5;
    }
  }

  /* Appearance modifiers */
  :host([appearance='outlined']) {
    .number-field {
      background-color: var(--cs-form-control-background-color);
      border-color: var(--cs-form-control-border-color);
    }

    .stepper {
      color: var(--cs-color-neutral-on-quiet);

      @media (hover: hover) {
        &:hover:not(:disabled) {
          color: var(--cs-color-neutral-on-quiet);
          background-color: var(--cs-color-neutral-fill-quiet);
        }
      }

      &:active:not(:disabled) {
        color: color-mix(in oklab, var(--cs-color-neutral-on-quiet), var(--cs-color-mix-active));
        background-color: color-mix(in oklab, var(--cs-color-neutral-fill-quiet), var(--cs-color-mix-active));
      }
    }
  }

  :host([appearance='filled']) {
    .number-field {
      background-color: var(--cs-color-neutral-fill-quiet);
      border-color: var(--cs-color-neutral-fill-quiet);
    }

    .stepper {
      color: var(--cs-color-neutral-on-quiet);

      @media (hover: hover) {
        &:hover:not(:disabled) {
          color: var(--cs-color-neutral-on-normal);
          background-color: var(--cs-color-neutral-fill-normal);
        }
      }

      &:active:not(:disabled) {
        color: color-mix(in oklab, var(--cs-color-neutral-on-normal), var(--cs-color-mix-active));
        background-color: color-mix(in oklab, var(--cs-color-neutral-fill-normal), var(--cs-color-mix-active));
      }
    }
  }

  :host([appearance='filled-outlined']) {
    .number-field {
      background-color: var(--cs-color-neutral-fill-quiet);
      border-color: var(--cs-form-control-border-color);
    }

    .stepper {
      color: var(--cs-color-neutral-on-quiet);

      @media (hover: hover) {
        &:hover:not(:disabled) {
          color: var(--cs-color-neutral-on-normal);
          background-color: var(--cs-color-neutral-fill-normal);
        }
      }

      &:active:not(:disabled) {
        color: color-mix(in oklab, var(--cs-color-neutral-on-normal), var(--cs-color-mix-active));
        background-color: color-mix(in oklab, var(--cs-color-neutral-fill-normal), var(--cs-color-mix-active));
      }
    }
  }

  :host([pill]) {
    .number-field,
    .stepper {
      border-radius: var(--cs-border-radius-pill);
    }
  }

  .number-field {
    /* Show autofill styles over the entire number field, not just the native <input> */
    &:has(:autofill),
    &:has(:-webkit-autofill) {
      background-color: var(--cs-color-brand-fill-quiet) !important;
    }

    input {
      flex: auto;
      height: 100%;
      width: auto;
      min-width: 0;
      margin: 0;
      padding: 0 var(--cs-form-control-padding-inline);
      outline: none;
      box-shadow: none;
      border: none;
      background-color: transparent;
      font: inherit;
      transition: inherit;
      cursor: inherit;
      -webkit-appearance: none;

      /* Center-align and use tabular numbers for better alignment */
      text-align: center;
      font-variant-numeric: tabular-nums;

      /* Hide the number spinners in Firefox */
      -moz-appearance: textfield;

      /* Hide the number spinners in Chrome/Safari */
      &::-webkit-outer-spin-button,
      &::-webkit-inner-spin-button {
        -webkit-appearance: none;
        margin: 0;
        display: none;
      }

      /* Turn off Safari's autofill styles */
      &:-webkit-autofill,
      &:-webkit-autofill:hover,
      &:-webkit-autofill:focus,
      &:-webkit-autofill:active {
        -webkit-background-clip: text;
        background-color: transparent;
        -webkit-text-fill-color: inherit;
      }
    }

    &:autofill {
      &,
      &:hover,
      &:focus,
      &:active {
        box-shadow: none;
        caret-color: var(--cs-form-control-value-color);
      }
    }

    &::placeholder {
      color: var(--cs-form-control-placeholder-color);
      user-select: none;
      -webkit-user-select: none;
    }

    &:focus {
      outline: none;
    }
  }

  .start,
  .end {
    display: inline-flex;
    flex: 1;
    align-items: center;
    cursor: default;

    &::slotted(cs-icon) {
      color: var(--cs-color-neutral-on-quiet);
    }
  }

  .start {
    justify-content: start;
    margin-inline-start: var(--cs-form-control-padding-inline);
  }

  .end {
    justify-content: end;
    margin-inline-end: var(--cs-form-control-padding-inline);
  }

  /*
   * Steppers - horizontal layout with minus on start, plus on end
   */

  .stepper {
    color: var(--cs-color-neutral-on-quiet);
    display: flex;
    align-items: center;
    justify-content: center;
    aspect-ratio: 1 / 1;
    height: calc(100% - var(--cs-form-control-border-width) * 2);
    flex: 0 0 auto;
    border: none;
    border-radius: calc(var(--cs-form-control-border-radius) - var(--cs-form-control-border-width) * 2);
    background: transparent;
    cursor: pointer;
    margin: var(--cs-form-control-border-width);
    padding: 0;
    font-size: inherit;
    transition-property: background-color, color;
    transition-duration: var(--cs-transition-fast);
    transition-timing-function: var(--cs-transition-easing);

    &:disabled {
      opacity: 0.5;
      cursor: not-allowed;
    }

    &:focus {
      outline: none;
    }
  }

  :host([without-steppers]) .stepper {
    display: none;
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.7C5LGY7P.js
  var CsNumberInput = class extends CornerstoneFormAssociatedElement {
    constructor() {
      super(...arguments);
      this.assumeInteractionOn = ["blur", "input"];
      this.hasSlotController = new HasSlotController(this, "hint", "label");
      this.localize = new LocalizeController2(this);
      this.title = "";
      this._value = null;
      this.defaultValue = this.getAttribute("value") || null;
      this.size = "m";
      this.appearance = "outlined";
      this.pill = false;
      this.label = "";
      this.hint = "";
      this.placeholder = "";
      this.readonly = false;
      this.required = false;
      this.step = 1;
      this.withoutSteppers = false;
      this.inputmode = "numeric";
      this.ssrLabel = false;
      this.ssrHint = false;
    }
    static get validators() {
      return [...super.validators, MirrorValidator()];
    }
    /** The current value of the input, submitted as a name/value pair with form data. */
    get value() {
      if (this.valueHasChanged) {
        return this._value;
      }
      return this._value ?? this.defaultValue;
    }
    set value(val) {
      if (this._value === val) {
        return;
      }
      this.valueHasChanged = true;
      this._value = val;
    }
    /**
     * @internal
     */
    updateFormValue(value) {
      if (value == null) {
        this.setValue("", null);
        return;
      }
      super.updateFormValue(value);
    }
    /** Returns true if the value is at or below the minimum. */
    get isAtMin() {
      if (this.min === void 0) {
        return false;
      }
      const numValue = parseFloat(this.value || "");
      return !isNaN(numValue) && numValue <= this.min;
    }
    /** Returns true if the value is at or above the maximum. */
    get isAtMax() {
      if (this.max === void 0) {
        return false;
      }
      const numValue = parseFloat(this.value || "");
      return !isNaN(numValue) && numValue >= this.max;
    }
    handleChange(event) {
      this.value = this.input.value;
      this.relayNativeEvent(event, { bubbles: true, composed: true });
    }
    handleInput() {
      this.value = this.input.value;
    }
    handleKeyDown(event) {
      submitOnEnter(event, this);
      if (event.key === "ArrowUp" || event.key === "ArrowDown") {
        requestAnimationFrame(() => {
          if (this.value !== this.input.value) {
            this.value = this.input.value;
          }
        });
      }
    }
    handleStepperPointerUp(direction, event) {
      if (this.disabled || this.readonly) {
        return;
      }
      const beforeInputEvent = new InputEvent("beforeinput", { bubbles: true, cancelable: true, composed: true });
      this.dispatchEvent(beforeInputEvent);
      if (beforeInputEvent.defaultPrevented) {
        return;
      }
      if (direction === "up") {
        this.input.stepUp();
      } else {
        this.input.stepDown();
      }
      if (this.value !== this.input.value) {
        this.value = this.input.value;
      }
      this.dispatchEvent(new InputEvent("input", { bubbles: true, composed: true }));
      this.dispatchEvent(new Event("change", { bubbles: true, composed: true }));
      if (event.pointerType !== "touch") {
        this.input.focus();
      }
    }
    handleStepperPointerDown(event) {
      if (event.pointerType === "touch") {
        return;
      }
      event.preventDefault();
      this.input.focus();
    }
    updated(changedProperties) {
      super.updated(changedProperties);
      if (changedProperties.has("value") || changedProperties.has("defaultValue")) {
        if (this.input && this.value && this.input.value !== this.value) {
          this._value = this.input.value;
        }
        this.customStates.set("blank", !this.value);
      }
    }
    handleStepChange() {
      this.input.step = String(this.step);
      this.updateValidity();
    }
    /** Sets focus on the input. */
    focus(options2) {
      this.input.focus(options2);
    }
    /** Removes focus from the input. */
    blur() {
      this.input.blur();
    }
    /** Selects all the text in the input. */
    select() {
      this.input.select();
    }
    /** Increments the value by the step amount. */
    stepUp() {
      this.input.stepUp();
      if (this.value !== this.input.value) {
        this.value = this.input.value;
      }
    }
    /** Decrements the value by the step amount. */
    stepDown() {
      this.input.stepDown();
      if (this.value !== this.input.value) {
        this.value = this.input.value;
      }
    }
    formResetCallback() {
      this.value = this.defaultValue;
      super.formResetCallback();
    }
    render() {
      const hasLabelSlot = this.hasSlotController.test("label", "ssrLabel");
      const hasHintSlot = this.hasSlotController.test("hint", "ssrHint");
      const hasLabel = this.label ? true : !!hasLabelSlot;
      const hasHint = this.hint ? true : !!hasHintSlot;
      return b2`
      <label
        part="form-control-label"
        class=${e7({
        label: true,
        "has-label": hasLabel
      })}
        for="input"
        aria-hidden=${hasLabel ? "false" : "true"}
      >
        <slot name="label">${this.label}</slot>
      </label>

      <div part="number-input" class="number-field">
        ${!this.withoutSteppers ? b2`
                <button
                  part="stepper stepper-decrement"
                  class="stepper stepper-decrement"
                  type="button"
                  tabindex="-1"
                  aria-label=${this.localize.term("decrement")}
                  ?disabled=${this.disabled || this.readonly || this.isAtMin}
                  @pointerdown=${this.handleStepperPointerDown}
                  @pointerup=${(event) => this.handleStepperPointerUp("down", event)}
                >
                  <slot name="decrement-icon">
                    <cs-icon name="remove" library="system"></cs-icon>
                  </slot>
                </button>
              ` : ""}

        <slot name="start" part="start" class="start"></slot>

        <input
          part="input"
          id="input"
          class="control"
          type="number"
          inputmode=${o8(this.inputmode)}
          title=${this.title}
          name=${o8(this.name)}
          ?disabled=${this.disabled}
          ?readonly=${this.readonly}
          ?required=${this.required}
          placeholder=${o8(this.placeholder)}
          min=${o8(this.min)}
          max=${o8(this.max)}
          step=${o8(this.step)}
          .value=${l5(this.value ?? "")}
          autocomplete=${o8(this.autocomplete)}
          ?autofocus=${this.autofocus}
          enterkeyhint=${o8(this.enterkeyhint)}
          aria-describedby="hint"
          @change=${this.handleChange}
          @input=${this.handleInput}
          @keydown=${this.handleKeyDown}
        />

        <slot name="end" part="end" class="end"></slot>

        ${!this.withoutSteppers ? b2`
                <button
                  part="stepper stepper-increment"
                  class="stepper stepper-increment"
                  type="button"
                  tabindex="-1"
                  aria-label=${this.localize.term("increment")}
                  ?disabled=${this.disabled || this.readonly || this.isAtMax}
                  @pointerdown=${this.handleStepperPointerDown}
                  @pointerup=${(event) => this.handleStepperPointerUp("up", event)}
                >
                  <slot name="increment-icon">
                    <cs-icon name="add" library="system"></cs-icon>
                  </slot>
                </button>
              ` : ""}
      </div>

      <slot
        id="hint"
        part="hint"
        name="hint"
        class=${e7({
        "has-slotted": hasHint
      })}
        aria-hidden=${hasHint ? "false" : "true"}
        >${this.hint}</slot
      >
    `;
    }
  };
  CsNumberInput.css = [size_styles_default, form_control_styles_default, number_input_styles_default];
  CsNumberInput.shadowRootOptions = { ...CornerstoneFormAssociatedElement.shadowRootOptions, delegatesFocus: true };
  __decorateClass([
    e5("input")
  ], CsNumberInput.prototype, "input", 2);
  __decorateClass([
    n4()
  ], CsNumberInput.prototype, "title", 2);
  __decorateClass([
    r5()
  ], CsNumberInput.prototype, "value", 1);
  __decorateClass([
    n4({ attribute: "value", reflect: true })
  ], CsNumberInput.prototype, "defaultValue", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsNumberInput.prototype, "size", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsNumberInput.prototype, "appearance", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsNumberInput.prototype, "pill", 2);
  __decorateClass([
    n4()
  ], CsNumberInput.prototype, "label", 2);
  __decorateClass([
    n4({ attribute: "hint" })
  ], CsNumberInput.prototype, "hint", 2);
  __decorateClass([
    n4()
  ], CsNumberInput.prototype, "placeholder", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsNumberInput.prototype, "readonly", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsNumberInput.prototype, "required", 2);
  __decorateClass([
    n4({ type: Number })
  ], CsNumberInput.prototype, "min", 2);
  __decorateClass([
    n4({ type: Number })
  ], CsNumberInput.prototype, "max", 2);
  __decorateClass([
    n4()
  ], CsNumberInput.prototype, "step", 2);
  __decorateClass([
    n4({ attribute: "without-steppers", type: Boolean })
  ], CsNumberInput.prototype, "withoutSteppers", 2);
  __decorateClass([
    n4()
  ], CsNumberInput.prototype, "autocomplete", 2);
  __decorateClass([
    n4({ type: Boolean })
  ], CsNumberInput.prototype, "autofocus", 2);
  __decorateClass([
    n4()
  ], CsNumberInput.prototype, "enterkeyhint", 2);
  __decorateClass([
    n4()
  ], CsNumberInput.prototype, "inputmode", 2);
  __decorateClass([
    n4({ attribute: "ssr-label", type: Boolean })
  ], CsNumberInput.prototype, "ssrLabel", 2);
  __decorateClass([
    n4({ attribute: "ssr-hint", type: Boolean })
  ], CsNumberInput.prototype, "ssrHint", 2);
  __decorateClass([
    watch("step", { waitUntilFirstUpdate: true })
  ], CsNumberInput.prototype, "handleStepChange", 1);
  CsNumberInput = __decorateClass([
    customElement("cs-number-input")
  ], CsNumberInput);
  CsNumberInput.disableWarning?.("change-in-update");

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.CMBDV5NH.js
  var option_styles_default = i`
  :host {
    --current-text-color: var(--cs-color-brand-on-loud);

    display: block;
    color: var(--cs-color-text-normal);
    -webkit-user-select: none;
    user-select: none;

    position: relative;
    display: flex;
    align-items: center;
    font: inherit;
    padding: 0.5em 1em 0.5em 0.25em;
    border-radius: var(--cs-border-radius-s);
    line-height: var(--cs-line-height-condensed);
    transition: var(--cs-transition-fast) background-color var(--cs-transition-easing);
    cursor: pointer;
  }

  :host(:focus) {
    outline: none;
  }

  @media (hover: hover) {
    :host(:not(:state(disabled), :state(current)):is(:state(hover), :hover)) {
      background-color: var(--cs-color-neutral-fill-normal);
      color: var(--cs-color-neutral-on-normal);
    }
  }

  :host(:state(current)),
  :host(:state(disabled):state(current)) {
    background-color: var(--cs-form-control-activated-color);
    color: var(--current-text-color);
    opacity: 1;
  }

  :host(:state(disabled)) {
    outline: none;
    opacity: 0.5;
    cursor: not-allowed;
  }

  .label {
    flex: 1 1 auto;
    display: inline-block;
  }

  .check {
    flex: 0 0 auto;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: var(--cs-font-size-smaller);
    visibility: hidden;
    width: 2em;
  }

  :host(:state(selected)) .check {
    visibility: visible;
  }

  .start,
  .end {
    flex: 0 0 auto;
    display: flex;
    align-items: center;
  }

  /* These are !important because a document-level universal margin reset would otherwise beat them:
     a slotted element belongs to the outer tree. See callout.styles.ts for the full note. */
  .start::slotted(*) {
    margin-inline-end: 0.5em !important;
  }

  .end::slotted(*) {
    margin-inline-start: 0.5em !important;
  }

  @media (forced-colors: active) {
    :host(:hover:not([aria-disabled='true'])) {
      outline: dashed 1px SelectedItem;
      outline-offset: -1px;
    }
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.P74M3H26.js
  function getText(root, depth = 0) {
    if (!root || !globalThis.Node) {
      return "";
    }
    if (typeof root[Symbol.iterator] === "function") {
      const nodes = Array.isArray(root) ? root : [...root];
      return nodes.map((node2) => getText(node2, --depth)).join("");
    }
    const node = root;
    if (node.nodeType === Node.TEXT_NODE) {
      return node.textContent ?? "";
    }
    if (node.nodeType === Node.ELEMENT_NODE) {
      const element = node;
      if (element.hasAttribute("slot") || element.matches("style, script")) {
        return "";
      }
      if (element instanceof HTMLSlotElement) {
        const assignedNodes = element.assignedNodes({ flatten: true });
        if (assignedNodes.length > 0) {
          return getText(assignedNodes, --depth);
        }
      }
      return depth > -1 ? getText(element, --depth) : element.textContent ?? "";
    }
    return node.hasChildNodes() ? getText(node.childNodes, --depth) : "";
  }
  var CsOption = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.localize = new LocalizeController2(this);
      this.cachedDefaultLabel = "";
      this.isInitialized = false;
      this.isDefaultLabelDirty = true;
      this.current = false;
      this.value = "";
      this.disabled = false;
      this.selected = false;
      this.defaultSelected = false;
      this._label = "";
      this.handleHover = (event) => {
        if (event.type === "mouseenter") {
          this.customStates.set("hover", true);
        } else if (event.type === "mouseleave") {
          this.customStates.set("hover", false);
        }
      };
    }
    set label(value) {
      const oldValue = this._label;
      this._label = value || "";
      if (this._label !== oldValue) {
        this.requestUpdate("label", oldValue);
      }
    }
    get label() {
      if (this._label) {
        return this._label;
      }
      return this.defaultLabel;
    }
    /** The default label, generated from the element contents. Will be equal to `label` in most cases. */
    get defaultLabel() {
      if (this.isDefaultLabelDirty || !this.cachedDefaultLabel) {
        this.updateDefaultLabel();
      }
      return this.cachedDefaultLabel;
    }
    connectedCallback() {
      super.connectedCallback();
      this.setAttribute("role", "option");
      this.setAttribute("aria-selected", "false");
      this.addEventListener("mouseenter", this.handleHover);
      this.addEventListener("mouseleave", this.handleHover);
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this.removeEventListener("mouseenter", this.handleHover);
      this.removeEventListener("mouseleave", this.handleHover);
    }
    handleDefaultSlotChange() {
      this.isDefaultLabelDirty = true;
      if (this.isInitialized) {
        customElements.whenDefined("cs-select").then(() => {
          const controller = this.closest("cs-select");
          if (controller) {
            controller.handleDefaultSlotChange?.();
          }
        });
        customElements.whenDefined("cs-combobox").then(() => {
          const controller = this.closest("cs-combobox");
          if (controller) {
            controller.handleDefaultSlotChange?.();
          }
        });
      } else {
        this.isInitialized = true;
      }
    }
    willUpdate(changedProperties) {
      if (changedProperties.has("defaultSelected")) {
        if (this.didSSR && this.hasUpdated || !this.didSSR) {
          this.syncDefaultSelected();
        }
      }
      super.willUpdate(changedProperties);
    }
    syncDefaultSelected() {
      if ("closest" in this) {
        if (!this.closest("cs-combobox, cs-select")?.hasInteracted) {
          if (this.defaultSelected) {
            const oldVal = this.selected;
            this.selected = this.defaultSelected;
            this.requestUpdate("selected", oldVal);
          }
        }
      }
    }
    updated(changedProperties) {
      if (changedProperties.has("disabled")) {
        this.setAttribute("aria-disabled", this.disabled ? "true" : "false");
        this.customStates.set("disabled", this.disabled);
      }
      if (changedProperties.has("selected")) {
        this.setAttribute("aria-selected", this.selected ? "true" : "false");
        this.customStates.set("selected", this.selected);
      }
      if (changedProperties.has("value")) {
        if (typeof this.value !== "string") {
          this.value = String(this.value);
        }
        this.handleDefaultSlotChange();
      }
      if (changedProperties.has("current")) {
        this.customStates.set("current", this.current);
      }
      super.updated(changedProperties);
    }
    async firstUpdated(changedProperties) {
      super.firstUpdated(changedProperties);
      if (this.didSSR && !this.hasUpdated) {
        await this.updateComplete;
        this.syncDefaultSelected();
      } else {
        this.syncDefaultSelected();
      }
      if (this.selected && !this.defaultSelected) {
        const parent = this.closest("cs-select, cs-combobox");
        if (parent && !parent.hasInteracted) {
          await customElements.whenDefined(parent.localName);
          await parent.updateComplete;
          parent.selectionChanged?.();
        }
      }
    }
    updateDefaultLabel() {
      const oldValue = this.cachedDefaultLabel;
      this.cachedDefaultLabel = getText(this).trim();
      this.isDefaultLabelDirty = false;
      const changed = this.cachedDefaultLabel !== oldValue;
      if (!this._label && changed) {
        this.requestUpdate("label", oldValue);
      }
      return changed;
    }
    render() {
      const selected = this.selected;
      if (this.didSSR && !this.hasUpdated) {
        this.updateComplete.then(() => {
          this.requestUpdate();
        });
        return A;
      }
      return b2`
      ${selected ? b2`<cs-icon part="checked-icon" class="check" name="check" library="system" aria-hidden="true"></cs-icon>` : b2`<span part="checked-icon" class="check" aria-hidden="true"></span>`}
      <slot part="start" name="start" class="start"></slot>
      <slot part="label" class="label" @slotchange=${this.handleDefaultSlotChange}></slot>
      <slot part="end" name="end" class="end"></slot>
    `;
    }
  };
  CsOption.css = option_styles_default;
  __decorateClass([
    e5(".label")
  ], CsOption.prototype, "defaultSlot", 2);
  __decorateClass([
    r5()
  ], CsOption.prototype, "current", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsOption.prototype, "value", 2);
  __decorateClass([
    n4({ type: Boolean })
  ], CsOption.prototype, "disabled", 2);
  __decorateClass([
    n4({ type: Boolean, attribute: false })
  ], CsOption.prototype, "selected", 2);
  __decorateClass([
    n4({ type: Boolean, attribute: "selected" })
  ], CsOption.prototype, "defaultSelected", 2);
  __decorateClass([
    n4()
  ], CsOption.prototype, "label", 1);
  CsOption = __decorateClass([
    customElement("cs-option")
  ], CsOption);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.3253QLBI.js
  var CsCompleteEvent = class extends Event {
    constructor() {
      super("cs-complete", { bubbles: true, cancelable: true, composed: true });
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.Q2I3P62M.js
  var otp_input_styles_default = i`
  :host(:focus) {
    outline: none;
  }

  /* Segments container */
  .segments {
    position: relative;
    /* Codes read left-to-right regardless of locale — keep segment order and caret movement LTR
       even when the surrounding page is RTL. */
    direction: ltr;
    display: inline-flex;
    align-items: center;
    align-self: start;
    gap: var(--segment-gap, var(--cs-space-xs));
    cursor: text;
    /* Never grow past the host's available width — long values or large segment sizes scroll
       horizontally instead of overflowing the page. */
    max-width: 100%;
    overflow-x: auto;
    scrollbar-width: none;
    /* Setting overflow-x forces overflow-y to also compute to non-visible, which would otherwise
       clip the focus ring's bleed around the active segment — above/below for any segment, and
       left/right for the first/last segment specifically. Reserve room for it with padding, then
       cancel the layout impact with an equal negative margin on both axes. */
    padding: calc(var(--cs-focus-ring-offset) + var(--cs-focus-ring-width));
    margin: calc(-1 * (var(--cs-focus-ring-offset) + var(--cs-focus-ring-width)));
  }

  .segments::-webkit-scrollbar {
    width: 0;
    height: 0;
  }

  :host(:state(disabled)) .segments {
    cursor: not-allowed;
    opacity: 0.5;
  }

  :host(:state(readonly)) .segments {
    cursor: default;
  }

  /* Focus ring on the active segment, and on every segment in a multi-character selection */
  .segments:focus-within .segment--active,
  .segments:focus-within .segment--selected {
    outline-color: var(--cs-color-focus);
  }

  /* Readonly has no per-segment active/selected state (see render()), so every segment rings
     at once to show the control as a whole has focus. */
  :host(:state(readonly)) .segments:focus-within .segment {
    outline-color: var(--cs-color-focus);
  }

  /* Hidden real input — off-screen but focusable.
     Chromium mishandles typing over a full selection (drops the inserted character) when a
     text input has zero layout size, so this stays a non-zero 1x1px box instead of 0x0. */
  .hidden-input {
    position: absolute;
    width: 1px;
    height: 1px;
    opacity: 0;
    overflow: hidden;
    pointer-events: none;
    border: none;
    padding: 0;
    margin: 0;
  }

  /* Individual visual segment */
  .segment {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    width: var(--segment-size, 2.5em);
    height: var(--segment-size, 2.5em);
    border-radius: var(--segment-border-radius, var(--cs-form-control-border-radius));
    font-size: 1em;
    font-family: inherit;
    font-variant-numeric: tabular-nums;
    position: relative;
    user-select: none;
    outline: var(--cs-focus-ring-style) var(--cs-focus-ring-width) transparent;
    outline-offset: var(--cs-focus-ring-offset);
    transition:
      background-color var(--cs-transition-normal),
      border-color var(--cs-transition-normal),
      outline-color var(--cs-transition-fast);
    transition-timing-function: var(--cs-transition-easing);
  }

  /* Blinking caret in the active segment */
  .caret {
    position: absolute;
    width: 1.5px;
    height: 60%;
    background-color: currentColor;
    animation: cs-otp-caret-blink 1s step-end infinite;
  }

  @keyframes cs-otp-caret-blink {
    0%,
    100% {
      opacity: 1;
    }
    50% {
      opacity: 0;
    }
  }

  /* Literal separator character between segment groups */
  .segment-literal {
    display: inline-block;
    flex-shrink: 0;
    color: var(--cs-color-text-quiet);
    white-space: pre;
    user-select: none;
  }

  /* Appearance: outlined (default) */
  :host(:not([appearance='filled'], [appearance='filled-outlined'], [appearance='contained'])) .segment {
    background-color: var(--cs-form-control-background-color);
    border: var(--cs-form-control-border-width) var(--cs-form-control-border-style) var(--cs-form-control-border-color);
  }

  /* Appearance: filled */
  :host([appearance='filled']) .segment {
    background-color: var(--cs-color-neutral-fill-quiet);
    border: var(--cs-form-control-border-width) var(--cs-form-control-border-style) transparent;
  }

  /* Appearance: filled-outlined */
  :host([appearance='filled-outlined']) .segment {
    background-color: var(--cs-color-neutral-fill-quiet);
    border: var(--cs-form-control-border-width) var(--cs-form-control-border-style) var(--cs-form-control-border-color);
  }

  /* Appearance: contained */
  :host([appearance='contained']) .segments {
    gap: 0;
    border: var(--cs-form-control-border-width) var(--cs-form-control-border-style) var(--cs-form-control-border-color);
    border-radius: var(--segment-border-radius, var(--cs-form-control-border-radius));
    background-color: var(--cs-form-control-background-color);
    overflow: hidden;
    /* The focus ring is drawn inward here (see outline-offset below), so there's no outward bleed
       to reserve room for. .segments is also the visible bordered box in this appearance, so the
       padding/negative-margin bleed trick from the base rule would visibly shift and inflate it. */
    padding: 0;
    margin: 0;
  }

  :host([appearance='contained']) .segment {
    border: none;
    border-radius: 0;
    /* Contained segments sit flush with zero gap and have no border of their own, so a ring drawn
       outside the segment edge (the default, positive offset) bleeds into the neighboring segment.
       Draw it inward instead so it stays within this segment's own box. */
    outline-offset: calc(-1 * var(--cs-focus-ring-width));
  }

  /* Dividers between contained segments */
  :host([appearance='contained']) .segment + .segment,
  :host([appearance='contained']) .segment-literal + .segment {
    border-left: var(--cs-form-control-border-width) var(--cs-form-control-border-style)
      var(--cs-form-control-border-color);
  }

  /* ── Active segment (where next char will go), and every segment in a multi-character
     selection (e.g. from Cmd/Ctrl+A) — same border + focus-ring treatment for both.
     :host(...) wrapper matches the specificity of the appearance rules above so this
     border-color isn't silently lost to the cascade. ── */
  :host(:not(:state(readonly))) .segment--active,
  :host(:not(:state(readonly))) .segment--selected {
    border-color: var(--cs-color-focus);
  }

  /* Masked filled character, and the empty-segment hint shown when with-mask is set, both draw
     --mask-char via a pseudo-element instead of real text, so a masked value never touches the
     DOM as plain text (nothing to find via view-source or copy). */
  .segment--masked::before,
  .segment--mask-hint::before {
    content: var(--mask-char, '•');
  }

  .segment--mask-hint::before {
    opacity: 0.35;
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.N6MI6RJR.js
  var CsOtpInput = class extends CornerstoneFormAssociatedElement {
    constructor() {
      super(...arguments);
      this.hasSlotController = new HasSlotController(this, "label", "hint");
      this._focused = false;
      this._activeIndex = -1;
      this._selectionAnchor = -1;
      this._pendingClickIndex = null;
      this._value = "";
      this.defaultValue = this.getAttribute("value") ?? null;
      this.length = 6;
      this.appearance = "outlined";
      this.type = "numeric";
      this.mask = false;
      this.case = "preserve";
      this.size = "m";
      this.label = "";
      this.hint = "";
      this.ssrLabel = false;
      this.ssrHint = false;
      this.format = "";
      this.autocomplete = "one-time-code";
      this.required = false;
      this.readonly = false;
      this.autosubmit = false;
      this.autofocus = false;
      this.withMask = false;
      this.assumeInteractionOn = ["blur", "input"];
      this._lastChangeValue = "";
    }
    static get validators() {
      return [...super.validators, MirrorValidator()];
    }
    get validationTarget() {
      return this.segmentsContainer;
    }
    get hasSelection() {
      return this._selectionAnchor >= 0 && this._selectionAnchor !== this._activeIndex;
    }
    // Move the caret to `index` with no active selection.
    setCaretIndex(index) {
      this._activeIndex = index;
      this._selectionAnchor = -1;
    }
    /** The current value of the OTP field, submitted as a name/value pair with form data. */
    get value() {
      return this._value;
    }
    set value(val) {
      const next = this.filterAndTransform(val).slice(0, this.effectiveLength);
      if (this._value === next) {
        return;
      }
      const oldValue = this._value;
      this._value = next;
      this.setValue(next);
      if (this.input) {
        this.input.value = next;
      }
      if (this._focused) {
        this.setCaretIndex(Math.min(next.length, this.effectiveLength - 1));
      }
      this.requestUpdate("value", oldValue);
    }
    /** Number of segments derived from `format` (count of `#`) or `length`. */
    get effectiveLength() {
      return this.format ? [...this.format].filter((c6) => c6 === "#").length : this.length;
    }
    /** Parsed format array — each entry is a segment slot or a literal separator character. */
    get parsedFormat() {
      const src = this.format || "#".repeat(this.length);
      return [...src].map((c6) => ({ type: c6 === "#" ? "segment" : "separator", char: c6 }));
    }
    filterAndTransform(val) {
      let s7 = val;
      if (this.type === "numeric") {
        s7 = s7.replace(/\D/g, "");
      } else if (this.type === "alpha") {
        s7 = s7.replace(/[^a-zA-Z]/g, "");
      } else if (this.type === "alphanumeric") {
        s7 = s7.replace(/[^a-zA-Z0-9]/g, "");
      }
      if (this.case === "upper") {
        s7 = s7.toUpperCase();
      } else if (this.case === "lower") {
        s7 = s7.toLowerCase();
      }
      return s7;
    }
    willUpdate(changedProperties) {
      super.willUpdate(changedProperties);
      if (!this.hasUpdated) {
        const initial = this.filterAndTransform(this.defaultValue ?? "").slice(0, this.effectiveLength);
        if (this._value !== initial) {
          this._value = initial;
          this.setValue(initial);
          this._lastChangeValue = initial;
        }
      }
      if (this.hasUpdated && (changedProperties.has("type") || changedProperties.has("case") || changedProperties.has("length") || changedProperties.has("format"))) {
        const refiltered = this.filterAndTransform(this._value).slice(0, this.effectiveLength);
        if (refiltered !== this._value) {
          this._value = refiltered;
          this.setValue(refiltered);
          if (this.input) {
            this.input.value = refiltered;
          }
        }
      }
    }
    updated(changedProperties) {
      super.updated(changedProperties);
      const v2 = this._value;
      this.customStates.set("--blank", v2.length === 0);
      this.customStates.set("--filled", v2.length === this.effectiveLength);
      this.customStates.set("readonly", this.readonly);
      if (changedProperties.has("value") || changedProperties.has("required") || changedProperties.has("length") || changedProperties.has("format")) {
        this.updateValidity();
      }
      this.syncCursor();
      const activeSegment = this.segmentsContainer?.querySelector(".segment--active, .segment--selected");
      if (activeSegment && this.segmentsContainer) {
        scrollIntoView(activeSegment, this.segmentsContainer, "horizontal", "auto");
      }
    }
    // Position the hidden input cursor at _activeIndex. If the slot has a character,
    // select it (so typing replaces it); otherwise place an empty cursor there.
    syncCursor() {
      if (!this._focused || !this.input || this._activeIndex < 0) {
        return;
      }
      if (this.hasSelection) {
        return;
      }
      const len = this._value.length;
      const start = Math.min(this._activeIndex, len);
      const end = this._activeIndex < len ? start + 1 : start;
      this.input.setSelectionRange(start, end);
    }
    formResetCallback() {
      super.formResetCallback();
      const reset = this.filterAndTransform(this.defaultValue ?? "").slice(0, this.effectiveLength);
      const oldValue = this._value;
      this._value = reset;
      this.setValue(reset);
      this._lastChangeValue = reset;
      if (this.input) {
        this.input.value = reset;
      }
      this.requestUpdate("value", oldValue);
    }
    handleInput(event) {
      if (this.readonly) {
        return;
      }
      const input = event.target;
      const rawValue = input.value;
      const rawCursor = input.selectionStart ?? rawValue.length;
      const filtered = this.filterAndTransform(rawValue).slice(0, this.effectiveLength);
      let cursorAfterFilter = rawCursor;
      if (rawValue !== filtered) {
        input.value = filtered;
        const rawPrefix = rawValue.slice(0, rawCursor);
        cursorAfterFilter = Math.min(this.filterAndTransform(rawPrefix).length, this.effectiveLength);
      }
      this.setCaretIndex(Math.min(cursorAfterFilter, this.effectiveLength - 1));
      const prevLength = this._value.length;
      const oldValue = this._value;
      this._value = filtered;
      this.setValue(filtered);
      this.maybeDispatchComplete(filtered.length === this.effectiveLength && prevLength < this.effectiveLength);
      this.requestUpdate("value", oldValue);
    }
    // Dispatch cs-complete when the value just became fully filled, then submit the form if
    // autosubmit is enabled and no listener canceled the event.
    maybeDispatchComplete(justCompleted) {
      if (!justCompleted) {
        return;
      }
      const notCanceled = this.dispatchEvent(new CsCompleteEvent());
      if (this.autosubmit && notCanceled) {
        setTimeout(() => submitForm(this));
      }
    }
    handleKeyDown(event) {
      if (event.isComposing) {
        return;
      }
      const max2 = this.effectiveLength;
      if (event.key === "Enter") {
        submitOnEnter(event, this);
      } else if (this.readonly) {
        if (event.key === "Backspace" || event.key === "Delete") {
          event.preventDefault();
        }
      } else if (event.key === "ArrowRight") {
        event.preventDefault();
        if (this.hasSelection) {
          this.setCaretIndex(Math.min(Math.max(this._selectionAnchor, this._activeIndex), max2 - 1));
        } else {
          this._activeIndex = Math.min(this._activeIndex + 1, max2 - 1);
        }
      } else if (event.key === "ArrowLeft") {
        event.preventDefault();
        if (this.hasSelection) {
          this.setCaretIndex(Math.max(Math.min(this._selectionAnchor, this._activeIndex), 0));
        } else {
          this._activeIndex = Math.max(this._activeIndex - 1, 0);
        }
      } else if (event.key === "Backspace") {
        event.preventDefault();
        if (this.hasSelection) {
          const start = Math.min(this._selectionAnchor, this._activeIndex);
          const end = Math.max(this._selectionAnchor, this._activeIndex);
          this.spliceValue(start, end);
          this.setCaretIndex(Math.min(start, max2 - 1));
        } else {
          const idx = this._activeIndex;
          if (idx < this._value.length) {
            this.spliceValue(idx);
          }
          this.setCaretIndex(Math.max(idx - 1, 0));
        }
      } else if (event.key === "Delete") {
        event.preventDefault();
        if (this.hasSelection) {
          const start = Math.min(this._selectionAnchor, this._activeIndex);
          const end = Math.max(this._selectionAnchor, this._activeIndex);
          this.spliceValue(start, end);
          this.setCaretIndex(Math.min(start, max2 - 1));
        } else {
          const idx = this._activeIndex;
          if (idx < this._value.length) {
            this.spliceValue(idx);
          }
        }
      }
    }
    // Remove the characters in [start, end) from _value and sync state.
    spliceValue(start, end = start + 1) {
      const next = this._value.slice(0, start) + this._value.slice(end);
      const oldValue = this._value;
      this._value = next;
      this.setValue(next);
      if (this.input) {
        this.input.value = next;
      }
      this.dispatchEvent(new InputEvent("input", { bubbles: true, composed: true }));
      this.requestUpdate("value", oldValue);
    }
    handlePaste(event) {
      event.preventDefault();
      if (this.readonly) {
        return;
      }
      const text = event.clipboardData?.getData("text/plain") ?? "";
      const filtered = this.filterAndTransform(text);
      if (!filtered) {
        return;
      }
      const idx = this._activeIndex;
      const max2 = this.effectiveLength;
      const slots = Array.from({ length: max2 }, (_2, i10) => this._value[i10] ?? "");
      for (let i10 = 0; i10 < filtered.length && idx + i10 < max2; i10++) {
        slots[idx + i10] = filtered[i10];
      }
      let end = max2 - 1;
      while (end >= 0 && !slots[end]) {
        end--;
      }
      const next = end >= 0 ? slots.slice(0, end + 1).join("") : "";
      const prevLength = this._value.length;
      const oldValue = this._value;
      this._value = next;
      this.setValue(next);
      if (this.input) {
        this.input.value = next;
      }
      this.setCaretIndex(Math.min(idx + filtered.length, max2 - 1));
      this.dispatchEvent(new InputEvent("input", { bubbles: true, composed: true }));
      this.maybeDispatchComplete(next.length === max2 && prevLength < max2);
      this.requestUpdate("value", oldValue);
    }
    handleFocus() {
      this._focused = true;
      this.setCaretIndex(this._pendingClickIndex ?? Math.min(this._value.length, this.effectiveLength - 1));
    }
    // Mirror a native multi-character selection (e.g. Cmd/Ctrl+A) into component state so
    // render() can highlight it and Backspace/Delete can clear the whole range.
    handleSelect() {
      if (!this.input) {
        return;
      }
      const start = this.input.selectionStart ?? 0;
      const end = this.input.selectionEnd ?? start;
      if (end - start > 1) {
        this._selectionAnchor = start;
        this._activeIndex = end;
      } else if (this._selectionAnchor !== -1) {
        this._selectionAnchor = -1;
      }
    }
    handleBlur() {
      this._focused = false;
      this.setCaretIndex(-1);
      if (this._value !== this._lastChangeValue) {
        this._lastChangeValue = this._value;
        this.dispatchEvent(new Event("change", { bubbles: true, composed: true }));
      }
    }
    // Index of the segment under `target`, clamped so clicking past the filled portion lands on
    // the first empty slot rather than the segments array boundary. Null if the click missed every
    // segment (e.g. it landed on the container's padding).
    segmentIndexAt(target) {
      const segment = target.closest('[part~="segment"]');
      if (!segment || !this.shadowRoot) {
        return null;
      }
      const segments = [...this.shadowRoot.querySelectorAll('[part~="segment"]')];
      const index = segments.indexOf(segment);
      return index >= 0 ? Math.min(index, this._value.length) : null;
    }
    handleSegmentsPointerDown(event) {
      if (this.disabled) {
        return;
      }
      this._pendingClickIndex = this.segmentIndexAt(event.target);
    }
    handleSegmentsClick(event) {
      if (this.disabled) {
        return;
      }
      this.input?.focus();
      const index = this.segmentIndexAt(event.target);
      if (index !== null) {
        this.setCaretIndex(index);
      }
      this._pendingClickIndex = null;
    }
    /** Clears the current value and returns focus to the field. */
    clear() {
      this.value = "";
      this.dispatchEvent(new CsClearEvent());
      this.focus();
    }
    /** Focuses the field. */
    focus(options2) {
      this.input?.focus(options2);
    }
    /** Removes focus from the field. */
    blur() {
      this.input?.blur();
    }
    /** Selects all entered characters in the hidden input. */
    select() {
      this.input?.select();
    }
    render() {
      const hasLabelSlot = this.hasSlotController.test("label", "ssrLabel");
      const hasHintSlot = this.hasSlotController.test("hint", "ssrHint");
      const hasLabel = this.label ? true : !!hasLabelSlot;
      const hasHint = this.hint ? true : !!hasHintSlot;
      const chars = [...this._value];
      const parts = this.parsedFormat;
      const activeIndex = this._activeIndex;
      const selected = this.hasSelection ? [Math.min(this._selectionAnchor, activeIndex), Math.max(this._selectionAnchor, activeIndex)] : null;
      let segmentIndex = 0;
      return b2`
      <label
        id="label"
        part="label"
        class=${e7({ label: true, "has-label": hasLabel })}
        for="hidden-input"
        aria-hidden=${hasLabel ? "false" : "true"}
      >
        <slot name="label">${this.label}</slot>
      </label>

      <div
        part="segments"
        class="segments"
        role="group"
        aria-labelledby="label"
        @pointerdown=${this.handleSegmentsPointerDown}
        @click=${this.handleSegmentsClick}
      >
        ${parts.map((part) => {
        if (part.type === "separator") {
          return b2`<span part="segment-literal" class="segment-literal" aria-hidden="true">${part.char}</span>`;
        }
        const i10 = segmentIndex++;
        const char = chars[i10] ?? "";
        const filled = Boolean(char);
        const isSelected = !this.readonly && selected !== null && i10 >= selected[0] && i10 < selected[1];
        const isActive = !this.readonly && selected === null && i10 === activeIndex;
        const masked = filled && this.mask;
        return b2`
            <div
              part="segment"
              class=${e7({
          segment: true,
          "segment--active": isActive,
          "segment--selected": isSelected,
          "segment--filled": filled,
          "segment--masked": masked,
          "segment--mask-hint": !filled && this.withMask
        })}
              aria-hidden="true"
            >
              ${masked ? "" : char} ${isActive && !char ? b2`<span class="caret"></span>` : ""}
            </div>
          `;
      })}

        <input
          id="hidden-input"
          class="hidden-input"
          type="text"
          .value=${l5(this._value)}
          minlength=${this.effectiveLength}
          autocomplete=${this.autocomplete}
          inputmode=${this.type === "numeric" ? "numeric" : "text"}
          aria-describedby="hint"
          ?required=${this.required}
          ?disabled=${this.disabled}
          ?readonly=${this.readonly}
          ?autofocus=${this.autofocus}
          @input=${this.handleInput}
          @keydown=${this.handleKeyDown}
          @paste=${this.handlePaste}
          @focus=${this.handleFocus}
          @blur=${this.handleBlur}
          @select=${this.handleSelect}
        />
      </div>

      <slot
        id="hint"
        part="hint"
        name="hint"
        class=${e7({ hint: true, "has-slotted": hasHint })}
        aria-hidden=${hasHint ? "false" : "true"}
        >${this.hint}</slot
      >
    `;
    }
  };
  CsOtpInput.shadowRootOptions = { ...CornerstoneFormAssociatedElement.shadowRootOptions, delegatesFocus: true };
  CsOtpInput.css = [size_styles_default, form_control_styles_default, otp_input_styles_default];
  __decorateClass([
    e5(".hidden-input")
  ], CsOtpInput.prototype, "input", 2);
  __decorateClass([
    e5(".segments")
  ], CsOtpInput.prototype, "segmentsContainer", 2);
  __decorateClass([
    r5()
  ], CsOtpInput.prototype, "_focused", 2);
  __decorateClass([
    r5()
  ], CsOtpInput.prototype, "_activeIndex", 2);
  __decorateClass([
    r5()
  ], CsOtpInput.prototype, "_selectionAnchor", 2);
  __decorateClass([
    n4({ attribute: "value", reflect: true })
  ], CsOtpInput.prototype, "defaultValue", 2);
  __decorateClass([
    n4({ type: Number, reflect: true })
  ], CsOtpInput.prototype, "length", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsOtpInput.prototype, "appearance", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsOtpInput.prototype, "type", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsOtpInput.prototype, "mask", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsOtpInput.prototype, "case", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsOtpInput.prototype, "size", 2);
  __decorateClass([
    n4()
  ], CsOtpInput.prototype, "label", 2);
  __decorateClass([
    n4()
  ], CsOtpInput.prototype, "hint", 2);
  __decorateClass([
    n4({ attribute: "ssr-label", type: Boolean })
  ], CsOtpInput.prototype, "ssrLabel", 2);
  __decorateClass([
    n4({ attribute: "ssr-hint", type: Boolean })
  ], CsOtpInput.prototype, "ssrHint", 2);
  __decorateClass([
    n4()
  ], CsOtpInput.prototype, "format", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsOtpInput.prototype, "autocomplete", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsOtpInput.prototype, "required", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsOtpInput.prototype, "readonly", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsOtpInput.prototype, "autosubmit", 2);
  __decorateClass([
    n4({ type: Boolean })
  ], CsOtpInput.prototype, "autofocus", 2);
  __decorateClass([
    n4({ type: Boolean, attribute: "with-mask", reflect: true })
  ], CsOtpInput.prototype, "withMask", 2);
  CsOtpInput = __decorateClass([
    customElement("cs-otp-input")
  ], CsOtpInput);
  CsOtpInput.disableWarning?.("change-in-update");

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.7P3XWUWJ.js
  var page_mobile_styles_default = (breakpoint = "768px") => `
  @media screen and (width < ${breakpoint}) {
    [part~='navigation'] {
      display: none;
    }

    :host(:not([disable-navigation-toggle])) slot[name~='navigation-toggle'] {
      display: contents;
    }
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.YXJMIWU2.js
  var page_styles_default = i`
  :host {
    display: block;
    background-color: var(--cs-color-surface-default);
    box-sizing: border-box;
    min-height: 100%;
    --menu-width: auto;
    --main-width: 1fr;
    --aside-width: auto;
    --banner-height: 0px;
    --header-height: 0px;
    --subheader-height: 0px;
    --scroll-margin-top: calc(var(--header-height, 0px) + var(--subheader-height, 0px) + 0.5em);

    --banner-top: var(--banner-height);
    --header-top: var(--header-height);
    --subheader-top: var(--subheader-height);
  }

  slot[name]:not([name='skip-to-content'], [name='navigation-toggle'])::slotted(*) {
    display: flex;
    background-color: var(--cs-color-surface-default);
  }

  ::slotted([slot='banner']) {
    align-items: center;
    justify-content: center;
    gap: var(--cs-space-m);
    padding: var(--cs-space-xs) var(--cs-space-m);
  }

  ::slotted([slot='header']) {
    align-items: center;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: var(--cs-space-m);
    padding: var(--cs-space-m);
    flex: auto;
  }

  ::slotted([slot='subheader']) {
    align-items: center;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: var(--cs-space-m);
    padding: var(--cs-space-xs) var(--cs-space-m);
  }

  ::slotted([slot*='navigation']),
  ::slotted([slot='menu']),
  ::slotted([slot='aside']) {
    flex-direction: column;
    gap: var(--cs-space-m);
    padding: var(--cs-space-m);
  }

  ::slotted([slot='main-header']) {
    align-items: center;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: var(--cs-space-m);
    padding: var(--cs-space-m) var(--cs-space-3xl);
  }

  slot:not([name]) {
    /* See #331 */
    &::slotted(main),
    &::slotted(section) {
      padding: var(--cs-space-3xl);
    }
  }

  ::slotted([slot='main-footer']),
  ::slotted([slot='footer']) {
    align-items: start;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: var(--cs-space-m);
    padding: var(--cs-space-3xl);
  }

  :host([disable-sticky~='banner']) {
    --banner-top: 0px;
  }
  :host([disable-sticky~='header']) {
    --header-top: 0px;
  }
  :host([disable-sticky~='subheader']) {
    --subheader-top: 0px;
  }

  /* Nothing else depends on subheader-height. */
  :host([disable-sticky~='subheader']) {
  }
  :host([disable-sticky~='aside']) [part~='aside'],
  :host([disable-sticky~='menu']) [part~='menu'] {
    height: unset;
    max-height: unset;
  }

  :host([disable-sticky~='banner']) [part~='banner'],
  :host([disable-sticky~='header']) [part~='header'],
  :host([disable-sticky~='subheader']) [part~='subheader'],
  :host([disable-sticky~='aside']) [part~='aside'],
  :host([disable-sticky~='menu']) [part~='menu'] {
    position: static;
    overflow: unset;
    z-index: unset;
  }

  :host([disable-sticky~='aside']) [part~='aside'],
  :host([disable-sticky~='menu']) [part~='menu'] {
    height: auto;
    max-height: auto;
  }

  [part~='page'] {
    min-height: 100dvh;
    display: grid;
    grid-template-rows: repeat(3, minmax(0, auto)) minmax(0, 1fr) minmax(0, auto);
    grid-template-columns: 100%;
    width: 100%;
    grid-template-areas:
      'banner'
      'header'
      'subheader'
      'body'
      'footer';
  }

  /* Grid areas */
  [part~='banner'] {
    grid-area: banner;
  }
  [part~='header'] {
    grid-area: header;
  }
  [part~='subheader'] {
    grid-area: subheader;
  }
  [part~='menu'] {
    grid-area: menu;
  }
  [part~='body'] {
    grid-area: body;
  }
  [part~='main'] {
    grid-area: main;
  }
  [part~='aside'] {
    grid-area: aside;
  }
  [part~='footer'] {
    grid-area: footer;
  }

  /* Z-indexes */
  [part~='banner'],
  [part~='header'],
  [part~='subheader'] {
    position: sticky;
    z-index: 5;
  }
  [part~='banner'] {
    top: 0px;
  }
  [part~='header'] {
    top: var(--banner-top);

    /** Make the header flex so that you don't unexpectedly have the default toggle button appearing above a slotted div because block elements are fun. */
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: space-between;
  }
  [part~='subheader'] {
    top: calc(var(--header-top) + var(--banner-top));
  }
  [part~='body'] {
    display: grid;
    min-height: 100%;
    align-items: start;
    grid-template-columns: minmax(0, var(--menu-width)) minmax(0, var(--main-width)) minmax(0, var(--aside-width));
    grid-template-rows: minmax(0, 1fr);
    grid-template-areas: 'menu main aside';
  }
  [part~='main'] {
    display: grid;
    min-height: 100%;
    grid-template-columns: minmax(0, 1fr);
    grid-template-rows: minmax(0, auto) minmax(0, 1fr) minmax(0, auto);
    grid-template-areas:
      'main-header'
      'main-content'
      'main-footer';
  }
  [part~='main-header'] {
    grid-area: main-header;
  }
  [part~='main-content'] {
    grid-area: main-content;
  }
  [part~='main-footer'] {
    grid-area: main-footer;
  }

  /*
   * Matched on the part, not a class. This rule used to select .skip-to-content, and the element render() emits
   * carries part="skip-to-content" with class="cs-visually-hidden" -- never that class -- so none of this had
   * ever applied, for any consumer. The link still worked, because the visually-hidden utility reveals it on
   * focus, but it revealed as unstyled UA-default link text in the top-left corner, over the page content,
   * instead of the panel described below.
   */
  [part~='skip-to-content'] {
    position: absolute;
    top: var(--cs-space-m);
    left: var(--cs-space-m);
    z-index: 6;
    border-radius: var(--cs-border-radius-m);
    background-color: var(--cs-color-surface-default);
    color: var(--cs-color-text-link);
    text-decoration: none;
    padding: var(--cs-space-s) var(--cs-space-m);
    box-shadow: var(--cs-shadow-l);
    outline: var(--cs-focus-ring);
    outline-offset: var(--cs-focus-ring-offset);
  }

  [part~='menu'],
  [part~='aside'] {
    position: sticky;
    top: calc(var(--banner-top) + var(--header-top) + var(--subheader-top));
    z-index: 4;
    min-height: 0;
    /** Allows the menu / aside to always be 100% of the height of the main content area */
    align-self: stretch;
    max-height: calc(100dvh - var(--header-top) - var(--banner-top) - var(--subheader-top));
    overflow: auto;
  }

  [part~='navigation'] {
    /*
     * min-height, not height. The menu is a scroll container, and this element holds its scrolling content. At
     * height: 100% the box is pinned to the scroller's visible height and a long navigation overflows it instead of
     * growing it, so any padding on this element or on what it slots renders at that edge — mid-scroll — rather
     * than after the last item. min-height keeps a short navigation filling the column, which is what the 1fr row
     * below is for, while letting a long one grow.
     */
    min-height: 100%;
    display: grid;
    grid-template-columns: minmax(0, 1fr);
    grid-template-rows: minmax(0, auto) minmax(0, 1fr) minmax(0, auto);
  }

  [part~='drawer']::part(dialog) {
    background-color: var(--cs-color-surface-default);
  }

  /* Set these on the slot because we don't always control the navigation-toggle since that may be slotted. */
  slot[name~='navigation-toggle'],
  :host([disable-navigation-toggle]) slot[name~='navigation-toggle'] {
    display: none;
  }

  /* Sometimes the media query in the viewport is stubborn in iframes. This is an extra check to make it behave properly. */
  :host(:not([disable-navigation-toggle])[view='mobile']) slot[name~='navigation-toggle'] {
    display: contents;
  }

  [part~='navigation-toggle'] {
    /* Use only a margin-inline-start because the slotted header is expected to have default padding
        so it looks really awkward if this sets a margin-inline-end and the slotted header has a padding-inline-start. */
    margin-inline-start: var(--cs-space-m);
  }
`;

  // node_modules/lit-html/directives/unsafe-html.js
  var e9 = class extends i5 {
    constructor(i10) {
      if (super(i10), this.it = A, i10.type !== t5.CHILD) throw Error(this.constructor.directiveName + "() can only be used in child bindings");
    }
    render(r9) {
      if (r9 === A || null == r9) return this._t = void 0, this.it = r9;
      if (r9 === E) return r9;
      if ("string" != typeof r9) throw Error(this.constructor.directiveName + "() called with a non-string value");
      if (r9 === this.it) return this._t;
      this.it = r9;
      const s7 = [r9];
      return s7.raw = s7, this._t = { _$litType$: this.constructor.resultType, strings: s7, values: [] };
    }
  };
  e9.directiveName = "unsafeHTML", e9.resultType = 1;
  var o12 = e6(e9);

  // node_modules/lit-html/directives/when.js
  function n7(n9, r9, t8) {
    return n9 ? r9(n9) : t8?.(n9);
  }

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.6CMZ3BCW.js
  function toPx(value, element = document.documentElement) {
    if (!Number.isNaN(Number(value))) {
      return Number(value);
    }
    if (!window.CSS || !CSS.registerProperty) {
      if (typeof value === "string" && value.endsWith("px")) {
        return parseFloat(value);
      }
      return Number(value) || 0;
    }
    const resolver = "--cs-length-resolver";
    if (!CSS.registerProperty.toString().includes(resolver)) {
      try {
        CSS.registerProperty({
          name: resolver,
          syntax: "<length>",
          inherits: false,
          initialValue: "0px"
        });
      } catch {
      }
    }
    const previousValue = element.style.getPropertyValue(resolver);
    element.style.setProperty(resolver, value);
    const computedValue = getComputedStyle(element)?.getPropertyValue(resolver);
    element.style.setProperty(resolver, previousValue);
    if (computedValue?.endsWith("px")) {
      return parseFloat(computedValue);
    }
    return Number(computedValue) || 0;
  }
  function toLength(px) {
    return Number.isNaN(Number(px)) ? px : `${px}px`;
  }
  var CsPage = class extends CornerstoneElement {
    constructor() {
      super();
      this.headerResizeObserver = !o5 ? this.slotResizeObserver("header") : null;
      this.subheaderResizeObserver = !o5 ? this.slotResizeObserver("subheader") : null;
      this.bannerResizeObserver = !o5 ? this.slotResizeObserver("banner") : null;
      this.footerResizeObserver = !o5 ? this.slotResizeObserver("footer") : null;
      this.handleNavigationToggle = (e12) => {
        if (this.view === "desktop") {
          this.hideNavigation();
          return;
        }
        const path = e12.composedPath();
        const navigationToggleSlot = this.navigationToggleSlot;
        if (path.find((el) => {
          return el.hasAttribute?.("data-toggle-nav") || el.assignedSlot === navigationToggleSlot || el === navigationToggleSlot;
        })) {
          e12.preventDefault();
          this.toggleNavigation();
        }
      };
      this.view = "desktop";
      this.navOpen = false;
      this.mobileBreakpoint = "768px";
      this.navigationPlacement = "start";
      this.disableNavigationToggle = false;
      this.pageResizeObserver = typeof ResizeObserver !== "undefined" ? new ResizeObserver((entries) => {
        requestAnimationFrame(() => {
          for (const entry of entries) {
            if (entry.contentBoxSize) {
              const contentBoxSize = entry.borderBoxSize[0];
              const pageWidth = contentBoxSize.inlineSize;
              const oldView = this.view;
              if (pageWidth >= toPx(this.mobileBreakpoint)) {
                this.view = "desktop";
              } else {
                this.view = "mobile";
              }
              this.requestUpdate("view", oldView);
            }
          }
        });
      }) : null;
      this.updateNavigationToggleState = (e12) => {
        if (e12) {
          const slotName = e12.target.name;
          if (!["navigation", "navigation-header", "navigation-footer"].includes(slotName)) {
            return;
          }
        }
        const hasCustomToggle = Boolean(this.querySelector(":not([slot='navigation-toggle']) [data-toggle-nav]"));
        const hasNavigationContent = Boolean(this.querySelector('[slot="navigation"]')) || Boolean(this.querySelector('[slot="navigation-header"]')) || Boolean(this.querySelector('[slot="navigation-footer"]'));
        this.disableNavigationToggle = hasCustomToggle || !hasNavigationContent;
      };
      if (!o5) {
        this.addEventListener("click", this.handleNavigationToggle);
      }
    }
    slotResizeObserver(slot) {
      return new ResizeObserver((entries) => {
        requestAnimationFrame(() => {
          for (const entry of entries) {
            if (entry.contentBoxSize) {
              const contentBoxSize = entry.borderBoxSize[0];
              this.style.setProperty(`--${slot}-height`, `${Math.round(contentBoxSize.blockSize)}px`);
            }
          }
        });
      });
    }
    updated(changedProperties) {
      if (changedProperties.has("view")) {
        this.hideNavigation();
      }
      super.updated(changedProperties);
    }
    connectedCallback() {
      super.connectedCallback();
      if (!o5) {
        setTimeout(() => {
          requestAnimationFrame(() => {
            this.pageResizeObserver?.observe(this);
            this.headerResizeObserver?.observe(this.header);
            this.subheaderResizeObserver?.observe(this.subheader);
            this.bannerResizeObserver?.observe(this.banner);
            this.footerResizeObserver?.observe(this.footer);
          });
        });
      }
    }
    /**
     * https://stackoverflow.com/a/26831113
     * This prevents awkward gaps when scrolling the page and the aside / menu don't "fill" the gaps.
     */
    visiblePixelsInViewport(element) {
      if (!element) {
        return null;
      }
      const elementHeight = element.clientHeight;
      const windowHeight = window.innerHeight;
      const rect = element.getBoundingClientRect?.();
      if (!rect) {
        return null;
      }
      const { top, bottom } = rect;
      return Math.max(0, top > 0 ? Math.min(elementHeight, windowHeight - top) : Math.min(bottom, windowHeight));
    }
    firstUpdated(changedProperties) {
      if (!document.getElementById("main-content")) {
        const div = document.createElement("div");
        div.id = "main-content";
        div.slot = "skip-to-content-target";
        this.prepend(div);
      }
      this.shadowRoot.addEventListener("slotchange", this.updateNavigationToggleState);
      this.updateNavigationToggleState();
      super.firstUpdated(changedProperties);
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this.pageResizeObserver?.unobserve(this);
      this.headerResizeObserver?.unobserve(this.header);
      this.subheaderResizeObserver?.unobserve(this.subheader);
      this.footerResizeObserver?.unobserve(this.footer);
      this.bannerResizeObserver?.unobserve(this.banner);
    }
    /**
     * Shows the mobile navigation drawer
     */
    showNavigation() {
      this.navOpen = true;
    }
    /**
     * Hides the mobile navigation drawer
     */
    hideNavigation() {
      this.navOpen = false;
    }
    /**
     * Toggles the mobile navigation drawer
     */
    toggleNavigation() {
      this.navOpen = !this.navOpen;
    }
    render() {
      return b2`
      <a href="#main-content" part="skip-to-content" class="cs-visually-hidden">
        <slot name="skip-to-content">Skip to content</slot>
      </a>

      \x3C!-- unsafeHTML needed for SSR until this is solved: https://github.com/lit/lit/issues/4696 -->
      ${o12(`
        <style id="mobile-styles">
          ${page_mobile_styles_default(toLength(this.mobileBreakpoint))}
        </style>
      `)}

      <div class="base" part="page">
        <div class="banner" part="banner">
          <slot name="banner"></slot>
        </div>
        <div class="header" part="header">
          <slot name="navigation-toggle">
            <cs-button part="navigation-toggle" size="s" appearance="plain" variant="neutral">
              <slot name="navigation-toggle-icon">
                <cs-icon name="menu" part="navigation-toggle-icon" label="Toggle navigation drawer"></cs-icon>
              </slot>
            </cs-button>
          </slot>
          <slot name="header"></slot>
        </div>
        <div class="subheader" part="subheader">
          <slot name="subheader"></slot>
        </div>
        <div class="body" part="body">
          <div class="menu" part="menu">
            <slot name="menu">
              <nav name="navigation" class="navigation" part="navigation navigation-desktop">
                \x3C!-- Add fallback divs so that CSS grid works properly. -->
                <slot name="desktop-navigation-header">
                  ${n7(
        this.view === "desktop",
        () => b2`<slot name="navigation-header"><div></div></slot>`,
        () => b2`<div></div>`
      )}
                </slot>
                <slot name="desktop-navigation">
                  ${n7(
        this.view === "desktop",
        () => b2`<slot name="navigation"><div></div></slot>`,
        () => b2`<div></div>`
      )}
                </slot>
                <slot name="desktop-navigation-footer">
                  ${n7(
        this.view === "desktop",
        () => b2`<slot name="navigation-footer"><div></div></slot>`,
        () => b2`<div></div>`
      )}
                </slot>
              </nav>
            </slot>
          </div>
          <div class="main" part="main">
            <div class="main-header" part="main-header">
              <slot name="main-header"></slot>
            </div>
            <div class="main-content" part="main-content">
              <slot name="skip-to-content-target"></slot>
              <slot></slot>
            </div>
            <div class="main-footer" part="main-footer">
              <slot name="main-footer"></slot>
            </div>
          </div>
          <div class="aside" part="aside">
            <slot name="aside"></slot>
          </div>
        </div>
        <div class="footer" part="footer">
          <slot name="footer"></slot>
        </div>
      </div>
      <cs-drawer
        part="drawer"
        placement=${this.navigationPlacement}
        light-dismiss
        ?open=${l5(this.navOpen)}
        @cs-after-show=${() => this.navOpen = this.navigationDrawer.open}
        @cs-after-hide=${() => this.navOpen = this.navigationDrawer.open}
        exportparts="
          dialog:drawer__dialog,
          header:drawer__header,
          header-actions:drawer__header-actions,
          title:drawer__title,
          close-button:drawer__close-button,
          close-button__button:drawer__close-button__button,
          body:drawer__body,
          footer:drawer__footer
        "
        class="navigation-drawer"
      >
        <slot slot="label" part="navigation-header" name="mobile-navigation-header">
          ${n7(
        this.view === "mobile",
        () => b2`<slot name="navigation-header"><div></div></slot>`,
        () => b2`<div></div>`
      )}
        </slot>
        <slot name="mobile-navigation">
          ${n7(
        this.view === "mobile",
        () => b2`<slot name="navigation"><div></div></slot>`,
        () => b2`<div></div>`
      )}
        </slot>

        <slot slot="footer" name="mobile-navigation-footer">
          ${n7(
        this.view === "mobile",
        () => b2`<slot part="navigation-footer" name="navigation-footer"><div></div></slot>`,
        () => b2`<div></div>`
      )}
        </slot>
      </cs-drawer>
    `;
    }
  };
  CsPage.css = [visually_hidden_styles_default, page_styles_default];
  __decorateClass([
    e5("[part~='header']")
  ], CsPage.prototype, "header", 2);
  __decorateClass([
    e5("[part~='menu']")
  ], CsPage.prototype, "menu", 2);
  __decorateClass([
    e5("[part~='main']")
  ], CsPage.prototype, "main", 2);
  __decorateClass([
    e5("[part~='aside']")
  ], CsPage.prototype, "aside", 2);
  __decorateClass([
    e5("[part~='subheader']")
  ], CsPage.prototype, "subheader", 2);
  __decorateClass([
    e5("[part~='footer']")
  ], CsPage.prototype, "footer", 2);
  __decorateClass([
    e5("[part~='banner']")
  ], CsPage.prototype, "banner", 2);
  __decorateClass([
    e5("[part~='drawer']")
  ], CsPage.prototype, "navigationDrawer", 2);
  __decorateClass([
    e5("slot[name~='navigation-toggle']")
  ], CsPage.prototype, "navigationToggleSlot", 2);
  __decorateClass([
    n4({ attribute: "view", reflect: true })
  ], CsPage.prototype, "view", 2);
  __decorateClass([
    n4({ attribute: "nav-open", reflect: true, type: Boolean })
  ], CsPage.prototype, "navOpen", 2);
  __decorateClass([
    n4({ attribute: "mobile-breakpoint", type: String })
  ], CsPage.prototype, "mobileBreakpoint", 2);
  __decorateClass([
    n4({ attribute: "navigation-placement", reflect: true })
  ], CsPage.prototype, "navigationPlacement", 2);
  __decorateClass([
    n4({ attribute: "disable-navigation-toggle", reflect: true, type: Boolean })
  ], CsPage.prototype, "disableNavigationToggle", 2);
  CsPage = __decorateClass([
    customElement("cs-page")
  ], CsPage);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.766IYUWO.js
  var CsPageChangeEvent = class extends Event {
    constructor(detail) {
      super("cs-page-change", { bubbles: true, cancelable: true, composed: true });
      this.detail = detail;
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.7IICUWUW.js
  var CsAfterPageChangeEvent = class extends Event {
    constructor(detail) {
      super("cs-after-page-change", { bubbles: true, cancelable: false, composed: true });
      this.detail = detail;
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.MC6E45WC.js
  var pagination_styles_default = i`
  @layer cs-component {
    :host {
      display: contents;
    }
  }

  .container {
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    /* Sizing is relative to the current font size, so we use em rather than spacing tokens */
    gap: 1em;
  }

  .summary {
    font-size: 0.875em;
    color: var(--cs-color-text-quiet);
    white-space: nowrap;
  }

  /* Compact layout */
  .label {
    display: inline-flex;
    align-items: center;
    min-height: max(2.16em, 24px);
    padding-inline: 0.75em;
    color: var(--cs-color-text-normal);
    white-space: nowrap;
  }

  .pagination {
    display: flex;
  }

  .pages {
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    gap: 0.25em;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .pages li {
    display: flex;
  }

  .button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    box-sizing: border-box;

    /* Guarantee a minimum 24×24px target (WCAG 2.5.8) while still scaling with font-size. */
    min-width: max(2.16em, 24px);
    min-height: max(2.16em, 24px);
    padding-inline: 0.25em;

    font: inherit;
    font-size: inherit;
    line-height: 1;
    color: var(--cs-color-text-normal);
    text-decoration: none;

    background-color: transparent;
    /* Default (outlined) appearance */
    border: solid var(--cs-border-width-s) var(--cs-color-neutral-border-quiet);
    border-radius: var(--cs-border-radius-m);
    cursor: pointer;
    user-select: none;
    -webkit-user-select: none;
    transition:
      background-color var(--cs-transition-fast),
      border-color var(--cs-transition-fast),
      color var(--cs-transition-fast);
  }

  /* Ellipsis */
  .button.ellipsis {
    color: var(--cs-color-text-quiet);
    position: relative;
  }

  .button.ellipsis:hover,
  .button.ellipsis:focus-visible {
    color: var(--cs-color-text-normal);
  }

  .button:hover {
    background-color: var(--cs-color-neutral-fill-quiet);
  }

  .button:focus {
    outline: none;
  }

  .button:focus-visible {
    outline: var(--cs-focus-ring);
    outline-offset: var(--cs-focus-ring-offset);
  }

  /* Current page */
  .button.current {
    font-weight: var(--cs-font-weight-bold);
    color: var(--cs-color-brand-on-loud);
    background-color: var(--cs-form-control-activated-color);
    /* Read as a solid chip: drop the outlined border so it doesn't double up against the fill. */
    border-color: transparent;
  }

  .button.current:hover {
    background-color: var(--cs-form-control-activated-color);
  }

  /* Disabled (pagination buttons use aria-disabled so they stay focusable) */
  .button[aria-disabled='true'] {
    opacity: 0.5;
    cursor: not-allowed;
  }

  .button[aria-disabled='true']:hover {
    background-color: transparent;
  }

  cs-icon {
    font-size: 0.875em;
  }

  /* Filled */
  :host([appearance='filled']) .button {
    background-color: var(--cs-color-neutral-fill-quiet);
    border-color: transparent;
  }

  :host([appearance='filled']) .button:hover {
    background-color: var(--cs-color-neutral-fill-normal);
  }

  :host([appearance='filled']) .button.current,
  :host([appearance='filled']) .button.current:hover {
    background-color: var(--cs-form-control-activated-color);
  }

  /* Plain */
  :host([appearance='plain']) .button {
    background-color: transparent;
    border-color: transparent;
  }

  :host([appearance='plain']) .button:hover {
    background-color: transparent;
  }

  :host([appearance='plain']) .button.current {
    color: var(--cs-color-brand-on-loud);
    background-color: var(--cs-form-control-activated-color);
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.SUVKJDXO.js
  function range(start, end) {
    const length = end - start + 1;
    return length > 0 ? Array.from({ length }, (_2, i10) => start + i10) : [];
  }
  function getPaginationRange(options2) {
    const totalPages = Math.max(1, Math.trunc(options2.totalPages));
    const page = Math.min(Math.max(1, Math.trunc(options2.page)), totalPages);
    const siblingCount = Math.max(0, Math.trunc(options2.siblingCount));
    const boundaryCount = Math.max(0, Math.trunc(options2.boundaryCount));
    const totalSlots = siblingCount * 2 + boundaryCount * 2 + 3;
    if (totalSlots >= totalPages) {
      return range(1, totalPages).map((value) => ({ type: "page", value }));
    }
    const firstMiddle = boundaryCount + 1;
    const lastMiddle = totalPages - boundaryCount;
    let windowStart = page - siblingCount;
    let windowEnd = page + siblingCount;
    if (windowStart < firstMiddle) {
      windowEnd += firstMiddle - windowStart;
      windowStart = firstMiddle;
    }
    if (windowEnd > lastMiddle) {
      windowStart -= windowEnd - lastMiddle;
      windowEnd = lastMiddle;
    }
    windowStart = Math.max(windowStart, firstMiddle);
    windowEnd = Math.min(windowEnd, lastMiddle);
    let freedSlots = (windowStart > firstMiddle ? 0 : 1) + (windowEnd < lastMiddle ? 0 : 1);
    while (freedSlots > 0) {
      if (windowEnd < lastMiddle) {
        windowEnd++;
      } else if (windowStart > firstMiddle) {
        windowStart--;
      } else {
        break;
      }
      freedSlots--;
    }
    const showStartEllipsis = windowStart > firstMiddle;
    const showEndEllipsis = windowEnd < lastMiddle;
    const items = [];
    range(1, boundaryCount).forEach((value) => items.push({ type: "page", value }));
    if (showStartEllipsis) {
      items.push({ type: "ellipsis", position: "start" });
    }
    range(windowStart, windowEnd).forEach((value) => items.push({ type: "page", value }));
    if (showEndEllipsis) {
      items.push({ type: "ellipsis", position: "end" });
    }
    range(totalPages - boundaryCount + 1, totalPages).forEach((value) => items.push({ type: "page", value }));
    return items;
  }
  var CsPagination = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.localize = new LocalizeController2(this);
      this.total = 0;
      this.pageSize = 10;
      this.page = 1;
      this.siblingCount = 2;
      this.boundaryCount = 1;
      this.withoutNav = false;
      this.withEdges = false;
      this.withSummary = false;
      this.format = "standard";
      this.hrefTemplate = "";
      this.hideSinglePage = false;
      this.label = "";
      this.appearance = "outlined";
      this.disabled = false;
      this.shouldRestoreFocus = false;
    }
    /** The total number of pages, derived from `total` and `pageSize`. */
    get totalPages() {
      if (this.pageSize <= 0) {
        return 1;
      }
      return Math.max(1, Math.ceil(this.total / this.pageSize));
    }
    handleDisabledChange() {
      this.customStates.set("disabled", this.disabled);
    }
    handlePageBoundsChange() {
      const clamped = clamp(Math.trunc(this.page) || 1, 1, this.totalPages);
      if (clamped !== this.page) {
        this.page = clamped;
      }
    }
    /** Resolves the URL for a given page when `hrefTemplate` is set, supporting both the string and function forms. */
    getHref(page) {
      if (!this.hrefTemplate) {
        return void 0;
      }
      if (typeof this.hrefTemplate === "function") {
        return this.hrefTemplate(page);
      }
      return this.hrefTemplate.split("{page}").join(String(page));
    }
    /**
     * Requests a change to the given page. Emits a cancelable `cs-page-change`; if not canceled, updates `page`,
     * restores focus to the new current page, emits `cs-after-page-change`, and announces the change.
     */
    async requestPage(page, restoreFocus) {
      const target = clamp(page, 1, this.totalPages);
      if (this.disabled || target === this.page) {
        return;
      }
      const beforeEvent = new CsPageChangeEvent({ page: target, pageSize: this.pageSize });
      this.dispatchEvent(beforeEvent);
      if (beforeEvent.defaultPrevented) {
        return;
      }
      this.shouldRestoreFocus = restoreFocus;
      this.page = target;
      await this.updateComplete;
      this.dispatchEvent(new CsAfterPageChangeEvent({ page: this.page, pageSize: this.pageSize }));
      this.announcePage();
    }
    /** Moves focus to the current page item after an in-component activation, so focus never falls back to the body. */
    restoreFocusToCurrentPage() {
      const current = this.shadowRoot?.querySelector('[part~="page-current"]');
      const active = getDeepestActiveElement();
      if (current && active && this.shadowRoot?.contains(active)) {
        current.focus();
      }
    }
    /** Announces the current page to assistive technology via the shared light-DOM live region. */
    announcePage() {
      announce(this.localize.term("pageXOfY", this.page, this.totalPages), "polite");
    }
    updated() {
      if (this.shouldRestoreFocus) {
        this.shouldRestoreFocus = false;
        this.restoreFocusToCurrentPage();
      }
    }
    /** Renders a navigation control (first/previous/next/last). */
    renderNavButton(options2) {
      const { part, targetPage, enabled, label, icon, slotName } = options2;
      const isDisabled = this.disabled || !enabled;
      const href = this.getHref(targetPage);
      if (href !== void 0) {
        return b2`
        <li role="listitem">
          <a
            part="button ${part}"
            class="button nav-button"
            href=${o8(isDisabled ? void 0 : href)}
            aria-label=${label}
            aria-disabled=${isDisabled ? "true" : "false"}
          >
            <slot name=${slotName}><cs-icon library="system" name=${icon}></cs-icon></slot>
          </a>
        </li>
      `;
      }
      return b2`
      <li role="listitem">
        <button
          part="button ${part}"
          class="button nav-button"
          type="button"
          aria-label=${label}
          aria-disabled=${isDisabled ? "true" : "false"}
          @click=${isDisabled ? null : () => this.requestPage(targetPage, true)}
        >
          <slot name=${slotName}><cs-icon library="system" name=${icon}></cs-icon></slot>
        </button>
      </li>
    `;
    }
    /** Renders a single numbered page item. The visible number is the accessible name — no redundant aria-label. */
    renderPage(page) {
      const isCurrent = page === this.page;
      const href = this.getHref(page);
      const label = this.localize.number(page);
      const part = `button page${isCurrent ? " page-current" : ""}`;
      if (href !== void 0) {
        return b2`
        <li role="listitem">
          <a
            part=${part}
            class=${e7({ button: true, page: true, current: isCurrent })}
            href=${o8(isCurrent || this.disabled ? void 0 : href)}
            aria-current=${o8(isCurrent ? "page" : void 0)}
            aria-disabled=${o8(this.disabled ? "true" : void 0)}
            >${label}</a
          >
        </li>
      `;
      }
      return b2`
      <li role="listitem">
        <button
          part=${part}
          class=${e7({ button: true, page: true, current: isCurrent })}
          type="button"
          aria-current=${o8(isCurrent ? "page" : void 0)}
          aria-disabled=${o8(this.disabled ? "true" : void 0)}
          @click=${this.disabled || isCurrent ? null : () => this.requestPage(page, true)}
        >
          ${label}
        </button>
      </li>
    `;
    }
    /**
     * Renders an ellipsis for a run of collapsed pages. Clicking it jumps a fixed number of pages toward that side, so a
     * `start` ellipsis jumps backward and an `end` ellipsis jumps forward. `data-ellipsis` lets the styles collapse a
     * second ellipsis when space is tight.
     */
    renderEllipsis(position, index) {
      const jump = CsPagination.jumpDistance;
      const isStart = position === "start";
      const targetPage = clamp(isStart ? this.page - jump : this.page + jump, 1, this.totalPages);
      const label = this.localize.term(isStart ? "jumpBackwardX" : "jumpForwardX", jump);
      const href = this.getHref(targetPage);
      const content = b2`
      <cs-icon class="ellipsis-default" library="system" name="more_horiz" label=${label}></cs-icon>
    `;
      if (href !== void 0) {
        return b2`
        <li role="listitem">
          <a
            part="ellipsis"
            class="button ellipsis"
            data-ellipsis=${index}
            href=${o8(this.disabled ? void 0 : href)}
            aria-label=${label}
            aria-disabled=${o8(this.disabled ? "true" : void 0)}
          >
            ${content}
          </a>
        </li>
      `;
      }
      return b2`
      <li role="listitem">
        <button
          part="ellipsis"
          class="button ellipsis"
          data-ellipsis=${index}
          type="button"
          aria-label=${label}
          aria-disabled=${o8(this.disabled ? "true" : void 0)}
          @click=${this.disabled ? null : () => this.requestPage(targetPage, true)}
        >
          ${content}
        </button>
      </li>
    `;
    }
    render() {
      const totalPages = this.totalPages;
      if (this.hideSinglePage && totalPages <= 1) {
        return b2``;
      }
      const isRtl = this.localize.dir() === "rtl";
      const onFirstPage = this.page <= 1;
      const onLastPage = this.page >= totalPages;
      if (this.format === "compact") {
        return b2`
        <div class="container">
          ${this.renderSummary()}
          <nav part="pagination" class="pagination" aria-label=${this.label || this.localize.term("pagination")}>
            <ul part="pages" class="pages" role="list">
              ${this.renderNavButton({
          part: "previous-button",
          targetPage: this.page - 1,
          enabled: !onFirstPage,
          label: this.localize.term("previousPage"),
          icon: isRtl ? "keyboard_arrow_right" : "keyboard_arrow_left",
          slotName: "previous-icon"
        })}
              <li role="listitem">
                <span part="label" class="label" aria-current="page">
                  ${this.localize.term("compactPageXOfY", this.page, totalPages)}
                </span>
              </li>
              ${this.renderNavButton({
          part: "next-button",
          targetPage: this.page + 1,
          enabled: !onLastPage,
          label: this.localize.term("nextPage"),
          icon: isRtl ? "keyboard_arrow_left" : "keyboard_arrow_right",
          slotName: "next-icon"
        })}
            </ul>
          </nav>
        </div>
      `;
      }
      const items = getPaginationRange({
        page: this.page,
        totalPages,
        siblingCount: this.siblingCount,
        boundaryCount: this.boundaryCount
      });
      let ellipsisCount = 0;
      return b2`
      <div class="container">
        ${this.renderSummary()}
        <nav part="pagination" class="pagination" aria-label=${this.label || this.localize.term("pagination")}>
          <ul part="pages" class="pages" role="list">
            ${this.withEdges ? this.renderNavButton({
        part: "first-button",
        targetPage: 1,
        enabled: !onFirstPage,
        label: this.localize.term("firstPage"),
        icon: isRtl ? "keyboard_double_arrow_right" : "keyboard_double_arrow_left",
        slotName: "first-icon"
      }) : ""}
            ${this.withoutNav ? "" : this.renderNavButton({
        part: "previous-button",
        targetPage: this.page - 1,
        enabled: !onFirstPage,
        label: this.localize.term("previousPage"),
        icon: isRtl ? "keyboard_arrow_right" : "keyboard_arrow_left",
        slotName: "previous-icon"
      })}
            ${items.map((item) => {
        if (item.type === "ellipsis") {
          ellipsisCount++;
          return this.renderEllipsis(item.position, ellipsisCount);
        }
        return this.renderPage(item.value);
      })}
            ${this.withoutNav ? "" : this.renderNavButton({
        part: "next-button",
        targetPage: this.page + 1,
        enabled: !onLastPage,
        label: this.localize.term("nextPage"),
        icon: isRtl ? "keyboard_arrow_left" : "keyboard_arrow_right",
        slotName: "next-icon"
      })}
            ${this.withEdges ? this.renderNavButton({
        part: "last-button",
        targetPage: totalPages,
        enabled: !onLastPage,
        label: this.localize.term("lastPage"),
        icon: isRtl ? "keyboard_double_arrow_left" : "keyboard_double_arrow_right",
        slotName: "last-icon"
      }) : ""}
          </ul>
        </nav>
      </div>
    `;
    }
    /** Renders the optional "1–10 of 237" summary. */
    renderSummary() {
      if (!this.withSummary) {
        return "";
      }
      const start = this.total === 0 ? 0 : (this.page - 1) * this.pageSize + 1;
      const end = Math.min(this.page * this.pageSize, this.total);
      return b2`
      <span part="summary" class="summary"> ${this.localize.term("showingXtoYofZ", start, end, this.total)} </span>
    `;
    }
  };
  CsPagination.css = pagination_styles_default;
  CsPagination.jumpDistance = 5;
  __decorateClass([
    n4({ type: Number })
  ], CsPagination.prototype, "total", 2);
  __decorateClass([
    n4({ attribute: "page-size", type: Number })
  ], CsPagination.prototype, "pageSize", 2);
  __decorateClass([
    n4({ type: Number, reflect: true })
  ], CsPagination.prototype, "page", 2);
  __decorateClass([
    n4({ attribute: "sibling-count", type: Number })
  ], CsPagination.prototype, "siblingCount", 2);
  __decorateClass([
    n4({ attribute: "boundary-count", type: Number })
  ], CsPagination.prototype, "boundaryCount", 2);
  __decorateClass([
    n4({ attribute: "without-nav", type: Boolean })
  ], CsPagination.prototype, "withoutNav", 2);
  __decorateClass([
    n4({ attribute: "with-edges", type: Boolean })
  ], CsPagination.prototype, "withEdges", 2);
  __decorateClass([
    n4({ attribute: "with-summary", type: Boolean })
  ], CsPagination.prototype, "withSummary", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsPagination.prototype, "format", 2);
  __decorateClass([
    n4({ attribute: "href-template" })
  ], CsPagination.prototype, "hrefTemplate", 2);
  __decorateClass([
    n4({ attribute: "hide-single-page", type: Boolean })
  ], CsPagination.prototype, "hideSinglePage", 2);
  __decorateClass([
    n4()
  ], CsPagination.prototype, "label", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsPagination.prototype, "appearance", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsPagination.prototype, "disabled", 2);
  __decorateClass([
    r5()
  ], CsPagination.prototype, "shouldRestoreFocus", 2);
  __decorateClass([
    watch("disabled", { waitUntilFirstUpdate: true })
  ], CsPagination.prototype, "handleDisabledChange", 1);
  __decorateClass([
    watch("page"),
    watch("total"),
    watch("pageSize")
  ], CsPagination.prototype, "handlePageBoundsChange", 1);
  CsPagination = __decorateClass([
    customElement("cs-pagination")
  ], CsPagination);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.QY64BVLX.js
  var popover_styles_default = i`
  :host {
    --arrow-size: 0.375rem;
    --max-width: 25rem;
    --show-duration: var(--cs-transition-fast);
    --hide-duration: var(--cs-transition-fast);

    display: contents;

    /** Defaults for inherited CSS properties */
    font-size: var(--cs-font-size-m);
    line-height: var(--cs-line-height-normal);
    text-align: start;
    white-space: normal;
  }

  /* The native dialog element */
  .dialog {
    display: none;
    position: fixed;
    inset: 0;
    width: 100%;
    height: 100%;
    margin: 0;
    padding: 0;
    border: none;
    background: transparent;
    overflow: visible;
    pointer-events: none;

    &:focus {
      outline: none;
    }

    &[open] {
      display: block;
    }
  }

  /* The <cs-popup> element */
  .popover {
    --arrow-size: inherit;
    --popup-border-width: var(--cs-panel-border-width);
    --show-duration: inherit;
    --hide-duration: inherit;

    pointer-events: auto;

    /* Inset box-shadow, not a border: Safari seams a clip-path edge that runs along a border. */
    &::part(arrow) {
      background-color: var(--cs-color-surface-default);
      border: none;
      box-shadow: inset calc(-1 * var(--cs-panel-border-width)) calc(-1 * var(--cs-panel-border-width)) 0 0
        var(--cs-color-surface-border);
    }
  }

  .popover[placement^='top']::part(popup) {
    transform-origin: bottom;
  }

  .popover[placement^='bottom']::part(popup) {
    transform-origin: top;
  }

  .popover[placement^='left']::part(popup) {
    transform-origin: right;
  }

  .popover[placement^='right']::part(popup) {
    transform-origin: left;
  }

  /* Body */
  .body {
    display: flex;
    flex-direction: column;
    width: auto;
    max-width: min(var(--max-width), 100vw);
    padding: var(--cs-space-l);
    background-color: var(--cs-color-surface-default);
    border: var(--cs-panel-border-width) solid var(--cs-color-surface-border);
    border-radius: var(--cs-panel-border-radius);
    border-style: var(--cs-panel-border-style);
    box-shadow: var(--cs-shadow-l);
    color: var(--cs-color-text-normal);
    user-select: none;
    -webkit-user-select: none;
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.LLZ4U73I.js
  var openPopovers = /* @__PURE__ */ new Set();
  var CsPopover = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.anchor = null;
      this.placement = "top";
      this.open = false;
      this.distance = 8;
      this.skidding = 0;
      this.for = null;
      this.withoutArrow = false;
      this.eventController = new AbortController();
      this.handleAnchorClick = () => {
        this.open = !this.open;
      };
      this.handleBodyClick = (event) => {
        const target = event.target;
        const button = target.closest('[data-popover="close"]');
        if (button) {
          event.stopPropagation();
          this.open = false;
        }
      };
      this.handleDocumentKeyDown = (event) => {
        if (event.key === "Escape" && this.open && isTopDismissible(this)) {
          event.preventDefault();
          event.stopPropagation();
          this.open = false;
          const anchor = this.anchor;
          if (anchor && typeof anchor.focus === "function") {
            anchor.focus({ preventScroll: true });
          }
        }
      };
      this.handleDocumentClick = (event) => {
        if (this.anchor && event.composedPath().includes(this.anchor)) {
          return;
        }
        if (!event.composedPath().includes(this)) {
          this.open = false;
        }
      };
    }
    connectedCallback() {
      super.connectedCallback();
      if (!this.id) {
        this.id = uniqueId("cs-popover-");
      }
      if (this.eventController.signal.aborted) {
        this.eventController = new AbortController();
      }
      if (this.for && this.anchor) {
        this.anchor = null;
        this.handleForChange();
      }
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      document.removeEventListener("keydown", this.handleDocumentKeyDown);
      unregisterDismissible(this);
      this.eventController.abort();
    }
    firstUpdated(changedProperties) {
      super.firstUpdated(changedProperties);
      if (this.open) {
        this.dialog.show();
        this.popup.active = true;
        this.popup.reposition();
      }
    }
    updated(changedProperties) {
      if (changedProperties.has("open")) {
        this.customStates.set("open", this.open);
      }
    }
    async handleOpenChange() {
      if (this.open) {
        const csShowEvent = new CsShowEvent();
        this.dispatchEvent(csShowEvent);
        if (csShowEvent.defaultPrevented) {
          this.open = false;
          return;
        }
        openPopovers.forEach((popover) => popover.open = false);
        document.addEventListener("keydown", this.handleDocumentKeyDown, { signal: this.eventController.signal });
        document.addEventListener("click", this.handleDocumentClick, { signal: this.eventController.signal });
        this.dialog.setAttribute("open", "");
        this.popup.active = true;
        openPopovers.add(this);
        registerDismissible(this);
        requestAnimationFrame(() => {
          const elementToFocus = this.querySelector("[autofocus]");
          if (elementToFocus && typeof elementToFocus.focus === "function") {
            elementToFocus.focus({ preventScroll: true });
          } else {
            this.dialog.focus({ preventScroll: true });
          }
        });
        await animateWithClass(this.popup.popup, "show-with-scale");
        this.popup.reposition();
        this.dispatchEvent(new CsAfterShowEvent());
      } else {
        const csHideEvent = new CsHideEvent();
        this.dispatchEvent(csHideEvent);
        if (csHideEvent.defaultPrevented) {
          this.open = true;
          return;
        }
        document.removeEventListener("keydown", this.handleDocumentKeyDown);
        document.removeEventListener("click", this.handleDocumentClick);
        openPopovers.delete(this);
        unregisterDismissible(this);
        await animateWithClass(this.popup.popup, "hide-with-scale");
        this.popup.active = false;
        this.dialog.close();
        this.dispatchEvent(new CsAfterHideEvent());
      }
    }
    handleForChange() {
      const rootNode = this.getRootNode();
      if (!rootNode) {
        return;
      }
      const newAnchor = this.for ? rootNode.getElementById(this.for) : null;
      const oldAnchor = this.anchor;
      if (newAnchor === oldAnchor) {
        return;
      }
      const { signal } = this.eventController;
      if (newAnchor) {
        newAnchor.addEventListener("click", this.handleAnchorClick, { signal });
      }
      if (oldAnchor) {
        oldAnchor.removeEventListener("click", this.handleAnchorClick);
      }
      this.anchor = newAnchor;
      if (this.for && !newAnchor) {
        console.warn(
          `A popover was assigned to an element with an ID of "${this.for}" but the element could not be found.`,
          this
        );
      }
    }
    async handleOptionsChange() {
      if (this.hasUpdated) {
        await this.updateComplete;
        this.popup.reposition();
      }
    }
    /** Shows the popover. */
    async show() {
      if (this.open) {
        return void 0;
      }
      this.open = true;
      return waitForEvent(this, "cs-after-show");
    }
    /** Hides the popover. */
    async hide() {
      if (!this.open) {
        return void 0;
      }
      this.open = false;
      return waitForEvent(this, "cs-after-hide");
    }
    render() {
      return b2`
      <dialog part="dialog" class="dialog">
        <cs-popup
          part="popup"
          exportparts="
            popup:popup__popup,
            arrow:popup__arrow
          "
          class=${e7({
        popover: true,
        "popover-open": this.open
      })}
          placement=${this.placement}
          distance=${this.distance}
          skidding=${this.skidding}
          flip
          shift
          shift-padding="8"
          ?arrow=${!this.withoutArrow}
          .anchor=${this.anchor}
        >
          <div part="body" class="body" @click=${this.handleBodyClick}>
            <slot></slot>
          </div>
        </cs-popup>
      </dialog>
    `;
    }
  };
  CsPopover.css = popover_styles_default;
  CsPopover.dependencies = { "cs-popup": CsPopup };
  __decorateClass([
    e5("dialog")
  ], CsPopover.prototype, "dialog", 2);
  __decorateClass([
    e5(".body")
  ], CsPopover.prototype, "body", 2);
  __decorateClass([
    e5("cs-popup")
  ], CsPopover.prototype, "popup", 2);
  __decorateClass([
    r5()
  ], CsPopover.prototype, "anchor", 2);
  __decorateClass([
    n4()
  ], CsPopover.prototype, "placement", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsPopover.prototype, "open", 2);
  __decorateClass([
    n4({ type: Number })
  ], CsPopover.prototype, "distance", 2);
  __decorateClass([
    n4({ type: Number })
  ], CsPopover.prototype, "skidding", 2);
  __decorateClass([
    n4()
  ], CsPopover.prototype, "for", 2);
  __decorateClass([
    n4({ attribute: "without-arrow", type: Boolean, reflect: true })
  ], CsPopover.prototype, "withoutArrow", 2);
  __decorateClass([
    watch("open", { waitUntilFirstUpdate: true })
  ], CsPopover.prototype, "handleOpenChange", 1);
  __decorateClass([
    watch("for")
  ], CsPopover.prototype, "handleForChange", 1);
  __decorateClass([
    watch(["distance", "placement", "skidding"])
  ], CsPopover.prototype, "handleOptionsChange", 1);
  CsPopover = __decorateClass([
    customElement("cs-popover")
  ], CsPopover);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.WL5EMPEV.js
  var progress_bar_styles_default = i`
  :host {
    --track-height: 1rem;
    --track-color: var(--cs-color-neutral-fill-normal);
    --indicator-color: var(--cs-color-brand-fill-loud);

    display: flex;
  }

  .progress-bar {
    flex: 1 1 auto;
    display: flex;
    position: relative;
    overflow: hidden;
    height: var(--track-height);
    border-radius: var(--cs-border-radius-pill);
    background-color: var(--track-color);
    color: var(--cs-color-brand-on-loud);
    font-size: var(--cs-font-size-s);
  }

  .indicator {
    width: var(--percentage);
    display: flex;
    align-items: center;
    justify-content: center;
    background-color: var(--indicator-color);
    text-align: center;
    white-space: nowrap;
    overflow: hidden;
    line-height: 1;
    font-weight: var(--cs-font-weight-semibold);
    transition: all var(--cs-transition-slow, 200ms) var(--cs-transition-easing, ease);
    user-select: none;
    -webkit-user-select: none;
  }

  /* Indeterminate */
  :host([indeterminate]) .indicator {
    position: absolute;
    inset-block: 0;
    inline-size: 50%;
    animation: cs-progress-indeterminate 2.5s infinite cubic-bezier(0.37, 0, 0.63, 1);
  }

  @media (forced-colors: active) {
    .progress-bar {
      outline: solid 1px SelectedItem;
      background-color: var(--cs-color-surface-default);
    }

    .indicator {
      outline: solid 1px SelectedItem;
      background-color: SelectedItem;
    }
  }

  @keyframes cs-progress-indeterminate {
    0% {
      inset-inline-start: -50%;
    }

    75%,
    100% {
      inset-inline-start: 100%;
    }
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.QX4CQ5D3.js
  var CsProgressBar = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.localize = new LocalizeController2(this);
      this.value = 0;
      this.indeterminate = false;
      this.label = "";
    }
    willUpdate(changedProperties) {
      if (this.style == null) {
        this.setStyleProperty("--percentage", `${clamp(this.value, 0, 100)}%`);
      }
      super.willUpdate(changedProperties);
    }
    updated(changedProperties) {
      if (changedProperties.has("value")) {
        requestAnimationFrame(() => {
          this.style.setProperty("--percentage", `${clamp(this.value, 0, 100)}%`);
        });
      }
      super.updated(changedProperties);
    }
    render() {
      return b2`
      <div
        part="progress-bar"
        class="progress-bar"
        role="progressbar"
        title=${o8(this.title)}
        aria-label=${this.label.length > 0 ? this.label : this.localize.term("progress")}
        aria-describedby="label"
        aria-valuemin="0"
        aria-valuemax="100"
        aria-valuenow=${this.indeterminate ? "0" : this.value}
      >
        <div part="indicator" class="indicator">
          ${!this.indeterminate ? b2` <slot id="label" part="label" class="label"></slot> ` : ""}
        </div>
      </div>
    `;
    }
  };
  CsProgressBar.css = progress_bar_styles_default;
  __decorateClass([
    n4({ type: Number, reflect: true })
  ], CsProgressBar.prototype, "value", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsProgressBar.prototype, "indeterminate", 2);
  __decorateClass([
    n4()
  ], CsProgressBar.prototype, "label", 2);
  CsProgressBar = __decorateClass([
    customElement("cs-progress-bar")
  ], CsProgressBar);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.YWCSCVF3.js
  var progress_ring_styles_default = i`
  :host {
    --size: 8rem;
    --track-width: 0.25em; /* avoid using rems here */
    --track-color: var(--cs-color-neutral-fill-normal);
    --indicator-width: var(--track-width);
    --indicator-color: var(--cs-color-brand-fill-loud);
    --indicator-transition-duration: 0.35s;

    display: inline-flex;
  }

  .progress-ring {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    position: relative;
  }

  .image {
    width: var(--size);
    height: var(--size);
    rotate: -90deg;
    transform-origin: 50% 50%;
  }

  .track,
  .indicator {
    --radius: calc(var(--size) / 2 - max(var(--track-width), var(--indicator-width)) * 0.5);
    --circumference: calc(var(--radius) * 2 * 3.141592654);

    fill: none;
    r: var(--radius);
    cx: calc(var(--size) / 2);
    cy: calc(var(--size) / 2);
  }

  .track {
    stroke: var(--track-color);
    stroke-width: var(--track-width);
  }

  .indicator {
    stroke: var(--indicator-color);
    stroke-width: var(--indicator-width);
    stroke-linecap: round;
    transition-property: stroke-dashoffset;
    transition-duration: var(--indicator-transition-duration);
    stroke-dasharray: var(--circumference) var(--circumference);
    stroke-dashoffset: calc(var(--circumference) - var(--percentage) * var(--circumference));
  }

  .label {
    display: flex;
    align-items: center;
    justify-content: center;
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    text-align: center;
    user-select: none;
    -webkit-user-select: none;
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.INZSHPL2.js
  var CsProgressRing = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.localize = new LocalizeController2(this);
      this.value = 0;
      this.label = "";
    }
    updated(changedProperties) {
      super.updated(changedProperties);
      if (changedProperties.has("value")) {
        const radius = parseFloat(getComputedStyle(this.indicator).getPropertyValue("r"));
        const circumference = 2 * Math.PI * radius;
        const offset3 = circumference - this.value / 100 * circumference;
        this.indicatorOffset = `${offset3}px`;
      }
    }
    render() {
      return b2`
      <div
        part="progress-ring"
        class="progress-ring"
        role="progressbar"
        aria-label=${this.label.length > 0 ? this.label : this.localize.term("progress")}
        aria-describedby="label"
        aria-valuemin="0"
        aria-valuemax="100"
        aria-valuenow="${this.value}"
        style=${o7({ "--percentage": this.value / 100 })}
      >
        <svg class="image">
          <circle part="track" class="track"></circle>
          <circle
            part="indicator"
            class="indicator"
            style=${o7({ "stroke-dashoffset": this.indicatorOffset })}
          ></circle>
        </svg>

        <slot id="label" part="label" class="label"></slot>
      </div>
    `;
    }
  };
  CsProgressRing.css = progress_ring_styles_default;
  __decorateClass([
    e5(".indicator")
  ], CsProgressRing.prototype, "indicator", 2);
  __decorateClass([
    r5()
  ], CsProgressRing.prototype, "indicatorOffset", 2);
  __decorateClass([
    n4({ type: Number, reflect: true })
  ], CsProgressRing.prototype, "value", 2);
  __decorateClass([
    n4()
  ], CsProgressRing.prototype, "label", 2);
  CsProgressRing = __decorateClass([
    customElement("cs-progress-ring")
  ], CsProgressRing);
  CsProgressRing.disableWarning?.("change-in-update");

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.DL2M7GSC.js
  var qr_code_styles_default = i`
  :host {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    aspect-ratio: 1;
  }

  canvas {
    width: 100%;
    height: 100%;
    /* We force a near-instant transition so we can listen for transitionend when the color changes */
    transition: color 1ms;
  }

  span {
    /* We force a near-instant transition so we can listen for transitionend when the color changes */
    transition: color 1ms;
  }
`;

  // node_modules/@konnorr/qr-creator/src/qr-creator.js
  var qrCodeGenerator = function(_options, _element, _callback) {
  };
  var QrCreator = class {
    /**
     * @param {Partial<Settings>} config
     * @param {HTMLElement} $element
     * @param {() => void} [callback]
     */
    static render(config, $element, callback) {
      qrCodeGenerator(config, $element, callback);
    }
  };
  (function(vendor_qrcode) {
    function createQRCode(text, level, version, quiet) {
      var vqr = vendor_qrcode(version, level);
      vqr.addData(text);
      vqr.make();
      quiet = quiet || 0;
      var qrModuleCount = vqr.getModuleCount();
      var quietModuleCount = vqr.getModuleCount() + 2 * quiet;
      function isDark(row, col) {
        row -= quiet;
        col -= quiet;
        if (row < 0 || row >= qrModuleCount || col < 0 || col >= qrModuleCount) {
          return false;
        }
        return vqr.isDark(row, col);
      }
      return {
        text,
        level,
        version,
        moduleCount: quietModuleCount,
        isDark
      };
    }
    function createMinQRCode(text, level, minVersion, maxVersion, quiet) {
      minVersion = /** @type {VERSION_NUM} */
      Math.max(1, minVersion || 1);
      maxVersion = /** @type {VERSION_NUM} */
      Math.min(40, maxVersion || 40);
      for (var version = minVersion; version <= maxVersion; version += 1) {
        try {
          return createQRCode(text, level, version, quiet);
        } catch (err) {
        }
      }
      return void 0;
    }
    function drawBackground(_qr, context, settings) {
      if (settings.background) {
        context.fillStyle = settings.background;
        context.fillRect(settings.left, settings.top, settings.size, settings.size);
      }
    }
    function drawModuleRoundedDark(ctx, l6, t8, r9, b3, rad, nw, ne, se, sw) {
      if (nw) {
        ctx.moveTo(l6 + rad, t8);
      } else {
        ctx.moveTo(l6, t8);
      }
      function lal(b4, x0, y0, x1, y1, r0, r1) {
        if (b4) {
          ctx.lineTo(x0 + r0, y0 + r1);
          ctx.arcTo(x0, y0, x1, y1, rad);
        } else {
          ctx.lineTo(x0, y0);
        }
      }
      lal(ne, r9, t8, r9, b3, -rad, 0);
      lal(se, r9, b3, l6, b3, 0, -rad);
      lal(sw, l6, b3, l6, t8, rad, 0);
      lal(nw, l6, t8, r9, t8, 0, rad);
    }
    function drawModuleRoundendLight(ctx, l6, t8, r9, b3, rad, nw, ne, se, sw) {
      function mlla(x2, y3, r0, r1) {
        ctx.moveTo(x2 + r0, y3);
        ctx.lineTo(x2, y3);
        ctx.lineTo(x2, y3 + r1);
        ctx.arcTo(x2, y3, x2 + r0, y3, rad);
      }
      if (nw) mlla(l6, t8, rad, rad);
      if (ne) mlla(r9, t8, -rad, rad);
      if (se) mlla(r9, b3, -rad, -rad);
      if (sw) mlla(l6, b3, rad, -rad);
    }
    function drawModuleRounded(qr, context, settings, left, top, width, row, col) {
      var isDark = qr.isDark, right = left + width, bottom = top + width, rowT = row - 1, rowB = row + 1, colL = col - 1, colR = col + 1, radius = Math.floor(Math.min(0.5, Math.max(0, settings.radius)) * width), center = isDark(row, col), northwest = isDark(rowT, colL), north = isDark(rowT, col), northeast = isDark(rowT, colR), east = isDark(row, colR), southeast = isDark(rowB, colR), south = isDark(rowB, col), southwest = isDark(rowB, colL), west = isDark(row, colL);
      left = Math.round(left);
      top = Math.round(top);
      right = Math.round(right);
      bottom = Math.round(bottom);
      if (center) {
        drawModuleRoundedDark(context, left, top, right, bottom, radius, !north && !west, !north && !east, !south && !east, !south && !west);
      } else {
        drawModuleRoundendLight(context, left, top, right, bottom, radius, north && west && northwest, north && east && northeast, south && east && southeast, south && west && southwest);
      }
    }
    function drawModules(qr, context, settings, corners) {
      var moduleCount = qr.moduleCount;
      var moduleSize = settings.size / moduleCount;
      var row = 0;
      var col = 0;
      context.beginPath();
      const cornerOffset = 7 + settings.quiet;
      for (row = 0; row < moduleCount; row += 1) {
        for (col = 0; col < moduleCount; col += 1) {
          const isCorner = col < cornerOffset && row < cornerOffset || col >= moduleCount - cornerOffset && row < cornerOffset || col < cornerOffset && row >= moduleCount - cornerOffset;
          if (isCorner !== corners) {
            continue;
          }
          var l6 = settings.left + col * moduleSize;
          var t8 = settings.top + row * moduleSize;
          var w2 = moduleSize;
          drawModuleRounded(qr, context, settings, l6, t8, w2, row, col);
        }
      }
      setFill(context, settings, corners);
      context.fill();
    }
    function setFill(context, settings, corners) {
      const fill = corners ? settings.cornerFill || settings.fill : settings.fill;
      if (typeof fill === "string") {
        context.fillStyle = fill;
        return;
      }
      const type = fill["type"];
      const position = fill["position"];
      const colorStops = fill["colorStops"];
      let gradient;
      if (type === "linear-gradient") {
        const absolutePosition = (
          /** @type {Parameters<CanvasRenderingContext2D["createLinearGradient"]>} */
          position.slice(0, 4).map((coordinate) => Math.round(coordinate * settings.size))
        );
        gradient = context.createLinearGradient.apply(context, absolutePosition);
      } else if (type === "radial-gradient") {
        const absolutePosition = (
          /** @type {Parameters<CanvasRenderingContext2D["createRadialGradient"]>} */
          position.slice(0, 6).map((coordinate) => Math.round(coordinate * settings.size))
        );
        gradient = context.createRadialGradient.apply(context, absolutePosition);
      } else {
        throw new Error("Unsupported fill");
      }
      colorStops.forEach(([offset3, color]) => {
        gradient.addColorStop(offset3, color);
      });
      context.fillStyle = gradient;
    }
    function drawOnCanvas(qr, canvas, settings) {
      qr = createMinQRCode(settings.text, settings.ecLevel, settings.minVersion, settings.maxVersion, settings.quiet);
      if (!qr) {
        return null;
      }
      var context = settings.context || canvas?.getContext("2d");
      if (!context) {
        return canvas;
      }
      drawBackground(qr, context, settings);
      drawModules(qr, context, settings, true);
      drawModules(qr, context, settings, false);
      return canvas;
    }
    function createCanvas(qr, settings) {
      var $canvas = document.createElement("canvas");
      $canvas.width = settings.size;
      $canvas.height = settings.size;
      return drawOnCanvas(qr, $canvas, settings);
    }
    var defaults = (
      /** @type {Settings} */
      {
        // version range somewhere in 1 .. 40
        "minVersion": 1,
        "maxVersion": 40,
        // error correction level: `'L'`, `'M'`, `'Q'` or `'H'`
        "ecLevel": "L",
        // offset in pixel if drawn onto existing canvas
        "left": 0,
        "top": 0,
        // size in pixel
        "size": 200,
        // code color or image element
        "fill": "#000",
        "cornerFill": null,
        // Falls back to fill
        // background color, `null` for transparent background
        "background": null,
        // content
        "text": "no text",
        // corner radius relative to module width: 0.0 .. 0.5
        "radius": 0.5,
        // quiet zone in modules
        "quiet": 0,
        "image": null,
        "imageEcCover": 0.5
      }
    );
    qrCodeGenerator = function(options2, $element, callback) {
      var settings = Object.assign({}, defaults, options2);
      settings.minVersion = settings["minVersion"];
      settings.maxVersion = settings["maxVersion"];
      settings.ecLevel = settings["ecLevel"];
      settings.left = settings["left"];
      settings.top = settings["top"];
      settings.size = settings["size"];
      settings.fill = settings["fill"];
      settings.background = settings["background"];
      settings.text = settings["text"];
      settings.radius = settings["radius"];
      settings.quiet = settings["quiet"];
      settings.cornerFill = settings["cornerFill"] || settings.fill;
      settings.image = settings["image"];
      settings.imageBackground = settings["imageBackground"];
      settings.imageEcCover = settings["imageEcCover"];
      settings.imagePadding = settings["imagePadding"];
      var qr = createMinQRCode(
        settings.text,
        settings.ecLevel,
        settings.minVersion,
        settings.maxVersion,
        settings.quiet
      );
      if (!qr) {
        return;
      }
      callback = callback || function() {
      };
      const render = function() {
        var canvasElement = $element;
        if ($element instanceof HTMLCanvasElement) {
          if ($element.width !== settings.size || $element.height !== settings.size) {
            $element.width = settings.size;
            $element.height = settings.size;
          }
          const context = $element.getContext("2d");
          if (context) {
            context.clearRect(0, 0, $element.width, $element.height);
          }
          drawOnCanvas(
            qr,
            $element,
            /** @type {Settings} */
            settings
          );
        } else if (settings.context) {
          const context = settings.context;
          context.clearRect(0, 0, settings.size, settings.size);
          drawOnCanvas(
            qr,
            null,
            /** @type {Settings} */
            settings
          );
        } else {
          if (qr) {
            const canvasEl = createCanvas(
              qr,
              /** @type {Settings} */
              settings
            );
            if (canvasEl) {
              canvasElement = canvasEl;
              $element.appendChild(canvasElement);
            }
          }
        }
        return (
          /** @type {HTMLCanvasElement} */
          canvasElement
        );
      };
      if (settings.image) {
        const img = new Image();
        img.onload = function() {
          if (!qr) {
            return;
          }
          const ecCover = (
            /** @type {number} */
            settings.imageEcCover ?? defaults.imageEcCover
          );
          const quietModuleCount = qr.moduleCount - settings.quiet * 2;
          const moduleSize = settings.size / quietModuleCount;
          const ratio = img.naturalWidth / img.naturalHeight;
          let maxImageWidth = settings.size * ecCover;
          maxImageWidth = Math.min(maxImageWidth, maxImageWidth * ratio);
          let maxImageHeight = settings.size * ecCover;
          maxImageHeight = Math.min(maxImageHeight, maxImageHeight / ratio);
          const dataPixels = quietModuleCount * quietModuleCount - (49 * 3 + 25);
          const area = {
            "L": 0.07,
            "M": 0.15,
            "Q": 0.25,
            "H": 0.3
          }[settings.ecLevel] * ecCover * dataPixels | 0;
          var imageModuleWidth = Math.min(quietModuleCount, Math.sqrt(area * ratio) | 0, maxImageWidth);
          var imageModuleHeight = imageModuleWidth / ratio | 0;
          if (imageModuleHeight > quietModuleCount) {
            imageModuleHeight = quietModuleCount;
            imageModuleWidth = imageModuleHeight * ratio | 0;
          }
          imageModuleHeight = Math.min(imageModuleHeight, maxImageHeight);
          const imageModuleLeft = qr.moduleCount / 2 - imageModuleWidth / 2 | 0;
          const imageModuleTop = qr.moduleCount / 2 - imageModuleHeight / 2 | 0;
          const isDark = qr.isDark;
          qr.isDark = function(row, col) {
            if (imageModuleLeft <= col && col < imageModuleLeft + imageModuleWidth && imageModuleTop <= row && row < imageModuleTop + imageModuleHeight) {
              return false;
            }
            return isDark(row, col);
          };
          const imageFloatModuleWidth = Math.min(imageModuleWidth, imageModuleHeight * ratio) - settings.quiet;
          const imageFloatModuleHeight = Math.min(imageModuleHeight, imageModuleWidth / ratio) - settings.quiet;
          const imageLeft = imageModuleLeft + (imageModuleWidth - imageFloatModuleWidth) / 2 - settings.quiet;
          const imageTop = imageModuleTop + (imageModuleHeight - imageFloatModuleHeight) / 2 - settings.quiet;
          let left = imageLeft * moduleSize;
          let top = imageTop * moduleSize;
          let width = imageFloatModuleWidth * moduleSize;
          let height = imageFloatModuleHeight * moduleSize;
          var canvas = render();
          const context = canvas.getContext("2d");
          if (context) {
            context.fillStyle = settings.imageBackground || "transparent";
            context.fillRect(
              left - 4,
              top - 4,
              width + 8,
              height + 8
            );
            context.drawImage(
              img,
              left,
              top,
              width,
              height
            );
          }
          callback();
        };
        img.onerror = () => {
          render();
          callback();
        };
        img.src = settings.image;
      } else {
        render();
        callback();
      }
    };
  })((function() {
    var qrcode = (function() {
      function qrcode2(typeNumber, errorCorrectLevel) {
        var PAD0 = 236;
        var PAD1 = 17;
        var _typeNumber = typeNumber;
        var _errorCorrectLevel = QRErrorCorrectLevel[errorCorrectLevel];
        var _modules = null;
        var _moduleCount = 0;
        var _dataCache = null;
        var _dataList = new Array();
        var _this = {};
        var makeImpl = function(test, maskPattern) {
          _moduleCount = _typeNumber * 4 + 17;
          _modules = (function(moduleCount) {
            var modules = new Array(moduleCount);
            for (var row = 0; row < moduleCount; row += 1) {
              modules[row] = new Array(moduleCount);
              for (var col = 0; col < moduleCount; col += 1) {
                modules[row][col] = null;
              }
            }
            return modules;
          })(_moduleCount);
          setupPositionProbePattern(0, 0);
          setupPositionProbePattern(_moduleCount - 7, 0);
          setupPositionProbePattern(0, _moduleCount - 7);
          setupPositionAdjustPattern();
          setupTimingPattern();
          setupTypeInfo(test, maskPattern);
          if (_typeNumber >= 7) {
            setupTypeNumber(test);
          }
          if (_dataCache == null) {
            _dataCache = createData(_typeNumber, _errorCorrectLevel, _dataList);
          }
          mapData(_dataCache, maskPattern);
        };
        var setupPositionProbePattern = function(row, col) {
          if (_modules == null) {
            return;
          }
          for (var r9 = -1; r9 <= 7; r9 += 1) {
            if (row + r9 <= -1 || _moduleCount <= row + r9) continue;
            for (var c6 = -1; c6 <= 7; c6 += 1) {
              if (col + c6 <= -1 || _moduleCount <= col + c6) continue;
              if (0 <= r9 && r9 <= 6 && (c6 == 0 || c6 == 6) || 0 <= c6 && c6 <= 6 && (r9 == 0 || r9 == 6) || 2 <= r9 && r9 <= 4 && 2 <= c6 && c6 <= 4) {
                _modules[row + r9][col + c6] = true;
              } else {
                _modules[row + r9][col + c6] = false;
              }
            }
          }
        };
        var getBestMaskPattern = function() {
          var minLostPoint = 0, pattern = 0;
          for (var i10 = 0; i10 < 8; i10 += 1) {
            makeImpl(true, i10);
            var lostPoint = QRUtil.getLostPoint(_this);
            if (i10 == 0 || minLostPoint > lostPoint) {
              minLostPoint = lostPoint;
              pattern = i10;
            }
          }
          return pattern;
        };
        var setupTimingPattern = function() {
          if (!_modules) {
            return;
          }
          for (var r9 = 8; r9 < _moduleCount - 8; r9 += 1) {
            if (_modules[r9][6] != null) {
              continue;
            }
            _modules[r9][6] = r9 % 2 == 0;
          }
          for (var c6 = 8; c6 < _moduleCount - 8; c6 += 1) {
            if (_modules[6][c6] != null) {
              continue;
            }
            _modules[6][c6] = c6 % 2 == 0;
          }
        };
        var setupPositionAdjustPattern = function() {
          if (!_modules) {
            return;
          }
          var pos = QRUtil.getPatternPosition(_typeNumber);
          for (var i10 = 0; i10 < pos.length; i10 += 1) {
            for (var j2 = 0; j2 < pos.length; j2 += 1) {
              var row = pos[i10];
              var col = pos[j2];
              if (_modules[row][col] != null) {
                continue;
              }
              for (var r9 = -2; r9 <= 2; r9 += 1) {
                for (var c6 = -2; c6 <= 2; c6 += 1) {
                  _modules[row + r9][col + c6] = r9 == -2 || r9 == 2 || c6 == -2 || c6 == 2 || r9 == 0 && c6 == 0;
                }
              }
            }
          }
        };
        var setupTypeNumber = function(test) {
          if (!_modules) {
            return;
          }
          var bits = QRUtil.getBCHTypeNumber(_typeNumber);
          for (var i10 = 0; i10 < 18; i10 += 1) {
            var mod = !test && (bits >> i10 & 1) == 1;
            _modules[Math.floor(i10 / 3)][i10 % 3 + _moduleCount - 8 - 3] = mod;
          }
          for (var i10 = 0; i10 < 18; i10 += 1) {
            var mod = !test && (bits >> i10 & 1) == 1;
            _modules[i10 % 3 + _moduleCount - 8 - 3][Math.floor(i10 / 3)] = mod;
          }
        };
        var setupTypeInfo = function(test, maskPattern) {
          var data = _errorCorrectLevel << 3 | maskPattern;
          var bits = QRUtil.getBCHTypeInfo(data);
          if (!_modules) {
            return;
          }
          for (var i10 = 0; i10 < 15; i10 += 1) {
            let mod = !test && (bits >> i10 & 1) == 1;
            _modules[i10 < 6 ? i10 : i10 < 8 ? i10 + 1 : _moduleCount - 15 + i10][8] = mod;
            _modules[8][i10 < 8 ? _moduleCount - i10 - 1 : i10 < 9 ? 15 - i10 : 14 - i10] = mod;
          }
          _modules[_moduleCount - 8][8] = !test;
        };
        var mapData = function(data, maskPattern) {
          var inc = -1, row = _moduleCount - 1, bitIndex = 7, byteIndex = 0, maskFunc = QRUtil.getMaskFunction(maskPattern);
          for (var col = _moduleCount - 1; col > 0; col -= 2) {
            if (col == 6) col -= 1;
            while (true) {
              for (var c6 = 0; c6 < 2; c6 += 1) {
                if (_modules && _modules[row][col - c6] == null) {
                  var dark = false;
                  if (byteIndex < data.length) {
                    dark = (data[byteIndex] >>> bitIndex & 1) == 1;
                  }
                  var mask = maskFunc(row, col - c6);
                  if (mask) {
                    dark = !dark;
                  }
                  _modules[row][col - c6] = dark;
                  bitIndex -= 1;
                  if (bitIndex == -1) {
                    byteIndex += 1;
                    bitIndex = 7;
                  }
                }
              }
              row += inc;
              if (row < 0 || _moduleCount <= row) {
                row -= inc;
                inc = -inc;
                break;
              }
            }
          }
        };
        var createBytes = function(buffer, rsBlocks) {
          var offset3 = 0, maxDcCount = 0, maxEcCount = 0, dcdata = new Array(rsBlocks.length), ecdata = new Array(rsBlocks.length);
          for (var r9 = 0; r9 < rsBlocks.length; r9 += 1) {
            var dcCount = rsBlocks[r9].dataCount, ecCount = rsBlocks[r9].totalCount - dcCount;
            maxDcCount = Math.max(maxDcCount, dcCount);
            maxEcCount = Math.max(maxEcCount, ecCount);
            dcdata[r9] = new Array(dcCount);
            for (var i10 = 0; i10 < dcdata[r9].length; i10 += 1) {
              dcdata[r9][i10] = 255 & buffer.getBuffer()[i10 + offset3];
            }
            offset3 += dcCount;
            var rsPoly = QRUtil.getErrorCorrectPolynomial(ecCount), rawPoly = qrPolynomial(dcdata[r9], rsPoly.getLength() - 1), modPoly = rawPoly.mod(rsPoly);
            ecdata[r9] = new Array(rsPoly.getLength() - 1);
            for (var i10 = 0; i10 < ecdata[r9].length; i10 += 1) {
              var modIndex = i10 + modPoly.getLength() - ecdata[r9].length;
              ecdata[r9][i10] = modIndex >= 0 ? modPoly.getAt(modIndex) : 0;
            }
          }
          var totalCodeCount = 0;
          for (var i10 = 0; i10 < rsBlocks.length; i10 += 1) {
            totalCodeCount += rsBlocks[i10].totalCount;
          }
          var data = new Array(totalCodeCount);
          var index = 0;
          for (var i10 = 0; i10 < maxDcCount; i10 += 1) {
            for (var r9 = 0; r9 < rsBlocks.length; r9 += 1) {
              if (i10 < dcdata[r9].length) {
                data[index] = dcdata[r9][i10];
                index += 1;
              }
            }
          }
          for (var i10 = 0; i10 < maxEcCount; i10 += 1) {
            for (var r9 = 0; r9 < rsBlocks.length; r9 += 1) {
              if (i10 < ecdata[r9].length) {
                data[index] = ecdata[r9][i10];
                index += 1;
              }
            }
          }
          return data;
        };
        var createData = function(typeNumber2, errorCorrectLevel2, dataList) {
          var rsBlocks = QRRSBlock.getRSBlocks(typeNumber2, errorCorrectLevel2);
          var buffer = qrBitBuffer();
          for (var i10 = 0; i10 < dataList.length; i10 += 1) {
            var data = dataList[i10];
            buffer.put(data.getMode(), 4);
            buffer.put(data.getLength(), QRUtil.getLengthInBits(data.getMode(), typeNumber2));
            data.write(buffer);
          }
          var totalDataCount = 0;
          for (var i10 = 0; i10 < rsBlocks.length; i10 += 1) {
            totalDataCount += rsBlocks[i10].dataCount;
          }
          if (buffer.getLengthInBits() > totalDataCount * 8) {
            throw new Error("code length overflow. (" + buffer.getLengthInBits() + ">" + totalDataCount * 8 + ")");
          }
          if (buffer.getLengthInBits() + 4 <= totalDataCount * 8) {
            buffer.put(0, 4);
          }
          while (buffer.getLengthInBits() % 8 != 0) {
            buffer.putBit(false);
          }
          while (true) {
            if (buffer.getLengthInBits() >= totalDataCount * 8) {
              break;
            }
            buffer.put(PAD0, 8);
            if (buffer.getLengthInBits() >= totalDataCount * 8) {
              break;
            }
            buffer.put(PAD1, 8);
          }
          return createBytes(buffer, rsBlocks);
        };
        _this.addData = function(data) {
          var newData = qr8BitByte(data);
          _dataList.push(newData);
          _dataCache = null;
        };
        _this.isDark = function(row, col) {
          if (!_modules) {
            throw new Error("_modules is null");
          }
          if (row < 0 || _moduleCount <= row || col < 0 || _moduleCount <= col) {
            throw new Error(row + "," + col);
          }
          return _modules[row][col];
        };
        _this.getModuleCount = function() {
          return _moduleCount;
        };
        _this.make = function() {
          makeImpl(false, getBestMaskPattern());
        };
        return _this;
      }
      ;
      qrcode2.stringToBytes = function(s7) {
        return new TextEncoder().encode(s7);
      };
      var QRMode = {
        MODE_8BIT_BYTE: 1 << 2
      };
      var QRErrorCorrectLevel = {
        "L": 1,
        "M": 0,
        "Q": 3,
        "H": 2
      };
      var QRMaskPattern = {
        PATTERN000: 0,
        PATTERN001: 1,
        PATTERN010: 2,
        PATTERN011: 3,
        PATTERN100: 4,
        PATTERN101: 5,
        PATTERN110: 6,
        PATTERN111: 7
      };
      var QRUtil = (function() {
        var PATTERN_POSITION_TABLE = [
          [],
          [6, 18],
          [6, 22],
          [6, 26],
          [6, 30],
          [6, 34],
          [6, 22, 38],
          [6, 24, 42],
          [6, 26, 46],
          [6, 28, 50],
          [6, 30, 54],
          [6, 32, 58],
          [6, 34, 62],
          [6, 26, 46, 66],
          [6, 26, 48, 70],
          [6, 26, 50, 74],
          [6, 30, 54, 78],
          [6, 30, 56, 82],
          [6, 30, 58, 86],
          [6, 34, 62, 90],
          [6, 28, 50, 72, 94],
          [6, 26, 50, 74, 98],
          [6, 30, 54, 78, 102],
          [6, 28, 54, 80, 106],
          [6, 32, 58, 84, 110],
          [6, 30, 58, 86, 114],
          [6, 34, 62, 90, 118],
          [6, 26, 50, 74, 98, 122],
          [6, 30, 54, 78, 102, 126],
          [6, 26, 52, 78, 104, 130],
          [6, 30, 56, 82, 108, 134],
          [6, 34, 60, 86, 112, 138],
          [6, 30, 58, 86, 114, 142],
          [6, 34, 62, 90, 118, 146],
          [6, 30, 54, 78, 102, 126, 150],
          [6, 24, 50, 76, 102, 128, 154],
          [6, 28, 54, 80, 106, 132, 158],
          [6, 32, 58, 84, 110, 136, 162],
          [6, 26, 54, 82, 110, 138, 166],
          [6, 30, 58, 86, 114, 142, 170]
        ];
        var G15 = 1 << 10 | 1 << 8 | 1 << 5 | 1 << 4 | 1 << 2 | 1 << 1 | 1 << 0;
        var G18 = 1 << 12 | 1 << 11 | 1 << 10 | 1 << 9 | 1 << 8 | 1 << 5 | 1 << 2 | 1 << 0;
        var G15_MASK = 1 << 14 | 1 << 12 | 1 << 10 | 1 << 4 | 1 << 1;
        var _this = {};
        var getBCHDigit = function(data) {
          var digit = 0;
          while (data != 0) {
            digit += 1;
            data >>>= 1;
          }
          return digit;
        };
        _this.getBCHTypeInfo = function(data) {
          var d3 = data << 10;
          while (getBCHDigit(d3) - getBCHDigit(G15) >= 0) {
            d3 ^= G15 << getBCHDigit(d3) - getBCHDigit(G15);
          }
          return (data << 10 | d3) ^ G15_MASK;
        };
        _this.getBCHTypeNumber = function(data) {
          var d3 = data << 12;
          while (getBCHDigit(d3) - getBCHDigit(G18) >= 0) {
            d3 ^= G18 << getBCHDigit(d3) - getBCHDigit(G18);
          }
          return data << 12 | d3;
        };
        _this.getPatternPosition = function(typeNumber) {
          return PATTERN_POSITION_TABLE[typeNumber - 1];
        };
        _this.getMaskFunction = function(maskPattern) {
          switch (maskPattern) {
            case QRMaskPattern.PATTERN000:
              return function(i10, j2) {
                return (i10 + j2) % 2 == 0;
              };
            case QRMaskPattern.PATTERN001:
              return function(i10, _j) {
                return i10 % 2 == 0;
              };
            case QRMaskPattern.PATTERN010:
              return function(_i, j2) {
                return j2 % 3 == 0;
              };
            case QRMaskPattern.PATTERN011:
              return function(i10, j2) {
                return (i10 + j2) % 3 == 0;
              };
            case QRMaskPattern.PATTERN100:
              return function(i10, j2) {
                return (Math.floor(i10 / 2) + Math.floor(j2 / 3)) % 2 == 0;
              };
            case QRMaskPattern.PATTERN101:
              return function(i10, j2) {
                return i10 * j2 % 2 + i10 * j2 % 3 == 0;
              };
            case QRMaskPattern.PATTERN110:
              return function(i10, j2) {
                return (i10 * j2 % 2 + i10 * j2 % 3) % 2 == 0;
              };
            case QRMaskPattern.PATTERN111:
              return function(i10, j2) {
                return (i10 * j2 % 3 + (i10 + j2) % 2) % 2 == 0;
              };
            default:
              throw new Error("bad maskPattern:" + maskPattern);
          }
        };
        _this.getErrorCorrectPolynomial = function(errorCorrectLength) {
          var a4 = qrPolynomial([1], 0);
          for (var i10 = 0; i10 < errorCorrectLength; i10 += 1) {
            a4 = a4.multiply(qrPolynomial([1, QRMath.gexp(i10)], 0));
          }
          return a4;
        };
        _this.getLengthInBits = function(mode, type) {
          if (mode != QRMode.MODE_8BIT_BYTE || type < 1 || type > 40)
            throw new Error("mode: " + mode + "; type: " + type);
          return type < 10 ? 8 : 16;
        };
        _this.getLostPoint = function(qrcode3) {
          var moduleCount = qrcode3.getModuleCount();
          var lostPoint = 0;
          for (var row = 0; row < moduleCount; row += 1) {
            for (var col = 0; col < moduleCount; col += 1) {
              var sameCount = 0, dark = qrcode3.isDark(row, col);
              for (var r9 = -1; r9 <= 1; r9 += 1) {
                if (row + r9 < 0 || moduleCount <= row + r9) {
                  continue;
                }
                for (var c6 = -1; c6 <= 1; c6 += 1) {
                  if (col + c6 < 0 || moduleCount <= col + c6) {
                    continue;
                  }
                  if (r9 == 0 && c6 == 0) {
                    continue;
                  }
                  if (dark == qrcode3.isDark(row + r9, col + c6)) {
                    sameCount += 1;
                  }
                }
              }
              if (sameCount > 5) {
                lostPoint += 3 + sameCount - 5;
              }
            }
          }
          ;
          for (var row = 0; row < moduleCount - 1; row += 1) {
            for (var col = 0; col < moduleCount - 1; col += 1) {
              var count = 0;
              if (qrcode3.isDark(row, col)) count += 1;
              if (qrcode3.isDark(row + 1, col)) count += 1;
              if (qrcode3.isDark(row, col + 1)) count += 1;
              if (qrcode3.isDark(row + 1, col + 1)) count += 1;
              if (count == 0 || count == 4) {
                lostPoint += 3;
              }
            }
          }
          for (var row = 0; row < moduleCount; row += 1) {
            for (var col = 0; col < moduleCount - 6; col += 1) {
              if (qrcode3.isDark(row, col) && !qrcode3.isDark(row, col + 1) && qrcode3.isDark(row, col + 2) && qrcode3.isDark(row, col + 3) && qrcode3.isDark(row, col + 4) && !qrcode3.isDark(row, col + 5) && qrcode3.isDark(row, col + 6)) {
                lostPoint += 40;
              }
            }
          }
          for (var col = 0; col < moduleCount; col += 1) {
            for (var row = 0; row < moduleCount - 6; row += 1) {
              if (qrcode3.isDark(row, col) && !qrcode3.isDark(row + 1, col) && qrcode3.isDark(row + 2, col) && qrcode3.isDark(row + 3, col) && qrcode3.isDark(row + 4, col) && !qrcode3.isDark(row + 5, col) && qrcode3.isDark(row + 6, col)) {
                lostPoint += 40;
              }
            }
          }
          var darkCount = 0;
          for (var col = 0; col < moduleCount; col += 1) {
            for (var row = 0; row < moduleCount; row += 1) {
              if (qrcode3.isDark(row, col)) {
                darkCount += 1;
              }
            }
          }
          var ratio = Math.abs(100 * darkCount / moduleCount / moduleCount - 50) / 5;
          lostPoint += ratio * 10;
          return lostPoint;
        };
        return _this;
      })();
      var QRMath = (function() {
        var LOG_TABLE = new Array(256);
        var EXP_TABLE = new Array(256);
        for (var i10 = 0; i10 < 8; i10 += 1) {
          EXP_TABLE[i10] = 1 << i10;
        }
        for (var i10 = 8; i10 < 256; i10 += 1) {
          EXP_TABLE[i10] = EXP_TABLE[i10 - 4] ^ EXP_TABLE[i10 - 5] ^ EXP_TABLE[i10 - 6] ^ EXP_TABLE[i10 - 8];
        }
        for (var i10 = 0; i10 < 255; i10 += 1) {
          LOG_TABLE[EXP_TABLE[i10]] = i10;
        }
        var _this = {};
        _this.glog = function(n9) {
          if (n9 < 1) {
            throw new Error("glog(" + n9 + ")");
          }
          return LOG_TABLE[n9];
        };
        _this.gexp = function(n9) {
          while (n9 < 0) {
            n9 += 255;
          }
          while (n9 >= 256) {
            n9 -= 255;
          }
          return EXP_TABLE[n9];
        };
        return _this;
      })();
      function qrPolynomial(num, shift3) {
        if (typeof num.length == "undefined") {
          throw new Error(num.length + "/" + shift3);
        }
        var _num = (function() {
          var offset3 = 0;
          while (offset3 < num.length && num[offset3] == 0) {
            offset3 += 1;
          }
          var _num2 = new Array(num.length - offset3 + shift3);
          for (var i10 = 0; i10 < num.length - offset3; i10 += 1) {
            _num2[i10] = num[i10 + offset3];
          }
          return _num2;
        })();
        var _this = {};
        _this.getAt = function(index) {
          return _num[index];
        };
        _this.getLength = function() {
          return _num.length;
        };
        _this.multiply = function(e12) {
          var num2 = new Array(_this.getLength() + e12.getLength() - 1);
          for (var i10 = 0; i10 < _this.getLength(); i10 += 1) {
            for (var j2 = 0; j2 < e12.getLength(); j2 += 1) {
              num2[i10 + j2] ^= QRMath.gexp(QRMath.glog(_this.getAt(i10)) + QRMath.glog(e12.getAt(j2)));
            }
          }
          return qrPolynomial(num2, 0);
        };
        _this.mod = function(e12) {
          if (_this.getLength() - e12.getLength() < 0) {
            return _this;
          }
          var ratio = QRMath.glog(_this.getAt(0)) - QRMath.glog(e12.getAt(0));
          var num2 = new Array(_this.getLength());
          for (var i10 = 0; i10 < _this.getLength(); i10 += 1) {
            num2[i10] = _this.getAt(i10);
          }
          for (var i10 = 0; i10 < e12.getLength(); i10 += 1) {
            num2[i10] ^= QRMath.gexp(QRMath.glog(e12.getAt(i10)) + ratio);
          }
          return qrPolynomial(num2, 0).mod(e12);
        };
        return _this;
      }
      ;
      var QRRSBlock = (function() {
        var RS_BLOCK_TABLE = [
          // L
          // M
          // Q
          // H
          // 1
          [1, 26, 19],
          [1, 26, 16],
          [1, 26, 13],
          [1, 26, 9],
          // 2
          [1, 44, 34],
          [1, 44, 28],
          [1, 44, 22],
          [1, 44, 16],
          // 3
          [1, 70, 55],
          [1, 70, 44],
          [2, 35, 17],
          [2, 35, 13],
          // 4
          [1, 100, 80],
          [2, 50, 32],
          [2, 50, 24],
          [4, 25, 9],
          // 5
          [1, 134, 108],
          [2, 67, 43],
          [2, 33, 15, 2, 34, 16],
          [2, 33, 11, 2, 34, 12],
          // 6
          [2, 86, 68],
          [4, 43, 27],
          [4, 43, 19],
          [4, 43, 15],
          // 7
          [2, 98, 78],
          [4, 49, 31],
          [2, 32, 14, 4, 33, 15],
          [4, 39, 13, 1, 40, 14],
          // 8
          [2, 121, 97],
          [2, 60, 38, 2, 61, 39],
          [4, 40, 18, 2, 41, 19],
          [4, 40, 14, 2, 41, 15],
          // 9
          [2, 146, 116],
          [3, 58, 36, 2, 59, 37],
          [4, 36, 16, 4, 37, 17],
          [4, 36, 12, 4, 37, 13],
          // 10
          [2, 86, 68, 2, 87, 69],
          [4, 69, 43, 1, 70, 44],
          [6, 43, 19, 2, 44, 20],
          [6, 43, 15, 2, 44, 16],
          // 11
          [4, 101, 81],
          [1, 80, 50, 4, 81, 51],
          [4, 50, 22, 4, 51, 23],
          [3, 36, 12, 8, 37, 13],
          // 12
          [2, 116, 92, 2, 117, 93],
          [6, 58, 36, 2, 59, 37],
          [4, 46, 20, 6, 47, 21],
          [7, 42, 14, 4, 43, 15],
          // 13
          [4, 133, 107],
          [8, 59, 37, 1, 60, 38],
          [8, 44, 20, 4, 45, 21],
          [12, 33, 11, 4, 34, 12],
          // 14
          [3, 145, 115, 1, 146, 116],
          [4, 64, 40, 5, 65, 41],
          [11, 36, 16, 5, 37, 17],
          [11, 36, 12, 5, 37, 13],
          // 15
          [5, 109, 87, 1, 110, 88],
          [5, 65, 41, 5, 66, 42],
          [5, 54, 24, 7, 55, 25],
          [11, 36, 12, 7, 37, 13],
          // 16
          [5, 122, 98, 1, 123, 99],
          [7, 73, 45, 3, 74, 46],
          [15, 43, 19, 2, 44, 20],
          [3, 45, 15, 13, 46, 16],
          // 17
          [1, 135, 107, 5, 136, 108],
          [10, 74, 46, 1, 75, 47],
          [1, 50, 22, 15, 51, 23],
          [2, 42, 14, 17, 43, 15],
          // 18
          [5, 150, 120, 1, 151, 121],
          [9, 69, 43, 4, 70, 44],
          [17, 50, 22, 1, 51, 23],
          [2, 42, 14, 19, 43, 15],
          // 19
          [3, 141, 113, 4, 142, 114],
          [3, 70, 44, 11, 71, 45],
          [17, 47, 21, 4, 48, 22],
          [9, 39, 13, 16, 40, 14],
          // 20
          [3, 135, 107, 5, 136, 108],
          [3, 67, 41, 13, 68, 42],
          [15, 54, 24, 5, 55, 25],
          [15, 43, 15, 10, 44, 16],
          // 21
          [4, 144, 116, 4, 145, 117],
          [17, 68, 42],
          [17, 50, 22, 6, 51, 23],
          [19, 46, 16, 6, 47, 17],
          // 22
          [2, 139, 111, 7, 140, 112],
          [17, 74, 46],
          [7, 54, 24, 16, 55, 25],
          [34, 37, 13],
          // 23
          [4, 151, 121, 5, 152, 122],
          [4, 75, 47, 14, 76, 48],
          [11, 54, 24, 14, 55, 25],
          [16, 45, 15, 14, 46, 16],
          // 24
          [6, 147, 117, 4, 148, 118],
          [6, 73, 45, 14, 74, 46],
          [11, 54, 24, 16, 55, 25],
          [30, 46, 16, 2, 47, 17],
          // 25
          [8, 132, 106, 4, 133, 107],
          [8, 75, 47, 13, 76, 48],
          [7, 54, 24, 22, 55, 25],
          [22, 45, 15, 13, 46, 16],
          // 26
          [10, 142, 114, 2, 143, 115],
          [19, 74, 46, 4, 75, 47],
          [28, 50, 22, 6, 51, 23],
          [33, 46, 16, 4, 47, 17],
          // 27
          [8, 152, 122, 4, 153, 123],
          [22, 73, 45, 3, 74, 46],
          [8, 53, 23, 26, 54, 24],
          [12, 45, 15, 28, 46, 16],
          // 28
          [3, 147, 117, 10, 148, 118],
          [3, 73, 45, 23, 74, 46],
          [4, 54, 24, 31, 55, 25],
          [11, 45, 15, 31, 46, 16],
          // 29
          [7, 146, 116, 7, 147, 117],
          [21, 73, 45, 7, 74, 46],
          [1, 53, 23, 37, 54, 24],
          [19, 45, 15, 26, 46, 16],
          // 30
          [5, 145, 115, 10, 146, 116],
          [19, 75, 47, 10, 76, 48],
          [15, 54, 24, 25, 55, 25],
          [23, 45, 15, 25, 46, 16],
          // 31
          [13, 145, 115, 3, 146, 116],
          [2, 74, 46, 29, 75, 47],
          [42, 54, 24, 1, 55, 25],
          [23, 45, 15, 28, 46, 16],
          // 32
          [17, 145, 115],
          [10, 74, 46, 23, 75, 47],
          [10, 54, 24, 35, 55, 25],
          [19, 45, 15, 35, 46, 16],
          // 33
          [17, 145, 115, 1, 146, 116],
          [14, 74, 46, 21, 75, 47],
          [29, 54, 24, 19, 55, 25],
          [11, 45, 15, 46, 46, 16],
          // 34
          [13, 145, 115, 6, 146, 116],
          [14, 74, 46, 23, 75, 47],
          [44, 54, 24, 7, 55, 25],
          [59, 46, 16, 1, 47, 17],
          // 35
          [12, 151, 121, 7, 152, 122],
          [12, 75, 47, 26, 76, 48],
          [39, 54, 24, 14, 55, 25],
          [22, 45, 15, 41, 46, 16],
          // 36
          [6, 151, 121, 14, 152, 122],
          [6, 75, 47, 34, 76, 48],
          [46, 54, 24, 10, 55, 25],
          [2, 45, 15, 64, 46, 16],
          // 37
          [17, 152, 122, 4, 153, 123],
          [29, 74, 46, 14, 75, 47],
          [49, 54, 24, 10, 55, 25],
          [24, 45, 15, 46, 46, 16],
          // 38
          [4, 152, 122, 18, 153, 123],
          [13, 74, 46, 32, 75, 47],
          [48, 54, 24, 14, 55, 25],
          [42, 45, 15, 32, 46, 16],
          // 39
          [20, 147, 117, 4, 148, 118],
          [40, 75, 47, 7, 76, 48],
          [43, 54, 24, 22, 55, 25],
          [10, 45, 15, 67, 46, 16],
          // 40
          [19, 148, 118, 6, 149, 119],
          [18, 75, 47, 31, 76, 48],
          [34, 54, 24, 34, 55, 25],
          [20, 45, 15, 61, 46, 16]
        ];
        var qrRSBlock = function(totalCount, dataCount) {
          var _this2 = {};
          _this2.totalCount = totalCount;
          _this2.dataCount = dataCount;
          return _this2;
        };
        var _this = {};
        var getRsBlockTable = function(typeNumber, errorCorrectLevel) {
          switch (errorCorrectLevel) {
            case QRErrorCorrectLevel["L"]:
              return RS_BLOCK_TABLE[(typeNumber - 1) * 4 + 0];
            case QRErrorCorrectLevel["M"]:
              return RS_BLOCK_TABLE[(typeNumber - 1) * 4 + 1];
            case QRErrorCorrectLevel["Q"]:
              return RS_BLOCK_TABLE[(typeNumber - 1) * 4 + 2];
            case QRErrorCorrectLevel["H"]:
              return RS_BLOCK_TABLE[(typeNumber - 1) * 4 + 3];
            default:
              return void 0;
          }
        };
        _this.getRSBlocks = function(typeNumber, errorCorrectLevel) {
          var rsBlock = getRsBlockTable(typeNumber, errorCorrectLevel);
          if (typeof rsBlock == "undefined") {
            throw new Error("bad rs block @ typeNumber:" + typeNumber + "/errorCorrectLevel:" + errorCorrectLevel);
          }
          var length = rsBlock.length / 3, list2 = new Array();
          for (var i10 = 0; i10 < length; i10 += 1) {
            var count = rsBlock[i10 * 3 + 0], totalCount = rsBlock[i10 * 3 + 1], dataCount = rsBlock[i10 * 3 + 2];
            for (var j2 = 0; j2 < count; j2 += 1) {
              list2.push(qrRSBlock(totalCount, dataCount));
            }
          }
          return list2;
        };
        return _this;
      })();
      var qrBitBuffer = function() {
        var _buffer = new Array();
        var _length = 0;
        var _this = {};
        _this.getBuffer = function() {
          return _buffer;
        };
        _this.getAt = function(index) {
          var bufIndex = Math.floor(index / 8);
          return (_buffer[bufIndex] >>> 7 - index % 8 & 1) == 1;
        };
        _this.put = function(num, length) {
          for (var i10 = 0; i10 < length; i10 += 1) {
            _this.putBit((num >>> length - i10 - 1 & 1) == 1);
          }
        };
        _this.getLengthInBits = function() {
          return _length;
        };
        _this.putBit = function(bit) {
          var bufIndex = Math.floor(_length / 8);
          if (_buffer.length <= bufIndex) {
            _buffer.push(0);
          }
          if (bit) {
            _buffer[bufIndex] |= 128 >>> _length % 8;
          }
          _length += 1;
        };
        return _this;
      };
      var qr8BitByte = function(data) {
        var _mode = QRMode.MODE_8BIT_BYTE;
        var _bytes = qrcode2.stringToBytes(data);
        var _this = {};
        _this.getMode = function() {
          return _mode;
        };
        _this.getLength = function(_buffer) {
          return _bytes.length;
        };
        _this.write = function(buffer) {
          for (var i10 = 0; i10 < _bytes.length; i10 += 1) {
            buffer.put(_bytes[i10], 8);
          }
        };
        return _this;
      };
      return qrcode2;
    })();
    return qrcode;
  })());

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.YMIUA75R.js
  var CsQrCode = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.value = "";
      this.label = "";
      this.size = 128;
      this.fill = "";
      this.background = "";
      this.radius = 0;
      this.errorCorrection = "H";
      this.image = null;
      this.imageBackground = null;
      this.imageCoverage = null;
      this.imagePadding = null;
      this.computedStyle = null;
    }
    updated(changedProperties) {
      super.updated(changedProperties);
      this.generate();
    }
    generate() {
      if (!this.hasUpdated) {
        return;
      }
      this.canvas.style.maxWidth = `${this.size}px`;
      this.canvas.style.maxHeight = `${this.size}px`;
      this.computedStyle || (this.computedStyle = getComputedStyle(this));
      const computedStyle = this.computedStyle;
      const span = this.shadowRoot?.querySelector("span");
      if (span) {
        this.spanComputedStyle || (this.spanComputedStyle = getComputedStyle(span));
      }
      QrCreator.render(
        {
          text: this.value,
          radius: this.radius,
          ecLevel: this.errorCorrection,
          // Use the deprecated `fill` attribute if set, otherwise use the current text color
          fill: this.fill || computedStyle.color,
          // Use the deprecated `background` attribute if set, otherwise use transparent (the host has the bg color now)
          background: this.background || null,
          // We draw the canvas larger and scale its container down to avoid blurring on high-density displays
          size: this.size * 2,
          image: this.image,
          imageEcCover: this.imageCoverage,
          imagePadding: this.imagePadding,
          imageBackground: this.imageBackground || this.background,
          // @ts-expect-error -- cornerFill is not in the QrCreator option types, but the library reads it
          cornerFill: this.spanComputedStyle?.color
        },
        this.canvas
      );
    }
    render() {
      return b2`
      <canvas
        part="qr-code"
        class="qr-code"
        role="img"
        aria-label=${this.label.length > 0 ? this.label : this.value}
        style=${o7({
        maxWidth: `${this.size}px`,
        maxHeight: `${this.size}px`,
        minWidth: `${this.size}px`,
        minHeight: `${this.size}px`
      })}
        @transitionend=${(event) => {
        if (event.propertyName === "color") {
          this.generate();
        }
      }}
      >
        <span style="color: var(--corner-color);"></span>
      </canvas>
    `;
    }
  };
  CsQrCode.css = qr_code_styles_default;
  __decorateClass([
    e5("canvas")
  ], CsQrCode.prototype, "canvas", 2);
  __decorateClass([
    n4()
  ], CsQrCode.prototype, "value", 2);
  __decorateClass([
    n4()
  ], CsQrCode.prototype, "label", 2);
  __decorateClass([
    n4({ type: Number })
  ], CsQrCode.prototype, "size", 2);
  __decorateClass([
    n4()
  ], CsQrCode.prototype, "fill", 2);
  __decorateClass([
    n4()
  ], CsQrCode.prototype, "background", 2);
  __decorateClass([
    n4({ type: Number })
  ], CsQrCode.prototype, "radius", 2);
  __decorateClass([
    n4({ attribute: "error-correction" })
  ], CsQrCode.prototype, "errorCorrection", 2);
  __decorateClass([
    n4()
  ], CsQrCode.prototype, "image", 2);
  __decorateClass([
    n4({ attribute: "image-background" })
  ], CsQrCode.prototype, "imageBackground", 2);
  __decorateClass([
    n4({ attribute: "image-coverage", type: Number })
  ], CsQrCode.prototype, "imageCoverage", 2);
  __decorateClass([
    n4({ attribute: "image-padding", type: Number })
  ], CsQrCode.prototype, "imagePadding", 2);
  CsQrCode = __decorateClass([
    customElement("cs-qr-code")
  ], CsQrCode);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.6QN4ZK7V.js
  var radio_styles_default = i`
  :host {
    --checked-icon-color: var(--cs-form-control-activated-color);
    --checked-icon-scale: 0.7;

    color: var(--cs-form-control-value-color);
    display: inline-flex;
    flex-direction: row;
    align-items: top;
    font-family: inherit;
    font-weight: var(--cs-form-control-value-font-weight);
    line-height: var(--cs-form-control-value-line-height);
    cursor: pointer;
    user-select: none;
    -webkit-user-select: none;
  }

  :host(:focus) {
    outline: none;
  }

  /* When the control isn't checked, hide the circle for Windows High Contrast mode a11y */
  :host(:not(:state(checked))) svg circle {
    opacity: 0;
  }

  [part~='label'] {
    display: inline;
  }

  [part~='hint'] {
    margin-block-start: 0.5em;
  }

  /* Default spacing for default appearance radios */
  :host(:not([appearance='button'])) {
    margin-block: 0.375em; /* Half of the original 0.75em gap on each side */
  }

  :host(:not([appearance='button'])[data-cs-radio-horizontal]) {
    margin-block: 0;
    margin-inline: 0.5em; /* Half of the original 1em gap on each side */
  }

  /* Remove margin from first/last items to prevent extra space */
  :host(:not([appearance='button'])[data-cs-radio-first]) {
    margin-block-start: 0;
    margin-inline-start: 0;
  }

  :host(:not([appearance='button'])[data-cs-radio-last]) {
    margin-block-end: 0;
    margin-inline-end: 0;
  }

  /* Button appearance have no spacing, they get handled by the overlap margins below */
  :host([appearance='button']) {
    margin: 0;
    align-items: center;
    min-height: var(--cs-form-control-height);
    background-color: var(--cs-color-surface-default);
    border: var(--cs-form-control-border-width) var(--cs-form-control-border-style) var(--cs-form-control-border-color);
    border-radius: var(--cs-border-radius-m);
    padding: 0 var(--cs-form-control-padding-inline);
    transition:
      background-color var(--cs-transition-fast),
      border-color var(--cs-transition-fast);
  }

  /* Default appearance */
  :host(:not([appearance='button'])) {
    .control {
      flex: 0 0 auto;
      position: relative;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: var(--cs-form-control-toggle-size);
      height: var(--cs-form-control-toggle-size);
      border-color: var(--cs-form-control-border-color);
      border-radius: 50%;
      border-style: var(--cs-form-control-border-style);
      border-width: var(--cs-form-control-border-width);
      background-color: var(--cs-form-control-background-color);
      color: transparent;
      transition:
        background var(--cs-transition-normal),
        border-color var(--cs-transition-fast),
        box-shadow var(--cs-transition-fast),
        color var(--cs-transition-fast);
      transition-timing-function: var(--cs-transition-easing);

      margin-inline-end: 0.5em;
    }

    .checked-icon {
      display: flex;
      fill: currentColor;
      width: var(--cs-form-control-toggle-size);
      height: var(--cs-form-control-toggle-size);
      scale: var(--checked-icon-scale);
    }
  }

  /* Button appearance */
  :host([appearance='button']) {
    .control {
      display: none;
    }
  }

  /* Checked */
  :host(:state(checked)) .control {
    color: var(--checked-icon-color);
    border-color: var(--cs-form-control-activated-color);
    background-color: var(--cs-form-control-background-color);
  }

  /* Focus */
  :host(:focus-visible) .control {
    outline: var(--cs-focus-ring);
    outline-offset: var(--cs-focus-ring-offset);
  }

  /* Disabled */
  :host(:state(disabled)) {
    opacity: 0.5;
    cursor: not-allowed;
  }

  /* Horizontal grouping - remove inner border radius */
  :host([appearance='button'][data-cs-radio-horizontal][data-cs-radio-inner]) {
    border-radius: 0;
  }

  :host([appearance='button'][data-cs-radio-horizontal][data-cs-radio-first]) {
    border-start-end-radius: 0;
    border-end-end-radius: 0;
  }

  :host([appearance='button'][data-cs-radio-horizontal][data-cs-radio-last]) {
    border-start-start-radius: 0;
    border-end-start-radius: 0;
  }

  /* Vertical grouping - remove inner border radius */
  :host([appearance='button'][data-cs-radio-vertical][data-cs-radio-inner]) {
    border-radius: 0;
  }

  :host([appearance='button'][data-cs-radio-vertical][data-cs-radio-first]) {
    border-end-start-radius: 0;
    border-end-end-radius: 0;
  }

  :host([appearance='button'][data-cs-radio-vertical][data-cs-radio-last]) {
    border-start-start-radius: 0;
    border-start-end-radius: 0;
  }

  @media (hover: hover) {
    :host([appearance='button']:hover:not(:state(disabled), :state(checked))) {
      background-color: color-mix(in srgb, var(--cs-color-surface-default) 95%, var(--cs-color-mix-hover));
    }
  }

  :host([appearance='button']:focus-visible) {
    outline: var(--cs-focus-ring);
    outline-offset: var(--cs-focus-ring-offset);
  }

  :host([appearance='button']:state(checked)) {
    border-color: var(--cs-form-control-activated-color);
    background-color: var(--cs-color-brand-fill-quiet);
  }

  :host([appearance='button']:state(checked):focus-visible) {
    outline: var(--cs-focus-ring);
    outline-offset: var(--cs-focus-ring-offset);
  }

  /* Button overlap margins */
  :host([appearance='button'][data-cs-radio-horizontal]:not([data-cs-radio-first])) {
    margin-inline-start: calc(-1 * var(--cs-form-control-border-width));
  }

  :host([appearance='button'][data-cs-radio-vertical]:not([data-cs-radio-first])) {
    margin-block-start: calc(-1 * var(--cs-form-control-border-width));
  }

  /* Ensure interactive states are visible above adjacent buttons */
  :host([appearance='button']:hover),
  :host([appearance='button']:state(checked)) {
    position: relative;
    z-index: 1;
  }

  :host([appearance='button']:focus-visible) {
    z-index: 2;
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.P7NUKR3A.js
  var CsRadio = class extends CornerstoneFormAssociatedElement {
    constructor() {
      super();
      this.checked = false;
      this.forceDisabled = false;
      this.appearance = "default";
      this.disabled = false;
      this.handleClick = () => {
        if (!this.disabled && !this.forceDisabled) {
          this.checked = true;
        }
      };
      if (!o5) {
        this.addEventListener("click", this.handleClick);
      }
    }
    connectedCallback() {
      super.connectedCallback();
      this.setInitialAttributes();
    }
    setInitialAttributes() {
      this.setAttribute("role", "radio");
      this.tabIndex = 0;
      this.setAttribute("aria-disabled", this.disabled || this.forceDisabled ? "true" : "false");
    }
    updated(changedProperties) {
      super.updated(changedProperties);
      if (changedProperties.has("checked")) {
        this.customStates.set("checked", this.checked);
        this.setAttribute("aria-checked", this.checked ? "true" : "false");
        if (!this.disabled && !this.forceDisabled) {
          this.tabIndex = this.checked ? 0 : -1;
        }
      }
      if (changedProperties.has("disabled") || changedProperties.has("forceDisabled")) {
        const effectivelyDisabled = this.disabled || this.forceDisabled;
        this.customStates.set("disabled", effectivelyDisabled);
        this.setAttribute("aria-disabled", effectivelyDisabled ? "true" : "false");
        if (effectivelyDisabled) {
          this.tabIndex = -1;
        } else {
          this.tabIndex = this.checked ? 0 : -1;
        }
      }
    }
    /**
     * @override
     */
    setValue() {
    }
    render() {
      return b2`
      <span part="control" class="control">
        ${this.checked ? b2`
                <svg viewBox="0 0 16 16" xmlns="http://www.w3.org/2000/svg" part="checked-icon" class="checked-icon">
                  <circle cx="8" cy="8" r="8" />
                </svg>
              ` : ""}
      </span>

      <slot part="label" class="label"></slot>
    `;
    }
  };
  CsRadio.css = [form_control_styles_default, size_styles_default, radio_styles_default];
  __decorateClass([
    r5()
  ], CsRadio.prototype, "checked", 2);
  __decorateClass([
    r5()
  ], CsRadio.prototype, "forceDisabled", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsRadio.prototype, "value", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsRadio.prototype, "appearance", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsRadio.prototype, "size", 2);
  __decorateClass([
    n4({ type: Boolean })
  ], CsRadio.prototype, "disabled", 2);
  CsRadio = __decorateClass([
    customElement("cs-radio")
  ], CsRadio);
  CsRadio.disableWarning?.("change-in-update");

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.W3JUPGVS.js
  var radio_group_styles_default = i`
  .form-control {
    position: relative;
    border: none;
    padding: 0;
    margin: 0;
  }

  .label {
    padding: 0;
  }

  .radio-group-required .label::after {
    content: var(--cs-form-control-required-content);
    margin-inline-start: var(--cs-form-control-required-content-offset);
  }

  [part~='form-control-input'] {
    display: flex;
    flex-direction: column;
    flex-wrap: wrap;
    gap: 0; /* Radios handle their own spacing */
  }

  /* Horizontal */
  :host([orientation='horizontal']) [part~='form-control-input'] {
    flex-direction: row;
  }

  /* Help text */
  [part~='hint'] {
    margin-block-start: 0.5em;
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.22IYOOCA.js
  var CsRadioGroup = class extends CornerstoneFormAssociatedElement {
    constructor() {
      super();
      this.hasSlotController = new HasSlotController(this, "hint", "label");
      this.label = "";
      this.hint = "";
      this.name = null;
      this.disabled = false;
      this.orientation = "vertical";
      this._value = null;
      this.defaultValue = this.getAttribute("value") || null;
      this.required = false;
      this.ssrLabel = false;
      this.ssrHint = false;
      this.handleRadioClick = (e12) => {
        const clickedRadio = e12.target.closest("cs-radio");
        if (!clickedRadio || clickedRadio.disabled || clickedRadio.forceDisabled || this.disabled) {
          return;
        }
        const oldValue = this.value;
        this.value = clickedRadio.value;
        clickedRadio.checked = true;
        const radios = this.getAllRadios();
        for (const radio of radios) {
          if (clickedRadio === radio) {
            continue;
          }
          radio.checked = false;
          radio.setAttribute("tabindex", "-1");
        }
        if (this.value !== oldValue) {
          this.updateComplete.then(() => {
            this.dispatchEvent(new InputEvent("input", { bubbles: true, composed: true }));
            this.dispatchEvent(new Event("change", { bubbles: true, composed: true }));
          });
        }
      };
      if (!o5) {
        this.addEventListener("keydown", this.handleKeyDown);
        this.addEventListener("click", this.handleRadioClick);
      }
    }
    static get validators() {
      return [
        ...super.validators,
        RequiredValidator({
          validationElement: () => Object.assign(document.createElement("input"), {
            required: true,
            type: "radio",
            // we need an id that's guaranteed to be unique; users will never see this
            name: uniqueId("__wa-radio")
          })
        })
      ];
    }
    get value() {
      if (this.valueHasChanged) {
        return this._value;
      }
      return this._value ?? this.defaultValue;
    }
    set value(val) {
      if (typeof val === "number") {
        val = String(val);
      }
      this.valueHasChanged = true;
      this._value = val;
    }
    /**
     * We use the first available radio as the validationTarget similar to native HTML that shows the validation popup on
     * the first radio element.
     */
    get validationTarget() {
      if (o5) {
        return void 0;
      }
      const radio = this.querySelector(":is(cs-radio):not([disabled])");
      if (!radio) {
        return void 0;
      }
      return radio;
    }
    updated(changedProperties) {
      if (changedProperties.has("disabled") || changedProperties.has("size") || changedProperties.has("value") || changedProperties.has("defaultValue")) {
        this.syncRadioElements();
      }
    }
    formResetCallback(...args) {
      this._value = null;
      super.formResetCallback(...args);
      this.syncRadioElements();
    }
    getAllRadios() {
      return [...this.querySelectorAll("cs-radio")];
    }
    handleLabelClick() {
      this.focus();
    }
    async syncRadioElements() {
      const radios = this.getAllRadios();
      radios.forEach((radio, index) => {
        if (this.size) {
          radio.setAttribute("size", this.size);
        }
        radio.toggleAttribute("data-cs-radio-horizontal", this.orientation !== "vertical");
        radio.toggleAttribute("data-cs-radio-vertical", this.orientation === "vertical");
        radio.toggleAttribute("data-cs-radio-first", index === 0);
        radio.toggleAttribute("data-cs-radio-inner", index !== 0 && index !== radios.length - 1);
        radio.toggleAttribute("data-cs-radio-last", index === radios.length - 1);
        radio.forceDisabled = this.disabled;
      });
      await Promise.all(
        radios.map(async (radio) => {
          await radio.updateComplete;
          if (!radio.disabled && radio.value === this.value) {
            radio.checked = true;
          } else {
            radio.checked = false;
          }
        })
      );
      if (this.disabled) {
        radios.forEach((radio) => {
          radio.tabIndex = -1;
        });
      } else {
        const enabledRadios = radios.filter((radio) => !radio.disabled);
        const checkedRadio = enabledRadios.find((radio) => radio.checked);
        if (enabledRadios.length > 0) {
          if (checkedRadio) {
            enabledRadios.forEach((radio) => {
              radio.tabIndex = radio.checked ? 0 : -1;
            });
          } else {
            enabledRadios.forEach((radio, index) => {
              radio.tabIndex = index === 0 ? 0 : -1;
            });
          }
        }
        radios.filter((radio) => radio.disabled).forEach((radio) => {
          radio.tabIndex = -1;
        });
      }
    }
    handleKeyDown(event) {
      if (!["ArrowUp", "ArrowDown", "ArrowLeft", "ArrowRight", " "].includes(event.key) || this.disabled) {
        return;
      }
      const radios = this.getAllRadios().filter((radio) => !radio.disabled);
      if (radios.length <= 0) {
        return;
      }
      event.preventDefault();
      const oldValue = this.value;
      const checkedRadio = radios.find((radio) => radio.checked) ?? radios[0];
      const incr = event.key === " " ? 0 : ["ArrowUp", "ArrowLeft"].includes(event.key) ? -1 : 1;
      let index = radios.indexOf(checkedRadio) + incr;
      if (!index) {
        index = 0;
      }
      if (index < 0) {
        index = radios.length - 1;
      }
      if (index > radios.length - 1) {
        index = 0;
      }
      const hasRadioButtons = radios.some((radio) => radio.tagName.toLowerCase() === "cs-radio-button");
      this.getAllRadios().forEach((radio) => {
        radio.checked = false;
        if (!hasRadioButtons) {
          radio.setAttribute("tabindex", "-1");
        }
      });
      this.value = radios[index].value;
      radios[index].checked = true;
      if (!hasRadioButtons) {
        radios[index].setAttribute("tabindex", "0");
        radios[index].focus();
      } else {
        radios[index].shadowRoot.querySelector("button").focus();
      }
      if (this.value !== oldValue) {
        this.updateComplete.then(() => {
          this.dispatchEvent(new InputEvent("input", { bubbles: true, composed: true }));
          this.dispatchEvent(new Event("change", { bubbles: true, composed: true }));
        });
      }
      event.preventDefault();
    }
    /** Sets focus on the radio group. */
    focus(options2) {
      if (this.disabled) {
        return;
      }
      const radios = this.getAllRadios();
      const checked = radios.find((radio) => radio.checked);
      const firstEnabledRadio = radios.find((radio) => !radio.disabled);
      const radioToFocus = checked || firstEnabledRadio;
      if (radioToFocus) {
        radioToFocus.focus(options2);
      }
    }
    render() {
      const hasLabelSlot = this.hasSlotController.test("label", "ssrLabel");
      const hasHintSlot = this.hasSlotController.test("hint", "ssrHint");
      const hasLabel = this.label ? true : !!hasLabelSlot;
      const hasHint = this.hint ? true : !!hasHintSlot;
      return b2`
      <fieldset
        part="form-control"
        class=${e7({
        "form-control": true,
        "form-control-radio-group": true,
        "form-control-has-label": hasLabel
      })}
        role="radiogroup"
        aria-labelledby="label"
        aria-describedby="hint"
        aria-errormessage="error-message"
        aria-orientation=${this.orientation}
      >
        <label
          part="form-control-label"
          id="label"
          class=${e7({
        label: true,
        "has-label": hasLabel
      })}
          aria-hidden=${hasLabel ? "false" : "true"}
          @click=${this.handleLabelClick}
        >
          <slot name="label">${this.label}</slot>
        </label>

        <slot part="form-control-input" @slotchange=${this.syncRadioElements}></slot>

        <slot
          id="hint"
          name="hint"
          part="hint"
          class=${e7({
        "has-slotted": hasHint
      })}
          aria-hidden=${hasHint ? "false" : "true"}
          >${this.hint}</slot
        >
      </fieldset>
    `;
    }
  };
  CsRadioGroup.css = [size_styles_default, form_control_styles_default, radio_group_styles_default];
  CsRadioGroup.shadowRootOptions = { ...CornerstoneFormAssociatedElement.shadowRootOptions, delegatesFocus: true };
  __decorateClass([
    e5("slot:not([name])")
  ], CsRadioGroup.prototype, "defaultSlot", 2);
  __decorateClass([
    n4()
  ], CsRadioGroup.prototype, "label", 2);
  __decorateClass([
    n4({ attribute: "hint" })
  ], CsRadioGroup.prototype, "hint", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsRadioGroup.prototype, "name", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsRadioGroup.prototype, "disabled", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsRadioGroup.prototype, "orientation", 2);
  __decorateClass([
    r5()
  ], CsRadioGroup.prototype, "value", 1);
  __decorateClass([
    n4({ attribute: "value", reflect: true })
  ], CsRadioGroup.prototype, "defaultValue", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsRadioGroup.prototype, "size", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsRadioGroup.prototype, "required", 2);
  __decorateClass([
    n4({ type: Boolean, attribute: "ssr-label" })
  ], CsRadioGroup.prototype, "ssrLabel", 2);
  __decorateClass([
    n4({ type: Boolean, attribute: "ssr-hint" })
  ], CsRadioGroup.prototype, "ssrHint", 2);
  CsRadioGroup = __decorateClass([
    customElement("cs-radio-group")
  ], CsRadioGroup);
  CsRadioGroup.disableWarning?.("change-in-update");

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.37VVCOZ2.js
  var CsContentChangeEvent = class extends Event {
    constructor(detail) {
      super("cs-content-change", { bubbles: true, cancelable: false, composed: true });
      this.detail = detail;
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.4MRPDNGL.js
  var random_content_styles_default = i`
  :host {
    display: contents;
  }

  /*
   * Force-hide unselected children. A bare [hidden] (display: none from the UA sheet) loses to any
   * author display set on the child — a utility class like .cs-flank, or a component's own
   * :host display — so children with their own layout wouldn't actually hide without this.
   */
  ::slotted([hidden]) {
    display: none !important;
  }

  /*
   * @keyframes are defined in both document scope (random-content.ts) and here:
   * Chromium resolves animation-name from the document for slotted elements;
   * WebKit resolves it from the shadow root. Both copies are needed.
   */

  @keyframes cs-rc-fade {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }

  @keyframes cs-rc-fade-up {
    from {
      opacity: 0;
      transform: translateY(var(--animation-translate, 0.5em));
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes cs-rc-fade-down {
    from {
      opacity: 0;
      transform: translateY(calc(-1 * var(--animation-translate, 0.5em)));
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes cs-rc-fade-left {
    from {
      opacity: 0;
      transform: translateX(var(--animation-translate, 0.5em));
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  @keyframes cs-rc-fade-right {
    from {
      opacity: 0;
      transform: translateX(calc(-1 * var(--animation-translate, 0.5em)));
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  /* The JS already skips animations under reduced motion; this guards CSS-only consumers too. */
  @media (prefers-reduced-motion: no-preference) {
    ::slotted([data-cs-animation]) {
      animation-duration: var(--animation-duration, 300ms);
      animation-timing-function: var(--animation-easing, ease);
      animation-fill-mode: both;
    }

    ::slotted([data-cs-animation='fade']) {
      animation-name: cs-rc-fade;
    }

    ::slotted([data-cs-animation='fade-up']) {
      animation-name: cs-rc-fade-up;
    }

    ::slotted([data-cs-animation='fade-down']) {
      animation-name: cs-rc-fade-down;
    }

    ::slotted([data-cs-animation='fade-left']) {
      animation-name: cs-rc-fade-left;
    }

    ::slotted([data-cs-animation='fade-right']) {
      animation-name: cs-rc-fade-right;
    }
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.P6CTBKUK.js
  if (typeof document !== "undefined") {
    const sheet = new CSSStyleSheet();
    sheet.replaceSync(`
    @keyframes cs-rc-fade {
      from { opacity: 0; }
      to { opacity: 1; }
    }
    @keyframes cs-rc-fade-up {
      from { opacity: 0; transform: translateY(var(--animation-translate, 0.5em)); }
      to { opacity: 1; transform: translateY(0); }
    }
    @keyframes cs-rc-fade-down {
      from { opacity: 0; transform: translateY(calc(-1 * var(--animation-translate, 0.5em))); }
      to { opacity: 1; transform: translateY(0); }
    }
    @keyframes cs-rc-fade-left {
      from { opacity: 0; transform: translateX(var(--animation-translate, 0.5em)); }
      to { opacity: 1; transform: translateX(0); }
    }
    @keyframes cs-rc-fade-right {
      from { opacity: 0; transform: translateX(calc(-1 * var(--animation-translate, 0.5em))); }
      to { opacity: 1; transform: translateX(0); }
    }
  `);
    document.adoptedStyleSheets = [...document.adoptedStyleSheets, sheet];
  }
  var CsRandomContent = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.sequenceCursor = 0;
      this.uniqueQueue = [];
      this.currentSelection = /* @__PURE__ */ new Set();
      this.isInitialSelection = true;
      this.autoplayController = new AutoplayController(this, () => this.randomize());
      this.animationCleanups = /* @__PURE__ */ new WeakMap();
      this.liveAnnouncement = "";
      this.items = 1;
      this.mode = "unique";
      this.autoplay = false;
      this.autoplayInterval = 3e3;
      this.animation = "none";
    }
    connectedCallback() {
      super.connectedCallback();
      if (this.hasUpdated) {
        this.syncAutoplay();
      }
    }
    firstUpdated(changedProperties) {
      super.firstUpdated(changedProperties);
      this.syncAutoplay();
    }
    handleAutoplayChange() {
      this.syncAutoplay();
    }
    handleModeChange() {
      this.sequenceCursor = 0;
      this.uniqueQueue = [];
      this.currentSelection.clear();
      this.randomize();
    }
    handleItemsChange() {
      this.uniqueQueue = [];
      this.randomize();
    }
    /** Selects a new set of children using the current mode. Returns the elements now shown. */
    randomize() {
      const children = this.assignedChildren();
      if (!children.length) {
        return [];
      }
      const count = Math.min(Math.max(1, this.items), children.length);
      let selected;
      if (this.mode === "sequence") {
        selected = [];
        Array.from({ length: count }).forEach((_2, i10) => {
          selected.push(children[(this.sequenceCursor + i10) % children.length]);
        });
        this.sequenceCursor = (this.sequenceCursor + count) % children.length;
      } else if (this.mode === "unique") {
        if (this.uniqueQueue.length < count) {
          const queued = new Set(this.uniqueQueue);
          const rest = children.filter((c6) => !this.currentSelection.has(c6) && !queued.has(c6));
          const current = children.filter((c6) => this.currentSelection.has(c6) && !queued.has(c6));
          this.uniqueQueue.push(...this.sample(rest, rest.length), ...this.sample(current, current.length));
          if (this.uniqueQueue.length < count) {
            this.uniqueQueue = this.sample([...children], children.length);
          }
        }
        selected = this.uniqueQueue.splice(0, count);
        this.currentSelection = new Set(selected);
      } else {
        const pool = children.filter((c6) => !this.currentSelection.has(c6));
        selected = this.sample(pool.length >= count ? pool : children, count);
        this.currentSelection = new Set(selected);
      }
      const firstShown = selected[0];
      const lastShown = selected[selected.length - 1];
      children.forEach((el) => {
        const htmlEl = el;
        const isSelected = selected.includes(el);
        delete htmlEl.dataset["csAnimation"];
        htmlEl.style.display = "";
        htmlEl.hidden = !isSelected;
        htmlEl.style.marginBlockStart = isSelected && el === firstShown ? "0" : "";
        htmlEl.style.marginBlockEnd = isSelected && el === lastShown ? "0" : "";
      });
      if (this.animation !== "none" && !prefersReducedMotion()) {
        selected.forEach((el) => {
          const htmlEl = el;
          if (this.animation !== "fade" && getComputedStyle(el).display === "inline") {
            htmlEl.style.display = "inline-block";
          }
          el.getAnimations().forEach((a4) => a4.cancel());
          htmlEl.dataset["csAnimation"] = this.animation;
          this.animationCleanups.get(el)?.abort();
          const cleanup = new AbortController();
          this.animationCleanups.set(el, cleanup);
          htmlEl.addEventListener("animationend", () => delete htmlEl.dataset["csAnimation"], {
            once: true,
            signal: cleanup.signal
          });
        });
      }
      if (this.isInitialSelection) {
        this.isInitialSelection = false;
      } else {
        this.liveAnnouncement = selected.map((el) => el.textContent?.trim()).filter(Boolean).join(", ");
      }
      this.dispatchEvent(new CsContentChangeEvent({ items: selected }));
      return selected;
    }
    syncAutoplay() {
      this.autoplayController.stop();
      if (this.autoplay && this.autoplayInterval > 0) {
        this.autoplayController.start(this.autoplayInterval);
      }
    }
    assignedChildren() {
      const slot = this.shadowRoot?.querySelector("slot");
      return slot?.assignedElements() ?? [];
    }
    // Fisher-Yates partial shuffle — picks `count` unique items from `pool`.
    sample(pool, count) {
      const arr = [...pool];
      Array.from({ length: count }).forEach((_2, i10) => {
        const j2 = i10 + Math.floor(Math.random() * (arr.length - i10));
        [arr[i10], arr[j2]] = [arr[j2], arr[i10]];
      });
      return arr.slice(0, count);
    }
    handleSlotChange() {
      this.randomize();
    }
    render() {
      return b2`
      <slot @slotchange=${this.handleSlotChange}></slot>
      <div class="cs-visually-hidden" role="status" aria-live="polite" aria-atomic="true">${this.liveAnnouncement}</div>
    `;
    }
  };
  CsRandomContent.css = [random_content_styles_default, visually_hidden_styles_default];
  __decorateClass([
    r5()
  ], CsRandomContent.prototype, "liveAnnouncement", 2);
  __decorateClass([
    n4({ type: Number })
  ], CsRandomContent.prototype, "items", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsRandomContent.prototype, "mode", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsRandomContent.prototype, "autoplay", 2);
  __decorateClass([
    n4({ type: Number, attribute: "autoplay-interval" })
  ], CsRandomContent.prototype, "autoplayInterval", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsRandomContent.prototype, "animation", 2);
  __decorateClass([
    watch(["autoplay", "autoplayInterval"], { waitUntilFirstUpdate: true })
  ], CsRandomContent.prototype, "handleAutoplayChange", 1);
  __decorateClass([
    watch("mode", { waitUntilFirstUpdate: true })
  ], CsRandomContent.prototype, "handleModeChange", 1);
  __decorateClass([
    watch("items", { waitUntilFirstUpdate: true })
  ], CsRandomContent.prototype, "handleItemsChange", 1);
  CsRandomContent = __decorateClass([
    customElement("cs-random-content")
  ], CsRandomContent);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.RISZO43T.js
  var CsHoverEvent = class extends Event {
    constructor(detail) {
      super("cs-hover", { bubbles: true, cancelable: false, composed: true });
      this.detail = detail;
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.PWPD4WGT.js
  var rating_styles_default = i`
  :host {
    --symbol-color: var(--cs-color-neutral-on-quiet);
    --symbol-color-active: var(--cs-color-yellow-70);
    --symbol-spacing: 0.125em;

    display: inline-flex;
    border-radius: var(--cs-border-radius-m);
    vertical-align: middle;
    touch-action: none;
  }

  :host(:focus) {
    outline: none;
  }

  :host(:focus-visible) {
    outline: var(--cs-focus-ring);
    outline-offset: var(--cs-focus-ring-offset);
  }

  .rating {
    position: relative;
    display: inline-flex;
  }

  .symbols {
    display: inline-flex;
    gap: 0.125em;
    position: relative;
    line-height: 0;
    color: var(--symbol-color);
    white-space: nowrap;
    cursor: pointer;
  }

  .symbols > * {
    padding: var(--symbol-spacing);
  }

  .symbol-active,
  .partial-filled {
    color: var(--symbol-color-active);
  }

  .partial-symbol-container {
    position: relative;
  }

  .partial-filled {
    position: absolute;
    top: var(--symbol-spacing);
    left: var(--symbol-spacing);
  }

  .symbol {
    transition: scale var(--cs-transition-normal) var(--cs-transition-easing);
    pointer-events: none;
  }

  .symbol-hover {
    scale: 1.2;
  }

  .rating-readonly .symbols {
    cursor: default;
  }

  :host([disabled]) .symbol-hover,
  .rating-readonly .symbol-hover {
    scale: none;
  }

  :host([disabled]) {
    opacity: 0.5;
  }

  :host([disabled]) .symbols {
    cursor: not-allowed;
  }

  /* Forced colors mode */
  @media (forced-colors: active) {
    .symbol-active {
      color: SelectedItem;
    }
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.H5E7N4LZ.js
  var CsRating = class extends CornerstoneFormAssociatedElement {
    constructor() {
      super(...arguments);
      this.assumeInteractionOn = ["change"];
      this.localize = new LocalizeController2(this);
      this.role = "slider";
      this.hoverValue = 0;
      this.isHovering = false;
      this.name = null;
      this.label = "";
      this.value = 0;
      this.defaultValue = 0;
      this.max = 5;
      this.precision = 1;
      this.readonly = false;
      this.required = false;
      this.getSymbol = (_value, isSelected) => {
        return isSelected ? '<cs-icon name="star" library="system" variant="fill"></cs-icon>' : '<cs-icon name="star" library="system"></cs-icon>';
      };
      this.size = "m";
      this.handleClick = (event) => {
        if (this.disabled) {
          return;
        }
        this.setRatingValue(this.getValueFromXCoordinate(event.clientX));
        this.updateComplete.then(() => {
          this.dispatchEvent(new Event("change", { bubbles: true, composed: true }));
        });
      };
      this.handleKeyDown = (event) => {
        const isLtr = this.matches(":dir(ltr)");
        const isRtl = this.localize.dir() === "rtl";
        const oldValue = this.value;
        if (this.disabled || this.readonly) {
          return;
        }
        if (event.key === "ArrowDown" || isLtr && event.key === "ArrowLeft" || isRtl && event.key === "ArrowRight") {
          const decrement = event.shiftKey ? 1 : this.precision;
          this.value = Math.max(0, this.value - decrement);
          event.preventDefault();
        }
        if (event.key === "ArrowUp" || isLtr && event.key === "ArrowRight" || isRtl && event.key === "ArrowLeft") {
          const increment = event.shiftKey ? 1 : this.precision;
          this.value = Math.min(this.max, this.value + increment);
          event.preventDefault();
        }
        if (event.key === "Home") {
          this.value = 0;
          event.preventDefault();
        }
        if (event.key === "End") {
          this.value = this.max;
          event.preventDefault();
        }
        if (this.value !== oldValue) {
          this.updateComplete.then(() => {
            this.dispatchEvent(new Event("change", { bubbles: true, composed: true }));
          });
        }
      };
      this.handlePointerEnter = (event) => {
        this.isHovering = true;
        this.hoverValue = this.getValueFromPointerPosition(event);
      };
      this.handlePointerMove = (event) => {
        this.hoverValue = this.getValueFromPointerPosition(event);
      };
      this.handlePointerLeave = () => {
        this.isHovering = false;
      };
      this.handlePointerDown = (event) => {
        if (event.button !== 0) {
          return;
        }
        this.isHovering = true;
        this.hoverValue = this.getValueFromPointerPosition(event);
        this.setPointerCapture(event.pointerId);
        event.preventDefault();
      };
      this.handlePointerUp = (event) => {
        this.releasePointerCapture(event.pointerId);
        this.isHovering = false;
      };
    }
    static get validators() {
      return [...super.validators, RequiredValidator()];
    }
    connectedCallback() {
      super.connectedCallback();
      this.setAttribute("aria-valuenow", String(this.value));
      this.setAttribute("aria-valuemin", "0");
      this.setAttribute("aria-valuemax", String(this.max));
      this.setAttribute("aria-disabled", this.disabled ? "true" : "false");
      this.setAttribute("aria-readonly", this.readonly ? "true" : "false");
      if (this.label) {
        this.setAttribute("aria-label", this.label);
      }
      if (!this.disabled && !this.readonly) {
        this.tabIndex = 0;
      } else {
        this.tabIndex = -1;
      }
      this.addEventListener("click", this.handleClick);
      this.addEventListener("keydown", this.handleKeyDown);
      this.addEventListener("pointerenter", this.handlePointerEnter);
      this.addEventListener("pointermove", this.handlePointerMove);
      this.addEventListener("pointerleave", this.handlePointerLeave);
      this.addEventListener("pointerdown", this.handlePointerDown);
      this.addEventListener("pointerup", this.handlePointerUp);
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this.removeEventListener("click", this.handleClick);
      this.removeEventListener("keydown", this.handleKeyDown);
      this.removeEventListener("pointerenter", this.handlePointerEnter);
      this.removeEventListener("pointermove", this.handlePointerMove);
      this.removeEventListener("pointerleave", this.handlePointerLeave);
      this.removeEventListener("pointerdown", this.handlePointerDown);
      this.removeEventListener("pointerup", this.handlePointerUp);
    }
    updated(changedProperties) {
      super.updated(changedProperties);
      if (changedProperties.has("value")) {
        this.setAttribute("aria-valuenow", String(this.value));
      }
      if (changedProperties.has("max")) {
        this.setAttribute("aria-valuemax", String(this.max));
      }
      if (changedProperties.has("disabled")) {
        this.setAttribute("aria-disabled", this.disabled ? "true" : "false");
        this.tabIndex = this.disabled || this.readonly ? -1 : 0;
      }
      if (changedProperties.has("readonly")) {
        this.setAttribute("aria-readonly", this.readonly ? "true" : "false");
        this.tabIndex = this.disabled || this.readonly ? -1 : 0;
      }
      if (changedProperties.has("label")) {
        if (this.label) {
          this.setAttribute("aria-label", this.label);
        } else {
          this.removeAttribute("aria-label");
        }
      }
    }
    getValueFromPointerPosition(event) {
      return this.getValueFromXCoordinate(event.clientX);
    }
    getValueFromXCoordinate(coordinate) {
      const isRtl = this.localize.dir() === "rtl";
      const { left, right, width } = this.getBoundingClientRect();
      const value = isRtl ? this.roundToPrecision((right - coordinate) / width * this.max, this.precision) : this.roundToPrecision((coordinate - left) / width * this.max, this.precision);
      return clamp(value, 0, this.max);
    }
    setRatingValue(newValue) {
      if (this.disabled || this.readonly) {
        return;
      }
      this.value = newValue === this.value ? 0 : newValue;
      this.isHovering = false;
    }
    roundToPrecision(numberToRound, precision = 0.5) {
      const multiplier = 1 / precision;
      return Math.ceil(numberToRound * multiplier) / multiplier;
    }
    handleHoverValueChange() {
      this.dispatchEvent(
        new CsHoverEvent({
          phase: "move",
          value: this.hoverValue
        })
      );
    }
    handleIsHoveringChange() {
      this.dispatchEvent(
        new CsHoverEvent({
          phase: this.isHovering ? "start" : "end",
          value: this.hoverValue
        })
      );
    }
    formResetCallback() {
      this.value = this.defaultValue;
      super.formResetCallback();
    }
    render() {
      const isRtl = this.didSSR && !this.hasUpdated ? this.dir : this.localize.dir() === "rtl";
      const counter = Array.from(Array(this.max).keys());
      let displayValue = 0;
      if (this.disabled || this.readonly) {
        displayValue = this.value;
      } else {
        displayValue = this.isHovering ? this.hoverValue : this.value;
      }
      return b2`
      <div
        part="rating"
        class=${e7({
        rating: true,
        "rating-readonly": this.readonly,
        "rating-disabled": this.disabled
      })}
      >
        <span class="symbols">
          ${counter.map((index) => {
        const isSelected = displayValue >= index + 1;
        if (displayValue > index && displayValue < index + 1) {
          return b2`
                <span
                  class=${e7({
            symbol: true,
            "partial-symbol-container": true,
            "symbol-hover": this.isHovering && Math.ceil(displayValue) === index + 1
          })}
                  role="presentation"
                >
                  <div
                    style=${o7({
            clipPath: isRtl ? `inset(0 ${(displayValue - index) * 100}% 0 0)` : `inset(0 0 0 ${(displayValue - index) * 100}%)`
          })}
                  >
                    ${o12(this.getSymbol(index + 1, false))}
                  </div>
                  <div
                    class="partial-filled"
                    style=${o7({
            clipPath: isRtl ? `inset(0 0 0 ${100 - (displayValue - index) * 100}%)` : `inset(0 ${100 - (displayValue - index) * 100}% 0 0)`
          })}
                  >
                    ${o12(this.getSymbol(index + 1, true))}
                  </div>
                </span>
              `;
        }
        return b2`
              <span
                class=${e7({
          symbol: true,
          "symbol-hover": this.isHovering && Math.ceil(displayValue) === index + 1,
          "symbol-active": displayValue >= index + 1
        })}
                role="presentation"
              >
                ${o12(this.getSymbol(index + 1, isSelected))}
              </span>
            `;
      })}
        </span>
      </div>
    `;
    }
  };
  CsRating.css = [size_styles_default, rating_styles_default];
  __decorateClass([
    n4({ reflect: true })
  ], CsRating.prototype, "role", 2);
  __decorateClass([
    r5()
  ], CsRating.prototype, "hoverValue", 2);
  __decorateClass([
    r5()
  ], CsRating.prototype, "isHovering", 2);
  __decorateClass([
    n4()
  ], CsRating.prototype, "name", 2);
  __decorateClass([
    n4()
  ], CsRating.prototype, "label", 2);
  __decorateClass([
    n4({ type: Number })
  ], CsRating.prototype, "value", 2);
  __decorateClass([
    n4({ type: Number, attribute: "default-value" })
  ], CsRating.prototype, "defaultValue", 2);
  __decorateClass([
    n4({ type: Number })
  ], CsRating.prototype, "max", 2);
  __decorateClass([
    n4({ type: Number })
  ], CsRating.prototype, "precision", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsRating.prototype, "readonly", 2);
  __decorateClass([
    n4({ type: Boolean })
  ], CsRating.prototype, "disabled", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsRating.prototype, "required", 2);
  __decorateClass([
    n4()
  ], CsRating.prototype, "getSymbol", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsRating.prototype, "size", 2);
  __decorateClass([
    watch("hoverValue")
  ], CsRating.prototype, "handleHoverValueChange", 1);
  __decorateClass([
    watch("isHovering")
  ], CsRating.prototype, "handleIsHoveringChange", 1);
  CsRating = __decorateClass([
    customElement("cs-rating")
  ], CsRating);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.3B5NC6A5.js
  var availableUnits = [
    { max: 276e4, value: 6e4, unit: "minute" },
    // max 46 minutes
    { max: 72e6, value: 36e5, unit: "hour" },
    // max 20 hours
    { max: 5184e5, value: 864e5, unit: "day" },
    // max 6 days
    { max: 24192e5, value: 6048e5, unit: "week" },
    // max 28 days
    { max: 28512e6, value: 2592e6, unit: "month" },
    // max 11 months
    { max: Infinity, value: 31536e6, unit: "year" }
  ];
  var CsRelativeTime = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.localize = new LocalizeController2(this);
      this.isoTime = "";
      this.relativeTime = "";
      this.date = /* @__PURE__ */ new Date();
      this.format = "long";
      this.numeric = "auto";
      this.sync = false;
      this.referenceDate = null;
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      clearTimeout(this.updateTimeout);
    }
    willUpdate(changedProperties) {
      const now = this.referenceDate || /* @__PURE__ */ new Date();
      const then = new Date(this.date);
      if (isNaN(then.getMilliseconds())) {
        this.relativeTime = "";
        this.isoTime = "";
        return super.willUpdate(changedProperties);
      }
      const diff = then.getTime() - now.getTime();
      const { unit, value } = availableUnits.find((singleUnit) => Math.abs(diff) < singleUnit.max);
      this.isoTime = then.toISOString();
      this.relativeTime = this.localize.relativeTime(Math.round(diff / value), unit, {
        numeric: this.numeric,
        style: this.format
      });
      clearTimeout(this.updateTimeout);
      if (this.sync) {
        let nextInterval;
        if (unit === "minute") {
          nextInterval = getTimeUntilNextUnit("second");
        } else if (unit === "hour") {
          nextInterval = getTimeUntilNextUnit("minute");
        } else if (unit === "day") {
          nextInterval = getTimeUntilNextUnit("hour");
        } else {
          nextInterval = getTimeUntilNextUnit("day");
        }
        this.updateTimeout = setTimeout(() => this.requestUpdate(), nextInterval);
      }
    }
    render() {
      if (this.relativeTime === "" && this.isoTime === "") {
        return "";
      }
      return b2`<time datetime=${this.isoTime}>${this.relativeTime}</time>`;
    }
  };
  __decorateClass([
    r5()
  ], CsRelativeTime.prototype, "isoTime", 2);
  __decorateClass([
    r5()
  ], CsRelativeTime.prototype, "relativeTime", 2);
  __decorateClass([
    n4()
  ], CsRelativeTime.prototype, "date", 2);
  __decorateClass([
    n4()
  ], CsRelativeTime.prototype, "format", 2);
  __decorateClass([
    n4()
  ], CsRelativeTime.prototype, "numeric", 2);
  __decorateClass([
    n4({ type: Boolean })
  ], CsRelativeTime.prototype, "sync", 2);
  __decorateClass([
    r5()
  ], CsRelativeTime.prototype, "referenceDate", 2);
  CsRelativeTime = __decorateClass([
    customElement("cs-relative-time")
  ], CsRelativeTime);
  function getTimeUntilNextUnit(unit) {
    const units = { second: 1e3, minute: 6e4, hour: 36e5, day: 864e5 };
    const value = units[unit];
    return value - Date.now() % value;
  }

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.AJRAHE5B.js
  var CsResizeEvent = class extends Event {
    constructor(detail) {
      super("cs-resize", { bubbles: true, cancelable: false, composed: true });
      this.detail = detail;
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.3TCBZHEE.js
  var resize_observer_styles_default = i`
  :host {
    display: contents;
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.Q2VXCA57.js
  var CsResizeObserver = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.observedElements = [];
      this.disabled = false;
    }
    connectedCallback() {
      super.connectedCallback();
      if (!o5) {
        this.resizeObserver = new ResizeObserver((entries) => {
          this.dispatchEvent(new CsResizeEvent({ entries }));
        });
        if (!this.disabled) {
          this.updateComplete.then(() => {
            this.startObserver();
          });
        }
      }
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this.stopObserver();
    }
    handleSlotChange() {
      if (!this.disabled) {
        this.startObserver();
      }
    }
    startObserver() {
      const slot = this.shadowRoot.querySelector("slot");
      if (slot !== null) {
        const elements = slot.assignedElements({ flatten: true });
        this.observedElements.forEach((el) => this.resizeObserver.unobserve(el));
        this.observedElements = [];
        elements.forEach((el) => {
          this.resizeObserver.observe(el);
          this.observedElements.push(el);
        });
      }
    }
    stopObserver() {
      this.resizeObserver.disconnect();
    }
    handleDisabledChange() {
      if (this.disabled) {
        this.stopObserver();
      } else {
        this.startObserver();
      }
    }
    render() {
      return b2` <slot @slotchange=${this.handleSlotChange}></slot> `;
    }
  };
  CsResizeObserver.css = resize_observer_styles_default;
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsResizeObserver.prototype, "disabled", 2);
  __decorateClass([
    watch("disabled", { waitUntilFirstUpdate: true })
  ], CsResizeObserver.prototype, "handleDisabledChange", 1);
  CsResizeObserver = __decorateClass([
    customElement("cs-resize-observer")
  ], CsResizeObserver);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.IHUWZ2BJ.js
  var scroller_styles_default = i`
  :host {
    --shadow-color: var(--cs-color-surface-default);
    --shadow-size: 2rem;

    /* private (defined dynamically) */
    --start-shadow-opacity: 0;
    --end-shadow-opacity: 0;

    display: block;
    position: relative;
    max-width: 100%;
    overflow: hidden;
    isolation: isolate;
  }

  :host([orientation='vertical']) {
    display: flex;
    flex-direction: column;
    height: 100%;
  }

  #content {
    z-index: 1; /* below shadows */
    border-radius: inherit;
    scroll-behavior: smooth;
    scrollbar-width: thin;

    /* Prevent text in mobile Safari from being larger when the container width larger than the viewport */
    -webkit-text-size-adjust: 100%;

    &:focus {
      outline: none;
    }

    &:focus-visible {
      outline: var(--cs-focus-ring);
      outline-offset: var(--cs-focus-ring-offset);
    }
  }

  :host([without-scrollbar]) #content {
    scrollbar-width: none;
  }

  :host([orientation='horizontal']) #content {
    overflow-x: auto;
    overflow-y: hidden;
  }

  :host([orientation='vertical']) #content {
    flex: 1 1 auto;
    min-height: 0; /* This is crucial for flex children to respect overflow */
    overflow-x: hidden;
    overflow-y: auto;
  }

  #start-shadow,
  #end-shadow {
    z-index: 2;
  }

  #start-shadow {
    opacity: var(--start-shadow-opacity);
  }

  #end-shadow {
    opacity: var(--end-shadow-opacity);
  }

  /* Horizontal shadows */
  :host([orientation='horizontal']) {
    #start-shadow,
    #end-shadow {
      position: absolute;
      top: 0;
      bottom: 0;
      width: var(--shadow-size);
      pointer-events: none;
    }

    #start-shadow {
      &:dir(ltr) {
        left: 0;
        background: linear-gradient(to right, var(--shadow-color), transparent 100%);
      }

      &:dir(rtl) {
        right: 0;
        background: linear-gradient(to left, var(--shadow-color), transparent 100%);
      }
    }

    #end-shadow {
      &:dir(ltr) {
        right: 0;
        background: linear-gradient(to left, var(--shadow-color), transparent 100%);
      }

      &:dir(rtl) {
        left: 0;
        background: linear-gradient(to right, var(--shadow-color), transparent 100%);
      }
    }
  }

  /* Vertical shadows */
  :host([orientation='vertical']) {
    #start-shadow,
    #end-shadow {
      position: absolute;
      right: 0;
      left: 0;
      height: var(--shadow-size);
      pointer-events: none;
    }

    #start-shadow {
      top: 0;
      background: linear-gradient(to bottom, var(--shadow-color), transparent 100%);
    }

    #end-shadow {
      bottom: 0;
      background: linear-gradient(to top, var(--shadow-color), transparent 100%);
    }
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.DMHAYTTX.js
  var CsScroller = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.localize = new LocalizeController2(this);
      this.resizeObserver = null;
      this.canScroll = false;
      this.orientation = "horizontal";
      this.withoutScrollbar = false;
      this.withoutShadow = false;
    }
    connectedCallback() {
      super.connectedCallback();
      if (!o5) {
        this.resizeObserver = new ResizeObserver(() => this.updateScroll());
        this.resizeObserver.observe(this);
      }
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this.resizeObserver?.disconnect();
    }
    handleKeyDown(event) {
      if (event.key === "Home") {
        event.preventDefault();
        this.content.scrollTo({
          left: this.orientation === "horizontal" ? 0 : void 0,
          top: this.orientation === "vertical" ? 0 : void 0
        });
      }
      if (event.key === "End") {
        event.preventDefault();
        this.content.scrollTo({
          left: this.orientation === "horizontal" ? this.content.scrollWidth : void 0,
          top: this.orientation === "vertical" ? this.content.scrollHeight : void 0
        });
      }
    }
    handleSlotChange() {
      this.updateScroll();
    }
    updateScroll() {
      if (this.orientation === "horizontal") {
        const clientWidth = Math.ceil(this.content.clientWidth);
        const scrollLeft = Math.abs(Math.ceil(this.content.scrollLeft));
        const scrollWidth = Math.ceil(this.content.scrollWidth);
        const maxScroll = scrollWidth - clientWidth;
        this.canScroll = maxScroll > 0;
        const startShadowOpacity = Math.min(1, scrollLeft / (maxScroll * 0.05));
        const endShadowOpacity = Math.min(1, (maxScroll - scrollLeft) / (maxScroll * 0.05));
        this.style.setProperty("--start-shadow-opacity", String(startShadowOpacity || 0));
        this.style.setProperty("--end-shadow-opacity", String(endShadowOpacity || 0));
      } else {
        const clientHeight = Math.ceil(this.content.clientHeight);
        const scrollTop = Math.abs(Math.ceil(this.content.scrollTop));
        const scrollHeight = Math.ceil(this.content.scrollHeight);
        const maxScroll = scrollHeight - clientHeight;
        this.canScroll = maxScroll > 0;
        const startShadowOpacity = Math.min(1, scrollTop / (maxScroll * 0.05));
        const endShadowOpacity = Math.min(1, (maxScroll - scrollTop) / (maxScroll * 0.05));
        this.style.setProperty("--start-shadow-opacity", String(startShadowOpacity || 0));
        this.style.setProperty("--end-shadow-opacity", String(endShadowOpacity || 0));
      }
    }
    render() {
      return b2`
      ${this.withoutShadow ? "" : b2`
              <div id="start-shadow" part="start-shadow" aria-hidden="true"></div>
              <div id="end-shadow" part="end-shadow" aria-hidden="true"></div>
            `}

      <div
        id="content"
        part="content"
        role="region"
        aria-label=${this.localize.term("scrollableRegion")}
        tabindex=${this.canScroll ? "0" : "-1"}
        @keydown=${this.handleKeyDown}
        @scroll=${this.updateScroll}
      >
        <slot @slotchange=${this.handleSlotChange}></slot>
      </div>
    `;
    }
  };
  CsScroller.css = [scroller_styles_default];
  __decorateClass([
    e5("#content")
  ], CsScroller.prototype, "content", 2);
  __decorateClass([
    r5()
  ], CsScroller.prototype, "canScroll", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsScroller.prototype, "orientation", 2);
  __decorateClass([
    n4({ attribute: "without-scrollbar", type: Boolean, reflect: true })
  ], CsScroller.prototype, "withoutScrollbar", 2);
  __decorateClass([
    n4({ attribute: "without-shadow", type: Boolean, reflect: true })
  ], CsScroller.prototype, "withoutShadow", 2);
  __decorateClass([
    t4({ passive: true })
  ], CsScroller.prototype, "updateScroll", 1);
  CsScroller = __decorateClass([
    customElement("cs-scroller")
  ], CsScroller);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.QSHPBN4O.js
  var select_styles_default = i`
  :host {
    --tag-max-size: 10ch;
    --show-duration: var(--cs-transition-fast);
    --hide-duration: var(--cs-transition-fast);
  }

  /* Add ellipses to multi select options */
  :host cs-tag::part(content) {
    display: initial;
    white-space: nowrap;
    text-overflow: ellipsis;
    overflow: hidden;
    max-width: var(--tag-max-size);
  }

  :host .disabled [part~='combobox'] {
    opacity: 0.5;
    cursor: not-allowed;
    outline: none;
  }

  :host .enabled:is(.open, :focus-within) [part~='combobox'] {
    outline-color: var(--cs-color-focus);
  }

  /** The popup */
  .select {
    flex: 1 1 auto;
    display: inline-flex;
    width: 100%;
    position: relative;
    vertical-align: middle;

    /* Pass through from select to the popup */
    --show-duration: inherit;
    --hide-duration: inherit;

    &::part(popup) {
      z-index: 900;
    }

    &[data-current-placement^='top']::part(popup) {
      transform-origin: bottom;
    }

    &[data-current-placement^='bottom']::part(popup) {
      transform-origin: top;
    }
  }

  /* Combobox */
  .combobox {
    flex: 1;
    display: flex;
    width: 100%;
    min-width: 0;
    align-items: center;
    justify-content: start;

    min-height: var(--cs-form-control-height);

    background-color: var(--cs-form-control-background-color);
    border-color: var(--cs-form-control-border-color);
    border-radius: var(--cs-form-control-border-radius);
    border-style: var(--cs-form-control-border-style);
    border-width: var(--cs-form-control-border-width);
    color: var(--cs-form-control-value-color);
    cursor: pointer;
    font-family: inherit;
    font-weight: var(--cs-form-control-value-font-weight);
    line-height: var(--cs-form-control-value-line-height);
    overflow: hidden;
    padding: 0 var(--cs-form-control-padding-inline);
    position: relative;
    vertical-align: middle;
    transition:
      background-color var(--cs-transition-normal),
      border-color var(--cs-transition-normal),
      outline-color var(--cs-transition-fast);
    transition-timing-function: var(--cs-transition-easing);
    outline: var(--cs-focus-ring-style) var(--cs-focus-ring-width) transparent;
    outline-offset: var(--cs-focus-ring-offset);

    /* Pills */
    :host([pill]) & {
      border-radius: var(--cs-border-radius-pill);
    }
  }

  /* Appearance modifiers */
  :host([appearance='outlined']) .combobox {
    background-color: var(--cs-form-control-background-color);
    border-color: var(--cs-form-control-border-color);
  }

  :host([appearance='filled']) .combobox {
    background-color: var(--cs-color-neutral-fill-quiet);
    border-color: var(--cs-color-neutral-fill-quiet);
  }

  :host([appearance='filled-outlined']) .combobox {
    background-color: var(--cs-color-neutral-fill-quiet);
    border-color: var(--cs-form-control-border-color);
  }

  .display-input {
    position: relative;
    width: 100%;
    font: inherit;
    border: none;
    background: none;
    line-height: var(--cs-form-control-value-line-height);
    color: var(--cs-form-control-value-color);
    cursor: inherit;
    overflow: hidden;
    padding: 0;
    margin: 0;
    -webkit-appearance: none;

    &:focus {
      outline: none;
    }

    &::placeholder {
      color: var(--cs-form-control-placeholder-color);
    }
  }

  /* Manage spacing when tags are present */
  :host([multiple]) {
    --_padding-with-tags: calc(var(--cs-form-control-height) * 0.1 - var(--cs-form-control-border-width));

    & .combobox:has(.tags cs-tag) {
      padding-block: var(--_padding-with-tags);
      padding-inline-start: var(--_padding-with-tags);
    }
  }

  /* Visually hide the display input when multiple is enabled */
  :host([multiple]) .combobox:has(.tags cs-tag) .display-input {
    position: absolute;
    z-index: -1;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    opacity: 0;
  }

  .value-input {
    position: absolute;
    z-index: -1;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    opacity: 0;
    padding: 0;
    margin: 0;
  }

  .tags {
    display: flex;
    flex: 1;
    align-items: center;
    flex-wrap: wrap;
    gap: 0.25em;

    /* These are !important because a document-level universal margin reset would otherwise beat them:
     a slotted element belongs to the outer tree. See callout.styles.ts for the full note. */
    &::slotted(cs-tag) {
      cursor: pointer !important;
    }

    .disabled &,
    .disabled &::slotted(cs-tag) {
      cursor: not-allowed !important;
    }
  }

  /* Start and End */

  .start,
  .end {
    flex: 0;
    display: inline-flex;
    align-items: center;
    color: var(--cs-color-neutral-on-quiet);
  }

  .end::slotted(*) {
    margin-inline-start: var(--cs-form-control-padding-inline) !important;
  }

  .start::slotted(*) {
    margin-inline-end: var(--cs-form-control-padding-inline) !important;
  }

  :host([multiple]) .combobox:has(.tags cs-tag) .start::slotted(*) {
    margin-inline-start: calc(var(--cs-form-control-padding-inline) - var(--_padding-with-tags)) !important;
  }

  /* Clear button */
  [part~='clear-button'] {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    font-size: inherit;
    color: var(--cs-color-neutral-on-quiet);
    border: none;
    background: none;
    padding: 0;
    transition: color var(--cs-transition-normal);
    cursor: pointer;
    margin-inline-start: var(--cs-form-control-padding-inline);

    &:focus {
      outline: none;
    }

    @media (hover: hover) {
      &:hover {
        color: color-mix(in oklab, currentColor, var(--cs-color-mix-hover));
      }
    }

    &:active {
      color: color-mix(in oklab, currentColor, var(--cs-color-mix-active));
    }
  }

  /* Expand icon */
  .expand-icon {
    flex: 0 0 auto;
    display: flex;
    align-items: center;
    color: var(--cs-color-neutral-on-quiet);
    transition: rotate var(--cs-transition-slow) var(--cs-transition-easing);
    rotate: 0deg;
    margin-inline-start: var(--cs-form-control-padding-inline);

    .open & {
      rotate: -180deg;
    }
  }

  /* Listbox */
  .listbox {
    display: block;
    position: relative;
    font: inherit;
    box-shadow: var(--cs-shadow-m);
    background: var(--cs-color-surface-raised);
    border-color: var(--cs-color-surface-border);
    border-radius: var(--cs-border-radius-m);
    border-style: var(--cs-border-style);
    border-width: var(--cs-border-width-s);
    padding: 0.25em;
    overflow: auto;
    overscroll-behavior: none;

    /* Make sure it adheres to the popup's auto size */
    max-width: var(--auto-size-available-width);
    max-height: var(--auto-size-available-height);

    &::slotted(cs-divider) {
      --spacing: 0.5em;
    }
  }

  /* Space options with half the listbox's padding */
  .listbox slot:not([name]) {
    display: flex;
    flex-direction: column;
    gap: 0.125em;
  }

  slot:not([name])::slotted(small) {
    display: block;
    font-size: var(--cs-font-size-smaller);
    font-weight: var(--cs-font-weight-semibold);
    color: var(--cs-color-text-quiet);
    padding-block: 0.5em;
    padding-inline: 2.25em;
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.WHC2RXRQ.js
  var CsSelect = class extends CornerstoneFormAssociatedElement {
    constructor() {
      super(...arguments);
      this.assumeInteractionOn = ["blur", "input"];
      this.cachedOptions = null;
      this.hasSlotController = new HasSlotController(this, "hint", "label");
      this.localize = new LocalizeController2(this);
      this.selectionOrder = /* @__PURE__ */ new Map();
      this.typeToSelectString = "";
      this.slotChangePending = false;
      this.displayLabel = "";
      this.selectedOptions = [];
      this.name = "";
      this._defaultValue = null;
      this.size = "m";
      this.placeholder = "";
      this.multiple = false;
      this.maxOptionsVisible = 3;
      this.disabled = false;
      this.withClear = false;
      this.open = false;
      this.appearance = "outlined";
      this.pill = false;
      this.label = "";
      this.placement = "bottom";
      this.hint = "";
      this.ssrLabel = false;
      this.ssrHint = false;
      this.required = false;
      this.getTag = (option) => {
        return b2`
      <cs-tag
        part="tag"
        exportparts="
            content:tag__content,
            remove-button:tag__remove-button,
            remove-button__button:tag__remove-button__button
          "
        ?pill=${this.pill}
        size=${this.size}
        with-remove
        data-value=${option.value}
        @cs-remove=${(event) => this.handleTagRemove(event, option)}
      >
        ${option.label}
      </cs-tag>
    `;
      };
      this.handleDocumentFocusIn = (event) => {
        const path = event.composedPath();
        if (!path.includes(this)) {
          this.hide();
        }
      };
      this.handleDocumentKeyDown = (event) => {
        const target = event.target;
        const isClearButton = target.closest('[part~="clear-button"]') !== null;
        const isButton = target.closest("cs-button") !== null;
        if (isClearButton || isButton) {
          return;
        }
        if (event.key === "Escape" && this.open && isTopDismissible(this)) {
          event.preventDefault();
          event.stopPropagation();
          this.hide();
          this.displayInput.focus({ preventScroll: true });
        }
        if (event.key === "Enter" || event.key === " " && this.typeToSelectString === "") {
          event.preventDefault();
          event.stopImmediatePropagation();
          if (!this.open) {
            this.show();
            return;
          }
          if (this.currentOption && !this.currentOption.disabled) {
            this.valueHasChanged = true;
            this.hasInteracted = true;
            if (this.multiple) {
              this.toggleOptionSelection(this.currentOption);
            } else {
              this.setSelectedOptions(this.currentOption);
            }
            this.updateComplete.then(() => {
              this.dispatchEvent(new InputEvent("input", { bubbles: true, composed: true }));
              this.dispatchEvent(new Event("change", { bubbles: true, composed: true }));
            });
            if (!this.multiple) {
              this.hide();
              this.displayInput.focus({ preventScroll: true });
            }
          }
          return;
        }
        if (["ArrowUp", "ArrowDown", "Home", "End"].includes(event.key)) {
          const allOptions = this.getAllOptions();
          const currentIndex = allOptions.indexOf(this.currentOption);
          let newIndex = Math.max(0, currentIndex);
          event.preventDefault();
          if (!this.open) {
            this.show();
            if (this.currentOption) {
              return;
            }
          }
          if (event.key === "ArrowDown") {
            newIndex = currentIndex + 1;
            if (newIndex > allOptions.length - 1) {
              newIndex = 0;
            }
          } else if (event.key === "ArrowUp") {
            newIndex = currentIndex - 1;
            if (newIndex < 0) {
              newIndex = allOptions.length - 1;
            }
          } else if (event.key === "Home") {
            newIndex = 0;
          } else if (event.key === "End") {
            newIndex = allOptions.length - 1;
          }
          this.setCurrentOption(allOptions[newIndex]);
        }
        if (event.key?.length === 1 || event.key === "Backspace") {
          const allOptions = this.getAllOptions();
          if (event.metaKey || event.ctrlKey || event.altKey) {
            return;
          }
          if (!this.open) {
            if (event.key === "Backspace") {
              return;
            }
            this.show();
          }
          event.stopPropagation();
          event.preventDefault();
          clearTimeout(this.typeToSelectTimeout);
          this.typeToSelectTimeout = window.setTimeout(() => this.typeToSelectString = "", 1e3);
          if (event.key === "Backspace") {
            this.typeToSelectString = this.typeToSelectString.slice(0, -1);
          } else {
            this.typeToSelectString += event.key.toLowerCase();
          }
          for (const option of allOptions) {
            const label = option.label.toLowerCase();
            if (label.startsWith(this.typeToSelectString)) {
              this.setCurrentOption(option);
              break;
            }
          }
        }
      };
      this.handleDocumentMouseDown = (event) => {
        const path = event.composedPath();
        if (!path.includes(this)) {
          this.hide();
        }
      };
    }
    static get validators() {
      return [
        ...super.validators,
        RequiredValidator({
          validationElement: () => Object.assign(document.createElement("select"), { required: true })
        })
      ];
    }
    /** Where to anchor native constraint validation */
    get validationTarget() {
      return this.valueInput;
    }
    set defaultValue(val) {
      this._defaultValue = this.convertDefaultValue(val);
    }
    get defaultValue() {
      return this.convertDefaultValue(this._defaultValue);
    }
    rawValuesEqual(a4, b3) {
      if (a4 == null && b3 == null) {
        return true;
      }
      if (a4 == null || b3 == null) {
        return false;
      }
      if (a4.length !== b3.length) {
        return false;
      }
      return a4.every((v2, i10) => v2 === b3[i10]);
    }
    /**
     * @private
     * A converter for defaultValue from array to string if its multiple. Also fixes some hydration issues.
     */
    convertDefaultValue(val) {
      const isMultiple = this.multiple || this.hasAttribute("multiple");
      if (!isMultiple && Array.isArray(val)) {
        val = val[0];
      }
      return val;
    }
    set value(val) {
      const oldValue = this.value;
      if (val instanceof FormData) {
        val = val.getAll(this.name);
      }
      if (val != null && !Array.isArray(val)) {
        val = [val];
      }
      const oldRawValue = this._value;
      this._value = val ?? null;
      if (!this.rawValuesEqual(oldRawValue, this._value)) {
        this.valueHasChanged = true;
        this.requestUpdate("value", oldValue);
      }
    }
    get value() {
      let value = this._value ?? this.defaultValue ?? null;
      if (value != null) {
        value = Array.isArray(value) ? value : [value];
      }
      this.optionValues = new Set(
        this.getAllOptions().filter((option) => !option.disabled).map((option) => option.value)
      );
      let ret = value;
      if (value != null) {
        ret = value.filter((v2) => this.optionValues.has(v2));
        ret = this.multiple ? ret : ret[0];
        ret = ret ?? null;
      }
      return ret;
    }
    connectedCallback() {
      super.connectedCallback();
      this.processSlotChange();
      this.open = false;
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this.removeOpenListeners();
      this.cachedOptions = null;
    }
    updateDefaultValue() {
      const allOptions = this.getAllOptions();
      const defaultSelectedOptions = allOptions.filter((el) => el.hasAttribute("selected") || el.defaultSelected);
      if (defaultSelectedOptions.length > 0) {
        const selectedValues = defaultSelectedOptions.map((el) => el.value);
        this._defaultValue = this.multiple ? selectedValues : selectedValues[0];
      }
      if (this.hasAttribute("value")) {
        this._defaultValue = this.getAttribute("value") || null;
      }
    }
    addOpenListeners() {
      document.addEventListener("focusin", this.handleDocumentFocusIn);
      document.addEventListener("keydown", this.handleDocumentKeyDown);
      document.addEventListener("mousedown", this.handleDocumentMouseDown);
      registerDismissible(this);
      if (this.getRootNode() !== document) {
        this.getRootNode().addEventListener("focusin", this.handleDocumentFocusIn);
      }
    }
    removeOpenListeners() {
      document.removeEventListener("focusin", this.handleDocumentFocusIn);
      document.removeEventListener("keydown", this.handleDocumentKeyDown);
      document.removeEventListener("mousedown", this.handleDocumentMouseDown);
      unregisterDismissible(this);
      if (this.getRootNode() !== document) {
        this.getRootNode().removeEventListener("focusin", this.handleDocumentFocusIn);
      }
    }
    handleFocus() {
      this.displayInput.setSelectionRange(0, 0);
    }
    handleLabelClick() {
      this.displayInput.focus();
    }
    handleComboboxClick(event) {
      event.preventDefault();
    }
    handleComboboxMouseDown(event) {
      const path = event.composedPath();
      const isButton = path.some((el) => el instanceof Element && el.tagName.toLowerCase() === "cs-button");
      if (this.disabled || isButton) {
        return;
      }
      event.preventDefault();
      this.displayInput.focus({ preventScroll: true });
      this.open = !this.open;
    }
    handleComboboxKeyDown(event) {
      event.stopPropagation();
      this.handleDocumentKeyDown(event);
    }
    handleClearClick(event) {
      event.stopPropagation();
      this.hasInteracted = true;
      this.valueHasChanged = true;
      if (this.value !== null) {
        this.displayLabel = "";
        this.selectionOrder.clear();
        this.setSelectedOptions([]);
        this.displayInput.focus({ preventScroll: true });
        this.updateComplete.then(() => {
          this.dispatchEvent(new CsClearEvent());
          this.dispatchEvent(new InputEvent("input", { bubbles: true, composed: true }));
          this.dispatchEvent(new Event("change", { bubbles: true, composed: true }));
        });
      }
    }
    handleClearMouseDown(event) {
      event.stopPropagation();
      event.preventDefault();
    }
    handleOptionClick(event) {
      const target = event.target;
      const option = target.closest("cs-option");
      if (option && !option.disabled) {
        this.hasInteracted = true;
        this.valueHasChanged = true;
        if (this.multiple) {
          this.toggleOptionSelection(option);
        } else {
          this.setSelectedOptions(option);
        }
        this.updateComplete.then(() => this.displayInput.focus({ preventScroll: true }));
        this.requestUpdate("value");
        this.updateComplete.then(() => {
          this.dispatchEvent(new InputEvent("input", { bubbles: true, composed: true }));
          this.dispatchEvent(new Event("change", { bubbles: true, composed: true }));
        });
        if (!this.multiple) {
          this.hide();
          this.displayInput.focus({ preventScroll: true });
        }
      }
    }
    /* @internal - used by options to update labels */
    handleDefaultSlotChange() {
      if (this.slotChangePending) {
        return;
      }
      this.slotChangePending = true;
      queueMicrotask(() => {
        this.slotChangePending = false;
        this.processSlotChange();
      });
    }
    processSlotChange() {
      if (!customElements.get("cs-option")) {
        customElements.whenDefined("cs-option").then(() => this.handleDefaultSlotChange());
      }
      if (this.didSSR && !this.hasUpdated) {
        this.updateComplete.then(() => {
          this.handleDefaultSlotChange();
        });
        return;
      }
      this.cachedOptions = null;
      const allOptions = this.getAllOptions();
      this.updateDefaultValue();
      let value = this.value;
      if (value == null || !this.valueHasChanged && !this.hasInteracted) {
        this.selectionChanged();
        return;
      }
      if (!Array.isArray(value)) {
        value = [value];
      }
      const selectedOptions = allOptions.filter((el) => value.includes(el.value));
      this.setSelectedOptions(selectedOptions);
    }
    handleTagRemove(event, directOption) {
      event.stopPropagation();
      if (this.disabled) {
        return;
      }
      this.hasInteracted = true;
      this.valueHasChanged = true;
      let option = directOption;
      if (!option) {
        const tagElement = event.target.closest("cs-tag[data-value]");
        if (tagElement) {
          const value = tagElement.dataset.value;
          option = this.selectedOptions.find((opt) => opt.value === value);
        }
      }
      if (option) {
        this.toggleOptionSelection(option, false);
        this.updateComplete.then(() => {
          this.dispatchEvent(new InputEvent("input", { bubbles: true, composed: true }));
          this.dispatchEvent(new Event("change", { bubbles: true, composed: true }));
        });
      }
    }
    // Gets an array of all `<cs-option>` elements
    getAllOptions() {
      if (this.cachedOptions) {
        return this.cachedOptions;
      }
      if (!this?.querySelectorAll) {
        return [];
      }
      this.cachedOptions = [...this.querySelectorAll("cs-option")];
      return this.cachedOptions;
    }
    // Gets the first `<cs-option>` element
    getFirstOption() {
      return this.querySelector("cs-option");
    }
    // Sets the current option, which is the option the user is currently interacting with (e.g. via keyboard). Only one
    // option may be "current" at a time.
    setCurrentOption(option) {
      const allOptions = this.getAllOptions();
      allOptions.forEach((el) => {
        el.current = false;
        el.tabIndex = -1;
      });
      if (option) {
        this.currentOption = option;
        option.current = true;
        option.tabIndex = 0;
        option.focus({ preventScroll: true });
        if (this.open && !this.listbox.hidden) {
          scrollIntoView(option, this.listbox, "vertical", "auto");
        }
      }
    }
    // Sets the selected option(s)
    setSelectedOptions(option) {
      const allOptions = this.getAllOptions();
      const newSelectedOptions = Array.isArray(option) ? option : [option];
      allOptions.forEach((el) => {
        if (newSelectedOptions.includes(el)) {
          return;
        }
        el.selected = false;
      });
      if (newSelectedOptions.length) {
        newSelectedOptions.forEach((el) => el.selected = true);
      }
      this.selectionChanged();
    }
    // Toggles an option's selected state
    toggleOptionSelection(option, force) {
      if (force === true || force === false) {
        option.selected = force;
      } else {
        option.selected = !option.selected;
      }
      this.selectionChanged();
    }
    // @internal This method must be called whenever the selection changes. It will update the selected options cache, the
    // current value, and the display value. The option component uses it internally to update labels as they change.
    selectionChanged() {
      const options2 = this.getAllOptions();
      const newSelectedOptions = options2.filter((el) => {
        if (!this.hasInteracted && !this.valueHasChanged) {
          const defaultValue = this.defaultValue;
          const defaultValues = Array.isArray(defaultValue) ? defaultValue : [defaultValue];
          return el.hasAttribute("selected") || el.defaultSelected || el.selected || defaultValues.includes(el.value);
        }
        return el.selected;
      });
      const newSelectedValues = new Set(newSelectedOptions.map((el) => el.value));
      for (const value of this.selectionOrder.keys()) {
        if (!newSelectedValues.has(value)) {
          this.selectionOrder.delete(value);
        }
      }
      const maxOrder = this.selectionOrder.size > 0 ? Math.max(...this.selectionOrder.values()) : -1;
      let nextOrder = maxOrder + 1;
      for (const option of newSelectedOptions) {
        if (!this.selectionOrder.has(option.value)) {
          this.selectionOrder.set(option.value, nextOrder++);
        }
      }
      this.selectedOptions = newSelectedOptions.sort((a4, b3) => {
        const orderA = this.selectionOrder.get(a4.value) ?? 0;
        const orderB = this.selectionOrder.get(b3.value) ?? 0;
        return orderA - orderB;
      });
      const selectedValues = new Set(this.selectedOptions.map((el) => el.value));
      if (selectedValues.size > 0 || this._value) {
        const oldValue = this._value;
        if (this._value == null) {
          const value = this.defaultValue ?? [];
          this._value = Array.isArray(value) ? value : [value];
        }
        this._value = this._value?.filter((value) => !this.optionValues?.has(value)) ?? null;
        this._value?.unshift(...selectedValues);
        this.requestUpdate("value", oldValue);
      }
      if (this.multiple) {
        if (this.placeholder && !this.value?.length) {
          this.displayLabel = "";
        } else {
          this.displayLabel = this.localize.term("numOptionsSelected", this.selectedOptions.length);
        }
      } else {
        const selectedOption = this.selectedOptions[0];
        this.displayLabel = selectedOption?.label ?? "";
      }
      this.updateComplete.then(() => {
        this.updateValidity();
      });
    }
    get tags() {
      return this.selectedOptions.map((option, index) => {
        if (index < this.maxOptionsVisible || this.maxOptionsVisible <= 0) {
          const tag2 = this.getTag(option, index);
          if (!tag2) {
            return null;
          }
          return typeof tag2 === "string" ? o12(tag2) : tag2;
        } else if (index === this.maxOptionsVisible) {
          return b2`
          <cs-tag
            part="tag"
            exportparts="
              content:tag__content,
              remove-button:tag__remove-button,
              remove-button__button:tag__remove-button__button
            "
            >+${this.selectedOptions.length - index}</cs-tag
          >
        `;
        }
        return null;
      });
    }
    updated(changedProperties) {
      super.updated(changedProperties);
      if (changedProperties.has("value") || changedProperties.has("displayLabel")) {
        this.customStates.set("blank", !this.value && !this.displayLabel);
      }
    }
    handleDisabledChange() {
      if (this.disabled && this.open) {
        this.open = false;
      }
    }
    handleValueChange() {
      const allOptions = this.getAllOptions();
      const value = Array.isArray(this.value) ? this.value : [this.value];
      const selectedOptions = allOptions.filter((el) => value.includes(el.value));
      this.setSelectedOptions(selectedOptions);
      this.updateValidity();
    }
    async handleOpenChange() {
      if (this.open && !this.disabled) {
        this.setCurrentOption(this.selectedOptions[0] || this.getFirstOption());
        const csShowEvent = new CsShowEvent();
        this.dispatchEvent(csShowEvent);
        if (csShowEvent.defaultPrevented) {
          this.open = false;
          return;
        }
        this.addOpenListeners();
        this.listbox.hidden = false;
        this.popup.active = true;
        requestAnimationFrame(() => {
          this.setCurrentOption(this.currentOption);
        });
        await animateWithClass(this.popup.popup, "show");
        if (this.currentOption) {
          scrollIntoView(this.currentOption, this.listbox, "vertical", "auto");
        }
        this.dispatchEvent(new CsAfterShowEvent());
      } else {
        const csHideEvent = new CsHideEvent();
        this.dispatchEvent(csHideEvent);
        if (csHideEvent.defaultPrevented) {
          this.open = false;
          return;
        }
        this.removeOpenListeners();
        await animateWithClass(this.popup.popup, "hide");
        this.listbox.hidden = true;
        this.popup.active = false;
        this.dispatchEvent(new CsAfterHideEvent());
      }
    }
    /** Shows the listbox. */
    async show() {
      if (this.open || this.disabled) {
        this.open = false;
        return void 0;
      }
      this.open = true;
      return waitForEvent(this, "cs-after-show");
    }
    /** Hides the listbox. */
    async hide() {
      if (!this.open || this.disabled) {
        this.open = false;
        return void 0;
      }
      this.open = false;
      return waitForEvent(this, "cs-after-hide");
    }
    /** Sets focus on the control. */
    focus(options2) {
      this.displayInput.focus(options2);
    }
    /** Removes focus from the control. */
    blur() {
      this.displayInput.blur();
    }
    formResetCallback() {
      this.selectionOrder.clear();
      this.value = this.defaultValue;
      super.formResetCallback();
      this.handleValueChange();
      this.updateComplete.then(() => {
        this.dispatchEvent(new InputEvent("input", { bubbles: true, composed: true }));
        this.dispatchEvent(new Event("change", { bubbles: true, composed: true }));
      });
    }
    render() {
      const hasLabelSlot = this.hasSlotController.test("label", "ssrLabel");
      const hasHintSlot = this.hasSlotController.test("hint", "ssrHint");
      const hasLabel = this.label ? true : !!hasLabelSlot;
      const hasHint = this.hint ? true : !!hasHintSlot;
      const hasClearIcon = (this.hasUpdated || o5) && this.withClear && !this.disabled && (this.displayLabel || this.value && this.value.length > 0);
      return b2`
      <div
        part="form-control"
        class=${e7({
        "form-control": true,
        "form-control-has-label": hasLabel
      })}
      >
        <label
          id="label"
          part="form-control-label"
          class=${e7({
        label: true,
        "has-label": hasLabel
      })}
          aria-hidden=${hasLabel ? "false" : "true"}
          @click=${this.handleLabelClick}
        >
          <slot name="label">${this.label}</slot>
        </label>

        <div part="form-control-input" class="form-control-input">
          <cs-popup
            class=${e7({
        select: true,
        open: this.open,
        disabled: this.disabled,
        enabled: !this.disabled,
        multiple: this.multiple
      })}
            placement=${this.placement}
            flip
            shift
            sync="width"
            auto-size="vertical"
            auto-size-padding="10"
          >
            <div
              part="combobox"
              class="combobox"
              slot="anchor"
              @keydown=${this.handleComboboxKeyDown}
              @mousedown=${this.handleComboboxMouseDown}
              @click=${this.handleComboboxClick}
            >
              <slot part="start" name="start" class="start"></slot>

              <input
                part="display-input"
                class="display-input"
                type="text"
                placeholder=${this.placeholder}
                .disabled=${this.disabled}
                .value=${this.displayLabel}
                ?required=${this.required}
                autocomplete="off"
                spellcheck="false"
                autocapitalize="off"
                readonly
                aria-invalid=${!this.validity.valid}
                aria-controls="listbox"
                aria-expanded=${this.open ? "true" : "false"}
                aria-haspopup="listbox"
                aria-labelledby="label"
                aria-disabled=${this.disabled ? "true" : "false"}
                aria-describedby="hint"
                role="combobox"
                tabindex="0"
                @focus=${this.handleFocus}
              />

              \x3C!-- Tags need to wait for first hydration before populating otherwise it will create a hydration mismatch. -->
              ${this.multiple && this.hasUpdated ? b2`<div part="tags" class="tags" @cs-remove=${this.handleTagRemove}>${this.tags}</div>` : ""}

              <input
                class="value-input"
                type="text"
                ?disabled=${this.disabled}
                ?required=${this.required}
                .value=${Array.isArray(this.value) ? this.value.join(", ") : this.value}
                tabindex="-1"
                aria-hidden="true"
                @focus=${() => this.focus()}
              />

              ${hasClearIcon ? b2`
                      <button
                        part="clear-button"
                        type="button"
                        aria-label=${this.localize.term("clearEntry")}
                        @mousedown=${this.handleClearMouseDown}
                        @click=${this.handleClearClick}
                        tabindex="-1"
                      >
                        <slot name="clear-icon">
                          <cs-icon name="cancel" library="system"></cs-icon>
                        </slot>
                      </button>
                    ` : ""}

              <slot name="end" part="end" class="end"></slot>

              <slot name="expand-icon" part="expand-icon" class="expand-icon">
                <cs-icon library="system" name="keyboard_arrow_down"></cs-icon>
              </slot>
            </div>

            <div
              id="listbox"
              role="listbox"
              aria-expanded=${this.open ? "true" : "false"}
              aria-multiselectable=${this.multiple ? "true" : "false"}
              aria-labelledby="label"
              part="listbox"
              class="listbox"
              tabindex="-1"
              @mouseup=${this.handleOptionClick}
            >
              <slot @slotchange=${this.handleDefaultSlotChange}></slot>
            </div>
          </cs-popup>
        </div>

        <slot
          id="hint"
          name="hint"
          part="hint"
          class=${e7({
        "has-slotted": hasHint
      })}
          aria-hidden=${hasHint ? "false" : "true"}
          >${this.hint}</slot
        >
      </div>
    `;
    }
  };
  CsSelect.css = [select_styles_default, form_control_styles_default, size_styles_default];
  __decorateClass([
    e5(".select")
  ], CsSelect.prototype, "popup", 2);
  __decorateClass([
    e5(".combobox")
  ], CsSelect.prototype, "combobox", 2);
  __decorateClass([
    e5(".display-input")
  ], CsSelect.prototype, "displayInput", 2);
  __decorateClass([
    e5(".value-input")
  ], CsSelect.prototype, "valueInput", 2);
  __decorateClass([
    e5(".listbox")
  ], CsSelect.prototype, "listbox", 2);
  __decorateClass([
    r5()
  ], CsSelect.prototype, "displayLabel", 2);
  __decorateClass([
    r5()
  ], CsSelect.prototype, "currentOption", 2);
  __decorateClass([
    r5()
  ], CsSelect.prototype, "selectedOptions", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsSelect.prototype, "name", 2);
  __decorateClass([
    n4({
      attribute: false
    })
  ], CsSelect.prototype, "defaultValue", 1);
  __decorateClass([
    n4({ attribute: "value", reflect: false })
  ], CsSelect.prototype, "value", 1);
  __decorateClass([
    n4({ reflect: true })
  ], CsSelect.prototype, "size", 2);
  __decorateClass([
    n4()
  ], CsSelect.prototype, "placeholder", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsSelect.prototype, "multiple", 2);
  __decorateClass([
    n4({ attribute: "max-options-visible", type: Number })
  ], CsSelect.prototype, "maxOptionsVisible", 2);
  __decorateClass([
    n4({ type: Boolean })
  ], CsSelect.prototype, "disabled", 2);
  __decorateClass([
    n4({ attribute: "with-clear", type: Boolean })
  ], CsSelect.prototype, "withClear", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsSelect.prototype, "open", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsSelect.prototype, "appearance", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsSelect.prototype, "pill", 2);
  __decorateClass([
    n4()
  ], CsSelect.prototype, "label", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsSelect.prototype, "placement", 2);
  __decorateClass([
    n4({ attribute: "hint" })
  ], CsSelect.prototype, "hint", 2);
  __decorateClass([
    n4({ attribute: "ssr-label", type: Boolean })
  ], CsSelect.prototype, "ssrLabel", 2);
  __decorateClass([
    n4({ attribute: "ssr-hint", type: Boolean })
  ], CsSelect.prototype, "ssrHint", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsSelect.prototype, "required", 2);
  __decorateClass([
    n4({ attribute: false })
  ], CsSelect.prototype, "getTag", 2);
  __decorateClass([
    watch("disabled", { waitUntilFirstUpdate: true })
  ], CsSelect.prototype, "handleDisabledChange", 1);
  __decorateClass([
    watch("value", { waitUntilFirstUpdate: true })
  ], CsSelect.prototype, "handleValueChange", 1);
  __decorateClass([
    watch("open", { waitUntilFirstUpdate: true })
  ], CsSelect.prototype, "handleOpenChange", 1);
  CsSelect = __decorateClass([
    customElement("cs-select")
  ], CsSelect);
  CsSelect.disableWarning?.("change-in-update");

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.YS5PIYHC.js
  var CsRemoveEvent = class extends Event {
    constructor() {
      super("cs-remove", { bubbles: true, cancelable: false, composed: true });
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.L4JLRQUB.js
  var tag_styles_default = i`
  @layer cs-component {
    :host {
      display: inline-flex;
      gap: 0.5em;
      border-radius: var(--cs-border-radius-m);
      align-items: center;
      background-color: var(--cs-color-fill-quiet, var(--cs-color-neutral-fill-quiet));
      border-color: var(--cs-color-border-normal, var(--cs-color-neutral-border-normal));
      border-style: var(--cs-border-style);
      border-width: var(--cs-border-width-s);
      color: var(--cs-color-on-quiet, var(--cs-color-neutral-on-quiet));
      font-size: inherit;
      line-height: 1;
      white-space: nowrap;
      user-select: none;
      -webkit-user-select: none;
      height: calc(var(--cs-form-control-height) * 0.8);
      line-height: calc(var(--cs-form-control-height) - var(--cs-form-control-border-width) * 2);
      padding: 0 0.75em;
    }

    /* Appearance modifiers */
    :host([appearance='outlined']) {
      color: var(--cs-color-on-quiet, var(--cs-color-neutral-on-quiet));
      background-color: transparent;
      border-color: var(--cs-color-border-loud, var(--cs-color-neutral-border-loud));
    }

    :host([appearance='filled']) {
      color: var(--cs-color-on-quiet, var(--cs-color-neutral-on-quiet));
      background-color: var(--cs-color-fill-quiet, var(--cs-color-neutral-fill-quiet));
      border-color: transparent;
    }

    :host([appearance='filled-outlined']) {
      color: var(--cs-color-on-quiet, var(--cs-color-neutral-on-quiet));
      background-color: var(--cs-color-fill-quiet, var(--cs-color-neutral-fill-quiet));
      border-color: var(--cs-color-border-normal, var(--cs-color-neutral-border-normal));
    }

    :host([appearance='accent']) {
      color: var(--cs-color-on-loud, var(--cs-color-neutral-on-loud));
      background-color: var(--cs-color-fill-loud, var(--cs-color-neutral-fill-loud));
      border-color: transparent;
    }
  }

  .content {
    font-size: var(--cs-font-size-smaller);
  }

  [part='remove-button'] {
    line-height: 1;
  }

  [part='remove-button']::part(button) {
    padding: 0;
    height: 1em;
    width: 1em;
    color: currentColor;
  }

  @media (hover: hover) {
    :host(:hover) > [part='remove-button']::part(button) {
      background-color: transparent;
      color: color-mix(in oklab, currentColor, var(--cs-color-mix-hover));
    }
  }

  :host(:active) > [part='remove-button']::part(button) {
    background-color: transparent;
    color: color-mix(in oklab, currentColor, var(--cs-color-mix-active));
  }

  /*
   * Pill modifier
   */
  :host([pill]) {
    border-radius: var(--cs-border-radius-pill);
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.BX3RGP2A.js
  var CsTag = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.localize = new LocalizeController2(this);
      this.variant = "neutral";
      this.appearance = "filled-outlined";
      this.size = "m";
      this.pill = false;
      this.withRemove = false;
    }
    handleRemoveClick() {
      this.dispatchEvent(new CsRemoveEvent());
    }
    render() {
      return b2`
      <slot part="content" class="content"></slot>

      ${this.withRemove ? b2`
              <cs-button
                part="remove-button"
                exportparts="button:remove-button__button"
                class="remove"
                appearance="plain"
                size=${this.size}
                @click=${this.handleRemoveClick}
                tabindex="-1"
              >
                <cs-icon name="close" library="system" label=${this.localize.term("remove")}></cs-icon>
              </cs-button>
            ` : ""}
    `;
    }
  };
  CsTag.css = [tag_styles_default, variants_styles_default, size_styles_default];
  __decorateClass([
    n4({ reflect: true })
  ], CsTag.prototype, "variant", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsTag.prototype, "appearance", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsTag.prototype, "size", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsTag.prototype, "pill", 2);
  __decorateClass([
    n4({ attribute: "with-remove", type: Boolean })
  ], CsTag.prototype, "withRemove", 2);
  CsTag = __decorateClass([
    customElement("cs-tag")
  ], CsTag);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.3UGMU57B.js
  var skeleton_styles_default = i`
  :host {
    --color: var(--cs-color-neutral-fill-normal);
    --sheen-color: color-mix(in oklab, var(--color), var(--cs-color-surface-raised));

    display: flex;
    position: relative;
    width: 100%;
    height: 100%;
    min-height: 1rem;
  }

  .indicator {
    flex: 1 1 auto;
    background: var(--color);
    border-radius: var(--cs-border-radius-pill);
  }

  :host([effect='sheen']) .indicator {
    background: linear-gradient(270deg, var(--sheen-color), var(--color), var(--color), var(--sheen-color));
    background-size: 400% 100%;
    animation: sheen 8s ease-in-out infinite;
  }

  :host([effect='pulse']) .indicator {
    animation: pulse 2s ease-in-out 0.5s infinite;
  }

  /* Forced colors mode */
  @media (forced-colors: active) {
    :host {
      --color: GrayText;
    }
  }

  @keyframes sheen {
    0% {
      background-position: 200% 0;
    }
    to {
      background-position: -200% 0;
    }
  }

  @keyframes pulse {
    0% {
      opacity: 1;
    }
    50% {
      opacity: 0.4;
    }
    100% {
      opacity: 1;
    }
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.BSW22LKT.js
  var CsSkeleton = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.effect = "none";
    }
    render() {
      return b2` <div part="indicator" class="indicator"></div> `;
    }
  };
  CsSkeleton.css = skeleton_styles_default;
  __decorateClass([
    n4({ reflect: true })
  ], CsSkeleton.prototype, "effect", 2);
  CsSkeleton = __decorateClass([
    customElement("cs-skeleton")
  ], CsSkeleton);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.EU3JHMMD.js
  var slider_styles_default = i`
  /* The label carries this when the slider has no label, and the two range-thumb bound names always do.
     It was referenced before it existed, so an unlabelled slider's label element was never hidden. */
  .vh {
    position: absolute;
    width: 1px;
    height: 1px;
    clip-path: inset(50%);
    overflow: hidden;
    white-space: nowrap;
  }

  :host {
    --track-size: 0.5em;
    --thumb-width: 1.4em;
    --thumb-height: 1.4em;
    --marker-width: 0.1875em;
    --marker-height: 0.1875em;
  }

  :host([orientation='vertical']) {
    width: auto;
  }

  #label:has(~ .vertical) {
    display: block;
    order: 2;
    max-width: none;
    text-align: center;
  }

  #description:has(~ .vertical) {
    order: 3;
    text-align: center;
  }

  /* Add extra space between slider and label, when present */
  #label.has-label ~ #slider {
    &.horizontal {
      margin-block-start: 0.5em;
    }
    &.vertical {
      margin-block-end: 0.5em;
    }
  }

  #slider {
    touch-action: none;

    &:focus {
      outline: none;
    }

    &:focus-visible:not(.disabled) #thumb,
    &:focus-visible:not(.disabled) #thumb-min,
    &:focus-visible:not(.disabled) #thumb-max {
      outline: var(--cs-focus-ring);
      /* intentionally no offset due to border */
    }
  }

  #track {
    position: relative;
    border-radius: 9999px;
    background: var(--cs-color-neutral-fill-normal);
    isolation: isolate;
  }

  /* Orientation */
  .horizontal #track {
    height: var(--track-size);
  }

  .vertical #track {
    order: 1;
    width: var(--track-size);
    height: 200px;
  }

  /* Disabled */
  .disabled #track {
    cursor: not-allowed;
    opacity: 0.5;
  }

  /* Indicator */
  #indicator {
    position: absolute;
    border-radius: inherit;
    background-color: var(--cs-form-control-activated-color);

    &:dir(ltr) {
      right: calc(100% - max(var(--start), var(--end)));
      left: min(var(--start), var(--end));
    }

    &:dir(rtl) {
      right: min(var(--start), var(--end));
      left: calc(100% - max(var(--start), var(--end)));
    }
  }

  .horizontal #indicator {
    top: 0;
    height: 100%;
  }

  .vertical #indicator {
    top: calc(100% - var(--end));
    bottom: var(--start);
    left: 0;
    width: 100%;
  }

  /* Thumbs */
  #thumb,
  #thumb-min,
  #thumb-max {
    z-index: 3;
    position: absolute;
    width: var(--thumb-width);
    height: var(--thumb-height);
    border: solid 0.125em var(--cs-color-surface-default);
    border-radius: 50%;
    background-color: var(--cs-form-control-activated-color);
    cursor: pointer;
  }

  .disabled #thumb,
  .disabled #thumb-min,
  .disabled #thumb-max {
    cursor: inherit;
  }

  .horizontal #thumb,
  .horizontal #thumb-min,
  .horizontal #thumb-max {
    top: calc(50% - var(--thumb-height) / 2);

    &:dir(ltr) {
      right: auto;
      left: calc(var(--position) - var(--thumb-width) / 2);
    }

    &:dir(rtl) {
      right: calc(var(--position) - var(--thumb-width) / 2);
      left: auto;
    }
  }

  .vertical #thumb,
  .vertical #thumb-min,
  .vertical #thumb-max {
    bottom: calc(var(--position) - var(--thumb-height) / 2);
    left: calc(50% - var(--thumb-width) / 2);
  }

  /* Range-specific thumb styles */
  :host([range]) {
    #thumb-min:focus-visible,
    #thumb-max:focus-visible {
      z-index: 4; /* Ensure focused thumb appears on top */
      outline: var(--cs-focus-ring);
      /* intentionally no offset due to border */
    }
  }

  /* Markers */
  #markers {
    pointer-events: none;
  }

  .marker {
    z-index: 2;
    position: absolute;
    width: var(--marker-width);
    height: var(--marker-height);
    border-radius: 50%;
    background-color: var(--cs-color-surface-default);
  }

  .marker:first-of-type,
  .marker:last-of-type {
    display: none;
  }

  .horizontal .marker {
    top: calc(50% - var(--marker-height) / 2);
    left: calc(var(--position) - var(--marker-width) / 2);
  }

  .vertical .marker {
    top: calc(var(--position) - var(--marker-height) / 2);
    left: calc(50% - var(--marker-width) / 2);
  }

  /* Marker labels */
  #references {
    position: relative;

    slot {
      display: flex;
      justify-content: space-between;
      height: 100%;
    }

    ::slotted(*) {
      color: var(--cs-color-text-quiet);
      font-size: 0.875em;
      line-height: 1;
    }
  }

  .horizontal {
    #references {
      margin-block-start: 0.5em;
    }
  }

  .vertical {
    display: flex;
    margin-inline: auto;

    #track {
      order: 1;
    }

    #references {
      order: 2;
      width: min-content;
      margin-inline-start: 0.75em;

      slot {
        flex-direction: column;
      }
    }
  }

  .vertical #references slot {
    flex-direction: column;
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.SAL5IQ75.js
  function isStepMismatch(value, min2, step) {
    const steps = (value - min2) / step;
    return Math.abs(steps - Math.round(steps)) > 1e-9;
  }
  var SliderValidator = () => {
    return {
      observedAttributes: ["min", "max", "step"],
      checkValidity(element) {
        const validity = {
          message: "",
          isValid: true,
          invalidKeys: []
        };
        const createNativeRange = (value, min2, max2, step) => {
          if (typeof document === "undefined") {
            return "";
          }
          const input = document.createElement("input");
          input.type = "range";
          input.min = String(min2);
          input.max = String(max2);
          input.step = String(step);
          input.value = String(value);
          input.checkValidity();
          return input.validationMessage;
        };
        if (element.isRange) {
          const minValue = element.minValue;
          const maxValue = element.maxValue;
          if (minValue < element.min) {
            validity.isValid = false;
            validity.invalidKeys.push("rangeUnderflow");
            validity.message = createNativeRange(minValue, element.min, element.max, element.step) || `Value must be greater than or equal to ${element.min}.`;
            return validity;
          }
          if (maxValue > element.max) {
            validity.isValid = false;
            validity.invalidKeys.push("rangeOverflow");
            validity.message = createNativeRange(maxValue, element.min, element.max, element.step) || `Value must be less than or equal to ${element.max}.`;
            return validity;
          }
          if (element.step && element.step !== 1) {
            const minStepMismatch = isStepMismatch(minValue, element.min, element.step);
            const maxStepMismatch = isStepMismatch(maxValue, element.min, element.step);
            if (minStepMismatch || maxStepMismatch) {
              validity.isValid = false;
              validity.invalidKeys.push("stepMismatch");
              const testValue = minStepMismatch ? minValue : maxValue;
              validity.message = createNativeRange(testValue, element.min, element.max, element.step) || `Value must be a multiple of ${element.step}.`;
              return validity;
            }
          }
        } else {
          const value = element.value;
          if (value < element.min) {
            validity.isValid = false;
            validity.invalidKeys.push("rangeUnderflow");
            validity.message = createNativeRange(value, element.min, element.max, element.step) || `Value must be greater than or equal to ${element.min}.`;
            return validity;
          }
          if (value > element.max) {
            validity.isValid = false;
            validity.invalidKeys.push("rangeOverflow");
            validity.message = createNativeRange(value, element.min, element.max, element.step) || `Value must be less than or equal to ${element.max}.`;
            return validity;
          }
          if (element.step && element.step !== 1 && isStepMismatch(value, element.min, element.step)) {
            validity.isValid = false;
            validity.invalidKeys.push("stepMismatch");
            validity.message = createNativeRange(value, element.min, element.max, element.step) || `Value must be a multiple of ${element.step}.`;
            return validity;
          }
        }
        return validity;
      }
    };
  };
  var CsSlider = class extends CornerstoneFormAssociatedElement {
    constructor() {
      super(...arguments);
      this.draggableThumbMin = null;
      this.draggableThumbMax = null;
      this.hasSlotController = new HasSlotController(this, "hint", "label");
      this.localize = new LocalizeController2(this);
      this.activeThumb = null;
      this.lastTrackPosition = null;
      this.label = "";
      this.hint = "";
      this.minValue = 0;
      this.maxValue = 50;
      this.defaultValue = this.getAttribute("value") == null ? this.minValue : Number(this.getAttribute("value"));
      this._value = null;
      this.range = false;
      this.disabled = false;
      this.readonly = false;
      this.orientation = "horizontal";
      this.size = "m";
      this.min = 0;
      this.max = 100;
      this.step = 1;
      this.tooltipDistance = 8;
      this.tooltipPlacement = "top";
      this.withMarkers = false;
      this.withTooltip = false;
      this.ssrLabel = false;
      this.ssrHint = false;
      this.ssrReference = false;
    }
    static get validators() {
      return [...super.validators, SliderValidator()];
    }
    // Track last position for direction detection
    get focusableAnchor() {
      return this.isRange ? this.thumbMin || this.slider : this.slider;
    }
    /** Override validation target to point to the focusable element */
    get validationTarget() {
      return this.focusableAnchor;
    }
    /** The current value of the slider, submitted as a name/value pair with form data. */
    get value() {
      if (this.valueHasChanged) {
        const val2 = this._value ?? this.minValue ?? 0;
        return clamp(val2, this.min, this.max);
      }
      const val = this._value ?? this.defaultValue;
      return clamp(val, this.min, this.max);
    }
    /**
     * What a non-numeric value is replaced with, mirroring `<input type="range">`'s value sanitization
     * algorithm: the control's default value, which for a range is the midpoint of `min` and `max` — or
     * `min` alone when `max` is below it.
     *
     * Note this is deliberately `min`/`max`, the slider's bounds, and not `minValue`/`maxValue`, which are
     * the two thumb positions of a range slider.
     */
    get sanitizedValue() {
      return this.max < this.min ? this.min : this.min + (this.max - this.min) / 2;
    }
    set value(val) {
      const numeric = Number(val);
      val = Number.isNaN(numeric) ? this.sanitizedValue : numeric;
      if (this._value === val) {
        return;
      }
      this.valueHasChanged = true;
      this._value = val;
    }
    /** Get if this is a range slider */
    get isRange() {
      return this.range;
    }
    firstUpdated(changedProperties) {
      super.firstUpdated(changedProperties);
      if (this.isRange) {
        this.draggableThumbMin = new DraggableElement(this.thumbMin, {
          start: () => {
            this.activeThumb = "min";
            this.trackBoundingClientRect = this.track.getBoundingClientRect();
            this.valueWhenDraggingStarted = this.minValue;
            this.customStates.set("dragging", true);
            this.showRangeTooltips();
          },
          move: (x2, y3) => {
            this.setThumbValueFromCoordinates(x2, y3, "min");
          },
          stop: () => {
            if (this.minValue !== this.valueWhenDraggingStarted) {
              this.updateComplete.then(() => {
                this.dispatchEvent(new Event("change", { bubbles: true, composed: true }));
              });
              this.hasInteracted = true;
            }
            this.hideRangeTooltips();
            this.customStates.set("dragging", false);
            this.valueWhenDraggingStarted = void 0;
            this.activeThumb = null;
          }
        });
        this.draggableThumbMax = new DraggableElement(this.thumbMax, {
          start: () => {
            this.activeThumb = "max";
            this.trackBoundingClientRect = this.track.getBoundingClientRect();
            this.valueWhenDraggingStarted = this.maxValue;
            this.customStates.set("dragging", true);
            this.showRangeTooltips();
          },
          move: (x2, y3) => {
            this.setThumbValueFromCoordinates(x2, y3, "max");
          },
          stop: () => {
            if (this.maxValue !== this.valueWhenDraggingStarted) {
              this.updateComplete.then(() => {
                this.dispatchEvent(new Event("change", { bubbles: true, composed: true }));
              });
              this.hasInteracted = true;
            }
            this.hideRangeTooltips();
            this.customStates.set("dragging", false);
            this.valueWhenDraggingStarted = void 0;
            this.activeThumb = null;
          }
        });
        this.draggableTrack = new DraggableElement(this.track, {
          start: (x2, y3) => {
            this.trackBoundingClientRect = this.track.getBoundingClientRect();
            if (this.activeThumb) {
              this.valueWhenDraggingStarted = this.activeThumb === "min" ? this.minValue : this.maxValue;
            } else {
              const value = this.getValueFromCoordinates(x2, y3);
              const minDistance = Math.abs(value - this.minValue);
              const maxDistance = Math.abs(value - this.maxValue);
              if (minDistance === maxDistance) {
                if (value > this.maxValue) {
                  this.activeThumb = "max";
                } else if (value < this.minValue) {
                  this.activeThumb = "min";
                } else {
                  const isRtl = this.localize.dir() === "rtl";
                  const isVertical = this.orientation === "vertical";
                  const position = isVertical ? y3 : x2;
                  const previousPosition = this.lastTrackPosition || position;
                  this.lastTrackPosition = position;
                  const movingForward = position > previousPosition !== isRtl && !isVertical || position < previousPosition && isVertical;
                  this.activeThumb = movingForward ? "max" : "min";
                }
              } else {
                this.activeThumb = minDistance <= maxDistance ? "min" : "max";
              }
              this.valueWhenDraggingStarted = this.activeThumb === "min" ? this.minValue : this.maxValue;
            }
            this.customStates.set("dragging", true);
            this.setThumbValueFromCoordinates(x2, y3, this.activeThumb);
            this.showRangeTooltips();
          },
          move: (x2, y3) => {
            if (this.activeThumb) {
              this.setThumbValueFromCoordinates(x2, y3, this.activeThumb);
            }
          },
          stop: () => {
            if (this.activeThumb) {
              const currentValue = this.activeThumb === "min" ? this.minValue : this.maxValue;
              if (currentValue !== this.valueWhenDraggingStarted) {
                this.updateComplete.then(() => {
                  this.dispatchEvent(new Event("change", { bubbles: true, composed: true }));
                });
                this.hasInteracted = true;
              }
            }
            this.hideRangeTooltips();
            this.customStates.set("dragging", false);
            this.valueWhenDraggingStarted = void 0;
            this.activeThumb = null;
          }
        });
      } else {
        this.draggableTrack = new DraggableElement(this.slider, {
          start: (x2, y3) => {
            this.trackBoundingClientRect = this.track.getBoundingClientRect();
            this.valueWhenDraggingStarted = this.value;
            this.customStates.set("dragging", true);
            this.setValueFromCoordinates(x2, y3);
            this.showTooltip();
          },
          move: (x2, y3) => {
            this.setValueFromCoordinates(x2, y3);
          },
          stop: () => {
            if (this.value !== this.valueWhenDraggingStarted) {
              this.updateComplete.then(() => {
                this.dispatchEvent(new Event("change", { bubbles: true, composed: true }));
              });
              this.hasInteracted = true;
            }
            this.hideTooltip();
            this.customStates.set("dragging", false);
            this.valueWhenDraggingStarted = void 0;
          }
        });
      }
    }
    willUpdate(changedProperties) {
      if (this.isRange) {
        if (changedProperties.has("minValue") || changedProperties.has("maxValue") || changedProperties.has("min") || changedProperties.has("max")) {
          this.minValue = clamp(this.minValue, this.min, this.maxValue);
          this.maxValue = clamp(this.maxValue, this.minValue, this.max);
        }
      }
      super.willUpdate(changedProperties);
    }
    updated(changedProperties) {
      if (this.isRange) {
        if (changedProperties.has("minValue") || changedProperties.has("maxValue")) {
          this.updateFormValue();
        }
      }
      if (changedProperties.has("disabled") || changedProperties.has("readonly")) {
        const enabled = !(this.disabled || this.readonly);
        if (this.isRange) {
          if (this.draggableThumbMin) {
            this.draggableThumbMin.toggle(enabled);
          }
          if (this.draggableThumbMax) {
            this.draggableThumbMax.toggle(enabled);
          }
        }
        if (this.draggableTrack) {
          this.draggableTrack.toggle(enabled);
        }
      }
      super.updated(changedProperties);
    }
    /** @internal Called when a containing fieldset is disabled. */
    formDisabledCallback(isDisabled) {
      this.disabled = isDisabled;
    }
    /** @internal Called when the form is reset. */
    formResetCallback() {
      if (this.isRange) {
        this.minValue = parseFloat(this.getAttribute("min-value") ?? String(this.min));
        this.maxValue = parseFloat(this.getAttribute("max-value") ?? String(this.max));
      } else {
        this._value = null;
        this.defaultValue = this.defaultValue ?? parseFloat(this.getAttribute("value") ?? String(this.min));
      }
      this.valueHasChanged = false;
      this.hasInteracted = false;
      super.formResetCallback();
    }
    /** Clamps a number to min/max while ensuring it's a valid step interval. */
    clampAndRoundToStep(value) {
      const stepPrecision = (String(this.step).split(".")[1] || "").replace(/0+$/g, "").length;
      const step = Number(this.step);
      const min2 = Number(this.min);
      const max2 = Number(this.max);
      value = Math.round(value / step) * step;
      value = clamp(value, min2, max2);
      return parseFloat(value.toFixed(stepPrecision));
    }
    /** Given a value, returns its percentage within a range of min/max. */
    getPercentageFromValue(value) {
      return (value - this.min) / (this.max - this.min) * 100;
    }
    /** Converts coordinates to slider value */
    getValueFromCoordinates(x2, y3) {
      const isRtl = this.localize.dir() === "rtl";
      const isVertical = this.orientation === "vertical";
      const { top, right, bottom, left, height, width } = this.trackBoundingClientRect;
      const pointerPosition = isVertical ? y3 : x2;
      const sliderCoords = isVertical ? { start: top, end: bottom, size: height } : { start: left, end: right, size: width };
      const relativePosition = isVertical ? sliderCoords.end - pointerPosition : isRtl ? sliderCoords.end - pointerPosition : pointerPosition - sliderCoords.start;
      const percentage = relativePosition / sliderCoords.size;
      return this.clampAndRoundToStep(this.min + (this.max - this.min) * percentage);
    }
    handleBlur() {
      if (this.isRange) {
        requestAnimationFrame(() => {
          const focusedElement = this.shadowRoot?.activeElement;
          const thumbHasFocus = focusedElement === this.thumbMin || focusedElement === this.thumbMax;
          if (!thumbHasFocus) {
            this.hideRangeTooltips();
          }
        });
      } else {
        this.hideTooltip();
      }
      this.customStates.set("focused", false);
      this.dispatchEvent(new FocusEvent("blur", { bubbles: true, composed: true }));
    }
    handleFocus(event) {
      const target = event.target;
      if (this.isRange) {
        if (target === this.thumbMin) {
          this.activeThumb = "min";
        } else if (target === this.thumbMax) {
          this.activeThumb = "max";
        }
        this.showRangeTooltips();
      } else {
        this.showTooltip();
      }
      this.customStates.set("focused", true);
      this.dispatchEvent(new FocusEvent("focus", { bubbles: true, composed: true }));
    }
    handleKeyDown(event) {
      const isRtl = this.localize.dir() === "rtl";
      const target = event.target;
      if (this.disabled || this.readonly) {
        return;
      }
      if (this.isRange) {
        if (target === this.thumbMin) {
          this.activeThumb = "min";
        } else if (target === this.thumbMax) {
          this.activeThumb = "max";
        }
        if (!this.activeThumb) {
          return;
        }
      }
      const current = this.isRange ? this.activeThumb === "min" ? this.minValue : this.maxValue : this.value;
      let newValue = current;
      switch (event.key) {
        // Increase
        case "ArrowUp":
        case (isRtl ? "ArrowLeft" : "ArrowRight"):
          event.preventDefault();
          newValue = this.clampAndRoundToStep(current + this.step);
          break;
        // Decrease
        case "ArrowDown":
        case (isRtl ? "ArrowRight" : "ArrowLeft"):
          event.preventDefault();
          newValue = this.clampAndRoundToStep(current - this.step);
          break;
        // Minimum value
        case "Home":
          event.preventDefault();
          newValue = this.isRange && this.activeThumb === "min" ? this.min : this.isRange ? this.minValue : this.min;
          break;
        // Maximum value
        case "End":
          event.preventDefault();
          newValue = this.isRange && this.activeThumb === "max" ? this.max : this.isRange ? this.maxValue : this.max;
          break;
        // Move up 10%
        case "PageUp": {
          event.preventDefault();
          const stepUp = Math.max(
            current + (this.max - this.min) / 10,
            current + this.step
            // make sure we at least move up to the next step
          );
          newValue = this.clampAndRoundToStep(stepUp);
          break;
        }
        // Move down 10%
        case "PageDown": {
          event.preventDefault();
          const stepDown = Math.min(
            current - (this.max - this.min) / 10,
            current - this.step
            // make sure we at least move down to the previous step
          );
          newValue = this.clampAndRoundToStep(stepDown);
          break;
        }
        // Handle form submission on Enter
        case "Enter":
          submitOnEnter(event, this);
          return;
      }
      if (newValue === current) {
        return;
      }
      if (this.isRange) {
        if (this.activeThumb === "min") {
          if (newValue > this.maxValue) {
            this.maxValue = newValue;
            this.minValue = newValue;
          } else {
            this.minValue = Math.max(this.min, newValue);
          }
        } else {
          if (newValue < this.minValue) {
            this.minValue = newValue;
            this.maxValue = newValue;
          } else {
            this.maxValue = Math.min(this.max, newValue);
          }
        }
        this.updateFormValue();
      } else {
        this.value = clamp(newValue, this.min, this.max);
      }
      this.updateComplete.then(() => {
        this.dispatchEvent(new InputEvent("input", { bubbles: true, composed: true }));
        this.dispatchEvent(new Event("change", { bubbles: true, composed: true }));
      });
      this.hasInteracted = true;
    }
    handleLabelPointerDown(event) {
      event.preventDefault();
      if (!this.disabled) {
        if (this.isRange) {
          this.thumbMin?.focus();
        } else {
          this.slider.focus();
        }
      }
    }
    setValueFromCoordinates(x2, y3) {
      const oldValue = this.value;
      this.value = this.getValueFromCoordinates(x2, y3);
      if (this.value !== oldValue) {
        this.updateComplete.then(() => {
          this.dispatchEvent(new InputEvent("input", { bubbles: true, composed: true }));
        });
      }
    }
    setThumbValueFromCoordinates(x2, y3, thumb) {
      const value = this.getValueFromCoordinates(x2, y3);
      const oldValue = thumb === "min" ? this.minValue : this.maxValue;
      if (thumb === "min") {
        if (value > this.maxValue) {
          this.maxValue = value;
          this.minValue = value;
        } else {
          this.minValue = Math.max(this.min, value);
        }
      } else {
        if (value < this.minValue) {
          this.minValue = value;
          this.maxValue = value;
        } else {
          this.maxValue = Math.min(this.max, value);
        }
      }
      if (oldValue !== (thumb === "min" ? this.minValue : this.maxValue)) {
        this.updateFormValue();
        this.updateComplete.then(() => {
          this.dispatchEvent(new InputEvent("input", { bubbles: true, composed: true }));
        });
      }
    }
    showTooltip() {
      if (this.withTooltip && this.tooltip) {
        this.tooltip.open = true;
      }
    }
    hideTooltip() {
      if (this.withTooltip && this.tooltip) {
        this.tooltip.open = false;
      }
    }
    showRangeTooltips() {
      if (!this.withTooltip) {
        return;
      }
      const tooltipMin = this.shadowRoot?.getElementById("tooltip-thumb-min");
      const tooltipMax = this.shadowRoot?.getElementById("tooltip-thumb-max");
      if (this.activeThumb === "min") {
        if (tooltipMin) {
          tooltipMin.open = true;
        }
        if (tooltipMax) {
          tooltipMax.open = false;
        }
      } else if (this.activeThumb === "max") {
        if (tooltipMax) {
          tooltipMax.open = true;
        }
        if (tooltipMin) {
          tooltipMin.open = false;
        }
      }
    }
    hideRangeTooltips() {
      if (!this.withTooltip) {
        return;
      }
      const tooltipMin = this.shadowRoot?.getElementById("tooltip-thumb-min");
      const tooltipMax = this.shadowRoot?.getElementById("tooltip-thumb-max");
      if (tooltipMin) {
        tooltipMin.open = false;
      }
      if (tooltipMax) {
        tooltipMax.open = false;
      }
    }
    /** Updates the form value submission for range sliders */
    /**
     * @internal
     */
    updateFormValue(value) {
      if (this.isRange) {
        const formData = new FormData();
        formData.append(this.name || "", String(this.minValue));
        formData.append(this.name || "", String(this.maxValue));
        this.setValue(formData, formData);
        return;
      }
      super.updateFormValue(value);
    }
    /** Sets focus to the slider. */
    focus() {
      if (this.isRange) {
        this.thumbMin?.focus();
      } else {
        this.slider.focus();
      }
    }
    /** Removes focus from the slider. */
    blur() {
      if (this.isRange) {
        for (const activeElement of activeElements()) {
          if (activeElement === this.thumbMin) {
            this.thumbMin.blur();
            break;
          } else if (activeElement === this.thumbMax) {
            this.thumbMax.blur();
            break;
          }
        }
      } else {
        this.slider.blur();
      }
    }
    /**
     * Decreases the slider's value by `step`. This is a programmatic change, so `input` and `change` events will not be
     * emitted when this is called.
     */
    stepDown() {
      if (this.isRange) {
        const newValue = this.clampAndRoundToStep(this.minValue - this.step);
        this.minValue = clamp(newValue, this.min, this.maxValue);
        this.updateFormValue();
      } else {
        const newValue = this.clampAndRoundToStep(this.value - this.step);
        this.value = newValue;
      }
    }
    /**
     * Increases the slider's value by `step`. This is a programmatic change, so `input` and `change` events will not be
     * emitted when this is called.
     */
    stepUp() {
      if (this.isRange) {
        const newValue = this.clampAndRoundToStep(this.maxValue + this.step);
        this.maxValue = clamp(newValue, this.minValue, this.max);
        this.updateFormValue();
      } else {
        const newValue = this.clampAndRoundToStep(this.value + this.step);
        this.value = newValue;
      }
    }
    render() {
      const hasLabelSlot = this.hasSlotController.test("label", "ssrLabel");
      const hasHintSlot = this.hasSlotController.test("hint", "ssrHint");
      const hasLabel = this.label ? true : !!hasLabelSlot;
      const hasHint = this.hint ? true : !!hasHintSlot;
      const hasReference = this.hasSlotController.test("reference", "ssrReference");
      const sliderClasses = e7({
        xs: this.size === "xs",
        s: this.size === "s",
        m: this.size === "m",
        l: this.size === "l",
        xl: this.size === "xl",
        horizontal: this.orientation === "horizontal",
        vertical: this.orientation === "vertical",
        disabled: this.disabled
      });
      const markers = [];
      if (this.withMarkers) {
        for (let i10 = this.min; i10 <= this.max; i10 += this.step) {
          markers.push(this.getPercentageFromValue(i10));
        }
      }
      const label = b2`
      <label
        id="label"
        part="label"
        for=${this.isRange ? "thumb-min" : "text-box"}
        class=${e7({ vh: !hasLabel, "has-label": hasLabel })}
        @pointerdown=${this.handleLabelPointerDown}
      >
        <slot name="label">${this.label}</slot>
      </label>
    `;
      const hint = b2`
      <div
        id="hint"
        part="hint"
        class=${e7({
        "has-slotted": hasHint
      })}
      >
        <slot name="hint">${this.hint}</slot>
      </div>
    `;
      const markersTemplate = this.withMarkers ? b2`
          <div id="markers" part="markers">
            ${markers.map(
        (marker) => b2`<span part="marker" class="marker" style=${o7({ "--position": `${marker}%` })}></span>`
      )}
          </div>
        ` : "";
      const referencesTemplate = hasReference ? b2`
          <div id="references" part="references" aria-hidden="true">
            <slot name="reference"></slot>
          </div>
        ` : "";
      const createTooltip = (thumbId, value) => this.withTooltip ? b2`
            <cs-tooltip
              id=${`tooltip${thumbId !== "thumb" ? "-" + thumbId : ""}`}
              part="tooltip"
              exportparts="
                tooltip:tooltip__tooltip,
                body:tooltip__body,
                arrow:tooltip__arrow
              "
              trigger="manual"
              distance=${this.tooltipDistance}
              placement=${this.tooltipPlacement}
              for=${thumbId}
              activation="manual"
              dir=${this.localize.dir()}
            >
              <span aria-hidden="true">
                ${typeof this.valueFormatter === "function" ? this.valueFormatter(value) : this.localize.number(value)}
              </span>
            </cs-tooltip>
          ` : "";
      if (this.isRange) {
        const minThumbPosition = clamp(this.getPercentageFromValue(this.minValue), 0, 100);
        const maxThumbPosition = clamp(this.getPercentageFromValue(this.maxValue), 0, 100);
        return b2`
        ${label}

        <div id="slider" part="slider" class=${sliderClasses}>
          <div id="track" part="track">
            <div
              id="indicator"
              part="indicator"
              style=${o7({
          "--start": `${Math.min(minThumbPosition, maxThumbPosition)}%`,
          "--end": `${Math.max(minThumbPosition, maxThumbPosition)}%`
        })}
            ></div>

            ${markersTemplate}

            <span
              id="thumb-min"
              part="thumb thumb-min"
              style=${o7({ "--position": `${minThumbPosition}%` })}
              role="slider"
              aria-valuemin=${this.min}
              aria-valuenow=${this.minValue}
              aria-valuetext=${typeof this.valueFormatter === "function" ? this.valueFormatter(this.minValue) : this.localize.number(this.minValue)}
              aria-valuemax=${this.max}
              aria-labelledby="label thumb-min-bound"
              aria-orientation=${this.orientation}
              aria-disabled=${this.disabled ? "true" : "false"}
              aria-readonly=${this.readonly ? "true" : "false"}
              tabindex=${this.disabled ? -1 : 0}
              @blur=${this.handleBlur}
              @focus=${this.handleFocus}
              @keydown=${this.handleKeyDown}
            ></span>

            <span
              id="thumb-max"
              part="thumb thumb-max"
              style=${o7({ "--position": `${maxThumbPosition}%` })}
              role="slider"
              aria-valuemin=${this.min}
              aria-valuenow=${this.maxValue}
              aria-valuetext=${typeof this.valueFormatter === "function" ? this.valueFormatter(this.maxValue) : this.localize.number(this.maxValue)}
              aria-valuemax=${this.max}
              aria-labelledby="label thumb-max-bound"
              aria-orientation=${this.orientation}
              aria-disabled=${this.disabled ? "true" : "false"}
              aria-readonly=${this.readonly ? "true" : "false"}
              tabindex=${this.disabled ? -1 : 0}
              @blur=${this.handleBlur}
              @focus=${this.handleFocus}
              @keydown=${this.handleKeyDown}
            ></span>

            <span id="thumb-min-bound" class="vh">minimum value</span>
            <span id="thumb-max-bound" class="vh">maximum value</span>
          </div>

          ${referencesTemplate} ${hint}
        </div>

        ${createTooltip("thumb-min", this.minValue)} ${createTooltip("thumb-max", this.maxValue)}
      `;
      } else {
        const thumbPosition = clamp(this.getPercentageFromValue(this.value), 0, 100);
        const indicatorOffsetPosition = clamp(
          this.getPercentageFromValue(typeof this.indicatorOffset === "number" ? this.indicatorOffset : this.min),
          0,
          100
        );
        return b2`
        ${label}

        <div
          id="slider"
          part="slider"
          class=${sliderClasses}
          role="slider"
          aria-disabled=${this.disabled ? "true" : "false"}
          aria-readonly=${this.disabled ? "true" : "false"}
          aria-orientation=${this.orientation}
          aria-valuemin=${this.min}
          aria-valuenow=${this.value}
          aria-valuetext=${typeof this.valueFormatter === "function" ? this.valueFormatter(this.value) : this.localize.number(this.value)}
          aria-valuemax=${this.max}
          aria-labelledby="label"
          aria-describedby="hint"
          tabindex=${this.disabled ? -1 : 0}
          @blur=${this.handleBlur}
          @focus=${this.handleFocus}
          @keydown=${this.handleKeyDown}
        >
          <div id="track" part="track">
            <div
              id="indicator"
              part="indicator"
              style=${o7({ "--start": `${indicatorOffsetPosition}%`, "--end": `${thumbPosition}%` })}
            ></div>

            ${markersTemplate}
            <span id="thumb" part="thumb" style=${o7({ "--position": `${thumbPosition}%` })}></span>
          </div>

          ${referencesTemplate} ${hint}
        </div>

        ${createTooltip("thumb", this.value)}
      `;
      }
    }
  };
  CsSlider.formAssociated = true;
  CsSlider.observeSlots = true;
  CsSlider.css = [size_styles_default, form_control_styles_default, slider_styles_default];
  __decorateClass([
    e5("#slider")
  ], CsSlider.prototype, "slider", 2);
  __decorateClass([
    e5("#thumb")
  ], CsSlider.prototype, "thumb", 2);
  __decorateClass([
    e5("#thumb-min")
  ], CsSlider.prototype, "thumbMin", 2);
  __decorateClass([
    e5("#thumb-max")
  ], CsSlider.prototype, "thumbMax", 2);
  __decorateClass([
    e5("#track")
  ], CsSlider.prototype, "track", 2);
  __decorateClass([
    e5("#tooltip")
  ], CsSlider.prototype, "tooltip", 2);
  __decorateClass([
    n4()
  ], CsSlider.prototype, "label", 2);
  __decorateClass([
    n4({ attribute: "hint" })
  ], CsSlider.prototype, "hint", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsSlider.prototype, "name", 2);
  __decorateClass([
    n4({ type: Number, attribute: "min-value" })
  ], CsSlider.prototype, "minValue", 2);
  __decorateClass([
    n4({ type: Number, attribute: "max-value" })
  ], CsSlider.prototype, "maxValue", 2);
  __decorateClass([
    n4({ attribute: "value", reflect: true, type: Number })
  ], CsSlider.prototype, "defaultValue", 2);
  __decorateClass([
    r5()
  ], CsSlider.prototype, "value", 1);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsSlider.prototype, "range", 2);
  __decorateClass([
    n4({ type: Boolean })
  ], CsSlider.prototype, "disabled", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsSlider.prototype, "readonly", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsSlider.prototype, "orientation", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsSlider.prototype, "size", 2);
  __decorateClass([
    n4({ attribute: "indicator-offset", type: Number })
  ], CsSlider.prototype, "indicatorOffset", 2);
  __decorateClass([
    n4({ type: Number })
  ], CsSlider.prototype, "min", 2);
  __decorateClass([
    n4({ type: Number })
  ], CsSlider.prototype, "max", 2);
  __decorateClass([
    n4({ type: Number })
  ], CsSlider.prototype, "step", 2);
  __decorateClass([
    n4({ type: Boolean })
  ], CsSlider.prototype, "autofocus", 2);
  __decorateClass([
    n4({ attribute: "tooltip-distance", type: Number })
  ], CsSlider.prototype, "tooltipDistance", 2);
  __decorateClass([
    n4({ attribute: "tooltip-placement", reflect: true })
  ], CsSlider.prototype, "tooltipPlacement", 2);
  __decorateClass([
    n4({ attribute: "with-markers", type: Boolean })
  ], CsSlider.prototype, "withMarkers", 2);
  __decorateClass([
    n4({ attribute: "with-tooltip", type: Boolean })
  ], CsSlider.prototype, "withTooltip", 2);
  __decorateClass([
    n4({ attribute: "ssr-label", type: Boolean })
  ], CsSlider.prototype, "ssrLabel", 2);
  __decorateClass([
    n4({ attribute: "ssr-hint", type: Boolean })
  ], CsSlider.prototype, "ssrHint", 2);
  __decorateClass([
    n4({ attribute: "ssr-reference", type: Boolean })
  ], CsSlider.prototype, "ssrReference", 2);
  __decorateClass([
    n4({ attribute: false })
  ], CsSlider.prototype, "valueFormatter", 2);
  CsSlider = __decorateClass([
    customElement("cs-slider")
  ], CsSlider);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.YBY46IXH.js
  var split_panel_styles_default = i`
  :host {
    --divider-width: 0.25rem;
    --divider-hit-area: 0.75rem;
    --min: 0%;
    --max: 100%;

    display: grid;
  }

  .start,
  .end {
    overflow: hidden;
  }

  .divider {
    flex: 0 0 var(--divider-width);
    display: flex;
    position: relative;
    align-items: center;
    justify-content: center;
    background-color: var(--cs-color-neutral-border-normal);
    color: var(--cs-color-neutral-on-normal);
    z-index: 1;
  }

  .divider:focus {
    outline: none;
  }

  :host(:not([disabled])) .divider:focus-visible {
    outline: var(--cs-focus-ring);
  }

  :host([disabled]) .divider {
    cursor: not-allowed;
  }

  /* Horizontal */
  :host(:not([orientation='vertical'], [disabled])) .divider {
    cursor: col-resize;
  }

  :host(:not([orientation='vertical'])) .divider::after {
    display: flex;
    content: '';
    position: absolute;
    height: 100%;
    left: calc(var(--divider-hit-area) / -2 + var(--divider-width) / 2);
    width: var(--divider-hit-area);
  }

  /* Vertical */
  :host([orientation='vertical']) {
    flex-direction: column;
  }

  :host([orientation='vertical']:not([disabled])) .divider {
    cursor: row-resize;
  }

  :host([orientation='vertical']) .divider::after {
    content: '';
    position: absolute;
    width: 100%;
    top: calc(var(--divider-hit-area) / -2 + var(--divider-width) / 2);
    height: var(--divider-hit-area);
  }

  @media (forced-colors: active) {
    .divider {
      outline: solid 1px transparent;
    }
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.CCQ33B4W.js
  var CsSplitPanel = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.isCollapsed = false;
      this.localize = new LocalizeController2(this);
      this.positionBeforeCollapsing = 0;
      this.position = 50;
      this.orientation = "horizontal";
      this.disabled = false;
      this.snapThreshold = 12;
    }
    connectedCallback() {
      super.connectedCallback();
      if (!o5) {
        this.resizeObserver = new ResizeObserver((entries) => this.handleResize(entries));
        this.updateComplete.then(() => this.resizeObserver.observe(this));
        this.detectSize();
        this.cachedPositionInPixels = this.percentageToPixels(this.position);
      }
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this.resizeObserver?.unobserve(this);
    }
    detectSize() {
      const { width, height } = this.getBoundingClientRect();
      this.size = this.orientation === "vertical" ? height : width;
    }
    percentageToPixels(value) {
      return this.size * (value / 100);
    }
    pixelsToPercentage(value) {
      return value / this.size * 100;
    }
    handleDrag(event) {
      const isRtl = this.didSSR && !this.hasUpdated ? this.dir === "rtl" : this.localize.dir() === "rtl";
      if (this.disabled) {
        return;
      }
      if (event.cancelable) {
        event.preventDefault();
      }
      drag(this, {
        onMove: (x2, y3) => {
          let newPositionInPixels = this.orientation === "vertical" ? y3 : x2;
          if (this.primary === "end") {
            newPositionInPixels = this.size - newPositionInPixels;
          }
          if (this.snap) {
            const snaps = this.snap.split(" ");
            snaps.forEach((value) => {
              let snapPoint;
              if (value.endsWith("%")) {
                snapPoint = this.size * (parseFloat(value) / 100);
              } else {
                snapPoint = parseFloat(value);
              }
              if (isRtl && this.orientation === "horizontal") {
                snapPoint = this.size - snapPoint;
              }
              if (newPositionInPixels >= snapPoint - this.snapThreshold && newPositionInPixels <= snapPoint + this.snapThreshold) {
                newPositionInPixels = snapPoint;
              }
            });
          }
          this.position = clamp(this.pixelsToPercentage(newPositionInPixels), 0, 100);
        },
        initialEvent: event
      });
    }
    handleKeyDown(event) {
      if (this.disabled) {
        return;
      }
      if (["ArrowLeft", "ArrowRight", "ArrowUp", "ArrowDown", "Home", "End", "Enter"].includes(event.key)) {
        let newPosition = this.position;
        const incr = (event.shiftKey ? 10 : 1) * (this.primary === "end" ? -1 : 1);
        event.preventDefault();
        if (event.key === "ArrowLeft" && this.orientation === "horizontal" || event.key === "ArrowUp" && this.orientation === "vertical") {
          newPosition -= incr;
        }
        if (event.key === "ArrowRight" && this.orientation === "horizontal" || event.key === "ArrowDown" && this.orientation === "vertical") {
          newPosition += incr;
        }
        if (event.key === "Home") {
          newPosition = this.primary === "end" ? 100 : 0;
        }
        if (event.key === "End") {
          newPosition = this.primary === "end" ? 0 : 100;
        }
        if (event.key === "Enter") {
          if (this.isCollapsed) {
            newPosition = this.positionBeforeCollapsing;
            this.isCollapsed = false;
          } else {
            const positionBeforeCollapsing = this.position;
            newPosition = 0;
            requestAnimationFrame(() => {
              this.isCollapsed = true;
              this.positionBeforeCollapsing = positionBeforeCollapsing;
            });
          }
        }
        this.position = clamp(newPosition, 0, 100);
      }
    }
    handleResize(entries) {
      const { width, height } = entries[0].contentRect;
      this.size = this.orientation === "vertical" ? height : width;
      if (isNaN(this.cachedPositionInPixels) || this.position === Infinity) {
        this.cachedPositionInPixels = Number(this.getAttribute("position-in-pixels"));
        this.positionInPixels = Number(this.getAttribute("position-in-pixels"));
        this.position = this.pixelsToPercentage(this.positionInPixels);
      }
      if (this.primary) {
        const newPosition = this.pixelsToPercentage(this.cachedPositionInPixels);
        if (this.position !== newPosition) {
          this.position = newPosition;
        }
      }
    }
    handlePositionChange() {
      this.cachedPositionInPixels = this.percentageToPixels(this.position);
      const newPositionInPixels = this.percentageToPixels(this.position);
      if (this.positionInPixels !== newPositionInPixels) {
        this.positionInPixels = newPositionInPixels;
      }
      this.isCollapsed = false;
      this.positionBeforeCollapsing = 0;
      this.dispatchEvent(new CsRepositionEvent());
    }
    handlePositionInPixelsChange() {
      const newPosition = this.pixelsToPercentage(this.positionInPixels);
      if (this.position !== newPosition) {
        this.position = newPosition;
      }
    }
    handleVerticalChange() {
      this.detectSize();
    }
    updateStyles() {
      const gridTemplate = this.orientation === "vertical" ? "gridTemplateRows" : "gridTemplateColumns";
      const gridTemplateAlt = this.orientation === "vertical" ? "gridTemplateColumns" : "gridTemplateRows";
      const isRtl = this.hasUpdated ? this.localize.dir() === "rtl" : this.dir === "rtl";
      const primary = `
      clamp(
        0%,
        clamp(
          var(--min),
          ${this.position}% - var(--divider-width) / 2,
          var(--max)
        ),
        calc(100% - var(--divider-width))
      )
    `;
      const secondary = "auto";
      if (this.primary === "end") {
        if (isRtl && this.orientation === "horizontal") {
          this.setStyle(gridTemplate, `${primary} var(--divider-width) ${secondary}`);
        } else {
          this.setStyle(gridTemplate, `${secondary} var(--divider-width) ${primary}`);
        }
      } else {
        if (isRtl && this.orientation === "horizontal") {
          this.setStyle(gridTemplate, `${secondary} var(--divider-width) ${primary}`);
        } else {
          this.setStyle(gridTemplate, `${primary} var(--divider-width) ${secondary}`);
        }
      }
      this.setStyle(gridTemplateAlt, "unset");
    }
    willUpdate(changedProperties) {
      if (!this.style) {
        this.updateStyles();
      }
      super.willUpdate(changedProperties);
    }
    updated(changedProperties) {
      super.updated(changedProperties);
    }
    render() {
      if (this.style) {
        this.updateStyles();
      }
      return b2`
      <slot name="start" part="panel start" class="start"></slot>

      <div
        part="divider"
        class="divider"
        tabindex=${o8(this.disabled ? void 0 : "0")}
        role="separator"
        aria-valuenow=${this.position}
        aria-valuemin="0"
        aria-valuemax="100"
        aria-label=${this.localize.term("resize")}
        @keydown=${this.handleKeyDown}
        @mousedown=${this.handleDrag}
        @touchstart=${this.handleDrag}
      >
        <slot name="divider"></slot>
      </div>

      <slot name="end" part="panel end" class="end"></slot>
    `;
    }
  };
  CsSplitPanel.css = split_panel_styles_default;
  __decorateClass([
    e5(".divider")
  ], CsSplitPanel.prototype, "divider", 2);
  __decorateClass([
    n4({ type: Number, reflect: true })
  ], CsSplitPanel.prototype, "position", 2);
  __decorateClass([
    n4({ attribute: "position-in-pixels", type: Number })
  ], CsSplitPanel.prototype, "positionInPixels", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsSplitPanel.prototype, "orientation", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsSplitPanel.prototype, "disabled", 2);
  __decorateClass([
    n4()
  ], CsSplitPanel.prototype, "primary", 2);
  __decorateClass([
    n4()
  ], CsSplitPanel.prototype, "snap", 2);
  __decorateClass([
    n4({ type: Number, attribute: "snap-threshold" })
  ], CsSplitPanel.prototype, "snapThreshold", 2);
  __decorateClass([
    watch("position")
  ], CsSplitPanel.prototype, "handlePositionChange", 1);
  __decorateClass([
    watch("positionInPixels")
  ], CsSplitPanel.prototype, "handlePositionInPixelsChange", 1);
  __decorateClass([
    watch("vertical")
  ], CsSplitPanel.prototype, "handleVerticalChange", 1);
  CsSplitPanel = __decorateClass([
    customElement("cs-split-panel")
  ], CsSplitPanel);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.2LDMR3WU.js
  var switch_styles_default = i`
  :host {
    --height: var(--cs-form-control-toggle-size);
    --width: calc(var(--height) * 1.75);
    --thumb-size: 0.75em;

    display: inline-flex;
    line-height: var(--cs-form-control-value-line-height);
  }

  label {
    position: relative;
    display: flex;
    align-items: center;
    font: inherit;
    color: var(--cs-form-control-value-color);
    vertical-align: middle;
    cursor: pointer;
  }

  .switch {
    flex: 0 0 auto;
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    width: var(--width);
    height: var(--height);
    background-color: var(--cs-form-control-background-color);
    border-color: var(--cs-form-control-border-color);
    border-radius: var(--height);
    border-style: var(--cs-form-control-border-style);
    border-width: var(--cs-form-control-border-width);
    transition-property: translate, background, border-color, box-shadow;
    transition-duration: var(--cs-transition-normal);
    transition-timing-function: var(--cs-transition-easing);
  }

  :host([did-ssr]:not(:defined)) .switch {
    transition-property: unset;
    transition-duration: unset;
    transition-timing-function: unset;
  }

  .switch .thumb {
    aspect-ratio: 1 / 1;
    width: var(--thumb-size);
    height: var(--thumb-size);
    background-color: var(--cs-form-control-border-color);
    border-radius: 50%;
    translate: calc((var(--width) - var(--height)) / -2);
    transition: inherit;
  }
  .switch .thumb:dir(rtl) {
    translate: calc((var(--width) - var(--height)) / 2);
  }

  .input {
    position: absolute;
    opacity: 0;
    padding: 0;
    margin: 0;
    pointer-events: none;
  }

  /* Focus */
  label:not(.disabled) .input:focus-visible ~ .switch .thumb {
    outline: var(--cs-focus-ring);
    outline-offset: var(--cs-focus-ring-offset);
  }

  /* Checked */
  .checked .switch {
    background-color: var(--cs-form-control-activated-color);
    border-color: var(--cs-form-control-activated-color);
  }

  .checked .switch .thumb {
    background-color: var(--cs-color-surface-default);
    translate: calc((var(--width) - var(--height)) / 2);
  }
  .checked .switch .thumb:dir(rtl) {
    translate: calc((var(--width) - var(--height)) / -2);
  }

  /* Disabled */
  label:has(> :disabled) {
    opacity: 0.5;
    cursor: not-allowed;
  }

  [part~='label'] {
    display: inline-block;
    line-height: var(--height);
    margin-inline-start: 0.5em;
    user-select: none;
    -webkit-user-select: none;
  }

  :host([required]) [part~='label']::after {
    content: var(--cs-form-control-required-content);
    color: var(--cs-form-control-required-content-color);
    margin-inline-start: var(--cs-form-control-required-content-offset);
  }

  @media (forced-colors: active) {
    :checked:enabled + .switch:hover .thumb,
    :checked + .switch .thumb {
      background-color: ButtonText;
    }
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.K4AF46HC.js
  var CsSwitch = class extends CornerstoneFormAssociatedElement {
    constructor() {
      super(...arguments);
      this.hasSlotController = new HasSlotController(this, "hint");
      this.localize = new LocalizeController2(this);
      this.title = "";
      this.name = null;
      this._value = this.getAttribute("value") ?? null;
      this.size = "m";
      this.disabled = false;
      this._checked = null;
      this.defaultChecked = this.hasAttribute("checked");
      this.required = false;
      this.hint = "";
      this.ssrHint = false;
    }
    static get validators() {
      return [...super.validators, MirrorValidator()];
    }
    /** The value of the switch, submitted as a name/value pair with form data. */
    get value() {
      return this._value ?? "on";
    }
    set value(val) {
      this._value = val;
    }
    get checked() {
      if (this.valueHasChanged) {
        return Boolean(this._checked);
      }
      return this._checked ?? this.defaultChecked;
    }
    set checked(val) {
      this._checked = Boolean(val);
      this.valueHasChanged = true;
    }
    handleClick() {
      this.hasInteracted = true;
      this.checked = !this.checked;
      this.updateComplete.then(() => {
        this.dispatchEvent(new Event("change", { bubbles: true, composed: true }));
      });
    }
    handleKeyDown(event) {
      const isRtl = this.localize.dir() === "rtl";
      if (event.key === "ArrowLeft") {
        event.preventDefault();
        this.checked = isRtl;
        this.updateComplete.then(() => {
          this.dispatchEvent(new Event("change", { bubbles: true, composed: true }));
          this.dispatchEvent(new InputEvent("input", { bubbles: true, composed: true }));
        });
      }
      if (event.key === "ArrowRight") {
        event.preventDefault();
        this.checked = !isRtl;
        this.updateComplete.then(() => {
          this.dispatchEvent(new Event("change", { bubbles: true, composed: true }));
          this.dispatchEvent(new InputEvent("input", { bubbles: true, composed: true }));
        });
      }
    }
    willUpdate(changedProperties) {
      super.willUpdate(changedProperties);
      if (changedProperties.has("value") || changedProperties.has("checked") || changedProperties.has("defaultChecked") || changedProperties.has("disabled")) {
        this.handleValueOrCheckedChange();
      }
    }
    handleValueOrCheckedChange() {
      if (this.didSSR && !this.hasUpdated) {
        this.updateComplete.then(() => {
          this.handleValueOrCheckedChange();
        });
        return;
      }
      this.setValue(this.checked ? this.value : null, this._value);
      this.updateValidity();
    }
    handleStateChange() {
      if (this.hasUpdated) {
        this.input.checked = this.checked;
      }
      this.customStates.set("checked", this.checked);
      this.updateValidity();
    }
    handleDisabledChange() {
      this.updateValidity();
    }
    /** Simulates a click on the switch. */
    click() {
      this.input.click();
    }
    /** Sets focus on the switch. */
    focus(options2) {
      this.input.focus(options2);
    }
    /** Removes focus from the switch. */
    blur() {
      this.input.blur();
    }
    setValue(value, stateValue) {
      if (!this.checked) {
        this.internals.setFormValue(null, null);
        return;
      }
      this.internals.setFormValue(value ?? "on", stateValue);
    }
    formResetCallback() {
      this._checked = null;
      super.formResetCallback();
      this.handleValueOrCheckedChange();
    }
    render() {
      const hasHintSlot = this.hasSlotController.test("hint", "ssrHint");
      const hasHint = this.hint ? true : !!hasHintSlot;
      const checkedAttribute = this.didSSR && !this.hasUpdated ? this.checked : this.defaultChecked;
      const checkedProperty = this.didSSR && !this.hasUpdated ? null : l5(this.checked);
      return b2`
      <label
        part="switch"
        class=${e7({
        checked: this.checked,
        disabled: this.disabled
      })}
      >
        <input
          class="input"
          type="checkbox"
          title=${this.title}
          name=${o8(this.name)}
          value=${o8(this.value)}
          .checked=${o8(checkedProperty)}
          ?checked=${checkedAttribute}
          ?disabled=${this.disabled}
          ?required=${this.required}
          role="switch"
          aria-checked=${this.checked ? "true" : "false"}
          aria-describedby="hint"
          @click=${this.handleClick}
          @keydown=${this.handleKeyDown}
        />

        <span part="control" class="switch">
          <span part="thumb" class="thumb"></span>
        </span>

        <slot part="label" class="label"></slot>
      </label>

      <slot
        id="hint"
        name="hint"
        part="hint"
        class=${e7({
        "has-slotted": hasHint
      })}
        aria-hidden=${hasHint ? "false" : "true"}
        >${this.hint}</slot
      >
    `;
    }
  };
  CsSwitch.shadowRootOptions = { ...CornerstoneFormAssociatedElement.shadowRootOptions, delegatesFocus: true };
  CsSwitch.css = [form_control_styles_default, size_styles_default, switch_styles_default];
  __decorateClass([
    e5('input[type="checkbox"]')
  ], CsSwitch.prototype, "input", 2);
  __decorateClass([
    n4()
  ], CsSwitch.prototype, "title", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsSwitch.prototype, "name", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsSwitch.prototype, "value", 1);
  __decorateClass([
    n4({ reflect: true })
  ], CsSwitch.prototype, "size", 2);
  __decorateClass([
    n4({ type: Boolean })
  ], CsSwitch.prototype, "disabled", 2);
  __decorateClass([
    n4({ type: Boolean, attribute: false })
  ], CsSwitch.prototype, "checked", 1);
  __decorateClass([
    n4({ type: Boolean, attribute: "checked", reflect: true })
  ], CsSwitch.prototype, "defaultChecked", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsSwitch.prototype, "required", 2);
  __decorateClass([
    n4({ attribute: "hint" })
  ], CsSwitch.prototype, "hint", 2);
  __decorateClass([
    n4({ attribute: "ssr-hint", type: Boolean })
  ], CsSwitch.prototype, "ssrHint", 2);
  __decorateClass([
    watch(["checked", "defaultChecked"])
  ], CsSwitch.prototype, "handleStateChange", 1);
  __decorateClass([
    watch("disabled", { waitUntilFirstUpdate: true })
  ], CsSwitch.prototype, "handleDisabledChange", 1);
  CsSwitch = __decorateClass([
    customElement("cs-switch")
  ], CsSwitch);
  CsSwitch.disableWarning?.("change-in-update");

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.CJIK67RV.js
  var tab_styles_default = i`
  :host {
    display: inline-block;
    color: var(--cs-color-neutral-on-quiet);
    font-weight: var(--cs-font-weight-action);
  }

  .tab {
    display: inline-flex;
    align-items: center;
    font: inherit;
    padding: 1em 1.5em;
    white-space: nowrap;
    user-select: none;
    -webkit-user-select: none;
    cursor: pointer;
    transition: color var(--cs-transition-fast) var(--cs-transition-easing);

    /* These are !important because a document-level universal margin reset would otherwise beat them:
     a slotted element belongs to the outer tree. See callout.styles.ts for the full note. */
    ::slotted(cs-icon:first-child) {
      margin-inline-end: 0.5em !important;
    }

    ::slotted(cs-icon:last-child) {
      margin-inline-start: 0.5em !important;
    }
  }

  @media (hover: hover) {
    :host(:hover:not([disabled])) .tab {
      color: currentColor;
    }
  }

  :host(:focus) {
    outline: transparent;
  }

  :host(:focus-visible) .tab {
    outline: var(--cs-focus-ring);
    outline-offset: calc(-1 * var(--cs-border-width-l) - var(--cs-focus-ring-offset));
  }

  :host([active]:not([disabled])) {
    color: var(--cs-color-brand-on-quiet);
  }

  :host([disabled]) .tab {
    opacity: 0.5;
    cursor: not-allowed;
  }

  @media (forced-colors: active) {
    :host([active]:not([disabled])) {
      outline: solid 1px transparent;
      outline-offset: -3px;
    }
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.2KWIVIQW.js
  var id = 0;
  var CsTab = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.attrId = ++id;
      this.componentId = `cs-tab-${this.attrId}`;
      this.panel = "";
      this.active = false;
      this.disabled = false;
      this.tabIndex = 0;
      this.slot = "nav";
      this.role = "tab";
    }
    handleActiveChange() {
      this.setAttribute("aria-selected", this.active ? "true" : "false");
    }
    handleDisabledChange() {
      this.setAttribute("aria-disabled", this.disabled ? "true" : "false");
      if (this.disabled && !this.active) {
        this.tabIndex = -1;
      } else {
        this.tabIndex = 0;
      }
    }
    render() {
      this.id = this.id?.length > 0 ? this.id : this.componentId;
      return b2`
      <div
        part="tab"
        class=${e7({
        tab: true,
        "tab-active": this.active
      })}
      >
        <slot></slot>
      </div>
    `;
    }
  };
  CsTab.css = tab_styles_default;
  __decorateClass([
    e5(".tab")
  ], CsTab.prototype, "tab", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsTab.prototype, "panel", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsTab.prototype, "active", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsTab.prototype, "disabled", 2);
  __decorateClass([
    n4({ type: Number, reflect: true })
  ], CsTab.prototype, "tabIndex", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsTab.prototype, "slot", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsTab.prototype, "role", 2);
  __decorateClass([
    watch("active")
  ], CsTab.prototype, "handleActiveChange", 1);
  __decorateClass([
    watch("disabled")
  ], CsTab.prototype, "handleDisabledChange", 1);
  CsTab = __decorateClass([
    customElement("cs-tab")
  ], CsTab);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.UQ2XZMRE.js
  var CsTabShowEvent = class extends Event {
    constructor(detail) {
      super("cs-tab-show", { bubbles: true, cancelable: false, composed: true });
      this.detail = detail;
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.DP7I3K2F.js
  var CsTabHideEvent = class extends Event {
    constructor(detail) {
      super("cs-tab-hide", { bubbles: true, cancelable: false, composed: true });
      this.detail = detail;
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.HS5USC24.js
  var tab_group_styles_default = i`
  :host {
    --indicator-color: var(--cs-color-brand-fill-loud);
    --track-color: var(--cs-color-neutral-fill-normal);
    --track-width: 0.125rem;

    /* Private */
    --safe-track-width: max(0.5px, round(var(--track-width), 0.5px));

    display: block;
  }

  .tab-group {
    display: flex;
    border-radius: 0;
  }

  .tabs {
    display: flex;
    position: relative;
  }

  .indicator {
    position: absolute;
  }

  .tab-group-has-scroll-controls .nav-container {
    position: relative;
    padding: 0 1.5em;
  }

  .body {
    display: block;
  }

  .scroll-button {
    display: flex;
    align-items: center;
    justify-content: center;
    position: absolute;
    top: 0;
    bottom: 0;
    width: 1.5em;
  }

  .scroll-button-start {
    inset-inline-start: 0;
  }

  .scroll-button-end {
    inset-inline-end: 0;
  }

  /*
    * Top
    */

  .tab-group-top {
    flex-direction: column;
  }

  .tab-group-top .nav-container {
    order: 1;
  }

  .tab-group-top .nav {
    display: flex;
    overflow-x: auto;

    /* Hide scrollbar in Firefox */
    scrollbar-width: none;
  }

  /* Hide scrollbar in Chrome/Safari */
  .tab-group-top .nav::-webkit-scrollbar {
    width: 0;
    height: 0;
  }

  .tab-group-top .tabs {
    flex: 1 1 auto;
    position: relative;
    flex-direction: row;
    border-bottom: solid var(--safe-track-width) var(--track-color);
  }

  .tab-group-top .indicator {
    bottom: calc(-1 * var(--safe-track-width));
    border-bottom: solid var(--safe-track-width) var(--indicator-color);
  }

  .tab-group-top .body {
    order: 2;
  }

  /* These are !important because a document-level universal margin reset would otherwise beat them:
     a slotted element belongs to the outer tree. See callout.styles.ts for the full note. */
  .tab-group-top ::slotted(cs-tab[active]) {
    border-block-end: solid var(--safe-track-width) var(--indicator-color);
    margin-block-end: calc(-1 * var(--safe-track-width)) !important;
  }

  .tab-group-top .body slot::slotted(cs-tab-panel) {
    --padding: var(--cs-space-xl) 0;
  }

  /*
    * Bottom
    */

  .tab-group-bottom {
    flex-direction: column;
  }

  .tab-group-bottom .nav-container {
    order: 2;
  }

  .tab-group-bottom .nav {
    display: flex;
    overflow-x: auto;

    /* Hide scrollbar in Firefox */
    scrollbar-width: none;
  }

  /* Hide scrollbar in Chrome/Safari */
  .tab-group-bottom .nav::-webkit-scrollbar {
    width: 0;
    height: 0;
  }

  .tab-group-bottom .tabs {
    flex: 1 1 auto;
    position: relative;
    flex-direction: row;
    border-top: solid var(--safe-track-width) var(--track-color);
  }

  .tab-group-bottom .indicator {
    top: calc(-1 * var(--safe-track-width));
    border-top: solid var(--safe-track-width) var(--indicator-color);
  }

  .tab-group-bottom .body {
    order: 1;
  }

  .tab-group-bottom ::slotted(cs-tab[active]) {
    border-block-start: solid var(--safe-track-width) var(--indicator-color);
    margin-block-start: calc(-1 * var(--safe-track-width)) !important;
  }

  .tab-group-bottom .body slot::slotted(cs-tab-panel) {
    --padding: var(--cs-space-xl) 0;
  }

  /*
    * Start
    */

  .tab-group-start {
    flex-direction: row;
  }

  .tab-group-start .nav-container {
    order: 1;
  }

  .tab-group-start .tabs {
    flex: 0 0 auto;
    flex-direction: column;
    border-inline-end: solid var(--safe-track-width) var(--track-color);
  }

  .tab-group-start .indicator {
    inset-inline-end: calc(-1 * var(--safe-track-width));
    border-right: solid var(--safe-track-width) var(--indicator-color);
  }

  .tab-group-start .body {
    flex: 1 1 auto;
    order: 2;
  }

  .tab-group-start ::slotted(cs-tab[active]) {
    border-inline-end: solid var(--safe-track-width) var(--indicator-color);
    margin-inline-end: calc(-1 * var(--safe-track-width)) !important;
  }

  .tab-group-start .body slot::slotted(cs-tab-panel) {
    --padding: 0 var(--cs-space-xl);
  }

  /*
    * End
    */

  .tab-group-end {
    flex-direction: row;
  }

  .tab-group-end .nav-container {
    order: 2;
  }

  .tab-group-end .tabs {
    flex: 0 0 auto;
    flex-direction: column;
    border-left: solid var(--safe-track-width) var(--track-color);
  }

  .tab-group-end .indicator {
    inset-inline-start: calc(-1 * var(--safe-track-width));
    border-inline-start: solid var(--safe-track-width) var(--indicator-color);
  }

  .tab-group-end .body {
    flex: 1 1 auto;
    order: 1;
  }

  .tab-group-end ::slotted(cs-tab[active]) {
    border-inline-start: solid var(--safe-track-width) var(--indicator-color);
    margin-inline-start: calc(-1 * var(--safe-track-width)) !important;
  }

  .tab-group-end .body slot::slotted(cs-tab-panel) {
    --padding: 0 var(--cs-space-xl);
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.QELUGXLH.js
  var CsTabGroup = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.tabs = [];
      this.focusableTabs = [];
      this.panels = [];
      this.localize = new LocalizeController2(this);
      this.hasScrollControls = false;
      this.active = "";
      this.placement = "top";
      this.activation = "auto";
      this.withoutScrollControls = false;
    }
    connectedCallback() {
      super.connectedCallback();
      if (o5) {
        return;
      }
      this.resizeObserver = new ResizeObserver(() => {
        this.updateScrollControls();
      });
      this.mutationObserver = new MutationObserver((mutations) => {
        if (mutations.some((m3) => !["aria-labelledby", "aria-controls"].includes(m3.attributeName))) {
          setTimeout(() => this.setAriaLabels());
        }
        const relevantMutations = mutations.filter((m3) => {
          const target = m3.target;
          return target.closest("cs-tab-group") === this;
        });
        if (relevantMutations.some((m3) => m3.attributeName === "disabled")) {
          this.syncTabsAndPanels();
        } else if (relevantMutations.some((m3) => m3.attributeName === "active")) {
          const tabs = relevantMutations.filter((m3) => m3.attributeName === "active" && m3.target.tagName.toLowerCase() === "cs-tab").map((m3) => m3.target);
          const newActiveTab = tabs.find((tab) => tab.active);
          if (newActiveTab && newActiveTab.closest("cs-tab-group") === this) {
            this.setActiveTab(newActiveTab);
          }
        }
      });
      this.updateComplete.then(() => {
        this.syncTabsAndPanels();
        this.mutationObserver.observe(this, { attributes: true, childList: true, subtree: true });
        this.resizeObserver.observe(this.nav);
        const intersectionObserver = new IntersectionObserver((entries, observer2) => {
          if (entries[0].intersectionRatio > 0) {
            this.setAriaLabels();
            if (this.active) {
              const tab = this.tabs.find((t8) => t8.panel === this.active);
              if (tab) {
                this.setActiveTab(tab);
              }
            } else {
              this.setActiveTab(this.getActiveTab() ?? this.tabs[0], { emitEvents: false });
            }
            observer2.unobserve(entries[0].target);
          }
        });
        intersectionObserver.observe(this.tabGroup);
      });
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this.mutationObserver?.disconnect();
      if (this.nav) {
        this.resizeObserver?.unobserve(this.nav);
      }
    }
    getAllTabs() {
      const slot = this.shadowRoot.querySelector('slot[name="nav"]');
      return [...slot.assignedElements()].filter((el) => {
        return el.tagName.toLowerCase() === "cs-tab";
      });
    }
    getAllPanels() {
      return [...this.defaultSlot.assignedElements()].filter((el) => el.tagName.toLowerCase() === "cs-tab-panel");
    }
    getActiveTab() {
      return this.tabs.find((el) => el.active);
    }
    handleClick(event) {
      const target = event.target;
      const tab = target.closest("cs-tab");
      const tabGroup = tab?.closest("cs-tab-group");
      if (tabGroup !== this) {
        return;
      }
      if (tab !== null) {
        this.setActiveTab(tab, { scrollBehavior: "smooth" });
      }
    }
    handleKeyDown(event) {
      const target = event.target;
      const tab = target.closest("cs-tab");
      const tabGroup = tab?.closest("cs-tab-group");
      if (tabGroup !== this) {
        return;
      }
      if (["Enter", " "].includes(event.key)) {
        if (tab !== null) {
          this.setActiveTab(tab, { scrollBehavior: "smooth" });
          event.preventDefault();
        }
        return;
      }
      if (["ArrowLeft", "ArrowRight", "ArrowUp", "ArrowDown", "Home", "End"].includes(event.key)) {
        const activeEl = this.tabs.find((t8) => t8.matches(":focus"));
        const isRtl = this.localize.dir() === "rtl";
        let nextTab = null;
        if (activeEl?.tagName.toLowerCase() === "cs-tab") {
          if (event.key === "Home") {
            nextTab = this.focusableTabs[0];
          } else if (event.key === "End") {
            nextTab = this.focusableTabs[this.focusableTabs.length - 1];
          } else if (["top", "bottom"].includes(this.placement) && event.key === (isRtl ? "ArrowRight" : "ArrowLeft") || ["start", "end"].includes(this.placement) && event.key === "ArrowUp") {
            const currentIndex = this.tabs.findIndex((el) => el === activeEl);
            nextTab = this.findNextFocusableTab(currentIndex, "backward");
          } else if (["top", "bottom"].includes(this.placement) && event.key === (isRtl ? "ArrowLeft" : "ArrowRight") || ["start", "end"].includes(this.placement) && event.key === "ArrowDown") {
            const currentIndex = this.tabs.findIndex((el) => el === activeEl);
            nextTab = this.findNextFocusableTab(currentIndex, "forward");
          }
          if (!nextTab) {
            return;
          }
          nextTab.tabIndex = 0;
          nextTab.focus({ preventScroll: true });
          if (this.activation === "auto") {
            this.setActiveTab(nextTab, { scrollBehavior: "smooth" });
          } else {
            this.tabs.forEach((tabEl) => {
              tabEl.tabIndex = tabEl === nextTab ? 0 : -1;
            });
          }
          if (["top", "bottom"].includes(this.placement)) {
            scrollIntoView(nextTab, this.nav, "horizontal");
          }
          event.preventDefault();
        }
      }
    }
    findNextFocusableTab(currentIndex, direction) {
      let nextTab = null;
      const iterator = direction === "forward" ? 1 : -1;
      let nextIndex = currentIndex + iterator;
      while (currentIndex < this.tabs.length) {
        nextTab = this.tabs[nextIndex] || null;
        if (nextTab === null) {
          if (direction === "forward") {
            nextTab = this.focusableTabs[0];
          } else {
            nextTab = this.focusableTabs[this.focusableTabs.length - 1];
          }
          break;
        }
        if (!nextTab.disabled) {
          break;
        }
        nextIndex += iterator;
      }
      return nextTab;
    }
    handleScrollToStart() {
      this.nav.scroll({
        left: this.localize.dir() === "rtl" ? this.nav.scrollLeft + this.nav.clientWidth : this.nav.scrollLeft - this.nav.clientWidth,
        behavior: "smooth"
      });
    }
    handleScrollToEnd() {
      this.nav.scroll({
        left: this.localize.dir() === "rtl" ? this.nav.scrollLeft - this.nav.clientWidth : this.nav.scrollLeft + this.nav.clientWidth,
        behavior: "smooth"
      });
    }
    setActiveTab(tab, options2) {
      options2 = {
        emitEvents: true,
        scrollBehavior: "auto",
        ...options2
      };
      if (tab.closest("cs-tab-group") !== this) {
        return;
      }
      if (tab !== this.activeTab && !tab.disabled) {
        const previousTab = this.activeTab;
        this.active = tab.panel;
        this.activeTab = tab;
        this.tabs.forEach((el) => {
          el.active = el === this.activeTab;
          el.tabIndex = el === this.activeTab ? 0 : -1;
        });
        this.panels.forEach((el) => el.active = el.name === this.activeTab?.panel);
        if (["top", "bottom"].includes(this.placement)) {
          scrollIntoView(this.activeTab, this.nav, "horizontal", options2.scrollBehavior);
        }
        if (options2.emitEvents) {
          if (previousTab) {
            this.dispatchEvent(new CsTabHideEvent({ name: previousTab.panel }));
          }
          this.dispatchEvent(new CsTabShowEvent({ name: this.activeTab.panel }));
        }
      }
    }
    setAriaLabels() {
      this.tabs.forEach((tab) => {
        const panel = this.panels.find((el) => el.name === tab.panel);
        if (panel) {
          tab.setAttribute("aria-controls", panel.getAttribute("id"));
          panel.setAttribute("aria-labelledby", tab.getAttribute("id"));
        }
      });
    }
    // This stores tabs and panels so we can refer to a cache instead of calling querySelectorAll() multiple times.
    syncTabsAndPanels() {
      this.tabs = this.getAllTabs();
      this.focusableTabs = this.tabs.filter((el) => !el.disabled);
      this.panels = this.getAllPanels();
      this.updateComplete.then(() => this.updateScrollControls());
    }
    updateActiveTab() {
      const tab = this.tabs.find((el) => el.panel === this.active);
      if (tab) {
        this.setActiveTab(tab, { scrollBehavior: "smooth" });
      }
    }
    updateScrollControls() {
      if (this.withoutScrollControls) {
        this.hasScrollControls = false;
      } else {
        this.hasScrollControls = ["top", "bottom"].includes(this.placement) && this.nav.scrollWidth > this.nav.clientWidth + 1;
      }
    }
    render() {
      const isRtl = this.hasUpdated ? this.localize.dir() === "rtl" : this.dir === "rtl";
      return b2`
      <div
        part="tab-group"
        class=${e7({
        "tab-group": true,
        "tab-group-top": this.placement === "top",
        "tab-group-bottom": this.placement === "bottom",
        "tab-group-start": this.placement === "start",
        "tab-group-end": this.placement === "end",
        "tab-group-has-scroll-controls": this.hasScrollControls
      })}
        @click=${this.handleClick}
        @keydown=${this.handleKeyDown}
      >
        <div class="nav-container" part="nav">
          ${this.hasScrollControls ? b2`
                  <cs-button
                    part="scroll-button scroll-button-start"
                    exportparts="button:scroll-button__button"
                    class="scroll-button scroll-button-start"
                    appearance="plain"
                    @click=${this.handleScrollToStart}
                  >
                    <cs-icon
                      name=${isRtl ? "keyboard_arrow_right" : "keyboard_arrow_left"}
                      library="system"
                      label=${this.localize.term("scrollToStart")}
                    ></cs-icon>
                  </cs-button>
                ` : ""}

          \x3C!-- We have a focus listener because in Firefox (and soon to be Chrome) overflow containers are focusable. -->
          <div class="nav" @focus=${() => this.activeTab?.focus({ preventScroll: true })}>
            <div part="tabs" class="tabs" role="tablist">
              <slot name="nav" @slotchange=${this.syncTabsAndPanels}></slot>
            </div>
          </div>

          ${this.hasScrollControls ? b2`
                  <cs-button
                    part="scroll-button scroll-button-end"
                    class="scroll-button scroll-button-end"
                    exportparts="button:scroll-button__button"
                    appearance="plain"
                    @click=${this.handleScrollToEnd}
                  >
                    <cs-icon
                      name=${isRtl ? "keyboard_arrow_left" : "keyboard_arrow_right"}
                      library="system"
                      label=${this.localize.term("scrollToEnd")}
                    ></cs-icon>
                  </cs-button>
                ` : ""}
        </div>

        <div part="body" class="body"><slot @slotchange=${this.syncTabsAndPanels}></slot></div>
      </div>
    `;
    }
  };
  CsTabGroup.css = tab_group_styles_default;
  __decorateClass([
    e5(".tab-group")
  ], CsTabGroup.prototype, "tabGroup", 2);
  __decorateClass([
    e5(".body slot")
  ], CsTabGroup.prototype, "defaultSlot", 2);
  __decorateClass([
    e5(".nav")
  ], CsTabGroup.prototype, "nav", 2);
  __decorateClass([
    r5()
  ], CsTabGroup.prototype, "hasScrollControls", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsTabGroup.prototype, "active", 2);
  __decorateClass([
    n4()
  ], CsTabGroup.prototype, "placement", 2);
  __decorateClass([
    n4()
  ], CsTabGroup.prototype, "activation", 2);
  __decorateClass([
    n4({ attribute: "without-scroll-controls", type: Boolean })
  ], CsTabGroup.prototype, "withoutScrollControls", 2);
  __decorateClass([
    watch("active")
  ], CsTabGroup.prototype, "updateActiveTab", 1);
  __decorateClass([
    watch("withoutScrollControls", { waitUntilFirstUpdate: true })
  ], CsTabGroup.prototype, "updateScrollControls", 1);
  CsTabGroup = __decorateClass([
    customElement("cs-tab-group")
  ], CsTabGroup);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.JYSS4DK4.js
  var tab_panel_styles_default = i`
  :host {
    --padding: 0;

    display: none;
  }

  :host([active]) {
    display: block;
  }

  .tab-panel {
    display: block;
    padding: var(--padding);
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.YBRGR5SM.js
  var id2 = 0;
  var CsTabPanel = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.attrId = ++id2;
      this.componentId = `cs-tab-panel-${this.attrId}`;
      this.name = "";
      this.active = false;
      this.role = "tabpanel";
    }
    connectedCallback() {
      super.connectedCallback();
      this.id = (this.id || "").length > 0 ? this.id : this.componentId;
    }
    handleActiveChange() {
      this.setAttribute("aria-hidden", this.active ? "false" : "true");
    }
    render() {
      return b2`
      <slot
        class=${e7({
        "tab-panel": true,
        "tab-panel-active": this.active
      })}
      ></slot>
    `;
    }
  };
  CsTabPanel.css = tab_panel_styles_default;
  __decorateClass([
    n4({ reflect: true })
  ], CsTabPanel.prototype, "name", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsTabPanel.prototype, "active", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsTabPanel.prototype, "role", 2);
  __decorateClass([
    watch("active")
  ], CsTabPanel.prototype, "handleActiveChange", 1);
  CsTabPanel = __decorateClass([
    customElement("cs-tab-panel")
  ], CsTabPanel);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.NZ7XRXTS.js
  var textarea_styles_default = i`
  :host {
    border-width: 0;
  }

  .textarea {
    display: grid;
    align-items: center;
    margin: 0;
    border: none;
    outline: none;
    cursor: inherit;
    font: inherit;
    background-color: var(--cs-form-control-background-color);
    border-color: var(--cs-form-control-border-color);
    border-radius: var(--cs-form-control-border-radius);
    border-style: var(--cs-form-control-border-style);
    border-width: var(--cs-form-control-border-width);
    -webkit-appearance: none;
    outline: var(--cs-focus-ring-style) var(--cs-focus-ring-width) transparent;
    outline-offset: var(--cs-focus-ring-offset);

    &:focus-within {
      outline-color: var(--cs-color-focus);
    }

    /* Style disabled textareas */
    &:has(:disabled) {
      cursor: not-allowed;
      opacity: 0.5;
    }
  }

  /* Appearance modifiers */
  :host([appearance='outlined']) .textarea {
    background-color: var(--cs-form-control-background-color);
    border-color: var(--cs-form-control-border-color);
  }

  :host([appearance='filled']) .textarea {
    background-color: var(--cs-color-neutral-fill-quiet);
    border-color: var(--cs-color-neutral-fill-quiet);
  }

  :host([appearance='filled-outlined']) .textarea {
    background-color: var(--cs-color-neutral-fill-quiet);
    border-color: var(--cs-form-control-border-color);
  }

  textarea {
    display: block;
    width: 100%;
    border: none;
    background: transparent;
    font: inherit;
    color: inherit;
    cursor: inherit;
    scroll-padding-block: var(--cs-form-control-padding-block);
    padding: calc(var(--cs-form-control-padding-block) - ((1lh - 1em) / 2)) var(--cs-form-control-padding-inline); /* accounts for the larger line height of textarea content */
    min-height: calc(var(--cs-form-control-height) - var(--border-width) * 2);
    box-shadow: none;
    margin: 0;

    &::placeholder {
      color: var(--cs-form-control-placeholder-color);
      user-select: none;
      -webkit-user-select: none;
    }

    &:autofill {
      &,
      &:hover,
      &:focus,
      &:active {
        box-shadow: none;
        caret-color: var(--cs-form-control-value-color);
      }
    }

    &:focus {
      outline: none;
    }
  }

  /* Shared textarea and size-adjuster positioning */
  .control,
  .size-adjuster {
    grid-area: 1 / 1 / 2 / 2;
  }

  .size-adjuster {
    visibility: hidden;
    pointer-events: none;
    opacity: 0;
    padding: 0;
  }

  textarea::-webkit-search-decoration,
  textarea::-webkit-search-cancel-button,
  textarea::-webkit-search-results-button,
  textarea::-webkit-search-results-decoration {
    -webkit-appearance: none;
  }

  /*
   * Resize types
   */

  :host([resize='none']) textarea {
    resize: none;
  }

  textarea,
  :host([resize='vertical']) textarea {
    resize: vertical;
  }

  :host([resize='horizontal']) textarea {
    resize: horizontal;
  }

  :host([resize='both']) textarea {
    resize: both;
  }

  :host([resize='auto']) textarea {
    height: auto;
    resize: none;
    overflow-y: hidden;
  }

  /*
   * Footer (hint + character count)
   */

  /*
   * This element carries the hint part, so the shared form control styles apply to it. Those styles set display:block
   * and hide the element when it has no hint, both of which have to be undone when a character count is present.
   */
  .footer.has-slotted,
  .footer.has-count {
    display: flex;
    align-items: baseline;
    gap: 1em;
  }

  /* Slots default to display:contents, which would leave the hint unable to shrink below its content */
  .footer.has-count .hint {
    display: block;
    flex: 1 1 auto;
    min-width: 0;
  }

  .count {
    flex: 0 0 auto;
    color: var(--cs-form-control-hint-color);
    font-weight: var(--cs-form-control-hint-font-weight);
    line-height: var(--cs-form-control-hint-line-height);
    margin-block-start: 0.5em;
    font-size: var(--cs-font-size-smaller);
    margin-inline-start: auto;
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.RLM6SOMP.js
  var CsTextarea = class extends CornerstoneFormAssociatedElement {
    constructor() {
      super(...arguments);
      this.assumeInteractionOn = ["blur", "input"];
      this.hasSlotController = new HasSlotController(this, "hint", "label");
      this.localize = new LocalizeController2(this);
      this.announcedCountText = "";
      this.title = "";
      this.name = null;
      this._value = null;
      this.defaultValue = this.getAttribute("value") ?? "";
      this.size = "m";
      this.appearance = "outlined";
      this.label = "";
      this.hint = "";
      this.placeholder = "";
      this.rows = 4;
      this.resize = "vertical";
      this.disabled = false;
      this.readonly = false;
      this.required = false;
      this.spellcheck = true;
      this.ssrLabel = false;
      this.ssrHint = false;
      this.withCount = false;
      this.lastObservedWidth = 0;
    }
    static get validators() {
      return [...super.validators, MirrorValidator()];
    }
    /** The current value of the input, submitted as a name/value pair with form data. */
    get value() {
      if (this.valueHasChanged) {
        return this._value;
      }
      return this._value ?? this.defaultValue;
    }
    set value(val) {
      if (this._value === val) {
        return;
      }
      this.valueHasChanged = true;
      this._value = val;
    }
    connectedCallback() {
      super.connectedCallback();
      this.updateComplete.then(() => {
        this.setTextareaDimensions();
        this.updateResizeObserver();
        if (this.didSSR && this.input && this.value !== this.input.value) {
          const value = this.input.value;
          this.value = value;
        }
      });
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      clearTimeout(this.countAnnounceTimeout);
      this.resizeObserver?.disconnect();
      this.resizeObserver = void 0;
    }
    /**
     * @internal
     */
    updateFormValue(value) {
      if (value == null) {
        this.setValue("", null);
        return;
      }
      super.updateFormValue(value);
    }
    /** Creates or destroys the resize observer based on the current resize mode. */
    updateResizeObserver() {
      const needsObserver = this.resize !== "none";
      if (this.resizeObserver) {
        this.resizeObserver.disconnect();
        this.resizeObserver = void 0;
      }
      if (needsObserver && this.input) {
        if (this.resize === "auto") {
          this.resizeObserver = new ResizeObserver((entries) => {
            const width = entries[0]?.contentRect.width ?? 0;
            if (width !== this.lastObservedWidth) {
              this.lastObservedWidth = width;
              requestAnimationFrame(() => this.setTextareaDimensions());
            }
          });
          this.resizeObserver.observe(this);
        } else {
          this.resizeObserver = new ResizeObserver(() => this.setTextareaDimensions());
          this.resizeObserver.observe(this.input);
        }
      }
    }
    handleBlur() {
      this.checkValidity();
    }
    handleChange(event) {
      this.valueHasChanged = true;
      this.value = this.input.value;
      this.setTextareaDimensions();
      this.checkValidity();
      this.relayNativeEvent(event, { bubbles: true, composed: true });
    }
    handleInput(event) {
      this.valueHasChanged = true;
      this.value = this.input.value;
      this.relayNativeEvent(event, { bubbles: true, composed: true });
      this.scheduleCountAnnouncement();
    }
    scheduleCountAnnouncement() {
      clearTimeout(this.countAnnounceTimeout);
      this.countAnnounceTimeout = setTimeout(() => {
        const currentLength = (this.value ?? "").length;
        this.announcedCountText = this.maxlength != null ? this.localize.term("numCharactersRemaining", this.maxlength - currentLength) : this.localize.term("numCharacters", currentLength);
      }, 1e3);
    }
    setTextareaDimensions() {
      if (this.resize === "none") {
        this.base.style.width = ``;
        this.base.style.height = ``;
        return;
      }
      if (this.resize === "auto") {
        this.sizeAdjuster.style.height = `${this.input.clientHeight}px`;
        this.input.style.height = "auto";
        const newHeight = this.input.scrollHeight;
        this.input.style.height = `${newHeight}px`;
        this.sizeAdjuster.style.height = `${newHeight}px`;
        this.base.style.width = ``;
        this.base.style.height = ``;
        return;
      }
      if (this.input.style.width) {
        const width = Number(this.input.style.width.split(/px/)[0]) + 2;
        this.base.style.width = `${width}px`;
      }
      if (this.input.style.height) {
        const height = Number(this.input.style.height.split(/px/)[0]) + 2;
        this.base.style.height = `${height}px`;
      }
    }
    handleRowsChange() {
      this.setTextareaDimensions();
    }
    async handleValueChange() {
      await this.updateComplete;
      this.checkValidity();
      this.setTextareaDimensions();
    }
    updated(changedProperties) {
      if (changedProperties.has("resize")) {
        this.setTextareaDimensions();
        this.updateResizeObserver();
      }
      super.updated(changedProperties);
      if (changedProperties.has("value")) {
        this.customStates.set("blank", !this.value);
      }
    }
    /** Sets focus on the textarea. */
    focus(options2) {
      this.input.focus(options2);
    }
    /** Removes focus from the textarea. */
    blur() {
      this.input.blur();
    }
    /** Selects all the text in the textarea. */
    select() {
      this.input.select();
    }
    /** Gets or sets the textarea's scroll position. */
    scrollPosition(position) {
      if (position) {
        if (typeof position.top === "number") {
          this.input.scrollTop = position.top;
        }
        if (typeof position.left === "number") {
          this.input.scrollLeft = position.left;
        }
        return void 0;
      }
      return {
        top: this.input.scrollTop,
        left: this.input.scrollLeft
      };
    }
    /** Sets the start and end positions of the text selection (0-based). */
    setSelectionRange(selectionStart, selectionEnd, selectionDirection = "none") {
      this.input.setSelectionRange(selectionStart, selectionEnd, selectionDirection);
    }
    /** Replaces a range of text with a new string. */
    setRangeText(replacement, start, end, selectMode = "preserve") {
      const selectionStart = start ?? this.input.selectionStart;
      const selectionEnd = end ?? this.input.selectionEnd;
      this.input.setRangeText(replacement, selectionStart, selectionEnd, selectMode);
      if (this.value !== this.input.value) {
        this.value = this.input.value;
        this.setTextareaDimensions();
      }
    }
    formResetCallback() {
      this._value = null;
      if (this.input) {
        this.input.value = this.value || "";
      }
      super.formResetCallback();
    }
    render() {
      const hasLabelSlot = this.hasSlotController.test("label", "ssrLabel");
      const hasHintSlot = this.hasSlotController.test("hint", "ssrHint");
      const hasLabel = this.label ? true : !!hasLabelSlot;
      const hasHint = this.hint ? true : !!hasHintSlot;
      const currentLength = (this.value ?? "").length;
      const countText = this.maxlength != null ? this.localize.term("numCharactersRemaining", this.maxlength - currentLength) : this.localize.term("numCharacters", currentLength);
      return b2`
      <label
        part="form-control-label"
        class=${e7({
        label: true,
        "has-label": hasLabel
      })}
        for="input"
        aria-hidden=${hasLabel ? "false" : "true"}
      >
        <slot name="label">${this.label}</slot>
      </label>

      <div part="textarea-wrapper" class="textarea">
        <textarea
          part="textarea"
          id="input"
          class="control"
          title=${this.title}
          name=${o8(this.name)}
          .value=${l5(this.value)}
          ?disabled=${this.disabled}
          ?readonly=${this.readonly}
          ?required=${this.required}
          placeholder=${o8(this.placeholder)}
          rows=${o8(this.rows)}
          minlength=${o8(this.minlength)}
          maxlength=${o8(this.maxlength)}
          autocapitalize=${o8(this.autocapitalize)}
          autocorrect=${o8(this.autocorrect)}
          ?autofocus=${this.autofocus}
          spellcheck=${o8(this.spellcheck)}
          enterkeyhint=${o8(this.enterkeyhint)}
          inputmode=${o8(this.inputmode)}
          aria-describedby="hint"
          @change=${this.handleChange}
          @input=${this.handleInput}
          @blur=${this.handleBlur}
        ></textarea>

        \x3C!-- This "adjuster" exists to prevent layout shifting. https://github.com/shoelace-style/shoelace/issues/2180 -->
        <div part="textarea-adjuster" class="size-adjuster" ?hidden=${this.resize !== "auto"}></div>
      </div>

      <div
        part="hint"
        class=${e7({
        footer: true,
        "has-count": this.withCount,
        "has-slotted": hasHint
      })}
      >
        <slot id="hint" name="hint" class="hint" aria-hidden=${hasHint ? "false" : "true"}>${this.hint}</slot>

        ${this.withCount ? b2`
                <div part="count" class="count" aria-hidden="true">${countText}</div>
                <div class="cs-visually-hidden-force" aria-live="polite">${this.announcedCountText}</div>
              ` : ""}
      </div>
    `;
    }
  };
  CsTextarea.css = [textarea_styles_default, form_control_styles_default, size_styles_default, visually_hidden_styles_default];
  __decorateClass([
    r5()
  ], CsTextarea.prototype, "announcedCountText", 2);
  __decorateClass([
    e5(".control")
  ], CsTextarea.prototype, "input", 2);
  __decorateClass([
    e5('[part~="textarea-wrapper"]')
  ], CsTextarea.prototype, "base", 2);
  __decorateClass([
    e5(".size-adjuster")
  ], CsTextarea.prototype, "sizeAdjuster", 2);
  __decorateClass([
    n4()
  ], CsTextarea.prototype, "title", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsTextarea.prototype, "name", 2);
  __decorateClass([
    r5()
  ], CsTextarea.prototype, "value", 1);
  __decorateClass([
    n4({ attribute: "value", reflect: true })
  ], CsTextarea.prototype, "defaultValue", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsTextarea.prototype, "size", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsTextarea.prototype, "appearance", 2);
  __decorateClass([
    n4()
  ], CsTextarea.prototype, "label", 2);
  __decorateClass([
    n4({ attribute: "hint" })
  ], CsTextarea.prototype, "hint", 2);
  __decorateClass([
    n4()
  ], CsTextarea.prototype, "placeholder", 2);
  __decorateClass([
    n4({ type: Number })
  ], CsTextarea.prototype, "rows", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsTextarea.prototype, "resize", 2);
  __decorateClass([
    n4({ type: Boolean })
  ], CsTextarea.prototype, "disabled", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsTextarea.prototype, "readonly", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsTextarea.prototype, "required", 2);
  __decorateClass([
    n4({ type: Number })
  ], CsTextarea.prototype, "minlength", 2);
  __decorateClass([
    n4({ type: Number })
  ], CsTextarea.prototype, "maxlength", 2);
  __decorateClass([
    n4()
  ], CsTextarea.prototype, "autocapitalize", 2);
  __decorateClass([
    n4({
      type: Boolean,
      converter: {
        fromAttribute: (value) => !value || value === "off" ? false : true,
        toAttribute: (value) => value ? "on" : "off"
      }
    })
  ], CsTextarea.prototype, "autocorrect", 2);
  __decorateClass([
    n4()
  ], CsTextarea.prototype, "autocomplete", 2);
  __decorateClass([
    n4({ type: Boolean })
  ], CsTextarea.prototype, "autofocus", 2);
  __decorateClass([
    n4()
  ], CsTextarea.prototype, "enterkeyhint", 2);
  __decorateClass([
    n4({
      type: Boolean,
      converter: {
        // Allow "true|false" attribute values but keep the property boolean
        fromAttribute: (value) => !value || value === "false" ? false : true,
        toAttribute: (value) => value ? "true" : "false"
      }
    })
  ], CsTextarea.prototype, "spellcheck", 2);
  __decorateClass([
    n4()
  ], CsTextarea.prototype, "inputmode", 2);
  __decorateClass([
    n4({ attribute: "ssr-label", type: Boolean })
  ], CsTextarea.prototype, "ssrLabel", 2);
  __decorateClass([
    n4({ attribute: "ssr-hint", type: Boolean })
  ], CsTextarea.prototype, "ssrHint", 2);
  __decorateClass([
    n4({ attribute: "with-count", type: Boolean, reflect: true })
  ], CsTextarea.prototype, "withCount", 2);
  __decorateClass([
    watch("rows", { waitUntilFirstUpdate: true })
  ], CsTextarea.prototype, "handleRowsChange", 1);
  __decorateClass([
    watch("value", { waitUntilFirstUpdate: true })
  ], CsTextarea.prototype, "handleValueChange", 1);
  CsTextarea = __decorateClass([
    customElement("cs-textarea")
  ], CsTextarea);
  CsTextarea.disableWarning?.("change-in-update");

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.UN7CJLC5.js
  var layoutCache = /* @__PURE__ */ new Map();
  function buildTimeSegmentLayout(locale, opts) {
    const key = `${locale || "en"}|${opts.hour12 ? 12 : 24}|${opts.withSeconds ? 1 : 0}`;
    const cached = layoutCache.get(key);
    if (cached) {
      return cached;
    }
    const formatter = new Intl.DateTimeFormat(locale || "en", {
      hour: "2-digit",
      minute: "2-digit",
      second: opts.withSeconds ? "2-digit" : void 0,
      hour12: opts.hour12,
      calendar: "gregory",
      numberingSystem: "latn"
    });
    const probeDate = new Date(2026, 0, 1, 13, 45, 30);
    const parts = formatter.formatToParts(probeDate);
    const tokens = [];
    const order = [];
    for (const part of parts) {
      if (part.type === "hour") {
        tokens.push({ kind: "segment", field: "hour" });
        order.push("hour");
      } else if (part.type === "minute") {
        tokens.push({ kind: "segment", field: "minute" });
        order.push("minute");
      } else if (part.type === "second") {
        tokens.push({ kind: "segment", field: "second" });
        order.push("second");
      } else if (part.type === "dayPeriod") {
        tokens.push({ kind: "segment", field: "dayPeriod" });
        order.push("dayPeriod");
      } else if (part.type === "literal") {
        tokens.push({ kind: "literal", text: part.value });
      }
    }
    const expectedSegments = 2 + (opts.withSeconds ? 1 : 0) + (opts.hour12 ? 1 : 0);
    if (order.length !== expectedSegments) {
      const fallbackTokens = [
        { kind: "segment", field: "hour" },
        { kind: "literal", text: ":" },
        { kind: "segment", field: "minute" }
      ];
      const fallbackOrder = ["hour", "minute"];
      if (opts.withSeconds) {
        fallbackTokens.push({ kind: "literal", text: ":" });
        fallbackTokens.push({ kind: "segment", field: "second" });
        fallbackOrder.push("second");
      }
      if (opts.hour12) {
        fallbackTokens.push({ kind: "literal", text: " " });
        fallbackTokens.push({ kind: "segment", field: "dayPeriod" });
        fallbackOrder.push("dayPeriod");
      }
      const fallback2 = { tokens: fallbackTokens, order: fallbackOrder };
      layoutCache.set(key, fallback2);
      return fallback2;
    }
    const layout = { tokens, order };
    layoutCache.set(key, layout);
    return layout;
  }
  function resolveHour12(locale) {
    try {
      const probe = new Intl.DateTimeFormat(locale || "en", { hour: "numeric" });
      return probe.resolvedOptions().hour12 ?? false;
    } catch {
      return false;
    }
  }
  function formatDayPeriod(locale, period) {
    try {
      const formatter = new Intl.DateTimeFormat(locale || "en", { hour: "numeric", hour12: true });
      const date = new Date(2026, 0, 1, period === 0 ? 9 : 15);
      const parts = formatter.formatToParts(date);
      const dp = parts.find((p4) => p4.type === "dayPeriod");
      return dp?.value || (period === 0 ? "AM" : "PM");
    } catch {
      return period === 0 ? "AM" : "PM";
    }
  }
  function timeSegmentBounds(field, hour12) {
    if (field === "hour") {
      return hour12 ? { min: 1, max: 12 } : { min: 0, max: 23 };
    }
    if (field === "minute" || field === "second") {
      return { min: 0, max: 59 };
    }
    return { min: 0, max: 1 };
  }
  function stepTimeSegment(segments, field, delta, hour12, now = /* @__PURE__ */ new Date()) {
    const next = { ...segments };
    const current = segments[field];
    if (field === "dayPeriod") {
      const seed = current == null ? now.getHours() < 12 ? 0 : 1 : current;
      next.dayPeriod = seed === 0 ? 1 : 0;
      return next;
    }
    const { min: min2, max: max2 } = timeSegmentBounds(field, hour12);
    if (current == null) {
      if (field === "hour") {
        const h24 = now.getHours();
        next.hour = hour12 ? h24 % 12 || 12 : h24;
      } else if (field === "minute") {
        next.minute = now.getMinutes();
      } else {
        next.second = now.getSeconds();
      }
      return next;
    }
    const span = max2 - min2 + 1;
    const stepped = ((current - min2 + delta) % span + span) % span + min2;
    if (field === "hour") {
      next.hour = stepped;
    } else if (field === "minute") {
      next.minute = stepped;
    } else {
      next.second = stepped;
    }
    return next;
  }
  function typeTimeDigit(field, buffer, digit, hour12) {
    if (!/^[0-9]$/.test(digit)) {
      return { value: bufferToValue(buffer), buffer, advance: false };
    }
    if (field === "dayPeriod") {
      return { value: bufferToValue(buffer), buffer, advance: false };
    }
    if (field === "hour") {
      if (hour12) {
        return typeNumericSegment(buffer, digit, 1, 12);
      }
      return typeNumericSegment(buffer, digit, 0, 23);
    }
    return typeNumericSegment(buffer, digit, 0, 59);
  }
  function typeNumericSegment(buffer, digit, min2, max2) {
    const d3 = Number(digit);
    if (buffer === "") {
      if (d3 === 0 && min2 === 0) {
        return { value: 0, buffer: "0", advance: false };
      }
      if (d3 === 0) {
        return { value: null, buffer: "0", advance: false };
      }
      if (d3 * 10 > max2) {
        return { value: clamp3(d3, min2, max2), buffer: "", advance: true };
      }
      return { value: d3, buffer: digit, advance: false };
    }
    const combined = Number(buffer + digit);
    if (combined >= min2 && combined <= max2) {
      return { value: combined, buffer: "", advance: true };
    }
    if (buffer === "0" && d3 === 0) {
      return { value: min2 === 0 ? 0 : null, buffer: "0", advance: false };
    }
    return typeNumericSegment("", digit, min2, max2);
  }
  function clamp3(value, min2, max2) {
    return Math.min(max2, Math.max(min2, value));
  }
  function bufferToValue(buffer) {
    if (!buffer) {
      return null;
    }
    const n9 = Number(buffer);
    return Number.isFinite(n9) ? n9 : null;
  }
  function dayPeriodFromKey(key) {
    if (key === "a" || key === "A") {
      return 0;
    }
    if (key === "p" || key === "P") {
      return 1;
    }
    return null;
  }
  function formatTimeSegmentText(field, value, buffer, placeholder, locale) {
    if (field === "dayPeriod") {
      if (value == null) {
        return placeholder;
      }
      return formatDayPeriod(locale, value);
    }
    if (buffer) {
      return buffer.padStart(2, "0");
    }
    if (value == null) {
      return placeholder;
    }
    return String(value).padStart(2, "0");
  }
  function isTimeComplete(segments, opts) {
    if (segments.hour == null || segments.minute == null) {
      return false;
    }
    if (opts.withSeconds && segments.second == null) {
      return false;
    }
    if (opts.hour12 && segments.dayPeriod == null) {
      return false;
    }
    return true;
  }
  function isTimeEmpty(segments) {
    return segments.hour == null && segments.minute == null && segments.second == null && segments.dayPeriod == null;
  }
  function timeSegmentsToWire(segments, opts) {
    if (!isTimeComplete(segments, opts)) {
      return "";
    }
    let h24 = segments.hour;
    if (opts.hour12) {
      const period = segments.dayPeriod;
      h24 = h24 === 12 ? period === 0 ? 0 : 12 : period === 1 ? h24 + 12 : h24;
    }
    if (h24 < 0 || h24 > 23) {
      return "";
    }
    const min2 = segments.minute;
    if (min2 < 0 || min2 > 59) {
      return "";
    }
    const hh = String(h24).padStart(2, "0");
    const mm = String(min2).padStart(2, "0");
    if (!opts.withSeconds) {
      return `${hh}:${mm}`;
    }
    const sec = segments.second;
    if (sec < 0 || sec > 59) {
      return "";
    }
    const ss = String(sec).padStart(2, "0");
    return `${hh}:${mm}:${ss}`;
  }
  function wireToTimeSegments(wire, opts) {
    const empty = { hour: null, minute: null, second: null, dayPeriod: null };
    if (!wire) {
      return empty;
    }
    const match = /^(\d{1,2}):(\d{2})(?::(\d{2}(?:\.\d+)?))?$/.exec(wire);
    if (!match) {
      return empty;
    }
    const h24 = Number(match[1]);
    const min2 = Number(match[2]);
    const sec = match[3] != null ? Math.trunc(Number(match[3])) : null;
    if (!Number.isFinite(h24) || !Number.isFinite(min2)) {
      return empty;
    }
    if (h24 < 0 || h24 > 23 || min2 < 0 || min2 > 59) {
      return empty;
    }
    if (sec != null && (sec < 0 || sec > 59)) {
      return empty;
    }
    let displayHour;
    let dayPeriod = null;
    if (opts.hour12) {
      dayPeriod = h24 >= 12 ? 1 : 0;
      displayHour = h24 % 12 || 12;
    } else {
      displayHour = h24;
    }
    return {
      hour: displayHour,
      minute: min2,
      second: opts.withSeconds ? sec ?? 0 : null,
      dayPeriod: opts.hour12 ? dayPeriod : null
    };
  }
  function withSecondsForStep(step) {
    if (step === "any") {
      return true;
    }
    if (!Number.isFinite(step) || step <= 0) {
      return false;
    }
    return step < 60 || step % 60 !== 0;
  }
  function timeSegmentRules(opts) {
    const now = opts.now ?? (() => /* @__PURE__ */ new Date());
    return {
      typeDigit: (group, field, buffer, digit) => {
        const result = typeTimeDigit(field, buffer, digit, opts.hour12());
        const segments = opts.getSegments(group);
        const next = { ...segments, [field]: result.value };
        opts.setSegments(group, next);
        return result;
      },
      step: (group, field, delta) => {
        const next = stepTimeSegment(opts.getSegments(group), field, delta, opts.hour12(), now());
        opts.setSegments(group, next);
        return { value: next[field] };
      },
      bounds: (_g, field) => timeSegmentBounds(field, opts.hour12()),
      commitBuffer: (group, field, buffer) => {
        const value = bufferToValue(buffer);
        const segments = opts.getSegments(group);
        opts.setSegments(group, { ...segments, [field]: value });
        return value;
      },
      clear: (group, field) => {
        const segments = opts.getSegments(group);
        if (segments[field] == null) {
          return false;
        }
        opts.setSegments(group, { ...segments, [field]: null });
        return true;
      }
    };
  }

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.F6RDAE4S.js
  var time_input_styles_default = i`
  :host {
    --show-duration: var(--cs-transition-fast);
    --hide-duration: var(--cs-transition-fast);
    --column-item-height: 2.25em;
    --column-width: 3em;
  }

  :host(:state(disabled)) {
    cursor: not-allowed;
  }

  /* Popup */
  .time-input-popup {
    flex: 1 1 auto;
    display: inline-flex;
    width: 100%;
    position: relative;
    vertical-align: middle;
    --show-duration: inherit;
    --hide-duration: inherit;

    &::part(popup) {
      z-index: 900;
    }

    &[data-current-placement^='top']::part(popup) {
      transform-origin: bottom;
    }

    &[data-current-placement^='bottom']::part(popup) {
      transform-origin: top;
    }
  }

  /* Popup body — bordered card with the column listboxes. */
  .popup-body {
    display: inline-flex;
    flex-direction: column;
    background-color: var(--cs-color-surface-raised);
    border: var(--cs-border-style) var(--cs-border-width-s) var(--cs-color-surface-border);
    border-radius: var(--cs-border-radius-m);
    box-shadow: var(--cs-shadow-m);
    color: var(--cs-color-text-normal);
    font-size: inherit;
    padding: var(--cs-space-2xs);
  }

  .columns {
    display: inline-flex;
    gap: var(--cs-space-2xs);
    align-items: stretch;
  }

  .column {
    display: flex;
    flex-direction: column;
    width: var(--column-width);
    max-height: calc(var(--column-item-height) * 7);
    overflow-y: auto;
    scroll-snap-type: y mandatory;
    scrollbar-width: none;
    /* Don't let column scroll bubble to the page. */
    overscroll-behavior: contain;
    outline: none;
    border-radius: var(--cs-border-radius-s);
  }

  .column::-webkit-scrollbar {
    display: none;
  }

  .column:focus-visible {
    outline: var(--cs-focus-ring-style) var(--cs-focus-ring-width) var(--cs-color-focus);
    outline-offset: 2px;
  }

  .column-item {
    flex: 0 0 var(--column-item-height);
    display: inline-flex;
    align-items: center;
    justify-content: center;
    font: inherit;
    font-variant-numeric: tabular-nums;
    cursor: pointer;
    scroll-snap-align: center;
    border-radius: var(--cs-border-radius-s);
    color: var(--cs-color-text-normal);
    background: transparent;
    border: none;
    padding: 0;
    user-select: none;
    transition:
      background-color var(--cs-transition-fast),
      color var(--cs-transition-fast);
  }

  .column-item:hover:not([aria-disabled='true']):not([aria-selected='true']) {
    background-color: var(--cs-color-neutral-fill-quiet);
  }

  .column-item[aria-selected='true'] {
    background-color: var(--cs-color-brand-fill-loud);
    color: var(--cs-color-brand-on-loud);
  }

  .column-item[aria-disabled='true'] {
    color: var(--cs-color-text-quiet);
    cursor: not-allowed;
  }

  /* Footer / Now button */
  .popup-footer {
    display: flex;
    align-items: center;
    justify-content: flex-end;
    gap: var(--cs-space-xs);
    margin-top: var(--cs-space-xs);
    padding-top: var(--cs-space-xs);
    border-top: var(--cs-border-style) var(--cs-border-width-s) var(--cs-color-surface-border);
  }

  .now-button {
    appearance: none;
    background: transparent;
    border: var(--cs-border-style) var(--cs-border-width-s) var(--cs-color-surface-border);
    border-radius: var(--cs-border-radius-s);
    padding: var(--cs-space-2xs) var(--cs-space-s);
    font: inherit;
    color: inherit;
    cursor: pointer;
    transition: background-color var(--cs-transition-fast);
  }

  .now-button:hover {
    background-color: var(--cs-color-neutral-fill-quiet);
  }

  .now-button:focus-visible {
    outline: var(--cs-focus-ring-style) var(--cs-focus-ring-width) var(--cs-color-focus);
    outline-offset: 2px;
  }

  /* Input wrapper */
  .input-wrapper {
    flex: 1;
    display: flex;
    width: 100%;
    min-width: 0;
    align-items: center;
    min-height: var(--cs-form-control-height);
    background-color: var(--cs-form-control-background-color);
    border-color: var(--cs-form-control-border-color);
    border-radius: var(--cs-form-control-border-radius);
    border-style: var(--cs-form-control-border-style);
    border-width: var(--cs-form-control-border-width);
    color: var(--cs-form-control-value-color);
    cursor: text;
    font-family: inherit;
    font-weight: var(--cs-form-control-value-font-weight);
    line-height: var(--cs-form-control-value-line-height);
    padding: 0 var(--cs-form-control-padding-inline);
    transition:
      background-color var(--cs-transition-normal),
      border-color var(--cs-transition-normal),
      outline-color var(--cs-transition-fast);
    transition-timing-function: var(--cs-transition-easing);
    outline: var(--cs-focus-ring-style) var(--cs-focus-ring-width) transparent;
    outline-offset: var(--cs-focus-ring-offset);
  }

  :host([pill]) .input-wrapper {
    border-radius: var(--cs-border-radius-pill);
  }

  :host(:focus-within) .input-wrapper {
    outline-color: var(--cs-color-focus);
  }

  :host(:state(disabled)) .input-wrapper {
    opacity: 0.5;
    cursor: not-allowed;
  }

  /* Appearance variants */
  :host([appearance='filled']) .input-wrapper,
  :host([appearance='filled-outlined']) .input-wrapper {
    background-color: var(--cs-color-surface-lowered);
  }

  :host([appearance='filled']) .input-wrapper {
    border-color: transparent;
  }

  /* Segmented input — same patterns as cs-date-input. */
  .segments {
    flex: 1;
    min-width: 0;
    display: inline-flex;
    align-items: center;
    flex-wrap: nowrap;
    color: inherit;
    font: inherit;
    font-variant-numeric: tabular-nums;
    caret-color: transparent;
  }

  .segment {
    display: inline-block;
    padding: 0 0.15em;
    margin: 0;
    background: transparent;
    border: none;
    outline: none;
    color: inherit;
    font: inherit;
    text-align: center;
    cursor: text;
    user-select: none;
    white-space: nowrap;
    border-radius: var(--cs-border-radius-s);
    transition:
      background-color var(--cs-transition-fast),
      color var(--cs-transition-fast);
  }

  .segment.empty {
    color: var(--cs-color-text-quiet);
  }

  /* Focus style — applies to keyboard *and* pointer focus so a click always shows the selection. Soft brand fill
     reads as "selected" without competing with the popup's loud selected items. */
  .segment:focus {
    background-color: var(--cs-color-brand-fill-quiet);
    color: var(--cs-color-brand-on-quiet);
    outline: none;
  }

  .segment.empty:focus {
    color: var(--cs-color-brand-on-quiet);
  }

  .segment-literal {
    display: inline-block;
    color: var(--cs-color-text-quiet);
    white-space: pre;
    user-select: none;
  }

  :host([disabled]) .segment,
  :host([readonly]) .segment {
    cursor: inherit;
  }

  /* Hidden form-value input (anchored under the wrapper for native validity tooltips). */
  .value-input {
    position: absolute;
    inset-inline-start: var(--cs-form-control-padding-inline);
    inset-block-start: 50%;
    transform: translateY(-50%);
    width: 1px;
    height: 1px;
    opacity: 0;
    pointer-events: none;
    border: none;
    padding: 0;
    margin: 0;
  }

  /* Trailing buttons (.clear-button, .expand-button), the .expand-icon box, and the start/end
     decoration slots are shared with <cs-date-input> via segmentedFieldStyles so both pickers
     stay on <cs-select>'s trailing optical axis. See segmented-field.styles.ts. */

  /* Animations */
  .time-input-popup::part(popup).show {
    animation: cs-time-input-show var(--show-duration) var(--cs-transition-easing);
  }

  .time-input-popup::part(popup).hide {
    animation: cs-time-input-hide var(--hide-duration) var(--cs-transition-easing);
  }

  @keyframes cs-time-input-show {
    from {
      opacity: 0;
      transform: scale(0.97);
    }
    to {
      opacity: 1;
      transform: scale(1);
    }
  }

  @keyframes cs-time-input-hide {
    from {
      opacity: 1;
      transform: scale(1);
    }
    to {
      opacity: 0;
      transform: scale(0.97);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    :host {
      --show-duration: 0ms;
      --hide-duration: 0ms;
    }
    .column {
      scroll-behavior: auto;
    }
  }

  /* Visually hidden helper */
  .visually-hidden {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.7KLZQDV2.js
  var DEFAULT_SEPARATORS = ["/", ".", "-", ":", ",", " "];
  var SegmentedFieldController = class {
    constructor(host, config) {
      this.buffers = /* @__PURE__ */ new Map();
      this.active = null;
      this.handleFocus = (event) => {
        const el = event.currentTarget;
        const group = el.dataset.group;
        const field = el.dataset.segment;
        this.active = { group, field };
        for (const segment of this.segmentElements()) {
          segment.tabIndex = segment === el ? 0 : -1;
        }
      };
      this.handleBlur = (event) => {
        const el = event.currentTarget;
        const group = el.dataset.group;
        const field = el.dataset.segment;
        if (this.getBuffer(group, field)) {
          this.flushBuffer(group, field);
        }
      };
      this.handleKeyDown = (event) => {
        const el = event.currentTarget ?? event.composedPath().find((t8) => {
          return t8 instanceof HTMLElement && t8.dataset.group && t8.dataset.segment;
        }) ?? null;
        if (!el) {
          return;
        }
        const group = el.dataset.group;
        const field = el.dataset.segment;
        if (!group || !field) {
          return;
        }
        if (event.key === "ArrowUp" || event.key === "ArrowDown") {
          event.preventDefault();
          if (this.isReadonlyOrDisabled()) {
            return;
          }
          if (this.getBuffer(group, field)) {
            this.flushBuffer(group, field);
          }
          const delta = event.key === "ArrowUp" ? 1 : -1;
          const result = this.config.rules.step(group, field, delta);
          if (result) {
            this.config.onCommit?.(group, field, result.value);
          }
          return;
        }
        if (event.key === "ArrowLeft" || event.key === "ArrowRight") {
          event.preventDefault();
          if (this.getBuffer(group, field)) {
            this.flushBuffer(group, field);
          }
          const visualLeft = event.key === "ArrowLeft";
          const logicalPrev = this.config.isRtl() ? !visualLeft : visualLeft;
          this.moveFocus(el, logicalPrev ? -1 : 1);
          return;
        }
        if (event.key === "Home") {
          event.preventDefault();
          this.segmentElements()[0]?.focus({ preventScroll: true });
          return;
        }
        if (event.key === "End") {
          event.preventDefault();
          const segments = this.segmentElements();
          segments[segments.length - 1]?.focus({ preventScroll: true });
          return;
        }
        if (event.key === "Tab") {
          if (this.getBuffer(group, field)) {
            this.flushBuffer(group, field);
          }
          return;
        }
        if (event.key === "Backspace" || event.key === "Delete") {
          event.preventDefault();
          if (this.isReadonlyOrDisabled()) {
            return;
          }
          const buffer = this.getBuffer(group, field);
          if (buffer) {
            this.setBuffer(group, field, "");
            this.config.onCommit?.(group, field, null);
          } else {
            const hadValue = this.config.rules.clear(group, field);
            if (hadValue) {
              this.config.onCommit?.(group, field, null);
            } else if (event.key === "Backspace") {
              this.moveFocus(el, -1);
            }
          }
          return;
        }
        if (/^[0-9]$/.test(event.key)) {
          event.preventDefault();
          if (this.isReadonlyOrDisabled()) {
            return;
          }
          const buffer = this.getBuffer(group, field);
          const result = this.config.rules.typeDigit(group, field, buffer, event.key);
          this.setBuffer(group, field, result.buffer);
          this.config.onCommit?.(group, field, result.value);
          if (result.advance) {
            this.moveFocus(el, 1);
          }
          return;
        }
        const seps = this.config.separatorKeys ?? DEFAULT_SEPARATORS;
        if (seps.includes(event.key)) {
          event.preventDefault();
          if (this.getBuffer(group, field)) {
            this.flushBuffer(group, field);
          }
          this.moveFocus(el, 1);
          return;
        }
      };
      this.host = host;
      this.config = config;
      host.addController(this);
    }
    //
    // ReactiveController lifecycle
    //
    hostConnected() {
    }
    hostDisconnected() {
      this.buffers.clear();
      this.active = null;
    }
    //
    // Public API
    //
    /** Get the buffer for a segment. Empty string when nothing is pending. */
    getBuffer(group, field) {
      return this.buffers.get(this.key(group, field)) ?? "";
    }
    /** Replace the buffer for a segment. Pass `''` to clear. */
    setBuffer(group, field, buffer) {
      const k2 = this.key(group, field);
      if (buffer) {
        this.buffers.set(k2, buffer);
      } else {
        this.buffers.delete(k2);
      }
    }
    /** Drop every pending buffer. Used by the host when the canonical value is replaced wholesale. */
    clearBuffers() {
      this.buffers.clear();
    }
    /** The active segment, or `null` if focus has never landed on one. */
    getActiveSegment() {
      return this.active;
    }
    /** Sets the active segment (does not move DOM focus). Used by hosts that programmatically restore focus. */
    setActiveSegment(group, field) {
      this.active = { group, field };
    }
    /** All segment elements in the host's shadow root, in DOM (logical) order. */
    segmentElements() {
      const root = this.host.shadowRoot;
      if (!root) {
        return [];
      }
      return Array.from(root.querySelectorAll("[data-segment][data-group]"));
    }
    /** The segment element for a given (group, field), or `null` if not in the DOM. */
    segmentElementFor(group, field) {
      const root = this.host.shadowRoot;
      if (!root) {
        return null;
      }
      return root.querySelector(`[data-group="${group}"][data-segment="${field}"]`);
    }
    /**
     * Returns the segment that should receive initial focus: the first empty segment (no value AND no buffer) per
     * `isEmpty`, otherwise the first segment in the layout. The host supplies the emptiness predicate since the
     * controller doesn't know the field's stored value.
     */
    findFocusableSegment(isEmpty) {
      const segments = this.segmentElements();
      if (segments.length === 0) {
        return null;
      }
      const firstEmpty = segments.find((el) => {
        const group = el.dataset.group;
        const field = el.dataset.segment;
        return isEmpty(group, field) && !this.getBuffer(group, field);
      });
      return firstEmpty ?? segments[0];
    }
    /** Restore focus to the most recently active segment (or the first segment if none has been active). */
    focusActiveSegment(options2) {
      if (this.active) {
        const el = this.segmentElementFor(this.active.group, this.active.field);
        if (el) {
          el.focus({ preventScroll: true, ...options2 });
          return;
        }
      }
      this.segmentElements()[0]?.focus({ preventScroll: true, ...options2 });
    }
    /** Move focus to the neighbor segment in logical (DOM) order. */
    moveFocus(from, direction, options2) {
      const segments = this.segmentElements();
      const idx = segments.indexOf(from);
      if (idx < 0) {
        return;
      }
      const next = segments[idx + direction];
      if (next) {
        next.focus({ preventScroll: true, ...options2 });
      }
    }
    /**
     * Commit a pending buffer to its committed value via `rules.commitBuffer`. Notifies the host via `onCommit`. Does
     * nothing if no buffer is pending.
     */
    flushBuffer(group, field) {
      const buffer = this.getBuffer(group, field);
      if (!buffer) {
        return false;
      }
      const value = this.config.rules.commitBuffer(group, field, buffer);
      this.setBuffer(group, field, "");
      this.config.onCommit?.(group, field, value);
      return true;
    }
    /**
     * Flush every pending buffer across all groups. Used when the host needs to read a final value (e.g., on form
     * submission outside of a focus change).
     */
    flushAllBuffers() {
      for (const [k2, buffer] of this.buffers) {
        if (!buffer) {
          continue;
        }
        const [group, field] = k2.split(":");
        const value = this.config.rules.commitBuffer(group, field, buffer);
        this.config.onCommit?.(group, field, value);
      }
      this.buffers.clear();
    }
    /**
     * Returns the event handlers a segment element should bind. Hosts wire these via `@keydown=${handlers.keydown}` etc.
     * The returned functions are stable references safe to pass to Lit's directive cache.
     *
     * Hosts that need host-level shortcuts (e.g. `Alt+ArrowDown` to open a popup) should call `handleKeyDown` themselves
     * after handling their own keys. The controller's `keydown` handler is safe to invoke when the host has not consumed
     * the event — it ignores keys it doesn't recognize.
     */
    eventHandlers() {
      return {
        keydown: this.handleKeyDown,
        focus: this.handleFocus,
        blur: this.handleBlur
      };
    }
    /**
     * Direct entry point for hosts that wrap the controller's keydown with their own pre-handler (e.g. for popup
     * shortcuts). Returns `true` if the controller consumed the event (called `preventDefault`), `false` otherwise.
     */
    handleKeyDownEvent(event) {
      const defaultPreventedBefore = event.defaultPrevented;
      this.handleKeyDown(event);
      return event.defaultPrevented && !defaultPreventedBefore;
    }
    //
    // Helpers
    //
    key(group, field) {
      return `${group}:${field}`;
    }
    isReadonlyOrDisabled() {
      return !!(this.config.isReadonly?.() || this.config.isDisabled?.());
    }
  };
  var segmented_field_styles_default = i`
  /* font: inherit lifts the UA default button font-size so children that size with em
     (e.g. the expand icon) resolve against the host size-driven font-size instead of ~13px. */
  [part~='clear-button'],
  [part~='expand-button'] {
    flex: 0 0 auto;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    background: transparent;
    border: none;
    cursor: pointer;
    color: var(--cs-color-text-quiet);
    font: inherit;
    padding: 0.25em;
    /* Trailing padding overhangs the content edge rather than displacing the glyph. */
    margin-inline-end: -0.25em;
    border-radius: var(--cs-border-radius-s);
    transition: color var(--cs-transition-fast);
  }

  /* Fixed widths (= glyph + 2×0.25em padding) keep each glyph centered on the trailing axis
     regardless of the slotted icon's intrinsic width. */
  [part~='expand-button'] {
    inline-size: 1.75em;
    /* Leading gap that lands the clear button on <cs-select>'s clear axis. Scales with the
       form-control padding token (like select's own spacing) so it holds across themes; the
       0.125em offset accounts for the fixed button widths. */
    margin-inline-start: calc(var(--cs-form-control-padding-inline) - 0.125em);
  }

  [part~='clear-button'] {
    inline-size: 1.5em;
    margin-inline-start: var(--cs-form-control-padding-inline);
  }

  [part~='clear-button']:hover,
  [part~='expand-button']:hover {
    color: var(--cs-color-text-normal);
  }

  [part~='expand-button']:focus-visible {
    outline: var(--cs-focus-ring-style) var(--cs-focus-ring-width) var(--cs-color-focus);
    outline-offset: 2px;
  }

  /* font-size scales the glyph with the host size attribute; the button width handles centering. */
  [part~='expand-icon'] {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    color: var(--cs-color-text-quiet);
    font-size: 1.25em;
  }

  /* Start / end decoration slots. Spaced with the same --cs-form-control-padding-inline gap as
     <cs-input>/<cs-select> so slotted icons line up with the rest of the form controls, rather
     than the tighter 0.25em the pickers used before. */
  [part~='start'],
  [part~='end'] {
    display: inline-flex;
    align-items: center;
    color: var(--cs-color-text-quiet);
  }

  [part~='start']::slotted(*) {
    margin-inline-end: var(--cs-form-control-padding-inline);
  }

  [part~='end']::slotted(*) {
    margin-inline-start: var(--cs-form-control-padding-inline);
  }
`;
  var COLUMN_PARTS = {
    hour: "column-hour",
    minute: "column-minute",
    second: "column-second",
    dayPeriod: "column-day-period"
  };
  var uniqueId2 = 0;
  var generateId2 = () => `cs-time-input-${++uniqueId2}`;
  var SINGLE_GROUP = "single";
  var CsTimeInput = class extends CornerstoneFormAssociatedElement {
    constructor() {
      super(...arguments);
      this.assumeInteractionOn = ["input"];
      this.hasSlotController = new HasSlotController(this, "hint", "label", "footer");
      this.localize = new LocalizeController2(this);
      this.popupId = generateId2();
      this.keyboardHelpId = `${this.popupId}-help`;
      this.pendingValue = null;
      this.moveFocusToColumnOnShow = false;
      this.lastEmittedValue = "";
      this.segments = { hour: null, minute: null, second: null, dayPeriod: null };
      this.segmentsController = new SegmentedFieldController(this, {
        getLayout: () => this.getLayout(),
        isRtl: () => this.isRtl,
        isReadonly: () => this.readonly,
        isDisabled: () => this.disabled,
        rules: timeSegmentRules({
          getSegments: () => this.segments,
          setSegments: (_group, next) => {
            this.segments = next;
          },
          hour12: () => this.resolvedHour12
        }),
        onCommit: () => {
          this.recomputeValue();
          this.requestUpdate();
        }
      });
      this.name = "";
      this._value = "";
      this.defaultValue = this.getAttribute("value") ?? "";
      this.disabled = false;
      this.required = false;
      this.readonly = false;
      this.size = "m";
      this.appearance = "outlined";
      this.pill = false;
      this.label = "";
      this.hint = "";
      this.autocomplete = "";
      this.withClear = false;
      this.withNow = false;
      this.ssrLabel = false;
      this.ssrHint = false;
      this.ssrFooter = false;
      this.min = "";
      this.max = "";
      this.step = 60;
      this.hourFormat = "auto";
      this.open = false;
      this.placement = "bottom-start";
      this.distance = 0;
      this.handleDocumentFocusIn = (event) => {
        const path = event.composedPath();
        if (!path.includes(this)) {
          this.hide();
        }
      };
      this.handleDocumentKeyDown = (event) => {
        if (event.key === "Escape" && this.open && isTopDismissible(this)) {
          event.stopPropagation();
          event.preventDefault();
          this.hide();
        }
      };
      this.handleDocumentMouseDown = (event) => {
        const path = event.composedPath();
        if (!path.includes(this)) {
          this.hide();
        }
      };
      this.handleSegmentFocus = (event) => {
        this.segmentsController.eventHandlers().focus(event);
      };
      this.handleSegmentBlur = (event) => {
        this.segmentsController.eventHandlers().blur(event);
      };
      this.handleInputWrapperPointerDown = (event) => {
        if (this.disabled || this.readonly || this.open) {
          return;
        }
        for (const node of event.composedPath()) {
          if (node === this) {
            break;
          }
          if (!(node instanceof Element)) {
            continue;
          }
          const tag2 = node.tagName;
          if (tag2 === "BUTTON" || tag2 === "A" || node.getAttribute("role") === "button") {
            return;
          }
        }
        this.show();
      };
      this.handleSegmentKeyDown = (event) => {
        const el = event.currentTarget;
        const field = el.dataset.segment;
        if (event.altKey && event.key === "ArrowDown") {
          event.preventDefault();
          this.moveFocusToColumnOnShow = true;
          if (this.open) {
            this.focusFirstColumn();
          } else {
            this.show();
          }
          return;
        }
        if (event.altKey && event.key === "ArrowUp") {
          event.preventDefault();
          this.hide();
          return;
        }
        if (event.key === "Enter") {
          event.preventDefault();
          if (this.segmentsController.getBuffer(SINGLE_GROUP, field)) {
            this.segmentsController.flushBuffer(SINGLE_GROUP, field);
            this.recomputeValue();
          }
          if (this.open) {
            this.hide();
          }
          return;
        }
        if (field === "dayPeriod") {
          const period = dayPeriodFromKey(event.key);
          if (period != null) {
            event.preventDefault();
            if (this.readonly) {
              return;
            }
            this.segments = { ...this.segments, dayPeriod: period };
            this.recomputeValue();
            this.requestUpdate();
            this.segmentsController.moveFocus(el, 1);
            return;
          }
        }
        this.segmentsController.eventHandlers().keydown(event);
      };
      this.handleExpandButtonClick = () => {
        if (this.open) {
          this.hide();
        } else {
          this.moveFocusToColumnOnShow = true;
          this.show();
        }
      };
      this.handleClearClick = (event) => {
        event.stopPropagation();
        if (!this._value && isTimeEmpty(this.segments)) {
          return;
        }
        this._value = "";
        this.valueHasChanged = true;
        this.segmentsController.clearBuffers();
        this.syncSegmentsFromCanonical();
        this.updateValidity();
        this.dispatchEvent(new CsClearEvent());
        this.dispatchEvent(new InputEvent("input", { bubbles: true, composed: true }));
        this.dispatchEvent(new Event("change", { bubbles: true, composed: true }));
        this.lastEmittedValue = "";
        this.focus();
      };
      this.handleClearMouseDown = (event) => {
        event.preventDefault();
        event.stopPropagation();
      };
      this.handleNowClick = () => {
        const now = /* @__PURE__ */ new Date();
        this.value = now;
        this.dispatchEvent(new InputEvent("input", { bubbles: true, composed: true }));
        this.dispatchEvent(new Event("change", { bubbles: true, composed: true }));
        this.lastEmittedValue = this._value;
        this.hide();
      };
      this.handleColumnItemClick = (event) => {
        const target = event.target.closest(".column-item");
        if (!target || target.getAttribute("aria-disabled") === "true") {
          return;
        }
        const field = target.dataset.field;
        const value = Number(target.dataset.value);
        if (Number.isNaN(value)) {
          return;
        }
        this.segments = { ...this.segments, [field]: value };
        this.recomputeValue();
        this.requestUpdate();
      };
      this.handleColumnKeyDown = (event) => {
        const column = event.currentTarget;
        const field = column.dataset.field;
        if (event.key === "Escape") {
          event.preventDefault();
          this.hide();
          return;
        }
        if (event.key === "Enter") {
          event.preventDefault();
          this.hide();
          return;
        }
        if (event.key === "ArrowLeft" || event.key === "ArrowRight") {
          event.preventDefault();
          const fields = this.columnFields;
          if (fields.length < 2) {
            return;
          }
          const delta = event.key === "ArrowLeft" ? -1 : 1;
          const currentIdx = fields.indexOf(field);
          const nextIdx = ((currentIdx + delta) % fields.length + fields.length) % fields.length;
          const nextField = fields[nextIdx];
          const nextColumn = this.shadowRoot?.querySelector(`.column[data-field="${nextField}"]`);
          nextColumn?.focus({ preventScroll: true });
          return;
        }
        if (event.key === "ArrowUp" || event.key === "ArrowDown" || event.key === "PageUp" || event.key === "PageDown") {
          event.preventDefault();
          const delta = event.key === "ArrowUp" || event.key === "PageUp" ? -1 : 1;
          const jump = event.key === "PageUp" || event.key === "PageDown" ? 5 : 1;
          const items = this.columnItemsFor(field);
          if (items.length === 0) {
            return;
          }
          const currentValue = this.segments[field];
          const idx = currentValue == null ? 0 : Math.max(
            0,
            items.findIndex((it) => it.value === currentValue)
          );
          const nextIdx = ((idx + delta * jump) % items.length + items.length) % items.length;
          const nextItem = items[nextIdx];
          this.segments = { ...this.segments, [field]: nextItem.value };
          this.recomputeValue();
          this.requestUpdate();
          requestAnimationFrame(() => {
            const itemEl = column.querySelector(`[data-value="${nextItem.value}"]`);
            if (itemEl) {
              this.keepItemInView(column, itemEl);
            }
          });
          return;
        }
        if (event.key === "Home") {
          event.preventDefault();
          const items = this.columnItemsFor(field);
          if (items.length === 0) {
            return;
          }
          this.segments = { ...this.segments, [field]: items[0].value };
          this.recomputeValue();
          this.requestUpdate();
          return;
        }
        if (event.key === "End") {
          event.preventDefault();
          const items = this.columnItemsFor(field);
          if (items.length === 0) {
            return;
          }
          const lastItem = items[items.length - 1];
          this.segments = { ...this.segments, [field]: lastItem.value };
          this.recomputeValue();
          this.requestUpdate();
          return;
        }
      };
    }
    static get validators() {
      return [
        ...super.validators,
        RequiredValidator({
          validationElement: () => Object.assign(document.createElement("input"), { required: true })
        }),
        // Mirrors the hidden native `<input type="time">` so its min/max/step constraints (including
        // reversed/overnight ranges) surface as rangeUnderflow/rangeOverflow/stepMismatch validity.
        MirrorValidator()
      ];
    }
    /** Localized term lookup. Falls back to the English string if a locale hasn't translated the key yet. */
    term(key, fallback2) {
      return this.localize.term(key) || fallback2;
    }
    get validationTarget() {
      return this.valueInput;
    }
    /**
     * The time picker's value as a wire-format string matching HTML `<input type="time">`: `HH:mm`, `HH:mm:ss`, or
     * `HH:mm:ss.sss` (always 24-hour). The setter also accepts a `Date` (extracts local h/m/s) or `null`.
     */
    get value() {
      if (this.valueHasChanged) {
        return this._value;
      }
      return this._value || this.defaultValue || "";
    }
    set value(val) {
      const normalized = this.normalizeIncomingValue(val);
      if (normalized === this._value) {
        return;
      }
      const oldValue = this._value;
      this._value = normalized;
      this.valueHasChanged = true;
      if (this.hasUpdated) {
        this.syncSegmentsFromCanonical();
      } else {
        this.pendingValue = this._value;
      }
      this.requestUpdate("value", oldValue);
    }
    //
    // Lifecycle
    //
    disconnectedCallback() {
      super.disconnectedCallback();
      this.removeOpenListeners();
    }
    firstUpdated(changedProperties) {
      super.firstUpdated(changedProperties);
      if (this.pendingValue != null) {
        this._value = this.pendingValue;
        this.pendingValue = null;
      } else if (!this._value && this.defaultValue) {
        this._value = this.defaultValue;
      }
      this.syncSegmentsFromCanonical();
      this.input = this.valueInput;
      this.updateValidity();
      this.lastEmittedValue = this._value;
    }
    updated(changed) {
      super.updated(changed);
      if (changed.has("value")) {
        this.customStates.set("blank", !this.value);
      }
      if (changed.has("disabled")) {
        this.customStates.set("disabled", this.disabled);
      }
      if (changed.has("open")) {
        this.customStates.set("open", this.open);
      }
      if (changed.has("step") || changed.has("hourFormat")) {
        this.syncSegmentsFromCanonical();
      }
      if (changed.has("min") || changed.has("max") || changed.has("step")) {
        this.updateValidity();
      }
    }
    handleDisabledChange() {
      if (this.disabled && this.open) {
        this.open = false;
      }
    }
    async handleOpenChange() {
      if (this.open && !this.disabled) {
        const showEvent = new CsShowEvent();
        this.dispatchEvent(showEvent);
        if (showEvent.defaultPrevented) {
          this.open = false;
          return;
        }
        this.addOpenListeners();
        this.popup.active = true;
        await this.updateComplete;
        await animateWithClass(this.popup.popup, "show");
        this.scrollColumnsToCurrent();
        if (this.moveFocusToColumnOnShow) {
          this.moveFocusToColumnOnShow = false;
          this.focusFirstColumn();
        }
        this.dispatchEvent(new CsAfterShowEvent());
      } else {
        const hideEvent = new CsHideEvent();
        this.dispatchEvent(hideEvent);
        if (hideEvent.defaultPrevented) {
          this.open = true;
          return;
        }
        this.removeOpenListeners();
        await animateWithClass(this.popup.popup, "hide");
        this.popup.active = false;
        this.dispatchEvent(new CsAfterHideEvent());
        const active = this.shadowRoot?.activeElement;
        if (active && this.popup?.contains(active)) {
          this.focusActiveSegment();
        }
      }
    }
    //
    // Public API
    //
    /** Sets focus on the first empty (else first) segment. */
    focus(options2) {
      const target = this.segmentsController.findFocusableSegment((_g, f3) => this.segments[f3] == null);
      target?.focus(options2);
    }
    /** Removes focus from the time picker. */
    blur() {
      const active = this.shadowRoot?.activeElement;
      active?.blur();
    }
    /** Opens the popup. */
    async show() {
      if (this.open || this.disabled) {
        return;
      }
      this.open = true;
      await waitForEvent(this, "cs-after-show");
    }
    /** Closes the popup. */
    async hide() {
      if (!this.open || this.disabled) {
        return;
      }
      this.open = false;
      await waitForEvent(this, "cs-after-hide");
    }
    /** The time as a `Date` (today + wire value), or `null` when empty. */
    get valueAsDate() {
      const value = this.value;
      if (!value) {
        return null;
      }
      const segments = wireToTimeSegments(value, { hour12: false, withSeconds: this.resolvedWithSeconds });
      if (segments.hour == null || segments.minute == null) {
        return null;
      }
      const d3 = /* @__PURE__ */ new Date();
      d3.setHours(segments.hour, segments.minute, segments.second ?? 0, 0);
      return d3;
    }
    /** Milliseconds since midnight, or `NaN` when empty. */
    get valueAsNumber() {
      const d3 = this.valueAsDate;
      if (!d3) {
        return Number.NaN;
      }
      return d3.getHours() * 36e5 + d3.getMinutes() * 6e4 + d3.getSeconds() * 1e3;
    }
    //
    // Form association
    //
    formResetCallback() {
      this._value = this.defaultValue;
      this.valueHasChanged = false;
      this.segmentsController.clearBuffers();
      this.syncSegmentsFromCanonical();
      super.formResetCallback();
      this.lastEmittedValue = this._value;
      this.requestUpdate();
    }
    formStateRestoreCallback(state2) {
      if (typeof state2 === "string") {
        this._value = state2;
        if (this.hasUpdated) {
          this.syncSegmentsFromCanonical();
        } else {
          this.pendingValue = state2;
        }
        this.requestUpdate();
      }
      this.updateValidity();
    }
    //
    // Internal helpers
    //
    get resolvedLocale() {
      return this.localize.lang() || "en";
    }
    get isRtl() {
      return this.localize.dir() === "rtl";
    }
    /** Final hour12 decision: `auto` defers to the locale; explicit `'12'` / `'24'` overrides. */
    get resolvedHour12() {
      if (this.hourFormat === "12") {
        return true;
      }
      if (this.hourFormat === "24") {
        return false;
      }
      return resolveHour12(this.resolvedLocale);
    }
    /** Whether the seconds segment is shown given the current `step` value. */
    get resolvedWithSeconds() {
      return withSecondsForStep(this.step);
    }
    getLayout() {
      return buildTimeSegmentLayout(this.resolvedLocale, {
        hour12: this.resolvedHour12,
        withSeconds: this.resolvedWithSeconds
      });
    }
    normalizeIncomingValue(val) {
      if (val == null) {
        return "";
      }
      if (typeof val === "string") {
        return val;
      }
      if (val instanceof Date) {
        const hh = String(val.getHours()).padStart(2, "0");
        const mm = String(val.getMinutes()).padStart(2, "0");
        const ss = String(val.getSeconds()).padStart(2, "0");
        return this.resolvedWithSeconds ? `${hh}:${mm}:${ss}` : `${hh}:${mm}`;
      }
      return "";
    }
    /** Recompute the segment state from the canonical `_value`. Discards any in-progress digit buffers. */
    syncSegmentsFromCanonical() {
      this.segmentsController.clearBuffers();
      this.segments = wireToTimeSegments(this._value, {
        hour12: this.resolvedHour12,
        withSeconds: this.resolvedWithSeconds
      });
      this.updateHiddenInput();
    }
    updateHiddenInput() {
      if (this.valueInput) {
        this.valueInput.value = this._value;
      }
      this.setValue(this._value || null);
    }
    /**
     * Recompute the canonical value from the current segments. Fires `input` always, and `change` when the value
     * transitions, matching `<input type="time">` semantics.
     */
    recomputeValue() {
      const oldValue = this._value;
      const newValue = timeSegmentsToWire(this.segments, {
        hour12: this.resolvedHour12,
        withSeconds: this.resolvedWithSeconds
      });
      if (newValue !== oldValue) {
        this._value = newValue;
        this.valueHasChanged = true;
        this.updateHiddenInput();
        this.updateValidity();
      }
      this.dispatchEvent(new InputEvent("input", { bubbles: true, composed: true }));
      if (newValue !== this.lastEmittedValue) {
        this.lastEmittedValue = newValue;
        this.dispatchEvent(new Event("change", { bubbles: true, composed: true }));
      }
    }
    //
    // Popup listeners
    //
    addOpenListeners() {
      document.addEventListener("focusin", this.handleDocumentFocusIn);
      document.addEventListener("keydown", this.handleDocumentKeyDown);
      document.addEventListener("mousedown", this.handleDocumentMouseDown);
      registerDismissible(this);
    }
    removeOpenListeners() {
      document.removeEventListener("focusin", this.handleDocumentFocusIn);
      document.removeEventListener("keydown", this.handleDocumentKeyDown);
      document.removeEventListener("mousedown", this.handleDocumentMouseDown);
      unregisterDismissible(this);
    }
    //
    // Segment focus restore
    //
    focusActiveSegment() {
      const active = this.segmentsController.getActiveSegment();
      if (active) {
        const el = this.segmentsController.segmentElementFor(active.group, active.field);
        if (el) {
          el.focus({ preventScroll: true });
          return;
        }
      }
      this.segmentsController.findFocusableSegment((_g, f3) => this.segments[f3] == null)?.focus({ preventScroll: true });
    }
    //
    // Popup columns
    //
    /** Field list for the visible columns, based on the resolved layout. */
    get columnFields() {
      return this.getLayout().order.filter((f3) => f3 !== void 0);
    }
    columnItemsFor(field) {
      if (field === "dayPeriod") {
        return [
          { label: this.term("am", formatDayPeriod(this.resolvedLocale, 0)), value: 0, disabled: false },
          { label: this.term("pm", formatDayPeriod(this.resolvedLocale, 1)), value: 1, disabled: false }
        ];
      }
      if (field === "hour") {
        const items2 = [];
        if (this.resolvedHour12) {
          for (let h3 = 1; h3 <= 12; h3++) {
            items2.push({ label: String(h3).padStart(2, "0"), value: h3, disabled: false });
          }
        } else {
          for (let h3 = 0; h3 <= 23; h3++) {
            items2.push({ label: String(h3).padStart(2, "0"), value: h3, disabled: false });
          }
        }
        return items2;
      }
      const stepSec = typeof this.step === "number" && Number.isFinite(this.step) && this.step > 0 ? this.step : 1;
      const stride = field === "minute" ? stepSec < 60 ? 1 : Math.max(1, Math.floor(stepSec / 60)) : Math.max(1, Math.floor(stepSec));
      const items = [];
      for (let v2 = 0; v2 < 60; v2 += stride) {
        items.push({ label: String(v2).padStart(2, "0"), value: v2, disabled: false });
      }
      return items;
    }
    focusFirstColumn() {
      if (!this.shadowRoot) {
        return;
      }
      const first = this.shadowRoot.querySelector(".column");
      first?.focus({ preventScroll: true });
    }
    scrollColumnsToCurrent() {
      if (!this.shadowRoot) {
        return;
      }
      for (const column of this.shadowRoot.querySelectorAll(".column")) {
        const field = column.dataset.field;
        const value = this.segments[field];
        if (value == null) {
          continue;
        }
        const item = column.querySelector(`[data-value="${value}"]`);
        if (item) {
          this.keepItemInView(column, item);
        }
      }
    }
    /**
     * Scroll `column` only as far as needed to keep `item` in view. Equivalent to `scrollIntoView({ block: 'nearest' })`
     * but scoped to the column so it can't scroll ancestors (the page, popup, etc.). Measured with
     * `getBoundingClientRect` because the items' `offsetParent` isn't the column.
     */
    keepItemInView(column, item) {
      const columnRect = column.getBoundingClientRect();
      const itemRect = item.getBoundingClientRect();
      if (itemRect.top < columnRect.top) {
        column.scrollTop += itemRect.top - columnRect.top;
      } else if (itemRect.bottom > columnRect.bottom) {
        column.scrollTop += itemRect.bottom - columnRect.bottom;
      }
    }
    //
    // Segment placeholders / a11y labels
    //
    /** Visual placeholder rendered in an empty segment. Matches the native `<input type="time">` UI. */
    placeholderFor(_field) {
      return "--";
    }
    /** Localized readable name of the field, used for the spinbutton's aria-label. */
    fieldLabelFor(field) {
      const fallback2 = field === "hour" ? "Hour" : field === "minute" ? "Minute" : field === "second" ? "Second" : "AM/PM";
      return this.term(field, fallback2);
    }
    segmentAriaValueText(field) {
      const value = this.segments[field];
      const buffer = this.segmentsController.getBuffer(SINGLE_GROUP, field);
      if (buffer) {
        return buffer;
      }
      if (value == null) {
        return this.term("empty", "Empty");
      }
      if (field === "dayPeriod") {
        return value === 0 ? this.term("am", "AM") : this.term("pm", "PM");
      }
      return String(value);
    }
    //
    // Render
    //
    render() {
      const hasLabelSlot = this.hasSlotController.test("label", "ssrLabel");
      const hasHintSlot = this.hasSlotController.test("hint", "ssrHint");
      const hasFooterSlot = this.hasSlotController.test("footer", "ssrFooter");
      const hasLabel = !!this.label || !!hasLabelSlot;
      const hasHint = !!this.hint || !!hasHintSlot;
      const hasValue = !!this._value;
      const layout = this.getLayout();
      const groupAriaLabel = this.label || this.term("time", "Time");
      return b2`
      <div
        part="form-control"
        class=${e7({
        "form-control": true,
        "form-control-has-label": hasLabel
      })}
      >
        <label
          id="label"
          part="form-control-label"
          class=${e7({ label: true, "has-label": hasLabel })}
          aria-hidden=${hasLabel ? "false" : "true"}
          @click=${() => this.focus()}
        >
          <slot name="label">${this.label}</slot>
        </label>

        <div part="form-control-input" class="form-control-input">
          <cs-popup
            class=${e7({ "time-input-popup": true, open: this.open })}
            placement=${this.placement}
            distance=${this.distance}
            ?active=${this.open}
            flip
            shift
          >
            <div
              part="time-input input-wrapper"
              class="input-wrapper"
              slot="anchor"
              @pointerdown=${this.handleInputWrapperPointerDown}
            >
              <slot name="start" part="start" class="start"></slot>

              <div
                part="input"
                class="segments"
                role="group"
                aria-labelledby=${hasLabel ? "label" : A}
                aria-label=${hasLabel ? A : groupAriaLabel}
              >
                ${this.renderSegmentGroup(layout)}
              </div>

              <span id=${this.keyboardHelpId} class="visually-hidden">
                ${this.term(
        "timeInputKeyboardHelp",
        "Use arrow keys to change values; press Alt+Down Arrow to open the time picker."
      )}
              </span>

              <input
                class="value-input"
                type="time"
                tabindex="-1"
                aria-hidden="true"
                .value=${this._value}
                min=${o8(this.min || void 0)}
                max=${o8(this.max || void 0)}
                step=${o8(this.step === "any" ? "any" : String(this.step))}
                ?disabled=${this.disabled}
                ?required=${this.required}
                autocomplete=${o8(this.autocomplete || void 0)}
              />

              ${this.withClear && hasValue ? b2`<button
                      part="clear-button"
                      type="button"
                      class="clear-button"
                      aria-label=${this.localize.term("clearEntry")}
                      tabindex="-1"
                      @mousedown=${this.handleClearMouseDown}
                      @click=${this.handleClearClick}
                    >
                      <slot name="clear-icon">
                        <cs-icon name="cancel" library="system"></cs-icon>
                      </slot>
                    </button>` : A}

              <slot name="end" part="end" class="end"></slot>

              <button
                part="expand-button"
                type="button"
                class="expand-button"
                aria-label=${this.open ? this.term("closeTimeInput", "Close time picker") : this.term("chooseTime", "Choose time")}
                aria-haspopup="dialog"
                aria-expanded=${this.open ? "true" : "false"}
                aria-controls=${this.popupId}
                ?disabled=${this.disabled}
                @click=${this.handleExpandButtonClick}
              >
                <slot name="expand-icon" part="expand-icon" class="expand-icon">
                  <cs-icon library="system" name="schedule"></cs-icon>
                </slot>
              </button>
            </div>

            <div
              id=${this.popupId}
              part="popup"
              class="popup-body"
              role="dialog"
              aria-modal="true"
              aria-label=${this.term("chooseTime", "Choose time")}
            >
              <div part="columns" class="columns">${this.columnFields.map((f3) => this.renderColumn(f3))}</div>
              ${hasFooterSlot ? b2`<div class="popup-footer"><slot name="footer"></slot></div>` : this.withNow ? b2`<div class="popup-footer">
                        <button part="now-button" type="button" class="now-button" @click=${this.handleNowClick}>
                          ${this.term("now", "Now")}
                        </button>
                      </div>` : A}
            </div>
          </cs-popup>
        </div>

        <slot
          id="hint"
          name="hint"
          part="hint"
          class=${e7({ "has-slotted": hasHint })}
          aria-hidden=${hasHint ? "false" : "true"}
        >
          ${this.hint}
        </slot>
      </div>
    `;
    }
    renderSegmentGroup(layout) {
      const active = this.segmentsController.getActiveSegment();
      let tabAssigned = false;
      const nodes = [];
      for (const token of layout.tokens) {
        if (token.kind === "literal") {
          nodes.push(b2`<span part="segment-literal" class="segment-literal" aria-hidden="true">${token.text}</span>`);
        } else {
          const field = token.field;
          const isFirstActive = !tabAssigned && (active == null || active.field === field);
          if (isFirstActive) {
            tabAssigned = true;
          }
          nodes.push(this.renderSegment(field, isFirstActive));
        }
      }
      return nodes;
    }
    renderSegment(field, isTabStop) {
      const value = this.segments[field];
      const buffer = this.segmentsController.getBuffer(SINGLE_GROUP, field);
      const placeholder = this.placeholderFor(field);
      const display = formatTimeSegmentText(field, value, buffer, placeholder, this.resolvedLocale);
      const isEmptySegment = value == null && !buffer;
      const bounds = field === "hour" ? this.resolvedHour12 ? { min: 1, max: 12 } : { min: 0, max: 23 } : field === "minute" || field === "second" ? { min: 0, max: 59 } : { min: 0, max: 1 };
      const ariaValueText = this.segmentAriaValueText(field);
      return b2`<span
      part="segment"
      class=${e7({ segment: true, empty: isEmptySegment, [`segment-${field}`]: true })}
      data-group=${SINGLE_GROUP}
      data-segment=${field}
      role="spinbutton"
      tabindex=${this.disabled ? -1 : isTabStop ? 0 : -1}
      aria-label=${this.fieldLabelFor(field)}
      aria-valuemin=${bounds.min}
      aria-valuemax=${bounds.max}
      aria-valuenow=${o8(value == null ? void 0 : value)}
      aria-valuetext=${ariaValueText}
      aria-readonly=${this.readonly ? "true" : "false"}
      aria-disabled=${this.disabled ? "true" : "false"}
      aria-describedby=${this.keyboardHelpId}
      inputmode=${field === "dayPeriod" ? "text" : "numeric"}
      @keydown=${this.handleSegmentKeyDown}
      @focus=${this.handleSegmentFocus}
      @blur=${this.handleSegmentBlur}
      >${display}</span
    >`;
    }
    renderColumn(field) {
      const items = this.columnItemsFor(field);
      const currentValue = this.segments[field];
      const activedescendantId = currentValue != null ? `${this.popupId}-${field}-${currentValue}` : void 0;
      return b2`<div
      part="column ${COLUMN_PARTS[field]}"
      class=${e7({ column: true, [`column-${field}`]: true })}
      data-field=${field}
      role="listbox"
      tabindex="0"
      aria-label=${this.fieldLabelFor(field)}
      aria-orientation="vertical"
      aria-activedescendant=${o8(activedescendantId)}
      @click=${this.handleColumnItemClick}
      @keydown=${this.handleColumnKeyDown}
    >
      ${items.map((item) => {
        const id3 = `${this.popupId}-${field}-${item.value}`;
        const selected = item.value === currentValue;
        return b2`<button
          id=${id3}
          part="column-item ${selected ? "column-item-selected" : ""}"
          class="column-item"
          data-field=${field}
          data-value=${item.value}
          type="button"
          role="option"
          aria-selected=${selected ? "true" : "false"}
          aria-disabled=${item.disabled ? "true" : "false"}
          tabindex="-1"
        >
          ${item.label}
        </button>`;
      })}
    </div>`;
    }
  };
  CsTimeInput.css = [size_styles_default, form_control_styles_default, segmented_field_styles_default, time_input_styles_default];
  CsTimeInput.shadowRootOptions = {
    ...CornerstoneFormAssociatedElement.shadowRootOptions,
    delegatesFocus: true
  };
  __decorateClass([
    e5(".time-input-popup")
  ], CsTimeInput.prototype, "popup", 2);
  __decorateClass([
    e5(".value-input")
  ], CsTimeInput.prototype, "valueInput", 2);
  __decorateClass([
    r5()
  ], CsTimeInput.prototype, "segments", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsTimeInput.prototype, "name", 2);
  __decorateClass([
    r5()
  ], CsTimeInput.prototype, "value", 1);
  __decorateClass([
    n4({ attribute: "value", reflect: true })
  ], CsTimeInput.prototype, "defaultValue", 2);
  __decorateClass([
    n4({ type: Boolean })
  ], CsTimeInput.prototype, "disabled", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsTimeInput.prototype, "required", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsTimeInput.prototype, "readonly", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsTimeInput.prototype, "size", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsTimeInput.prototype, "appearance", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsTimeInput.prototype, "pill", 2);
  __decorateClass([
    n4()
  ], CsTimeInput.prototype, "label", 2);
  __decorateClass([
    n4({ attribute: "hint" })
  ], CsTimeInput.prototype, "hint", 2);
  __decorateClass([
    n4()
  ], CsTimeInput.prototype, "autocomplete", 2);
  __decorateClass([
    n4({ attribute: "with-clear", type: Boolean })
  ], CsTimeInput.prototype, "withClear", 2);
  __decorateClass([
    n4({ attribute: "with-now", type: Boolean })
  ], CsTimeInput.prototype, "withNow", 2);
  __decorateClass([
    n4({ attribute: "ssr-label", type: Boolean })
  ], CsTimeInput.prototype, "ssrLabel", 2);
  __decorateClass([
    n4({ attribute: "ssr-hint", type: Boolean })
  ], CsTimeInput.prototype, "ssrHint", 2);
  __decorateClass([
    n4({ attribute: "ssr-footer", type: Boolean })
  ], CsTimeInput.prototype, "ssrFooter", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsTimeInput.prototype, "min", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsTimeInput.prototype, "max", 2);
  __decorateClass([
    n4({ converter: { fromAttribute: stepFromAttribute, toAttribute: stepToAttribute } })
  ], CsTimeInput.prototype, "step", 2);
  __decorateClass([
    n4({ attribute: "hour-format", reflect: true })
  ], CsTimeInput.prototype, "hourFormat", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsTimeInput.prototype, "open", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsTimeInput.prototype, "placement", 2);
  __decorateClass([
    n4({ type: Number, reflect: true })
  ], CsTimeInput.prototype, "distance", 2);
  __decorateClass([
    watch("disabled", { waitUntilFirstUpdate: true })
  ], CsTimeInput.prototype, "handleDisabledChange", 1);
  __decorateClass([
    watch("open", { waitUntilFirstUpdate: true })
  ], CsTimeInput.prototype, "handleOpenChange", 1);
  CsTimeInput = __decorateClass([
    customElement("cs-time-input")
  ], CsTimeInput);
  function stepFromAttribute(value) {
    if (value == null) {
      return 60;
    }
    if (value === "any") {
      return "any";
    }
    const n9 = Number(value);
    return Number.isFinite(n9) && n9 > 0 ? n9 : 60;
  }
  function stepToAttribute(value) {
    if (value === "any") {
      return "any";
    }
    return String(value);
  }

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.DHVELIOX.js
  var toast_styles_default = i`
  :host {
    --gap: var(--cs-space-s);
    --width: 28rem;
    --reorder-duration: var(--cs-transition-normal);

    display: flex;
    flex-direction: column;
    position: fixed;
    width: var(--width);
    /* --width is 28rem (448px), wider than a 390px phone viewport. Centered placements offset the host
       by half its own width, so an unclamped host hangs off-screen. The 480px media query below also
       covers this, but only once a viewport change has been laid out — this states the invariant
       directly so it holds at every width. */
    max-width: 100%;
    height: 100dvh;
    max-height: 100dvh;
    margin: 0;
    padding: var(--cs-space-m);
    overflow-y: auto;
    gap: var(--gap);
    border: none;
    background: transparent;
    pointer-events: none;
    scrollbar-width: thin;

    /* Reset inset properties so placement changes work correctly */
    inset-block-start: auto;
    inset-block-end: auto;
    inset-inline-start: auto;
    inset-inline-end: auto;
    translate: none;
    align-content: normal;
    justify-content: normal;
  }

  :host(:not(:popover-open)) {
    display: none;
  }

  /* Placement positioning using logical properties for RTL support */
  :host([placement='top-start']) {
    inset-block-start: 0;
    inset-inline-start: 0;
  }

  :host([placement='top-center']) {
    inset-block-start: 0;
    inset-inline-start: 50%;
    translate: -50% 0;
  }

  :host([placement='top-end']) {
    inset-block-start: 0;
    inset-inline-start: auto;
    inset-inline-end: 0;
  }

  :host([placement='bottom-start']) {
    inset-block-end: 0;
    inset-inline-start: 0;
    align-content: end;
  }

  :host([placement='bottom-center']) {
    inset-block-end: 0;
    inset-inline-start: 50%;
    translate: -50% 0;
    align-content: end;
  }

  :host([placement='bottom-end']) {
    inset-block-end: 0;
    inset-inline-start: auto;
    inset-inline-end: 0;
    align-content: end;
  }

  /* Bottom placements: justify content to end */
  :host([placement^='bottom']) {
    justify-content: end;
  }

  .stack {
    display: flex;
    flex-direction: column;
    gap: var(--gap);
    width: 100%;
    pointer-events: auto;
  }

  /* Bottom placements: reverse stack order so newest appears at bottom */
  :host([placement^='bottom']) .stack {
    flex-direction: column-reverse;
  }

  /* Mobile: full width */
  @media (max-width: 480px) {
    :host {
      width: 100%;
      padding: var(--cs-space-s);
    }
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.P2QYS5O3.js
  var sharedLiveRegionContainer = null;
  var sharedLiveRegionRefCount = 0;
  function mountSharedLiveRegions() {
    sharedLiveRegionRefCount += 1;
    if (sharedLiveRegionContainer || typeof document === "undefined") {
      return;
    }
    const container = document.createElement("div");
    container.id = uniqueId("cs-toast-live-region-");
    container.setAttribute("data-cs-toast-live-region", "");
    container.style.cssText = `
    position: absolute;
    width: 1px;
    height: 1px;
    overflow: hidden;
    white-space: nowrap;
    clip-path: inset(50%);
    pointer-events: none;
    top: 0;
    left: 0;
  `;
    document.body.append(container);
    sharedLiveRegionContainer = container;
  }
  function unmountSharedLiveRegions() {
    sharedLiveRegionRefCount = Math.max(0, sharedLiveRegionRefCount - 1);
    if (sharedLiveRegionRefCount > 0) {
      return;
    }
    sharedLiveRegionContainer?.remove();
    sharedLiveRegionContainer = null;
  }
  function announceViaSharedLiveRegion(text, variant) {
    if (typeof document === "undefined") {
      return;
    }
    const container = sharedLiveRegionContainer;
    if (!container) {
      return;
    }
    const trimmed = text.trim();
    if (!trimmed) {
      return;
    }
    const announcer = document.createElement("div");
    announcer.setAttribute("role", variant === "danger" ? "alert" : "status");
    announcer.setAttribute("aria-live", variant === "danger" ? "assertive" : "polite");
    announcer.setAttribute("aria-atomic", "true");
    container.append(announcer);
    requestAnimationFrame(() => {
      requestAnimationFrame(() => {
        announcer.textContent = trimmed;
      });
    });
    setTimeout(() => announcer.remove(), 1e3);
  }
  var CsToast = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.activatedToastItems = /* @__PURE__ */ new WeakSet();
      this.positionCache = /* @__PURE__ */ new Map();
      this.placement = "top-end";
      this.handleDocumentKeyDown = async (event) => {
        await new Promise((resolve) => setTimeout(resolve));
        if (event.key === "Escape" && !event.defaultPrevented) {
          const toastItems = this.getToastItems();
          if (toastItems.length > 0) {
            event.preventDefault();
            const mostRecent = toastItems[toastItems.length - 1];
            mostRecent?.hide();
          }
        }
      };
      this.handleAfterHide = async (event) => {
        const toastItem = event.target;
        if (toastItem.parentElement === this) {
          this.capturePositions();
          toastItem.remove();
          await this.animatePositions();
        }
        if (this.getToastItems().length === 0) {
          this.hideStack();
        }
      };
    }
    connectedCallback() {
      super.connectedCallback();
      if (!o5) {
        this.popover = "manual";
        mountSharedLiveRegions();
        document.addEventListener("keydown", this.handleDocumentKeyDown);
      }
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      document.removeEventListener?.("keydown", this.handleDocumentKeyDown);
      unmountSharedLiveRegions();
    }
    handleSlotChange() {
      const toastItems = this.getToastItems();
      const newItems = [];
      toastItems.forEach((toastItem) => {
        if (!this.activatedToastItems.has(toastItem)) {
          newItems.push(toastItem);
        }
      });
      if (newItems.length > 0) {
        this.capturePositions();
        newItems.forEach((toastItem) => {
          this.activatedToastItems.add(toastItem);
          this.showStack();
          toastItem.startTimer();
          this.announceToastItem(toastItem);
        });
        requestAnimationFrame(() => this.animatePositions());
      }
    }
    announceToastItem(toastItem) {
      announceViaSharedLiveRegion(toastItem.textContent ?? "", toastItem.variant);
    }
    getToastItems() {
      return [...this.querySelectorAll(":scope > cs-toast-item")];
    }
    /** Captures the current position of all toast items for FLIP animation. */
    capturePositions() {
      this.positionCache.clear();
      for (const item of this.getToastItems()) {
        this.positionCache.set(item, item.getBoundingClientRect());
      }
    }
    /** Animates toast items from their cached positions to their new positions using FLIP. */
    async animatePositions() {
      if (prefersReducedMotion()) {
        this.positionCache.clear();
        return;
      }
      const animations = [];
      for (const item of this.getToastItems()) {
        const oldRect = this.positionCache.get(item);
        if (!oldRect) {
          continue;
        }
        const newRect = item.getBoundingClientRect();
        const deltaY = oldRect.top - newRect.top;
        if (Math.abs(deltaY) > 1) {
          const animation = animate(item, [{ transform: `translateY(${deltaY}px)` }, { transform: "translateY(0)" }], {
            duration: 200,
            easing: "cubic-bezier(0.2, 0, 0, 1)"
            // Material Design standard easing
          });
          animations.push(animation);
        }
      }
      this.positionCache.clear();
      await Promise.all(animations);
    }
    showStack() {
      if (!this.matches(":popover-open")) {
        this.showPopover();
        this.customStates.set("visible", true);
      }
    }
    hideStack() {
      if (this.matches(":popover-open")) {
        this.hidePopover();
        this.customStates.set("visible", false);
      }
    }
    /**
     * Creates a toast notification programmatically and adds it to the stack. Returns a reference to the created toast
     * item element.
     */
    async create(message, options2) {
      const opts = {
        allowHtml: false,
        duration: 5e3,
        variant: "neutral",
        size: "m",
        ...options2
      };
      const toastItem = document.createElement("cs-toast-item");
      toastItem.variant = opts.variant;
      toastItem.size = opts.size;
      toastItem.duration = opts.duration;
      if (opts.allowHtml) {
        toastItem.innerHTML = message;
      } else {
        toastItem.textContent = message;
      }
      if (opts.icon) {
        const icon = document.createElement("cs-icon");
        icon.setAttribute("slot", "icon");
        if (typeof opts.icon === "string") {
          icon.setAttribute("name", opts.icon);
        } else {
          icon.setAttribute("name", opts.icon.name);
          if (opts.icon.library) {
            icon.setAttribute("library", opts.icon.library);
          }
          if (opts.icon.family) {
            icon.setAttribute("family", opts.icon.family);
          }
          if (opts.icon.variant) {
            icon.setAttribute("variant", opts.icon.variant);
          }
        }
        toastItem.prepend(icon);
      }
      this.activatedToastItems.add(toastItem);
      this.capturePositions();
      this.showStack();
      this.prepend(toastItem);
      await toastItem.updateComplete;
      this.animatePositions();
      toastItem.startTimer();
      this.announceToastItem(toastItem);
      return toastItem;
    }
    render() {
      return b2`
      <div part="stack" class="stack" @cs-after-hide=${this.handleAfterHide}>
        <slot @slotchange=${this.handleSlotChange}></slot>
      </div>
    `;
    }
  };
  CsToast.css = toast_styles_default;
  __decorateClass([
    e5(".stack")
  ], CsToast.prototype, "stack", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsToast.prototype, "placement", 2);
  CsToast = __decorateClass([
    customElement("cs-toast")
  ], CsToast);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.IPW7QIUW.js
  var toast_item_styles_default = i`
  :host {
    --accent-width: 4px;
    --show-duration: var(--cs-transition-normal);
    --hide-duration: var(--cs-transition-normal);
    --accent-color: var(--cs-color-fill-loud);

    display: block;
    pointer-events: auto;
  }

  /* Sizes */
  :host([size='xs']) {
    --padding: var(--cs-space-xs);
  }
  :host([size='s']) {
    --padding: var(--cs-space-s);
  }
  :host([size='m']) {
    --padding: var(--cs-space-m);
  }
  :host([size='l']) {
    --padding: var(--cs-space-l);
  }
  :host([size='xl']) {
    --padding: var(--cs-space-xl);
  }

  .toast-item {
    display: flex;
    align-items: stretch;
    background: var(--cs-color-surface-raised);
    border: var(--cs-border-width-s) solid var(--cs-color-surface-border);
    border-radius: var(--cs-border-radius-m);
    box-shadow: var(--cs-shadow-l);
    overflow: hidden;
  }

  /* Animations */
  .toast-item.show {
    animation: toast-show var(--show-duration) var(--cs-transition-easing) forwards;
  }

  .toast-item.hide {
    animation: toast-hide var(--hide-duration) var(--cs-transition-easing) forwards;
  }

  @keyframes toast-show {
    from {
      opacity: 0;
      translate: 0 -0.5rem;
    }
    to {
      opacity: 1;
      translate: 0;
    }
  }

  @keyframes toast-hide {
    from {
      opacity: 1;
      translate: 0;
    }
    to {
      opacity: 0;
      translate: 0 -0.5rem;
    }
  }

  /* Accent line */
  .accent {
    flex: 0 0 auto;
    width: var(--accent-width);
    background: var(--accent-color);
  }

  /* Icon - only show if slot has content */
  .icon {
    display: flex;
    align-items: center;
    padding: var(--padding, var(--cs-space-m));
    padding-inline-end: 0;
    color: var(--accent-color);
    font-size: 1.25em;
  }

  .toast-item:not(.toast-item--has-icon) .icon {
    display: none;
  }

  /* Content */
  .content {
    flex: 1 1 auto;
    align-self: center;
    min-width: 0;
    padding: var(--padding, var(--cs-space-m));
    color: var(--cs-color-text-normal);
  }

  /* Close button */
  .close-button {
    flex: 0 0 auto;
    display: flex;
    align-items: center;
    justify-content: center;
    align-self: stretch;
    padding-inline: var(--padding, var(--cs-space-m));
    background: transparent;
    border: none;
    border-start-end-radius: var(--border-radius);
    border-end-end-radius: var(--border-radius);
    color: var(--cs-color-neutral-on-quiet);
    font-size: inherit;
    cursor: pointer;
    transition: background-color var(--cs-transition-fast);

    @media (hover: hover) {
      &:hover {
        color: color-mix(in oklab, currentColor, var(--cs-color-mix-hover));
      }
    }

    &:focus {
      outline: none;
    }

    &:focus-visible {
      outline: var(--cs-focus-ring);
      outline-offset: calc(var(--cs-focus-ring-width) * -1);
    }
  }

  /* Progress ring styling */
  cs-progress-ring {
    --size: var(--cs-form-control-height);
    --track-width: 0.125rem;
    --indicator-width: 0.125rem;
    --track-color: var(--cs-color-neutral-fill-quiet);
    --indicator-color: var(--accent-color);
    --indicator-transition-duration: 50ms;
  }

  /* Hide progress ring indicator when no duration */
  .toast-item:not(.toast-item--has-duration) cs-progress-ring {
    --track-color: transparent;
    --indicator-color: transparent;
  }

  /* Reduced motion */
  @media (prefers-reduced-motion: reduce) {
    .toast-item.show,
    .toast-item.hide {
      animation: none;
    }
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.NTVJFVZV.js
  var CsToastItem = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.hasSlotController = new HasSlotController(this, "icon");
      this.localize = new LocalizeController2(this);
      this.animationFrame = null;
      this.startTime = null;
      this.isHovering = false;
      this.isFocused = false;
      this.timeLeft = 100;
      this.variant = "neutral";
      this.size = "m";
      this.duration = 5e3;
      this.ssrIcon = false;
      this.tick = () => {
        if (!this.startTime) {
          return;
        }
        const elapsed = performance.now() - this.startTime;
        const progress = Math.min(elapsed / this.duration, 1);
        this.timeLeft = 100 * (1 - progress);
        if (progress < 1) {
          this.animationFrame = requestAnimationFrame(this.tick);
        } else {
          this.hide();
        }
      };
      this.handlePointerEnter = (event) => {
        if (event.pointerType === "mouse" || event.pointerType === "pen") {
          this.isHovering = true;
          this.pauseTimer();
        }
      };
      this.handlePointerLeave = () => {
        if (this.isHovering) {
          this.isHovering = false;
          this.resumeTimer();
        }
      };
      this.handleFocusIn = () => {
        this.isFocused = true;
        this.pauseTimer();
      };
      this.handleFocusOut = () => {
        this.isFocused = false;
        this.resumeTimer();
      };
    }
    connectedCallback() {
      super.connectedCallback();
      this.addEventListener("pointerenter", this.handlePointerEnter);
      this.addEventListener("pointerleave", this.handlePointerLeave);
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this.stopTimer();
      this.removeEventListener("pointerenter", this.handlePointerEnter);
      this.removeEventListener("pointerleave", this.handlePointerLeave);
    }
    /** @internal Starts the toast item's timer and shows it. Called by the parent toast component. */
    async startTimer() {
      const showEvent = new CsShowEvent();
      this.dispatchEvent(showEvent);
      if (showEvent.defaultPrevented) {
        return;
      }
      await this.updateComplete;
      await animateWithClass(this.toastItemElement, "show");
      this.dispatchEvent(new CsAfterShowEvent());
      if (this.duration > 0 && Number.isFinite(this.duration)) {
        this.startTime = performance.now();
        this.timeLeft = 100;
        this.tick();
      }
    }
    /** @internal Stops the toast item's timer. */
    stopTimer() {
      if (this.animationFrame !== null) {
        cancelAnimationFrame(this.animationFrame);
        this.animationFrame = null;
      }
    }
    /** Hides the toast item with animation and removes it from the DOM. */
    async hide() {
      this.stopTimer();
      const hideEvent = new CsHideEvent();
      this.dispatchEvent(hideEvent);
      if (hideEvent.defaultPrevented) {
        return;
      }
      await animateWithClass(this.toastItemElement, "hide");
      this.dispatchEvent(new CsAfterHideEvent());
      this.remove();
    }
    handleCloseClick() {
      this.hide();
    }
    pauseTimer() {
      this.stopTimer();
      this.timeLeft = 100;
    }
    resumeTimer() {
      if (!this.isHovering && !this.isFocused && this.duration > 0) {
        this.startTime = performance.now();
        this.tick();
      }
    }
    render() {
      const hasIcon = this.hasSlotController.test("icon", "ssrIcon");
      const hasDuration = this.duration > 0;
      return b2`
      <div
        part="toast-item"
        class=${e7({
        "toast-item": true,
        "toast-item--has-icon": hasIcon,
        "toast-item--has-duration": hasDuration
      })}
      >
        <div part="accent" class="accent"></div>

        <div part="icon" class="icon">
          <slot name="icon"></slot>
        </div>

        <div part="content" class="content">
          <slot></slot>
        </div>

        <button
          part="close-button"
          class="close-button"
          type="button"
          aria-label=${this.localize.term("close")}
          @click=${this.handleCloseClick}
          @focusin=${this.handleFocusIn}
          @focusout=${this.handleFocusOut}
        >
          <cs-progress-ring
            part="progress-ring"
            exportparts="
              progress-ring:progress-ring__progress-ring,
              label:progress-ring__label,
              track:progress-ring__track,
              indicator:progress-ring__indicator
            "
            value=${this.timeLeft}
            aria-hidden="true"
          >
            <cs-icon part="close-icon" exportparts="svg:close-icon__svg" name="close" library="system"></cs-icon>
          </cs-progress-ring>
        </button>
      </div>
    `;
    }
  };
  CsToastItem.css = [toast_item_styles_default, variants_styles_default, size_styles_default];
  __decorateClass([
    e5(".toast-item")
  ], CsToastItem.prototype, "toastItemElement", 2);
  __decorateClass([
    r5()
  ], CsToastItem.prototype, "timeLeft", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsToastItem.prototype, "variant", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsToastItem.prototype, "size", 2);
  __decorateClass([
    n4({ type: Number })
  ], CsToastItem.prototype, "duration", 2);
  __decorateClass([
    n4({ attribute: "ssr-icon", type: Boolean })
  ], CsToastItem.prototype, "ssrIcon", 2);
  CsToastItem = __decorateClass([
    customElement("cs-toast-item")
  ], CsToastItem);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.QE35BTCZ.js
  var CsSelectionChangeEvent = class extends Event {
    constructor(detail) {
      super("cs-selection-change", { bubbles: true, cancelable: false, composed: true });
      this.detail = detail;
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.Q522XM7R.js
  var CsLazyChangeEvent = class extends Event {
    constructor() {
      super("cs-lazy-change", { bubbles: true, cancelable: false, composed: true });
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.EB3IAVUR.js
  var CsLazyLoadEvent = class extends Event {
    constructor() {
      super("cs-lazy-load", { bubbles: true, cancelable: false, composed: true });
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.ZJUSWIFH.js
  var CsExpandEvent = class extends Event {
    constructor() {
      super("cs-expand", { bubbles: true, cancelable: false, composed: true });
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.DJJJADLC.js
  var CsCollapseEvent = class extends Event {
    constructor() {
      super("cs-collapse", { bubbles: true, cancelable: false, composed: true });
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.4QEGLJGY.js
  var CsAfterCollapseEvent = class extends Event {
    constructor() {
      super("cs-after-collapse", { bubbles: true, cancelable: false, composed: true });
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.FK2TIGYL.js
  var CsAfterExpandEvent = class extends Event {
    constructor() {
      super("cs-after-expand", { bubbles: true, cancelable: false, composed: true });
    }
  };

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.5SHQYK4Y.js
  var tree_item_styles_default = i`
  :host {
    /* Private - set by the component to control indentation depth */
    --indent: 0px;
    --show-duration: var(--cs-transition-normal);
    --hide-duration: var(--cs-transition-normal);

    display: block;
    color: var(--cs-color-text-normal);
    outline: 0;
    z-index: 0;
  }

  :host(:focus) {
    outline: none;
  }

  /* These are !important because a document-level universal margin reset would otherwise beat them:
     a slotted element belongs to the outer tree. See callout.styles.ts for the full note. */
  slot:not([name])::slotted(cs-icon) {
    margin-inline-end: 0.5em !important;
  }

  .tree-item {
    position: relative;
    display: flex;
    align-items: stretch;
    flex-direction: column;
    cursor: default;
    user-select: none;
    -webkit-user-select: none;
  }

  .checkbox {
    line-height: var(--cs-form-control-value-line-height);
    pointer-events: none;
  }

  .expand-button,
  .checkbox,
  .label {
    font-family: inherit;
    font-size: inherit;
    font-weight: inherit;
  }

  .checkbox::part(checkbox) {
    display: flex;
    align-items: center;
  }

  .indentation {
    display: block;
    width: var(--indent);
    flex-shrink: 0;
  }

  .expand-button {
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--cs-color-text-quiet);
    width: 2em;
    height: 2em;
    flex-shrink: 0;
    cursor: pointer;
  }

  .expand-button {
    transition: rotate var(--cs-transition-normal) var(--cs-transition-easing);
  }

  .tree-item-expanded .expand-button {
    rotate: 90deg;
  }

  .tree-item-expanded:dir(rtl) .expand-button {
    rotate: -90deg;
  }

  .tree-item-expanded:not(.tree-item-loading) slot[name='expand-icon'],
  .tree-item:not(.tree-item-expanded) slot[name='collapse-icon'] {
    display: none;
  }

  .tree-item:not(.tree-item-has-expand-button):not(.tree-item-loading) .expand-icon-slot {
    display: none;
  }

  .tree-item:not(.tree-item-has-expand-button):not(.tree-item-loading) .expand-button {
    cursor: default;
  }

  .tree-item-loading .expand-icon-slot cs-icon {
    display: none;
  }

  .expand-button-visible {
    cursor: pointer;
  }

  .item {
    display: flex;
    align-items: center;
    border-inline-start: solid 0.1875em transparent;
  }

  :host([disabled]) .item {
    opacity: 0.5;
    outline: none;
    cursor: not-allowed;
  }

  :host(:focus-visible) .item {
    outline: var(--cs-focus-ring);
    outline-offset: var(--cs-focus-ring-offset);
    z-index: 2;
  }

  :host(:not([aria-disabled='true'])) .tree-item-selected .item {
    background-color: var(--cs-color-neutral-fill-quiet);
    border-inline-start-color: var(--cs-color-brand-fill-loud);
  }

  :host(:not([aria-disabled='true'])) .expand-button {
    color: var(--cs-color-text-quiet);
  }

  .label {
    display: flex;
    align-items: center;
    transition: color var(--cs-transition-normal) var(--cs-transition-easing);
  }

  .children {
    display: block;
  }

  /* Indentation lines */
  .children {
    position: relative;
  }

  .children::before {
    content: '';
    position: absolute;
    top: var(--indent-guide-offset);
    bottom: var(--indent-guide-offset);
    inset-inline-start: calc(0.1875em + var(--indent) + 1em - (var(--indent-guide-width) / 2));
    border-inline-end: var(--indent-guide-width) var(--indent-guide-style) var(--indent-guide-color);
    z-index: 1;
  }

  @media (forced-colors: active) {
    :host(:not([aria-disabled='true'])) .tree-item-selected .item {
      outline: dashed 1px SelectedItem;
    }
  }
`;

  // node_modules/@lit/context/lib/context-request-event.js
  var s4 = class extends Event {
    constructor(s7, t8, e12, o13) {
      super("context-request", { bubbles: true, composed: true }), this.context = s7, this.contextTarget = t8, this.callback = e12, this.subscribe = o13 ?? false;
    }
  };

  // node_modules/@lit/context/lib/create-context.js
  function n8(n9) {
    return n9;
  }

  // node_modules/@lit/context/lib/controllers/context-consumer.js
  var s5 = class {
    constructor(t8, s7, i10, h3) {
      if (this.subscribe = false, this.provided = false, this.value = void 0, this.t = (t9, s8) => {
        this.unsubscribe && (this.unsubscribe !== s8 && (this.provided = false, this.unsubscribe()), this.subscribe || this.unsubscribe()), this.value = t9, this.host.requestUpdate(), this.provided && !this.subscribe || (this.provided = true, this.callback && this.callback(t9, s8)), this.unsubscribe = s8;
      }, this.host = t8, void 0 !== s7.context) {
        const t9 = s7;
        this.context = t9.context, this.callback = t9.callback, this.subscribe = t9.subscribe ?? false;
      } else this.context = s7, this.callback = i10, this.subscribe = h3 ?? false;
      this.host.addController(this);
    }
    hostConnected() {
      this.dispatchRequest();
    }
    hostDisconnected() {
      this.unsubscribe && (this.unsubscribe(), this.unsubscribe = void 0);
    }
    dispatchRequest() {
      this.host.dispatchEvent(new s4(this.context, this.host, this.t, this.subscribe));
    }
  };

  // node_modules/@lit/context/lib/value-notifier.js
  var s6 = class {
    get value() {
      return this.o;
    }
    set value(s7) {
      this.setValue(s7);
    }
    setValue(s7, t8 = false) {
      const i10 = t8 || !Object.is(s7, this.o);
      this.o = s7, i10 && this.updateObservers();
    }
    constructor(s7) {
      this.subscriptions = /* @__PURE__ */ new Map(), this.updateObservers = () => {
        for (const [s8, { disposer: t8 }] of this.subscriptions) s8(this.o, t8);
      }, void 0 !== s7 && (this.value = s7);
    }
    addCallback(s7, t8, i10) {
      if (!i10) return void s7(this.value);
      this.subscriptions.has(s7) || this.subscriptions.set(s7, { disposer: () => {
        this.subscriptions.delete(s7);
      }, consumerHost: t8 });
      const { disposer: h3 } = this.subscriptions.get(s7);
      s7(this.value, h3);
    }
    clearCallbacks() {
      this.subscriptions.clear();
    }
  };

  // node_modules/@lit/context/lib/controllers/context-provider.js
  var e10 = class extends Event {
    constructor(t8, s7) {
      super("context-provider", { bubbles: true, composed: true }), this.context = t8, this.contextTarget = s7;
    }
  };
  var i9 = class extends s6 {
    constructor(s7, e12, i10) {
      super(void 0 !== e12.context ? e12.initialValue : i10), this.onContextRequest = (t8) => {
        if (t8.context !== this.context) return;
        const s8 = t8.contextTarget ?? t8.composedPath()[0];
        s8 !== this.host && (t8.stopPropagation(), this.addCallback(t8.callback, s8, t8.subscribe));
      }, this.onProviderRequest = (s8) => {
        if (s8.context !== this.context) return;
        if ((s8.contextTarget ?? s8.composedPath()[0]) === this.host) return;
        const e13 = /* @__PURE__ */ new Set();
        for (const [s9, { consumerHost: i11 }] of this.subscriptions) e13.has(s9) || (e13.add(s9), i11.dispatchEvent(new s4(this.context, i11, s9, true)));
        s8.stopPropagation();
      }, this.host = s7, void 0 !== e12.context ? this.context = e12.context : this.context = e12, this.attachListeners(), this.host.addController?.(this);
    }
    attachListeners() {
      this.host.addEventListener("context-request", this.onContextRequest), this.host.addEventListener("context-provider", this.onProviderRequest);
    }
    hostConnected() {
      this.host.dispatchEvent(new e10(this.context, this.host));
    }
  };

  // node_modules/@lit/context/lib/decorators/provide.js
  function e11({ context: e12 }) {
    return (n9, i10) => {
      const r9 = /* @__PURE__ */ new WeakMap();
      if ("object" == typeof i10) return { get() {
        return n9.get.call(this);
      }, set(t8) {
        return r9.get(this).setValue(t8), n9.set.call(this, t8);
      }, init(n10) {
        return r9.set(this, new i9(this, { context: e12, initialValue: n10 })), n10;
      } };
      {
        n9.constructor.addInitializer(((n10) => {
          r9.set(n10, new i9(n10, { context: e12 }));
        }));
        const o13 = Object.getOwnPropertyDescriptor(n9, i10);
        let s7;
        if (void 0 === o13) {
          const t8 = /* @__PURE__ */ new WeakMap();
          s7 = { get() {
            return t8.get(this);
          }, set(e13) {
            r9.get(this).setValue(e13), t8.set(this, e13);
          }, configurable: true, enumerable: true };
        } else {
          const t8 = o13.set;
          s7 = { ...o13, set(e13) {
            r9.get(this).setValue(e13), t8?.call(this, e13);
          } };
        }
        return void Object.defineProperty(n9, i10, s7);
      }
    };
  }

  // node_modules/@lit/context/lib/decorators/consume.js
  function c5({ context: c6, subscribe: e12 }) {
    return (o13, n9) => {
      "object" == typeof n9 ? n9.addInitializer((function() {
        new s5(this, { context: c6, callback: (t8) => {
          o13.set.call(this, t8);
        }, subscribe: e12 });
      })) : o13.constructor.addInitializer(((o14) => {
        new s5(o14, { context: c6, callback: (t8) => {
          o14[n9] = t8;
        }, subscribe: e12 });
      }));
    };
  }

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.2THL7BRS.js
  var treeItemContext = n8("cs-tree-item");
  var CsTreeItem = class extends CornerstoneElement {
    constructor() {
      super(...arguments);
      this.localize = new LocalizeController2(this);
      this.indeterminate = false;
      this.isLeaf = false;
      this.loading = false;
      this.selectable = false;
      this.expanded = false;
      this.selected = false;
      this.disabled = false;
      this.lazy = false;
      this._treeItemContext = { depth: 0, expanded: this.expanded };
      this._parentTreeContext = null;
      this.animationGeneration = 0;
      this.tabIndex = -1;
      this.role = "treeitem";
    }
    static isTreeItem(node) {
      const el = node;
      return el && (el.role === "treeitem" || el.getAttribute?.("role") === "treeitem");
    }
    connectedCallback() {
      super.connectedCallback();
      this.setAttribute("role", "treeitem");
      this.setAttribute("tabIndex", this.tabIndex.toString());
      if (this.isNestedItem()) {
        this.setAttribute("slot", "children");
        if (!this._parentTreeContext?.expanded) {
          this.expanded = false;
        }
      }
      if (this._parentTreeContext) {
        this._treeItemContext = { depth: this._parentTreeContext.depth + 1, expanded: this.expanded };
      }
      this.updateIndentation();
    }
    firstUpdated(changedProperties) {
      super.firstUpdated(changedProperties);
      this.childrenContainer.hidden = !this.expanded;
      this.childrenContainer.style.height = this.expanded ? "auto" : "0";
      this.isLeaf = !this.lazy && this.getChildrenItems().length === 0;
      this.handleExpandedChange();
    }
    async animateCollapse(generation) {
      this.dispatchEvent(new CsCollapseEvent());
      const duration = parseDuration(getComputedStyle(this.childrenContainer).getPropertyValue("--hide-duration"));
      await animate(
        this.childrenContainer,
        [
          // We can't animate from 'auto', so use the scroll height for now
          { height: `${this.childrenContainer.scrollHeight}px`, opacity: "1", overflow: "hidden" },
          { height: "0", opacity: "0", overflow: "hidden" }
        ],
        { duration, easing: "cubic-bezier(0.4, 0.0, 0.2, 1)" }
      );
      if (this.animationGeneration !== generation) {
        return;
      }
      this.childrenContainer.hidden = true;
      this.dispatchEvent(new CsAfterCollapseEvent());
    }
    // Checks whether the item is nested into an item
    isNestedItem() {
      if (this._parentTreeContext !== null) {
        return true;
      }
      const parent = this.parentElement;
      return !!parent && CsTreeItem.isTreeItem(parent);
    }
    /** Counts the nesting depth and sets the private --indent property on the host for indentation. */
    updateIndentation() {
      const depth = Math.max(this._treeItemContext?.depth || 0, this.getDepth());
      this.setStyleProperty("--indent", `calc(${depth} * var(--indent-size, 2em))`);
    }
    getDepth() {
      let depth = 0;
      let node = this.parentElement;
      while (node) {
        if (CsTreeItem.isTreeItem(node)) {
          depth++;
        }
        node = node.parentElement;
      }
      return depth;
    }
    handleChildrenSlotChange() {
      this.loading = false;
      this.isLeaf = !this.lazy && this.getChildrenItems().length === 0;
    }
    willUpdate(changedProperties) {
      if (changedProperties.has("selected") && !changedProperties.has("indeterminate")) {
        this.indeterminate = false;
      }
      super.willUpdate(changedProperties);
    }
    async animateExpand(generation) {
      this.dispatchEvent(new CsExpandEvent());
      this.childrenContainer.hidden = false;
      const duration = parseDuration(getComputedStyle(this.childrenContainer).getPropertyValue("--show-duration"));
      await animate(
        this.childrenContainer,
        [
          { height: "0", opacity: "0", overflow: "hidden" },
          { height: `${this.childrenContainer.scrollHeight}px`, opacity: "1", overflow: "hidden" }
        ],
        {
          duration,
          easing: "cubic-bezier(0.4, 0.0, 0.2, 1)"
        }
      );
      if (this.animationGeneration !== generation) {
        return;
      }
      this.childrenContainer.style.height = "auto";
      this.dispatchEvent(new CsAfterExpandEvent());
    }
    handleLoadingChange() {
      this.setAttribute("aria-busy", this.loading ? "true" : "false");
      if (!this.loading) {
        this.animateExpand(this.animationGeneration);
      }
    }
    handleDisabledChange() {
      this.customStates.set("disabled", this.disabled);
      this.setAttribute("aria-disabled", this.disabled ? "true" : "false");
    }
    handleExpandedState() {
      this.customStates.set("expanded", this.expanded);
    }
    handleIndeterminateStateChange() {
      this.customStates.set("indeterminate", this.indeterminate);
    }
    handleSelectedChange() {
      this.customStates.set("selected", this.selected);
      this.setAttribute("aria-selected", this.selected ? "true" : "false");
    }
    handleExpandedChange() {
      if (!this.isLeaf) {
        this.setAttribute("aria-expanded", this.expanded ? "true" : "false");
      } else {
        this.removeAttribute("aria-expanded");
      }
    }
    handleExpandAnimation() {
      this.animationGeneration++;
      const generation = this.animationGeneration;
      if (this.expanded) {
        if (this.lazy) {
          this.loading = true;
          this.dispatchEvent(new CsLazyLoadEvent());
        } else {
          this.animateExpand(generation);
        }
      } else {
        this.animateCollapse(generation);
      }
    }
    handleLazyChange() {
      this.dispatchEvent(new CsLazyChangeEvent());
    }
    /** Gets all the nested tree items in this node. */
    getChildrenItems({ includeDisabled = true } = {}) {
      return this.childrenSlot ? [...this.childrenSlot.assignedElements({ flatten: true })].filter(
        (item) => CsTreeItem.isTreeItem(item) && (includeDisabled || !item.disabled)
      ) : [];
    }
    render() {
      const isRtl = this.localize.dir() === "rtl";
      const showExpandButton = !this.loading && (!this.isLeaf || this.lazy);
      return b2`
      <div
        part="tree-item"
        class="${e7({
        "tree-item": true,
        "tree-item-expanded": this.expanded,
        "tree-item-selected": this.selected,
        "tree-item-leaf": this.isLeaf,
        "tree-item-loading": this.loading,
        "tree-item-has-expand-button": showExpandButton
      })}"
      >
        <div class="item" part="item">
          <div class="indentation" part="indentation"></div>

          <div
            part="expand-button"
            class=${e7({
        "expand-button": true,
        "expand-button-visible": showExpandButton
      })}
            aria-hidden="true"
          >
            <slot class="expand-icon-slot" name="expand-icon">
              ${n7(
        this.loading,
        () => b2` <cs-spinner part="spinner" exportparts="spinner:spinner__spinner"></cs-spinner> `,
        () => b2`
                  <cs-icon name=${isRtl ? "keyboard_arrow_left" : "keyboard_arrow_right"} library="system"></cs-icon>
                `
      )}
            </slot>
            <slot class="expand-icon-slot" name="collapse-icon">
              <cs-icon name=${isRtl ? "keyboard_arrow_left" : "keyboard_arrow_right"} library="system"></cs-icon>
            </slot>
          </div>

          ${n7(
        this.selectable,
        () => b2`
              <cs-checkbox
                part="checkbox"
                exportparts="
                    checkbox:checkbox__checkbox,
                    control:checkbox__control,
                    checked-icon:checkbox__checked-icon,
                    indeterminate-icon:checkbox__indeterminate-icon,
                    label:checkbox__label
                  "
                class="checkbox"
                ?disabled="${this.disabled}"
                ?checked="${l5(this.selected)}"
                ?indeterminate="${this.indeterminate}"
                tabindex="-1"
              ></cs-checkbox>
            `
      )}

          <slot class="label" part="label"></slot>
        </div>

        <div class="children" part="children" role="group" ?hidden=${!this.expanded && !this.isConnected}>
          <slot name="children" @slotchange="${this.handleChildrenSlotChange}"></slot>
        </div>
      </div>
    `;
    }
  };
  CsTreeItem.css = tree_item_styles_default;
  __decorateClass([
    r5()
  ], CsTreeItem.prototype, "indeterminate", 2);
  __decorateClass([
    r5()
  ], CsTreeItem.prototype, "isLeaf", 2);
  __decorateClass([
    r5()
  ], CsTreeItem.prototype, "loading", 2);
  __decorateClass([
    r5()
  ], CsTreeItem.prototype, "selectable", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsTreeItem.prototype, "expanded", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsTreeItem.prototype, "selected", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsTreeItem.prototype, "disabled", 2);
  __decorateClass([
    n4({ type: Boolean, reflect: true })
  ], CsTreeItem.prototype, "lazy", 2);
  __decorateClass([
    e11({ context: treeItemContext })
  ], CsTreeItem.prototype, "_treeItemContext", 2);
  __decorateClass([
    c5({ context: treeItemContext, subscribe: false })
  ], CsTreeItem.prototype, "_parentTreeContext", 2);
  __decorateClass([
    e5("slot:not([name])")
  ], CsTreeItem.prototype, "defaultSlot", 2);
  __decorateClass([
    e5("slot[name=children]")
  ], CsTreeItem.prototype, "childrenSlot", 2);
  __decorateClass([
    e5(".item")
  ], CsTreeItem.prototype, "itemElement", 2);
  __decorateClass([
    e5(".children")
  ], CsTreeItem.prototype, "childrenContainer", 2);
  __decorateClass([
    e5(".expand-button slot")
  ], CsTreeItem.prototype, "expandButtonSlot", 2);
  __decorateClass([
    n4({ reflect: true, type: Number, attribute: "tabindex" })
  ], CsTreeItem.prototype, "tabIndex", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsTreeItem.prototype, "role", 2);
  __decorateClass([
    watch("loading", { waitUntilFirstUpdate: true })
  ], CsTreeItem.prototype, "handleLoadingChange", 1);
  __decorateClass([
    watch("disabled")
  ], CsTreeItem.prototype, "handleDisabledChange", 1);
  __decorateClass([
    watch("expanded")
  ], CsTreeItem.prototype, "handleExpandedState", 1);
  __decorateClass([
    watch("indeterminate")
  ], CsTreeItem.prototype, "handleIndeterminateStateChange", 1);
  __decorateClass([
    watch("selected")
  ], CsTreeItem.prototype, "handleSelectedChange", 1);
  __decorateClass([
    watch("expanded", { waitUntilFirstUpdate: true })
  ], CsTreeItem.prototype, "handleExpandedChange", 1);
  __decorateClass([
    watch("expanded", { waitUntilFirstUpdate: true })
  ], CsTreeItem.prototype, "handleExpandAnimation", 1);
  __decorateClass([
    watch("lazy", { waitUntilFirstUpdate: true })
  ], CsTreeItem.prototype, "handleLazyChange", 1);
  CsTreeItem = __decorateClass([
    customElement("cs-tree-item")
  ], CsTreeItem);
  CsTreeItem.disableWarning?.("change-in-update");

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.43XL544F.js
  var tree_styles_default = i`
  :host {
    /*
     * These are actually used by tree item, but we define them here so they can more easily be set and all tree items
     * stay consistent.
     */
    --indent-guide-color: var(--cs-color-surface-border);
    --indent-guide-offset: 0;
    --indent-guide-style: solid;
    --indent-guide-width: 0;
    --indent-size: 2em;

    display: block;
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.S4OTN7IE.js
  function syncCheckboxes(changedTreeItem, initialSync = false) {
    function syncParentItem(treeItem) {
      const children = treeItem.getChildrenItems({ includeDisabled: false });
      if (children.length) {
        const allChecked = children.every((item) => item.selected);
        const allUnchecked = children.every((item) => !item.selected && !item.indeterminate);
        treeItem.selected = allChecked;
        treeItem.indeterminate = !allChecked && !allUnchecked;
      }
    }
    function syncAncestors(treeItem) {
      const parentItem = treeItem.parentElement;
      if (CsTreeItem.isTreeItem(parentItem)) {
        syncParentItem(parentItem);
        syncAncestors(parentItem);
      }
    }
    function syncDescendants(treeItem) {
      for (const childItem of treeItem.getChildrenItems()) {
        childItem.selected = initialSync ? treeItem.selected || childItem.selected : !childItem.disabled && treeItem.selected;
        syncDescendants(childItem);
      }
      if (initialSync) {
        syncParentItem(treeItem);
      }
    }
    syncDescendants(changedTreeItem);
    syncAncestors(changedTreeItem);
  }
  var CsTree = class extends CornerstoneElement {
    constructor() {
      super();
      this.selection = "single";
      this.clickTarget = null;
      this.localize = new LocalizeController2(this);
      this.tabIndex = 0;
      this.role = "tree";
      this.initTreeItem = (item) => {
        item.updateComplete.then(() => {
          item.selectable = this.selection === "multiple" || this.selection === "leaf-multiple" && item.isLeaf;
          ["expand", "collapse"].filter((status) => !!this.querySelector(`[slot="${status}-icon"]`)).forEach((status) => {
            const existingIcon = item.querySelector(`[slot="${status}-icon"]`);
            const expandButtonIcon = this.getExpandButtonIcon(status);
            if (!expandButtonIcon) {
              return;
            }
            if (existingIcon === null) {
              item.append(expandButtonIcon);
            } else if (existingIcon.hasAttribute("data-default")) {
              existingIcon.replaceWith(expandButtonIcon);
            } else {
            }
          });
        });
      };
      this.handleTreeChanged = (mutations) => {
        for (const mutation of mutations) {
          const addedNodes = [...mutation.addedNodes].filter(CsTreeItem.isTreeItem);
          const removedNodes = [...mutation.removedNodes].filter(CsTreeItem.isTreeItem);
          addedNodes.forEach(this.initTreeItem);
          if (this.lastFocusedItem && removedNodes.includes(this.lastFocusedItem)) {
            this.lastFocusedItem = null;
          }
        }
      };
      this.handleFocusOut = (event) => {
        const relatedTarget = event.relatedTarget;
        if (!relatedTarget || !this.contains(relatedTarget)) {
          this.tabIndex = 0;
        }
      };
      this.handleFocusIn = (event) => {
        const target = event.target;
        if (event.target === this) {
          this.focusItem(this.lastFocusedItem || this.getAllTreeItems()[0]);
        }
        if (CsTreeItem.isTreeItem(target) && !target.disabled) {
          if (this.lastFocusedItem) {
            this.lastFocusedItem.tabIndex = -1;
          }
          this.lastFocusedItem = target;
          this.tabIndex = -1;
          target.tabIndex = 0;
        }
      };
      if ("addEventListener" in this) {
        this.addEventListener("focusin", this.handleFocusIn);
        this.addEventListener("focusout", this.handleFocusOut);
        this.addEventListener("cs-lazy-change", this.handleSlotChange);
      }
    }
    async connectedCallback() {
      super.connectedCallback();
      if (typeof MutationObserver !== "undefined") {
        await this.updateComplete;
        this.mutationObserver = new MutationObserver(this.handleTreeChanged);
        this.mutationObserver.observe(this, { childList: true, subtree: true });
      }
      this.setAttribute("tabindex", "0");
      this.setAttribute("role", "tree");
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this.mutationObserver?.disconnect();
    }
    // Generates a clone of the expand icon element to use for each tree item
    getExpandButtonIcon(status) {
      const slot = status === "expand" ? this.expandedIconSlot : this.collapsedIconSlot;
      const icon = slot.assignedElements({ flatten: true })[0];
      if (icon) {
        const clone = icon.cloneNode(true);
        [clone, ...clone.querySelectorAll("[id]")].forEach((el) => el.removeAttribute("id"));
        clone.setAttribute("data-default", "");
        clone.slot = `${status}-icon`;
        return clone;
      }
      return null;
    }
    selectItem(selectedItem) {
      const previousSelection = [...this.selectedItems];
      if (this.selection === "multiple") {
        selectedItem.selected = !selectedItem.selected;
        if (selectedItem.lazy) {
          selectedItem.expanded = true;
        }
        syncCheckboxes(selectedItem);
      } else if (this.selection === "leaf-multiple") {
        if (selectedItem.isLeaf) {
          selectedItem.selected = !selectedItem.selected;
        } else {
          selectedItem.expanded = !selectedItem.expanded;
        }
      } else if (this.selection === "single" || selectedItem.isLeaf) {
        const items = this.getAllTreeItems();
        for (const item of items) {
          item.selected = item === selectedItem;
        }
      } else if (this.selection === "leaf") {
        selectedItem.expanded = !selectedItem.expanded;
      }
      const nextSelection = this.selectedItems;
      if (previousSelection.length !== nextSelection.length || nextSelection.some((item) => !previousSelection.includes(item))) {
        Promise.all(nextSelection.map((el) => el.updateComplete)).then(() => {
          this.dispatchEvent(new CsSelectionChangeEvent({ selection: nextSelection }));
        });
      }
    }
    getAllTreeItems() {
      return [...this.querySelectorAll("cs-tree-item")];
    }
    focusItem(item) {
      item?.focus();
    }
    handleKeyDown(event) {
      if (!["ArrowDown", "ArrowUp", "ArrowRight", "ArrowLeft", "Home", "End", "Enter", " "].includes(event.key)) {
        return;
      }
      if (event.composedPath().some((el) => ["input", "textarea"].includes(el?.tagName?.toLowerCase()))) {
        return;
      }
      const items = this.getFocusableItems();
      const isLtr = this.matches(":dir(ltr)");
      const isRtl = this.localize.dir() === "rtl";
      if (items.length > 0) {
        const activeItemIndex = items.findIndex((item) => item.matches(":focus"));
        const activeItem = items[activeItemIndex];
        if (!activeItem && (event.key === "Enter" || event.key === " ")) {
          return;
        }
        event.preventDefault();
        const focusItemAt = (index) => {
          const item = items[clamp(index, 0, items.length - 1)];
          this.focusItem(item);
        };
        const toggleExpand = (expanded) => {
          activeItem.expanded = expanded;
        };
        if (event.key === "ArrowDown") {
          focusItemAt(activeItemIndex + 1);
        } else if (event.key === "ArrowUp") {
          focusItemAt(activeItemIndex - 1);
        } else if (isLtr && event.key === "ArrowRight" || isRtl && event.key === "ArrowLeft") {
          if (!activeItem || activeItem.disabled || activeItem.expanded || activeItem.isLeaf && !activeItem.lazy) {
            focusItemAt(activeItemIndex + 1);
          } else {
            toggleExpand(true);
          }
        } else if (isLtr && event.key === "ArrowLeft" || isRtl && event.key === "ArrowRight") {
          if (!activeItem || activeItem.disabled || activeItem.isLeaf || !activeItem.expanded) {
            focusItemAt(activeItemIndex - 1);
          } else {
            toggleExpand(false);
          }
        } else if (event.key === "Home") {
          focusItemAt(0);
        } else if (event.key === "End") {
          focusItemAt(items.length - 1);
        } else if (event.key === "Enter" || event.key === " ") {
          if (activeItem && !activeItem.disabled) {
            this.selectItem(activeItem);
          }
        }
      }
    }
    handleClick(event) {
      const target = event.target;
      const treeItem = target.closest("cs-tree-item");
      const isExpandButton = event.composedPath().some((el) => el?.classList?.contains("expand-button"));
      if (!treeItem || treeItem.disabled || target !== this.clickTarget) {
        return;
      }
      if (isExpandButton) {
        treeItem.expanded = !treeItem.expanded;
      } else {
        this.selectItem(treeItem);
      }
    }
    handleMouseDown(event) {
      this.clickTarget = event.target;
    }
    handleSlotChange() {
      const items = this.getAllTreeItems();
      items.forEach(this.initTreeItem);
    }
    async handleSelectionChange() {
      const isSelectionMultiple = this.selection === "multiple";
      const isSelectionLeafMultiple = this.selection === "leaf-multiple";
      const items = this.getAllTreeItems();
      this.setAttribute("aria-multiselectable", isSelectionMultiple || isSelectionLeafMultiple ? "true" : "false");
      for (const item of items) {
        item.updateComplete.then(() => {
          item.selectable = isSelectionMultiple || isSelectionLeafMultiple && item.isLeaf;
        });
      }
      if (isSelectionMultiple) {
        await this.updateComplete;
        [...this.querySelectorAll(":scope > cs-tree-item")].forEach((treeItem) => {
          treeItem.updateComplete.then(() => {
            syncCheckboxes(treeItem, true);
          });
        });
      }
    }
    /** @internal Returns the list of tree items that are selected in the tree. */
    get selectedItems() {
      const items = this.getAllTreeItems();
      const isSelected = (item) => item.selected;
      return items.filter(isSelected);
    }
    /** @internal Gets focusable tree items in the tree. */
    getFocusableItems() {
      const items = this.getAllTreeItems();
      const collapsedItems = /* @__PURE__ */ new Set();
      return items.filter((item) => {
        if (item.disabled) {
          return false;
        }
        const parent = item.parentElement?.closest("[role=treeitem]");
        if (parent && (!parent.expanded || parent.loading || collapsedItems.has(parent))) {
          collapsedItems.add(item);
        }
        return !collapsedItems.has(item);
      });
    }
    render() {
      return b2`
      <div
        part="tree"
        class="tree"
        @click=${this.handleClick}
        @keydown=${this.handleKeyDown}
        @mousedown=${this.handleMouseDown}
      >
        <slot @slotchange=${this.handleSlotChange}></slot>
        <span hidden aria-hidden="true"><slot name="expand-icon"></slot></span>
        <span hidden aria-hidden="true"><slot name="collapse-icon"></slot></span>
      </div>
    `;
    }
  };
  CsTree.css = tree_styles_default;
  __decorateClass([
    e5("slot:not([name])")
  ], CsTree.prototype, "defaultSlot", 2);
  __decorateClass([
    e5("slot[name=expand-icon]")
  ], CsTree.prototype, "expandedIconSlot", 2);
  __decorateClass([
    e5("slot[name=collapse-icon]")
  ], CsTree.prototype, "collapsedIconSlot", 2);
  __decorateClass([
    n4()
  ], CsTree.prototype, "selection", 2);
  __decorateClass([
    n4({ attribute: "tabindex", reflect: true, type: Number })
  ], CsTree.prototype, "tabIndex", 2);
  __decorateClass([
    n4({ reflect: true })
  ], CsTree.prototype, "role", 2);
  __decorateClass([
    watch("selection")
  ], CsTree.prototype, "handleSelectionChange", 1);
  CsTree = __decorateClass([
    customElement("cs-tree")
  ], CsTree);

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.LH3NPPW7.js
  var zoomable_frame_styles_default = i`
  :host {
    display: block;
    position: relative;
    aspect-ratio: 16 / 9;
    width: 100%;
    overflow: hidden;
    border-radius: var(--cs-border-radius-m);
  }

  #frame-container {
    position: absolute;
    top: 0;
    left: 0;
    width: calc(100% / var(--zoom));
    height: calc(100% / var(--zoom));
    transform: scale(var(--zoom));
    transform-origin: 0 0;
  }

  #iframe {
    width: 100%;
    height: 100%;
    border: none;
    border-radius: inherit;
    /* Prevent the iframe from being selected, e.g. by a double click. Doesn't affect selection withing the iframe. */
    user-select: none;
    -webkit-user-select: none;
  }

  #controls {
    display: flex;
    position: absolute;
    bottom: 0.5em;
    align-items: center;
    font-weight: var(--cs-font-weight-semibold);
    padding: 0.25em 0.5em;
    gap: 0.5em;
    border-radius: var(--cs-border-radius-s);
    background: #000b;
    color: white;
    font-size: min(12px, 0.75em);
    user-select: none;
    -webkit-user-select: none;

    &:dir(ltr) {
      right: 0.5em;
    }

    &:dir(rtl) {
      left: 0.5em;
    }

    button {
      display: flex;
      align-items: center;
      padding: 0.25em;
      border: none;
      background: none;
      color: inherit;
      cursor: pointer;

      &:focus {
        outline: none;
      }

      &:focus-visible {
        outline: var(--cs-focus-ring);
        outline-offset: var(--cs-focus-ring-offset);
      }

      &:disabled {
        cursor: not-allowed;
        opacity: 0.5;
      }
    }

    span {
      min-width: 4.5ch; /* extra space so numbers don't shift */
      font-variant-numeric: tabular-nums;
      text-align: center;
    }
  }
`;

  // node_modules/@cruglobal/cornerstone-components/dist/unbundled/chunks/chunk.IXYMBNTU.js
  var ColorSchemeController = class {
    constructor(host, onThemeChange) {
      this.handleTransitionEnd = () => {
        this.onThemeChange();
      };
      (this.host = host).addController(this);
      this.onThemeChange = onThemeChange;
      if (typeof document !== "undefined") {
        this.hiddenElement = document.createElement("div");
        this.hiddenElement.setAttribute("aria-hidden", "true");
        Object.assign(this.hiddenElement.style, {
          position: "absolute",
          width: "0",
          height: "0",
          overflow: "hidden",
          pointerEvents: "none",
          opacity: "0",
          // Transition on a WA surface token — changes whenever the theme class changes
          color: "var(--cs-color-surface-default, transparent)",
          transition: "color 0.001ms"
        });
      }
    }
    hostConnected() {
      if (this.hiddenElement) {
        this.host.appendChild(this.hiddenElement);
        this.hiddenElement.addEventListener("transitionend", this.handleTransitionEnd);
      }
    }
    hostDisconnected() {
      if (this.hiddenElement) {
        this.hiddenElement.removeEventListener("transitionend", this.handleTransitionEnd);
        this.hiddenElement.remove();
      }
    }
  };
  var CsZoomableFrame = class extends CornerstoneElement {
    constructor() {
      super();
      this.localize = new LocalizeController2(this);
      this.themeObserver = !o5 ? new MutationObserver(() => this.syncTheme()) : null;
      this.availableZoomLevels = [];
      this.allowfullscreen = false;
      this.loading = "eager";
      this.zoom = 1;
      this.zoomLevels = "25% 50% 75% 100% 125% 150% 175% 200%";
      this.withoutControls = false;
      this.withoutInteraction = false;
      this.withThemeSync = false;
      new ColorSchemeController(this, () => this.syncTheme());
    }
    /** Returns the internal iframe's `window` object. (Readonly property) */
    get contentWindow() {
      return this.iframe?.contentWindow || null;
    }
    /** Returns the internal iframe's `document` object. (Readonly property) */
    get contentDocument() {
      return this.iframe?.contentDocument || null;
    }
    parseZoomLevels(zoomLevelsString) {
      const tokens = parseSpaceDelimitedTokens(zoomLevelsString);
      const levels = [];
      for (const token of tokens) {
        let value;
        if (token.endsWith("%")) {
          const percentage = parseFloat(token.slice(0, -1));
          if (!isNaN(percentage)) {
            value = Math.max(0, percentage / 100);
          } else {
            continue;
          }
        } else {
          value = parseFloat(token);
          if (!isNaN(value)) {
            value = Math.max(0, value);
          } else {
            continue;
          }
        }
        levels.push(value);
      }
      return [...new Set(levels)].sort((a4, b3) => a4 - b3);
    }
    getCurrentZoomIndex() {
      if (this.availableZoomLevels.length === 0) {
        return -1;
      }
      let closestIndex = 0;
      let closestDiff = Math.abs(this.availableZoomLevels[0] - this.zoom);
      for (let i10 = 1; i10 < this.availableZoomLevels.length; i10++) {
        const diff = Math.abs(this.availableZoomLevels[i10] - this.zoom);
        if (diff < closestDiff) {
          closestDiff = diff;
          closestIndex = i10;
        }
      }
      return closestIndex;
    }
    isZoomInDisabled() {
      if (this.availableZoomLevels.length === 0) {
        return false;
      }
      const currentIndex = this.getCurrentZoomIndex();
      return currentIndex >= this.availableZoomLevels.length - 1;
    }
    isZoomOutDisabled() {
      if (this.availableZoomLevels.length === 0) {
        return false;
      }
      const currentIndex = this.getCurrentZoomIndex();
      return currentIndex <= 0;
    }
    willUpdate(changedProperties) {
      if (changedProperties.has("zoom")) {
        this.setStyleProperty("--zoom", `${this.zoom}`);
      }
      super.willUpdate(changedProperties);
    }
    updated(changedProperties) {
      if (changedProperties.has("zoomLevels")) {
        this.availableZoomLevels = this.parseZoomLevels(this.zoomLevels);
        if (this.availableZoomLevels.length > 0) {
          const currentIndex = this.getCurrentZoomIndex();
          if (Math.abs(this.availableZoomLevels[currentIndex] - this.zoom) > 1e-3) {
            this.zoom = this.availableZoomLevels[currentIndex];
          }
        }
      }
      if (changedProperties.has("withThemeSync")) {
        if (this.withThemeSync) {
          this.themeObserver?.observe(document.documentElement, { attributes: true, attributeFilter: ["class"] });
          this.syncTheme();
        } else {
          this.themeObserver?.disconnect();
        }
      }
      super.updated(changedProperties);
    }
    /** Zooms in to the next available zoom level. */
    zoomIn() {
      if (this.availableZoomLevels.length === 0) {
        this.zoom = Math.min(this.zoom + 0.05, 2);
        return;
      }
      const currentIndex = this.getCurrentZoomIndex();
      if (currentIndex < this.availableZoomLevels.length - 1) {
        this.zoom = this.availableZoomLevels[currentIndex + 1];
      }
    }
    /** Zooms out to the previous available zoom level. */
    zoomOut() {
      if (this.availableZoomLevels.length === 0) {
        this.zoom = Math.max(this.zoom - 0.05, 0);
        return;
      }
      const currentIndex = this.getCurrentZoomIndex();
      if (currentIndex > 0) {
        this.zoom = this.availableZoomLevels[currentIndex - 1];
      }
    }
    disconnectedCallback() {
      super.disconnectedCallback();
      this.themeObserver?.disconnect();
    }
    syncTheme() {
      if (!this.withThemeSync) {
        return;
      }
      try {
        const iframeRoot = this.contentDocument?.documentElement;
        if (!iframeRoot) {
          return;
        }
        const prefixes = ["cs-theme-", "cs-brand-", "cs-palette-"];
        const schemeCls = /* @__PURE__ */ new Set();
        const themeCls = /* @__PURE__ */ new Set();
        let el = this;
        let schemeFound = false;
        while (el) {
          if (!schemeFound) {
            if (el.classList.contains("cs-dark")) {
              schemeCls.add("cs-dark");
              schemeFound = true;
            } else if (el.classList.contains("cs-light")) {
              schemeCls.add("cs-light");
              schemeFound = true;
            }
          }
          for (const cls of el.classList) {
            if (prefixes.some((p4) => cls.startsWith(p4))) {
              themeCls.add(cls);
            }
          }
          el = el.parentElement;
        }
        iframeRoot.classList.toggle("cs-dark", schemeCls.has("cs-dark"));
        iframeRoot.classList.toggle("cs-light", schemeCls.has("cs-light"));
        const toRemove = Array.from(iframeRoot.classList).filter((c6) => prefixes.some((p4) => c6.startsWith(p4)));
        iframeRoot.classList.remove(...toRemove);
        iframeRoot.classList.add(...themeCls);
      } catch {
      }
    }
    handleLoad() {
      if (this.withThemeSync) {
        this.syncTheme();
      }
      this.dispatchEvent(new Event("load", { bubbles: false, cancelable: false, composed: true }));
    }
    handleError() {
      this.dispatchEvent(new Event("error", { bubbles: false, cancelable: false, composed: true }));
    }
    render() {
      return b2`
      <div id="frame-container">
        <iframe
          id="iframe"
          part="iframe"
          ?inert=${this.withoutInteraction}
          ?allowfullscreen=${this.allowfullscreen}
          loading=${this.loading}
          referrerpolicy=${this.referrerpolicy}
          sandbox=${o8(this.sandbox ?? void 0)}
          src=${o8(this.src ?? void 0)}
          srcdoc=${o8(this.srcdoc ?? void 0)}
          @load=${this.handleLoad}
          @error=${this.handleError}
        ></iframe>
      </div>

      ${!this.withoutControls ? b2`
              <div id="controls" part="controls">
                <button
                  part="zoom-out-button"
                  aria-label=${this.localize.term("zoomOut")}
                  @click=${this.zoomOut}
                  ?disabled=${this.isZoomOutDisabled()}
                >
                  <slot name="zoom-out-icon">
                    <cs-icon name="remove" label="Zoom out"></cs-icon>
                  </slot>
                </button>
                <span>${this.localize.number(this.zoom, { style: "percent", maximumFractionDigits: 1 })}</span>
                <button
                  part="zoom-in-button"
                  aria-label=${this.localize.term("zoomIn")}
                  @click=${this.zoomIn}
                  ?disabled=${this.isZoomInDisabled()}
                >
                  <slot name="zoom-in-icon">
                    <cs-icon name="add" label="Zoom in"></cs-icon>
                  </slot>
                </button>
              </div>
            ` : ""}
    `;
    }
  };
  CsZoomableFrame.css = zoomable_frame_styles_default;
  __decorateClass([
    r5()
  ], CsZoomableFrame.prototype, "availableZoomLevels", 2);
  __decorateClass([
    e5("#iframe")
  ], CsZoomableFrame.prototype, "iframe", 2);
  __decorateClass([
    n4()
  ], CsZoomableFrame.prototype, "src", 2);
  __decorateClass([
    n4()
  ], CsZoomableFrame.prototype, "srcdoc", 2);
  __decorateClass([
    n4({ type: Boolean })
  ], CsZoomableFrame.prototype, "allowfullscreen", 2);
  __decorateClass([
    n4()
  ], CsZoomableFrame.prototype, "loading", 2);
  __decorateClass([
    n4()
  ], CsZoomableFrame.prototype, "referrerpolicy", 2);
  __decorateClass([
    n4()
  ], CsZoomableFrame.prototype, "sandbox", 2);
  __decorateClass([
    n4({ type: Number, reflect: true })
  ], CsZoomableFrame.prototype, "zoom", 2);
  __decorateClass([
    n4({ attribute: "zoom-levels" })
  ], CsZoomableFrame.prototype, "zoomLevels", 2);
  __decorateClass([
    n4({ type: Boolean, attribute: "without-controls", reflect: true })
  ], CsZoomableFrame.prototype, "withoutControls", 2);
  __decorateClass([
    n4({ type: Boolean, attribute: "without-interaction", reflect: true })
  ], CsZoomableFrame.prototype, "withoutInteraction", 2);
  __decorateClass([
    n4({ type: Boolean, attribute: "with-theme-sync", reflect: true })
  ], CsZoomableFrame.prototype, "withThemeSync", 2);
  CsZoomableFrame = __decorateClass([
    customElement("cs-zoomable-frame")
  ], CsZoomableFrame);
  return __toCommonJS(index_exports);
})();

/* ── This system's runtime additions ──────────────────────────────────────
   Expose the library under this system's namespace, and replace the DEFAULT icon
   library with an inlined one.

   Upstream, <cs-icon> resolves each icon to
   https://cdn.jsdelivr.net/npm/@material-symbols/svg-400@0.46.0/sharp/<name>.svg and
   fetches it at runtime. Previews here make no network requests, so author-written
   icons had nothing to draw. The icons below are the same artwork —
   @material-symbols/svg-400 0.46.0, sharp cut, weight 400, which is what the Cru
   brand guideline specifies — served as data URIs, the mechanism the library already
   uses for its own chrome. The shim at the top of this file is what lets either of
   them resolve under this page's Content-Security-Policy.

   78 names: the ones these previews use, plus every name the components draw in their
   own shadow DOM. An unknown name falls back to the help glyph. Only the sharp/400 cut
   is inlined, so family and variant are ignored here. In your own app nothing is
   overridden: install the package and <cs-icon> fetches the full set as usual. */
window.CruCornerstone = window.Cornerstone;
(function () {
  var ICONS = {"account_circle":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M222-255q63-44 125-67.5T480-346q71 0 133.5 23.5T739-255q44-54 62.5-109T820-480q0-145-97.5-242.5T480-820q-145 0-242.5 97.5T140-480q0 61 19 116t63 109Zm160.5-234.5Q343-529 343-587t39.5-97.5Q422-724 480-724t97.5 39.5Q617-645 617-587t-39.5 97.5Q538-450 480-450t-97.5-39.5ZM480-80q-82 0-155-31.5t-127.5-86Q143-252 111.5-325T80-480q0-83 31.5-155.5t86-127Q252-817 325-848.5T480-880q83 0 155.5 31.5t127 86q54.5 54.5 86 127T880-480q0 82-31.5 155t-86 127.5q-54.5 54.5-127 86T480-80Zm107.5-76Q640-172 691-212q-51-36-104-55t-107-19q-54 0-107 19t-104 55q51 40 103.5 56T480-140q55 0 107.5-16Zm-52-375.5Q557-553 557-587t-21.5-55.5Q514-664 480-664t-55.5 21.5Q403-621 403-587t21.5 55.5Q446-510 480-510t55.5-21.5ZM480-587Zm0 374Z\"/></svg>","add":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M450-450H200v-60h250v-250h60v250h250v60H510v250h-60v-250Z\"/></svg>","add_circle":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M453-280h60v-166h167v-60H513v-174h-60v174H280v60h173v166Zm27.27 200q-82.74 0-155.5-31.5Q252-143 197.5-197.5t-86-127.34Q80-397.68 80-480.5t31.5-155.66Q143-709 197.5-763t127.34-85.5Q397.68-880 480.5-880t155.66 31.5Q709-817 763-763t85.5 127Q880-563 880-480.27q0 82.74-31.5 155.5Q817-252 763-197.68q-54 54.31-127 86Q563-80 480.27-80Zm.23-60Q622-140 721-239.5t99-241Q820-622 721.19-721T480-820q-141 0-240.5 98.81T140-480q0 141 99.5 240.5t241 99.5Zm-.5-340Z\"/></svg>","arrow_back":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"m274-450 248 248-42 42-320-320 320-320 42 42-248 248h526v60H274Z\"/></svg>","arrow_downward":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M450-800v526L202-522l-42 42 320 320 320-320-42-42-248 248v-526h-60Z\"/></svg>","arrow_forward":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M686-450H160v-60h526L438-758l42-42 320 320-320 320-42-42 248-248Z\"/></svg>","arrow_upward":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M450-160v-526L202-438l-42-42 320-320 320 320-42 42-248-248v526h-60Z\"/></svg>","attach_file":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M728-326q0 103-72.18 174.5-72.17 71.5-175 71.5Q378-80 305.5-151.5T233-326v-380q0-72.5 51.5-123.25T408-880q72 0 123.5 50.75T583-706v360q0 42-30 72t-72.5 30q-42.5 0-72.5-29.67-30-29.68-30-72.33v-370h60v370q0 17 12.5 29.5t30.64 12.5q18.14 0 30-12.5T523-346v-360q0-48-33.5-81t-81.71-33q-48.21 0-81.5 33.06T293-706v380q0 78 54.97 132T481-140q77.92 0 132.46-54Q668-248 668-326v-390h60v390Z\"/></svg>","attach_money":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M451-120v-84q-57-10-93.5-43.5T305-332l56-23q17 48 49 71.5t77 23.5q48 0 79-24t31-66q0-44-27.5-68T466-467q-72-23-107.5-61T323-623q0-55 35.5-92t92.5-42v-83h60v83q45 5 77.5 29.5T638-665l-56 24q-14-32-37.5-46.5T483-702q-46 0-73 21t-27 57q0 38 30 61.5T524-514q68 21 100.5 60.5T657-354q0 63-37 101.5T511-203v83h-60Z\"/></svg>","audio_file":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M427-192q41 0 70-28.31T526-290v-202h116v-60H486v187q-11-9-26.38-14-15.39-5-32.62-5-39.27 0-66.13 27.5Q334-329 334-289.5t26.87 68.5q26.86 29 66.13 29ZM160-80v-800h421l219 219v581H160Zm391-554v-186H220v680h520v-494H551ZM220-820v186-186 680-680Z\"/></svg>","book":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M160-80v-800h640v800H160Zm60-60h520v-680h-60v266l-97-56-97 56v-266H220v680Zm0 0v-680 680Zm266-414 97-56 97 56-97-56-97 56Z\"/></svg>","bookmark":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M200-120v-725h560v725L480-240 200-120Zm60-91 220-93 220 93v-574H260v574Zm0-574h440-440Z\"/></svg>","bug_report":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M480-180q72 0 123-50.5T654-353v-167q0-72-51-122.5T480-693q-72 0-123 50.5T306-520v167q0 72 51 122.5T480-180Zm-80-140h160v-60H400v60Zm0-173h160v-60H400v60Zm80.5 57h-.5.5-.5.5-.5.5-.5.5Zm-.5 316q-65 0-121-31t-83-89H160v-60h92q-7-26-7-52.5V-406h-85v-60h85q0-29 .5-57.5T254-580h-94v-60h120q14-28 37-49t51-35l-77-76 40-40 94 94q28-10 56.5-10t56.5 10l94-94 40 40-76 76q28 14 49.5 35.5T683-640h117v60h-94q9 28 8.5 56.5T714-466h86v60h-86q0 27 .5 53.5T708-300h92v60H685q-26 59-82.5 89.5T480-120Z\"/></svg>","calendar_month":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M440-400v-80h80v80h-80Zm-160 0v-80h80v80h-80Zm320 0v-80h80v80h-80ZM440-240v-80h80v80h-80Zm-160 0v-80h80v80h-80Zm320 0v-80h80v80h-80ZM120-80v-740h125v-60h65v60h340v-60h65v60h125v740H120Zm60-60h600v-430H180v430Zm0-490h600v-130H180v130Zm0 0v-130 130Z\"/></svg>","calendar_today":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M120-80v-740h125v-60h65v60h340v-60h65v60h125v740H120Zm60-60h600v-430H180v430Zm0-490h600v-130H180v130Zm0 0v-130 130Z\"/></svg>","call":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M780-120q-108 0-225-56.5T336-335Q233-437 176.5-554T120-780v-60h215l40 189-117 119q28 45 57.5 83.5T377-378q36 36 77 67.5t88 57.5l117-121 181 39v215h-60ZM229-588l81-82-23-110H180q0 38 11.5 85T229-588Zm369 363q41 19 89 31t93 14v-107l-103-21-79 83ZM229-588Zm369 363Z\"/></svg>","cancel":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"m330-288 150-150 150 150 42-42-150-150 150-150-42-42-150 150-150-150-42 42 150 150-150 150 42 42ZM480-80q-82 0-155-31.5t-127.5-86Q143-252 111.5-325T80-480q0-83 31.5-156t86-127Q252-817 325-848.5T480-880q83 0 156 31.5T763-763q54 54 85.5 127T880-480q0 82-31.5 155T763-197.5q-54 54.5-127 86T480-80Zm0-60q142 0 241-99.5T820-480q0-142-99-241t-241-99q-141 0-240.5 99T140-480q0 141 99.5 240.5T480-140Zm0-340Z\"/></svg>","check":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M378-246 154-470l43-43 181 181 384-384 43 43-427 427Z\"/></svg>","check_circle":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"m421-298 283-283-46-45-237 237-120-120-45 45 165 166Zm59 218q-82 0-155-31.5t-127.5-86Q143-252 111.5-325T80-480q0-83 31.5-156t86-127Q252-817 325-848.5T480-880q83 0 156 31.5T763-763q54 54 85.5 127T880-480q0 82-31.5 155T763-197.5q-54 54.5-127 86T480-80Zm0-60q142 0 241-99.5T820-480q0-142-99-241t-241-99q-141 0-240.5 99T140-480q0 141 99.5 240.5T480-140Zm0-340Z\"/></svg>","chevron_left":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M561-240 320-481l241-241 43 43-198 198 198 198-43 43Z\"/></svg>","chevron_right":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M530-481 332-679l43-43 241 241-241 241-43-43 198-198Z\"/></svg>","close":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"m249-207-42-42 231-231-231-231 42-42 231 231 231-231 42 42-231 231 231 231-42 42-231-231-231 231Z\"/></svg>","close_fullscreen":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"m122-80-42-42 298-298H160v-60h320v320h-60v-218L122-80Zm358-400v-320h60v218l298-298 42 42-298 298h218v60H480Z\"/></svg>","closed_caption":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M120-160v-640h720v640H120Zm60-60h600v-520H180v520Zm60-141h202v-72h-50v22H290v-138h102v22h50v-72H240v238Zm279 0h202v-72h-50v22H569v-138h102v22h50v-72H519v238ZM180-220v-520 520Z\"/></svg>","closed_caption_disabled":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"m361-599 50 50H290v138h102v-22h50v72H240v-238h121ZM246-800h594v594l-60-60v-474H306l-60-60Zm475 367v72h-36l-50-50h36v-22h50Zm-50-94v-22H569v72l-50-50v-72h202v72h-50Zm-128 24Zm-126 46ZM163-797l57 57h-40v520h474L26-848l43-43L876-84l-43 43-119-119H120v-637h43Z\"/></svg>","code_blocks":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"m379-343 44-44-93-93 92-92-44-44-136 136 137 137Zm202 0 137-137-137-137-44 44 93 93-93 93 44 44ZM120-120v-720h720v720H120Zm60-60h600v-600H180v600Zm0 0v-600 600Z\"/></svg>","coffee":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M442-242q-116 0-199-80t-83-195v-323h589q54 0 92.5 37t38.5 91q0 60-37 106t-94 46h-25v43q0 115-83 195t-199 80ZM220-620h444v-160H220v160Zm222 318q91 0 156.5-62T664-517v-43H220v43q0 91 65.5 153T442-302Zm282-318h25q33 0 52-28.5t19-63.5q0-29-21-48.5T749-780h-25v160ZM160-120v-60h640v60H160Zm282-440Z\"/></svg>","colorize":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M120-120v-168l377-377-72-72 41-41 92 92 166-166 128 128-166 166 92 92-41 41-72-72-377 377H120Zm60-60h87l355-355-87-87-355 355v87Zm463-420 124-124-43-43-124 124 43 43Zm0 0-43-43 43 43Z\"/></svg>","confirmation_number":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M480-283q12 0 21-9t9-21q0-12-9-21t-21-9q-12 0-21 9t-9 21q0 12 9 21t21 9Zm0-167q12 0 21-9t9-21q0-12-9-21t-21-9q-12 0-21 9t-9 21q0 12 9 21t21 9Zm0-167q12 0 21-9t9-21q0-12-9-21t-21-9q-12 0-21 9t-9 21q0 12 9 21t21 9ZM80-160v-213q37-8 61.5-37.5T166-480q0-40-24.5-70T80-587v-213h800v213q-37 7-61.5 37T794-480q0 40 24.5 69.5T880-373v213H80Zm60-60h680v-109q-38-26-62-65t-24-86q0-47 24-86t62-65v-109H140v109q39 26 62.5 65t23.5 86q0 47-23.5 86T140-329v109Zm340-260Z\"/></svg>","content_copy":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M240-200v-680h560v680H240Zm60-60h440v-560H300v560ZM120-80v-680h60v620h500v60H120Zm180-180v-560 560Z\"/></svg>","content_cut":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M782-114 481-415 364-298q11 17 13.5 33t2.5 35q0 64-43 107T230-80q-64 0-107-43T80-230q0-64 43-107t107-43q18 0 35.5 5t36.5 15l116-116-118-118q-17 8-34.5 11t-35.5 3q-64 0-107-43T80-730q0-64 43-107t107-43q64 0 107 43t43 107q0 19-2.5 36T367-662l514 514v34h-99ZM599-527l-66-66 249-249h99v33L599-527ZM294-666q26-26 26-64t-26-64q-26-26-64-26t-64 26q-26 26-26 64t26 64q26 26 64 26t64-26Zm202.5 203.5Q502-468 502-476t-5.5-13.5Q491-495 483-495t-13.5 5.5Q464-484 464-476t5.5 13.5Q475-457 483-457t13.5-5.5ZM294-166q26-26 26-64t-26-64q-26-26-64-26t-64 26q-26 26-26 64t26 64q26 26 64 26t64-26Z\"/></svg>","content_paste":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M120-120v-720h262q7-35 34.5-57.5T480-920q36 0 63.5 22.5T578-840h262v720H120Zm60-60h600v-600h-60v90H240v-90h-60v600Zm328.5-611.5Q520-803 520-820t-11.5-28.5Q497-860 480-860t-28.5 11.5Q440-837 440-820t11.5 28.5Q463-780 480-780t28.5-11.5Z\"/></svg>","dark_mode":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M480-120q-150 0-255-105T120-480q0-150 105-255t255-105q8 0 17 .5t23 1.5q-36 32-56 79t-20 99q0 90 63 153t153 63q52 0 99-18.5t79-51.5q1 12 1.5 19.5t.5 14.5q0 150-105 255T480-120Zm0-60q109 0 190-67.5T771-406q-25 11-53.67 16.5Q688.67-384 660-384q-114.69 0-195.34-80.66Q384-545.31 384-660q0-24 5-51.5t18-62.5q-98 27-162.5 109.5T180-480q0 125 87.5 212.5T480-180Zm-4-297Z\"/></svg>","delete":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M201-120v-630h-41v-60h188v-30h264v30h188v60h-41v630H201Zm60-60h438v-570H261v570Zm106-86h60v-399h-60v399Zm166 0h60v-399h-60v399ZM261-750v570-570Z\"/></svg>","description":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M319-250h322v-60H319v60Zm0-170h322v-60H319v60ZM160-80v-800h421l219 219v581H160Zm391-554v-186H220v680h520v-494H551ZM220-820v186-186 680-680Z\"/></svg>","directions_walk":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"m278-40 116-586-101 43v133h-61v-173l192-81q14-6 29.5-7.5T484-710q17 3 29.5 11t20.5 20l42 66q31 48 77.5 75.5T753-510v60q-70-2-123.5-30.5T533-568l-38 152 92 83v293h-60v-240l-108-98-79 338h-62Zm210.5-735.5Q467-797 467-827t21.5-51.5Q510-900 540-900t51.5 21.5Q613-857 613-827t-21.5 51.5Q570-754 540-754t-51.5-21.5Z\"/></svg>","do_not_disturb_on":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M280-453h400v-60H280v60ZM480-80q-82 0-155-31.5t-127.5-86Q143-252 111.5-325T80-480q0-83 31.5-156t86-127Q252-817 325-848.5T480-880q83 0 156 31.5T763-763q54 54 85.5 127T880-480q0 82-31.5 155T763-197.5q-54 54.5-127 86T480-80Zm0-60q142 0 241-99.5T820-480q0-142-99-241t-241-99q-141 0-240.5 99T140-480q0 141 99.5 240.5T480-140Zm0-340Z\"/></svg>","download":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M480-313 287-506l43-43 120 120v-371h60v371l120-120 43 43-193 193ZM160-160v-203h60v143h520v-143h60v203H160Z\"/></svg>","draft":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M160-80v-800h421l219 219v581H160Zm391-554v-186H220v680h520v-494H551ZM220-820v186-186 680-680Z\"/></svg>","drag_indicator":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M349.91-160q-28.91 0-49.41-20.59-20.5-20.59-20.5-49.5t20.59-49.41q20.59-20.5 49.5-20.5t49.41 20.59q20.5 20.59 20.5 49.5t-20.59 49.41q-20.59 20.5-49.5 20.5Zm260 0q-28.91 0-49.41-20.59-20.5-20.59-20.5-49.5t20.59-49.41q20.59-20.5 49.5-20.5t49.41 20.59q20.5 20.59 20.5 49.5t-20.59 49.41q-20.59 20.5-49.5 20.5Zm-260-250q-28.91 0-49.41-20.59-20.5-20.59-20.5-49.5t20.59-49.41q20.59-20.5 49.5-20.5t49.41 20.59q20.5 20.59 20.5 49.5t-20.59 49.41q-20.59 20.5-49.5 20.5Zm260 0q-28.91 0-49.41-20.59-20.5-20.59-20.5-49.5t20.59-49.41q20.59-20.5 49.5-20.5t49.41 20.59q20.5 20.59 20.5 49.5t-20.59 49.41q-20.59 20.5-49.5 20.5Zm-260-250q-28.91 0-49.41-20.59-20.5-20.59-20.5-49.5t20.59-49.41q20.59-20.5 49.5-20.5t49.41 20.59q20.5 20.59 20.5 49.5t-20.59 49.41q-20.59 20.5-49.5 20.5Zm260 0q-28.91 0-49.41-20.59-20.5-20.59-20.5-49.5t20.59-49.41q20.59-20.5 49.5-20.5t49.41 20.59q20.5 20.59 20.5 49.5t-20.59 49.41q-20.59 20.5-49.5 20.5Z\"/></svg>","edit":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M180-180h44l472-471-44-44-472 471v44Zm-60 60v-128l617-616 128 128-617 616H120Zm659-617-41-41 41 41Zm-105 64-22-22 44 44-22-22Z\"/></svg>","error":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M503.5-289.48q9.5-9.48 9.5-23.5t-9.48-23.52q-9.48-9.5-23.5-9.5t-23.52 9.48q-9.5 9.48-9.5 23.5t9.48 23.52q9.48 9.5 23.5 9.5t23.52-9.48ZM453-433h60v-253h-60v253Zm27.27 353q-82.74 0-155.5-31.5Q252-143 197.5-197.5t-86-127.34Q80-397.68 80-480.5t31.5-155.66Q143-709 197.5-763t127.34-85.5Q397.68-880 480.5-880t155.66 31.5Q709-817 763-763t85.5 127Q880-563 880-480.27q0 82.74-31.5 155.5Q817-252 763-197.68q-54 54.31-127 86Q563-80 480.27-80Zm.23-60Q622-140 721-239.5t99-241Q820-622 721.19-721T480-820q-141 0-240.5 98.81T140-480q0 141 99.5 240.5t241 99.5Zm-.5-340Z\"/></svg>","event":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M528-248.18q-28-28.19-28-69Q500-358 528.18-386q28.19-28 69-28Q638-414 666-385.82q28 28.19 28 69Q694-276 665.82-248q-28.19 28-69 28Q556-220 528-248.18ZM120-80v-740h125v-60h65v60h340v-60h65v60h125v740H120Zm60-60h600v-430H180v430Zm0-490h600v-130H180v130Zm0 0v-130 130Z\"/></svg>","expand_content":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M200-200v-240h60v180h180v60H200Zm500-320v-180H520v-60h240v240h-60Z\"/></svg>","fast_forward":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M104-240v-480l346 240-346 240Zm407 0v-480l346 240-346 240ZM164-480Zm407 0ZM164-355l181-125-181-125v250Zm407 0 181-125-181-125v250Z\"/></svg>","fast_rewind":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M854-240 508-480l346-240v480Zm-402 0L106-480l346-240v480Zm-60-240Zm402 0ZM392-355v-250L211-480l181 125Zm402 0v-250L613-480l181 125Z\"/></svg>","favorite":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"m480-121-41-37q-105.77-97.12-174.88-167.56Q195-396 154-451.5T96.5-552Q80-597 80-643q0-90.15 60.5-150.58Q201-854 290-854q57 0 105.5 27t84.5 78q42-54 89-79.5T670-854q89 0 149.5 60.42Q880-733.15 880-643q0 46-16.5 91T806-451.5Q765-396 695.88-325.56 626.77-255.12 521-158l-41 37Zm0-79q101.24-93 166.62-159.5Q712-426 750.5-476t54-89.14q15.5-39.13 15.5-77.72 0-66.14-42-108.64T670.22-794q-51.52 0-95.37 31.5T504-674h-49q-26-56-69.85-88-43.85-32-95.37-32Q224-794 182-751.5t-42 108.82q0 38.68 15.5 78.18 15.5 39.5 54 90T314-358q66 66 166 158Zm0-297Z\"/></svg>","filter_alt":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M400-160v-280L118-800h724L560-440v280H400Zm80-276 240-304H240l240 304Zm0 0Z\"/></svg>","first_page":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M240-240v-480h60v480h-60Zm447-3L453-477l234-234 43 43-191 191 191 191-43 43Z\"/></svg>","folder":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M80-160v-640h341l60 60h399v580H80Zm60-60h680v-460H456l-60-60H140v520Zm0 0v-520 520Z\"/></svg>","folder_zip":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M640-496v-92h92v92h-92Zm0 92h-92v-92h92v92Zm0 92v-92h92v92h-92ZM456-680l-60-60H140v520h408v-92h92v92h180v-460H640v92h-92v-92h-92ZM80-160v-640h341l60 60h399v580H80Zm60-60v-520 520Z\"/></svg>","format_quote":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"m248-240 94-162q-5 1-11 1.5t-11 .5q-66 0-113-47t-47-113q0-66 47-113t113-47q66 0 113 47t47 113q0 21-5.5 41T458-480L320-240h-72Zm360 0 94-162q-5 1-11 1.5t-11 .5q-66 0-113-47t-47-113q0-66 47-113t113-47q66 0 113 47t47 113q0 21-5.5 41T818-480L680-240h-72ZM376.5-503.5Q400-527 400-560t-23.5-56.5Q353-640 320-640t-56.5 23.5Q240-593 240-560t23.5 56.5Q287-480 320-480t56.5-23.5Zm360 0Q760-527 760-560t-23.5-56.5Q713-640 680-640t-56.5 23.5Q600-593 600-560t23.5 56.5Q647-480 680-480t56.5-23.5ZM680-560Zm-360 0Z\"/></svg>","groups":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M0-240v-53q0-38.57 41.5-62.78Q83-380 150.38-380q12.16 0 23.39.5t22.23 2.15q-8 17.35-12 35.17-4 17.81-4 37.18v65H0Zm240 0v-65q0-32 17.5-58.5T307-410q32-20 76.5-30t96.5-10q53 0 97.5 10t76.5 30q32 20 49 46.5t17 58.5v65H240Zm540 0v-65q0-19.86-3.5-37.43T765-377.27q11-1.73 22.17-2.23 11.17-.5 22.83-.5 67.5 0 108.75 23.77T960-293v53H780Zm-480-60h360v-6q0-37-50.5-60.5T480-390q-79 0-129.5 23.5T300-305v5ZM149.57-410q-28.57 0-49.07-20.56Q80-451.13 80-480q0-29 20.56-49.5Q121.13-550 150-550q29 0 49.5 20.5t20.5 49.93q0 28.57-20.5 49.07T149.57-410Zm660 0q-28.57 0-49.07-20.56Q740-451.13 740-480q0-29 20.56-49.5Q781.13-550 810-550q29 0 49.5 20.5t20.5 49.93q0 28.57-20.5 49.07T809.57-410ZM480-480q-50 0-85-35t-35-85q0-51 35-85.5t85-34.5q51 0 85.5 34.5T600-600q0 50-34.5 85T480-480Zm.35-60Q506-540 523-557.35t17-43Q540-626 522.85-643t-42.5-17q-25.35 0-42.85 17.15t-17.5 42.5q0 25.35 17.35 42.85t43 17.5ZM480-300Zm0-300Z\"/></svg>","help":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M511-258q11-11 11-27t-11-27q-11-11-27-11t-27 11q-11 11-11 27t11 27q11 11 27 11t27-11Zm-62-135h59q0-26 6.5-47.5T555-490q31-26 44-51t13-55q0-53-34.5-85T486-713q-49 0-86.5 24.5T345-621l53 20q11-28 33-43.5t52-15.5q34 0 55 18.5t21 47.5q0 22-13 41.5T508-512q-30 26-44.5 51.5T449-393Zm31 313q-82 0-155-31.5t-127.5-86Q143-252 111.5-325T80-480q0-83 31.5-156t86-127Q252-817 325-848.5T480-880q83 0 156 31.5T763-763q54 54 85.5 127T880-480q0 82-31.5 155T763-197.5q-54 54.5-127 86T480-80Zm0-60q142 0 241-99.5T820-480q0-142-99-241t-241-99q-141 0-240.5 99T140-480q0 141 99.5 240.5T480-140Zm0-340Z\"/></svg>","home":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M220-180h150v-250h220v250h150v-390L480-765 220-570v390Zm-60 60v-480l320-240 320 240v480H530v-250H430v250H160Zm320-353Z\"/></svg>","horizontal_rule":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M160-450v-60h640v60H160Z\"/></svg>","image":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M236-277h489L578-473 446-302l-93-127-117 152ZM120-120v-720h720v720H120Zm60-60h600v-600H180v600Zm0 0v-600 600Z\"/></svg>","info":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M453-280h60v-240h-60v240Zm50.5-323.2q9.5-9.2 9.5-22.8 0-14.45-9.48-24.22-9.48-9.78-23.5-9.78t-23.52 9.78Q447-640.45 447-626q0 13.6 9.48 22.8 9.48 9.2 23.5 9.2t23.52-9.2ZM480.27-80q-82.74 0-155.5-31.5Q252-143 197.5-197.5t-86-127.34Q80-397.68 80-480.5t31.5-155.66Q143-709 197.5-763t127.34-85.5Q397.68-880 480.5-880t155.66 31.5Q709-817 763-763t85.5 127Q880-563 880-480.27q0 82.74-31.5 155.5Q817-252 763-197.68q-54 54.31-127 86Q563-80 480.27-80Zm.23-60Q622-140 721-239.5t99-241Q820-622 721.19-721T480-820q-141 0-240.5 98.81T140-480q0 141 99.5 240.5t241 99.5Zm-.5-340Z\"/></svg>","keyboard_arrow_down":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M480-344 240-584l43-43 197 197 197-197 43 43-240 240Z\"/></svg>","keyboard_arrow_left":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M561-240 320-481l241-241 43 43-198 198 198 198-43 43Z\"/></svg>","keyboard_arrow_right":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M530-481 332-679l43-43 241 241-241 241-43-43 198-198Z\"/></svg>","keyboard_arrow_up":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M480-554 283-357l-43-43 240-240 240 240-43 43-197-197Z\"/></svg>","keyboard_double_arrow_left":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M453-241 213-481l240-240 42 42-198 198 198 198-42 42Zm253 0L466-481l240-240 42 42-198 198 198 198-42 42Z\"/></svg>","keyboard_double_arrow_right":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M411-481 213-679l42-42 240 240-240 240-42-42 198-198Zm253 0L466-679l42-42 240 240-240 240-42-42 198-198Z\"/></svg>","language":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M323-111.5Q250-143 196-197t-85-127.5Q80-398 80-482t31-156.5Q142-711 196-765t127-84.5Q396-880 480-880t157 30.5Q710-819 764-765t85 126.5Q880-566 880-482t-31 157.5Q818-251 764-197t-127 85.5Q564-80 480-80t-157-31.5ZM480-138q35-36 58.5-82.5T577-331H384q14 60 37.5 108t58.5 85Zm-85-12q-25-38-43-82t-30-99H172q38 71 88 111.5T395-150Zm171-1q72-23 129.5-69T788-331H639q-13 54-30.5 98T566-151ZM152-391h159q-3-27-3.5-48.5T307-482q0-25 1-44.5t4-43.5H152q-7 24-9.5 43t-2.5 45q0 26 2.5 46.5T152-391Zm221 0h215q4-31 5-50.5t1-40.5q0-20-1-38.5t-5-49.5H373q-4 31-5 49.5t-1 38.5q0 21 1 40.5t5 50.5Zm275 0h160q7-24 9.5-44.5T820-482q0-26-2.5-45t-9.5-43H649q3 35 4 53.5t1 34.5q0 22-1.5 41.5T648-391Zm-10-239h150q-33-69-90.5-115T565-810q25 37 42.5 80T638-630Zm-254 0h194q-11-53-37-102.5T480-820q-32 27-54 71t-42 119Zm-212 0h151q11-54 28-96.5t43-82.5q-75 19-131 64t-91 115Z\"/></svg>","last_page":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"m272-245-43-43 192-192-192-192 43-43 235 235-235 235Zm388 5v-480h60v480h-60Z\"/></svg>","light_mode":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M579-381q41-41 41-99t-41-99q-41-41-99-41t-99 41q-41 41-41 99t41 99q41 41 99 41t99-41Zm-240.5 42.5Q280-397 280-480t58.5-141.5Q397-680 480-680t141.5 58.5Q680-563 680-480t-58.5 141.5Q563-280 480-280t-141.5-58.5ZM200-450H40v-60h160v60Zm720 0H760v-60h160v60ZM450-760v-160h60v160h-60Zm0 720v-160h60v160h-60ZM262-658l-100-97 43-44 96 100-39 41Zm494 496-98-100 41-41 99 98-42 43Zm-99-537 98-99 44 42-99 98-43-41ZM162-205l99-98 42 42-98 99-43-43Zm318-275Z\"/></svg>","lightbulb":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M422.5-103.5Q399-127 399-161h162q0 34-23.5 57.5T480-80q-34 0-57.5-23.5ZM318-223v-60h324v60H318Zm5-121q-66-43-104.5-107.5T180-597q0-122 89-211t211-89q122 0 211 89t89 211q0 81-38 145.5T637-344H323Zm22-60h271q48-32 76-83t28-110q0-99-70.5-169.5T480-837q-99 0-169.5 70.5T240-597q0 59 28 110t77 83Zm135 0Z\"/></svg>","link":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M450-280H280q-83 0-141.5-58.5T80-480q0-83 58.5-141.5T280-680h170v60H280q-58.33 0-99.17 40.76-40.83 40.77-40.83 99Q140-422 180.83-381q40.84 41 99.17 41h170v60ZM325-450v-60h310v60H325Zm185 170v-60h170q58.33 0 99.17-40.76 40.83-40.77 40.83-99Q820-538 779.17-579q-40.84-41-99.17-41H510v-60h170q83 0 141.5 58.5T880-480q0 83-58.5 141.5T680-280H510Z\"/></svg>","local_cafe":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M160-120v-60h640v60H160Zm0-140v-580h720v280H724v300H160Zm60-60h444v-460H220v460Zm504-300h96v-160h-96v160ZM220-320h444-444Z\"/></svg>","location_on":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M529.5-510.5Q550-531 550-560t-20.5-49.5Q509-630 480-630t-49.5 20.5Q410-589 410-560t20.5 49.5Q451-490 480-490t49.5-20.5ZM480-159q133-121 196.5-219.5T740-552q0-118-75.5-193T480-820q-109 0-184.5 75T220-552q0 75 65 173.5T480-159Zm0 79Q319-217 239.5-334.5T160-552q0-150 96.5-239T480-880q127 0 223.5 89T800-552q0 100-79.5 217.5T480-80Zm0-480Z\"/></svg>","lock":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M160-80v-554h130v-96q0-78.85 55.61-134.42Q401.21-920 480.11-920q78.89 0 134.39 55.58Q670-808.85 670-730v96h130v554H160Zm60-60h520v-434H220v434Zm314.5-162.03Q557-324.06 557-355q0-30-22.67-54.5t-54.5-24.5q-31.83 0-54.33 24.5t-22.5 55q0 30.5 22.67 52.5t54.5 22q31.83 0 54.33-22.03ZM350-634h260v-96q0-54.17-37.88-92.08-37.88-37.92-92-37.92T388-822.08q-38 37.91-38 92.08v96ZM220-140v-434 434Z\"/></svg>","login":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M481-120v-60h299v-600H481v-60h359v720H481Zm-55-185-43-43 102-102H120v-60h363L381-612l43-43 176 176-174 174Z\"/></svg>","logout":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M120-120v-720h359v60H180v600h299v60H120Zm546-185-43-43 102-102H360v-60h363L621-612l43-43 176 176-174 174Z\"/></svg>","mail":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M80-160v-640h800v640H80Zm400-302L140-685v465h680v-465L480-462Zm0-60 336-218H145l335 218ZM140-685v-55 520-465Z\"/></svg>","menu":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M120-240v-60h720v60H120Zm0-210v-60h720v60H120Zm0-210v-60h720v60H120Z\"/></svg>","menu_open":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M120-240v-60h520v60H120Zm678-52L609-481l188-188 43 43-145 145 146 146-43 43ZM120-452v-60h400v60H120Zm0-208v-60h520v60H120Z\"/></svg>","more_horiz":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M207.86-432Q188-432 174-446.14t-14-34Q160-500 174.14-514t34-14Q228-528 242-513.86t14 34Q256-460 241.86-446t-34 14Zm272 0Q460-432 446-446.14t-14-34Q432-500 446.14-514t34-14Q500-528 514-513.86t14 34Q528-460 513.86-446t-34 14Zm272 0Q732-432 718-446.14t-14-34Q704-500 718.14-514t34-14Q772-528 786-513.86t14 34Q800-460 785.86-446t-34 14Z\"/></svg>","more_vert":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M479.86-160Q460-160 446-174.14t-14-34Q432-228 446.14-242t34-14Q500-256 514-241.86t14 34Q528-188 513.86-174t-34 14Zm0-272Q460-432 446-446.14t-14-34Q432-500 446.14-514t34-14Q500-528 514-513.86t14 34Q528-460 513.86-446t-34 14Zm0-272Q460-704 446-718.14t-14-34Q432-772 446.14-786t34-14Q500-800 514-785.86t14 34Q528-732 513.86-718t-34 14Z\"/></svg>","neurology":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M385-120q-51 0-86.5-38T258-244q-59-5-98.5-49.5T120-402q0-22 5.5-42.5T142-484q-11-18-16.5-38t-5.5-43q0-60 41.5-103t98.5-50q2-51 38.5-86.5T386-840q29 0 51.5 10.5T480-799q20-20 42-30.5t51-10.5q50 0 86 35.5t39 86.5q58 5 100 48.5T840-565q0 23-6 43.5T817-483q11 20 17 42t6 43q0 64-39.5 106.5T702-244q-5 48-41 86t-86 38q-30 0-52-10t-43-30q-21 20-43 30t-52 10Zm125-600v480q0 25 19.5 42.5T576-180q26 0 45-23t21-47q-23-8-43-21.5T565-305q-8-11-6-22.5t13-19.5q11-8 22.5-6t19.5 13q13 18 32 27.5t42 9.5q38 0 65-26t27-69q0-9-2-18.5t-4-19.5q-18 13-39.5 19.5T690-410q-13 0-21.5-8.5T660-440q0-13 8.5-21.5T690-470q38 0 64-28t26-67q0-38-28-65t-64-29q-10 24-28.5 42.5T617-589q-12 5-23-.5T579-607q-4-12 1-23.5t17-15.5q18-6 29.5-24t11.5-41q0-29-19-49t-45-20q-26 0-45 17.5T510-720Zm-60 480v-480q0-25-18.5-42.5T386-780q-26 0-45.5 20T321-711q0 23 11 40.5t29 23.5q12 4 17.5 15.5T380-609q-5 12-16.5 18t-23.5 1q-24-8-42-26.5T270-659q-35 3-62.5 29.5T180-565q0 39 26 67t64 28q13 0 21.5 8.5T300-440q0 13-8.5 21.5T270-410q-23 0-44.5-7T186-436q-3 8-4.5 17t-1.5 18q0 43 26.5 70.5T271-303q22 0 41.5-10t32.5-27q8-10 20-12.5t22 5.5q10 8 12.5 20t-5.5 22q-14 20-33.5 33.5T318-250q2 24 21 47t46 23q27 0 46-17.5t19-42.5Zm30-240Z\"/></svg>","notifications":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M160-200v-60h80v-304q0-84 49.5-150.5T420-798v-82h120v82q81 17 130.5 83.5T720-564v304h80v60H160Zm320-302Zm0 422q-33 0-56.5-23.5T400-160h160q0 33-23.5 56.5T480-80ZM300-260h360v-304q0-75-52.5-127.5T480-744q-75 0-127.5 52.5T300-564v304Z\"/></svg>","notifications_off":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M160-200v-60h80v-304q0-33 8.25-65T273-690l45 45q-9 19-13.5 39.46T300-564v304h319L75-805l42-42 726 727-42 42-122-122H160Zm560-129-60-60v-175q0-75-52.5-127.5T480-744q-35.14 0-67.57 13Q380-718 356-693l-46-45q23-23 50.5-38t59.5-22v-82h120v82q81 17 130.5 83.5T720-564v235Zm-260-91Zm20 340q-33 0-56.5-23.5T400-160h160q0 33-23.5 56.5T480-80Zm28-461Z\"/></svg>","open_in_full":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M120-120v-300h60v198l558-558H540v-60h300v300h-60v-198L222-180h198v60H120Z\"/></svg>","open_in_new":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M120-120v-720h339v60H180v600h600v-279h60v339H120Zm262-219-42-43 398-398H519v-60h321v321h-60v-218L382-339Z\"/></svg>","pause":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M525-200v-560h235v560H525Zm-325 0v-560h235v560H200Zm385-60h115v-440H585v440Zm-325 0h115v-440H260v440Zm0-440v440-440Zm325 0v440-440Z\"/></svg>","person":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M372-523q-42-42-42-108t42-108q42-42 108-42t108 42q42 42 42 108t-42 108q-42 42-108 42t-108-42ZM160-160v-94q0-38 19-65t49-41q67-30 128.5-45T480-420q62 0 123 15.5T731-360q31 14 50 41t19 65v94H160Zm60-60h520v-34q0-16-9.5-30.5T707-306q-64-31-117-42.5T480-360q-57 0-111 11.5T252-306q-14 7-23 21.5t-9 30.5v34Zm324.5-346.5Q570-592 570-631t-25.5-64.5Q519-721 480-721t-64.5 25.5Q390-670 390-631t25.5 64.5Q441-541 480-541t64.5-25.5ZM480-631Zm0 411Z\"/></svg>","pets":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M169.86-485Q132-485 106-511.14t-26-64Q80-613 106.14-639t64-26Q208-665 234-638.86t26 64Q260-537 233.86-511t-64 26ZM291-681.14q-26-26.14-26-64T291.14-809q26.14-26 64-26T419-808.86q26 26.14 26 64T418.86-681q-26.14 26-64 26T291-681.14Zm250 0q-26-26.14-26-64T541.14-809q26.14-26 64-26T669-808.86q26 26.14 26 64T668.86-681q-26.14 26-64 26T541-681.14ZM789.86-485Q752-485 726-511.14t-26-64Q700-613 726.14-639t64-26Q828-665 854-638.86t26 64Q880-537 853.86-511t-64 26ZM266-75q-42 0-69-31.53-27-31.52-27-74.47 0-42 25.5-74.5T250-318q22-22 41-46.5t36-50.5q29-44 65-82t88-38q52 0 88.5 38t65.5 83q17 26 35.5 50t40.5 46q29 30 54.5 62.5T790-181q0 42.95-27 74.47Q736-75 694-75q-54 0-107-9t-107-9q-54 0-107 9t-107 9Z\"/></svg>","picture_as_pdf":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M331-431h37v-83h70l15-15v-92l-15-15H331v205Zm37-120v-48h48v48h-48Zm129 120h106l15-15v-175l-15-15H497v205Zm37-37v-131h47v131h-47Zm133 37h37v-83h50v-37h-50v-48h50v-37h-87v205ZM200-200v-680h680v680H200Zm60-60h560v-560H260v560ZM80-80v-680h60v620h620v60H80Zm180-740v560-560Z\"/></svg>","picture_in_picture_alt":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M80-160v-640h800v640H80Zm60-60h680v-520H140v520Zm0 0v-520 520Zm306-45h335v-257H446v257Zm60-60v-137h215v137H506Z\"/></svg>","play_arrow":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M320-203v-560l440 280-440 280Zm60-280Zm0 171 269-171-269-171v342Z\"/></svg>","play_circle":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"m383-310 267-170-267-170v340Zm97 230q-82 0-155-31.5t-127.5-86Q143-252 111.5-325T80-480q0-83 31.5-156t86-127Q252-817 325-848.5T480-880q83 0 156 31.5T763-763q54 54 85.5 127T880-480q0 82-31.5 155T763-197.5q-54 54.5-127 86T480-80Zm0-60q142 0 241-99.5T820-480q0-142-99-241t-241-99q-141 0-240.5 99T140-480q0 141 99.5 240.5T480-140Zm0-340Z\"/></svg>","print":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M658-648v-132H302v132h-60v-192h476v192h-60Zm-518 60h680-680Zm599 95q12 0 21-9t9-21q0-12-9-21t-21-9q-12 0-21 9t-9 21q0 12 9 21t21 9Zm-81 313v-192H302v192h356Zm60 60H242v-176H80v-352h800v352H718v176Zm102-236v-232H140v232h102v-76h476v76h102Z\"/></svg>","radio_button_unchecked":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M480-80q-82 0-155-31.5t-127.5-86Q143-252 111.5-325T80-480q0-83 31.5-156t86-127Q252-817 325-848.5T480-880q83 0 156 31.5T763-763q54 54 85.5 127T880-480q0 82-31.5 155T763-197.5q-54 54.5-127 86T480-80Zm0-60q142 0 241-99.5T820-480q0-142-99-241t-241-99q-141 0-240.5 99T140-480q0 141 99.5 240.5T480-140Z\"/></svg>","refresh":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M480-160q-133 0-226.5-93.5T160-480q0-133 93.5-226.5T480-800q85 0 149 34.5T740-671v-129h60v254H546v-60h168q-38-60-97-97t-137-37q-109 0-184.5 75.5T220-480q0 109 75.5 184.5T480-220q83 0 152-47.5T728-393h62q-29 105-115 169t-195 64Z\"/></svg>","remove":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M200-450v-60h560v60H200Z\"/></svg>","save":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M840-683v563H120v-720h563l157 157Zm-60 27L656-780H180v600h600v-476ZM553.5-275.26q30.5-30.27 30.5-73.5 0-43.24-30.26-73.74-30.27-30.5-73.5-30.5-43.24 0-73.74 30.26-30.5 30.27-30.5 73.5 0 43.24 30.26 73.74 30.27 30.5 73.5 30.5 43.24 0 73.74-30.26ZM233-584h358v-143H233v143Zm-53-72v476-600 124Z\"/></svg>","schedule":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"m627-287 45-45-159-160v-201h-60v225l174 181ZM480-80q-82 0-155-31.5t-127.5-86Q143-252 111.5-325T80-480q0-82 31.5-155t86-127.5Q252-817 325-848.5T480-880q82 0 155 31.5t127.5 86Q817-708 848.5-635T880-480q0 82-31.5 155t-86 127.5Q708-143 635-111.5T480-80Zm0-400Zm0 340q140 0 240-100t100-240q0-140-100-240T480-820q-140 0-240 100T140-480q0 140 100 240t240 100Z\"/></svg>","search":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M796-121 533-384q-30 26-70 40.5T378-329q-108 0-183-75t-75-181q0-106 75-181t182-75q106 0 180.5 75T632-585q0 43-14 83t-42 75l264 262-44 44ZM377-389q81 0 138-57.5T572-585q0-81-57-138.5T377-781q-82 0-139.5 57.5T180-585q0 81 57.5 138.5T377-389Z\"/></svg>","set_meal":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M40-360v-520h880v520H40Zm60-460v400h760v-400H100Zm23 600-3-60 719-38 3 60-719 38Zm-3 99v-60h720v60H120Zm300-389q70.84 0 136.42-24.5Q622-559 672-610q5 37 37 57.5t71 22.5v-180q-40 3-72.5 23T671-629q-50.77-48.63-115.44-74.81Q490.89-730 420-730q-74.79 0-143.89 25.5Q207-679 162-620q45 59 114.11 84.5Q345.21-510 420-510ZM100-820v400-400Z\"/></svg>","settings":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"m388-80-20-126q-19-7-40-19t-37-25l-118 54-93-164 108-79q-2-9-2.5-20.5T185-480q0-9 .5-20.5T188-521L80-600l93-164 118 54q16-13 37-25t40-18l20-127h184l20 126q19 7 40.5 18.5T669-710l118-54 93 164-108 77q2 10 2.5 21.5t.5 21.5q0 10-.5 21t-2.5 21l108 78-93 164-118-54q-16 13-36.5 25.5T592-206L572-80H388Zm48-60h88l14-112q33-8 62.5-25t53.5-41l106 46 40-72-94-69q4-17 6.5-33.5T715-480q0-17-2-33.5t-7-33.5l94-69-40-72-106 46q-23-26-52-43.5T538-708l-14-112h-88l-14 112q-34 7-63.5 24T306-642l-106-46-40 72 94 69q-4 17-6.5 33.5T245-480q0 17 2.5 33.5T254-413l-94 69 40 72 106-46q24 24 53.5 41t62.5 25l14 112Zm44-210q54 0 92-38t38-92q0-54-38-92t-92-38q-54 0-92 38t-38 92q0 54 38 92t92 38Zm0-130Z\"/></svg>","share":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M686-80q-47.5 0-80.75-33.25T572-194q0-8 5-34L278-403q-16.28 17.34-37.64 27.17Q219-366 194-366q-47.5 0-80.75-33T80-480q0-48 33.25-81T194-594q24 0 45 9.3 21 9.29 37 25.7l301-173q-2-8-3.5-16.5T572-766q0-47.5 33.25-80.75T686-880q47.5 0 80.75 33.25T800-766q0 47.5-33.25 80.75T686-652q-23.27 0-43.64-9Q622-670 606-685L302-516q3 8 4.5 17.5t1.5 18q0 8.5-1 16t-3 15.5l303 173q16-15 36.09-23.5 20.1-8.5 43.07-8.5Q734-308 767-274.75T800-194q0 47.5-33.25 80.75T686-80Zm.04-60q22.96 0 38.46-15.54 15.5-15.53 15.5-38.5 0-22.96-15.54-38.46-15.53-15.5-38.5-15.5-22.96 0-38.46 15.54-15.5 15.53-15.5 38.5 0 22.96 15.54 38.46 15.53 15.5 38.5 15.5Zm-492-286q22.96 0 38.46-15.54 15.5-15.53 15.5-38.5 0-22.96-15.54-38.46-15.53-15.5-38.5-15.5-22.96 0-38.46 15.54-15.5 15.53-15.5 38.5 0 22.96 15.54 38.46 15.53 15.5 38.5 15.5ZM724.5-727.54q15.5-15.53 15.5-38.5 0-22.96-15.54-38.46-15.53-15.5-38.5-15.5-22.96 0-38.46 15.54-15.5 15.53-15.5 38.5 0 22.96 15.54 38.46 15.53 15.5 38.5 15.5 22.96 0 38.46-15.54ZM686-194ZM194-480Zm492-286Z\"/></svg>","skip_next":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M680-240v-480h60v480h-60Zm-460 0v-480l346 240-346 240Zm60-240Zm0 125 181-125-181-125v250Z\"/></svg>","skip_previous":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M220-240v-480h60v480h-60Zm520 0L394-480l346-240v480Zm-60-240Zm0 125v-250L499-480l181 125Z\"/></svg>","slideshow":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"m387-311 261-169-261-169v338ZM120-120v-720h720v720H120Zm60-60h600v-600H180v600Zm0 0v-600 600Z\"/></svg>","sort":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M120-240v-60h240v60H120Zm0-210v-60h480v60H120Zm0-210v-60h720v60H120Z\"/></svg>","speed":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M473.5-303.5Q517-305 537-336l216-339-335 219q-30 20-32 64t21 67q23 23 66.5 21.5ZM478-799q57 0 119 18.5T716-717l-52 37q-45-30-96.5-44.5T477.98-739q-140.47 0-239.23 100.22Q140-538.57 140-396.02 140-351 152.5-305q12.5 46 35.5 85h579q22-36 35-84t13-94q0-42-12.5-90.5T758-578l39-52q38 56 57 112.5T875-404q2 60-12 113t-41 98q-12 23-25.5 28t-33.5 5H192q-17 0-33.5-8.5T134-193q-26-48-40-97.5T80-396q0-83 31.5-156.5t85.5-128Q251-735 323.68-767T478-799Zm-9 331Z\"/></svg>","star":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"m323-245 157-94 157 95-42-178 138-120-182-16-71-168-71 167-182 16 138 120-42 178Zm-90 125 65-281L80-590l288-25 112-265 112 265 288 25-218 189 65 281-247-149-247 149Zm247-355Z\"/></svg>","sync":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M167-160v-60h130l-15-12q-64-51-93-111t-29-134q0-106 62.5-190.5T387-784v62q-75 29-121 96.5T220-477q0 63 23.5 109.5T307-287l30 21v-124h60v230H167Zm407-15v-63q76-29 121-96.5T740-483q0-48-23.5-97.5T655-668l-29-26v124h-60v-230h230v60H665l15 14q60 56 90 120t30 123q0 106-62 191T574-175Z\"/></svg>","table_chart":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M120-121v-719h720v719H120Zm60-518h600v-141H180v141Zm0 458h130v-398H180v398Zm470 0h130v-398H650v398Zm-280 0h220v-398H370v398Z\"/></svg>","thunderstorm":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"m300-40 36-100h-76l50-140h100l-43 100h83L340-40h-40Zm270-40 28-80h-78l43-120h100l-35 80h82L610-80h-40ZM290-340q-86.86 0-148.43-61.52Q80-463.04 80-549.83 80-629 136.5-691 193-753 277-759q32-56 84.5-88.5T480.42-880q90.58 0 152.08 57.5Q694-765 708-680q79 4 125.5 53.5T880-510.38Q880-440 830.42-390 780.83-340 710-340H290Zm0-60h420q46.2 0 78.1-32.5 31.9-32.5 31.9-78T788.1-588q-31.9-32-78.1-32h-60v-30q0-71-49.5-120.5T480.21-820q-51.48 0-93.84 27.5Q344-765 324-718l-8 18h-28q-62 2-105 45.39-43 43.4-43 104.47Q140-488 183.93-444T290-400Zm190-210Z\"/></svg>","tune":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M427-120v-225h60v83h353v60H487v82h-60Zm-307-82v-60h247v60H120Zm187-166v-82H120v-60h187v-84h60v226h-60Zm120-82v-60h413v60H427Zm166-165v-225h60v82h187v60H653v83h-60Zm-473-83v-60h413v60H120Z\"/></svg>","upload":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M450-313v-371L330-564l-43-43 193-193 193 193-43 43-120-120v371h-60ZM160-160v-203h60v143h520v-143h60v203H160Z\"/></svg>","video_file":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M317-240h245v-90l80 46v-152l-80 46v-90H317v240ZM160-80v-800h421l219 219v581H160Zm391-554v-186H220v680h520v-494H551ZM220-820v186-186 680-680Z\"/></svg>","visibility":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M600.5-379.5Q650-429 650-500t-49.5-120.5Q551-670 480-670t-120.5 49.5Q310-571 310-500t49.5 120.5Q409-330 480-330t120.5-49.5Zm-200-41Q368-453 368-500t32.5-79.5Q433-612 480-612t79.5 32.5Q592-547 592-500t-32.5 79.5Q527-388 480-388t-79.5-32.5ZM216-283Q98-366 40-500q58-134 176-217t264-83q146 0 264 83t176 217q-58 134-176 217t-264 83q-146 0-264-83Zm264-217Zm222.5 174.5Q804-391 857-500q-53-109-154.5-174.5T480-740q-121 0-222.5 65.5T102-500q54 109 155.5 174.5T480-260q121 0 222.5-65.5Z\"/></svg>","visibility_off":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"m629-419-44-44q26-71-27-118t-115-24l-44-44q17-11 38-16t43-5q71 0 120.5 49.5T650-500q0 22-5.5 43.5T629-419Zm129 129-40-40q49-36 85.5-80.5T857-500q-50-111-150-175.5T490-740q-42 0-86 8t-69 19l-46-47q35-16 89.5-28T485-800q143 0 261.5 81.5T920-500q-26 64-67 117t-95 93Zm58 226L648-229q-35 14-79 21.5t-89 7.5q-146 0-265-81.5T40-500q20-52 55.5-101.5T182-696L56-822l42-43 757 757-39 44ZM223-654q-37 27-71.5 71T102-500q51 111 153.5 175.5T488-260q33 0 65-4t48-12l-64-64q-11 5-27 7.5t-30 2.5q-70 0-120-49t-50-121q0-15 2.5-30t7.5-27l-97-97Zm305 142Zm-116 58Z\"/></svg>","volume_down":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M200-360v-240h160l200-200v640L360-360H200Zm420 48v-337q54 17 87 64t33 105q0 59-33 105t-87 63ZM500-648 387-540H260v120h127l113 109v-337ZM378-480Z\"/></svg>","volume_off":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M813-56 681-188q-28 20-60.5 34.5T553-131v-62q23-7 44.5-15.5T638-231L473-397v237L273-360H113v-240h156L49-820l43-43 764 763-43 44Zm-36-232-43-43q20-34 29.5-71.92Q773-440.85 773-481q0-103.32-60-184.66T553-769v-62q124 28 202 125.5T833-481q0 51-14 100t-42 93ZM643-422l-90-90v-130q47 22 73.5 66t26.5 96q0 15-2.5 29.5T643-422ZM473-592 369-696l104-104v208Zm-60 286v-150l-84-84H173v120h126l114 114Zm-42-192Z\"/></svg>","volume_up":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"M560-131v-62q97-28 158.5-107.5T780-481q0-101-61-181T560-769v-62q124 28 202 125.5T840-481q0 127-78 224.5T560-131ZM120-360v-240h160l200-200v640L280-360H120Zm420 48v-337q55 17 87.5 64T660-480q0 57-33 104t-87 64ZM420-648 307-540H180v120h127l113 109v-337Zm-94 168Z\"/></svg>","volunteer_activism":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"m566-48-311-89v57H40v-394h309l319 120v73h212v137L566-48Zm-466-92h94v-274h-94v274Zm462 30 256-78v-33H519l-129-40 22-58 115 38h77v-32L339-414h-84v214l307 90Zm84-346L482-616q-29.32-27.74-49.66-61.49Q412-711.24 412-751q0-53 35-91t87-38q34 0 62 17.5t50 43.5q22-26 50-43.5t62-17.5q52 0 87 38t35 91q0 39.66-20.5 73.33T810-616L646-456Zm0-83 120-119q20.12-19.85 37.06-42.32T820-751q0-28-17.5-48.5T758-820q-20 0-37 11t-30 27l-45 55-45-55q-13-16-30-27t-37-11q-27 0-44.5 20.5T472-751q0 28.21 16.94 50.68T526-658l120 119Zm0-188Z\"/></svg>","warning":"<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"48\" height=\"48\" viewBox=\"0 -960 960 960\"><path d=\"m40-120 440-760 440 760H40Zm104-60h672L480-760 144-180Zm361.5-65.68q8.5-8.67 8.5-21.5 0-12.82-8.68-21.32-8.67-8.5-21.5-8.5-12.82 0-21.32 8.68-8.5 8.67-8.5 21.5 0 12.82 8.68 21.32 8.67 8.5 21.5 8.5 12.82 0 21.32-8.68ZM454-348h60v-224h-60v224Zm26-122Z\"/></svg>"};
  var ns = window.Cornerstone;
  if (!ns || typeof ns.registerIconLibrary !== "function") return;
  ns.registerIconLibrary("default", {
    resolver: function (name) {
      var svg = ICONS[name] || ICONS.help;
      return svg ? "data:image/svg+xml," + encodeURIComponent(svg) : "";
    },
    mutator: function (svg) {
      if (!svg.hasAttribute("fill")) svg.setAttribute("fill", "currentColor");
    }
  });
})();
